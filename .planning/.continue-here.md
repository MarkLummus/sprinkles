---
context: default
phase: 03.4 — The design layer in code (INSERTED 2026-09-21, not yet discussed or planned)
task: 10
total_tasks: 11
status: ready
last_updated: 2026-09-21T19:10:00Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-reply promises the wrong scope | On the canvas, comment auto-replies twice promised less than the rule required (one ingredients block instead of the whole Two-Ink split; a question back instead of looking at the four stacked rails) and once opined on button colour without the contrast numbers. | advisory | Read the thread before acting; apply the governing rule from DESIGN.md, not the auto-reply's wording; post a correcting follow-up (acknowledge_duplicate) before resolving. |
| Anchor-text restore stacks elements | Re-inserting rails by searching for the text that followed them in the original matched the first occurrence every time, so all of a board's rails landed on row one (12 boards). | advisory | When re-placing repeated elements, search from the previous insertion point forward, then compare relative positions against the original before publishing. |
| Publish refused after a page save | After Mark saves in the page, a publish is refused until this session reads the artifact's bare URL, not only the changed file. | advisory | Before every publish that follows a page save: read the file, apply the edit on that copy, then read the bare URL, then publish. |
| Removing a design element to "simplify" | Rails were removed on single-destination pages under the marker-only option; Mark did not like it and asked for them back. | advisory | A structural removal on the canvas is Mark's call; draw it as an alternative board first. |
| Over-applying a rule | The handwriting rule once reached ingredients and method; the batch log board set specification in pen blue. Both are specification, not observation or record. | advisory | Check every element against the Two-Ink Rule and the Hand Rule in DESIGN.md before publishing a Sheet board. |

<current_state>
No GSD phase is in flight. Phase 03.3.1.1 is the last completed phase; Phase 4 is next and has no directory or CONTEXT.md. Main is at `431068f`, pushed, tree clean. No code under `app/` changed this session.

The session's work was the design layer: the Design System artifact was built, four DESIGN.md amendments landed (Sheet/App names and the `sheet-` prefix; five tentative text companions; the hand; App marks and the filled action), and the design canvas "Sprinkles Home" was re-pointed from recipe hues to destination colours and brought onto the approved neutrals.

Handoff tasks 7 to 10 are done. Task 11 (the code) remains and is phase-sized.
</current_state>

<completed_work>

Completed Tasks:
- Task 7: Design System artifact at https://claude.ai/artifact/M7LkrAkNQYA897PpKnjjzU (version 10): 21 colours with `sheet-`/`app-` prefixes and status in every usage note, five tentative `*-text` companions, 14 type styles including `app-hand`, Sheet spacing and touch, README from DESIGN.md, cover; no components - Done
- Task 8: The hand decided (Caveat; observations plus ideas; sheet pen blue in the App; tokens ride Task 11) and written to DESIGN.md at `8bc3595` and the design system - Done
- Task 9: Canvas re-pointed to destination colours (29 boards, canvas version 142). Rails kept on every list in the row's destination colour after Mark rejected removal; tallies filled = version, hollow = batch; nav and recipe tabs by place, Home app blue; History nodes and rings by where each version lives; filled action A (companion blue, white label) with F (outline) as secondary on 18 boards; "What a mark means" rewritten; new board "The filled action — six alternatives"; Main and Phone (as shipped) untouched - Done
- Task 10: Greys to tokens: Sheet boards' rules and mutes to ink, app dividers to `app-divider`, ghosts, the disclosure box and one card fill to `app-surface-subtle`. Then on Mark's comments: batch log specification in ink, bare churn dates on the making sheet, bench action column at a fixed width - Done
- DESIGN.md amendments: `d4855c6` Sheet/App and the prefix, `68a450a` companions, `8bc3595` the hand, `20a8d13` App marks and the filled action (quick 260921-gss and two fast tasks) - Done
</completed_work>

<remaining_work>

- Task 11 (now Phase 03.4, context gathered; next: /gsd-plan-phase 03.4): tokens.css to the approved app palette; `--ground`/`--ink`/`--pen-blue`/`--bookcloth`/`--gap-*`/`--type-*` renamed with the `sheet-` prefix (contract suites read these by name); the five `app-*-text` companions; Caveat as a repo file with `@font-face` and `--face-hand`, `--size-hand`, `--leading-hand`, `--size-hand-min` plus the forced-colours and print fallback; then Home reworked per route.md "Active work first" using the App marks grammar in DESIGN.md
- Optional sweep: canvas app boards still carry `#5b6169` (token `#595959`) and `#1a1c1f` (token `#141414`)
- route.md still calls the shared action colour open; bring it level with DESIGN.md when the brief is next touched (Impeccable owns it)
- The design system's README tracks DESIGN.md `8bc3595`; the App marks amendment is not mirrored there yet
</remaining_work>

<decisions_made>

- Contexts are **Sheet** and **App**; every token is `sheet-` or `app-`; "paper" is not a context name (Mark, 2026-09-21)
- The hand is Caveat, observations plus ideas only, always `sheet-pen-blue` in the App, the one sanctioned Sheet token in the App; text-face italic fallback in forced-colours and print; tokens land with the tokens.css rework
- Five text companions (same hue to 4.5:1 on white), tentatively approved; Notebook and Idea log carry caveats with `app-text` as fallback
- App marks: rail in the row's destination colour on every list; tallies filled = version, hollow = batch in the row's colour; nav underline in destination colour, Home in app blue; recipe tabs in the place's colour; History nodes by where each version lives. Rail removal and ink History nodes were rejected
- Filled action: primary `app-blue-text` fill with white label (4.51:1); secondary the same blue as outline and label. App blue under white fails (3.90:1)
- On the Sheet, specification (ingredients, method, version, provenance) is ink; batch rows are pen blue; observations in the hand. Batch cells on the sheet log are the bare date
</decisions_made>

<blockers>
- None. One non-blocking human action: confirm the tentative companions and filled action in DESIGN.md. The rail question is settled (every list; DESIGN.md 5c97348).
</blockers>

## Required Reading (in order)
1. `DESIGN.md` at `20a8d13` — Colors (companions, filled action, unresolved), Typography (Hand role, Hand Rule), Components › App marks
2. `.planning/design-md-hand-draft.md` — applied; keep for the rationale of the hand's boundaries
3. `.impeccable/surfaces/route.md` — Home direction "Active work first"; action colour still listed open there
4. Memory: `two-contexts-sheet-and-app-prefixes`, `the-hand-is-caveat-in-sheet-pen-blue`, `app-mark-grammar-destination-colours`
5. `app/src/styles/tokens.css` — the unprefixed Sheet tokens and the 18 stale jar/recipe values Task 11 replaces

## Critical Anti-Patterns (do NOT repeat these)
- AUTO-REPLY SCOPE: read the thread, apply the DESIGN.md rule, correct the auto-reply in a follow-up → never resolve on the auto-reply's word
- ANCHOR RESTORE: sequential placement from the last insertion point, then compare positions to the original before publishing
- PUBLISH AFTER PAGE SAVE: read file, edit that copy, read bare URL, publish

## Infrastructure State
- No dev server or watcher running. No async jobs.
- Canvas https://claude.ai/artifact/JHwDoAYHDf9yQ1CcUZyATq at version 142, private. Local working copies of every board are in this session's scratchpad and will be gone; re-read from the artifact before editing.
- Design system https://claude.ai/artifact/M7LkrAkNQYA897PpKnjjzU at version 10, private.
- Main at `431068f`, level with origin; tree clean.

<context>
Mark drove today from the canvas: comments arrive as threads sent to Claude, auto-replies fire first, and the real work is to read the thread, apply the rule, and correct the reply. The design layer is now coherent across DESIGN.md, the design system and the canvas, except for the rail question and the two near-token greys. Nothing in code reflects any of it; Task 11 is where it lands, and it should be planned as a GSD phase (or folded into Phase 4's planning), not run as a quick task.
</context>

<next_action>
Phase 03.4 context is gathered (03.4-CONTEXT.md, 20 decisions); run `/gsd-plan-phase 03.4`.
</next_action>
