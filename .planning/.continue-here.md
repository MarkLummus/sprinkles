---
context: default
phase: none — design exploration and document amendments between phases
task: 5
total_tasks: 11
status: in_progress
last_updated: 2026-09-21T14:28:14Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-reply contradicts the commenter | Artifact comment auto-replies twice stated the opposite of Mark's request (said it would remove handwriting he asked to add) and once invented a claim about the Design runtime. | advisory | Read the thread before acting; when the standing auto-reply is wrong, say so explicitly in the follow-up reply before describing the change. |
| Over-applying a rule | The handwriting rule was applied to ingredients and method, which are specification, not observation. "Your library" was used as a heading although it is not in the D17 vocabulary and collides with "Ingredient library". | advisory | Check every on-screen label against `product-requirements/05-domain-and-language.md` before publishing; state a rule's boundary on the board that carries it. |
| Grey on paper | The two paper-world boards used a grey mute and grey rules; DESIGN.md's paper rules allow no divider grey and require ink at hairline weight. | advisory | Paper boards read only ground, ink, pen blue; see remaining task 10. |
| Stale canvas index | Mark edits the canvas live between turns (moves boards, deletes notes, adds his own artboards). Publishing an index read minutes earlier would undo his edits. | advisory | Re-read `project/canvas.json` immediately before every publish that sends the index; coordinates are per page, so a moved board must be re-laid out. |

<current_state>
No GSD phase is in flight. The last completed phase is 03.3.1.1; two quick tasks and one quick batch landed this session and are pushed (main was 6 ahead of origin at last push; it is 9 ahead now including Mark's vocabulary commit `01cd24e`).

The live work is the design canvas "Sprinkles Home" at https://claude.ai/artifact/JHwDoAYHDf9yQ1CcUZyATq (private, Mark's): 8 pages, 34 artboards, built with values hand-copied from `app/src/styles/tokens.css`, not mounted from a design system. No Design System artifact exists yet on the account.

Mark has amended four documents and NOT committed them: `DESIGN.md`, `PRODUCT.md`, `.impeccable/surfaces/route.md`, `.impeccable/design.json`. Together they approve an app palette (Option B: six destination accents, an app blue distinct from pen blue, five neutrals), retire recipe-specific identity colours, and redirect Home to "Active work first". These reverse the colour grammar of most canvas boards and of the shipped home.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Quick 260918-jki, owning-history contract (History uncounted, Batches (n), `Batch · date` identity, `From batch · date` citation) - Done, `94948f3`, pushed
- Task 2: Quick batch 260918-mpx (child Written term, ordered Batches list, tasting note prose size) - Done, `cd6b07a`, pushed
- Task 3: Design canvas — Home as shipped, five alternatives answering the Impeccable critiques and D17, then pages for Notebook (3), Recipe book (3 lists + making sheet), Idea log (2 lists + 3 entry forms), Ingredients (5), Kitchen (2), Recipe (developing sheet + making sheet), Recipe History (timeline + first-version origins) - Done, on the canvas
- Task 4: DESIGN.md hand-rule draft - Done, `.planning/design-md-hand-draft.md` (copied out of the session scratchpad)
- Task 5: Mark's document amendments - In progress, written but uncommitted
</completed_work>

<remaining_work>

- Task 6: Commit the four amended documents (Mark's call)
- Task 7: Build the Design System artifact from DESIGN.md frontmatter — 16 colours in one light theme with status in usage notes (paper implemented, app approved-not-implemented, contrast on white for each accent), twelve paper type roles, paper spacing and touch minimum, no radius tokens (list the home's three under "in the code, not approved"), README from DESIGN.md's rules and unresolved list; no components, no hand
- Task 8: Decide the hand — review the draft, then DESIGN.md edit plus, via GSD, a self-hosted face and `--face-hand` tokens
- Task 9: Re-point canvas boards from recipe hues to destination colours; this is a redesign of the row (rails meant a recipe, markers must mean a destination), and "What a mark means" is now wrong
- Task 10: Fix five non-token colours: grey mute and rules on the two paper boards to ink hairline; connector and ghost greys to the home rule token; the disclosure box fill is a real decision (subtle surface is now approved)
- Task 11: GSD work — tokens.css to the approved palette (18 stale jar/recipe values, 12 approved values missing); Home reworked per route.md
</remaining_work>

<decisions_made>

- History is recipe-level with no count; Batches (n) counts attempts at the version in view; `Batch · [churn date]` is the shared link identity; `From batch · [date]` is the sole citation — settled 2026-09-18, implemented in 260918-jki
- Observation prose (tasting, Next time, Why, a batch note, an idea) is handwritten in pen blue; specification (ingredients, method, dates) and anything the app writes is set in type — Mark on the canvas 2026-09-19/20; draft for DESIGN.md pending review
- The making Recipe Sheet prints only the recipe; batches and history sit in an app band above it; one ruled line at the sheet foot to write a verdict — Mark's note and comments
- On the sheet's log table the batch cell is the bare date; on the History timeline the node keeps `Batch · date` because nothing else there says record type
- The filled action follows the standing: Not yet churned → Record a batch; Awaiting tasting → Record a tasting; Tasted → Next version; Ready to make → Record a batch; Saved idea → Develop in Notebook
- App palette Option B approved 2026-09-21; recipe-specific identity colours retired; colour identifies destinations, words identify recipes — Mark, in the uncommitted documents
- Home direction is "Active work first" (route.md, 2026-09-20); the shipped recipe-row list, import-first action and sprinkle-drop moment are no longer requirements
</decisions_made>

<blockers>
- None technical. Task 7 waits on Task 6 so the design system can cite a commit.
</blockers>

## Required Reading (in order)
1. `git diff DESIGN.md .impeccable/surfaces/route.md PRODUCT.md` — Mark's palette and Home amendments; the frontmatter `app-*` values are the approved app palette
2. `product-requirements/05-domain-and-language.md`, the D17 table — every on-screen label must come from it
3. `.impeccable/surfaces/route-recipe-version.md` § owning history (around line 140) — the contract the code and the canvas both follow
4. `.planning/design-md-hand-draft.md` — the pending hand rule
5. The canvas index: Artifact read `project/canvas.json` on https://claude.ai/artifact/JHwDoAYHDf9yQ1CcUZyATq — re-read before any publish
6. `.planning/STATE.md` — quick task rows 260918-jki and 260918-mpx
7. `.planning/METHODOLOGY.md` (if it exists)

## Critical Anti-Patterns (do NOT repeat these)
- AUTO-REPLY DRIFT: the comment auto-reply can promise the opposite of the request → read the thread, act on Mark's words, correct the auto-reply in your own reply
- RULE OVERREACH: a rule stated as "Mark's words" swallowed specification → the boundary is observation vs specification; state it on the board
- GREY ON PAPER: paper boards borrowed app greys → paper reads ground, ink, pen blue only
- STALE INDEX: publishing an old canvas.json undoes Mark's live edits → read it again right before the publish that carries it

## Infrastructure State
- Canvas: https://claude.ai/artifact/JHwDoAYHDf9yQ1CcUZyATq, private; this session's watch and comment auto-replies do not survive the session
- Session scratchpad (`/private/tmp/claude-501/.../scratchpad/jar/project/*.dc.html`) held local copies of every board; it will be gone — read boards from the artifact
- No dev server, no worktrees, no background jobs
- Repo: main at `01cd24e`, 9 commits ahead of origin, 7 uncommitted paths (4 modified documents, 2 untracked critique files, plus the handoff files this commit adds)

## Pre-Execution Critique Required
- Not applicable; nothing is paused between design and execution.

<context>
The canvas grew by comment: Mark left a note or a comment, I built or corrected a board, replied and resolved. The pattern to keep is: read what he said, build the smallest board that takes a position, say plainly where it departs from a document.

The big turn happened at the end. Until 2026-09-21 every board argued that colour is the recipe (rails, tallies, the mark contract) and that workspace is a word. Mark's palette approval says the opposite: colour is the destination, the recipe is a word. The documents now lead and the canvas lags. Do not "fix" the canvas by recolouring; the row grammar has to change.

The Design System artifact is ready to build and is the natural next step because it turns the approved frontmatter into something the canvas can mount and other agents can read. Build it from the documents as committed, record status honestly (approved vs implemented), and leave components and the hand for a second pass.
</context>

<next_action>
Start with: ask Mark to commit the four amended documents (or confirm he wants them committed), then create the Design System artifact from the Design System type (https://claude.ai/artifact/5M7UeXXcx16TP3vzVFNDzd) with `project/README.md`, `project/tokens.json` and `project/design-system.json` built from `DESIGN.md` frontmatter and `app/src/styles/tokens.css`.
</next_action>
