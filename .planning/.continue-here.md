---
context: phase
phase: 03.5-separate-the-recipe-from-the-sheet
task: width-constraints study (gap work between plan 08 and plan 09)
total_tasks: 3
status: paused
last_updated: 2026-09-26T13:33:57.610Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: no arbitrary breakpoints — Mark rejected picking a cut for the 1025–1365 band (options "A: 1024 layout below 1366" and "C: log below, 2-col Sheet" were both refused, 2026-09-25). Breakpoints must be DERIVED: measure content limits first, Mark sets values and precedence, then breakpoints fall out of sums. Do not propose a breakpoint before Mark has answered the precedence questions.
- [ ] CONSTRAINT: the sketch is the single authority — a layout decision is drawn into sketch 011 on the Sprinkles canvas and snapshotted before any app/ plan. No app/ edit outside a GSD command.
- [ ] CONSTRAINT: measure, never reason from CSS — every width claim comes from a real render (headless Chrome), with screenshots read by eye.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Local render ≠ canvas | A `<colgroup>` looked right locally but the canvas runtime dropped it; Mark saw the amount column at a third of the table. | blocking | Put board layout rules in the board's own `<helmet><style>` (via `extra_css` / `TABLE_CSS` in gen.py), never in markup the runtime may strip; ask Mark to confirm on the canvas itself. |
| Claiming a tool is gone without looking | I told Mark the board generator was lost; it is committed at `.planning/canvas-generators/`. | advisory | `find` before asserting absence. |
| Attributing orchestrator wording to Mark | "Whole milk 3.5% kept apart per Mark" was my prompt wording; it entered the review as his decision (since corrected). | advisory | Only quote Mark's own words as his decisions in planner prompts and docs. |

<current_state>
Phase 03.5: plans 01–07 complete; 03.5-08 (layout C conformance record at 1600/1366/1024/393) committed and "Open for Mark"; 03.5-09 (wire transcribed seed into the store, D-07 approval gate) not executed. All work is committed and pushed (origin/main = 812487a before this handoff commit).

Paused at the start of a width-constraints study that will settle the recipe route's responsive ladder between the drawn rungs.
</current_state>

<completed_work>
This session (2026-09-25/26), all pushed:
- DESIGN.md Hand Rule bent for Sheet batch edits (D-19); design.json authored sample → Before you start (bf8533c).
- Seed: Mexican Chocolate v2 added, chain v1→v2→v3→v4, v3 reason = v2→v3 change (quick 260925-kix + fast).
- Seed: Standard Base v1/v2, Underbelly Light Base v1/v2, Strawberry V1/V2/V2.1, Mocha v0–v3; Pineapple/Coconut real dates (quick 260925-lpd).
- Seed: Mark's answers applied — tastings only from page marks, per-version strawberry data, whole milk 3.5% merged, Underbelly v1 batch removed, Strawberry V2 base notes → batch note, Mocha v1 cites v0's batch (quick 260925-re3).
- App: batch notes each on their own line, 18px/4px (quick 260925-u3r, c4369e9).
- Sketch 011: ingredient table amount + name as two columns under one "Ingredient" head at every width, sized to content; 393 = plan amount | name | share on line one, as made under the plan amount on one right edge. Approved by Mark on the canvas (version 238), snapshotted, README decision 15 (812487a).
</completed_work>

<remaining_work>
1. Width-constraints study (next): from sketch 011's boards, measure
   - Sheet in 1-column mode: min width (table, Instructions, batch line read without collisions) and max (where it stops gaining);
   - Sheet in 2-column mode (Balance column beside): min and max;
   - log: min width;
   - side nav is fixed 224px and shows only from 1024 up (Mark, 2026-09-25).
   Deliver a widths table with screenshots at each limit, plus the precedence question: Mark has said Sheet 2-col takes precedence over Sheet 1-col; ask how the log ranks (does it drop below the Sheet before or after the Sheet drops to 1 column?).
2. Mark sets values + precedence → derive breakpoints as sums (nav + Sheet + log + gutters) → record in DESIGN.md (/impeccable document) and sketch 011 → GSD gap-closure plan in 03.5 building the table redesign, the side-nav cutoff and the derived ladder; re-run the 03.5-08 conformance at 760 and 1100 too.
3. Then 03.5-09 (D-07 gate: wire the transcribed seed into app/src/store/seed.js — Mark approves).
</remaining_work>

<decisions_made>
- Side nav only from 1024 up (Mark, 2026-09-25).
- Ingredient table: amount + name two columns under one head, all widths; content-sized columns (Mark approved 2026-09-26).
- 1025–1365 band: no picked cut; derive from measured constraints + Mark's precedence (Mark, 2026-09-25).
- Sheet 2-col mode takes precedence over Sheet 1-col mode (Mark, 2026-09-25).
- Seed recipes are Notebook records only; no Recipe Book selection exists (D-11) — Mark chooses per recipe when selection is built.
- No adaptation links unless Mark names the base (memory: no-adaptation-links-unless-mark-names-the-base).
</decisions_made>

<blockers>
- None technical. Human decisions pending (below).
</blockers>

## Human actions pending (Mark)
- 03.5-SEED-REVIEW.md open questions: Q9 real dates for 7 ordering-placeholder versions (Standard Base v1/v2, Underbelly v1, Strawberry V2, Mocha v0/v1/v2); Q13 almond extract coefficients; Q14 judgement readings (Strawberry V1 sweetness 2 @ −18 °C, Mocha v0 smoothness 5, Mocha v2 struck guar as 0 g); Q16 remaining no-cited-batch call.
- 03.5-08 conformance record is "Open for Mark".
- .claude/CLAUDE.md Architecture says rows embed "a structuredClone of the shared library entry"; Strawberry V1/V2 embed their own .ier definitions now — offer a wording fix (via /gsd-quick).

## Required Reading (in order)
1. `.planning/sketches/011-recipe-route-c/README.md` — boards, ladder, decisions 1–15.
2. `.planning/canvas-generators/gen.py` — how boards are built (`sheet_for`, `TABLE_CSS`, `layout_c_rung`, `PHONE_TABLE`).
3. `.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-SUMMARY.md` — the measured 1100 defect (Sheet ingredient region 235px, name column 47px at 1100, 113 at 1200, 180 at 1300, 224 at 1366) and why A/C were rejected.
4. `.planning/phases/03.5-separate-the-recipe-from-the-sheet/03.5-08-*` conformance record — geometry at the drawn rungs.
5. `DESIGN.md` responsive ladder section (stale re: "the spread collapses below 1100").

## Infrastructure State
- Dev servers: none running (Mark had two on :5173; both stopped). Only `vite preview --host` on :4173 runs (device UAT build; leave it alone). Project rule: at most one Vite process.
- Headless Chrome: playwright-core at `~/.npm/_npx/e41f203b7505f1fb/node_modules/playwright-core/index.mjs`, launch with `executablePath: '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'` (bundled Chromium 1243 missing). Example probe: `.planning/quick/260925-u3r-*/260925-u3r-probe.mjs`.
- npm/node: prepend `/Users/mark/.nvm/versions/node/v24.21.0/bin` to PATH.
- Sprinkles canvas: https://claude.ai/artifact/JHwDoAYHDf9yQ1CcUZyATq (layout C boards R35C_*). Generator workflow: copy `.planning/canvas-generators/*` to the session scratchpad, repoint `SP` in gen.py, `Artifact read` the live `project/AsBuiltRecipePage.dc.html`, `canvas.json`, `RecipeBookSheet.dc.html` and the R35C boards into `SP/artifact-files/8c08ac14-3ead-48f4-861a-5016f88c8338/project/`, run gen.py then longhist.py, confirm unchanged boards regenerate byte-identical, publish only changed chosen boards (never canvas.json unless layout changes; not A/B/PenFocus/PenStep). Local render: replace the `/_blob/` stylesheet link with the `<style>` block from a committed sketch 011 file and drop `support.js`. Sketch snapshot recipe: that same transform (font url points at `../../../app/public/fonts/caveat-regular.woff2`).

<context>
Mark's method: constraints first, then precedence, then checkpoints — he called the earlier options "arbitrary checkpoints". Bring him numbers and questions, not a layout proposal. The table redesign changed the Sheet's minimum, so measure with the new two-column table (sketch 011 as committed at 812487a).
</context>

<next_action>
Start with: render sketch 011's Sheet (1600-batch.html) and log in isolation at a sweep of container widths with headless Chrome, find the min/max limits listed in remaining_work item 1, and present the widths table + screenshots + the log-precedence question to Mark.
</next_action>
