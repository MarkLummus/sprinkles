---
phase: 03.1-the-imprint-and-the-binder
plan: 02
subsystem: ui
tags: [react, jsx, css-tokens, focus-management, keyboard, region-decomposition]

# Dependency graph
requires:
  - phase: 03.1-the-imprint-and-the-binder
    provides: Versions.jsx and PenFoot.jsx (the tracer), the openers/plan-pen ceremony shell, the recipe-band grid, plain-language running heads (plan 01)
provides:
  - Versions.jsx complete for reading state (D-04): openers, version list, labelled lineage (Parent/Batch/Reason/Show changes), batch list — and all four pens' ceremonies (plan, record, amend, tasting) in place of the openers while one is open
  - BatchMargin.jsx shrunk to the record's content alone — no opener, no save, no cancel, no button of any kind
  - PenFoot.jsx extended to all four pens, sharing one save-gate derivation (penSaveDisabled/penHint) with Versions so the two screen positions can never disagree
  - The keyboard contract: one document keydown listener for Escape, gated on each pen's own existing dirty check; a fork's save carries a router-state focus signal to the child's Develop control
affects: [03.1-03, 03.1-04, 03.1-05]

# Actuals (#2632)
actuals:
  tokens: 23133
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A save gate computed once at the page level (penSaveDisabled/penHint, beside canSaveOver) and passed to both the ceremony and the foot — neither component recomputes isTastingSaveable or blockedMessage itself (RESEARCH.md Pattern 2/Anti-Patterns, extended from plan 01's canSaveOver precedent)."
    - "One focus-return ref pair per opener (developButtonRef/wasDevelopingRef and its three siblings for record/amend/tasting), each keyed on the transition from open to closed via useEffect — Escape, Cancel and Save all close through the same handler, so all three return focus through the same effect with no separate Escape focus path."
    - "A pen's own existing dirty check (isPenDraftDirty/isDraftDirty/isTastingDraftDirty) is the single source Escape reads too — no fourth notion of dirtiness is introduced for the keyboard contract."
    - "A cross-page focus signal (the fork's landing on the child's Develop control) rides router navigation state, not a ref, since the ref cannot survive the route-keyed remount (router.jsx's `${id}::${batchId}` key) — read once in RecipePage via useLocation and passed down as a plain boolean prop, keeping the child component prop-driven and testable without a router state."

key-files:
  created: []
  modified:
    - app/src/ui/Versions.jsx
    - app/src/ui/Versions.test.jsx
    - app/src/ui/VersionStrip.jsx
    - app/src/ui/VersionStrip.test.jsx
    - app/src/ui/BatchMargin.jsx
    - app/src/ui/BatchMargin.test.jsx
    - app/src/ui/Headnote.jsx
    - app/src/ui/Headnote.test.jsx
    - app/src/ui/PenFoot.jsx
    - app/src/ui/PenFoot.test.jsx
    - app/src/ui/RecipePage.jsx
    - app/src/ui/RecipePage.test.jsx
    - app/src/styles/app.css

key-decisions:
  - "The tasting's own date field stays in BatchMargin's TastingForm (per the plan's literal deletion list, which named only the shortcut, Save tasting and Cancel buttons and the hint paragraph) while the ceremony in Versions gains its own separate date field with autoFocus — a genuine two-field duplication for that one value, both bound to the same tastingDraft.date/onChangeTastingField pair so they can never disagree. Flagging this for the re-critique: unlike the churn date (already migrated out of the margin in an earlier phase, so no duplication exists there), the tasting date now appears twice on the page."
  - "Removed autoFocus from BatchMargin's own tasting-date field (Rule 1): two autofocus attributes live in the same render once the ceremony gained its own date field, which is undefined browser behaviour. The ceremony's field is the one that now carries autoFocus; the margin's own field keeps D-25's 'outline on focus only' treatment, matching every other content field on the page."
  - "penHint supersedes Versions' plan-pen ceremony's own blockedMessage paragraph and PenFoot's blockedMessage prop entirely — both now read the one page-level derivation. The now-dead blockedMessage prop RecipePage was still threading to Versions (unused after the penHint refactor) was dropped in task 3 as a Rule 1 cleanup."
  - "The record/amend ceremony's Save button and PenFoot's plan-pen Save/Save-as carry no disabled attribute (matching D-21's blocked-but-enabled rule for the plan pen, and the batch pen's own save, which was never gated); only the tasting Save (in both the ceremony and the foot) reads penSaveDisabled, per the plan's own scoping of that derivation to `openPen === 'tasting'`."
  - "Test-file consolidation (moving BatchMargin/VersionStrip/Headnote/RecipePage's opener and ceremony coverage into Versions.test.jsx, extending PenFoot.test.jsx for the new branches) was done incrementally across all three tasks, not deferred wholesale to task 3 — each task's own code change would otherwise have broken the existing suite mid-plan, and the workflow's acceptance criteria require a green suite after every task. Task 3's own text assigns this consolidation to itself; in practice tasks 1 and 2 already carried most of it, and task 3 completed the keyboard-contract-specific additions (autofocus assertion, ceremony pair ordering) on top."

requirements-completed: []

coverage:
  - id: D1
    description: "Versions reads top to bottom in D-04's order: openers, version list, the current version's lineage as labelled Parent/Batch/Reason lines plus Show changes, and the batch list; the batch list is always a list (one entry even with none, reading 'no batch yet'); the churned date has left the recipe block entirely."
    verification:
      - kind: unit
        ref: "app/src/ui/Versions.test.jsx#Versions — the lineage, as labelled lines (D-08)"
        status: pass
      - kind: unit
        ref: "app/src/ui/Versions.test.jsx#Versions — the batch list, always a list (D-09)"
        status: pass
      - kind: unit
        ref: "app/src/ui/Versions.test.jsx#Versions — the version list, moved inside (D-07)"
        status: pass
      - kind: unit
        ref: "app/src/ui/Headnote.test.jsx#Headnote — the recipe block alone (D-02, D-03)"
        status: pass
      - kind: unit
        ref: "app/src/ui/VersionStrip.test.jsx"
        status: pass
    human_judgment: false
  - id: D2
    description: "Each of the four pens' ceremonies (plan, record/amend, tasting) renders in Versions in place of the openers while that pen is open, with Cancel before Save in every pair; the record's and tasting's own openers (Record batch/Record another, Amend, Add tasting) follow the same batches/openBatch conditions the margin used; the margin (BatchMargin.jsx) holds the record's content only, with no button of any kind."
    verification:
      - kind: unit
        ref: "app/src/ui/Versions.test.jsx#Versions — the record and amend ceremony (D-05, D-10)"
        status: pass
      - kind: unit
        ref: "app/src/ui/Versions.test.jsx#Versions — the tasting ceremony (D-05, D-10)"
        status: pass
      - kind: unit
        ref: "app/src/ui/Versions.test.jsx#Versions — the openers, present only with no pen open (D-05)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchMargin.test.jsx#BatchMargin — no opener of any kind renders here any more (D-04)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The foot band repeats whichever pair the open ceremony shows, with the identical handler references and the shared save-gate sentence (penHint) beside it; the tasting Save is disabled in both the ceremony and the foot from the same penSaveDisabled value, so the two screen positions can never disagree."
    verification:
      - kind: unit
        ref: "app/src/ui/PenFoot.test.jsx#PenFoot — the tasting branch, disabled Save with its hint (D-05, D-10, T-03.1-08)"
        status: pass
      - kind: unit
        ref: "app/src/ui/PenFoot.test.jsx#PenFoot — the record and amend branch (D-05, D-10)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Escape closes only an untouched pen (the pen's own existing dirty check reports no ink) and returns focus to its opener; with ink typed, Escape does nothing and Cancel is the one exit."
    verification: []
    human_judgment: true
    rationale: "renderToStaticMarkup executes no useEffect and dispatches no keydown event (RESEARCH.md Pitfall 4); the listener registration, the teardown, and the three dirty-check call sites are asserted statically (grep-verified in this plan's own verification block), but the actual key-press-to-cancel-call and the resulting focus transition are UAT-only, per human_verify_mode: end-of-phase."
  - id: D5
    description: "Closing any pen by save or cancel returns focus to the control that opened it; after a fork saves and the page lands on the child's URL, the child's Develop control receives focus on mount."
    verification: []
    human_judgment: true
    rationale: "Every focus-return effect and the autoFocus wiring for the fork's landing are structurally present and statically asserted (ref pairs, the focusDevelopOnMount prop, the autofocus=\"\" attribute on mount), but the actual focus() call and DOM focus state are not observable under renderToStaticMarkup (RESEARCH.md Pitfall 4) — deferred to end-of-phase UAT."

# Metrics
duration: ~27min
completed: 2026-09-09
status: complete
---

# Phase 03.1 Plan 02: Versions completed — the version list, lineage and batch list, all four pens' ceremonies, and the keyboard contract Summary

**Versions.jsx now carries the whole front-matter band described by D-04 (openers, version list, labelled lineage, batch list, and every pen's own ceremony), BatchMargin.jsx renders no control of any kind, PenFoot repeats the right pair for all four pens from one shared save-gate derivation, and Escape/focus-return close the loop the critique's first P1 opened.**

## Performance

- **Duration:** ~27 min
- **Completed:** 2026-09-09T02:41:14Z
- **Tasks:** 3
- **Files modified:** 13

## Accomplishments

- `Versions.jsx`: the version list (VersionStrip, now headless and always a list), the labelled lineage (`Parent`/`Batch`/`Reason` lines plus `Show changes`, absent on a root version), and the always-a-list batch list (`churned <date>` links, the open batch as plain text, `amended <date>` on its own line, `no batch yet` with zero batches) — all rendered beneath the openers/ceremony, top to bottom per D-04
- The record/amend ceremony (churned-date field, Cancel/Save) and the tasting ceremony (tasting-date field, the `As expected, nothing to note` shortcut, Cancel/Save) now live in Versions, replacing the openers exactly as the plan pen's ceremony already did; the second opener group fills in with `Record batch`/`Record another`, `Amend`, `Add tasting` following the same `batches`/`openBatch` conditions the margin used
- `BatchMargin.jsx` shrunk to the record's content alone: the churn section's fields (recording) or measured rows (reading), each tasting's fields, and the `recorded … against …` line — zero buttons, matching the critique's P1 (the printed spread carries in-place ink and never a button)
- `PenFoot.jsx` extended to all four pens with one shared `penSaveDisabled`/`penHint` derivation (computed once in `RecipePage`, beside `canSaveOver`) so the ceremony and the foot's repeated pair can never disagree on whether Save is blocked or why
- `RecipePage.jsx`: one `document` keydown listener for Escape, gated on `openPen` and each pen's own existing dirty check (`isPenDraftDirty`/`isDraftDirty`/`isTastingDraftDirty`) — no fourth dirtiness notion; `handleSaveAsNewVersion` carries a `{ state: { focusDevelop: true } }` navigation so the child's Develop control receives `autoFocus` on mount
- `Headnote.jsx`'s version-line paragraph reduced to the bare version label — the churned date's recording-state field left the recipe block for good (D-03 fully closed across plans 01 and 02)

## Task Commits

Each task was committed atomically:

1. **Task 1: The version list, the labelled lineage and the batch list move into Versions; the churned date leaves the recipe block** - `c654d28` (feat)
2. **Task 2: The record's and tasting's ceremonies move into Versions, the margin loses its last control, and the foot band covers all four pens** - `4173dfc` (feat)
3. **Task 3: The keyboard contract — focus returns to the opener, Escape closes only an untouched pen, and the fork lands on the child's Develop** - `ab16692` (feat)

_No plan-metadata commit yet — this SUMMARY and STATE.md updates land in the closing commit._

## Files Created/Modified

- `app/src/ui/Versions.jsx` - Complete for reading state; all four pens' ceremonies; the keyboard contract's focus-return refs and focusDevelopOnMount
- `app/src/ui/Versions.test.jsx` - Lineage, batch list, version list, all-four-ceremonies, and autofocus coverage
- `app/src/ui/VersionStrip.jsx` - Headless, no early return, no per-control pen-hint — nested inside Versions
- `app/src/ui/VersionStrip.test.jsx` - Updated for the removed head and the removed one-version early return
- `app/src/ui/BatchMargin.jsx` - Shrunk to the record's content alone; no opener, no save, no cancel; the tasting date field's autoFocus removed
- `app/src/ui/BatchMargin.test.jsx` - Updated for the narrowed props and the zero-button invariant
- `app/src/ui/Headnote.jsx` - Version-line paragraph reduced to the bare label (D-03)
- `app/src/ui/Headnote.test.jsx` - Updated for the narrowed props, no churn-date cases
- `app/src/ui/PenFoot.jsx` - Extended to record/amend/tasting branches sharing penSaveDisabled/penHint
- `app/src/ui/PenFoot.test.jsx` - Extended for the new branches and the disabled tasting Save
- `app/src/ui/RecipePage.jsx` - penSaveDisabled/penHint derivation; the Escape keydown effect; the fork's focus-signal navigation; narrowed Versions/BatchMargin/PenFoot call sites
- `app/src/ui/RecipePage.test.jsx` - Four-pen matrix narrowed to Develop-absence only (openers' own coverage moved to Versions.test.jsx)
- `app/src/styles/app.css` - `.versions__lineage`/`.versions__lineage-label`, `.versions__batch-amended`, `.versions__ceremony-field`/`.versions__ceremony-shortcut`; the retired `strip` grid area; dead `.headnote__lineage`/`.headnote__reason` rules removed

## Decisions Made

See `key-decisions` in the frontmatter above for full rationale. In brief: the tasting date field now exists in two places (the ceremony and the margin's own printed content), both bound to the same draft/handler pair, flagged for the re-critique; `autoFocus` was removed from the margin's own tasting-date field to avoid an undefined double-autofocus once the ceremony gained its own; `penHint` fully supersedes the old `blockedMessage` prop path in both Versions and PenFoot; only the tasting Save reads `penSaveDisabled`, matching the plan's own scoping of that derivation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed `autoFocus` from BatchMargin's own tasting-date field**
- **Found during:** Task 2 (moving the tasting ceremony's own date field into Versions)
- **Issue:** The plan's task 2 text adds a second, separate tasting-date field to the ceremony with `autoFocus`, but its explicit deletion list for `BatchMargin.jsx` never mentions removing the pre-existing `autoFocus` on the margin's own field — leaving both fields' `autoFocus` set simultaneously, which is undefined behaviour across browsers.
- **Fix:** Removed `autoFocus` from `BatchMargin.jsx`'s `TastingForm` date field only; the ceremony's own field keeps it, matching D-25's discipline that content fields carry "outline on focus only," not autofocus.
- **Files modified:** `app/src/ui/BatchMargin.jsx`, `app/src/ui/BatchMargin.test.jsx` (assertion inverted to confirm no autofocus)
- **Verification:** `BatchMargin.test.jsx` asserts the date input carries no `autofocus` attribute; full suite green.
- **Committed in:** `4173dfc` (Task 2 commit)

**2. [Rule 1 - Bug/dead code] Dropped Versions' now-unused `blockedMessage` prop**
- **Found during:** Task 3, reviewing the file after the `penHint` refactor landed in task 2
- **Issue:** Task 2's `penHint` derivation fully replaced the plan-pen ceremony's `blockedMessage` render, but `RecipePage.jsx` was still threading a `blockedMessage` prop to `Versions` that nothing in the component read any more.
- **Fix:** Removed the prop from both the `Versions` call site in `RecipePage.jsx` and `Versions`' own destructured signature.
- **Files modified:** `app/src/ui/RecipePage.jsx`, `app/src/ui/Versions.jsx`
- **Verification:** Build and full suite green after the removal.
- **Committed in:** `ab16692` (Task 3 commit)

**3. [Rule 1 - Test correctness] Test-file updates (VersionStrip, BatchMargin, Headnote, PenFoot, RecipePage) made incrementally in tasks 1 and 2, not deferred to task 3**
- **Found during:** Tasks 1 and 2, running each task's own `npm --prefix app test` verification
- **Issue:** The plan's task 3 text assigns "Update BatchMargin.test.jsx... Update VersionStrip.test.jsx... Headnote.test.jsx and RecipePage.test.jsx for the narrowed props" as its own action — but tasks 1 and 2's own code changes (removing openers, the batch list, the churn-date slot, and every save button from the components those tests exercise) broke the pre-existing assertions immediately, and the workflow's acceptance-criteria gate requires a green suite before starting the next task.
- **Fix:** Updated each affected test file's assertions in the same task commit as the code change that broke it (moving opener/ceremony coverage into `Versions.test.jsx` rather than deleting it, per the plan's own instruction), so by task 3 only the keyboard-contract-specific additions (autofocus assertion, ceremony ordering) remained.
- **Files modified:** `app/src/ui/VersionStrip.test.jsx`, `app/src/ui/BatchMargin.test.jsx`, `app/src/ui/Headnote.test.jsx` (task 1); `app/src/ui/PenFoot.test.jsx`, `app/src/ui/RecipePage.test.jsx`, `app/src/ui/Versions.test.jsx` (task 2)
- **Verification:** Full suite green (532/532, then 535/535 after task 3's additions) at every task boundary.
- **Committed in:** `c654d28` (Task 1), `4173dfc` (Task 2)

---

**Total deviations:** 3 auto-fixed (2 Rule 1 bugs/cleanup, 1 Rule 1 test-correctness). **Impact:** All three keep the built behavior correct, crash-free, and test-verified at every task boundary; none add scope beyond what the plan specified. No architectural changes, no new dependencies.

## Issues Encountered

None beyond the deviations above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Build green (`npm --prefix app run build`), full suite green (535/535, up from the 528/28 baseline this plan started from — still 28 test files, no new file), dependency set unchanged, repository seam untouched, no `dangerouslySetInnerHTML`.
- Ready for plan 03 (per `03.1-PATTERNS.md`'s own note: "plan 03 re-points four [selectors] by name" — `.version-strip__item.is-current`, `.ingredient-table tbody tr.is-marked`, `.batch-margin__list li.is-open`, `.headnote__show-changes[aria-pressed='true']` — all four kept untouched by this plan, as instructed).
- Carried to the re-critique: the tasting date field now renders twice on the page (the ceremony's own field and the margin's printed content), both bound to the same draft value; this is what the plan's literal deletion list produced and is flagged rather than silently resolved.
- Human-observable checks (Escape on an untouched pen closing it and returning focus; Escape on a pen with ink doing nothing; Cancel/Save focus-return; the fork's landing focus on the child's Develop control) are deferred to the end-of-phase UAT per `human_verify_mode: end-of-phase`, as the plan's own `<verification>` block specifies.

---
*Phase: 03.1-the-imprint-and-the-binder*
*Completed: 2026-09-09*

## Self-Check: PASSED

All 13 created/modified source files plus this SUMMARY confirmed present on disk; all three task commits (`c654d28`, `4173dfc`, `ab16692`) confirmed in git history; full test suite re-run green (535/535, 28 files) and `npm --prefix app run build` re-run green after the file self-check.
