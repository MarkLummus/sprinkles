---
phase: 03.1-the-imprint-and-the-binder
plan: 05
subsystem: ui
tags: [graduated-rules, grams-validation, column-arithmetic, two-ink, tokens, react]

# Dependency graph
requires:
  - phase: 03.1-the-imprint-and-the-binder
    provides: Versions.jsx and PenFoot.jsx complete for all four pens (plans 01-02), the binder's global control treatment, chevron room and outline split (plan 03), prose fields and the on-demand mechanism (plan 04)
provides:
  - One pen diff computed on the page (RecipePage.jsx) and passed to both Method and FormulationNote — Method no longer calls buildDiff or stepsWithStaleAmounts itself, so a keystroke costs one comparison rather than two
  - The plan pen's six graduated rules strike the parent figure, draw the hollow tick and carry the "was ..., now ..." change phrase, and leave the tab path while the pen is open (tabindex="-1") exactly as they already did while a batch is recorded
  - blockedSaveRowId (domain/lineage.js) — a non-numeric grams field blocks the save in words, names the row, marks it and focuses the field
  - formatGramsValue (domain/composition.js) — the bare one-decimal grams figure, with formatGrams defined in terms of it; total.fromValue / total.toValue on buildDiff's return
  - --col-step-pen (160px) and .ingredient-table.is-developing — the Step column widens in the pen, the Data column's header goes blank, and columns.test.js asserts the name column still clears its measured minimum
affects: []

# Actuals (#2632)
actuals:
  tokens: null
  tasks: 4
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "One comparison per render, lifted to the page: RecipePage derives penDiff (buildDiff(draftVersion, version) while developing) and its stale steps once, and passes them down. buildDiff filters each side through activeRows/activeSteps internally, so the same value is correct for the method, the table and the rules — a second, differently-filtered comparison is never needed."
    - "A bare-number formatter is first-class, not a strip at the render site: formatGramsValue returns the unit-less figure and formatGrams appends the unit to it, so the two can never print a different number for the same value and no render site ever composes two already-unit-suffixed strings."
    - "A column token's derivation lives in a comment beside the token and is re-asserted in columns.test.js as a named constant, so the number can be re-derived rather than trusted."

key-files:
  created: []
  modified:
    - app/src/domain/composition.js
    - app/src/domain/composition.test.js
    - app/src/domain/diff.js
    - app/src/domain/diff.test.js
    - app/src/domain/lineage.js
    - app/src/domain/lineage.test.js
    - app/src/styles/app.css
    - app/src/styles/columns.test.js
    - app/src/styles/tokens.css
    - app/src/ui/FormulationNote.jsx
    - app/src/ui/FormulationNote.test.jsx
    - app/src/ui/IngredientTable.jsx
    - app/src/ui/IngredientTable.test.jsx
    - app/src/ui/Method.jsx
    - app/src/ui/Method.test.jsx
    - app/src/ui/RecipePage.jsx
    - app/src/ui/Versions.jsx
    - app/src/ui/Versions.test.jsx

key-decisions:
  - "The total row's fix took the bare-number domain export, not a strip at the render site (planner decision 1, as built). formatGramsValue(grams) returns the one-decimal figure with no unit; formatGrams appends ' g' to it, leaving its output byte-identical for its four other callers; diff.js gained total.fromValue / total.toValue built from the same formatter, leaving from, to and changed untouched so no existing assertion moved. Recorded here as the plan asked: a future column-arithmetic touch must extend the formatter, never re-introduce the bug by stripping ' g' at a render site."
    
  - "--col-step-pen is 160px, derived and asserted rather than guessed, and the derivation as built reads: columns.test.js's measured 66.14px for the nine letters of 'estimated' gives ~7.35px per letter at the table body size; '3. Build the base' is seventeen characters, ~124.93px; plus the select's own chevron room (--gap-select-chevron-room, 20px, plan 03) and twice --table-cell-pad-x (6px each, 12px) — 124.93 + 20 + 12 = 156.93, rounded up to 160px so the arithmetic clears the estimate with a few px of slack rather than sitting exactly on it. The name column is the unsized one and absorbs the difference; no other column's token moved."
  - "The split-step suffix follows the brief, not the critique's wording (planner decision 4). route-recipe-version.md § 6 as revised 2026-09-08 requires 'unallocated + 3', so the show-changes step cell's primary always prints something — the resolved number when either side resolves, otherwise the word 'unallocated' — and the suffix stays. The critique's proposed suppression was not taken; the brief is both later and binding (D-29)."
  - "Only the two named Two-Ink leaks were closed. The pen-blue class came off the split-step suffix span (printed matter, reads in ink), and Versions.jsx's parent line was confirmed to carry none — a check, not a change, since plan 01 built it without one. 'unknown', 'unmarked' and 'date unknown' were left exactly as built: D-30 leaves the third voice open and the built colour stands."
  - "nowrap is scoped to the total row's numeric cell through the tfoot context, never applied to every numeric cell."

patterns-established:
  - "penDiff on the page: the pen's comparison against the version it opened on is derived once in RecipePage and passed to every consumer, rather than each component computing its own."

requirements-completed: []

coverage:
  - id: T1
    description: "The plan pen's six graduated rules strike the parent figure, draw the hollow tick and carry the change phrase in the accessible name, fed by one page-level pen diff rather than a second local computation (critique P1 #2 first half)."
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/FormulationNote.test.jsx — a struck head figure renders when a diff reporting a changed figure is given"
        status: pass
      - kind: unit
        ref: "app/src/ui/Method.test.jsx — the suite reads the diff prop rather than Method's own computation; assertions unchanged because the value is identical"
        status: pass
    human_judgment: true
    rationale: "The markup fact is proven. That PAC actually moves 24.1 to 23.8 and strikes as the maker types is a live-browser transition renderToStaticMarkup cannot execute (RESEARCH.md Pitfall 4) — carried to the hand-check list below."
  - id: T2
    description: "The six rules leave the tab path while the plan's pen is open, as they already do while a batch is recorded, staying clickable and keeping their focus treatment (critique P1 #2 second half, D-28)."
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/FormulationNote.test.jsx — rule buttons carry tabindex=\"-1\" in the developing mode as well as the recording one"
        status: pass
    human_judgment: true
    rationale: "Confirmed live by the 2026-09-10 re-critique, which measured the rules out of the pen's tab path and moved heuristic 7 from 3 to 4 for exactly this."
  - id: T3
    description: "A grams field holding a non-number blocks the save in words, names the row, marks it and focuses the field (critique P1 #3, D-21)."
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/domain/lineage.test.js — blockedSaveRowId cases"
        status: pass
      - kind: unit
        ref: "app/src/ui/IngredientTable.test.jsx — the row is marked and the field carries the blocked-save wiring"
        status: pass
    human_judgment: true
    rationale: "Partially closed, and knowingly so — see the open items below. The block and the sentence are proven; the strike is still decided by a raw string comparison, so an unreadable value strikes the printed figure while every figure goes on using the parent's. That residual was re-shaped by Mark on 2026-09-09 as an open decision (6af7a6e) rather than a fix, after the quick plan against it was reverted (d74d01d)."
  - id: T4
    description: "The total row strikes a bare number and prints the unit once on one line in both the pen and show-changes; the Step column reads in the pen with the Data column blank-headed and the name column still above its measured minimum; no step cell prints a dangling suffix; both named Two-Ink leaks read in ink (critique P2 #1 and P2 #2, D-22, D-30)."
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/domain/composition.test.js — the bare formatter, and formatGrams still returning what it always did"
        status: pass
      - kind: unit
        ref: "app/src/domain/diff.test.js — total.fromValue / total.toValue"
        status: pass
      - kind: unit
        ref: "app/src/ui/IngredientTable.test.jsx — the unit appears exactly once in the pen and in show-changes; the total cell carries the nowrap-bearing class; the step cell reads the word when neither side resolves and the word plus suffix when only the split survives; the split-step suffix span carries no pen-blue class"
        status: pass
      - kind: unit
        ref: "app/src/styles/columns.test.js — the pen-state budget case, with --col-step-pen's derivation as a named constant"
        status: pass
    human_judgment: false

# Phase-closing verification (re-run 2026-09-09 at close-out, all green)
phase-verification:
  - "npm --prefix app run build — exit 0, 115 modules, built in 78ms"
  - "npm --prefix app test — exit 0, 30 files, 646 tests passed"
  - "dependency set unchanged: @vitejs/plugin-react,fake-indexeddb,idb,react,react-dom,react-router,vite,vitest"
  - "repository seam held — 0 modules outside store/db.js import idb"
  - "0 dangerouslySetInnerHTML anywhere under app/src"
  - "domain framework-free — 0 react/react-dom/react-router/idb imports in rows.js, lineage.js, composition.js, diff.js"
  - "0 <button> in Headnote.jsx and BatchMargin.jsx — the printed spread carries no control"
---

# Plan 03.1-05 — the three build gaps and the column arithmetic

## What happened

Tasks 1-3 were executed on 2026-09-08 and committed as `1f8318d`, `de6ea96` and `e311bd0`. The run ended before this SUMMARY was written, so every index reported the plan incomplete while the work sat in the tree. Closed out on 2026-09-09 after the safe-resume gate caught the mismatch: no re-execution, no new commits against the three tasks — the committed work was verified in place (build, suite, and all of Task 3's own verify gates and acceptance greps re-run green) and Task 4's checkpoint was taken with Mark.

## Task 4 — the re-critique, and Mark's call

The re-critique ran twice: `.impeccable/critique/2026-09-09T11-54-37Z__app-src-ui-recipepage-jsx.md`, then `.impeccable/critique/2026-09-10T00-26-25Z__app-src-ui-recipepage-jsx.md` after the 2026-09-09 quick fixes (`260909-oov`, `-oow`, `-oox`). Against the `2026-09-08T12-33-31Z` baseline of 25/40, the page now scores 28/40.

Of the five items Task 4 named as the done-check:

- **P1, the developing pen is a form not a page** (plans 01, 02, 04) — not closed. The pen's method column is still 32 bordered buttons, 16 chips at 376px and 12 clipping selects. This is no longer an open design question: sketch 003's handoff records Mark's 2026-09-09 decision that the method opens read-only in the pen with one `edit this step` text control per step. It is approved work awaiting a plan.
- **P1, the pen's rules never strike the parent figure** (this plan, task 1) — **closed**, and the re-critique moved heuristic 7 from 3 to 4 for it.
- **P1, native control chrome breaks the four-colour system** (plan 03) — closed.
- **P2, Two-Ink leaks** (this plan task 3, plan 01) — closed. The re-critique's own claim that pen blue paints nowhere was checked and withdrawn as an enumeration artifact; every value that paints resolves to a token.
- **P2, numeric-column arithmetic** (this plan, task 3) — closed.

**Mark approved close-out on 2026-09-09** with three P1-class items carried forward rather than resolved here (below). One correction to the record the re-critique makes and this SUMMARY repeats: the CLI detector's exit 0 on `.jsx` is a **null result, not a clean bill** — it emits nothing whatsoever for JSX, verified against a deliberately broken control. All static evidence in both snapshots comes from the in-page detector.

## The imprint is superseded

This phase built the imprint — the front-matter band beside the headnote that took every control out of the printed spread — across `Headnote.jsx`, `Versions.jsx`, `VersionStrip.jsx`, `PenFoot.jsx`, `BatchMargin.jsx` and `app.css`, against `route-recipe.md` as revised 2026-09-08.

**`route-recipe.md` was revised again on 2026-09-09 and retires it.** After sketch 003 (variant B), the imprint and the tray are both retired in favour of **two front-matter rows across the page**; the margin is retired as a region, its content splitting upward into the batch row and downward under Balance; the ingredient table reads in step order with portions; the pen's method opens read-only; a blank prose line is ruled. A second revision the same day settled that a row's portions are the authored amounts and the total derives, retiring `step`/`splitStep`.

The imprint's retirement does **not** invalidate this plan's work. Plan 05 builds against `route-recipe-version.md`, whose last structural revision is 2026-09-08 and which sketch 003 did not touch — its subject is the pen's internals (the graduated rules, the grams guard, the column arithmetic), not the front matter's arrangement. Those close as built and carry into the rebuild.

What is superseded is the phase's front-matter *layout* work (plans 01 and 02, and the parts of 03 and 04 that dress it). The front-matter-rows rebuild is its own phase.

## Open items carried forward

Not closed here, and each needs its own work:

1. **The fifth loose grams reader.** The strike is decided by a raw string comparison at `IngredientTable.jsx:591`, so `-5`, `4o`, `.5` and `45.` render `~~40~~ -5` while every figure reads 40, with no message until Save; and `.5` / `45.` are legitimate 0.01 g transcriptions being told they are "not a number". Re-shaped by Mark on 2026-09-09 as an **open decision** (`6af7a6e`), after the quick plan against it was reverted (`d74d01d`).
2. **The `declaredAxes` P0** (new, not this plan's). `liftVersionRecord` never lifts `declaredAxes` — while lifting the identical string-to-object drift for authored notes twelve lines above it — so two of the six tasting axes ship with no name, no anchors, and one shared native radio group: marking the second clears the first. `VERSION_SCHEMA_VERSION` stayed at 2, so no migration runs. It bites exactly the milestone-1 working case. **The portions change is positioned to repeat it**: retiring `step`/`splitStep` changes a stored shape the same way, so `liftVersionRecord`, `VERSION_SCHEMA_VERSION` and `DB_VERSION` must move in lockstep, batch snapshots included.
3. **The settled vocabulary is unshipped.** Mark settled the recipe page's labels on 2026-09-09 at 13:31; thirteen renames (Advisories → Things to check, Come-up → Time to temperature, Overrun → Air, Meltdown at 20 min → Melt test, and the rest) are still the old word on screen. Product authority, not taste.

Plus the two the plan itself sent to the re-critique: the **Firefox calendar icon** still shows with no CSS hook to hide it (plan 03, RESEARCH.md Pitfall 3), and **the parent's version line prints twice** while the plan's pen is open, once in the recipe block and once as `was ...` beneath the field (plan 01).

## The hand-check list

`renderToStaticMarkup` produces markup, never a transition, and no DOM-driving test library is installed (RESEARCH.md Pitfall 4), so these were carried out of their plans deliberately. The 2026-09-10 re-critique verified the starred ones live in the browser:

- ★ Escape closes an untouched pen and returns focus to its opener; Escape on a pen holding ink does nothing, and Cancel is the one exit — **verified on all three pens**
- Cancel and Save each return focus to the control that opened the pen; a fork's save lands focus on the child's Develop control — Cancel verified; **focus after Save lands on `<body>`, still open**
- A blocked save moves focus to the offending field and the row scrolls into view; nothing dashes while you are still typing
- Opening `add purpose` focuses the new field; emptying it and tabbing away collapses it back without moving focus
- `change` opens the twelve boxes for one step, the names line updates live, and Escape inside the checklist closes the checklist only
- ★ Tab from the last table row skips the six graduated rules while the pen is open, and they stay clickable with their focus treatment — **verified**
- Hovering a control thickens its stroke without moving anything beside it

## Next

Per Task 4's own instruction, `/impeccable document` re-records `DESIGN.md` § Buttons, § Inputs and § Focus from what was built — worth doing before the front-matter rebuild changes them again.
