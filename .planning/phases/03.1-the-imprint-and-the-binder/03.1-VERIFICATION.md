---
phase: 03.1-the-imprint-and-the-binder
verified: 2026-09-09T21:35:00Z
status: passed
score: 43/50 must-haves verified; the 3 FAILED are accepted below as recorded overrides (Mark, 2026-09-09), leaving 0 open gaps
overrides:
  - must_have: "The pen's step body reads as a step, not a form (critique P1, plan 04; ROADMAP goal clause 2, \"the plan's pen reads as the page\")."
    gap: 1
    reason: "Mark's 2026-09-09 sketch-003 decision supersedes plan 04's approach with a read-only-in-pen method carrying one 'edit this step' text control per step. The redesign is approved and unbuilt. Carried forward to the front-matter rows rebuild, which rewrites the pen's method column anyway."
    accepted_by: "Mark"
    accepted_at: "2026-09-09"
  - must_have: "Every field in the batch record carries a visible label (route-recipe-batch.md section 6; D-25's always-present churn fields)."
    gap: 2
    reason: "A regression this phase's plan 04 introduced, and knowingly accepted rather than patched twice. The 2026-09-09 revision of route-recipe.md answers it with a ruled hairline baseline and no label, and retires the margin as a region — so BatchMargin.jsx is rewritten by the front-matter rows rebuild. Fixing it here would build the superseded answer."
    accepted_by: "Mark"
    accepted_at: "2026-09-09"
  - must_have: "Closing any pen by save or cancel returns focus to the control that opened it (D-27, plan 02)."
    gap: 3
    reason: "Cancel is verified on all three pens; Save lands on <body> for the batch pen. Carried forward with the same focus family as code-review finding WR-01 (the blocked-save focus effect fires only on the first press), to be closed together in the front-matter rows rebuild."
    accepted_by: "Mark"
    accepted_at: "2026-09-09"

covered_files:
  - .planning/REQUIREMENTS.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-01-PLAN.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-01-SUMMARY.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-02-PLAN.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-02-SUMMARY.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-03-PLAN.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-03-SUMMARY.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-04-PLAN.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-04-SUMMARY.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-05-PLAN.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-05-SUMMARY.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-CONTEXT.md
  - .planning/phases/03.1-the-imprint-and-the-binder/03.1-RESEARCH.md
  - app/src/domain/composition.js
  - app/src/domain/diff.js
  - app/src/domain/lineage.js
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/css-source.js
  - app/src/styles/tokens.css
  - app/src/ui/Authored.jsx
  - app/src/ui/BatchMargin.jsx
  - app/src/ui/FormulationNote.jsx
  - app/src/ui/Headnote.jsx
  - app/src/ui/IngredientTable.jsx
  - app/src/ui/Method.jsx
  - app/src/ui/PenFoot.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/VersionStrip.jsx
  - app/src/ui/Versions.jsx
covered_digest: "v1:sha256:c1a46391a8735bcaa26c4aea6faa6bf2687ece7a1694b3d80eac3460044d370d"
behavior_unverified: 3
gaps:
  - truth: "The plan's pen reads as the page — the pen's step body reads as a step, not a form (03.1-04-PLAN.md must-have; ROADMAP goal clause 2)."
    status: failed
    reason: "The phase's own final re-critique (2026-09-10T00-26-25Z, run at close-out, score 28/40, unchanged from the prior run) finds this explicitly NOT closed: 'Method column is a form (32 boxed buttons)... Now an approved decision awaiting build.' Live measurements: 32 bordered buttons, 16 target chips at 376px each, 12 selects that clip their own labels, the developing state expanding the document from 1833px to 3761px. Element-count reduction (202→92, plan 04's own D7 metric) is real but did not close the critique's own P1. Mark's 2026-09-09 sketch-003 decision (method opens read-only in the pen with one 'edit this step' control) is the accepted remedy, but it is unbuilt — no phase in ROADMAP.md currently carries it, so it cannot be treated as a Step 9b deferral to a documented later phase."
    artifacts:
      - path: "app/src/ui/Method.jsx"
        issue: "6 <button> elements and step-body markup that the live re-critique still scores as a form, not a page, even after plan 04's on-demand/collapse work"
    missing:
      - "A plan (not yet in ROADMAP.md) implementing Mark's 2026-09-09 sketch-003 decision: the method opens read-only in the pen with one 'edit this step' control per step"
      - "Or, if this is to be accepted as out-of-phase-scope work, a recorded override in this VERIFICATION.md and/or a ROADMAP phase entry that Step 9b can defer against"
  - truth: "The record's own prose (draw notes, ingredient notes, next-time notes) is genuinely usable prose editing, per route-recipe-batch.md § 6's visible-label requirement (binding source, D-29)."
    status: failed
    reason: "Plan 04's own decision to drop the visible label word from every prose field (planner decision 2) was applied uniformly to the batch record's always-present churn fields (D-25), which — unlike step prose — frequently start empty. The 2026-09-09 critique (confirmed still present at the 2026-09-10 close-out re-critique, table row 'Batch margin's prose fields invisible — Remains') found: 'Draw notes, Ingredient notes and Next time render as an empty <label> around a borderless .prose-field with no visible text and no placeholder — three 457x17px inputs in 89px of blank paper.' This is not one of the three items the phase disclosed as an approved carry-forward; it is a live-confirmed regression from this phase's own plan 04 work, unresolved at close-out."
    artifacts:
      - path: "app/src/ui/BatchMargin.jsx"
        issue: "Lines ~191-212: draw notes / ingredient notes / next-time textareas carry class=\"prose-field\" with no visible label and no placeholder, so an empty field while recording a batch or amending shows no discoverable affordance"
    missing:
      - "A small-print label word above each empty always-present prose field, or a hairline baseline rule until it holds text, per the critique's own suggested fix (routed to /impeccable clarify by the critique itself)"
  - truth: "Closing any pen by save or cancel returns focus to the control that opened it (03.1-02-PLAN.md must-have, D-27); a fork's save lands focus on the child's Develop control (03.1-02-PLAN.md must-have, D-27)."
    status: failed
    reason: "03.1-05-SUMMARY.md's own hand-check list, updated after the 2026-09-10 re-critique, states plainly: 'Cancel verified; focus after Save lands on `<body>`, still open.' The critique itself (2026-09-10T00-26-25Z) confirms live: 'After saving a batch, document.activeElement is `<body>`. The brief requires the opener.' Escape-driven closes are confirmed working; Save-driven closes are confirmed NOT returning focus to the opener, at least for a batch save. The fork-to-child's-Develop-control case (a distinct mechanism, an autoFocus attribute rather than a ref-based effect) was not exercised by any reported live check either way."
    artifacts:
      - path: "app/src/ui/RecipePage.jsx"
        issue: "The Save-driven close paths (handleSaveBatch and siblings) do not carry the same focus-return-to-opener effect that Escape and Cancel use — confirmed broken live for the batch pen's Save"
    missing:
      - "A focus-return mechanism on every Save path, not only Escape/Cancel, verified live for all four pens including the fork's landing on the child's Develop control"
deferred: []
advisory: []
behavior_unverified_items:
  - truth: "After a fork saves and the page lands on the child's URL, the child's Develop control receives focus on mount (D-27)."
    test: "Save-as (fork) an unchurned version's plan pen, land on the child's URL"
    expected: "document.activeElement is the child version's Develop control"
    why_human: "renderToStaticMarkup cannot execute the navigation + autoFocus-on-mount transition (RESEARCH.md Pitfall 4); no live check for this specific case is reported in any critique or quick-plan SUMMARY reviewed for this verification — it is neither confirmed working nor confirmed broken."
  - truth: "Pressing a blocked Save moves focus to the offending field (the grams field, or the version-line field for a blank/duplicate line) so the row scrolls into view (D-21, 03.1-05-PLAN.md)."
    test: "Type a non-numeric grams value (e.g. '4o'), press Save"
    expected: "The offending row is marked, the sentence appears, and document.activeElement is the offending input"
    why_human: "renderToStaticMarkup cannot execute the focus() call this requires (RESEARCH.md Pitfall 4); not named in any 'confirmed live' list in the reviewed critiques or quick-plan summaries."
  - truth: "Pressing 'change' opens the twelve checkboxes for one step and the names line above updates live as boxes change; Escape inside the open checklist closes the checklist only (D-24)."
    test: "Press 'change' on a step's uses line, toggle a box, press Escape once, then again"
    expected: "The checklist opens for that step only, the names line updates live, one Escape closes only the checklist and returns focus to the control, a second Escape applies the pen rule"
    why_human: "renderToStaticMarkup cannot execute the click/keydown transitions (RESEARCH.md Pitfall 4); not named in any 'confirmed live' list reviewed for this verification."
human_verification:
  - test: "Fork a plan pen's Save-as on an unchurned version and check where focus lands on the child page."
    expected: "Focus lands on the child version's Develop control (D-27)."
    why_human: "Not a markup fact; no live check reported either way."
  - test: "Type a non-numeric grams value, press Save, and check whether focus moves to the offending field."
    expected: "Focus moves to the offending grams (or version-line) field and the row scrolls into view (D-21)."
    why_human: "Not a markup fact; no live check reported either way."
  - test: "Press 'change' on a step's uses line, check the checklist opens/closes correctly and Escape is scoped to it."
    expected: "Checklist opens for one step, closes on 'done' or on one Escape without closing the pen (D-24)."
    why_human: "Not a markup fact; no live check reported either way."
---

# Phase 03.1: The imprint and the binder — Verification Report

**Phase Goal:** The recipe page gains its front-matter band and every control leaves the printed spread; the plan's pen reads as the page; the binder's control treatment replaces browser chrome; the pen's rules strike the parent figure; the table's columns hold in every state.
**Verified:** 2026-09-09
**Status:** passed (3 gaps accepted as recorded overrides, Mark 2026-09-09)
**Re-verification:** No — initial verification

## Summary Up Front

This is a large, mostly-real phase. Independent re-derivation confirms: build green (115 modules), full suite green (30 files, 646 tests), the front-matter `Versions`/`PenFoot` region genuinely exists and is wired, the global binder CSS rules genuinely exist and are asserted by a dedicated `binder.test.js`, the pen's graduated rules genuinely strike the parent figure and leave the tab path, the grams-validation and column-arithmetic domain fixes genuinely exist with tests. SUMMARY.md's structural claims for plans 01–03 and most of 04–05 check out against the actual code.

However, the phase's own final artifact — the 2026-09-10 re-critique that plan 05's Task 4 checkpoint names as **the phase's done-check** — found that **one of the five literal clauses in the ROADMAP goal ("the plan's pen reads as the page") is not achieved**, and surfaced two further live-confirmed defects (an invisible batch-record prose field, and Save not returning focus to its opener) that are not among the three items this verification's task brief pre-disclosed as "approved, not gaps." All three are documented in the project's own artifacts (the critique file and 03.1-05-SUMMARY.md's hand-check list) but none carries a recorded override, and none matches a documented later ROADMAP phase closely enough to defer under Step 9b's conservative rule. They are reported below as gaps, not silently absorbed into a passing verdict, per this agent's escalation-gate mandate.

## Requirements Coverage

**No-op for this phase, as stated in the task brief and confirmed in REQUIREMENTS.md.** No new requirement IDs are introduced (`grep -n "03.1" .planning/REQUIREMENTS.md` returns nothing beyond the phase-mapping note). UX1-01 is named as a build constraint only; its end-to-end verification is explicitly mapped to Phase 4 (`REQUIREMENTS.md` line 118: `| UX1-01 | Phase 4 | Pending |`). No orphaned requirements.

## Goal Achievement — the Five Roadmap Clauses

| # | Clause | Status | Evidence |
|---|--------|--------|----------|
| 1 | The recipe page gains its front-matter band | ✓ VERIFIED | `Versions.jsx`/`PenFoot.jsx` exist, exported, wired into `RecipePage.jsx`'s `.recipe-band` grid (`app/src/styles/app.css:232-246`); build/suite green; confirmed live by critique's own reading-state praise ("the reading state remains unmistakably this product's"). |
| 1b | …and every control leaves the printed spread | ⚠️ PARTIAL | True for `Headnote.jsx`/`BatchMargin.jsx` reading state (0 `<button>` each, independently grepped) and true for the ingredient table/notes when no pen is open. **Not fully true while a pen is open**: see clause 2. |
| 2 | The plan's pen reads as the page | ✗ FAILED | Final re-critique (2026-09-10, run at close-out): "Method column is a form (32 boxed buttons)... Now an approved decision awaiting build." Not closed. See Gap 1 below. |
| 3 | The binder's control treatment replaces browser chrome | ✓ VERIFIED | `binder.test.js` (24+ assertions) passes; global `button, select`, `:focus-visible`, `input[type='checkbox']`, `a` rules independently grepped in `app.css`; critique confirms live: "native control chrome breaks the four-colour system (plan 03) — closed," hover behavior confirmed correct, Firefox calendar-icon gap is a disclosed, accepted exception (no CSS hook exists in that engine). |
| 4 | The pen's rules strike the parent figure | ✓ VERIFIED | `penDiff` computed once in `RecipePage.jsx:544` and threaded to `FormulationNote`/`Method`; `FormulationNote.jsx:19` extends `tabIndex` to `'developing'`; critique confirms live, moving heuristic 7 from 3 to 4 for exactly this. |
| 5 | The table's columns hold in every state | ✓ VERIFIED | `formatGramsValue`/`total.fromValue`/`total.toValue` exist and tested; `--col-step-pen` derivation asserted in `columns.test.js`; critique confirms P2 numeric-column arithmetic closed. |

**One of five goal clauses fails outright, and a second is only partially true.** This is the headline finding.

## Gap 1 (BLOCKER): "The plan's pen reads as the page" is not achieved

The phase's own re-critique — which plan 05's Task 4 explicitly designates as the phase's done-check — scored the final state 28/40 (unchanged from the run immediately before close-out) and found:

> [P1] Method column is a form (32 boxed buttons) | **Remains** | 32 buttons; "uses nothing yet" x 5; `display: block` unchanged. Now an approved decision awaiting build.
> [P1] Target chips swell to 377px | **Remains** | 16 chips at 376px, two 180px fields each.
> [P1] "done differently" is an 850px band | **Remains, wider** | 10 controls at 892px, centred.

This is a direct, unresolved failure of a plan 04 must-have ("The pen's visible controls on a step fall to the targets, one `change`, the on-demand openers and one remove — a step still reads as a step, which is what closes critique P1 #1's other half") and of the literal second clause of the ROADMAP phase goal. Plan 04's own element-count metric (202→92 interactive elements) is real and independently plausible from the code, but the critique's live, holistic judgment — the actual test this phase set for itself — says the reduction did not close the gap.

**This is not one of the three items the verification task brief pre-disclosed as approved-and-not-a-gap** (the loose grams reader, the `declaredAxes` bug, the 13 stale labels). 03.1-05-SUMMARY.md itself is candid about it ("not closed... It is approved work awaiting a plan") and records that Mark's 2026-09-09 sketch-003 decision (the method should open read-only in the pen with one "edit this step" control) is the intended remedy — but that remedy is unbuilt, and no phase in `ROADMAP.md` currently carries it. Per Step 9b's conservative-matching rule, this cannot be deferred to a documented later phase because no such phase exists yet in the roadmap.

**This looks like an informed, intentional deviation** (Mark reviewed the re-critique and approved closing the plan-05 checkpoint anyway). To accept it formally rather than carry it as an open gap:

```yaml
overrides:
  - must_have: "The pen's step body reads as a step, not a form (critique P1, plan 04)."
    reason: "Mark's 2026-09-09 sketch-003 decision supersedes plan 04's approach with a read-only-in-pen redesign; that redesign is approved but not yet planned as a phase."
    accepted_by: "Mark"
    accepted_at: "2026-09-09T00:00:00Z"
```

## Gap 2 (BLOCKER): The batch record's always-present prose fields are invisible when empty

Plan 04's decision to drop every prose field's visible label (planner decision 2, applied uniformly across `Method.jsx`, `Headnote.jsx`, `Versions.jsx`, and `BatchMargin.jsx`) works for fields that already hold printed text, but D-25's own "always present" churn fields (draw notes, ingredient notes, next-time) frequently start empty while recording or amending a batch. The critique (first observed 2026-09-09, confirmed still present at the 2026-09-10 close-out re-critique) found:

> Draw notes, Ingredient notes and Next time render as an empty `<label>` around a borderless `.prose-field` with no visible text and no placeholder — three 457×17px inputs in 89px of blank paper between Overrun and ADVISORIES. `route-recipe-batch.md` section 6 requires a visible label on every field.

Independently confirmed in the current tree: `app/src/ui/BatchMargin.jsx` lines ~191-212 render these three fields with `className="prose-field"` and an `aria-label` only (no visible `<span>`), matching the critique's description exactly. This is a live-confirmed regression traceable to this phase's own plan 04 work, not disclosed to this verification as an approved carry-forward, and not resolved by 03.1-05 (which was scoped to the three build gaps and column arithmetic, not this finding).

## Gap 3 (BLOCKER): Save does not return focus to its opener

03.1-05-SUMMARY.md's hand-check list, updated after the phase's final re-critique, states: "Cancel and Save each return focus to the control that opened the pen... Cancel verified; **focus after Save lands on `<body>`, still open**." The critique itself confirms live: "After saving a batch, `document.activeElement` is `<body>`. The brief requires the opener." This directly fails a plan 02 must-have (D-27: "Closing any pen by save or cancel returns focus to the control that opened it") for at least the batch-record Save path. The plan-pen and tasting-pen Save paths were not separately confirmed either way, and the fork's landing-on-child's-Develop-control case (a different mechanism — an `autoFocus` attribute rather than the ref-based return effect) was not exercised by any reported live check; both are listed under Human Verification below rather than assumed broken by association.

## Carried-Forward Items (Approved, Not Gaps)

These three were disclosed in this verification's task brief as known, Mark-approved deviations, confirmed still present in the current tree, and are recorded here with their disposition rather than as failures of this phase:

1. **The fifth loose grams reader.** `IngredientTable.jsx`'s strike-vs-current comparison at the row level (`changedGrams = draftRow.grams !== String(row.grams) ? draftRow.grams : null`, independently confirmed in the current tree) is a raw string comparison, not routed through `parseGramsDraft`. Re-shaped by Mark as an open decision (commit `6af7a6e`) on 2026-09-09, after the quick plan against it (`ed03f93`) was reverted (`d74d01d`). 03.1-05-SUMMARY.md's own T3 coverage entry calls this "partially closed, and knowingly so."
2. **The `declaredAxes` lift bug.** Independently confirmed: `app/src/store/versionLift.js`'s `liftVersionRecord` spreads `...raw` and explicitly lifts `parentVersionId`, `reason`, `citedBatchId`, `createdAt`, `rows`, `method`, and `authored`, but never touches `declaredAxes` — unlike the adjacent `authored` field, which gets the same string-to-object normalization `declaredAxes` needs. A new P0 the re-critique found; pre-existing, not caused by this phase, and explicitly out of this phase's scope per the task brief.
3. **Thirteen stale UI labels.** Independently confirmed: `app/src/ui/DerivedAdvisories.jsx` still reads "Advisories" and `app/src/ui/BatchMargin.jsx` still reads "Come-up, min", "Overrun, %", "Meltdown at 20 min, g" — the pre-2026-09-09-13:31 vocabulary. Product-authority decision, not a build gap.

Two further items the phase's own plans flagged for the re-critique and which remain open, also not gaps:

4. **The Firefox calendar-icon gap.** No CSS hook exists in that engine (RESEARCH.md Pitfall 3); the WebKit rule is applied and costs nothing where it doesn't match. Accepted, documented exception (D-13's named limitation).
5. **The parent's version line prints twice** while the plan's pen is open (once in the recipe block per D-03, once as "was …" beneath the ceremony's field per `route-recipe-version.md` § 3) — flagged by plan 01 for the re-critique as an accepted consequence of two binding sources both requiring it; not resolved in code, and not asked to be.

## The Front-Matter Layout Is Superseded (Not a Gap)

Per the task brief: `route-recipe.md` was revised again on 2026-09-09 (after this phase's plans were written against the 2026-09-08 revision) to retire the imprint/Versions band and the tray in favor of two front-matter rows spanning the page. This phase is correctly verified against the 2026-09-08 brief its plans were written to; the later retirement does not make plans 01–02's delivered work a gap. The front-matter rebuild is explicitly a future phase's work.

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/ui/Versions.jsx` | The Versions region, all four ceremonies | ✓ VERIFIED | Exports `Versions`; independently grepped ceremonies, lineage labels, batch list, hint sentence, placeholders |
| `app/src/ui/PenFoot.jsx` | The foot band, all four pens | ✓ VERIFIED | Exports `PenFoot`; gated correctly on `openPen` |
| `app/src/ui/Headnote.jsx` | Recipe block alone, no control | ✓ VERIFIED | 0 `<button>` (independently grepped) |
| `app/src/ui/BatchMargin.jsx` | Record content alone, no control | ✓ VERIFIED (structurally) — see Gap 2 for a content-visibility defect | 0 `<button>` (independently grepped); three always-present prose fields render invisibly when empty |
| `app/src/domain/lineage.js` | `blockedSaveMessage`, `blockedSaveRowId` | ✓ VERIFIED | Both exported, independently grepped (`lineage.js:196`, `:213`) |
| `app/src/domain/composition.js` | `formatGramsValue`, `formatGrams` | ✓ VERIFIED | Both exported, independently grepped |
| `app/src/ui/FormulationNote.jsx` | Live pen diff, tab-path fix | ✓ VERIFIED | `tabIndex` condition independently grepped to cover `'developing'` |
| `app/src/styles/app.css` | Global binder rules | ✓ VERIFIED | `:focus-visible`, `a`, `input[type='checkbox']` rules independently grepped |
| `app/src/styles/binder.test.js` | The binder's own stylesheet contract | ✓ VERIFIED | Exists, part of the 30-file/646-test green suite |
| `app/src/ui/Method.jsx` | Step body reads as a step, not a form | ✗ NOT VERIFIED | See Gap 1 — live re-critique found this unresolved |

## Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| `PenFoot.jsx` | `RecipePage.jsx` | Shared handler references, no local derivation | ✓ VERIFIED (grep: no `useState`/`useEffect`/`derivePenState` in `PenFoot.jsx`) |
| `Versions.jsx` | `RecipePage.jsx` | `openPen`/`penReason` passed down, never re-derived | ✓ VERIFIED (grep: `derivePenState` count 0 in `Versions.jsx`) |
| `RecipePage.jsx` | `domain/diff.js` | One `buildDiff` for the pen, shared by `Method` and `FormulationNote` | ✓ VERIFIED (`penDiff` computed once at `RecipePage.jsx:544`, threaded to both) |
| `RecipePage.jsx` | `domain/lineage.js` | Blocked-save message and row id from one domain traversal | ✓ VERIFIED (`blockedSaveRowId` exported and grepped in both files) |
| `IngredientTable.jsx` | `domain/composition.js` | Total row strikes the bare-number formatter | ✓ VERIFIED (`formatGramsValue` exported and used) |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build | `npm --prefix app run build` | exit 0, 115 modules | ✓ PASS |
| Full suite | `npm --prefix app test` | exit 0, 30 files, 646 tests | ✓ PASS |
| Dependency set unchanged | `node -p "Object.keys({...deps,...devDeps}).sort().join(',')"` | matches phase's own pinned list | ✓ PASS |
| Repository seam held | `grep -rl "from 'idb'" app/src \| grep -v store/db.js` | 0 | ✓ PASS |
| No `dangerouslySetInnerHTML` | `grep -rl 'dangerouslySetInnerHTML' app/src` | 0 | ✓ PASS |
| Domain framework-free | `grep react\|react-dom\|react-router\|idb` in `rows.js`/`lineage.js`/`composition.js`/`diff.js` | 0 | ✓ PASS |
| No `<button>` on the printed spread (reading state) | `grep -c '<button' Headnote.jsx BatchMargin.jsx` | 0, 0 | ✓ PASS |

These are the "already-run gates" the task brief supplied; all seven independently re-run in this verification session with identical results.

## Requirements-vs-Bookkeeping Note (Informational, Not a Gap)

`ROADMAP.md` still shows `03.1-05-PLAN.md` unchecked (`[ ]`) and "Plans: 4/5 plans executed"; `STATE.md` still reads `stopped_at: Completed 03.1-04-PLAN.md` and phase status `EXECUTING`. This matches the task brief's account of a retroactive close-out (the run that produced plan 05's commits died before writing its SUMMARY, which was reconstructed the next day from the committed work). The code is the source of truth here and was verified directly; this is a bookkeeping lag in two tracking files, not a functional gap. Recommend updating `ROADMAP.md`'s checkbox and `STATE.md`'s `stopped_at`/status fields when this phase closes.

## Human Verification Required

3 items need live confirmation (see `behavior_unverified_items` in frontmatter for the full detail); summarized:

1. **Fork's landing focus.** Save-as (fork) an unchurned version's plan pen and confirm focus lands on the child's Develop control (D-27). Not confirmed either way by any reviewed critique or quick-plan SUMMARY.
2. **Blocked-save focus move.** Type a non-numeric grams value, press Save, confirm focus moves to the offending field (D-21). Not confirmed either way.
3. **Uses checklist open/close and its scoped Escape.** Press "change", confirm the checklist opens for one step, the names line updates live, and Escape closes only the checklist on the first press (D-24). Not confirmed either way.

Two related items ARE confirmed live and are NOT included above: Escape-closes-untouched-pen (confirmed "on all three pens" per the 2026-09-10 critique), Tab-skips-the-six-rules (confirmed, starred in 03.1-05-SUMMARY), Develop-doesn't-steal-focus and add-purpose/add-aside-focus-and-return (confirmed by Mark 2026-09-09 per `quick-260909-oov`'s SUMMARY), and hover-costs-no-movement (confirmed in the 2026-09-10 critique's Minor Observations: "done right").

## Overrides Accepted (Mark, 2026-09-09)

Mark reviewed all three gaps and accepted them as carried forward into the front-matter rows rebuild rather than closing any of them here. The overrides are recorded in this file's frontmatter; the phase closes with 0 open gaps and 3 accepted deviations.

The reasoning, in his framing: `route-recipe.md`'s 2026-09-09 revision retires the imprint and the tray in favour of two front-matter rows, retires the margin as a region, and makes the pen's method open read-only. That rebuild rewrites `Method.jsx`'s step body (Gap 1), rewrites `BatchMargin.jsx` (Gap 2), and re-draws every pen ceremony's open/close path (Gap 3). Closing any of the three against the 2026-09-08 brief would build an answer the 2026-09-09 brief has already replaced.

**This deferral has no destination until the rebuild is a roadmap phase.** The verifier's Step 9b note stands: no phase in `ROADMAP.md` currently carries the front-matter rows work. Adding it is the immediate next action, and these three overrides plus the items below should be listed in its scope.

**Carried into that phase, from this verification and the phase's code review:**

| Item | Source | Note |
|---|---|---|
| The pen's method reads as a form | Gap 1 | sketch 003: read-only method, one `edit this step` per step |
| Three invisible batch prose fields | Gap 2 | 2026-09-09 revision: hairline baseline, no label |
| Save doesn't return focus to its opener | Gap 3 | batch pen confirmed; plan and tasting pens unconfirmed |
| Blocked-save focus fires only on the first press | REVIEW.md WR-01 | `useEffect` keyed on the blocked identity itself |
| Fork's landing focus unconfirmed | Human verification 1 | `autoFocus` path, never exercised live |
| Uses checklist open/close and scoped Escape unconfirmed | Human verification 3 | `stopPropagation` proven by source only |
| The fifth loose grams reader | Carried-forward 1 | open decision, `6af7a6e` |
| The `declaredAxes` lift P0 | Carried-forward 2 | same stored-shape hazard as retiring `step`/`splitStep` |
| Thirteen stale labels | Carried-forward 3 | settled 2026-09-09 13:31 |
| Firefox calendar icon | Carried-forward 4 | no CSS hook; documented exception |
| Parent's version line prints twice | Carried-forward 5 | two binding sources both require it |

Human verification item 2 (the blocked-save focus move) is no longer unconfirmed: the phase's code review found it broken on a second press against the same invalid field. It is folded into WR-01 above.

## Gaps Summary

Three must-have truths, spanning two of this phase's own plans (04 and 02) and the literal second clause of the ROADMAP goal, are confirmed FAILED by the phase's own final artifact (the 2026-09-10 re-critique) and by direct code inspection — not merely unproven, but actively contradicted by live evidence the project already collected. None of the three carries a recorded override. All three are candid in the project's own SUMMARY/critique trail; none was hidden, but none was disclosed to this verification as an accepted deviation either. Recommend: either (a) record explicit overrides for the two Mark has effectively already accepted by approving close-out (Gap 1's Method-form P1, and Gap 3's Save-focus regression, if Mark considers it low-priority against the upcoming front-matter rebuild), with a named follow-up plan for each, or (b) open a short gap-closure plan against Gap 2 (the invisible batch-record prose fields) specifically, since the critique names its own fix ("a small-print label word above each empty field, or a hairline baseline rule") and it is the one of the three least entangled with the superseded front-matter work.

---

_Verified: 2026-09-09_
_Verifier: Claude (gsd-verifier)_
