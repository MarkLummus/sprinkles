---
phase: 03.2-the-portion-and-the-reset
plan: 04
type: execute
wave: 4
depends_on:
  - 03.2-03
files_modified:
  - app/src/domain/batch.js
  - app/src/domain/batch.test.js
  - app/src/data/batch-2026-08-02.js
  - app/src/store/transfer.js
  - app/src/store/transfer.test.js
  - app/src/ui/IngredientTable.jsx
  - app/src/ui/IngredientTable.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/domain/axes.test.js
  - app/src/domain/rows.test.js
  - app/tests/db-reset.test.js
autonomous: true
requirements:
  - D-01
  - D-04
  - D-05
  - D-07
  - D-10
  - D-11

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A batch's as-made value for a row is an array aligned index-for-index with that row's portions, so an as-made record has the same shape as the plan it records (D-10)."
    - "The 2 Aug batch's whole milk reads `120 + 263` and still totals 383 — the sheet's own line, matched line for line (phase Done-when, D-10)."
    - "The as-made total still reads 804.3 g: 799.68 of plan with whole milk at 383, heavy cream at 241, Drizzle at 45 and soy lecithin at a real 0 (D-10, D-11)."
    - "Sucrose carries no as-made key at all, because it matched the plan — absent still means untouched, and it is still not the same fact as done as written (D-10, BATCH1-01)."
    - "A written 0 is still a real value distinct from an absent one, now per portion: presence is decided by an own-property check on the row and an explicit null test on the element, never truthiness (D-11, D-18)."
    - "Nothing in the as-made column is rounded or re-formatted — the precision contract still holds, and only the computed total goes through the rounding convention (D-18)."
    - "The plan's own numbers never leak into the As made column: an unwritten portion reads the table's existing absent placeholder, never that portion's plan amount."
    - "The coefficient snapshot stays per ingredient and never per portion — `createBatch`'s `structuredClone` of the version's rows is unchanged (D-04)."
    - "The six tasting axes each take a mark independently — the four core axes plus the two the seeded version declares, each carrying its own anchor words, with no key collision between the two namespaces (phase Done-when, D-07)."
    - "The full suite is green and its count reconciles against this phase's own record of additions and deletions."
  artifacts:
    - app/src/domain/batch.js
    - app/src/data/batch-2026-08-02.js
    - app/src/domain/axes.test.js
    - app/src/domain/rows.test.js
  key_links:
    - "`app/src/data/batch-2026-08-02.js` -> `createBatch` -> `app/src/store/seed.js` -> the repository seam: the seeded batch is still built through the same functions a maker's own save calls, so the stored record stays indistinguishable from one that was typed."
    - "`asMade[rowId][i]` -> `row.portions[i]`: index alignment is the whole contract, and it is safe because a batch's snapshot rows are cloned once at creation and never retaken."
    - "`app/src/data/olive-oil.js` `declaredAxes` -> `createBatch`'s snapshot -> `app/src/domain/axes.js` `axesForBatch`: the path the six-axes guard walks, unchanged by this phase and asserted to still work after a reset and reseed."
  prohibitions:
    - "`120 + 263` is the AS-MADE record and never the authored split. The authored split of whole milk is `120 + 250.4`, and it is already stored by 03.2-01. Do not touch `app/src/data/olive-oil.js` in this plan (D-11)."
    - "Do NOT author, convert or migrate `declaredAxes` anywhere. The seed already declares its two axes in the target object shape and every reader is satisfied; the six-axes clause is a regression guard on work already done, not new work (D-07)."
    - "Do NOT let the plan's amount fill a blank in the As made column's reading. The plan fills the gap in the computed TOTAL and nowhere else."
    - "Do NOT round or re-format an as-made value anywhere; that convention belongs to computed figures only (D-18)."
    - "No file under `app/src` contains `dangerouslySetInnerHTML`; no component or stylesheet carries a visual literal; exactly one module under `app/src` imports the store library."
---

<objective>
Record what was actually done, per portion, and close the phase's regression guards.

The 2 Aug batch's whole milk stops being a single 383 and becomes the sheet's own `120 + 263`, matching the record line for line. As-made becomes an array aligned with the row's portions, and its total derives the same way the plan's does. Sucrose stays unsplit in the record, because it matched the plan.

Then the guards: the six tasting axes each take a mark independently, every row's total equals the sum of its portions, and the reseeded batch still reads its as-made out of a real IndexedDB.

Purpose: the as-made record and the plan it records now have the same shape. A maker comparing the two is comparing like with like, at the resolution the sheet was written at.

Output: as-made per portion end to end, the three regression guards, and a green suite with a reconciled count.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-the-portion-and-the-reset/03.2-CONTEXT.md
@.planning/phases/03.2-the-portion-and-the-reset/03.2-PATTERNS.md
@.planning/phases/03.2-the-portion-and-the-reset/03.2-01-SUMMARY.md
@.planning/phases/03.2-the-portion-and-the-reset/03.2-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

<planning_provenance>
**Spec-less probe fallback: gated and visibly SKIPPED.** No requirement IDs exist for this phase to probe; no probe-derived predicates were generated. `must_haves` derive from the ROADMAP Done-when and CONTEXT.md D-04, D-05, D-07, D-10 and D-11.

**Package legitimacy gate:** not applicable — this plan installs no packages and does not modify `app/package.json`.

**The key shape CONTEXT.md left to the planner (D-10).** PATTERNS.md offered two candidates: a positional array, or an object keyed by step. **The positional array is taken** — `asMade[rowId]` is an array aligned index-for-index with `row.portions`. It is safe because a batch's snapshot rows are `structuredClone`d once at creation and never retaken, so the alignment cannot drift under a stored record; and it avoids the number-as-object-key coercion an object keyed by step would introduce next to a codebase whose presence checks are deliberately own-property rather than truthiness. An unwritten portion inside a written row is an explicit `null`, never a hole, so `hasOwnProperty` still says the right thing about the row and an explicit null test still says the right thing about the portion. Reversibility: costly — it is a stored record shape, though the store is empty until reseeded.

**`BATCH_SCHEMA_VERSION` moves 1 → 2**, matching the phase's bookkeeping rule recorded in `03.2-01-PLAN.md`: each number moves by one because each shape changed once. Leaving it at 1 while the batch's as-made shape changed would put a false statement in the record.

**Why D-07 is a guard and not a task.** Verified against source on 2026-09-09 and re-verified during planning: `app/src/data/olive-oil.js` already declares its two axes as `{ name, low, high }` objects with real anchor strings; `createBatch` `structuredClone`s them into `batch.snapshot.declaredAxes`; and `axesForBatch` reads that snapshot and nothing else. The P0 the phase brief inherited was a missing entry in the retiring ladder and could only ever bite a record stored under the older string shape — exactly the records the reset discards. Any task proposing to author or convert `declaredAxes` has misread this.
</planning_provenance>

<tasks>

<!-- planner-discipline-allow: splitStep -->
<!-- planner-discipline-allow: versionLift -->
<!-- planner-discipline-allow: portions -->

<task type="auto">
  <name>Task 1: As-made becomes a list, aligned with the plan it records (D-10, D-11)</name>
  <files>app/src/domain/batch.js, app/src/domain/batch.test.js, app/src/data/batch-2026-08-02.js, app/src/store/transfer.js, app/src/store/transfer.test.js</files>
  <read_first>
    - app/src/domain/batch.js — the file being modified, in full: the precision contract at the head, `createBatch`'s as-made copy loop, `hasAsMade`, `asMadeFor`, `asMadeTotals` and its own-property comment
    - app/src/data/batch-2026-08-02.js — the file being modified; its header comment on being built through the same functions a maker's save calls, and the comment beside the rows that matched the plan
    - app/src/data/olive-oil.js — as left by 03.2-01, for the portion order each as-made array aligns to. READ ONLY; this plan does not modify it
    - app/src/store/transfer.js — `validateBatch`'s `churn.asMade` check, as left by 03.2-02
    - app/src/domain/rows.js — `rowGrams`
    - .planning/phases/03.2-the-portion-and-the-reset/03.2-CONTEXT.md — D-04, D-10, D-11, and the ROADMAP Prerequisite's trap
  </read_first>
  <action>
Give the as-made record the same shape as the plan it records.

**`app/src/domain/batch.js`** — set `BATCH_SCHEMA_VERSION` to 2. A batch's `churn.asMade[rowId]` becomes an array aligned index-for-index with that row's portions; an element is the number the maker wrote, or `null` for a portion nothing was written beside. The row's key is absent entirely when nothing was written beside any of its portions.
- `createBatch`'s as-made copy loop copies each row's array rather than each row's scalar, so the churn fields' own arrays are never shared with the stored record. Everything else in `createBatch` is untouched — in particular the `structuredClone` of the version's rows and `declaredAxes`, which is the snapshot taken once and never retaken, and which keeps the coefficient snapshot per ingredient and never per portion (D-04).
- `hasAsMade(batch, rowId)` is unchanged: an own-property check on the row, never truthiness.
- `asMadeFor(batch, rowId)` returns the stored array, or null when the row key is absent — never the plan's numbers. Update its doc comment to say array.
- Add `asMadeForPortion(batch, rowId, index)` returning the written value at that index, or null when the row key is absent or the element is null or absent. Use the same explicit-null discipline the rest of the module uses, so a written 0 is a real value and not an absence.
- `asMadeTotals(rows, asMade)` keeps its name, its return shape and its precision contract. `planTotal` sums `rowGrams(row)`. For each row: when the row's key is present, sum per portion — each element coerced with `Number()` and used when finite, falling back to that portion's own plan amount when it is null or does not parse; when the key is absent, add `rowGrams(row)`. This is exactly the existing "the plan fills the gap, never the reverse" rule, now applied one portion at a time. Extend the function's doc comment to say so, and keep the sentence explaining why a present value is coerced before summing (the live recording draft holds as-made values as typed strings until save).

**`app/src/data/batch-2026-08-02.js`** — the churn fields' as-made map becomes, exactly: whole milk `[120, 263]` with the comment naming it as the sheet's own two lines; heavy cream `[241]`; Graza Drizzle `[45]`, keeping its "Actually 45 g, written during the churn" comment; soy lecithin `[0]`, keeping its comment that this is a real 0 distinct from an untouched row. Sucrose gets no key, and the existing comment saying the sucrose and the gums matched the plan stays exactly as written. Nothing else in the file changes — the batch is still built through `createBatch` and `addTasting`, and the fixed id and recorded-at are still fixed.
`120 + 263` is the as-made and only the as-made. The authored split of whole milk is `120 + 250.4` and is already stored in `app/src/data/olive-oil.js` by 03.2-01. Do not open that file in this task. Writing the as-made pair as the authored split would take whole milk to 383, the batch mass to 812.6, and every figure on the churned version with it (D-11).

**`app/src/store/transfer.js`** — `validateBatch`'s as-made check becomes: each value is an array whose every element is a finite number or null, reported at its own indexed path in the file's existing collect-every-error style. Nothing else in the file changes.

**Tests** — in `app/src/domain/batch.test.js`, move the as-made fixtures to arrays and add cases: `asMadeTotals` over the seeded rows with the seeded batch's as-made returns a plan total of 799.68 and an as-made total of 804.28; a row whose key is present with one written portion and one null takes the written number for the first and the plan's own amount for the second; a written 0 contributes zero rather than being treated as absent; a row with no key contributes its whole plan total; and `asMadeForPortion` returns 0 rather than null for the soy lecithin's written zero. In `app/src/store/transfer.test.js`, add cases refusing a scalar as-made value and refusing an array element that is neither a finite number nor null, each at its own indexed path.
  </action>
  <verify>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit, or any line containing "Could not resolve" or "error" in the vite output</fails_when>
    <automated>npm --prefix app test -- src/domain src/data src/store tests</automated>
    <fails_when>non-zero exit; a "Test Files" summary line reporting `0 passed`; or any failing assertion, in particular an as-made total that is not 804.28 or a plan total that is not 799.68</fails_when>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c "'row-01': \[120, 263\]" app/src/data/batch-2026-08-02.js)" -ge 1` — the sheet's own two lines, stored as two numbers.
    - `test "$(grep -c "'row-05'" app/src/data/batch-2026-08-02.js)" = 0` — sucrose carries no as-made key, because it matched the plan.
    - `test "$(grep -c "'row-09': \[0\]" app/src/data/batch-2026-08-02.js)" -ge 1` — the written zero survives as a real value.
    - `git diff --quiet HEAD -- app/src/data/olive-oil.js` exits 0 — the authored split was not touched by this task, which is the concrete guard against writing the as-made pair as the authored one.
    - `test "$(grep -cE 'export const BATCH_SCHEMA_VERSION\\s*=\\s*2' app/src/domain/batch.js)" -ge 1`.
    - `test "$(grep -c 'export function asMadeForPortion' app/src/domain/batch.js)" -ge 1`.
    - `node --input-type=module -e "import {augustSecondBatch} from './app/src/data/batch-2026-08-02.js'; import {oliveOilVersion} from './app/src/data/olive-oil.js'; import {asMadeTotals} from './app/src/domain/batch.js'; import {activeRows} from './app/src/domain/rows.js'; const t=asMadeTotals(activeRows(oliveOilVersion), augustSecondBatch.churn.asMade); if(t.planTotal.toFixed(2)!=='799.68'||t.asMadeTotal.toFixed(2)!=='804.28') {console.error(JSON.stringify(t)); process.exit(1);}"` exits 0 — both totals unmoved.
    - `node --input-type=module -e "import {augustSecondBatch} from './app/src/data/batch-2026-08-02.js'; import {asMadeForPortion,hasAsMade} from './app/src/domain/batch.js'; if(asMadeForPortion(augustSecondBatch,'row-09',0)!==0) process.exit(1); if(hasAsMade(augustSecondBatch,'row-05')) process.exit(1);"` exits 0 — a written zero is a value and an untouched row is absent.
    - `test "$(grep -c 'structuredClone(version.rows)' app/src/domain/batch.js)" -ge 1` — the snapshot is still taken once, per ingredient, and never per portion.
    - `test "$(grep -rIl 'dangerouslySetInnerHTML' app/src | wc -l | tr -d ' ')" = 0`.
  </acceptance_criteria>
  <done>The 2 Aug batch stores whole milk as `120 + 263` totalling 383, sucrose carries no key, the soy lecithin's zero is still a real value, both totals read 799.68 and 804.28, the validator gates the new shape, and the domain, data, store and `app/tests` suites are green.</done>
  <reversibility rating="costly">A stored record shape. The store is empty until reseeded, so there is nothing to migrate today, but the shape is what every later batch is written in.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The As made column reads and records per portion (D-10, D-18)</name>
  <files>app/src/ui/IngredientTable.jsx, app/src/ui/IngredientTable.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx</files>
  <read_first>
    - app/src/ui/IngredientTable.jsx — as left by 03.2-03: `AsMadeCell`, the `asMadeValue` computed in the body for the accessible name, the as-made total row, and the grams cell's own per-portion field pattern, which this task mirrors
    - app/src/ui/RecipePage.jsx — as left by 03.2-03: `isDraftDirty` and the `stringMapsDiffer` helper it uses, `handleChangeAsMade`, `handleStartRecording`'s blank draft, `handleStartAmending`'s prefill, and `handleSaveBatch`'s parse-at-save loop
    - app/src/domain/batch.js — as left by Task 1: `hasAsMade`, `asMadeFor`, `asMadeForPortion`, `asMadeTotals`
    - app/src/domain/lineage.js — `parseGramsDraft`, the one rule every draft-amount reader takes
    - .planning/phases/03.2-the-portion-and-the-reset/03.2-03-SUMMARY.md — the join-marker class and the accessible-name convention 03.2-03 settled, which this task reuses rather than reinventing
  </read_first>
  <action>
Mirror the grams cell's per-portion shape into the as-made column, and carry it through the recording and amend drafts.

**`AsMadeCell`** keeps its three-way branch and its rule that blank stays blank and the plan never leaks.
- While recording: one controlled text field per portion, in portion order, each bound to the draft's array element for that row and index (`''` when the row key is absent), each calling `onChangeAsMade(row.id, i, value)`. Use the same join marker between adjacent fields that 03.2-03 introduced for the grams cell, and the same accessible-name rule: a one-portion row keeps `${row.ingredientName}, as made, grams` exactly as it reads today, and a row with more portions names each field `${row.ingredientName}, as made, grams, portion ${i + 1}`.
- While a saved batch's layer is showing and the row has an as-made key: render each portion's stored value in portion order, joined by the same marker, followed by the unit — so whole milk reads `120 + 263 g`. A written value renders exactly as stored, with no rounding and no re-formatting (the precision contract). A portion the maker wrote nothing beside renders the table's existing absent placeholder, never that portion's plan amount.
- Otherwise: nothing, as now.

**The row's accessible name** — the `asMadeValue` the body computes for the label becomes the same joined reading, so a row reads "as made 120 + 263 g". Keep it null when the row has no as-made key.

**The as-made total row** is unchanged: `asMadeTotals` already returns the same shape and now sums per portion, so the total reads 804.3 g with no edit at the render site.

**`handleChangeAsMade(rowId, portionIndex, value)`** — writes that portion's raw string. When the row has no key yet, seed an array of empty strings the length of that row's portions first, so the array is always aligned. When every portion of a row is back to an empty string, delete the row's key — the existing "an empty field means nothing was written" rule, applied at the row level as it is today.

**`handleStartAmending`'s prefill** — a row's draft entry becomes the stored array mapped to strings, with a null element becoming an empty string, so the amend draft is still the string form the recording inputs expect.

**`isDraftDirty`'s as-made comparison** — the helper it uses today compares two maps of strings and will silently report two different arrays as equal. Extend it, or give the as-made comparison its own array-aware helper beside it, so that a change to any portion of any row reads as dirty. Keep the presence-over-truthiness discipline: a row present in one draft and absent in the other is a difference, and an element differing at any index is a difference. The blank-baseline branch's own-key count is unchanged and still correct.

**`handleSaveBatch`'s parse-at-save loop** — each portion parses through `parseGramsDraft`; a portion that parses becomes its number, and a portion that is blank or that the amount rule rejects becomes `null`, which is the same fact as a portion the maker never touched. A row whose every portion resolves to null drops its key entirely, which preserves today's rule that a row that fails to parse is treated exactly like a row that was never touched and never as a stored `NaN`. The conversion still happens here and never on a keystroke.

**Tests** — in `app/src/ui/IngredientTable.test.jsx`: a saved batch's split row reads its two as-made values joined; a split row with one written portion renders the placeholder for the other and never that portion's plan amount; recording a split row renders two as-made fields with distinct accessible names; the total row still reads the as-made total. In `app/src/ui/RecipePage.test.jsx`: typing into the second as-made field of a split row leaves the first alone and moves the as-made total; clearing every as-made field of a row removes its key at save; a value the amount rule rejects in one portion leaves the other portion's written value intact and stores null for the rejected one; and an amend prefills both portions from the stored record.
  </action>
  <verify>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit, or any line containing "Could not resolve" or "error" in the vite output</fails_when>
    <automated>npm --prefix app test -- src/ui src/domain</automated>
    <fails_when>non-zero exit; a "Test Files" summary line reporting `0 passed`; or any failing assertion, in particular an As made cell rendering a plan amount for an unwritten portion, or a stored as-made value of `NaN`</fails_when>
  </verify>
  <acceptance_criteria>
    - `test "$(grep -c 'function handleChangeAsMade(rowId, portionIndex, rawValue)' app/src/ui/RecipePage.jsx)" -ge 1` — the handler carries the portion index.
    - `test "$(grep -c 'asMadeForPortion' app/src/ui/IngredientTable.jsx)" -ge 1` — the reading goes through the domain accessor, not a bracket read.
    - `test "$(grep -c 'parseGramsDraft' app/src/ui/RecipePage.jsx)" -ge 3` — the amount rule is applied at the plan pen's two sites and the batch save's, and `Number()` is still never called on a keystroke.
    - `grep -nE 'toFixed|Math\.round' app/src/ui/IngredientTable.jsx` — read the line numbers it prints and confirm every one of them sits on the computed-total path (`formatGrams`/`formatGramsValue`/`formatShareOfBatch`), never inside `AsMadeCell` or the accessible-name builder. A written as-made value is never rounded or re-formatted (D-18). State the line numbers and their paths in the SUMMARY.
    - `test "$(grep -IcE '#[0-9a-fA-F]{3,8}|[0-9]+px|[0-9]+rem' app/src/ui/IngredientTable.jsx app/src/ui/RecipePage.jsx)" = 0`.
    - `test "$(grep -rIl 'dangerouslySetInnerHTML' app/src | wc -l | tr -d ' ')" = 0`.
    - `git diff --quiet HEAD -- app/src/data/olive-oil.js` exits 0 — the authored split is untouched by this task too.
  </acceptance_criteria>
  <done>The As made column reads `120 + 263 g` for the seeded batch's whole milk, records one field per portion with distinct accessible names, never shows a plan amount in place of a blank, never rounds a written value, and the UI and domain suites are green.</done>
  <reversibility rating="reversible">Component rendering and draft state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The three regression guards, and the phase's count reconciled (D-05, D-07, D-01)</name>
  <files>app/src/domain/axes.test.js, app/src/domain/rows.test.js, app/tests/db-reset.test.js</files>
  <read_first>
    - app/src/domain/axes.js — `CORE_AXES`, `axesForBatch`, `markKeyFor` and `setMark`, and the comment explaining why the two key namespaces cannot collide
    - app/src/domain/axes.test.js — the file being extended; its existing coverage, so the guard adds rather than duplicates
    - app/src/data/olive-oil.js — the `declaredAxes` block, READ ONLY, to confirm it already carries the object shape with real anchor strings
    - app/tests/db-reset.test.js — as left by 03.2-01 and 03.2-02; the file being extended
    - app/src/domain/rows.test.js — the file being extended
    - .planning/phases/03.2-the-portion-and-the-reset/03.2-01-SUMMARY.md, 03.2-02-SUMMARY.md, 03.2-03-SUMMARY.md — the itemised additions and deletions the final count reconciles against
  </read_first>
  <action>
Write the three guards this phase's Done-when asks for, then reconcile the arithmetic.

**Guard 1 — the six axes each take a mark independently (D-07).** In `app/src/domain/axes.test.js`, add a block asserting, against the real seeded version and the real 2 Aug batch: `axesForBatch` returns exactly six axes, the four core ones in their fixed order followed by the two the version declares; every one of the six carries a non-empty label and non-empty low and high anchor words, so a mark always has something to read against; the six mark keys from `markKeyFor` are six distinct values, with the declared axis's own name never colliding with a core key; and marking each of the six in turn through `setMark` produces a marks object holding exactly that one key, then all six independently without any of them displacing another. This is a guard on behaviour that already works — it is here so a future change cannot break it silently. Do NOT author or convert `declaredAxes`; if the assertions do not pass as written, the correct response is to report it, not to edit the seed.

**Guard 2 — the derived-total invariant (D-01).** In `app/src/domain/rows.test.js`, add a block asserting that for every row of the seeded version `rowGrams(row)` equals the sum of its portions' amounts, that no row carries a stored total under any name, and that the rule holds identically for a one-portion row and a multi-portion row. This is the companion invariant the assumption-delta checkpoint accepted in `03.2-01-PLAN.md`: it goes red the instant a future phase reintroduces a stored total beside the portions.

**Guard 3 — the batch survives the reset (D-05, D-10).** Extend `app/tests/db-reset.test.js`'s reseed case: after a profile carrying a pre-03.2-shaped record opens at the new database version and reseeds, read the batch back out through the repository seam and assert its whole milk as-made reads the two numbers `120` and `263`, that `asMadeTotals` over the reseeded version's rows and the reseeded batch's as-made returns 799.68 and 804.28, and that the reseeded batch's snapshot still carries the two declared axes in the object shape `axesForBatch` reads. That closes the Done-when clause about a profile carrying the old shape reseeding clean, all the way through to the record the maker actually opens.

**Reconcile the count.** The established green baseline before this phase was 30 files and 646 tests. Add up every test each of this phase's four SUMMARYs records as added and as deleted, write the arithmetic down in this plan's SUMMARY, and confirm the suite's reported count matches it. A count that does not reconcile means a test disappeared without a recorded reason.

**Final sweep.** Confirm the standing conventions one last time across the whole phase: one module under `app/src` imports the store library, the domain imports no framework, DOM or store, no component or stylesheet carries a visual literal, nothing renders markup, and `app/package.json` and `app/package-lock.json` are untouched by the phase.
  </action>
  <verify>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit, or a "Test Files" summary line reporting any failed file, or a "Tests" summary line whose passing count does not equal 646 plus the additions minus the deletions itemised across the 03.2-01 through 03.2-04 SUMMARYs</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit, or any line containing "Could not resolve" or "error" in the vite output</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --input-type=module -e "import {augustSecondBatch} from './app/src/data/batch-2026-08-02.js'; import {axesForBatch,markKeyFor} from './app/src/domain/axes.js'; const a=axesForBatch(augustSecondBatch); if(a.length!==6) process.exit(1); if(new Set(a.map(markKeyFor)).size!==6) process.exit(1); if(a.some(x=>!x.label||!x.low||!x.high)) process.exit(1);"` exits 0 — six axes, six distinct keys, every anchor present.
    - `node --input-type=module -e "import {oliveOilVersion} from './app/src/data/olive-oil.js'; import {rowGrams} from './app/src/domain/rows.js'; for(const r of oliveOilVersion.rows){ if(rowGrams(r)!==r.portions.reduce((t,p)=>t+p.grams,0)) process.exit(1); if('grams' in r || 'step' in r) process.exit(1); }"` exits 0 — the invariant holds and no stored total survives.
    - `npm --prefix app test` reports `Test Files` all passed and zero failed.
    - `test "$(grep -rl "from 'idb'" app/src | wc -l | tr -d ' ')" = 1`.
    - `test "$(grep -rlE "from '(react|react-dom|\.\./store/)" app/src/domain | wc -l | tr -d ' ')" = 0`.
    - `test "$(grep -rIl 'dangerouslySetInnerHTML' app/src | wc -l | tr -d ' ')" = 0`.
    - `test "$(grep -rlE 'splitStep|versionLift' app/src app/tests 2>/dev/null | wc -l | tr -d ' ')" = 0`. <!-- planner-discipline-allow: splitStep -->
    - `git diff --quiet HEAD -- app/package.json app/package-lock.json` exits 0.
    - The SUMMARY states the count arithmetic in full: 646 baseline, plus N added across the four plans, minus M deleted, equals the number the suite reports.
  </acceptance_criteria>
  <done>The six axes each take a mark independently, every row's total provably equals the sum of its portions, the reseeded batch reads its as-made `120 + 263` out of a real IndexedDB with both totals unmoved, the full suite is green, and its count reconciles against the phase's own record.</done>
  <reversibility rating="reversible">Additive tests only.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan's additions to the phase-wide inventory in `03.2-01-PLAN.md`.

**New**
- `asMadeForPortion(batch, rowId, index)` — exported from `app/src/domain/batch.js`. The written value at that portion, or null when the row key is absent or the element is null.

**Changed shapes**
- `batch.churn.asMade[rowId]`: a scalar → an array aligned index-for-index with that row's portions, each element a number or `null`. The row key is absent when nothing was written beside any of its portions.
- `asMadeFor(batch, rowId)` returns that array (or null), not a number.
- The recording draft's `asMade[rowId]`: a raw string → an array of raw strings.

**Changed signatures**
- `onChangeAsMade(rowId, portionIndex, value)` / `handleChangeAsMade(rowId, portionIndex, rawValue)` — gained the portion index.

**Changed values**
- `BATCH_SCHEMA_VERSION`: 1 → 2.
- `app/src/data/batch-2026-08-02.js`: whole milk `383` → `[120, 263]`; heavy cream `[241]`; Drizzle `[45]`; soy lecithin `[0]`; sucrose still absent.

**Unchanged, deliberately**
- `hasAsMade` — still an own-property check on the row.
- `createBatch`'s `structuredClone` of the version's rows and `declaredAxes` — the snapshot is per ingredient, never per portion (D-04), and is still taken once and never retaken.
- `app/src/data/olive-oil.js` — not opened by this plan at all; its authored split is `120 + 250.4` (D-11).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maker's as-made keystrokes → recording draft → stored batch | free text, one raw string per portion, converted only at save |
| an imported batch's `churn.asMade` → `asMadeTotals` | untrusted array shape crossing `validateBatch` first |
| a stored batch's snapshot rows → the as-made column's index alignment | the alignment contract between `asMade[rowId][i]` and `snapshot.rows[j].portions[i]` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.2-16 | Tampering | an as-made array misaligned with the row's portions | high | mitigate | A batch's snapshot rows are `structuredClone`d once at creation and never retaken, so a stored record's alignment cannot drift under it. `handleChangeAsMade` seeds an array the length of the row's portions before writing any index, so a partially-written row is never a sparse array. `validateBatch` gates every element as a finite number or null. |
| T-03.2-17 | Tampering | an unparsed as-made keystroke becoming a stored value | high | mitigate | `parseGramsDraft` runs once per portion at save; a portion that is blank or rejected becomes `null`, which reads as untouched, never as a stored `NaN`. Named explicitly in Task 2's `<fails_when>`. |
| T-03.2-18 | Information Disclosure | the plan's amount leaking into the As made column | medium | mitigate | An unwritten portion renders the table's existing absent placeholder, never that portion's plan amount. The plan fills the gap in the computed total and nowhere else. Asserted by a case in Task 2. |
| T-03.2-19 | Repudiation | a written zero read as an absence | medium | mitigate | Presence is an own-property check on the row and an explicit null test on the element, never truthiness. A runnable acceptance check asserts the soy lecithin's written zero reads as `0` and that the untouched sucrose row reads as absent. |
| T-03.2-20 | Tampering | a declared axis reaching a tasting with no anchor words | medium | mitigate | `validateBatch` refuses a malformed `declaredAxes` element at the gate (D-07a, 03.2-02), and Guard 1 asserts all six axes carry non-empty labels and anchors after a real reset and reseed. |
| T-03.2-21 | Tampering | rendering an as-made note or prose as markup | high | mitigate | Notes and prose render as text everywhere under `app/src`; every task asserts zero occurrences of `dangerouslySetInnerHTML` repository-wide. |
| T-03.2-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan or by any plan in this phase. Task 3's final sweep asserts `app/package.json` and `app/package-lock.json` are unmodified across the whole phase. |
</threat_model>

<verification>
This plan closes the phase. Its gate is the whole suite plus a reconciled count.

- `npm --prefix app test` exits 0 with every test file passing.
- The reported test count equals 646 plus the additions minus the deletions itemised across the four SUMMARYs, and the arithmetic is written down.
- `npm --prefix app run build` exits 0 with no resolve error.
- The three guards pass: six axes with six distinct keys and complete anchors; every row's total equal to the sum of its portions with no stored total anywhere; and the reseeded batch reading `120 + 263` with totals 799.68 and 804.28 out of a real IndexedDB.
- The standing conventions hold across the whole phase: one store-library importer, a framework-free domain, no visual literals, no rendered markup, no package change.
</verification>

<success_criteria>
- The 2 Aug batch's whole milk stores `[120, 263]`, reads `120 + 263 g`, and totals 383; sucrose carries no as-made key; the soy lecithin's zero is still a real value.
- The as-made total reads 804.3 g and the plan total 799.7 g — both unmoved.
- The As made column records one field per portion with distinct accessible names, never rounds a written value, and never shows a plan amount in place of a blank.
- The six tasting axes each take a mark independently, after a real reset and reseed.
- Every row's total equals the sum of its portions, asserted for a one-portion and a multi-portion row alike.
- `npm --prefix app test` passes with a reconciled count; `npm --prefix app run build` is clean.
</success_criteria>

<output>
Create `.planning/phases/03.2-the-portion-and-the-reset/03.2-04-SUMMARY.md` when done. Record: the exact as-made map the seeded batch now carries; whether any rounding call survives in `IngredientTable.jsx` and on which path; the three guards' file locations; and the phase's full count arithmetic — 646 baseline, plus N, minus M, equals the reported number.
</output>
