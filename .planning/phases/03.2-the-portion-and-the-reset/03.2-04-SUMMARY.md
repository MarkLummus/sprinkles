---
phase: 03.2-the-portion-and-the-reset
plan: 04

subsystem: domain
tags: [as-made, portions, indexeddb, vitest, batch]

requires:
  - phase: 03.2-the-portion-and-the-reset (plan 01)
    provides: "row.portions on every version row and batch snapshot row; rowGrams(row); the store reset (DB_VERSION 4)"
  - phase: 03.2-the-portion-and-the-reset (plan 02)
    provides: "transfer.js narrowed to schemaVersion 4 alone, validateRow gated on portions"
  - phase: 03.2-the-portion-and-the-reset (plan 03)
    provides: "IngredientTable.jsx and RecipePage.jsx reading row.portions in all three states"
provides:
  - "batch.churn.asMade[rowId] as an array aligned index-for-index with that row's portions; BATCH_SCHEMA_VERSION 2"
  - "asMadeForPortion(batch, rowId, index), and asMadeTotals summing per portion"
  - "the As made column reading and recording one field per portion, with distinct accessible names on a split row"
  - "the phase's three regression guards: six axes each markable independently, the derived-total invariant, and the reseeded batch's as-made/axes read back through a real IndexedDB"
  - "the phase closes at 673 tests, 30 files, a reconciled count"
affects: [03.3]

actuals:
  tokens: 10374
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A batch's as-made value moves from a per-row scalar to a per-row array aligned with that row's portions — the same shape the plan (row.portions) and the record (churn.asMade) now share, so a maker comparing the two compares like with like."
    - "formatAsMadeReading (IngredientTable.jsx) is the one join used by both the As made cell's own rendering and the row's accessible name, so the two readings can never disagree — the same shared-derivation pattern GramsCell/StepCell already established."

key-files:
  created: []
  modified:
    - app/src/domain/batch.js
    - app/src/domain/batch.test.js
    - app/src/data/batch-2026-08-02.js
    - app/src/store/transfer.js
    - app/src/store/transfer.test.js
    - app/src/ui/IngredientTable.jsx
    - app/src/ui/IngredientTable.test.jsx
    - app/src/ui/RecipePage.jsx
    - app/src/ui/RecipePage.test.jsx
    - app/src/domain/axes.test.js
    - app/src/domain/rows.test.js
    - app/tests/db-reset.test.js

key-decisions:
  - "AsMadeCell's reading-state join uses a plain ' + ' string join (formatAsMadeReading) rather than wrapping each portion in a styled .ingredient-table__split-step span the way GramsCell/StepCell do for their editable/live fields — the reading state is already homogeneous ink-text with no pen-blue draft to visually separate from, so the extra span nesting bought nothing. Documented here since Task 2's action text says 'joined by the same marker', which this reads as the same joining string, not necessarily the same DOM wrapper."
  - "asMadeMapsDiffer replaces stringMapsDiffer in place (renamed, not added alongside) — the function had exactly one caller, so leaving the old name behind unused would have been an orphan per this project's surgical-changes discipline."
  - "The pre-existing 'sums the 2 Aug as-made entries, counting the written 0 on row-09' test in batch.test.js was rewritten to read augustSecondBatch.churn.asMade and oliveOilVersion.rows directly, rather than adding a second, separate test for the same fact Task 1's action text asked for ('asMadeTotals over the seeded rows with the seeded batch's as-made returns 799.68/804.28') — the existing test's hand-rolled fixture already matched the real seeded shape once updated to arrays, so rewriting it in place was more honest than duplicating it."
  - "Guard 3 (the reseeded batch's as-made and axes) extends the existing 'seedIfEmpty writes the seed back after the reset' test in db-reset.test.js rather than adding a new one, per the plan's own wording ('Extend db-reset.test.js's reseed case') — seedIfEmpty already writes both the version and the batch, so the same test that proves the six figures unmoved is the natural place to also prove the batch reads back correctly."

requirements-completed: [D-01, D-04, D-05, D-07, D-10, D-11]

coverage:
  - id: D1
    description: "A batch's churn.asMade[rowId] is an array aligned index-for-index with that row's portions; the 2 Aug batch stores whole milk as [120, 263] (383 total), soy lecithin's written zero as [0], and sucrose carries no key at all"
    requirement: D-10
    verification:
      - kind: unit
        ref: "app/src/domain/batch.test.js#createBatch, the blank/zero/plan-never-leaks discipline, asMadeTotals"
        status: pass
      - kind: other
        ref: "node --input-type=module check: asMadeTotals over the real seeded rows/batch returns planTotal 799.68, asMadeTotal 804.28"
        status: pass
    human_judgment: false
  - id: D2
    description: "asMadeForPortion(batch, rowId, index) returns the written value at that portion, or null when absent — a written 0 reads back as 0, never as absent"
    requirement: D-11
    verification:
      - kind: unit
        ref: "app/src/domain/batch.test.js#asMadeForPortion, the blank/zero/plan-never-leaks discipline"
        status: pass
      - kind: other
        ref: "node --input-type=module check: asMadeForPortion(augustSecondBatch,'row-09',0)===0; hasAsMade(augustSecondBatch,'row-05')===false"
        status: pass
    human_judgment: false
  - id: D3
    description: "transfer.js's validateBatch gates each as-made value as an array whose every element is a finite number or null, reported at its own indexed path; a scalar value is refused at the row's own path"
    requirement: D-10
    verification:
      - kind: unit
        ref: "app/src/store/transfer.test.js#validateStoreFile — accepts a written 0/null element, rejects a scalar value, rejects a non-numeric/non-null element"
        status: pass
    human_judgment: false
  - id: D4
    description: "The As made column renders one field per portion while recording (distinct accessible names on a split row) and each portion's stored value joined by ' + ' while reading a saved batch's layer; an unwritten portion within a written row renders blank, never that portion's plan amount"
    requirement: D-18
    verification:
      - kind: unit
        ref: "app/src/ui/IngredientTable.test.jsx#the As made column reads and records per portion (D-10, D-18)"
        status: pass
    human_judgment: false
  - id: D5
    description: "handleChangeAsMade carries a portionIndex and seeds/collapses the row's array correctly; handleStartAmending prefills per portion; handleSaveBatch parses each portion independently, dropping the row's key only when every portion resolves to null; isDraftDirty's array-aware comparison catches a single-portion change"
    requirement: D-10
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx#isDraftDirty — the churn check, against what it was filled from (T-03-43)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Guard 1: the six tasting axes each take a mark independently, against the real seeded version and batch — six distinct keys, complete anchors, each markable alone and all six together"
    requirement: D-07
    verification:
      - kind: unit
        ref: "app/src/domain/axes.test.js#the six-axes independence guard (phase Done-when, D-07)"
        status: pass
      - kind: other
        ref: "node --input-type=module check: axesForBatch(augustSecondBatch).length===6, six distinct markKeyFor values, every axis carries label/low/high"
        status: pass
    human_judgment: false
  - id: D7
    description: "Guard 2: every row's total equals the sum of its portions, identically for a one-portion and multi-portion row, with no stored total surviving under any name"
    requirement: D-01
    verification:
      - kind: unit
        ref: "app/src/domain/rows.test.js#the derived-total invariant (D-01, companion to the 03.2-01 assumption-delta checkpoint)"
        status: pass
      - kind: other
        ref: "node --input-type=module check: rowGrams(r)===portions sum and no 'grams'/'step' key on any seeded row"
        status: pass
    human_judgment: false
  - id: D8
    description: "Guard 3: a profile carrying a pre-03.2-shaped record reseeds clean, and the reseeded batch's as-made and axes read correctly out of a real IndexedDB through the repository seam"
    requirement: D-05
    verification:
      - kind: integration
        ref: "app/tests/db-reset.test.js#seedIfEmpty writes the seed back after the reset, and the six figures and mass read unmoved"
        status: pass
    human_judgment: false
  - id: D9
    description: "The full suite is green with a reconciled count: 646 baseline, +10/-4/+5 across 03.2-01 through 03.2-03, +16 this plan, equals 673"
    verification:
      - kind: other
        ref: "npm --prefix app test — 673 passed, 0 failed, 30 files"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-10
status: complete
---

# Phase 03.2 Plan 04: The Portion And The Reset Summary

**A batch's as-made record moves from a per-row scalar to a per-row array aligned with that row's portions — the 2 Aug batch's whole milk now stores `[120, 263]` and reads `120 + 263 g`, matching the plan's own shape — and the phase closes with three regression guards (six independent axes, the derived-total invariant, a reseeded batch read out of a real IndexedDB) at a reconciled 673 tests.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-10T00:00:29-04:00 (prior commit)
- **Completed:** 2026-09-10T00:15:46-04:00
- **Tasks:** 3
- **Files modified:** 12

## Accomplishments

- `app/src/domain/batch.js`: `BATCH_SCHEMA_VERSION` moved 1 → 2; `createBatch`'s as-made copy loop now shallow-copies each row's own array; `asMadeForPortion(batch, rowId, index)` added; `asMadeTotals` sums per portion, falling back to that portion's own plan amount when its element is null, blank, or does not parse.
- `app/src/data/batch-2026-08-02.js`: the 2 Aug batch's as-made map becomes `{'row-01': [120, 263], 'row-02': [241], 'row-03': [45], 'row-09': [0]}`; sucrose still carries no key.
- `app/src/store/transfer.js`: `validateBatch`'s as-made check gates each value as an array whose every element is a finite number or null, reported at its own indexed path; a scalar value is refused at the row's own path.
- `app/src/ui/IngredientTable.jsx`: `AsMadeCell` renders one text field per portion while recording (with distinct accessible names on a split row) and each portion's stored value joined by `' + '` while reading a saved batch's layer, through a new shared `formatAsMadeReading` helper; an unwritten portion within a written row renders blank, never the plan amount.
- `app/src/ui/RecipePage.jsx`: `handleChangeAsMade` carries a `portionIndex`, seeding/collapsing the row's array as needed; `handleStartAmending` prefills per portion; `handleSaveBatch` parses each portion independently; `isDraftDirty`'s as-made comparison (renamed `asMadeMapsDiffer`) is array-aware.
- `app/src/domain/axes.test.js`, `app/src/domain/rows.test.js`, `app/tests/db-reset.test.js`: the three phase-closing regression guards (six axes independently markable; the derived-total invariant; the reseeded batch's as-made and axes read correctly out of a real IndexedDB).

## Task Commits

Each task was committed atomically:

1. **Task 1: As-made becomes a list, aligned with the plan it records (D-10, D-11)** — `7dafba3` (feat)
2. **Task 2: The As made column reads and records per portion (D-10, D-18)** — `42efbb4` (feat)
3. **Task 3: The three regression guards, and the phase's count reconciled (D-05, D-07, D-01)** — `fbc07cb` (test)

## Files Created/Modified

- `app/src/domain/batch.js` — `BATCH_SCHEMA_VERSION` 2; `createBatch`'s array copy; `asMadeForPortion` added; `asMadeTotals` rewritten per portion.
- `app/src/domain/batch.test.js` — as-made fixtures moved to arrays throughout; new `asMadeTotals`/`asMadeForPortion` cases; `augustSecondBatch`'s expected as-made map updated.
- `app/src/data/batch-2026-08-02.js` — the churn fields' as-made map moved to arrays.
- `app/src/store/transfer.js` — `validateBatch`'s as-made check rewritten as an array-of-(number|null) gate.
- `app/src/store/transfer.test.js` — as-made fixtures moved to arrays; new scalar-rejection and null-element-acceptance cases; the non-numeric-element case renamed to name its own indexed path.
- `app/src/ui/IngredientTable.jsx` — `formatAsMadeReading` added; `AsMadeCell` rewritten per portion; the row's accessible-name computation updated.
- `app/src/ui/IngredientTable.test.jsx` — `makeBatch({ a: 38 })` fixture moved to array form; four new cases for the split-row per-portion reading/recording/total behaviour.
- `app/src/ui/RecipePage.jsx` — `stringMapsDiffer` renamed `asMadeMapsDiffer` and made array-aware; `handleChangeAsMade` gained `portionIndex`; `handleStartAmending`/`handleSaveBatch` updated for the array shape.
- `app/src/ui/RecipePage.test.jsx` — `makeAmendBaseline`'s as-made fixture moved to arrays; one new array-index-diff dirty-check case.
- `app/src/domain/axes.test.js` — new `describe('the six-axes independence guard...')` block, three cases.
- `app/src/domain/rows.test.js` — new `describe('the derived-total invariant...')` block, three cases.
- `app/tests/db-reset.test.js` — the reseed case extended to read the batch back and assert its as-made/axes.

## Decisions Made

See `key-decisions` in the frontmatter: the reading-state join uses a plain string join rather than a styled span wrapper; `stringMapsDiffer` was renamed in place rather than orphaned; the "seeded batch's as-made totals" case was rewritten in place rather than duplicated; Guard 3 extends the existing reseed test rather than adding a new one.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test-infrastructure] Task 2's acceptance criterion for the rounding-call grep did not account for zero matches**
- **Found during:** Task 2 self-check
- **Issue:** The task's acceptance criterion runs `grep -nE 'toFixed|Math\.round' app/src/ui/IngredientTable.jsx` and asks to confirm every reported line sits on the computed-total path. The command returns no lines at all — `IngredientTable.jsx` calls `formatGrams`/`formatGramsValue`/`formatShareOfBatch` (which live in `composition.js`) but contains no literal `toFixed`/`Math.round` call itself — so the criterion is vacuously satisfied (there is nothing outside the computed-total path, because there is nothing at all).
- **Fix:** No code change. Stating explicitly, as the criterion requires: **zero occurrences** of `toFixed`/`Math.round` in `IngredientTable.jsx`; all rounding lives in `app/src/domain/composition.js`'s `formatGrams`/`formatGramsValue`/`formatShareOfBatch`, called from `IngredientTable.jsx` only at the total-row/share-cell path, never inside `AsMadeCell` or `formatAsMadeReading`.
- **Files modified:** none
- **Verification:** `grep -nE 'toFixed|Math\.round' app/src/ui/IngredientTable.jsx` — no output.
- **Committed in:** n/a (no code change)

**2. [Rule 1 - Test-infrastructure] Task 2's two-file `grep -Ic` acceptance criterion doesn't collapse to a single number**
- **Found during:** Task 2 self-check
- **Issue:** The criterion `test "$(grep -IcE '#[0-9a-fA-F]{3,8}|[0-9]+px|[0-9]+rem' app/src/ui/IngredientTable.jsx app/src/ui/RecipePage.jsx)" = 0` fails as literally written: `grep -c` with two file arguments prints a `filename:count` line per file (`app/src/ui/IngredientTable.jsx:0` and `app/src/ui/RecipePage.jsx:0`), and the two-line string is never equal to the bare string `0`.
- **Fix:** No code change — confirmed the actual intent (zero visual literals in both files) via `grep -hIcE ... file1 file2`, which suppresses the filename prefix and printed `0` for both files individually.
- **Files modified:** none
- **Verification:** `grep -hIcE '#[0-9a-fA-F]{3,8}|[0-9]+px|[0-9]+rem' app/src/ui/IngredientTable.jsx app/src/ui/RecipePage.jsx` → `0` / `0`.
- **Committed in:** n/a (no code change)

---

**Total deviations:** 2 (both documented acceptance-criterion mismatches with no code change, matching this phase's own 03.2-01 precedent for grep-counting artifacts). **Impact:** neither reflects a gap in the delivered code — both are confirmed correct by a corrected equivalent check, recorded here so a reviewer isn't surprised by a literal criterion command that fails for reasons unrelated to the work.

## Test count reconciliation

Baseline before this phase (03.2-03's own measurement): **30 files, 657 tests, all passing.**

- **03.2-01:** +10 net (measured in 03.2-01/03.2-03's own SUMMARYs).
- **03.2-02:** −4 net (measured in 03.2-03's own SUMMARY).
- **03.2-03:** +5 net (measured in 03.2-03's own SUMMARY). Running total after 03.2-03: 646 + 10 − 4 + 5 = **657**.
- **03.2-04 (this plan):** +16 added, 0 deleted —
  - `app/src/domain/batch.test.js`: +3 (`asMadeTotals` one-written-one-null case; no-key-whole-plan case; new `describe('asMadeForPortion')` block)
  - `app/src/store/transfer.test.js`: +2 (null-element-acceptance case; scalar-value-rejection case)
  - `app/src/ui/IngredientTable.test.jsx`: +4 (split-row joined reading; placeholder-for-untouched-portion; recording split fields with distinct names; total row still reads the as-made total)
  - `app/src/ui/RecipePage.test.jsx`: +1 (array-index-diff dirty-check case)
  - `app/src/domain/axes.test.js`: +3 (the six-axes independence guard block)
  - `app/src/domain/rows.test.js`: +3 (the derived-total invariant block)
  - `app/tests/db-reset.test.js`: +0 (existing reseed case extended in place, no new `it()`)

**Arithmetic: 646 + 10 − 4 + 5 + 16 = 673.** `npm --prefix app test` reports 673 passed, 0 failed, across 30 files — reconciled exactly.

## Issues Encountered

None beyond the deviations documented above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- The whole `app/src` suite is green (673/673) and the build is clean. This is the last plan of Phase 03.2; the phase is complete.
- `03.3` (the front-matter rows and the page in step order) inherits: `asMadeForPortion` and the per-portion as-made shape are now the stable contract any future as-made reader/writer builds against; `app/src/data/olive-oil.js`'s authored split (`120 + 250.4`) was untouched by this plan across all three tasks, confirmed by `git diff --quiet` at every task.
- No stubs, no skipped tests, no unrun `<verify>` blocks — nothing added to `.planning/WINDOWS.md`.

## Self-Check: PASSED

- Modified files verified present on disk: `app/src/domain/batch.js`, `app/src/ui/IngredientTable.jsx`, `app/tests/db-reset.test.js`, `.planning/phases/03.2-the-portion-and-the-reset/03.2-04-SUMMARY.md`.
- Task commits verified in `git log --oneline`: `7dafba3`, `42efbb4`, `fbc07cb`.
- Plan-level `<verification>` re-run: `npm --prefix app test` (30 files, 673 passed, 0 failed); `npm --prefix app run build` (clean, 0 resolve errors); the three guards' node checks (axes, rows) both pass; `git diff --quiet HEAD -- app/src/data/olive-oil.js` (no diff); `git diff --quiet HEAD -- app/package.json app/package-lock.json` (no diff); `grep -rl "from 'idb'" app/src` (one file, `app/src/store/db.js`); `grep -rlE "from '(react|react-dom|\.\./store/)" app/src/domain` (empty); `grep -rIl 'dangerouslySetInnerHTML' app/src` (empty); `grep -rlE 'splitStep|versionLift' app/src app/tests` (empty).
- All Task 1, Task 2 and Task 3 acceptance criteria re-run individually via shell — all pass (two documented grep-command artifacts noted above).

---
*Phase: 03.2-the-portion-and-the-reset*
*Completed: 2026-09-10*
