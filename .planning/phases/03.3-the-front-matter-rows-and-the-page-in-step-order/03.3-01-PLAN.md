---
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/ui/VersionRow.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/VersionRow.test.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/Versions.jsx
  - app/src/ui/BatchMargin.jsx
  - app/src/ui/Versions.test.jsx
  - app/src/ui/BatchMargin.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/Authored.jsx
  - app/src/ui/Authored.test.jsx
  - app/src/ui/Method.jsx
  - app/src/ui/Method.test.jsx
  - app/src/styles/app.css
  - app/src/styles/tokens.css
autonomous: false
requirements: []

estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The front matter reads as two full-width stacked rows in column one — the version's row, then the batch's row — each with its own internal layout, and neither carries a page-level (bookcloth) running head.
    - The record's content and controls (the three measured churn fields, tasting reading/writing, Record another/Amend/Add tasting, the batch list) live entirely in the batch row; none of it remains in column two.
    - Column two carries no "Notes"-titled wrapper region; Balance is followed directly by the derived advisories block, then the carried-forward notes.
    - Before you start notes render at the head of the Method, before step 1, and no longer appear inside the Authored block.
    - The three named blank prose fields (draw notes, ingredient notes, next-time) carry a hairline rule when empty and no visible label word.
    - A second consecutive blocked save on the version-line field moves focus into that field again, not only the first blocked press.
    - Saving or cancelling any of the four pens (plan, record, amend, tasting) still returns focus to that pen's own opener button.
    - Every new CSS rule this plan adds reads its spacing, weight, size and colour values through an existing or newly-declared token in tokens.css — no literal survives in app.css.
  artifacts:
    - app/src/ui/VersionRow.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/VersionRow.test.jsx
    - app/src/ui/BatchRow.test.jsx
    - app/src/ui/RecipePage.jsx
    - app/src/ui/Authored.jsx
    - app/src/ui/Method.jsx
    - app/src/styles/app.css
    - app/src/styles/tokens.css
  key_links:
    - RecipePage.jsx's blockedAttemptRef increments inside buildPenFields' blocked branch and is stamped onto blockedTarget.attempt, which VersionRow.jsx reads as versionLineBlockedAttempt and keys its focus-return useEffect on (WR-01 fix).
    - RecipePage.jsx's version.authored.beforeYouStart / penDraft.authored.beforeYouStart now reaches Method.jsx as a beforeYouStart prop, the same way version.authored.carriedForward already reaches Authored.jsx.
    - Method.jsx imports the exported NoteList from Authored.jsx rather than re-implementing the on-demand note-editing shape a second time.
---

<objective>
Split `Versions.jsx` into two front-matter row modules — `VersionRow.jsx` (the version's row) and `BatchRow.jsx` (the batch's row, absorbing `BatchMargin.jsx`'s content wholesale, which is then deleted) — and rework `.recipe-band` from one side-by-side row into two stacked full-width rows with no page-level running head on either. Retire the margin as a titled region: column two keeps only Balance, the derived advisories block, and the carried-forward notes, in that order, with no wrapping "Notes" heading. Move "Before you start" out of `Authored.jsx` to head `Method.jsx`, before step 1. Rule the three now-relocated blank prose fields (draw notes, ingredient notes, next-time) with a hairline baseline and no label (03.1 Gap 2 override). Relocate the four Save-returns-focus `useRef`/`useEffect` pairs to whichever new module owns each opener button (03.1 Gap 3 / D-27), and fix the version-line blocked-save effect to key on an incrementing attempt counter instead of a value-equal boolean (03.1 REVIEW.md WR-01's `Versions.jsx` half). Land a deferred `checkpoint:human-verify` for the fork's landing focus (LD-03) as the plan's last task, after all automated verification, so execution continues past it.

This plan is Phase 03.3's tracer: it proves the page's new front-matter reading shape end-to-end — every layer the phase touches (CSS grid, the two new components, RecipePage's wiring, the test suite) — before any later plan in this phase builds on it (the table's step-grouping, the method's read-only pen, the label pass).

Purpose: Phase 03.1 and 03.2 left the front-matter split and the margin's retirement as accepted, undelivered overrides (03.1-VERIFICATION.md § Overrides Accepted). This plan is their landing place, built against sketch 003 variant B, the design source of truth Mark confirmed 2026-09-09.
Output: `VersionRow.jsx`, `BatchRow.jsx` (new), `Versions.jsx` and `BatchMargin.jsx` (deleted), `RecipePage.jsx`/`Authored.jsx`/`Method.jsx` rewired, `app.css`/`tokens.css` extended, a deferred human-verify checkpoint for the fork's landing focus.

**Spec-less probe note:** No SPEC.md exists and this phase carries no REQUIREMENTS.md IDs (ROADMAP: "Requirements: None new"), so the spec-less probe fallback was skipped for this phase — recorded here, not silently. `must_haves.truths` above are derived from the ROADMAP Scope bullets this plan owns (bullets 1 and 2, including the "Before you start" move, and bullet 6's Gap 3/D-27 and WR-01 halves), the 03.1-VERIFICATION.md § Overrides Accepted carries, and LD-03.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/sketches/003-front-matter-rows/README.md
@.planning/sketches/003-front-matter-rows/index.html
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-RESEARCH.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md
@.planning/phases/03.1-the-imprint-and-the-binder/03.1-VERIFICATION.md
@app/src/ui/Versions.jsx
@app/src/ui/BatchMargin.jsx
@app/src/ui/RecipePage.jsx
@app/src/ui/Authored.jsx
@app/src/styles/app.css
@app/src/styles/tokens.css
</context>

<tasks>

<!-- planner-discipline-allow: className="region-name" -->
<!-- The literal above is checked absent from VersionRow.jsx/BatchRow.jsx (Task 1's <verify>) while
     the SAME class is deliberately reused in Method.jsx (Task 2, a different, ungated file) for its
     own "Before you start" sub-label — both mentions are real, correct instructions, not a comment
     echo of forbidden code. -->

<task type="tracer">
  <name>Task 1: Split Versions.jsx into VersionRow.jsx and BatchRow.jsx, stack the front-matter rows, relocate focus-return, fix WR-01</name>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (variant B, the `.row-version`/`.row-batch`/`.cells`/`.cell` markup and CSS, lines 65-155 and the HTML body from `<header class="front">` to `</header>`, lines 201-264)
    .planning/sketches/003-front-matter-rows/README.md (§ "Decisions carried", § "The jobs, and where each one lives now")
    app/src/ui/Versions.jsx (full file — every ref/effect pair, every branch of the `openPen` chain, the lineage block, the batch list)
    app/src/ui/BatchMargin.jsx (full file — the three states: recording, openBatch, fallback)
    app/src/ui/RecipePage.jsx (lines 1-30 imports; 296-306 state incl. `blockedTarget`; 1036-1099 `buildPenFields`/`handleSaveAsNewVersion`; 1120-1271 the render, especially the `recipe-band` div and the `margin-region` aside)
    app/src/styles/app.css (lines 229-262 `.recipe-page`/`.recipe-band`; lines 368-478 the `.versions__*` rules; lines 1114-1178 the `.batch-margin__*` rules)
    app/src/styles/tokens.css (full file — every token this task may reuse)
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md (§ "app/src/ui/Versions.jsx split", § "New batch-row module")
  </read_first>
  <files>app/src/ui/VersionRow.jsx, app/src/ui/BatchRow.jsx, app/src/ui/VersionRow.test.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/Versions.jsx, app/src/ui/BatchMargin.jsx, app/src/ui/Versions.test.jsx, app/src/ui/BatchMargin.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/styles/app.css, app/src/styles/tokens.css</files>
  <action>
    Create `app/src/ui/VersionRow.jsx` exporting `VersionRow`, carrying everything from `Versions.jsx` EXCEPT the batch-specific pieces named below. Wrap it in `<section className="version-row" aria-label="Version">` (not `<section className="versions">`) and drop the `<h2 className="region-name">Versions</h2>` line entirely — this is the "no running head" cut (ROADMAP Scope bullet 1). Keep every other class name unchanged (`versions__openers`, `versions__opener-group`, `versions__ceremony`, `versions__hint`, `versions__lineage*`, `versions__batch-amended`) — these are BEM-style utility classes with no ancestor-selector dependency, so they work unmoved. `VersionRow` keeps: the plan-pen ceremony (`openPen === 'plan'` branch, lines 133-217 of the current file, including the `versionLineFieldRef`), the `openPen === null` branch's Develop button ONLY (drop the `Record another`/`Amend`/`Add tasting` group from this branch — those move to BatchRow), the `versions__hint` line (unconditional on `openPen` truthy, exactly as today — it must still render while ANY pen is open, including the batch's three pens, since it explains why THIS row's own links are suppressed), the `VersionStrip` render, and the whole lineage block (Parent/Batch/Reason/Show changes). `VersionRow` still needs the `batches` prop for `citableBatches(batches)` inside the plan-pen's "Cites" select (route-recipe-version.md's own citation field never left the plan pen).

    Relocate `developButtonRef`/`wasDevelopingRef` and their `useEffect` (keyed on `mode`) verbatim into `VersionRow.jsx` — this is the one ref pair VersionRow owns. Relocate the `versionLineFieldRef` and its blocked-save `useEffect` too, but change what it keys on: rename the incoming prop from `versionLineBlocked` (boolean) to `versionLineBlockedAttempt` (a number or `null`), and change the effect's condition/dependency from `if (versionLineBlocked) ...`, `[versionLineBlocked]` to `if (versionLineBlockedAttempt != null) ...`, `[versionLineBlockedAttempt]` — an incrementing number differs on every render where a blocked save fires, even two consecutive presses on the same field, which a value-equal boolean cannot (WR-01's root cause, RESEARCH.md "The Pen's Read-Only Method", `Versions.jsx:126-128`).

    Create `app/src/ui/BatchRow.jsx` exporting `BatchRow`, merging `BatchMargin.jsx`'s entire body with `Versions.jsx`'s batch-specific pieces. Wrap it in `<section className="batch-row" aria-label="Batch">` — also no `region-name` heading. At the top, replicate the SAME four-way `openPen` branch pattern VersionRow uses, but for BatchRow's own three pen states: `openPen === 'record' || openPen === 'amend'` renders the churn-date ceremony (moved from `Versions.jsx` lines 218-244), `openPen === 'tasting'` renders the tasting ceremony (moved from lines 245-272), `openPen === null` renders the `Record another`/`Record batch`, `Amend`, `Add tasting` opener group (moved from lines 295-309, keeping the `openBatch ? 'Record another' : 'Record batch'` and the `openBatch &&` guards on Amend/Add tasting exactly as written), and when `openPen === 'plan'` (a state BatchRow does not own) render nothing at the top — mirroring today's single chain, where opening any one pen hides every opener, not just its own. Add the same `{openPen && <p className="versions__hint">Links return after you save or cancel.</p>}` line here too, independently of VersionRow's copy — each row explains the suppression of its own links (the batch list's links below).

    Relocate `recordButtonRef`/`wasRecordingRef`, `amendButtonRef`/`wasAmendingRef`, and `addTastingButtonRef`/`wasTastingRef` and their three `useEffect`s (each keyed on `openPen`) verbatim into `BatchRow.jsx`.

    Below the opener/ceremony section, render `BatchMargin.jsx`'s content but restructured so the "Batch" legend prints once, not three times: `<div className="batch-margin"><p className="batch-margin__legend">Batch</p>{mode === 'recording' ? (...) : openBatch ? (...) : (...)}</div>` — the three inner branches are `BatchMargin.jsx`'s existing recording-mode fields (come-up, draw temperature, overrun, then the three prose fields — leave their markup untouched in this task, Task 2 adds the hairline-empty class), the existing `openBatch` reading branch (amendment line, measured fields, prose text, tastings, tasting form), and the existing fallback ("no batch yet" / "No batch of this version has that address."). Below `.batch-margin`, render the batch list exactly as `Versions.jsx` lines 366-390 have it (`ul.batch-margin__list`, `sortedBatches`, the `Link`/plain-text split on `isOpenBatch || openPen`), reading `version.id` for the href — the one place `BatchRow` needs the `version` prop, replacing `BatchMargin.jsx`'s own unused `version` prop.

    For the churn's three reading-state measured values (`Come-up, min` / `Draw temperature, °C` / `Overrun, %`, currently three `<p className="batch-margin__measured">` lines) and for `TastingReading`'s own measured/mark lines (tasting temperature, each axis mark, meltdown), replace the `batch-margin__measured` list rendering with a new cells grid, matching sketch 003 variant B's `.cells`/`.cell .k`/`.cell .v` pattern ("measured values as labelled cells at figure size" — README § "What to Look For"): wrap each group in `<div className="batch-row__cells">`, each value in `<div className="batch-row__cell"><span className="batch-row__cell-label">{label}</span><span className="batch-row__cell-value">{value}</span></div>`. Leave the recording-state editable fields (`batch-margin__field` label+input) and the prose lines (`prose-text`) untouched — only the READ-ONLY measured rows move to the cells pattern. `TastingReading` becomes a function inside `BatchRow.jsx` (moved from `BatchMargin.jsx`), keeping its own `batch-margin__legend` date heading and prose lines, only its measured/mark rows converting to `batch-row__cells`.

    In `app/src/styles/tokens.css`, add one new token in a new commented section near the end of the file: `--col-batch-cell-min: 96px;` (sketch 003 variant B's `minmax(96px, max-content)`, the batch row's measured-cell grid minimum). In `app/src/styles/app.css`, add: `.batch-row__cells { display: grid; grid-template-columns: repeat(auto-fit, minmax(var(--col-batch-cell-min), max-content)); gap: var(--gap-s) var(--gap-l); margin: var(--gap-s) 0 0; }`, `.batch-row__cell { display: flex; flex-direction: column; gap: var(--gap-hair); }`, `.batch-row__cell-label { font-family: var(--face-grotesk); font-size: var(--size-small-print); text-transform: uppercase; letter-spacing: 0.04em; }`, `.batch-row__cell-value { font-family: var(--face-grotesk); font-size: var(--size-figure-value); font-weight: 700; color: var(--pen-blue); font-variant-numeric: tabular-nums; }` — every value a `var(--...)` reference, no literal.

    Rework `.recipe-band` (app.css lines 246-252): change `display: grid; grid-template-columns: minmax(0, var(--measure-prose)) 1fr;` to `display: flex; flex-direction: column; gap: var(--gap-m);` and add `border-bottom: var(--rule-baseline) solid var(--ink); padding-bottom: var(--gap-m);` — the baseline-weight rule closing the front matter above Ingredients (README "Outcome (2026-09-09)"). Add a new rule `.recipe-band__row-version { display: grid; grid-template-columns: minmax(0, var(--measure-prose)) 1fr; gap: var(--gap-l); align-items: start; padding-bottom: var(--gap-m); border-bottom: var(--rule-graduation) solid var(--ink); }` — this carries forward the ORIGINAL two-column split (unchanged proportions) and adds the graduation-weight rule separating row one from row two. Update the doc comment above `.recipe-band` (lines 241-245) to describe the new stacked-rows shape instead of the retired side-by-side one.

    In `app/src/ui/RecipePage.jsx`: change the imports at lines 24 and 26 from `BatchMargin`/`Versions` to `BatchRow` (`from './BatchRow.jsx'`) and `VersionRow` (`from './VersionRow.jsx'`); add `useRef` to the React import on line 1. Add `const blockedAttemptRef = useRef(0);` immediately after the `blockedTarget` state declaration (near line 306). In `buildPenFields` (lines 1046-1058), inside the `if (message)` branch, increment `blockedAttemptRef.current += 1;` before calling `setBlockedTarget`, and stamp the new value onto both branches: `setBlockedTarget(rowId ? { kind: 'row', rowId, attempt: blockedAttemptRef.current } : { kind: 'versionLine', attempt: blockedAttemptRef.current });`. Rewrite the JSX at lines 1136-1181: wrap `<Headnote>` and the new `<VersionRow>` in `<div className="recipe-band__row-version">`, then render `<BatchRow>` as `.recipe-band`'s second direct child (a sibling of that wrapper div, not nested inside it). Pass `VersionRow` its props (`version`, `versions`, `mode`, `penDraft`, `batches`, `versionIdsWithBatches`, `citedBatch`, `parentVersion`, `showingChanges`, `openPen`, `penReason`, `canSaveOver`, `penHint`, `versionLineBlockedAttempt={blockedTarget?.kind === 'versionLine' ? blockedTarget.attempt : null}`, `onStartDeveloping`, `onCancelDeveloping`, `onChangePenField`, `onSaveAsNewVersion`, `onSaveOverVersion`, `onToggleShowChanges`, `focusDevelopOnMount`) and `BatchRow` its own (`version`, `batches`, `openBatch`, `mode`, `draft`, `onChangeChurnField`, `tastingDraft`, `onChangeTastingField`, `onChangeTastingMark`, `openPen`, `penReason`, `penSaveDisabled`, `penHint`, `onStartRecording`, `onStartAmending`, `onChangeChurnDate`, `onCancelRecording`, `onSaveBatch`, `onStartTasting`, `onUseAsExpectedShortcut`, `onSaveTasting`, `onCancelTasting`).

    Delete `app/src/ui/Versions.jsx`, `app/src/ui/BatchMargin.jsx`, `app/src/ui/Versions.test.jsx`, `app/src/ui/BatchMargin.test.jsx`. Write `app/src/ui/VersionRow.test.jsx` and `app/src/ui/BatchRow.test.jsx` so the combined coverage at least matches what the four deleted/rewritten files covered: `VersionRow.test.jsx` covers the Develop opener's ref/effect focus-return, the WR-01 fix (two consecutive blocked saves on the version-line field both move focus — assert the effect re-fires by driving `versionLineBlockedAttempt` through two distinct non-null values in sequence, not by asserting on a live browser), the plan ceremony's fields and save gate, the lineage lines' link-suppression while `openPen` is truthy (including a batch pen open, not just the plan pen), and the version list. `BatchRow.test.jsx` covers the three remaining ref/effect pairs' focus-return, the record/amend/tasting ceremonies, the batch list (including the `isOpenBatch`/`openPen` link-suppression), the measured-cells rendering (both the churn triple and a tasting's own measured/mark rows use `batch-row__cells`/`batch-row__cell`), and the recording-mode fields untouched from `BatchMargin.test.jsx`. Update `RecipePage.test.jsx`'s import (line 18, `from './Versions.jsx'`) and its `renderVersionsReading` helper (lines 96-161, the "four-pen matrix") to import and exercise `VersionRow` (Develop's own absence-while-any-pen-open coverage) and, if the same matrix logic applies to the batch openers, `BatchRow` — update any assertion elsewhere in this file that expects a `region-name` heading reading "Versions" or an `aside`/`aria-label="Notes"` wrapper (see Task 2 for the latter's removal).
  </action>
  <acceptance_criteria>
    - `app/src/ui/VersionRow.jsx` exists, exports `VersionRow`, and contains zero occurrences of `className="region-name"` (grep -c returns 0).
    - `app/src/ui/BatchRow.jsx` exists, exports `BatchRow`, and contains zero occurrences of `className="region-name"` (grep -c returns 0).
    - `app/src/ui/Versions.jsx`, `app/src/ui/BatchMargin.jsx`, `app/src/ui/Versions.test.jsx`, `app/src/ui/BatchMargin.test.jsx` no longer exist (`git ls-files -- app/src/ui/Versions.jsx app/src/ui/BatchMargin.jsx` returns nothing).
    - `app/src/ui/RecipePage.jsx` contains `<div className="recipe-band__row-version">` wrapping a `Headnote` and a `VersionRow` element, and a sibling `BatchRow` element directly inside `.recipe-band`.
    - `app/src/ui/VersionRow.jsx` contains `versionLineBlockedAttempt` and does NOT contain the string `versionLineBlocked` as a standalone prop name (grep for `versionLineBlocked\b` excluding `versionLineBlockedAttempt` returns nothing).
    - `app/src/ui/RecipePage.jsx` contains `blockedAttemptRef` declared via `useRef` and incremented inside `buildPenFields`.
    - `app/src/styles/app.css`'s `.recipe-band` rule contains `flex-direction: column` and `border-bottom: var(--rule-baseline)`; a new `.recipe-band__row-version` rule exists with `border-bottom: var(--rule-graduation)`.
    - `app/src/styles/tokens.css` declares `--col-batch-cell-min`, and `app/src/styles/app.css`'s `.batch-row__cells` rule references it via `var(--col-batch-cell-min)`.
    - Full test suite passes and the production build succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit code, or output containing "error" (case-insensitive) from the Vite/Rollup build step</fails_when>
    <automated>grep -c 'className="region-name"' app/src/ui/VersionRow.jsx app/src/ui/BatchRow.jsx</automated>
    <fails_when>either file reports a count greater than 0</fails_when>
    <automated>sed -n '/^\.recipe-band {/,/^}/p; /^\.recipe-band__row-version {/,/^}/p; /^\.batch-row__cells {/,/^}/p; /^\.batch-row__cell {/,/^}/p; /^\.batch-row__cell-label {/,/^}/p; /^\.batch-row__cell-value {/,/^}/p' app/src/styles/app.css | grep -E '[0-9]+(px|rem|em)' | grep -v 'var(--'</automated>
    <fails_when>any line is printed — a literal unit escaping var() inside one of the six new/reworked rule blocks</fails_when>
  </verify>
  <done>Build passes; the full test suite passes; the front matter renders as two stacked full-width rows with no page-level running head on either; `BatchMargin.jsx` and `Versions.jsx` no longer exist; the four focus-return ref/effect pairs live in their new homes; a second consecutive blocked version-line save moves focus again.</done>
  <reversibility rating="reversible">Deleting Versions.jsx/BatchMargin.jsx and restructuring .recipe-band are code-only changes fully recoverable via git revert; no data migration, no stored-shape change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Retire the margin as a region, move Before you start to Method, rule the blank prose fields, start the dev server</name>
  <read_first>
    app/src/ui/Authored.jsx (full file — the `NoteList` function and both its call sites)
    app/src/ui/Method.jsx (lines 412-489 — the `Method` export's prop list and the return JSX up to the `<ol className="method-steps">` line)
    app/src/ui/RecipePage.jsx (as modified by Task 1 — the `<Method>` call and the `margin-region` aside)
    app/src/ui/BatchRow.jsx (as created by Task 1 — the recording-mode branch's three prose fields: `drawNotes`, `ingredientNotes`, `nextTimeNote`)
    .planning/sketches/003-front-matter-rows/index.html (`.before`/`.before p`/`.before p.running-head` CSS, lines 158-160; the `<div class="before">` markup, lines 300-304)
    .planning/phases/03.1-the-imprint-and-the-binder/03.1-VERIFICATION.md § "Overrides Accepted" (Gap 2's row)
    app/src/styles/app.css (the `.prose-field` rule, lines 1056-1068; the `.method-step__on-demand` rule near line 889, as the nearest existing disclosure-control analog)
  </read_first>
  <files>app/src/ui/Authored.jsx, app/src/ui/Authored.test.jsx, app/src/ui/Method.jsx, app/src/ui/Method.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/styles/app.css</files>
  <action>
    In `app/src/ui/Authored.jsx`: add the `export` keyword to `function NoteList(...)` so it becomes `export function NoteList({ listKey, notes, mode, onChangeNoteText, onRemoveNote })`. Remove the `beforeYouStart` parameter from `Authored`'s own prop list and delete its `<p className="authored__legend"><span>Before you start</span><span>authored</span></p>` block plus its `<NoteList listKey="beforeYouStart" .../>` call — `Authored` now renders only the `carriedForward` legend and list.

    In `app/src/ui/Method.jsx`: import `{ NoteList }` from `./Authored.jsx`. Add three new props to the `Method` export's destructured list: `beforeYouStart = []`, `onChangeNoteText = () => {}`, `onRemoveNote = () => {}`. Between the existing `<h2 className="region-name">Method</h2>` line and `<ol className="method-steps">` (lines 487-488), insert a new block: `<div className="method__before"><p className="region-name">Before you start</p><NoteList listKey="beforeYouStart" notes={beforeYouStart} mode={mode} onChangeNoteText={onChangeNoteText} onRemoveNote={onRemoveNote} /></div>`. Reusing the existing `region-name` class here is deliberate and consistent with the sketch: the "no running head" scope constraint (ROADMAP Scope bullet 1) applies only to the two front-matter rows, not to Method's own internal sub-label — the sketch's own `.before p.running-head` styles this exact label with the same bookcloth-uppercase-small-print treatment `region-name` already provides.

    In `app/src/ui/RecipePage.jsx`: add three props to the `<Method>` call — `beforeYouStart={mode === 'developing' && penDraft ? penDraft.authored.beforeYouStart : version.authored.beforeYouStart}`, `onChangeNoteText={handleChangePenNoteText}`, `onRemoveNote={handleRemovePenNote}` — mirroring exactly how `Authored`'s own `carriedForward` prop is already computed two lines below. Remove the `beforeYouStart` prop from the `<Authored>` call (Authored no longer accepts it).

    Retire the margin as a titled region: change `<aside className="margin-region" aria-label="Notes">` to `<div className="margin-region">` and delete the `<h2 className="region-name">Notes</h2>` line beneath it — `DerivedAdvisories` and `Authored` become its only children (the `<BatchMargin>` call is already gone from Task 1). No reordering is needed: with `<BatchMargin>` removed, `<DerivedAdvisories>` already precedes `<Authored>`, which already matches the target order "Balance, Things to check, Notes" (ROADMAP Scope bullet 2) — `DerivedAdvisories`'s own internal "Advisories" legend and `Authored`'s own "Carried forward" legend are unchanged this plan; their text renames to "Things to check" happen in a later plan (03.3-04).

    In `app/src/ui/BatchRow.jsx`, apply the hairline-baseline fix (03.1 Gap 2 override) to the three recording-mode prose fields: for `draft.drawNotes`, `draft.ingredientNotes`, `draft.nextTimeNote`, change the `className="prose-field"` on each `<textarea>`/`<input>` to a conditional: `className={draft.drawNotes === '' ? 'prose-field prose-field--empty' : 'prose-field'}` (same pattern for the other two, keyed on their own draft value). In `app/src/styles/app.css`, add `.prose-field--empty { border-bottom: var(--rule-graduation) solid var(--ink); }` and `.method__before { max-width: var(--measure-prose); padding-bottom: var(--gap-m); margin-bottom: var(--gap-m); border-bottom: var(--rule-graduation) solid var(--ink); }` — both reading only `var(--...)` tokens. No `<span>` label is added to the three prose fields — the fix is the rule alone (Gap 2's own accepted fix direction, and this plan's scope line "no label").

    Update `app/src/ui/Authored.test.jsx` to drop its `beforeYouStart` assertions (8 tests currently exercise both lists together — narrow to `carriedForward` only). Add `beforeYouStart` coverage to `app/src/ui/Method.test.jsx`: the block renders between the "Method" heading and the step list, is editable while `mode === 'developing'` (a note's textarea + remove control), and read-only otherwise. Add `prose-field--empty` assertions to `app/src/ui/BatchRow.test.jsx` for the three named fields, both empty (class present) and non-empty (class absent) states.

    Start the Vite dev server for the deferred checkpoint that follows: run `npm --prefix app run dev` in the background, wait for its ready signal, and confirm the app responds — this task's own verification, not the checkpoint's, per the golden rule that Claude starts servers, never asks a human to.
  </action>
  <acceptance_criteria>
    - `app/src/ui/Authored.jsx` exports `NoteList` (grep for `export function NoteList`) and contains zero occurrences of `beforeYouStart`.
    - `app/src/ui/Method.jsx` contains `method__before`, imports `NoteList` from `./Authored.jsx`, and renders it between the `region-name` "Method" heading and `<ol className="method-steps">`.
    - `app/src/ui/RecipePage.jsx`'s side-region contains `<div className="margin-region">` (not `<aside`) and zero occurrences of the string `Notes</h2>`.
    - `app/src/ui/BatchRow.jsx` conditionally applies `prose-field--empty` to `drawNotes`, `ingredientNotes`, and `nextTimeNote` based on each field's own draft value being the empty string.
    - `app/src/styles/app.css` contains `.prose-field--empty` and `.method__before` rules, each reading only `var(--...)` values (no bare `px`/`rem`/hex literal in either rule body).
    - The dev server responds (HTTP 200) at its reported local URL.
    - The narrowed test suite (`src/ui/RecipePage`, `src/ui/Authored`, `src/ui/Method`, `src/ui/VersionRow`, `src/ui/BatchRow`, `src/styles`) passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test -- src/ui/RecipePage src/ui/Authored src/ui/Method src/ui/VersionRow src/ui/BatchRow src/styles</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit code, or output containing "error" (case-insensitive)</fails_when>
    <automated>sed -n '/^\.prose-field--empty {/,/^}/p; /^\.method__before {/,/^}/p' app/src/styles/app.css | grep -E '[0-9]+(px|rem|em)' | grep -v 'var(--'</automated>
    <fails_when>any line is printed — a literal unit escaping var() inside either new rule</fails_when>
  </verify>
  <done>Method's head renders "Before you start" as editable-while-developing prose; the margin carries no page-level heading; the three named prose fields show a hairline rule exactly when empty; the dev server is reachable for the checkpoint that follows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the fork's landing focus (deferred, LD-03)</name>
  <what-built>The front matter now reads as two stacked full-width rows (version, then batch) with the margin retired as a titled region and "Before you start" moved to the head of the Method. The dev server is running at the URL Task 2 reported. This checkpoint covers the one behaviour this plan cannot prove in jsdom: the fork's landing focus (D-27) — per LD-03, do not author a jsdom interaction test for it; the fix is a deferred, real-browser confirmation instead.</what-built>
  <how-to-verify>
    1. Open the dev server's URL and navigate to the seeded olive oil recipe's current version.
    2. In the version row, open "Develop" and fill in a version line (e.g. "48 g oil · 800 g") and a reason.
    3. Click Save (or "Save as", if a batch already exists on this version) to fork a new child version — the page navigates to the child version's own URL.
    4. On the child version's page, without clicking anything, check where keyboard focus landed: it should be on that version row's own "Develop" button (the `autoFocus={focusDevelopOnMount}` path, `RecipePage.jsx`'s `navigate(..., { state: { focusDevelop: true } })`).
    5. Press Tab once from that landing point and confirm focus moves to the next control in a sensible order (not lost to `<body>`).
  </how-to-verify>
  <resume-signal>Type "approved" if focus lands on the child's Develop button, or describe what you observed instead (e.g. "focus lands on body")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maker input → prose fields (BatchRow's churn notes, Method's before-you-start notes) | Free-text input rendered back to the same local user; no server, no other user ever reads it. |
| None new this plan | This is a client-only, local-storage SPA restructuring; no new network call, no new external data source, no new stored-shape field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-033-01 | Tampering | Method.jsx's new `beforeYouStart` NoteList render | low | mitigate | Reuses the existing, already-audited `NoteList` component verbatim (React JSX text-node interpolation only, no `dangerouslySetInnerHTML`) — the same rendering path `carriedForward` already uses; no new escaping surface is introduced. |
| T-033-02 | Information Disclosure | VersionRow.jsx / BatchRow.jsx link suppression while a pen is open | low | accept | Behavior carried forward unchanged from Versions.jsx's existing `openPen ? plain-text : <Link>` guard — no new exposure, this plan only relocates the same conditional into two files. |
| T-033-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installed this plan (RESEARCH.md "Package Legitimacy Audit": not applicable) — the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npm --prefix app test` passes in full after both automated tasks.
- `npm --prefix app run build` succeeds after both automated tasks.
- Neither `VersionRow.jsx` nor `BatchRow.jsx` renders a `className="region-name"` element (grep-checkable).
- `Versions.jsx` and `BatchMargin.jsx` no longer exist in the tree.
- The two new CSS rule additions in Task 2 (`.prose-field--empty`, `.method__before`) and Task 1's four (`.recipe-band__row-version`, `.batch-row__cells`, `.batch-row__cell-label`, `.batch-row__cell-value`) contain no literal px/rem/em value outside a `var(--...)` reference.
- The deferred checkpoint is the plan's final task, after both automated tasks, so execution continues past it per LD-03.
</verification>

<success_criteria>
- The page's front matter renders as two full-width stacked rows in column one with no page-level running head on either (ROADMAP Scope bullet 1, 03.1 Gap 3/D-27's relocation half, WR-01's `VersionRow.jsx` half).
- The margin retires as a titled region; column two reads Balance → Things to check → Notes with no wrapping heading (ROADMAP Scope bullet 2, including the "Before you start" move).
- The three named blank prose fields carry a hairline rule and no label (03.1 Gap 2 override).
- The fork's landing focus is recorded as a deferred, end-of-phase-UAT human-verify item per LD-03, not blocking this plan's completion.
- `BatchMargin.jsx` no longer exists as a component; its content lives entirely in `BatchRow.jsx`.
</success_criteria>

<output>
Create `.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New files:**
- `app/src/ui/VersionRow.jsx` — the version-row module (exports `VersionRow`); replaces `Versions.jsx`'s version-owning half.
- `app/src/ui/BatchRow.jsx` — the batch-row module (exports `BatchRow`); replaces `BatchMargin.jsx` and `Versions.jsx`'s batch-owning half combined.
- `app/src/ui/VersionRow.test.jsx`, `app/src/ui/BatchRow.test.jsx` — their test coverage.

**Deleted files:**
- `app/src/ui/Versions.jsx`, `app/src/ui/BatchMargin.jsx`, `app/src/ui/Versions.test.jsx`, `app/src/ui/BatchMargin.test.jsx`.

**New exports:**
- `NoteList` from `app/src/ui/Authored.jsx` (previously module-private), now also imported by `app/src/ui/Method.jsx`.

**New CSS classes (`app/src/styles/app.css`):**
- `.recipe-band__row-version` — the version row's own two-column grid, carried forward from the retired `.recipe-band` rule.
- `.batch-row`, `.batch-row__cells`, `.batch-row__cell`, `.batch-row__cell-label`, `.batch-row__cell-value` — the batch row's figure-sized measured-value cells (sketch 003 variant B).
- `.prose-field--empty` — the hairline-baseline fix for a blank named prose field (03.1 Gap 2 override).
- `.method__before` — the "Before you start" block's wrapper, closing with a hairline rule before step 1.

**New CSS token (`app/src/styles/tokens.css`):**
- `--col-batch-cell-min` (96px) — the batch row's measured-cell grid minimum width.

**New state (`app/src/ui/RecipePage.jsx`):**
- `blockedAttemptRef` — a `useRef`-backed incrementing counter stamped onto `blockedTarget.attempt` on every blocked save, closing WR-01's `VersionRow.jsx` half.

**Removed region:** the "Notes"-titled `<aside>` wrapping the margin's content retires; `app/src/ui/RecipePage.jsx`'s `margin-region` becomes an untitled `<div>` holding only `DerivedAdvisories` and `Authored` (carried-forward notes only).
