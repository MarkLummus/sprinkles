---
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
plan: 02
type: execute
wave: 2
depends_on: ["03.3-01"]
files_modified:
  - app/src/domain/composition.js
  - app/src/domain/composition.test.js
  - app/src/ui/IngredientTable.jsx
  - app/src/ui/IngredientTable.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/styles/app.css
  - app/src/styles/tokens.css
  - app/src/styles/columns.test.js
autonomous: true
requirements: []

estimate:
  tokens: 165000
  raw_tokens: 165000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The ingredient table's reading state groups portions by step, the step's lead-in text heading each group, and the Step column no longer exists in any of the three render branches — reading, pen, show-changes (LD-01, ROADMAP Scope bullet 3; no toggle, no written-order path).
    - A split ingredient (a row with more than one portion) prints its name once per step it participates in; each occurrence carries the sub-line "{portion grams} g of {row total} g · {row share}% in all" beneath the name, in that exact format including the middle dot.
    - "The pen no longer offers ANY control to change which step a portion belongs to — the step-choice <select> is removed outright, not relocated (LD-02). This is a deliberate, visible regression in editability: a fact editable before this plan (which step a portion is measured for) is no longer editable in the pen. Amounts stay editable per portion, unchanged."
    - A portion whose step cannot be resolved through the active step-number map (its step was removed) still renders, grouped under a distinct "Unallocated" head positioned after every numbered step group — never silently dropped, extending the existing `formatStepReferences`-era "unallocated" vocabulary into the new group-head structure, preserving the activeRows/activeSteps never-drop discipline.
    - A second consecutive blocked save on the same ingredient-table row's grams field moves focus into that field again, not only the first blocked press (WR-01, `IngredientTable.jsx:518-521`), by keying the focus effect on an incrementing attempt number instead of the row id alone.
    - formatPortionLine lives in `domain/composition.js` as a pure function composing the existing `formatGrams`/`formatShareOfBatch` — no framework, DOM, or store import — tested under Vitest's default `node` environment.
    - Every new CSS rule this plan adds reads its spacing, size and weight values through an existing token in tokens.css — no literal survives in app.css; the now-dead `--col-step`/`--col-step-pen` tokens and every CSS rule that consumed them are deleted, not left orphaned.
  artifacts:
    - app/src/domain/composition.js
    - app/src/ui/IngredientTable.jsx
    - app/src/ui/RecipePage.jsx
    - app/src/styles/app.css
    - app/src/styles/tokens.css
    - app/src/styles/columns.test.js
  key_links:
    - RecipePage.jsx's new `steps` prop on `<IngredientTable>` — the exact same expression Method.jsx's own `steps` prop already computes (`mode === 'developing' || showingChanges ? version.method : readingVersion.method`) — is what supplies the step lead-in text the group heads read; IngredientTable.jsx received no method/step data at all in the reading state before this plan.
    - RecipePage.jsx's `blockedTarget.attempt` (stamped by 03.3-01's `blockedAttemptRef`) is read as a new `blockedRowAttempt` prop by IngredientTable.jsx's focus-return effect, closing WR-01's `IngredientTable.jsx:518-521` half the same way 03.3-01 closed VersionRow.jsx's.
    - IngredientTable.jsx's new grouping helper resolves every portion's step through `currentStepNumbers` via `displayNumberOf` (never a hand-rolled filter mentioning `removed`), preserving the activeRows/activeSteps discipline `domain/rows.js`'s own header comment states as this file's one rule.
---

<objective>
Rebuild `IngredientTable.jsx`'s body across all three render branches (reading, pen, show-changes) so the table groups by step through `activeRows`/`activeSteps` — never a hand-rolled `removed` filter — with the step's lead-in text as each group's head and the Step `<th>`/`<td>` removed from every branch together, not just the reading state (LD-01: step order replaces written order outright; no toggle, no second render path, ever). A split ingredient appears once per step it is measured for, each occurrence carrying the sub-line "120 g of 370.4 g · 46.3% in all" beneath its name, built by a new `formatPortionLine` formatter added to `app/src/domain/composition.js` (composing the existing `formatGrams`/`formatShareOfBatch`, tested in `composition.test.js`). Per LD-02, the pen's step-choice `<select>` (`StepCell`) is deleted outright, not relocated — this is a deliberate, recorded regression in editability: which step a portion belongs to stops being editable in the pen this phase, while amounts stay editable exactly as before. The now-dead `--col-step`/`--col-step-pen` tokens and their consuming CSS are dropped. `IngredientTable.jsx`'s blocked-save-focus effect is fixed to key on an incrementing attempt counter instead of the blocked row's id alone (WR-01, pinned by RESEARCH.md to `IngredientTable.jsx:518-521`), mirroring 03.3-01's identical fix for `VersionRow.jsx`.

Purpose: The sketch (003, variant B, confirmed by Mark 2026-09-09) depicts exactly this reading-state shape; ROADMAP Scope bullet 3, LD-01 and LD-02 make it binding and settle the two design questions RESEARCH.md's Open Questions 1-2 raised. The pen and show-changes states carry no sketch of their own — this plan states explicitly, at each point they diverge, that they are derived from the reading state's grouping plus LD-02, not copied from any design source.
Output: `IngredientTable.jsx` rebuilt around per-portion, step-grouped rendering; `formatPortionLine` (new) in `domain/composition.js`; `RecipePage.jsx` threading a new `steps` prop and a new `blockedRowAttempt` prop to `<IngredientTable>`, and losing the now-dead `handleChangePenRowStep` handler; `app.css`/`tokens.css` losing the Step column's tokens and rules and gaining the step-head/portion-note rules; `IngredientTable.test.jsx` and `columns.test.js` rewritten to match.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/sketches/003-front-matter-rows/README.md
@.planning/sketches/003-front-matter-rows/index.html
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-RESEARCH.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md
@app/src/ui/IngredientTable.jsx
@app/src/domain/composition.js
@app/src/domain/rows.js
@app/src/domain/stepNumbers.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatPortionLine to domain/composition.js</name>
  <files>app/src/domain/composition.js, app/src/domain/composition.test.js</files>
  <read_first>
    app/src/domain/composition.js (full file — `formatShareOfBatch`, `formatGramsValue`, `formatGrams`, their doc comments and the never-round-a-stored-amount doctrine `formatGrams`'s own comment states)
    app/src/domain/composition.test.js (full file — the existing `formatShareOfBatch`/`formatGrams`/`formatGramsValue` describe blocks, for style and the seeded-mass numbers this task's own test reuses)
    .planning/sketches/003-front-matter-rows/index.html (line 396: `const note = split ? \`${fmt(p.g)} g of ${fmt(ROWS[i][1])} g · ${share(ROWS[i][1], M0)} in all\` : null;` — the sub-line's exact composition, and the seed numbers at lines 328-343: whole milk's two portions, 120 g at step 2 and 250.4 g at step 3, against `M0 = 799.68`)
  </read_first>
  <behavior>
    - `formatPortionLine(120, 370.4, 799.68)` returns the literal string `120 g of 370.4 g · 46.3% in all` (the sketch's own worked example — the middle dot is `·`/`·`, not a hyphen or bullet).
    - `formatPortionLine` composes `formatGrams(rowTotalGrams)` for the "of X g" segment and `formatShareOfBatch(rowTotalGrams, mass)` for the "Y% in all" segment — it never re-implements their rounding or trace-threshold rules.
    - `portionGrams` prints exactly as given, with no `.toFixed()` applied — the same never-round-a-stored-amount discipline `formatGrams`'s own doc comment states ("never for a measured or as-made value itself"): a portion's own grams is a stored/typed amount, not a computed total, so `formatPortionLine(0.16, ...)` must print `0.16 g of ...`, not `0.2 g of ...`.
  </behavior>
  <action>
    In `app/src/domain/composition.js`, add `formatPortionLine(portionGrams, rowTotalGrams, mass)` immediately after `formatGrams` (after line 108), with a doc comment in this file's existing style naming sketch 003 variant B and ROADMAP Scope bullet 3 as its source, and explicitly stating the portionGrams-prints-unrounded rule above (so a future reader does not "fix" it into `formatGramsValue(portionGrams)`). Its body is exactly the template literal `` `${portionGrams} g of ${formatGrams(rowTotalGrams)} · ${formatShareOfBatch(rowTotalGrams, mass)} in all` `` — one line, no branching, composing the two existing exports rather than duplicating their rounding or trace-threshold logic. Export it alongside the file's other exports.

    In `app/src/domain/composition.test.js`, add a `describe('formatPortionLine', ...)` block, in this file's existing style, with at least: the sketch's own worked example (`formatPortionLine(120, 370.4, 799.68)` → `'120 g of 370.4 g · 46.3% in all'`), a second seeded case using the whole-milk portion the app's real seed data carries (`formatPortionLine(250.4, 370.4, 799.68)` → `'250.4 g of 370.4 g · 46.3% in all'`, both portions of the same split row report the SAME "of X g · Y% in all" tail since it is the row total, not the portion, that segment describes), and one case asserting a sub-0.05%-share row reads `trace` in the tail (reusing `formatShareOfBatch`'s own existing trace fixture numbers from this file, e.g. `formatPortionLine(0.16, 0.16, 799.68)` → `'0.16 g of 0.2 g · trace in all'`).
  </action>
  <acceptance_criteria>
    - `grep -n 'export function formatPortionLine' app/src/domain/composition.js` shows the export, placed after `formatGrams`.
    - `formatPortionLine`'s body calls `formatGrams` and `formatShareOfBatch` (grep for both identifiers within a few lines of the function) rather than reimplementing `.toFixed`/trace-threshold logic inline.
    - `app/src/domain/composition.test.js` contains a `describe('formatPortionLine'` block asserting the literal string `120 g of 370.4 g · 46.3% in all`.
    - `formatPortionLine`'s own module — `app/src/domain/composition.js` — imports no framework, DOM, or store module (unchanged from before this task; grep for `from 'react'`/`from '../store` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test -- src/domain/composition.test.js</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
  </verify>
  <done>`formatPortionLine` exists in `domain/composition.js`, exported, composing `formatGrams`/`formatShareOfBatch`; its tests in `composition.test.js` pass, including the sketch's own literal worked example.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild IngredientTable.jsx around per-portion step-grouping; remove the pen's step-choice select (LD-02); fix WR-01; wire RecipePage.jsx</name>
  <files>app/src/ui/IngredientTable.jsx, app/src/ui/IngredientTable.test.jsx, app/src/ui/RecipePage.jsx</files>
  <read_first>
    app/src/ui/IngredientTable.jsx (full file, already in context — every helper function, the three render branches inside the single `rows.map` loop, the `blockedRowId` focus effect at lines 517-521, the header/tfoot column lists)
    app/src/ui/IngredientTable.test.jsx (full file, 1024 lines — every describe block's title and the fixture helpers `makeRow`/`makeVersion`/`onePortionDraftRow`/`makeBatch`/`sectionMarkup`/`countTag`/`assertCellCountsAgree`/`trContaining`)
    app/src/ui/RecipePage.jsx (lines 494-529: `readingVersion`/`draftVersion`; lines 577-578: `currentStepNumbers`/`baselineStepNumbers`; lines 908-927: `handleChangePenRowStep`; lines 1183-1232: the `<IngredientTable>` and `<Method>` call sites, especially `<Method>`'s own `steps` prop at line 1212 — the exact expression this task copies)
    app/src/domain/rows.js (full file — `activeRows`, `activeSteps`, `rowGrams`, the never-`.filter(...removed...)` discipline)
    app/src/domain/stepNumbers.js (full file — `displayNumbers`, `displayNumberOf`)
    app/src/domain/batch.js (`hasAsMade`, `asMadeForPortion`, `asMadeTotals` — already imported by IngredientTable.jsx)
    app/src/domain/diff.js (`buildRowDiff`/`buildDiff`'s doc comment and body — confirm `gramsFrom`/`gramsTo`/`shareFrom`/`shareTo`/`stepsFrom`/`stepsTo` are each ROW-level totals, never per-portion; this is why Task 2's show-changes branch attributes a struck comparison to a split row's first portion only, per the rule below)
    .planning/sketches/003-front-matter-rows/index.html (lines 372-408: `renderRows`/`rowHTML`, the step-order branch's exact markup shape — the step-head `<tr>`, the split-row's repeated name+sub-line per portion, the unsplit row's plain `rowHTML` call with `stepText=''`; lines 126-131: `tr.step-head td`/`.lead` CSS)
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-RESEARCH.md (§ "The Table's Step Grouping", § "Common Pitfalls" 1 and 4)
  </read_first>
  <action>
    **Domain function no longer reachable from the pen.** Per LD-02, the step-choice `<select>` (`StepCell`) is removed outright. Delete from `IngredientTable.jsx`: `StepCell`, `DiffStepCell`, `formatStepReferences`, `formatDiffSteps`, `resolveStepNumber`, `safeDisplayNumberOf`, and `formatAsMadeReading` (the last is row-level-joining logic Task 2's per-portion rendering below makes unreachable — replaced by direct, per-portion `asMadeForPortion` calls). Simplify `rowAccessibleLabel` and `rowDiffAccessibleLabel`: drop their `changedStep`/`currentStepNumbers`/`baselineStepNumbers` parameters and the `"was step X, now step Y"` phrase entirely — LD-02 makes a portion's step immutable in the pen, so this phrase can never fire again going forward, and no code should carry a branch for a fact the UI can no longer produce. Add `formatPortionLine` to the existing `composition.js` import line.

    **The grouping helper.** Add a local (not exported) `groupPortionsByStep(rows, steps, stepNumberMap)`: for every row in `rows`, for every portion at index `i`, resolve `displayNumberOf(stepNumberMap, portion.step)`. When it resolves, accumulate `{ row, portion, portionIndex: i }` into a group keyed by `portion.step`, whose head reads that step's `leadIn` (looked up in `steps`, the new prop below, by matching `.n === portion.step`) and whose sort key is the resolved display number. When it does not resolve, accumulate the entry into one final "Unallocated" bucket instead — this portion is never dropped (RESEARCH.md Pitfall 4, extended from rows to portions: the exact scenario `formatStepReferences`'s own `'unallocated'` return value used to cover). Return the numbered groups sorted by display number, with the Unallocated bucket (if non-empty) appended last, `n: null`. Within a group, entries stay in `rows`' own iteration order, then portion order — never re-sorted, matching the sketch's own `ING.forEach` iteration.

    **The new `steps` prop.** Add `steps = []` to `IngredientTable`'s destructured props (a new prop — the reading state currently receives no method/step data at all). It supplies `groupPortionsByStep`'s lead-in lookups for the reading and show-changes branches; the pen branch uses `draftVersion.method` instead (already available, and it is the live, unsaved method — including any in-progress step removal — that the pen's own grouping must reflect, not the stale baseline `steps` would give it here).

    **Column count and the Step `<th>`.** Delete the Step `<th>` (`<th scope="col" className="ingredient-table__col-step">Step</th>`) and every Step `<td>` across all three branches and the `tfoot` row. Compute `const columnCount = 4 + (hasAsMadeLayer ? 1 : 0) + (isDeveloping ? 1 : 0);` (Ingredient, Grams, % of batch, Data are the fixed four; As made and Remove are the two conditional ones) — this is both the new group-head `<td>`'s `colSpan` and, restated, the header's own remaining column count.

    **The group-head row.** For each group `groupPortionsByStep` returns, render one `<tr className="ingredient-table__step-head">` ahead of its entries: `<td colSpan={columnCount}>` containing the text `` `Step ${group.displayNumber}` `` followed by `<span className="ingredient-table__step-head-lead">{group.leadIn}</span>` for a numbered group, or the bare text `Unallocated` (no lead-in span) for the final group. This is the sketch's own structure (lines 391-392, 130-131) transcribed directly.

    **Per-portion rendering, one `<tr>` per entry, all three branches.** Replace the single `rows.map((row) => {...})` loop's body with a loop over `groupPortionsByStep`'s flattened entries (group-head row, then each group's entries, in order). For every entry `{ row, portion, portionIndex }`:
    - The name cell always reads `row.ingredientName`; when `row.portions.length > 1` (a split ingredient), append `<span className="ingredient-table__portion-note">{formatPortionLine(portionGrams, rowGrams(row), mass)}</span>` beneath it, where `portionGrams` and `mass` are whichever "current" values apply in that branch (the row's baseline portion grams and `baselineMass` in the reading and show-changes branches; the pen's own live `parseGramsDraft(draftPortion.grams) ?? portion.grams` and `currentMass` in the developing branch) — the sub-line always reports the row's present state, never a struck-then-current pair of its own, since it sits beside cells that already carry any needed strike.
    - `dataFlagFor(row)`, `OrphanedRowFlag` and `RemoveRowControl` stay row-level facts: render `OrphanedRowFlag` and the Remove cell's `RemoveRowControl` only when `portionIndex === 0` (an orphaned-row flag or a remove/restore control repeated on every portion line of a split row would be the same control shown twice); a later portion's Remove cell renders empty but still present, so every `<tr>` in a state keeps the same cell count `assertCellCountsAgree`-style invariants require. `dataFlagFor(row)`'s Data-column flag renders on every portion line (matching the sketch, which repeats `r.data` per portion).
    - **Reading branch** (`!isDeveloping && !isShowingChanges`): `GramsCell`/`AsMadeCell` become portion-scoped (see below); the % of batch cell reads `formatShareOfBatch(portion.grams, baselineMass)` — the PORTION's own share, not the row's (the row's own overall share is what the sub-line's "in all" segment already reports).
    - **Pen branch** (`isDeveloping`): same portion-scoped `GramsCell`/`AsMadeCell`; the % of batch cell's current value is `formatShareOfBatch(parseGramsDraft(draftPortion.grams) ?? portion.grams, currentMass)`, struck against the portion's own baseline share exactly as `ShareCell` already does today, just fed the portion's numbers instead of the row's. The removed-row strike (`draftRow.removed`) still applies to the whole entry (struck name, struck grams, struck share) on every portion line of a removed row — removal is a whole-row fact.
    - **Show-changes branch** (`isShowingChanges`): `diff.js`'s `rowDiff` carries only ROW-level `gramsFrom`/`gramsTo`/`shareFrom`/`shareTo` (confirmed in `<read_first>`) — it cannot attribute a struck comparison to one portion of a split row. Apply the existing `DiffGramsCell`/`DiffShareCell` struck-comparison rendering (fed `rowDiff` unchanged) ONLY when `row.portions.length === 1` (the portion IS the row, so the mapping is exact); for a split row (`row.portions.length > 1`), every portion line shows its own CURRENT grams/share plainly, through the same `formatShareOfBatch(portion.grams, currentMass)` computation the reading branch uses, with no strike — this is Task 2's own explicit, sourced interpretation for an unsketched state (RESEARCH.md's own "no sketch for show-changes" gap), stated here rather than left implicit. `rowDiff.removed`'s struck NAME still applies on every portion line of a removed split row (removal is unambiguous and whole-row, unlike a magnitude change); use `rowDiffAccessibleLabel`'s simplified grams phrase (portion-level current value; the row-level "was/now" phrase only when single-portion, matching the visible cell it describes).

    **Portion-scoped `GramsCell`/`AsMadeCell`.** Change both from "one field per portion, looped, joined with a `+` inside one cell" to "one field for the ONE portion this call is about," taking `portionIndex` instead of iterating `row.portions`/`draftRow.portions` internally. `GramsCell({ row, portionIndex, mode, penDraft, onChangePenGrams, inputRef })`: outside the pen, return `{row.portions[portionIndex].grams} g` unformatted (matching today's un-rounded display of a stored amount); inside the pen, struck-baseline-then-field exactly as today, bound to `penDraft.rows[row.id].portions[portionIndex]`, `aria-label` using the SAME multi-portion-aware rule already in place (`` `${row.ingredientName}, grams, portion ${portionIndex + 1}` `` when `row.portions.length > 1`, else the unqualified name), `onChange` calling `onChangePenGrams(row.id, portionIndex, value)` — this handler's signature is already portion-indexed (Phase 03.2), unchanged. `inputRef` is only passed through when `portionIndex === 0` (the blocked-focus target is always the row's first portion, matching today's behavior). `AsMadeCell({ row, portionIndex, mode, draft, openBatch, onChangeAsMade })`: in `'recording'` mode, one field bound to `draft.asMade[row.id]?.[portionIndex] ?? ''`, same aria-label rule, `onChange` calling `onChangeAsMade(row.id, portionIndex, value)` (already portion-indexed); otherwise, `openBatch && asMadeForPortion(openBatch, row.id, portionIndex)` rendered as `` `${value} g` `` in `.ink-text` when not `null`, else nothing — replacing the row-level `formatAsMadeReading` join entirely.

    **WR-01 fix.** Add a new prop `blockedRowAttempt = null`. Change the focus effect from `useEffect(() => { if (!blockedRowId) return; gramsInputsRef.current.get(blockedRowId)?.focus(); }, [blockedRowId])` to guard on `if (blockedRowAttempt == null || !blockedRowId) return;` with a dependency array of `[blockedRowAttempt]` alone — an incrementing number differs on every blocked press even when the same row blocks twice in a row, which the current value-equal-row-id dependency cannot detect (the identical root cause and fix 03.3-01 already applied to `VersionRow.jsx`'s `versionLineFieldRef` effect). `blockedRowId` keeps its existing role: which row's registered ref to focus, and the `is-marked` visual class.

    **RecipePage.jsx wiring.** At the `<IngredientTable>` call site (around line 1186): add `steps={mode === 'developing' || showingChanges ? version.method : readingVersion.method}` (the exact expression `<Method>`'s own `steps` prop already uses two lines below, copied verbatim for consistency); add `blockedRowAttempt={blockedTarget?.kind === 'row' ? blockedTarget.attempt : null}` beside the existing `blockedRowId={blockedTarget?.kind === 'row' ? blockedTarget.rowId : null}` (`blockedTarget.attempt` already exists — 03.3-01 stamps it via `blockedAttemptRef` for both `'row'` and `'versionLine'` kinds); remove `onChangePenRowStep={handleChangePenRowStep}` and the now-fully-orphaned `handleChangePenRowStep` function (lines 908-927) — its only call site is the deleted `StepCell`, and per the project's own orphan-cleanup convention this plan's own change is what orphans it. Remove `baselineStepNumbers={baselineStepNumbers}` from the `<IngredientTable>` call specifically (IngredientTable.jsx no longer consumes it after the deletions above); `<Method>`'s own `baselineStepNumbers` prop, and the `baselineStepNumbers` variable itself, are untouched — Method.jsx still needs it.

    **Test rewrite — `IngredientTable.test.jsx`.** Keep the fixture helpers (`makeRow`, `makeVersion`, `onePortionDraftRow`, `makeBatch`, `sectionMarkup`, `countTag`, `trContaining`) as-is; a test that wants a numbered group must now set `version.method`/`current.method`/`draftVersion.method` explicitly (a row whose portions reference steps absent from `method` falls into the Unallocated group, per the new fallback rule — several existing tests already set `.method` this way). Update `assertCellCountsAgree` to strip `<tr class="ingredient-table__step-head">...</tr>` blocks from the `tbody` markup before counting `<td>`s (a group-head row's single colspanned cell would otherwise violate the header/body/total column-count invariant the helper checks).

    DELETE the following describe blocks outright — each asserts `StepCell`/`DiffStepCell`/`formatStepReferences`/`formatDiffSteps` behavior this task removes, and this deletion is a deliberate, named consequence of LD-01/LD-02, not a silent drop: `'IngredientTable — the selector keeps a removed step in its list (G-03-3 S3, 03-10)'` (~lines 353-522), `'IngredientTable — the step column resolves references through the maps (03-10)'` (~524-604), `'IngredientTable — the show-changes step cell prints unallocated rather than a dangling suffix (critique P2 #2)'` (~818-874), `"IngredientTable — the split-step suffix reads in ink (critique P2 #1, D-30)"` (~878-908), `"IngredientTable — a row's portion step references join in portion order, regardless of count"` (~913-932), `'IngredientTable — show-changes prints each portion exactly once (03.2-03 regression guard)'` (~996-1024).

    ADAPT `"IngredientTable — the pen's grams cell carries one field per portion, each its own accessible name"` (~937-990): drop the `expect((markup.match(/<select /g) || []).length).toBe(1)` assertion (LD-02: zero selects now, not one); keep both accessible-name assertions (they check substring occurrence in the whole markup string, which still holds once the two portions render as two separate `<tr>`s rather than one joined cell).

    ADD new describe blocks covering: (1) the reading state groups portions by step in ascending display-number order, asserting the step-head text and the `.ingredient-table__step-head-lead` lead-in content, using a version with 2+ steps; (2) a split ingredient's sub-line prints on every occurrence, in the exact sketch format, using the whole-milk fixture (120 g at step 2, 250.4 g at step 3, mass 799.68) and asserting the literal string `120 g of 370.4 g · 46.3% in all` via `.ingredient-table__portion-note`; (3) a portion whose step is removed (absent from the step-number map) renders under an `Unallocated` head positioned after every numbered group, never dropped; (4) the pen renders zero `<select` elements anywhere (LD-02, a table-wide grep on the rendered markup, not just the removed-selector's own former test); (5) `blockedRowAttempt`'s own dependency-array wiring is a source-level property, not a `renderToStaticMarkup`-observable one (this file's own top comment already states it runs under node with no DOM) — cover it via Task 2's own `<acceptance_criteria>` grep below, not a new render test, and say so in this describe block's own leading comment so a future reader does not go looking for one.

    Re-run the existing show-changes reorder/strike/ink-discipline tests (~lines 39-111) unmodified first; they use single-portion rows with no `.method` set (all portions fall into one Unallocated group together), so their relative-order and strike assertions should hold once grouping lands — adjust only if a specific assertion's exact markup shape (not its intent) breaks.
  </action>
  <acceptance_criteria>
    - `grep -c '<select' app/src/ui/IngredientTable.jsx` returns 0 (LD-02: no step-choice control survives anywhere in this file).
    - `grep -c 'ingredient-table__col-step' app/src/ui/IngredientTable.jsx` returns 0 (the Step `<th>`/`<td>` is gone from every branch, header and tfoot).
    - `grep -n 'blockedRowAttempt' app/src/ui/IngredientTable.jsx` shows the prop destructured and present in the focus effect's dependency array; `grep -n '\[blockedRowId\]'` in this file returns nothing (the old, buggy dependency array no longer exists).
    - `app/src/ui/RecipePage.jsx` contains `steps={mode === 'developing' || showingChanges ? version.method : readingVersion.method}` on the `<IngredientTable>` call and no longer contains `handleChangePenRowStep` or `onChangePenRowStep` anywhere.
    - `app/src/domain/composition.js`'s `formatPortionLine` is imported and called inside `IngredientTable.jsx` (not re-implemented inline).
    - Full test suite scoped to this task's files passes; production build succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test -- src/ui/IngredientTable src/domain/composition</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit code, or output containing "error" (case-insensitive) from the Vite/Rollup build step</fails_when>
  </verify>
  <done>The ingredient table renders grouped by step in all three states with no Step column and no pen step-select anywhere; a split ingredient's sub-line reads in the sketch's exact format; a portion on a removed step groups under Unallocated; a second consecutive blocked save re-focuses the row's grams field; `IngredientTable.test.jsx` names and either deletes or adapts every test the rebuild invalidates.</done>
  <reversibility rating="reversible">Removing the pen's step-choice select is two-way in code — `git revert` restores it exactly — but it is a visible, deliberate editability regression per LD-02, which is why it is recorded here as a truth and in this task's own action text rather than gated behind a checkpoint (LD-02 is explicit that this is a recording obligation, not a one-way commitment).</reversibility>
</task>

<task type="auto">
  <name>Task 3: Retire the Step column's tokens and CSS, add the step-head/portion-note rules, fix columns.test.js, run full verification</name>
  <files>app/src/styles/app.css, app/src/styles/tokens.css, app/src/styles/columns.test.js</files>
  <read_first>
    app/src/styles/tokens.css (lines 1-170, already in context — the `--col-step`/`--col-step-pen` declarations and their comments, lines 40-50's `--col-ingredient` retirement comment as the project's own precedent for how a retired token gets documented)
    app/src/styles/app.css (lines 529-720, already read — `.ingredient-table__col-step`, `.ingredient-table.is-developing .ingredient-table__col-step`, `.ingredient-table__step-cell`, `.ingredient-table__step-cell select.ink-field`, and the surrounding rules this task leaves untouched)
    app/src/styles/columns.test.js (full file, already read — every reference to `colStep`/`colStepPen`/`STEP_CONTENT_SHARE_03_08`/`STEP_OPTION_ESTIMATE_17_CHARS`/`chevronRoom`, the `emittedColumns` assertion, the widest-state budget functions)
    .planning/sketches/003-front-matter-rows/index.html (lines 126-131: `tr.step-head td`/`.lead`/`.portion` CSS — the property/token pairing this task's two new rule blocks transcribe)
  </read_first>
  <action>
    In `app/src/styles/tokens.css`, delete the `--col-step` and `--col-step-pen` declarations and their multi-line comments (the "Ingredient-table column widths" comment block's Step-specific lines, and the separate "The Step column in the pen" comment block). Leave a short retirement note in the same style as the existing `--col-ingredient` retirement comment (lines 40-50) — name that these two tokens retired because the Step column itself was removed (03.3-02, LD-01/LD-02) — describing them by name in prose only, never restating either token's own declaration line (name plus colon plus value) inside the note, so a later grep for the retired declaration stays a true negative rather than a false hit on prose describing it.

    In `app/src/styles/app.css`, delete `.ingredient-table__col-step`, `.ingredient-table.is-developing .ingredient-table__col-step`, `.ingredient-table__step-cell`, and `.ingredient-table__step-cell select.ink-field` in full (all four rule blocks the Step column and its pen `<select>` alone consumed). Add two new rule blocks, transcribing the sketch's own `tr.step-head`/`.lead` and `.portion` CSS (lines 126-131) through this project's existing tokens: `.ingredient-table__step-head td` reads `font-family: var(--face-grotesk)`, `font-size: var(--size-small-print)`, `text-transform: uppercase`, `letter-spacing: 0.04em` (matching this file's own existing `.ingredient-table th` rule's bare `0.04em`/`0` letter-spacing precedent — not tokenized project-wide), `padding-top: var(--gap-s)`, `border-bottom-width: var(--rule-baseline)`; `.ingredient-table__step-head-lead` reads `text-transform: none`, `letter-spacing: 0`, `font-family: var(--face-text)`, `font-size: var(--size-deviation-words)`, `margin-left: var(--gap-s)`; `.ingredient-table__portion-note` reads `display: block`, `font-size: var(--size-small-print)`. Every size/spacing/weight value in all three rules reads through an existing token — none is new.

    In `app/src/styles/columns.test.js`: remove the `describe('the pen state — the widened Step column, budgeted the same way (D-22, 03.1-05)', ...)` block entirely (its subject, `--col-step-pen`, no longer exists). Remove `colStep`, `colStepPen`, `STEP_CONTENT_SHARE_03_08`, `STEP_OPTION_ESTIMATE_17_CHARS`, and the local `chevronRoom` binding IF (and only if) nothing else in the file still reads it after the removal above — `--gap-select-chevron-room` itself stays declared in tokens.css (it is still consumed elsewhere by the general select-chevron rule, unrelated to the Step column). Change the `for (const col of ['numeric', 'step'])` loop (task-1 describe block, ~line 155) to iterate `['numeric']` alone, and delete the "the step column token, less its own two paddings..." test (~lines 175-181) beside it. Change `emittedColumns` expectation (~line 186) from `new Set(['name', 'numeric', 'step', 'data', 'remove'])` to `new Set(['name', 'numeric', 'data', 'remove'])`. Change `for (const col of ['numeric', 'step', 'data', 'remove'])` (~line 203) to `['numeric', 'data', 'remove']`. In `sizedColumnsTotalWidestState()` (~line 234), drop the `+ colStep` term, leaving `3 * colNumeric + colData + colRemove`. In the "reading state's widest shape" test (~line 263), drop `+ colStep` from `readingStateTotal`, leaving `3 * colNumeric + colData`.
  </action>
  <acceptance_criteria>
    - `grep -c -- '--col-step:' app/src/styles/tokens.css` and `grep -c -- '--col-step-pen:' app/src/styles/tokens.css` both return 0.
    - `grep -c 'ingredient-table__col-step\|ingredient-table__step-cell' app/src/styles/app.css` returns 0.
    - `app/src/styles/app.css` contains `.ingredient-table__step-head`, `.ingredient-table__step-head-lead`, and `.ingredient-table__portion-note` rule blocks.
    - `app/src/styles/columns.test.js` contains no reference to `colStep`, `colStepPen`, or a `'step'` entry inside `emittedColumns`'s expected set.
    - Full test suite passes; production build succeeds.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '/^\.ingredient-table__step-head/,/^}/p; /^\.ingredient-table__step-head-lead/,/^}/p; /^\.ingredient-table__portion-note/,/^}/p' app/src/styles/app.css | grep -E '[0-9]+(px|rem|em)' | grep -v 'var(--'</automated>
    <fails_when>any line is printed — a literal unit escaping var() inside one of the three new rule blocks</fails_when>
    <automated>npm --prefix app test -- src/styles src/ui/IngredientTable src/domain</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit code, or output containing "error" (case-insensitive) from the Vite/Rollup build step</fails_when>
  </verify>
  <done>`--col-step`/`--col-step-pen` and every CSS rule that consumed them are gone; the three new step-head/portion-note rules exist, token-only; `columns.test.js` matches the table's new five-column shape; the full test suite and production build both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maker input → per-portion grams/as-made fields | Free-text numeric input rendered back to the same local user; no server, no other user ever reads it — unchanged surface, just re-homed into per-portion cells. |
| None new this plan | Client-only, local-storage SPA restructuring; no new network call, no new external data source, no new stored-shape field. `diff.js`, `stepNumbers.js`, `rows.js`, `batch.js` are read, never modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-033-03 | Tampering | `IngredientTable.jsx`'s new `formatPortionLine`-driven sub-line and step-head lead-in text render | low | mitigate | Plain JSX text-node interpolation of already-domain-validated numeric strings and stored lead-in text — no `dangerouslySetInnerHTML`, the same escaping discipline every other cell in this file already carries. |
| T-033-04 | Information Disclosure | LD-02's removal of the pen's step-choice control | n/a | accept | A narrower editable surface reduces exposure rather than widening it; nothing becomes visible that was not already visible through the (now-removed) Step column. |
| T-033-05 | Tampering | npm/pip/cargo installs | n/a | accept | No new package installed this plan (RESEARCH.md "Package Legitimacy Audit": not applicable) — the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npm --prefix app test` passes in full after all three tasks.
- `npm --prefix app run build` succeeds after all three tasks.
- `IngredientTable.jsx` contains no `<select` and no `ingredient-table__col-step` anywhere (grep-checkable).
- `tokens.css` declares neither `--col-step:` nor `--col-step-pen:`; `app.css` consumes neither.
- `RecipePage.jsx` no longer contains `handleChangePenRowStep`/`onChangePenRowStep`; it does contain the new `steps`/`blockedRowAttempt` props on `<IngredientTable>`.
- No checkpoint task exists in this plan (LD-03: neither of the phase's two deferred human-verify items belongs here).
</verification>

<success_criteria>
- The ingredient table groups by step in all three states (reading, pen, show-changes), the Step column and the pen's step-choice select both gone, matching LD-01/LD-02 and ROADMAP Scope bullet 3.
- A split ingredient's sub-line reads in the sketch's exact format, sourced from a new, tested `formatPortionLine` in `domain/composition.js`.
- A portion whose step was removed still renders, under an `Unallocated` group, never dropped.
- WR-01's `IngredientTable.jsx` half closes: a second consecutive blocked save on the same row re-focuses its grams field.
- `--col-step`/`--col-step-pen` and their consuming CSS are gone; no new CSS rule carries a literal outside `var(--...)`.
</success_criteria>

<output>
Create `.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New exports:**
- `formatPortionLine(portionGrams, rowTotalGrams, mass)` in `app/src/domain/composition.js` — the split-ingredient sub-line formatter, composing `formatGrams`/`formatShareOfBatch`.

**New identifiers (`app/src/ui/IngredientTable.jsx`):**
- `groupPortionsByStep(rows, steps, stepNumberMap)` — local grouping helper; resolves each portion's step through `displayNumberOf`, buckets unresolvable portions into a trailing `Unallocated` group.
- `steps` prop (new) — the method array supplying each group's lead-in text in the reading and show-changes states.
- `blockedRowAttempt` prop (new) — the WR-01 fix's re-fire key, paired with the existing `blockedRowId`.
- Portion-scoped `GramsCell`/`AsMadeCell` — now render one field for one named `portionIndex`, not a joined loop over `row.portions`.

**Removed identifiers (`app/src/ui/IngredientTable.jsx`):**
- `StepCell` — the pen's step-choice `<select>` (LD-02: removed outright, not relocated).
- `DiffStepCell`, `formatStepReferences`, `formatDiffSteps`, `resolveStepNumber`, `safeDisplayNumberOf`, `formatAsMadeReading` — all made unreachable by the deletions above.

**New CSS classes (`app/src/styles/app.css`):**
- `.ingredient-table__step-head`, `.ingredient-table__step-head-lead` — the group-head row and its lead-in span.
- `.ingredient-table__portion-note` — the split-ingredient sub-line.

**Removed CSS (`app/src/styles/app.css`):**
- `.ingredient-table__col-step`, `.ingredient-table.is-developing .ingredient-table__col-step`, `.ingredient-table__step-cell`, `.ingredient-table__step-cell select.ink-field`.

**Removed CSS tokens (`app/src/styles/tokens.css`):**
- `--col-step`, `--col-step-pen`.

**RecipePage.jsx changes:**
- `<IngredientTable>` gains `steps` and `blockedRowAttempt` props; loses `onChangePenRowStep` and `baselineStepNumbers`.
- `handleChangePenRowStep` function deleted (orphaned by `StepCell`'s removal).