---
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
plan: 03
type: execute
wave: 3
depends_on: ["03.3-01", "03.3-02"]
files_modified:
  - app/src/ui/Method.jsx
  - app/src/ui/Method.test.jsx
  - app/src/ui/IngredientTable.jsx
  - app/src/ui/IngredientTable.test.jsx
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
autonomous: false
requirements: []

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - While a version is being developed, each step's default rendering shows its lead-in/instruction prose, its targets, and its purpose/aside as read text — no `<input>`, `<textarea>`, `<select>`, checkbox or `<fieldset>` — plus exactly one `edit this step` text control, and no other clickable control (ROADMAP Scope bullet 4, "one edit this step text control per step").
    - Clicking `edit this step` reveals that step's existing, unchanged editable form (lead-in/instruction fields, target-chip fields, purpose/aside on demand, the uses line and its checklist, the removed-row cross-flag, remove/restore) — this plan changes nothing about what that form does, only when it is reachable.
    - On a removed step, the coverage cue (D-UAT-3 — `coverageSentence(coveredRows, currentStepNumbers)` in a `<p className="method-step__flag">`) stays visible on the CLOSED step, outside the `edit this step` reveal, exactly as it renders today. It is plain prose with no control, so it does not breach "one `edit this step` text control per step" (Mark, 2026-09-10: option B — cue visible, cross-flag inside the reveal).
    - A new `.text-control` CSS class exists in app.css, opts out of the global `button, select` binder rule (no border, an ink underline, both read through existing tokens — no new token), and is applied to the ROADMAP/RESEARCH-named per-step controls (`add purpose`/`add aside`, `change`/`done`, `remove`/`restore` in Method.jsx's `StepPenBody`) plus the one per-row control surviving from plan 02 (`RemoveRowControl`'s `remove`/`restore` in IngredientTable.jsx) and the new `edit this step` control — 03.1 Gap 1 override.
    - The `StepRecordingControls` component's "done differently" opener (a different pen — batch recording, not version developing) and the two removed-row cross-flag buttons ("remove this step" in Method.jsx, "remove this row" in IngredientTable.jsx) are explicitly left binder-styled — named, not silently dropped, since RESEARCH.md's own enumeration of the per-step/per-row controls in scope does not include them.
    - The uses checklist's open/close and its scoped Escape (`event.stopPropagation()`, unchanged by this plan) close as a deferred `checkpoint:human-verify`, the plan's final task, after every automated verification — execution continues past it (LD-03).
    - binder.test.js's existing "no selector other than `a`/`a:visited` declares an underline (D-18)" assertion is updated for `.text-control`'s new underline, and its global "no visual literal" checks (already applying to every rule in app.css) continue to pass against the new rule with no edit to those checks themselves.
  artifacts:
    - app/src/ui/Method.jsx
    - app/src/ui/Method.test.jsx
    - app/src/ui/IngredientTable.jsx
    - app/src/styles/app.css
    - app/src/styles/binder.test.js
  key_links:
    - Method.jsx's per-step `stepReveal = useOnDemandField(false, () => {})` (the same primitive already used for `purpose`/`aside`, applied once more at the whole-step level, per PATTERNS.md — never a second disclosure primitive) gates `StepPenBody`'s existing, unmodified return between a new closed-state read-only render (transcribed from the show-changes branch, Method.jsx's own `isShowingChanges` JSX) and the current form.
    - The new `.text-control` rule in app.css overrides the global `button, select` binder rule through selector specificity alone (a class beats a bare element regardless of source order) — no `!important`, matching how `select.ink-field` already overrides a bare `select` in this same file.
---

<objective>
Rewrite `Method.jsx`'s `StepPenBody` so a step in the developing pen opens read-only by default — reusing the show-changes branch's already-token-correct read-only JSX as the closed-state template, fed `draftStep`/`stepDiff` instead of `step`/`stepDiff` — with one `edit this step` text control per step (built on the existing `useOnDemandField` open/collapse primitive, `Method.jsx:49-72`, applied at the whole-step level for the first time) revealing the step's current, unchanged editable fields. Define a new `.text-control` CSS class, transcribed from sketch 003 variant B's own `.text-control` rule (`index.html:32-34`) entirely through this project's existing tokens — no new token — that opts out of the global `button, select` binder rule (app.css's own `button, select { ... }` block). Apply it to the ROADMAP/RESEARCH-named per-step controls (`remove`/`restore`, `change`/`done`, `add purpose`/`add aside`) plus the new `edit this step` control in Method.jsx, and to the one per-row control surviving plan 02's `IngredientTable.jsx` rebuild (`RemoveRowControl`'s own `remove`/`restore`) — 03.1 Gap 1 override. Land a deferred, blocking `checkpoint:human-verify` for the uses checklist's open/close and its scoped Escape (`stopPropagation`, unchanged by this plan) as the plan's final task, after all automated verification, so execution continues past it (LD-03).

Purpose: Phase 03.1 left the pen's method reading as a 32-field-per-step form as an accepted, undelivered override (Gap 1); Mark's 2026-09-09 sketch-003 handoff settled the fix as "read-only, one `edit this step` text control per step" — this plan is that fix's landing place, the last of the phase's three ROADMAP-scope-bearing rebuilds (01: the front-matter rows; 02: the step-grouped table; 03: the read-only pen).
Output: `Method.jsx`'s `StepPenBody` rewritten around a whole-step reveal gate; `.text-control` (new) in `app.css`, applied to five controls in `Method.jsx` and one in `IngredientTable.jsx`; `Method.test.jsx` and `binder.test.js` rewritten to match; a deferred human-verify checkpoint for the uses-checklist's scoped Escape.

**Specless-probe fallback note:** No SPEC.md exists and this phase carries no REQUIREMENTS.md IDs (ROADMAP: "Requirements: None new"), so the spec-less probe fallback was skipped for this phase — recorded here, not silently, matching 03.3-01/02's own identical note. `must_haves.truths` above derive from ROADMAP Scope bullet 4, the 03.1 Gap 1 override, RESEARCH.md's own named per-step-control enumeration, and LD-03.

**A genuinely unsketched call, stated rather than made silently (CLAUDE.md § 1):** the sketch depicts only the *reading* state's Method; RESEARCH.md notes twice that the pen's read-only shape "carries no sketch of its own." The removed-row cross-flag (`flaggedRows`) and the coverage cue (`coveredRows`) are part of `StepPenBody`'s existing form but are NOT part of the show-changes branch this plan uses as the closed-state template, and the ROADMAP's own words — "with ONE `edit this step` text control per step" — read as excluding any other clickable control from the closed view. **Resolved by Mark, 2026-09-10 (option B):** the removed-row cross-flag carries a `remove this step` control, so it moves inside the `edit this step` reveal with the rest of the form. The coverage cue is plain prose with no control, so it STAYS on the closed step — D-UAT-3 asked that correct silence be legible without asking, and a cue one click deeper would not be. The closed branch therefore renders the cue exactly as `StepPenBody` renders it today (`draftStep.removed && coveredRows.length > 0`), and nothing else from the form.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/sketches/003-front-matter-rows/README.md
@.planning/sketches/003-front-matter-rows/index.html
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-RESEARCH.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-01-SUMMARY.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-02-SUMMARY.md
@.planning/phases/03.1-the-imprint-and-the-binder/03.1-VERIFICATION.md
@app/src/ui/Method.jsx
@app/src/ui/IngredientTable.jsx
@app/src/styles/app.css
@app/src/styles/tokens.css
@app/src/styles/binder.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: `.text-control`, the whole-step reveal gate, and the named per-step/per-row controls (03.1 Gap 1 override)</name>
  <files>app/src/ui/Method.jsx, app/src/ui/IngredientTable.jsx, app/src/styles/app.css</files>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (lines 28-34: `.btn`/`.text-control`/`.text-control:hover`/`.text-control[disabled]` CSS; line 437: `<button class="text-control edit" onclick="...">edit this step</button>` — the sketch's own verbatim markup for this exact control, inside `STEPS.forEach`, its visibility gated in the sketch only by `[data-pen="develop"] .method .edit { display: inline }`, i.e. the control exists per step unconditionally and CSS alone shows/hides it — read_first only, this plan uses a JS conditional, not a CSS visibility toggle, for the reasons in Task 1's own action text)
    .planning/sketches/003-front-matter-rows/README.md (§ "Decisions carried": "The per-step controls in the pen are a read-only method with one 'edit this step' text control per step (Mark, 2026-09-09, from the critique's question)")
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-RESEARCH.md (§ "The Pen's Read-Only Method" in full)
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md (§ "app/src/ui/Method.jsx — read-only pen with edit this step disclosure" in full, including the `useOnDemandField` excerpt and the CSS analog note that `.method-step__on-demand` does NOT itself opt out of the binder)
    app/src/ui/Method.jsx (full file, already in context — `useOnDemandField` at lines 49-72; `StepPenBody` at lines 80-327, EVERY field and button inside it; `coverageSentence` at lines 19-37; the `isShowingChanges` branch at lines 533-618, the closed-state template's source; `StepRecordingControls` at lines 335-385 — NOT touched by this task, its `done differently` button keeps its current `className="method-step__on-demand"` alone, a different pen (`mode === 'recording'`), not named in RESEARCH.md's per-step-control enumeration)
    app/src/ui/IngredientTable.jsx (`RemoveRowControl`, its own comment already reading "a text button, never an icon and never a colour — exactly one control per row"; `OrphanedRowFlag`'s "remove this row" button, a DIFFERENT, unnamed control — left unchanged, matching `Method.jsx`'s own "remove this step" cross-flag boundary. Both are as plan 02 leaves them — re-read this file fresh, since 02 rebuilt its per-portion rendering; `RemoveRowControl`'s own body and the `.text-control`-worthy button inside it are unaffected by that rebuild per 02's own action text, gated only to `portionIndex === 0`.)
    app/src/styles/app.css (lines 1-90, already in context — the `button, select` binder block through `a:visited`; lines 733-920, already in context — every `.method-step__*` rule, especially `.method-step__on-demand` at 889-892 and `.method-step__uses-line button` at 883-885)
    app/src/styles/tokens.css (full file, already in context — confirm `--rule-ink-field` (1px), `--rule-baseline` (1.5px), `--offset-link-underline` (2px), `--size-small-print` (0.75rem), `--face-grotesk` are the only tokens this task needs; no new token is declared)
  </read_first>
  <action>
    **`.text-control` (new CSS class, app.css).** Immediately after the existing `button:disabled, select:disabled { ... }` rule block and before the `/* A lone \`select\` selector ... */` comment, insert a new doc comment naming sketch 003 variant B's own `.text-control` rule (`index.html:32-34`) and the 03.1 Gap 1 override as its source, then three rule blocks. `.text-control` itself: `appearance: none; border: none; border-radius: 0; background: none; padding: 0; color: var(--ink); font-family: var(--face-grotesk); font-size: var(--size-small-print); cursor: pointer; text-decoration: underline; text-decoration-thickness: var(--rule-ink-field); text-underline-offset: var(--offset-link-underline);` — the underline reads the exact same token pair the existing `a` rule already uses for its own underline (`--rule-ink-field`, `--offset-link-underline`), not a new value. `.text-control:hover` sets `text-decoration-thickness: var(--rule-baseline); padding: 0;` — the thickness step mirrors `button:hover, select:hover`'s own border-width step from `--rule-ink-field` to `--rule-baseline`, applied to an underline instead of a border; the explicit `padding: 0` is required because `button:hover, select:hover`'s `padding: var(--gap-xs)` declaration has higher selector specificity (a compound pseudo-class beats a lone class) than `.text-control`'s own rest-state `padding: 0`, and would otherwise visibly shift a hovered text-control by `--gap-xs` even though its border is already `none`. `.text-control:disabled` sets `text-decoration-style: dashed; color: inherit; cursor: default; border: none; padding: 0;` — the `border: none`/`padding: 0` repeats are needed for the identical specificity reason, this time against `button:disabled, select:disabled`. No new token is declared anywhere in this block — every value above is an existing token from `tokens.css`, confirmed in `<read_first>`.

    **The whole-step reveal gate (`Method.jsx`, `StepPenBody`).** Immediately after the existing `aside = useOnDemandField(...)` line and before the `usesOpen` state declaration, add `const stepReveal = useOnDemandField(false, () => {});` — the same primitive already used twice in this function (matching `purpose`/`aside`'s own object-kept-whole style, read as `purpose.isOpen`/`purpose.openField` rather than destructured), called a third time, matching PATTERNS.md's own recommendation to apply it "at the whole-step level instead of the per-field level," never a second implementation. Read only `stepReveal.isOpen` and `stepReveal.openField` below — the hook's `fieldRef`/`handleBlur` are never wired to anything this task adds; no auto-focus-on-reveal is built, since it is not named by ROADMAP Scope bullet 4, LD-03, or any locked decision. Note this explicitly as a stated scope boundary, not an oversight, so a reviewer does not read the unused `stepReveal.fieldRef`/`stepReveal.handleBlur` as a mistake.

    Wrap `StepPenBody`'s current `return (<div className="method-step__body"> ... </div>)` — every field and button it already renders, byte-for-byte unchanged — inside `stepReveal.isOpen ? ( <current form, unmodified> ) : ( <new closed-state form> )`. Both branches keep the outer `<div className="method-step__body">` wrapper so `.method-step__body`'s existing grid/spacing rule applies uniformly regardless of state.

    **The closed-state form.** Transcribe the `isShowingChanges` branch's read-only JSX (`Method.jsx`'s own `isDeveloping`-sibling branch, the block starting `<div className="method-step__body">` under `if (isShowingChanges)`) into `StepPenBody`'s new closed branch, substituting every `step.` read for `draftStep.` (this branch's own data is the maker's live edit, not the baseline `steps` prop) while keeping `stepDiff` exactly as already computed at the top of `StepPenBody` (the same `showStruckBeneath`/`showPurposeStruck`/`showAsideStruck` booleans `StepPenBody` already derives, unchanged): a `<p className="method-step__lead">` wrapping a `<span className={draftStep.removed ? 'method-step__prose--struck' : undefined}>` around `<b>{draftStep.leadIn}.</b> {draftStep.instruction}`, followed by the removed label span when `draftStep.removed`; the struck-beneath paragraph when `showStruckBeneath`; a `<p className="method-step__targets">` over `draftStep.targets` when non-empty, each `<span className="target-chip" key={index}>` (index-keyed, matching `StepPenBody`'s OWN existing open-state target map, not the show-changes branch's label-keyed map — this branch reads the same live, possibly-duplicate-labelled `draftStep.targets` array the open form edits) carrying the struck-value span when `stepDiff.targets[index]?.changed && stepDiff.targets[index].from != null`, then plain `target-chip__label`/`target-chip__value` spans (never input fields — this is the closed state); the stale-amount flag paragraph, identical to the existing one (`staleFlagVisible && staleEntry`); `draftStep.purpose` as a plain `<p className="method-step__purpose">` when present, its own struck-beneath paragraph when `showPurposeStruck`; `draftStep.aside` the same way with `showAsideStruck`. Then, still in the closed branch, render the coverage cue exactly as the existing form does — `{draftStep.removed && coveredRows.length > 0 && (<p className="method-step__flag">{coverageSentence(coveredRows, currentStepNumbers)}</p>)}` — so it reads on the closed step (option B, `<objective>`). Render NOTHING for `flaggedRows`/the uses line/the remove-restore control in this closed branch; that content lives only in the revealed form.

    End the closed branch with one new control: `<button type="button" className="text-control method-step__edit" aria-label={fieldLabel(step, draftStep.removed, 'edit this step')} onClick={stepReveal.openField}>edit this step</button>` — the visible text is the literal, lower-case string `edit this step`, matching the sketch's own control text exactly. Add a matching `.method-step__edit { display: block; margin-top: var(--gap-xs); }` rule to `app.css`, in the same style as the existing `.method-step__on-demand` layout rule immediately above it (this is a layout-only rule; the control's visual style comes entirely from the `text-control` class already applied alongside it).

    **Update `Method.jsx`'s own docblock.** The comment above the `Method` export (currently describing "a second render path takes over per step: text fields for leadIn/instruction/purpose/aside... a remove/restore control...") now reads accurately: the pen opens each step read-only by default; one `edit this step` control per step reveals that same unchanged field set. This is this task's own behaviour being described, not an unrelated adjacent edit.

    **The named per-step controls (`Method.jsx`, inside the now-revealed `StepPenBody` form — no other change to their own logic or position).** `add purpose`'s and `add aside`'s opener `<button>`s: change `className="method-step__on-demand"` to `className="method-step__on-demand text-control"` on both (the layout class stays, `.text-control` is additive). The uses line's `change`/`done` toggle `<button>` (currently unclassed): add `className="text-control"`. `StepPenBody`'s own bottom `remove`/`restore` `<button>` (currently unclassed): add `className="text-control"`. Do NOT touch `StepRecordingControls`'s `done differently` button (stays `className="method-step__on-demand"` alone — a different pen, not named in scope, per `<read_first>`) or the removed-row cross-flag's `remove this step` button inside `StepPenBody` (stays unclassed — the same stated boundary as `coveredRows`/`flaggedRows` above).

    **The one named per-row control (`IngredientTable.jsx`).** `RemoveRowControl`'s `<button type="button" onClick={onToggle}>{removed ? 'restore' : 'remove'}</button>`: add `className="text-control"`. Do NOT touch `OrphanedRowFlag`'s `remove this row` button — the per-row cross-flag, unnamed by RESEARCH.md's enumeration, the exact same stated boundary drawn for `Method.jsx`'s own cross-flag.
  </action>
  <acceptance_criteria>
    - `app/src/styles/app.css` contains a `.text-control` rule block, a `.text-control:hover` block, a `.text-control:disabled` block, and a `.method-step__edit` block; none of the four contains a bare `px`/`rem`/`em` value outside a `var(--...)` call.
    - `app/src/ui/Method.jsx` contains `const stepReveal = useOnDemandField(false,` and a ternary on `stepReveal.isOpen` inside `StepPenBody`'s return.
    - `app/src/ui/Method.jsx` renders the literal text `edit this step` exactly once per step (inside the new closed branch), with `className="text-control method-step__edit"`.
    - `grep -o 'className="[^"]*text-control[^"]*"' app/src/ui/Method.jsx | wc -l` reports exactly 5 (add purpose, add aside, change/done, remove/restore, edit this step).
    - `grep -o 'className="[^"]*text-control[^"]*"' app/src/ui/IngredientTable.jsx | wc -l` reports exactly 1 (`RemoveRowControl` only — `OrphanedRowFlag`'s button carries no `text-control` class).
    - `app/src/ui/Method.jsx` still contains `done differently` with no `text-control` in its own button's className (StepRecordingControls untouched).
    - `coverageSentence` itself is unchanged by this task and stays module-private; the closed branch calls it for a removed step with covered rows.
    - Production build succeeds.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '/^\.text-control {/,/^}/p; /^\.text-control:hover {/,/^}/p; /^\.text-control:disabled {/,/^}/p; /^\.method-step__edit {/,/^}/p' app/src/styles/app.css | grep -E '[0-9]+(px|rem|em)' | grep -v 'var(--'</automated>
    <fails_when>any line is printed — a literal unit escaping var() inside one of the four new rule blocks</fails_when>
    <automated>grep -o 'className="[^"]*text-control[^"]*"' app/src/ui/Method.jsx | wc -l | tr -d ' '</automated>
    <fails_when>output is not exactly "5" — a named per-step control was missed, or an out-of-scope one (done differently, either cross-flag) was wrongly included</fails_when>
    <automated>grep -o 'className="[^"]*text-control[^"]*"' app/src/ui/IngredientTable.jsx | wc -l | tr -d ' '</automated>
    <fails_when>output is not exactly "1" — RemoveRowControl missed its class, or OrphanedRowFlag's cross-flag was wrongly included</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit code, or output containing "error" (case-insensitive) from the Vite/Rollup build step</fails_when>
  </verify>
  <done>`.text-control` exists in app.css, token-only, opting out of the binder rule; `StepPenBody` opens read-only by default with one `edit this step` control per step revealing the unchanged existing form; the five named Method.jsx controls and IngredientTable.jsx's one named per-row control carry `.text-control`; `done differently` and both cross-flags are explicitly untouched; the build passes.</done>
  <reversibility rating="reversible">A CSS class addition and a render-gate wrapper around already-working, unmoved JSX — fully recoverable via git revert; no stored-data or API-shape change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Rewrite Method.test.jsx for the closed-by-default pen; export and unit-test coverageSentence; fix binder.test.js's underline assertion; full suite and build</name>
  <files>app/src/ui/Method.jsx, app/src/ui/Method.test.jsx, app/src/ui/IngredientTable.test.jsx, app/src/styles/binder.test.js</files>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (line 437, the sketch's own `edit this step` control text — confirms the literal, lower-case string this task's new assertions check for)
    app/src/ui/Method.jsx (as rewritten by Task 1 — the closed-state branch's exact JSX, the `stepReveal` gate, the five reclassed controls)
    app/src/ui/Method.test.jsx (full file, 1155 lines, already read this session — the existing `describe('Method — the uses line, closed by default (D-24)')` block at ~lines 707-747 and its own trailing comment at ~lines 749-756: this is the file's OWN established precedent for exactly this situation — a state transition `renderToStaticMarkup` cannot execute (no click simulation; this project has no jsdom/testing-library dependency, confirmed via `app/package.json` — `environment: 'node'` only, per `vitest.config.js`'s own comment) is documented as a UAT item rather than silently dropped or worked around with new test infrastructure. This task extends the SAME disposition to the whole-step reveal, not a new one.)
    app/src/styles/binder.test.js (full file, already read this session — the "no selector other than `a`/`a:visited` declares an underline (D-18)" test near the end, and the "buttons and selects" describe block's own style, for the new `.text-control` describe block this task adds)
    app/src/styles/css-source.js (already read this session — `ruleFor`/`readAllRules`/`resolveTokenPx`, the exact helpers this task's new binder.test.js assertions use)
  </read_first>
  <action>

    **Rewrite the pen-specific portion of `describe('Method — developing mode')`.** This describe block mixes two unrelated states: the FIRST ~180 lines (struck-beneath/removed-label/target-chip/purpose-aside-struck tests) exercise content this plan's closed-state template ALSO renders (transcribed verbatim from the same `stepDiff`-driven logic) — these should continue to pass with no change once the render call itself is updated to account for the new default-closed markup (no action needed on their OWN assertions if they already only check for `prose-struck-beneath`/`method-step__skipped-label`/`target-chip` substrings, which the closed state still produces; re-run them first and adjust only an assertion whose exact markup shape, not intent, breaks — matching this file's own established practice elsewhere). The SHOW-CHANGES-specific tests further down this same describe block (from `it('renders read-only, in ink, driven by the precomputed changeDiff — the show-changes state'` onward) exercise the `isShowingChanges` branch, untouched by Task 1 — leave these entirely alone.

    Two tests in this block DO need a full rewrite, since the content they assert moves inside the reveal:
    - `it('renders a coverage cue naming the covered rows and the covering step, and no cross-flag or remove-this-step control, for a removed step whose rows are all still used elsewhere (D-UAT-3)')`: KEEP its render-based assertion that the DEFAULT (closed) developing-mode markup for this removed step contains the coverage-cue sentence — the cue stays on the closed step (option B) so `renderToStaticMarkup` still reaches it. Keep the existing assertion that no `remove this step` button renders; add that the closed markup contains no `restore` button either (remove/restore now lives inside the reveal).
    - `it('renders remove/restore as restore on a removed step, alongside the on-demand openers')`: rewrite to assert the DEFAULT closed markup contains NONE of `restore`, `remove`, `add purpose`, `add aside` as button text, and DOES contain the literal text `edit this step` exactly once for that step, with `aria-label` matching `fieldLabel`'s own `"Removed step {n}, edit this step"` shape for a removed step (reuse this file's own `fieldLabel`-shaped aria assertions elsewhere in this file as the pattern).

    **Rewrite `describe('Method — the lead-in and instruction fields read as printed prose (03.1-04, D-13 § 8)')`** (the block immediately before "developing mode"): its current assertion (an `<input class="prose-field prose-field--lead-in">` visible directly in developing-mode markup) is no longer true by default. Change it to assert the CLOSED state instead: no `<input>`/`<textarea>` of any kind renders for this step in default developing-mode markup, and the plain lead-in/instruction text (`<b>{leadIn}.</b> {instruction}`) renders as prose inside `method-step__lead`, matching the reading state's own already-tested shape. Add a one-line comment noting the field-level input markup (`prose-field prose-field--lead-in`, unchanged since before this plan) is verified once the step is revealed — a UAT item, following the exact precedent at the file's own uses-checklist comment (`<read_first>`), not asserted here.

    **Rewrite `describe('Method — purpose and aside on demand (D-23)')`.** Both existing tests currently assert the purpose/aside field OR its opener button is visible directly in developing-mode markup — neither is true by default any more (both move inside the reveal). Replace with: (a) a test asserting the closed state renders `draftStep.purpose`/`draftStep.aside` as plain `<p className="method-step__purpose">`/`<p className="method-step__aside">` text when the baseline draft already carries them (reusing `makeBaselineVersionWithProse()`), and renders neither paragraph, neither an `add purpose`/`add aside` button, when the draft carries neither (reusing `makeBaselineVersion()`); (b) a one-line comment, matching the uses-checklist precedent, noting the on-demand opener/field toggle's own behaviour (unchanged from before this plan) is a UAT item once the step is revealed, not asserted here.

    **Rewrite `describe('Method — the uses line, closed by default (D-24)')`.** The uses line itself (not just its checklist) now also sits behind `edit this step`, so this block's own premise changes: replace `expect(markup).toContain('uses Row A')` / `'uses nothing yet'` with an assertion that NEITHER string appears in the default closed markup, and extend this block's own trailing comment (the one already at ~lines 749-756) to state explicitly that the uses line's own doubly-gated shape (edit this step, then change) is now two UAT items deep, not one — still using this file's own already-established disposition, not a new one.

    **Rewrite `describe('Method — accessible names carry the step (D-28)')`.** Replace the two `aria-label="Step 1, add purpose"`/`aria-label="Step 1, change"` assertions (no longer in default markup) with one asserting the new `edit this step` control's own `aria-label` reads `Step 1, edit this step` in the default markup, via `fieldLabel`.

    **`describe('Method — step display numbers ...')`** and everything after it in the file: unaffected by this task (the step's margin number renders outside `StepPenBody`, untouched by Task 1) — read to confirm, no edit expected.

    **`binder.test.js`.** Find the test `'no selector other than \`a\`/\`a:visited\` declares an underline (D-18)'` (the block `describe('no visual literal — every value is a var() read (D-13)')` — actually the links describe block above it, confirm via `<read_first>`) and update its expected array to include `.text-control` alongside `a`, in the ORDER `readAllRules` returns them (file order — run the suite once after Task 1's app.css edit and read the actual failure to confirm placement before hardcoding the array, rather than guessing). Add a new `describe('.text-control — the underline-only opt-out from the binder (03.1 Gap 1 override)', ...)` block, in this file's own established style (`ruleFor`, `resolveTokenPx`): one test asserting the `.text-control` rule declares `border: none` and `text-decoration: underline` through `var(--rule-ink-field)`; one test asserting `.text-control:hover` thickens to `var(--rule-baseline)`; one test asserting `.text-control:disabled` declares `text-decoration-style: dashed` and repeats `border: none`/`padding: 0` (the specificity-driven repeat Task 1's own action text explains).

    **`IngredientTable.test.jsx`.** Locate any existing assertion covering `RemoveRowControl`'s rendered `<button>` markup (search for `>remove<`/`>restore<`/the button inside the Remove column's cell, however plan 02 left it) and add or extend it to assert `className="text-control"` is present on that button; if no such assertion currently exists in the file plan 02 leaves behind, add one new, small test asserting it, in that file's own established style (do not restructure anything else in this file — this is a one-line addition, not a review of plan 02's own work).
  </action>
  <acceptance_criteria>
    - `app/src/ui/Method.jsx` does NOT contain `export function coverageSentence` (stays module-private — no export needed once the cue renders on the closed step).
    - `app/src/ui/Method.test.jsx`'s D-UAT-3 test still asserts the coverage-cue sentence in the DEFAULT closed developing-mode markup via `renderToStaticMarkup`.
    - `app/src/ui/Method.test.jsx` contains at least one assertion that the default developing-mode markup contains the literal text `edit this step`.
    - `app/src/ui/Method.test.jsx` contains at least one assertion that the default developing-mode markup does NOT contain `add purpose`, `add aside`, `restore`, or `<textarea` as visible/markup content for a step's own field set (the closed-by-default proof).
    - `app/src/styles/binder.test.js`'s underline-selector-list test's expected array contains both `.text-control` and `a`.
    - `app/src/styles/binder.test.js` contains a new describe block asserting `.text-control`'s rule declarations.
    - Full test suite passes; production build succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test -- src/ui/Method src/ui/IngredientTable src/styles</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit code, or any test output line containing "FAIL"</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit code, or output containing "error" (case-insensitive) from the Vite/Rollup build step</fails_when>
  </verify>
  <done>Method.test.jsx asserts the closed-by-default pen markup and no longer asserts field-level markup as unconditionally visible; the D-UAT-3 coverage cue is still asserted on the closed step through a render; binder.test.js's underline-selector list and its own new `.text-control` contract tests both pass; the full suite and build are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the uses checklist's open/close and its scoped Escape (deferred, LD-03)</name>
  <what-built>The pen's method now opens read-only by default, one `edit this step` text control per step revealing the step's existing fields — including the uses line and its "change" control, which opens a checklist of the recipe's rows. That checklist's own keydown handler calls `event.stopPropagation()` on Escape so the page's document-level Escape listener (which would otherwise cancel the whole pen) never fires for that key press; a second Escape, after the checklist is already closed, reaches the pen as normal. This behaviour is unchanged by this plan — only newly reachable, one click deeper than before, behind `edit this step`. Per LD-03, no jsdom interaction test was written for it (this project has no jsdom/testing-library dependency, and installing one is outside this plan's scope) — this checkpoint is the real, deferred confirmation.</what-built>
  <how-to-verify>
    1. Start the dev server (`npm --prefix app run dev` from `app/`, or reuse a server already running from 03.3-01/02's own checkpoints) and open the seeded olive oil recipe's current version.
    2. Open "Next version" (or "Develop") on the version row to enter the pen. Confirm the Method's steps now read as plain prose, each ending in an `edit this step` link-styled control (underlined, no border) — not the previous full form of fields.
    3. Click `edit this step` on any step that uses at least one ingredient row. Confirm its fields appear (lead-in, instruction, targets, purpose/aside openers, the uses line reading "uses ...").
    4. Click the uses line's `change` control. Confirm the checklist of ingredient rows opens (a list of checkboxes).
    5. Press Escape once. Confirm ONLY the checklist closes — the pen itself (the version's Develop ceremony) stays open, and keyboard focus returns to the `change` control.
    6. Press Escape a second time, now that the checklist is closed. Confirm this second press closes the whole pen (assuming no unsaved ink is present — if the draft is dirty, confirm instead that this second Escape does nothing, per D-24's unsaved-ink rule, and use Cancel to exit).
  </how-to-verify>
  <resume-signal>Type "approved" if the checklist's own Escape stays scoped to the checklist alone (step 5) and a second Escape reaches the pen (step 6), or describe what you observed instead (e.g. "the first Escape closed the whole pen")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | Client-only, local-storage SPA restructuring; the closed-state template reads the same already-domain-validated draft data the open form already reads and writes; no new input surface, no new stored field, no new network call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-033-06 | Tampering | `Method.jsx`'s new closed-state prose/target/purpose/aside render | low | mitigate | Plain JSX text-node interpolation transcribed from the already-audited show-changes branch — no `dangerouslySetInnerHTML`, the same escaping discipline every other cell in this file already carries. |
| T-033-07 | Information Disclosure | Moving the removed-row cross-flag and the uses line behind `edit this step` (the coverage cue stays on the closed step) | low | accept | Narrows default visibility, does not widen it; every fact remains reachable one click away, and the uses checklist's own reachability is explicitly exercised by this plan's deferred checkpoint (Task 3). |
| T-033-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installed this plan (confirmed: no jsdom, no testing-library added to `app/package.json`) — the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npm --prefix app test` passes in full after both automated tasks.
- `npm --prefix app run build` succeeds after both automated tasks.
- `app/src/ui/Method.jsx` opens every step read-only by default while developing, one `edit this step` `.text-control` per step, reachable via `grep -c 'edit this step' app/src/ui/Method.jsx` returning at least 1 (the button's own text).
- `.text-control` in `app.css` contains no literal unit outside `var(--...)` (grep-checkable, Task 1's own verify).
- `RemoveRowControl` in `IngredientTable.jsx` carries `className="text-control"`; `OrphanedRowFlag`'s cross-flag button does not.
- `StepRecordingControls`'s `done differently` button and both removed-row cross-flag buttons (`Method.jsx`, `IngredientTable.jsx`) remain binder-styled, unclassed by `.text-control` — a stated, sourced scope boundary, not an omission.
- The deferred checkpoint is the plan's final task, after both automated tasks, so execution continues past it per LD-03.
</verification>

<success_criteria>
- The pen's method opens read-only by default with one `edit this step` text control per step, revealing the step's existing, unchanged editable form on click (ROADMAP Scope bullet 4).
- `.text-control` exists, token-only, opting out of the global `button, select` binder rule, applied to the five named Method.jsx controls and IngredientTable.jsx's one named per-row control (03.1 Gap 1 override).
- The uses checklist's open/close and scoped Escape are recorded as a deferred, end-of-phase-UAT human-verify item per LD-03, not blocking this plan's completion.
- Method.test.jsx's closed-by-default assertions replace the now-unreachable field-level render assertions, following this file's own established "UAT item, not asserted here" precedent rather than inventing new test infrastructure.
- binder.test.js's underline-selector-list assertion and its own new `.text-control` contract both pass.
</success_criteria>

<output>
Create `.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New CSS class (`app/src/styles/app.css`):**
- `.text-control`, `.text-control:hover`, `.text-control:disabled` — the underline-only opt-out from the global `button, select` binder rule (sketch 003 variant B, 03.1 Gap 1 override), entirely token-sourced, no new token.
- `.method-step__edit` — the `edit this step` control's own layout rule (block, margin-top), mirroring `.method-step__on-demand`.

**New export (`app/src/ui/Method.jsx`):**

**New state (`app/src/ui/Method.jsx`, `StepPenBody`):**
- `stepReveal` — a per-step `useOnDemandField(false, () => {})` call gating the step's form between a new closed-state read-only render and the existing, unmodified `StepPenBody` fields.

**Reclassed controls:**
- `Method.jsx`: `add purpose`, `add aside`, the uses line's `change`/`done`, `StepPenBody`'s own `remove`/`restore` — all gain `.text-control`.
- `IngredientTable.jsx`: `RemoveRowControl`'s `remove`/`restore` gains `.text-control`.
- Explicitly NOT reclassed: `StepRecordingControls`'s `done differently` (a different pen); `Method.jsx`'s `remove this step` cross-flag; `IngredientTable.jsx`'s `OrphanedRowFlag`'s `remove this row` cross-flag.

**Deferred checkpoint:** the uses checklist's open/close and scoped Escape (LD-03) — the phase's second and final deferred human-verify item (the first, the fork's landing focus, was 03.3-01's).
</output>
