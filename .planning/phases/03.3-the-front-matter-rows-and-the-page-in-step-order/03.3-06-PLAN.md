---
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/domain/lineage.js
  - app/src/domain/lineage.test.js
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/styles/app.css
  - app/src/styles/tokens.css
autonomous: false
gap_closure: true
requirements: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After forking a new version, keyboard focus lands on the child version's own Next version button (D-27)."
    - "The version row's right-hand stack reads as sketch 003 variant B draws it: a Version region label, a Written date (root) or a From-version line folding in the written date (child), a Why line, a From-batch line where cited, and a Later count that discloses the descendant version list on demand."
    - "Next version and Record another (or Record batch, when the version has no batch yet) render side by side in the version row's own acts group."
  artifacts:
    - app/src/domain/lineage.js
    - app/src/ui/VersionRow.jsx
  key_links:
    - "descendantVersions(versionsForRecipe(versions, version.recipeId), version.id) feeds the Later toggle's count and, once opened, VersionStrip's filtered version list."
    - "VersionRow's own useEffect (not the autoFocus DOM attribute) drives focus onto the Next version button on a fresh fork-save mount."
    - "onStartRecording/openBatch, threaded from RecipePage into VersionRow, drive the relocated Record another/Record batch opener; the same values already reach BatchRow unchanged."
---

<objective>
Close G-03.3-1 and the version-row half of G-03.3-4: the version row (headnote's
right-hand stack) currently shows only a bare "Next version" button and a boxed
version-line chip — no written date, no reason, no From-batch line folded into
one place, no later-versions count, and "Record another" lives in the wrong
row. Separately, forking a new version lands keyboard focus on the wrong
element instead of the child's own "Next version" button.

Rebuild the version row's right-hand stack end to end against
`.planning/sketches/003-front-matter-rows/index.html` (sketch 003 variant B,
the confirmed acceptance target, `#vmeta`/`.row-version` markup and its
adjoining CSS at lines ~65-119): a "Version" region label, a
Written/From-version+date line, a Why line, a From-batch line, and a Later
disclosure — then move the "Record another"/"Record batch" opener beside
"Next version" in this row's own acts group, and fix the fork's landing
focus in the same pass since both live in the same acts-group code.

Purpose: The version row should carry the version's own facts and its two
starting actions in one place, per the confirmed design; forking a version
should hand the maker straight back into developing the next one, not strand
focus on a link.
Output: `domain/lineage.js` gains `descendantVersions`; `VersionRow.jsx`
carries the full right-hand stack and both acts-group buttons; the fork's
landing focus is deterministic and independent of DOM-attribute mount timing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-UAT.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-VERIFICATION.md
@.planning/sketches/003-front-matter-rows/index.html
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The version row's right-hand stack, end to end — region label, dl, and the Later disclosure</name>
  <files>app/src/domain/lineage.js, app/src/domain/lineage.test.js, app/src/ui/VersionRow.jsx, app/src/ui/VersionRow.test.jsx, app/src/styles/app.css, app/src/styles/tokens.css</files>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (lines 65-119: `.row-version`/`.vmeta`/`.acts`/`.list`/`.vtree` CSS; lines 201-234: the `#vmeta` markup and `#list-versions` section; lines 328-368, 440-447: the `VERSIONS`/`renderTree` demo data, illustrating that "later versions" is the full descendant subtree, not just direct children)
    app/src/ui/VersionRow.jsx (full file, already the current build)
    app/src/domain/lineage.js (full file — `sortedVersions`, `versionsForRecipe`, `createChildVersion`'s `createdAt`/`reason`/`parentVersionId` fields)
    app/src/data/olive-oil.js (lines ~45-53: the seed root version's own `parentVersionId: null`, `reason: null`, `createdAt: SEED_CREATED_AT` — confirms a root version has no reason recorded and a real createdAt)
    app/src/ui/VersionStrip.jsx (full file — the component this task re-scopes to descendants-only)
    app/src/styles/app.css (lines 417-494: `.versions__openers`/`.versions__opener-group`/`.versions__lineage`/`.versions__lineage-label`; lines 767+: `.region-name`)
    .planning/STATE.md (search "Kept 'From version'" — the 2026-09-10 decision to keep "From version" instead of the sketch's bare "From", for parallelism with "From batch"; this decision is LOCKED and must not be reverted)
  </read_first>
  <behavior>
    - `descendantVersions(versions, versionId)` with a 3-generation tree (root -> child -> grandchild) returns all 2 descendants (child and grandchild), regardless of order in the input array.
    - `descendantVersions(versions, versionId)` on a version with no children returns `[]`.
    - `descendantVersions` never mutates its `versions` argument and never reorders it.
  </behavior>
  <action>
    In `domain/lineage.js`, add `descendantVersions(versions, versionId)`: a pure function walking `parentVersionId` links recursively (children, then each child's own descendants) and returning the flat array of every descendant version record — mirroring the sketch's own `renderTree`, which discloses the WHOLE subtree flat, not just direct children. No framework/DOM/store import (matches this file's own header comment). Add its test to `lineage.test.js` per the `<behavior>` block above, written before the implementation.

    In `VersionRow.jsx`, restructure the right-hand stack (currently three standalone `<p className="versions__lineage">` elements, gated entirely behind `version.parentVersionId`, plus no visible "Version" label at all):

    - Add a visible `<h2 className="region-name">Version</h2>` heading at the top of the stack (the existing `<section aria-label="Version">` wrapper already carries "Version" as an accessible name only — this adds the sketch's visible label alongside it; leave the `aria-label` as-is, matching the same redundant-but-harmless pattern this file's own `aria-label="Version"`/upcoming `Batch` heading already use elsewhere in this phase).
    - Wrap the stack's facts in one new `<dl className="version-row__meta-list">`:
      - When `!version.parentVersionId` (root): `<dt className="versions__lineage-label">Written</dt><dd className="versions__lineage">{formatRecordDate(version.createdAt)}</dd>` (reuse the file's already-imported `formatRecordDate`; `version.createdAt` is always a real ISO string per `createChildVersion`/`saveOverVersion`/the seed, never guarded elsewhere in this file, so no defensive null-check here either).
      - When `version.parentVersionId` (child): keep the label **"From version"** exactly as already built (the locked 2026-09-10 decision above) — do not revert to the sketch's bare "From". Fold the written date into the SAME `<dd>`, after the existing Link/plain-text branch: `{...existing branch...}{' · written '}{formatRecordDate(version.createdAt)}`.
      - Always (both root and child): `<dt className="versions__lineage-label">Why</dt><dd className="versions__lineage">{version.reason ? version.reason : 'no reason recorded'}</dd>` — this line currently only renders for a child; un-gate it so a root version's row also states "no reason recorded" rather than omitting the fact.
      - Only when `version.citedBatchId && citedBatch` (existing condition, unchanged): `<dt className="versions__lineage-label">From batch</dt><dd className="versions__lineage">{...existing Link/plain-text branch, unchanged...}</dd>`.
      - Only when the descendant count is greater than zero: `<dt className="versions__lineage-label">Later</dt><dd className="versions__lineage"><button type="button" className="text-control" aria-expanded={laterVersionsOpen} onClick={() => setLaterVersionsOpen((open) => !open)}>{count} later version{count === 1 ? '' : 's'}</button></dd>`, where `count = descendantVersions(versionsForRecipe(versions, version.recipeId), version.id).length` and `laterVersionsOpen` is new local `useState(false)`.
    - Below the dl, when `laterVersionsOpen` is true, render a `<section aria-label="Later versions"><h2 className="region-name">Later versions</h2><VersionStrip versions={descendants} recipeId={version.recipeId} currentId={version.id} versionIdsWithBatches={versionIdsWithBatches} openPen={openPen} penReason={penReason} /></section>` — `descendants` is the SAME array the count above computed. Remove the current unconditional `<VersionStrip .../>` call (lines ~178-185 today) entirely; VersionStrip now renders only inside this disclosure, fed descendants of the CURRENT version rather than every version of the recipe. This is a deliberate, sketch-faithful behavior change (see `<reversibility>` below), not an oversight.
    - Where the `<dl>` replaces the three old `versions__lineage` paragraphs, reuse the SAME `versions__lineage`/`versions__lineage-label` class names on the new `dt`/`dd` elements (only the wrapping element changes, from `<p>` to a `<dl>` of `<dt>`/`<dd>` pairs) — this keeps the existing CSS rules (app.css ~469-486) working unchanged via class selectors that don't care about element type.
    - In `app.css`, add ONE new rule: `.version-row__meta-list { display: grid; grid-template-columns: max-content 1fr; gap: var(--gap-hair) var(--gap-s); margin: 0; }` and `.version-row__meta-list dd { margin: 0; }` (removing the browser's default `<dd>` indent) — every value here already reads an existing token (`--gap-hair`, `--gap-s`); do not add a new token unless this genuinely proves insufficient once built.
    - Update `VersionRow.test.jsx`: the existing case(s) asserting `VersionStrip`/sibling-version links render unconditionally now assert the CLOSED-by-default state instead (button present with the correct count text, `version-strip` markup absent) — following this file's own established precedent for internal disclosure state that only a live browser can open (Method.test.jsx's `stepReveal`/`usesOpen` precedent, cited in PATTERNS.md). Add a case for a root version with zero later versions and zero reason (asserts "Written", "no reason recorded", and no "Later" dt/dd at all) and a case for a child version with a reason and a later-versions count greater than zero (asserts "From version" — not "From" — folded with "· written", "Why" with the real reason, and the Later button's exact count text).
  </action>
  <acceptance_criteria>
    - domain/lineage.js exports descendantVersions; lineage.test.js has passing cases matching the &lt;behavior&gt; block (3-generation tree, no-children case, no mutation).
    - VersionRow.jsx renders an unconditional `&lt;h2 className="region-name"&gt;Version&lt;/h2&gt;`.
    - VersionRow.jsx renders a `&lt;dl className="version-row__meta-list"&gt;`: a root version's dl contains "Written"; a child version's dl contains "From version" (never bare "From") folded with "· written".
    - A "Why" dt/dd pair renders for both root and child versions, reading "no reason recorded" when version.reason is null.
    - A "Later" dt/dd pair renders only when descendantVersions(...).length is greater than zero, with the exact count text "{N} later version{s}".
    - VersionStrip is called exactly once in the file, inside the Later disclosure, fed the descendants array rather than the full versions list.
    - app.css's new .version-row__meta-list rule reads only existing tokens (--gap-hair, --gap-s) — no new token added.
  </acceptance_criteria>
  <reversibility rating="reversible">
    Repurposing VersionStrip from "always show every version of the recipe" to "only the current version's descendants, disclosed on demand" is a real behavior change with no direct precedent elsewhere in this app. It is reversible: a future gap could restore an always-visible full-recipe list as a separate element without touching this disclosure, since nothing here deletes VersionStrip's own filtering logic — it only changes what array this call site feeds it. Chosen because the sketch (the confirmed acceptance target) draws exactly this: a "Later" count with a toggle-revealed subtree, not a permanent full-recipe list.
  </reversibility>
  <verify>
    <automated>npm --prefix app test -- src/domain/lineage</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or descendantVersions is missing from the module's exports</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix app test -- src/ui/VersionRow</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'VersionStrip' app/src/ui/VersionRow.jsx</automated>
    <fails_when>the count is not exactly 2 (one import line, one call site inside the Later disclosure) — 1 means the call site was dropped, more than 2 means an unconditional second call site still exists alongside the disclosed one</fails_when>
  </verify>
  <done>domain/lineage.js exports descendantVersions with a passing test; VersionRow.jsx renders the Version region label, the Written/From-version+Why/From-batch dl, and a Later toggle that discloses VersionStrip scoped to descendants only; app.css carries the new .version-row__meta-list rule reading existing tokens only.</done>
</task>

<task type="auto">
  <name>Task 2: Acts group — relocate the Record opener beside Next version, and fix the fork's landing focus</name>
  <files>app/src/ui/VersionRow.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx</files>
  <read_first>
    app/src/ui/BatchRow.jsx (lines 147-268: the recordButtonRef/wasRecordingRef/effect trio and the openPen === null opener group)
    app/src/ui/RecipePage.jsx (lines 240-260: focusDevelopOnMount/location; lines 1073-1107: the two save handlers; lines 1109-1200: the VersionRow/BatchRow JSX call sites)
    app/src/router.jsx (the `${id}::${batchId}` key forcing a fresh RecipePage mount per version)
    app/src/main.jsx (StrictMode wraps the tree)
    app/src/ui/VersionStrip.jsx (confirms no `.focus()` call exists on the current-version Link — ruled out as a competing focus target)
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md ("Focus-return-on-close" shared pattern — the move-map discipline: ref + wasXRef + its own useEffect move as one indivisible unit)
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-UAT.md (G-03.3-1's full reason text)
  </read_first>
  <action>
    Move the `recordButtonRef`/`wasRecordingRef`/`useEffect` trio verbatim from BatchRow.jsx (~lines 151-162) into VersionRow.jsx, as one indivisible unit per PATTERNS.md's own move-map discipline. VersionRow gains two new props, `openBatch` and `onStartRecording`. Add the button into VersionRow's existing `versions__opener-group` div, beside "Next version": `<button type="button" ref={recordButtonRef} onClick={onStartRecording}>{openBatch ? 'Record another' : 'Record batch'}</button>`.

    Delete the moved ref/effect/button from BatchRow.jsx entirely, along with the now-unused `onStartRecording` prop (BatchRow keeps its `openBatch` prop — it is still read elsewhere in that file for the measured cells and tasting content). Update BatchRow.test.jsx to drop assertions tied to the deleted button. In RecipePage.jsx, add `openBatch={openBatch}` and `onStartRecording={handleStartRecording}` to the `<VersionRow>` call site — both values already exist in this file (the same references BatchRow's own call site already uses); do not recompute them.

    Fix G-03.3-1: replace `autoFocus={focusDevelopOnMount}` on the "Next version" button (VersionRow.jsx ~line 164) with an explicit effect: `useEffect(() => { if (focusDevelopOnMount) developButtonRef.current?.focus(); }, [focusDevelopOnMount]);`. Remove the `autoFocus` prop from that button — one focus mechanism, not two. Record the reasoning in a code comment near the effect, matching this file's existing comment density: the native `autoFocus` DOM attribute's exact firing point relative to React's commit phases is subtle when the whole RecipePage instance remounts fresh via router.jsx's `${id}::${batchId}` key (main.jsx wraps the tree in StrictMode, and RecipePage returns null entirely until its async `repository.getVersion` call resolves) — a page-level `useEffect`, which runs only after the full real tree (the child's actual data) is committed and is the last thing this component does in that commit, is deterministic regardless of that timing and cannot be pre-empted by anything else in the same render. Confirmed by grep across `app/src` that no other `.focus()`/`autoFocus` call is reachable on this reading-state, `openPen === null` mount path: VersionRow's own plan-pen ceremony `autoFocus` and BatchRow's record/amend/tasting ceremony `autoFocus`es only render while a pen is open, and no `.focus()` call exists anywhere on VersionStrip's current-version `Link`.
  </action>
  <acceptance_criteria>
    - VersionRow.jsx's acts group renders both "Next version" and "Record another"/"Record batch" buttons side by side.
    - BatchRow.jsx no longer references onStartRecording, recordButtonRef, or wasRecordingRef anywhere in the file.
    - VersionRow.jsx's "Next version" button carries no autoFocus attribute; a useEffect keyed on [focusDevelopOnMount] calls developButtonRef.current?.focus().
    - RecipePage.jsx's &lt;VersionRow&gt; call site passes openBatch and onStartRecording using the same references already passed to &lt;BatchRow&gt;.
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'autoFocus' app/src/ui/VersionRow.jsx</automated>
    <fails_when>the "Next version" button's line still shows `autoFocus={focusDevelopOnMount}` — the only remaining autoFocus occurrence should be the plan-pen ceremony's version-line input</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'onStartRecording' app/src/ui/BatchRow.jsx</automated>
    <fails_when>the count is not 0 (the prop/handler must be fully removed from BatchRow now that VersionRow owns the opener)</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix app test -- src/ui/VersionRow src/ui/BatchRow src/ui/RecipePage</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix app test &amp;&amp; npm --prefix app run build</automated>
    <fails_when>either command exits non-zero</fails_when>
  </verify>
  <done>VersionRow's acts group renders "Next version" and "Record another"/"Record batch" side by side, wired through the same onStartRecording/openBatch RecipePage already computes for BatchRow; BatchRow no longer owns that opener or its ref; the Next version button's landing focus is driven by an explicit useEffect, not the autoFocus DOM attribute.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Re-confirm the fork's landing focus and the Later-versions disclosure live (deferred — Mark away)</name>
  <files></files>
  <read_first>
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-VERIFICATION.md (the original deferred item 1, same steps, now re-run against the useEffect-based fix)
  </read_first>
  <what-built>Task 1's Later-versions disclosure and Task 2's relocated Record opener plus the useEffect-based fork-focus fix, all in VersionRow.jsx — the dev server is not yet running; start it as part of verifying this checkpoint.</what-built>
  <how-to-verify>
    1. Run `npm --prefix app run dev` and navigate to the seeded olive oil recipe's current version.
    2. In the version row, open "Next version", fill in a version line (e.g. "48 g oil · 800 g") and a reason, and click Save (or "Save as", if a batch already exists on this version) to fork a new child version — the page navigates to the child version's own URL.
    3. On the child version's page, without clicking anything, check where keyboard focus landed, then press Tab once. Expected: focus lands on the child version's own "Next version" button on mount, and a single Tab press moves focus forward in a sensible order (not lost to body).
    4. Separately, on any version, click the "Later" count link in the version row's meta stack. Expected: opens a list of the version's own descendant versions (not every version of the recipe); a second click closes it again.
  </how-to-verify>
  <resume-signal>Type "approved" if both behaviors match, or describe what actually happened.</resume-signal>
  <done>Mark confirms both behaviors match, or reports a remaining issue for a further gap.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `descendantVersions(versions, versionId)` — new pure function in `app/src/domain/lineage.js`.
- `.version-row__meta-list` — new CSS rule in `app/src/styles/app.css` (the vmeta `<dl>`'s grid).
- VersionRow's `laterVersionsOpen` local state and its "N later versions" disclosure toggle.
- VersionRow's new `openBatch`/`onStartRecording` props and its relocated "Record another"/"Record batch" opener (ref + effect moved from BatchRow.jsx).
- VersionRow's explicit fork-focus `useEffect`, replacing the `autoFocus={focusDevelopOnMount}` DOM attribute.

<threat_model>
## Trust Boundaries

Local-only IndexedDB app, no network, no auth. This plan renders more of a
version's own already-stored fields (createdAt, reason) and computes a
derived descendant list from data already loaded on the page; it introduces
no new trust boundary or external input path.

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | All data rendered (createdAt, reason, descendant versions) already exists in this maker's own local store; nothing new is fetched, transmitted, or accepted from outside the app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3-06-01 | Tampering | VersionRow's rendered `reason`/`versionLabel` text | low | accept | Rendered as JSX text content (React's default escaping), never via `dangerouslySetInnerHTML` — a project-wide invariant this plan does not touch. |
| T-03.3-06-02 | Information Disclosure | The Later-versions disclosure, scoped to descendants of the current version | low | accept | Descendant filtering is by `parentVersionId` links already present in the local store the maker themselves owns; no cross-recipe or cross-profile data is newly exposed — this is a single-user local app. |
</threat_model>

<verification>
- `npm --prefix app test` passes in full.
- `npm --prefix app run build` succeeds.
- The grep-based structural checks above confirm: `descendantVersions` exported and tested; VersionStrip called exactly once, inside the Later disclosure; no `autoFocus` DOM attribute remains on the Next version button; `onStartRecording` fully removed from BatchRow.
- Live confirmation via the deferred human-verify checkpoint (Task 3) closes G-03.3-1 for real; until Mark runs it, the gap stays open per this project's standing away-checkpoint convention.
</verification>

<success_criteria>
G-03.3-1 is closed (the fork's landing focus is deterministic, sourced from an explicit effect rather than DOM-attribute timing) and the version-row half of G-03.3-4 is closed (the right-hand stack and acts group match sketch 003 variant B), both confirmed at the source level by the checks above and, once Mark is back, by the live checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-06-SUMMARY.md` when done
</output>