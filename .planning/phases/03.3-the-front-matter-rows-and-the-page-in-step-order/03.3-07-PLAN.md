---
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
plan: 07
type: execute
wave: 2
depends_on: ["03.3-06"]
files_modified:
  - app/src/domain/rows.js
  - app/src/domain/rows.test.js
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/styles/app.css
  - app/src/styles/tokens.css
autonomous: false
gap_closure: true
requirements: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A version with no batch recorded shows exactly one 'no batch yet' line in the batch row."
    - "The batch row reads as sketch 003 variant B draws it: a Batch region label with the churned date and a later-batches count on one head line; measured figures with plain-word labels and units in small print beside each value, a plan sub-line under Time to temperature; a Tasting head line naming the temperature and date, then only the marked axes; Correct and Add tasting as underlined text controls at the foot of the row."
  artifacts:
    - app/src/domain/rows.js
    - app/src/ui/BatchRow.jsx
  key_links:
    - "targetValueFor(version, 'come-up') feeds the Time to temperature cell's plan sub-line, reading the version's own active method steps, never a batch snapshot field."
    - "batches.length - (openBatch ? 1 : 0) feeds the batch row's 'N later batches' count and its own disclosure toggle."
---

<objective>
Close G-03.3-3 and the batch-row half of G-03.3-4. G-03.3-3: a version with no
recorded batch currently shows "no batch yet" twice (an empty-record
paragraph plus the batch list's own single entry) — drop the paragraph,
keep the D-09 list entry. G-03.3-4 (batch-row half): the batch row currently
puts three binder buttons above the "Batch" label, prints "churned {date}"
as a separate boxed line at the row's foot, folds units into ALL-CAPS
measured-field labels, drops the plan/sub-lines, prints "unknown" where the
sketch prints "not measured", shows every tasting axis (marked or not)
under a bare "date unknown" heading, and renders "recorded ... against ..."
as loose prose instead of a figure.

Rebuild the batch row against `.planning/sketches/003-front-matter-rows/index.html`
(sketch 003 variant B, the confirmed acceptance target, `.row-batch` markup
and CSS at lines ~65-172, 236-263): one head line (Batch label, churned
date, later-batches count), measured cells with plain-word labels and
units-beside-figures, a Tasting section showing only marked axes, and
Correct/Add tasting relocated to underlined text controls at the row's foot.

Purpose: the batch row should read the way the confirmed design draws it —
figures for measured facts, prose for the maker's own words, and openers that
match their visual weight to what they do.
Output: `domain/rows.js` gains `targetValueFor`; `BatchRow.jsx` matches the
sketch's head line, cell shape, tasting section, and foot controls.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-UAT.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md
@.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-VERIFICATION.md
@.planning/sketches/003-front-matter-rows/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The head line, the measured cells, and the no-batch fix</name>
  <files>app/src/domain/rows.js, app/src/domain/rows.test.js, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/styles/app.css, app/src/styles/tokens.css</files>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (lines 65-172: `.row-batch`/`.head`/`.cells`/`.cell`; lines 236-244: the `#front`'s `.row-batch` markup for the head and the four measured cells)
    app/src/ui/BatchRow.jsx (full file, already the current build)
    app/src/domain/rows.js (full file — `activeRows`/`activeSteps`/`rowGrams`, the home for the new lookup)
    app/src/data/olive-oil.js (lines ~155-162: the pasteurisation step's own `targets` array — label `'come-up'`, value `'10–12 min'` — the real data this task's sub-line reads, not the sketch's fictional "time to temperature 10–12 min" label)
    app/src/domain/batch.js (lines 152-170: `readMeasured`'s own doc comment — "there is no code path here by which the recipe's own target can reach this function" — this task must not violate that invariant; the new plan-target lookup stays entirely separate from readMeasured)
    .planning/STATE.md (search "Draw temperature confirmed untouched" — the 2026-09-10 labels-pass verification note; this label's TEXT is locked and must not be renamed to the sketch's "Out of the machine")
    app/src/styles/app.css (lines 1208-1235: `.batch-row__cells`/`.batch-row__cell`/`.batch-row__cell-label`/`.batch-row__cell-value`, from 03.3-01)
  </read_first>
  <behavior>
    - `targetValueFor(version, 'come-up')` on the seeded olive-oil version returns `'10–12 min'` (the pasteurisation step's own target value).
    - `targetValueFor(version, 'no-such-label')` returns `null`.
    - `targetValueFor` never reads a removed step's targets (built on `activeSteps`, matching this module's own removal discipline).
  </behavior>
  <action>
    In `domain/rows.js`, add `targetValueFor(version, label)`: walks `activeSteps(version)` in order, returns the `value` of the first target whose `label` strictly equals the given `label`, or `null` if none matches. Pure, no framework/DOM/store import. Add its test to `rows.test.js` per the `<behavior>` block above, written before the implementation.

    In `BatchRow.jsx`, restructure the head and the measured cells for the OPEN-BATCH reading state only (the `mode === 'recording'` branch's own field labels/aria-labels are untouched by this task — this is a read-only-display change):

    - Add a head line above `<div className="batch-margin">`: `<div className="batch-row__head"><h2 className="region-name">Batch</h2>{openBatch && <span className="batch-row__date">{`churned ${openBatch.churn.churnDate ? formatRecordDate(openBatch.churn.churnDate) : 'date unknown'}`}</span>}{laterBatchesCount > 0 && <button type="button" className="text-control" aria-expanded={laterBatchesOpen} onClick={...}>{laterBatchesCount} later batch{laterBatchesCount === 1 ? '' : 'es'}</button>}</div>`, where `laterBatchesCount = batches.length - (openBatch ? 1 : 0)` and `laterBatchesOpen` is a new local `useState(false)`. Add ONE new rule in app.css: `.batch-row__head { display: flex; align-items: baseline; gap: var(--gap-s); flex-wrap: wrap; }` and `.batch-row__date { font-family: var(--face-text); font-size: var(--size-version-line); color: var(--pen-blue); }` — both values read existing tokens only.
    - Gate the existing `<ul className="batch-margin__list">` (the D-09 always-a-list batch list) behind `laterBatchesOpen` when `batches.length > 0` (when there IS at least one batch, the list is now the disclosed "later batches" panel, matching the head line's own count — it should NOT also render unconditionally beneath the row anymore). When `batches.length === 0`, keep the list rendering unconditionally exactly as it does today (D-09's "always a list" requirement — a version with zero batches has no count to disclose, so there is nothing to gate).
    - Fix G-03.3-3 in the same pass: the line `<p>{batches.length > 0 ? 'No batch of this version has that address.' : 'no batch yet'}</p>` (currently ~line 392, the `openPen === null`... rather the `!openBatch` branch inside `<div className="batch-margin">`) becomes `{batches.length > 0 && <p>No batch of this version has that address.</p>}` — when `batches.length === 0`, this paragraph renders nothing at all; the D-09 list's own `<li>no batch yet</li>` entry remains the ONLY place that phrase appears.
    - Restructure the three churn measured cells (Time to temperature, Draw temperature, Air/overrun) from a single label string plus `readMeasured(...)` to: a plain-word label (dropping the unit suffix from the LABEL text), the value with its unit in a small-print `<span>` beside it, and — for the FIRST two cells only — a sub-line. Concretely:
      - Cell 1: label **"Time to temperature"** (unchanged text, per the confirmed-untouched labels pass — only the ", min" suffix moves out of the label and into the unit span). Value: `{churnMeasured(openBatch.churn.comeUpMinutes)}<span className="batch-row__unit"> min</span>` when present, or `<span className="batch-row__unit">not measured</span>` alone when absent. Sub-line: `{targetValueFor(version, 'come-up') && <span className="batch-row__plan">plan {targetValueFor(version, 'come-up')}</span>}`.
      - Cell 2: label **"Draw temperature"** — kept exactly as the confirmed-untouched 2026-09-10 labels pass settled it; do NOT rename to the sketch's "Out of the machine" and do NOT add a "draw temperature" sub-line (it would be redundant with the label itself, and this label's text is locked). Value: signed reading with a "°C" unit span, or "not measured" when absent. No sub-line for this cell.
      - Cell 3: label **"Air"** (drop the ", overrun %" suffix from the label — this DOES change from the current folded string, since it was never itself one of the confirmed-untouched labels, and splitting word from term matches this same phase's own figures.js word/term precedent). Value with a "%" unit span, or "not measured" when absent. Sub-line: static text `overrun` (the technical term, matching the figures.js word+term idiom applied here to a churn field for the first time).
      - Define a small named helper near the top of BatchRow.jsx (alongside the existing `TastingReading`/`TastingForm` module-level helpers, not inline in JSX): `churnMeasured(value, options)` — calls the existing `readMeasured(value, options)` and returns `'not measured'` in place of its `'unknown'` result, leaving `readMeasured` itself, and every OTHER call site of it in this codebase, completely unchanged (BATCH1-02's own requirement text says a blank field "stays unknown" — this helper is a display-only override scoped to this row's own measured cells, not a change to the shared domain contract).
    - Do NOT build a new "oil bottle open" stored field or cell. This is a deliberate scope decision, not an oversight (see `<reversibility>` below) — see this task's own `<read_first>` note; the phase's own gap-closure instructions name only two items the row must newly thread through from the domain ("the later-versions link, the plan sub-line" — 03.3-UAT.md G-03.3-4's closing sentence), and both are built by this plan set. "Oil bottle open" names a date field that does not exist anywhere in this app's stored batch shape, in RESEARCH.md, or in REQUIREMENTS.md; inventing one would be a batch-schema change, not a layout-fidelity fix.
    - Instead, convert the two REAL existing prose facts this row already renders as full sentences into figure-style cells, added to the SAME `batch-row__cells` grid as the three measured cells above: `{latestAmendment && <div className="batch-row__cell"><span className="batch-row__cell-label">Amended</span><span className="batch-row__cell-value">{formatRecordDate(latestAmendment)}</span></div>}` and `<div className="batch-row__cell"><span className="batch-row__cell-label">Recorded</span><span className="batch-row__cell-value">{`${formatRecordDate(openBatch.recordedAt)} against ${openBatch.snapshot.versionLabel}`}</span></div>` — replacing the two `<p className="ink-text">` lines these facts currently render as (do not delete the underlying `openBatch.recordedAt`/`openBatch.snapshot.versionLabel`/`latestAmendment` reads, only their markup).
  </action>
  <acceptance_criteria>
    - domain/rows.js exports targetValueFor; rows.test.js has passing cases matching the &lt;behavior&gt; block (real seed match, no-match returns null, removed steps excluded).
    - BatchRow.jsx's reading state renders exactly one "no batch yet" occurrence — the D-09 list's own &lt;li&gt; — when batches.length is 0; the standalone paragraph renders nothing in that state.
    - The "Time to temperature" cell's label text is unchanged; its value carries a "min" unit span; a "plan {value}" sub-line renders only when targetValueFor(version, 'come-up') is non-null.
    - The "Draw temperature" label text is unchanged (not renamed to "Out of the machine") and carries no sub-line.
    - The "Air" cell's label reads "Air" alone with a "%" unit beside the value and a static "overrun" sub-line.
    - A blank value in these three cells reads "not measured" via the new churnMeasured helper; readMeasured's own default export and its other call sites are untouched.
    - The "recorded ... against ..." and "amended ..." facts render as batch-row__cell figures, not &lt;p className="ink-text"&gt; sentences.
  </acceptance_criteria>
  <reversibility rating="reversible">
    Declining to invent a new "oil bottle open" batch field is reversible — a future phase could add it as a genuine BATCH1-02-style measured field (with its own pen input) without this plan's own work needing to change. Chosen because no source artifact (REQUIREMENTS.md, RESEARCH.md, the phase's own gap-closure thread-through list) names this as data to add; only the sketch's own illustrative demo data shows it.
  </reversibility>
  <verify>
    <automated>npm --prefix app test -- src/domain/rows</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or targetValueFor is missing from the module's exports</fails_when>
  </verify>
  <verify>
    <automated>grep -c "no batch yet" app/src/ui/BatchRow.jsx</automated>
    <fails_when>the count is not exactly 1 (the phrase must appear in the D-09 list's own `&lt;li&gt;` only, never in a separate paragraph)</fails_when>
  </verify>
  <verify>
    <automated>grep -c "Draw temperature" app/src/ui/BatchRow.jsx</automated>
    <fails_when>the count drops to 0 (the locked label text must survive this task's rework — see the read_first note on "Draw temperature confirmed untouched")</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>domain/rows.js exports targetValueFor with a passing test; BatchRow's reading state shows one head line (Batch label, churned date, later-batches count), three measured cells with plain-word labels and units-beside-figures, a plan sub-line under Time to temperature, and no duplicate "no batch yet" line; the two prose facts (recorded-against, amended) render as figure cells.</done>
</task>

<task type="auto">
  <name>Task 2: The tasting section and the foot controls</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/styles/app.css</files>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (lines 93-94, 130-138 CSS for `.tasting`; lines 245-255 markup: the "Tasting · at −12 °C · date unknown" head line, only-marked-axes cells, and the `.acts` Correct/Add tasting text controls)
    app/src/ui/BatchRow.jsx (the current build, including this plan's own Task 1 changes: `TastingReading`, the `amendButtonRef`/`wasAmendingRef`/`addTastingButtonRef`/`wasTastingRef` effect pairs, and the now-empty `openPen === null` opener branch left behind once Task 1's plan-06 predecessor moved Record out)
    app/src/domain/axes.js (full file — `axesForBatch`, `markKeyFor`)
  </read_first>
  <action>
    Restructure `TastingReading` (module-level function, top of BatchRow.jsx): replace the separate `batch-margin__legend` date paragraph and the standalone "Tasting temperature, °C" cell with one head line: `<p className="region-name">Tasting <span className="batch-row__tasting-meta">{`· at ${churnMeasured(tasting.tastingTempC, { signed: true })} · ${dateWords}`}</span></p>` (reuse the `churnMeasured` helper Task 1 added, for the same "not measured" override). Add one new CSS rule, `.batch-row__tasting-meta { text-transform: none; letter-spacing: 0; color: var(--pen-blue); }`, undoing `.region-name`'s own uppercase/letter-spacing for this one inline span, matching the sketch's inline style override at index.html:246.

    Filter the per-axis cells to marked axes only: `axes.filter((axis) => Object.prototype.hasOwnProperty.call(tasting.marks, markKeyFor(axis)))` — an axis with no key in `tasting.marks` is dropped entirely from this render, no "unmarked" cell for it. Keep the melt-test cell unconditionally (present value or "not measured" via `churnMeasured`), with its unit span reading the static compound string `g at 20 min` (the melt-test protocol's own fixed timing, not a per-record field — matches the sketch's own static unit text for this one cell).

    Relocate the "Correct"/"Add tasting" buttons from the top of BatchRow's return (the `openPen === null` branch) to the row's foot, after the `batch-margin` content and its list. Their existing refs (`amendButtonRef`, `addTastingButtonRef`) and effects stay exactly where they are declared (unconditional, top of the function body) — only the JSX rendering their `<button>` elements moves, and their `className` changes from unclassed to `"text-control"`. Wrap them in a new `<div className="batch-row__acts">` (one new CSS rule: `.batch-row__acts { display: flex; gap: var(--gap-s); flex-wrap: wrap; margin-top: var(--gap-s); }`, existing tokens only). Since Record another/Record batch already left this file in the prior plan (03.3-06), the `openPen === null` branch's TOP content is now entirely empty — delete that branch's `versions__openers`/`versions__opener-group` wrapper markup outright; leave the `{openPen && <p className="versions__hint">Links return after you save or cancel.</p>}` line exactly where it sits today (it does not depend on the deleted wrapper).

    The ceremony branches (`openPen === 'record' || 'amend'` and `openPen === 'tasting'`) keep rendering at the TOP of BatchRow's return, unchanged — this is a deliberate, documented scoping choice: this task moves where the OPENER buttons sit, not where their ceremonies subsequently render (each ceremony's own `autoFocus` field still carries keyboard focus there regardless of the opener's position, and every OTHER pen in this app already follows the same "ceremony replaces the top" pattern).
  </action>
  <acceptance_criteria>
    - TastingReading renders one head line combining "Tasting", the temperature, and the date; no separate "Tasting temperature, °C" cell remains.
    - Only an axis present as a key in tasting.marks renders a cell; the word "unmarked" renders nowhere in the reading state.
    - The melt-test cell always renders, with a static "g at 20 min" unit span.
    - "Correct" and "Add tasting" both carry className="text-control" and render after the batch-margin content and its list, not before it.
    - The openPen === null branch no longer renders a versions__openers/versions__opener-group wrapper.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'className="text-control"' app/src/ui/BatchRow.jsx</automated>
    <fails_when>the count is less than 2 (Correct and Add tasting must both carry the text-control class)</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'unmarked' app/src/ui/BatchRow.jsx</automated>
    <fails_when>the count is not 0 (an unmarked-axis cell must no longer render in the reading state)</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow &amp;&amp; npm --prefix app test &amp;&amp; npm --prefix app run build</automated>
    <fails_when>any command exits non-zero, or "0 passed" appears in a summary line</fails_when>
  </verify>
  <done>Tasting reads as one head line naming the temperature and date, then only marked axes plus melt test; Correct and Add tasting render as underlined text controls at the row's foot, not binder buttons at the top; full suite and build stay green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Visual comparison against sketch 003 variant B, live (deferred — Mark away)</name>
  <files></files>
  <read_first>
    .planning/sketches/003-front-matter-rows/index.html (open directly in a browser, variant B, for a side-by-side comparison)
    .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-UAT.md (G-03.3-4's full itemised diff, to check each item individually rather than eyeballing the whole row)
  </read_first>
  <what-built>Task 1's head line/measured-cell rework and Task 2's tasting/foot-controls rework, all in BatchRow.jsx — the dev server is not yet running; start it as part of verifying this checkpoint.</what-built>
  <how-to-verify>
    1. Run `npm --prefix app run dev` and open the seeded olive oil recipe (a version with at least one recorded batch and one tasting, e.g. the churned version and its 2 Aug batch).
    2. Open the sketch file (`.planning/sketches/003-front-matter-rows/index.html`) in another tab/window at the same time, on variant B.
    3. Compare the batch row's head line, measured cells, tasting section, and foot controls item by item against G-03.3-4's own itemised list.
    4. Expected: one "BATCH · churned {date} · N later batches" head line; measured cells with plain-word labels and units in small print beside each figure, a "plan" sub-line under Time to temperature; only marked tasting axes under one "Tasting · at {temp} · {date}" line; Correct and Add tasting as underlined text at the foot, not bordered buttons at the top.
    5. Click the "N later batches" link and confirm it opens and closes the batch list correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if the row matches the sketch, or describe specific remaining divergences.</resume-signal>
  <done>Mark confirms the row now matches the sketch, or reports specific remaining divergences for a further gap.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `targetValueFor(version, label)` — new pure function in `app/src/domain/rows.js`.
- `churnMeasured(value, options)` — new local display helper in `app/src/ui/BatchRow.jsx`, wrapping `readMeasured` for this row's own "not measured" wording.
- `.batch-row__head`, `.batch-row__date`, `.batch-row__unit`, `.batch-row__plan`, `.batch-row__tasting-meta`, `.batch-row__acts` — new CSS rules in `app/src/styles/app.css`, every value reading an existing token.
- BatchRow's `laterBatchesOpen` local state and its "N later batches" disclosure toggle over the existing batch list.
- Two new figure-style cells (Amended, Recorded) replacing the two prose sentences the row previously rendered for the same facts.

<threat_model>
## Trust Boundaries

Local-only IndexedDB app, no network, no auth. This plan only reformats
already-stored batch/tasting fields for display and adds one pure lookup
over the version's own already-loaded method steps; it introduces no new
trust boundary or external input path.

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | All data rendered (churn fields, tasting marks, method targets) already exists in this maker's own local store; nothing new is fetched, transmitted, or accepted from outside the app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3-07-01 | Tampering | BatchRow's rendered prose (`drawNotes`, `ingredientNotes`, `nextTimeNote`, tasting `words`) | low | accept | Rendered as JSX text content, never via `dangerouslySetInnerHTML` — unchanged by this plan, a project-wide invariant. |
| T-03.3-07-02 | Information Disclosure | The later-batches disclosure | low | accept | Filters the same `batches` array already passed to this component today; no new data reaches the row, only a visibility toggle over facts the maker already owns. |
</threat_model>

<verification>
- `npm --prefix app test` passes in full.
- `npm --prefix app run build` succeeds.
- The grep-based structural checks above confirm: `targetValueFor` exported and tested; the duplicate "no batch yet" line is gone; "Draw temperature" survives unrenamed; Correct/Add tasting carry `.text-control`; no unmarked-axis cell remains.
- Live confirmation via the deferred human-verify checkpoint (Task 3) closes the batch-row half of G-03.3-4 for real; until Mark runs it, the gap stays open per this project's standing away-checkpoint convention.
</verification>

<success_criteria>
G-03.3-3 is closed (exactly one "no batch yet" line) and the batch-row half of G-03.3-4 is closed (head line, measured cells, tasting section, and foot controls match sketch 003 variant B), both confirmed at the source level by the checks above and, once Mark is back, by the live checkpoint.
</success_criteria>

<output>
Create `.planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-07-SUMMARY.md` when done
</output>