# Phase 03.3 Plan Outline

Coarse grouping by files touched (per PATTERNS.md's file classification and RESEARCH.md's Move Map). `app.css` is touched by 03.3-01, 03.3-02, and 03.3-03 for different rule sets, which forces a fully serial wave chain (one plan per wave) — every visual value in that CSS reads through a token in `tokens.css`, the constraint RESEARCH.md flags as most at risk this phase. 03.3-01 leads as the tracer (`TRACER_MODE=true`): the thinnest end-to-end proof of the new page's *reading* shape — two front-matter rows plus the margin's retirement — before any plan touches the table or the pen.

| Plan ID | Objective | Wave | Depends On | Requirements |
|---|---|---|---|---|
| 03.3-01 | **Tracer.** Split `Versions.jsx` into a version-row module and a batch-row module that absorbs `BatchMargin.jsx`'s content (delete `BatchMargin.jsx`); rework `.recipe-band` from one side-by-side row into two stacked full-width rows with no running head on either; retire the margin region — column two reorders to Balance → Things to check → Notes (carried-forward only; `Authored.jsx` drops `beforeYouStart`) and Method.jsx gains a "Before you start" head block; rule the three now-relocated blank prose fields (draw notes, ingredient notes, next-time) with a hairline baseline and no label; relocate the four Save-returns-focus `useRef`/`useEffect` pairs to whichever new module owns each opener button, and fix the version-line blocked-save effect to key on an attempt counter instead of a value-equal boolean; land a deferred `checkpoint:human-verify` for the fork's landing focus so execution continues past it. All new CSS reads through existing tokens (`--gap-l/m/s/xs`, `--rule-baseline`, `--measure-prose`) — no literals. Proven end-to-end as the page's new front-matter reading shape. | 1 | — | None new — Scope bullets 1, 2 (incl. the "Before you start" move), 6 (Gap 3/D-27 focus-return relocation + WR-01's `Versions.jsx` half); 03.1 Gap 2 override; LD-03 (fork's-landing-focus checkpoint) |
| 03.3-02 | Rebuild `IngredientTable.jsx`'s body across all three render branches (reading, pen, show-changes) to group by step through `activeRows`/`activeSteps` (never a hand-rolled `removed` filter), with a step-lead-in group head and the Step `<th>`/`<td>` removed from every branch together (not just the reading state); relocate the split ingredient's per-portion step-choice `<select>` into its step group without changing its current editability (LD-02 — a fact editable today stays editable, just relocated, not enhanced); add a split-ingredient sub-line formatter (e.g. `formatPortionLine`) to `domain/composition.js`, composing the existing `formatGrams`/`formatShareOfBatch`, tested in `composition.test.js`; drop the now-dead `--col-step`/`--col-step-pen` tokens and their consuming CSS; fix `IngredientTable.jsx`'s blocked-save-focus effect to key on an attempt counter (WR-01). | 2 | 03.3-01 | None new — LD-01 (step order replaces written order, no toggle), LD-02, Scope bullet 3 (table step-grouping); Scope bullet 6 (WR-01's `IngredientTable.jsx` half) |
| 03.3-03 | Rewrite `Method.jsx`'s `StepPenBody` to open read-only by default per step — reusing the show-changes branch's read-only JSX (`Method.jsx:551-618`) as the closed-state template — with one `edit this step` text control per step (built on the existing `useOnDemandField` open/collapse primitive, applied at the whole-step level) revealing the current editable fields; restyle the per-step controls this scope names (`remove`/`restore`, `change`/`done`, `add purpose`/`add aside`) as text controls via a new `.text-control` CSS class that opts out of the global `button, select` binder rule (03.1 Gap 1 override — these become underlined text, not bordered buttons); land a deferred `checkpoint:human-verify` for the uses-checklist's scoped Escape (`stopPropagation`) so execution continues past it. | 3 | 03.3-01, 03.3-02 | None new — 03.1 Gap 1 override, Scope bullet 4; LD-03 (uses-checklist-Escape checkpoint) |
| 03.3-04 | Apply the thirteen settled labels (`product-requirements/05-domain-and-language.md`, quoted verbatim in RESEARCH.md) across the version-row/batch-row modules, `DerivedAdvisories.jsx` ("Advisories" → "Things to check"), and `IngredientTable.jsx`'s "Data" → "Source" column head; change `domain/figures.js`'s figure `label` field from a plain string to a structured plain-word/technical-term pair (PAC/POD/MSNF/fat/sugar/solids) and render it as two sibling `<span>`s in `GraduatedRule.jsx` (never an HTML string), flattening to one string for the accessible name so it stays a single computed value. | 4 | 03.3-01, 03.3-02, 03.3-03 | None new — Scope bullet 7 (the thirteen labels) |

## Deliberately unplanned (named, not silently dropped)

- **The fifth loose grams reader** (batch pen's unblocked as-made field) — ROADMAP's own phase notes mark this "open, not necessarily closed here"; it belongs to `route-recipe-batch.md`, a different brief, and a prior quick-task attempt to fix it inline was explicitly withdrawn (`6af7a6e`).
- **The Firefox calendar icon** (no CSS hook into its shadow-root picker) — accepted, documented limitation from 03.1's own research; no plan re-attempts a JS date-picker workaround.
- **The parent's version line printing twice** (recipe block + ceremony's "was …" line) — both occurrences are required by two separate binding design sources; carried forward as-is unless a plan's own restructuring incidentally changes it as a side effect.
- **The sub-1280px responsive stack** — explicitly Phase 4's (UX1-01) per ROADMAP phase notes; no plan adds a breakpoint for the body's two-column collapse.
- **A written-order/step-order toggle** — LD-01 settles this: step order replaces written order outright, no toggle, no second render path.

## OUTLINE COMPLETE
