---
status: diagnosed
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
source: [03.3-VERIFICATION.md]
started: 2026-09-10T18:27:34Z
updated: 2026-09-10T19:00:46Z
---

## Current Test

none — all tests answered

## Tests

### 1. The fork's landing focus (D-27, deferred by 03.3-01 Task 3)
steps: |
  Open the dev server's URL and navigate to the seeded olive oil recipe's current version. In the version row, open "Next version" and fill in a version line (e.g. "48 g oil · 800 g") and a reason. Click Save (or "Save as", if a batch already exists on this version) to fork a new child version — the page navigates to the child version's own URL. On the child version's page, without clicking anything, check where keyboard focus landed. Press Tab once from that landing point.
expected: Focus lands on the child version's own "Next version" button on mount, and a single Tab press moves focus forward in a sensible order (not lost to <body>).
result: issue
reported: "focus landed on the forked version name, not on the Next version button" (Mark, 2026-09-10)

### 2. The uses checklist's open/close and its scoped Escape (deferred by 03.3-03 Task 3)
steps: |
  Start the dev server and open the seeded olive oil recipe's current version. Open "Next version" on the version row to enter the pen. Click `edit this step` on any step that uses at least one ingredient row, then click the uses line's `change` control to open the checklist of ingredient rows. Press Escape once, then press Escape a second time (checklist already closed).
expected: The first Escape closes ONLY the checklist — the pen stays open and focus returns to the `change` control. The second Escape closes the whole pen (if the draft is dirty, it does nothing per D-24's unsaved-ink rule).
result: issue
reported: "with the uses checklist open, the first Escape closes the pen" (Mark, 2026-09-10)

### 3. Batch row on a version with no batches (reported outside the scripted tests)
steps: |
  Open a version that has no batch recorded and read the batch row.
expected: One "no batch yet" line; the row otherwise reads Record batch, BATCH, and that single line.
result: issue
reported: "you see 4 elements: Record batch button, BATCH, 'no batch yet', 'no batch yet'. seems like the last element is extraneous" (Mark, 2026-09-10)

### 4. Front-matter rows against sketch 003 variant B (reported outside the scripted tests)
steps: |
  Compare the implemented version row and batch row (olive oil recipe, version with one batch and one tasting) against `.planning/sketches/003-front-matter-rows/index.html`.
expected: The rows read as the sketch draws them.
result: issue
reported: "pretty big difference" — two screenshots supplied (Mark, 2026-09-10); differences itemised in gap G-03.3-4

## Summary

total: 4
passed: 0
issues: 4
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-03.3-1
  truth: "After forking a new version, keyboard focus lands on the child version's own Next version button (D-27)."
  status: failed
  reason: "Mark observed focus on the forked version name instead. The Next version button carries autoFocus={focusDevelopOnMount} from router state (RecipePage.jsx:250, 1086; VersionRow.jsx:164) and the wiring is intact, so either the button mounts before the child version's data lands or a later effect moves focus. Needs a live-browser debug session; jsdom cannot reproduce it."
  severity: major
  test: 1

- gap_id: G-03.3-2
  truth: "With the uses checklist open in the pen, the first Escape closes only the checklist; the second reaches the pen."
  status: failed
  reason: "Confirmed from code: handleUsesKeyDown (Method.jsx:123-131) is attached only to the checklist <fieldset> (Method.jsx:290). Clicking the `change` control leaves focus on that button, which sits outside the fieldset, so Escape pressed there bypasses the scoped handler and RecipePage's document-level keydown listener (RecipePage.jsx:434-451) cancels the whole pen. Scoping only works when focus is on a checkbox. Fix direction: move focus into the checklist when it opens, or attach the Escape handler to an element that wraps both the control and the fieldset."
  severity: major
  test: 2

- gap_id: G-03.3-3
  truth: "A version with no batch recorded shows one 'no batch yet' line in the batch row."
  status: failed
  reason: "BatchRow.jsx renders the phrase twice: the empty-record paragraph at line 392 (`batches.length > 0 ? 'No batch of this version has that address.' : 'no batch yet'`) and the batch list's own single entry at line 401 (D-09: the list is always a list, even with none). One of the two is redundant; D-09 suggests the list entry is the one to keep and the paragraph should render nothing when there are no batches."
  severity: minor
  test: 3

- gap_id: G-03.3-4
  truth: "The version row and batch row read as sketch 003 variant B draws them (the design source Mark confirmed 2026-09-09)."
  status: failed
  reason: |
    Mark's screenshots (2026-09-10, sketch vs implemented) show the implemented rows diverging from the sketch on layout and content, not just polish:
    - Version row: the sketch carries a right-hand stack — VERSION label, WRITTEN date, WHY reason, LATER '3 later versions' link — with Next version and Record another side by side beneath it. Implemented shows only a Next version button and a boxed '50 g oil · 800 g churned' chip; no written date, no reason, no later-versions link, and Record another has moved into the batch row.
    - Batch row head: the sketch reads 'BATCH  churned 2 Aug 2026  1 later batch' as one head line. Implemented puts three binder buttons (Record another, Correct, Add tasting) above the BATCH label, and prints 'churned 2 Aug 2026' as a boxed line at the very bottom of the row.
    - Measured figures: the sketch labels are the plain word (TIME TO TEMPERATURE, OUT OF THE MACHINE, AIR, OIL BOTTLE OPEN) with the unit in small print beside the figure ('20 min', '−6 °C') and a sub-line under each ('plan 10–12', 'draw temperature', 'overrun'). Implemented folds the unit into the caps label ('TIME TO TEMPERATURE, MIN', 'AIR, OVERRUN %'), drops the plan/sub-lines, prints 'unknown' where the sketch prints 'not measured', and renders the oil bottle date and 'recorded 4 Aug 2026 against …' as loose prose sentences instead of figures.
    - Tasting: the sketch shows a 'TASTING · at −12 °C · date unknown' line then only the marked axes (OIL CHARACTER 4.5 of 5, BITTERNESS 5, SWEETNESS 4, MELT TEST 3 g at 20 min). Implemented draws a rule, a 'DATE UNKNOWN' head, and all eight axes including unmarked ones ('unmarked'), each label carrying its full anchor text in caps.
    - Controls: the sketch's Correct and Add tasting are underlined text controls at the foot of the row; implemented uses binder buttons at the top.
    The sketch is the confirmed design (Mark, 2026-09-09) and is the acceptance target for the fix: the gap plan must read `.planning/sketches/003-front-matter-rows/index.html` directly and reproduce its rows, not re-derive scope from ROADMAP bullets. Where the sketch shows data the row does not yet receive (the later-versions link, the plan sub-line), the plan threads that data through rather than dropping the element.
  severity: major
  test: 4
