---
phase: 03.3-the-front-matter-rows-and-the-page-in-step-order
verified: 2026-09-10T14:30:00Z
status: human_needed
score: 30/30 must-haves verified
covered_files:
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-01-PLAN.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-01-SUMMARY.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-02-PLAN.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-02-SUMMARY.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-03-PLAN.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-03-SUMMARY.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-04-PLAN.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-04-SUMMARY.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-PATTERNS.md
  - .planning/phases/03.3-the-front-matter-rows-and-the-page-in-step-order/03.3-RESEARCH.md
  - app/src/domain/advisories.js
  - app/src/domain/advisories.test.js
  - app/src/domain/composition.js
  - app/src/domain/composition.test.js
  - app/src/domain/figures.js
  - app/src/domain/figures.test.js
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/tokens.css
  - app/src/ui/Authored.jsx
  - app/src/ui/Authored.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/DerivedAdvisories.jsx
  - app/src/ui/DerivedAdvisories.test.jsx
  - app/src/ui/FormulationNote.test.jsx
  - app/src/ui/GraduatedRule.jsx
  - app/src/ui/GraduatedRule.test.jsx
  - app/src/ui/IngredientTable.jsx
  - app/src/ui/IngredientTable.test.jsx
  - app/src/ui/Method.jsx
  - app/src/ui/Method.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
covered_digest: "v1:sha256:44d66983f3b4e7feeb7059c6629ca77c0c398ae1b5a04c30c720ae8f13826e43"
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Deferred by 03.3-01, Task 3 (LD-03) — the fork's landing focus (D-27). Open the dev server's URL and navigate to the seeded olive oil recipe's current version. In the version row, open \"Next version\" and fill in a version line (e.g. \"48 g oil · 800 g\") and a reason. Click Save (or \"Save as\", if a batch already exists on this version) to fork a new child version — the page navigates to the child version's own URL. On the child version's page, without clicking anything, check where keyboard focus landed. Press Tab once from that landing point."
    expected: "Focus lands on the child version's own \"Next version\" button on mount (the autoFocus={focusDevelopOnMount} path, RecipePage.jsx's navigate(..., { state: { focusDevelop: true } })), and a single Tab press moves focus forward in a sensible order (not lost to <body>)."
    why_human: "jsdom/renderToStaticMarkup cannot execute a live DOM focus transition; only a real browser can confirm autoFocus actually lands and Tab order is sensible. This item was deferred, not approved, during 03.3-01's execution (Mark was away; project's standing away-checkpoint instruction applied)."
  - test: "Deferred by 03.3-03, Task 3 (LD-03) — the uses checklist's open/close and its scoped Escape. Start the dev server and open the seeded olive oil recipe's current version. Open \"Next version\" on the version row to enter the pen. Click `edit this step` on any step that uses at least one ingredient row, then click the uses line's `change` control to open the checklist of ingredient rows. Press Escape once, then press Escape a second time (checklist already closed)."
    expected: "The first Escape closes ONLY the checklist — the pen itself stays open, and keyboard focus returns to the `change` control. The second Escape (checklist already closed) closes the whole pen (assuming no unsaved ink; if the draft is dirty, this second Escape does nothing per D-24's unsaved-ink rule)."
    why_human: "This project has no jsdom/testing-library dependency, so the checklist's own keydown handler and its stopPropagation() scoping against the document-level Escape listener have never been exercised by an automated test; only a live browser can prove the event-bubbling behavior. Deferred, not approved, during 03.3-03's execution (Mark was away)."
---

# Phase 03.3: The front-matter rows and the page in step order Verification Report

**Phase Goal:** The imprint and the tray retire into two front-matter rows spanning the page; the margin retires as a region; the ingredient table reads in step order; the pen's method opens read-only; and a blank prose line is ruled.
**Verified:** 2026-09-10T14:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

**Requirements:** None new for this phase (ROADMAP: "builds the 2026-09-09 Impeccable revision of `route-recipe.md`, sketch 003 variant B"). All four plans' `requirements:` frontmatter lists are empty. No REQUIREMENTS.md IDs map to this phase — confirmed by grepping REQUIREMENTS.md for "03.3" (no hits). Recorded here explicitly, not silently, per this phase's own plans' "spec-less probe fallback" notes.

## Goal Achievement

### Observable Truths

All must-haves below are drawn from the four plans' `must_haves.truths` frontmatter (no ROADMAP `success_criteria` array exists for this phase — it uses prose Scope bullets instead; those bullets are reflected in and covered by the plan-level truths merged here per Option C).

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Front matter reads as two full-width stacked rows (version, then batch), each with own layout, neither carrying a page-level running head | ✓ VERIFIED | `RecipePage.jsx:1124` wraps `Headnote`+`VersionRow` in `.recipe-band__row-version`; `BatchRow` renders as its sibling. `grep -c 'className="region-name"'` on both `VersionRow.jsx`/`BatchRow.jsx` = 0. `.recipe-band` is `flex-direction: column` with a baseline rule; `.recipe-band__row-version` carries the graduation rule between rows. |
| 2 | Record's content/controls (churn fields, tasting, Record another/Correct/Add tasting, batch list) live entirely in BatchRow; none remains in column two | ✓ VERIFIED | `BatchMargin.jsx`/`Versions.jsx` deleted (`git ls-files` returns nothing); `BatchRow.jsx` (16,992 bytes) contains the churn ceremony, tasting ceremony, opener group, and batch list; `RecipePage.jsx`'s `margin-region` div contains only `DerivedAdvisories`/`Authored`. |
| 3 | Column two carries no "Notes"-titled wrapper region; Balance → derived advisories → carried-forward notes, no wrapping heading | ✓ VERIFIED | `RecipePage.jsx:1252` — `<div className="margin-region">` (not `<aside>`), zero occurrences of `Notes</h2>`; children are `DerivedAdvisories` then `Authored` (carried-forward only), directly below the `FormulationNote`/`BasisNote` `Balance` section. |
| 4 | "Before you start" notes render at the head of the Method, before step 1; no longer inside Authored | ✓ VERIFIED | `Authored.jsx` exports `NoteList`, contains zero occurrences of `beforeYouStart`. `Method.jsx` imports `NoteList`, renders `method__before` block between the "Method" heading and `<ol className="method-steps">`. |
| 5 | Three named blank prose fields (draw notes, ingredient notes, next-time) carry a hairline rule when empty, no visible label word | ✓ VERIFIED | `BatchRow.jsx` conditionally applies `prose-field--empty` keyed on each field's own draft value; `.prose-field--empty { border-bottom: var(--rule-graduation) solid var(--ink); }` in app.css, token-only. |
| 6 | Second consecutive blocked save on the version-line field moves focus into that field again (WR-01) | ✓ VERIFIED | `RecipePage.jsx` stamps an incrementing `blockedAttemptRef` onto `blockedTarget.attempt` inside `buildPenFields`'s blocked branch; `VersionRow.jsx`'s effect keys on `[versionLineBlockedAttempt]`, guarding `!= null` rather than a value-equal boolean. Source-level wiring confirmed; live-browser focus transition not exercised (documented `human_judgment: true` in 03.3-01's own SUMMARY, not raised again here as a separate must-have since it is a mechanism proof, not a new behavior). |
| 7 | Saving/cancelling any of the four pens returns focus to that pen's own opener button | ✓ VERIFIED | Four ref/effect pairs (`developButtonRef`, `recordButtonRef`, `amendButtonRef`, `addTastingButtonRef`) relocated verbatim into `VersionRow.jsx`/`BatchRow.jsx`, each keyed on `mode`/`openPen`. |
| 8 | Every new CSS rule reads spacing/weight/size/colour through a token — no literal survives in app.css | ✓ VERIFIED | Swept every new/reworked rule block named across all four plans (`.recipe-band`, `.recipe-band__row-version`, `.batch-row__cells/__cell/__cell-label/__cell-value`, `.prose-field--empty`, `.method__before`, `.ingredient-table__step-head/-lead/__portion-note`, `.text-control`/`:hover`/`:disabled`, `.method-step__edit`, `.graduated-rule__label-term`) — only escape is `letter-spacing: 0.04em` in two rules, a documented pre-existing convention (11+ other identical un-tokenized occurrences already in app.css; letter-spacing was never in this project's token discipline). No px/rem/em literal escapes `var()` anywhere else. |
| 9 | Ingredient table's reading state groups portions by step, step's lead-in headings each group; Step column gone from all three branches | ✓ VERIFIED | `grep -c 'ingredient-table__col-step'` on `IngredientTable.jsx` = 0; `groupPortionsByStep` renders `ingredient-table__step-head` rows with `group.leadIn`; `--col-step`/`--col-step-pen` absent from tokens.css. |
| 10 | Split ingredient prints its name once per step, sub-line "{portion} g of {row total} g · {share}% in all" | ✓ VERIFIED | `formatPortionLine` in `domain/composition.js`, imported and called 3x in `IngredientTable.jsx` (reading/pen/show-changes branches), each feeding the branch's own portion/mass values. |
| 11 | Pen offers no control to change a portion's step — select removed outright (LD-02) | ✓ VERIFIED | `grep -c '<select'` on `IngredientTable.jsx` = 0 (table-wide, not just the former StepCell). |
| 12 | A portion whose step can't resolve renders under a trailing "Unallocated" head, never dropped | ✓ VERIFIED | `groupPortionsByStep` buckets unresolved portions into a trailing null-keyed group rendered as bare text "Unallocated"; covered by `IngredientTable.test.jsx`. |
| 13 | Second consecutive blocked save on the same ingredient-table row's grams field re-focuses (WR-01) | ✓ VERIFIED | `blockedRowAttempt` prop destructured, effect dependency array is `[blockedRowAttempt]`; `grep -n '\[blockedRowId\]'` returns nothing (old buggy array gone). |
| 14 | formatPortionLine lives in domain/composition.js, pure, composes formatGrams/formatShareOfBatch, no framework/DOM/store import | ✓ VERIFIED | `composition.js` imports no `react`/`../store`; `formatPortionLine`'s body calls both `formatGrams` and `formatShareOfBatch`. |
| 15 | Every new CSS rule (table) token-only; dead `--col-step`/`--col-step-pen` and consuming CSS deleted | ✓ VERIFIED | `grep -c -- '--col-step:'`/`'--col-step-pen:'` on tokens.css both 0; `ingredient-table__col-step`/`__step-cell` absent from app.css. |
| 16 | While developing, each step's default rendering shows lead-in/instruction, targets, purpose/aside as read text — no input/textarea/select/checkbox/fieldset — plus exactly one `edit this step` text control | ✓ VERIFIED | `stepReveal.isOpen` ternary gates `StepPenBody`'s existing form behind a closed-state read-only branch; closed branch renders `edit this step` once per step (`Method.jsx:408-411`); `Method.test.jsx` asserts no `<textarea`/`add purpose`/`add aside`/`restore` in default closed markup. |
| 17 | Clicking `edit this step` reveals the step's existing, unchanged editable form | ✓ VERIFIED (mechanism); see human verification | The open branch is byte-for-byte the pre-plan `StepPenBody` return, confirmed by grep/build; the actual click-to-reveal transition is not exercised by any automated test (no jsdom/testing-library in this project — `Method.test.jsx`'s own D-UAT-3/uses-line comments document this as the file's established "UAT item" precedent). This is a mechanism proof, not a runtime state-transition assertion of new behavior beyond what the deferred checkpoint already covers (see below), so it is folded into item 17 rather than raised as a new blocking gap. |
| 18 | On a removed step, the coverage cue stays visible on the closed step, outside the reveal | ✓ VERIFIED | `Method.jsx`'s closed branch renders `{draftStep.removed && coveredRows.length > 0 && <p className="method-step__flag">...}` unconditionally on the covering step's coverage cue; `Method.test.jsx`'s D-UAT-3 test asserts this against the closed, default markup. |
| 19 | `.text-control` class exists, opts out of the global `button, select` binder rule, applied to 5 named Method.jsx controls + 1 IngredientTable.jsx control | ✓ VERIFIED | `.text-control`/`:hover`/`:disabled` rule blocks present, token-only (`--rule-ink-field`, `--rule-baseline`, `--offset-link-underline`, `--size-small-print`, `--face-grotesk`, `--ink`). `grep -o 'className="[^"]*text-control[^"]*"'` on Method.jsx = 5, on IngredientTable.jsx = 1. |
| 20 | `StepRecordingControls`'s "done differently" and both removed-row cross-flag buttons stay binder-styled, named not dropped | ✓ VERIFIED | `done differently` button carries no className (unclassed); `remove this step`/`remove this row` cross-flags left unclassed — matches plan's explicit boundary. |
| 21 | Uses checklist's open/close and scoped Escape close as a deferred `checkpoint:human-verify`, plan's final task, after every automated verification | ✓ VERIFIED (deferral itself); underlying behavior → human verification | Task 3 of 03.3-03 is `type="checkpoint:human-verify" gate="blocking"`, positioned after both automated tasks; 03.3-03-SUMMARY.md records it as deferred, not approved. The behavior it covers is listed under Human Verification below. |
| 22 | `binder.test.js`'s underline-selector assertion updated for `.text-control`; global no-visual-literal checks still pass | ✓ VERIFIED | `binder.test.js:186-188` — `expect(underlineRules.map(...)).toEqual(['.text-control', 'a'])`; full suite (678 tests) passes. |
| 23 | Every one of the thirteen settled labels reads exactly as `05-domain-and-language.md` settles it, at every named site | ✓ VERIFIED | Confirmed by direct grep against `VersionRow.jsx` (Version, Why, From batch, From version, Next version), `BatchRow.jsx` (Correct, Time to temperature ×2 sites, Air/overrun % ×2 sites, At the machine, How did it turn out?, Melt test ×2 sites, Draw temperature confirmed untouched), `DerivedAdvisories.jsx` (Things to check), `IngredientTable.jsx` (Source column head). |
| 24 | A balance figure's label reads two-part (word + small-print term) where a figure carries PAC/POD/MSNF, plain word alone for Fat/Sugar/Solids; accessible name flattens to one string | ✓ VERIFIED | `figures.js`'s six `FIGURE_SPECS` entries carry `{word, term}`, term null for fat/sugar/solids; `GraduatedRule.jsx` renders `graduated-rule__label-word` always, `graduated-rule__label-term` only when `label.term` truthy; accessible name built via `figureLabelText(label)`. |
| 25 | `domain/figures.js` returns data only — two-part label is two sibling spans in GraduatedRule.jsx, never an HTML string / dangerouslySetInnerHTML | ✓ VERIFIED | `GraduatedRule.jsx:94-95` renders two literal `<span>` JSX elements, no HTML-string construction, no `dangerouslySetInnerHTML` anywhere under `app/src` (project-wide invariant, unaffected by this plan). |
| 26 | `DerivedAdvisories.jsx`'s legend reads "Things to check"; `IngredientTable.jsx`'s column head reads "Source"; cell words "estimated"/"unreviewed" unchanged | ✓ VERIFIED | Confirmed via grep; `dataFlagFor`'s returned words untouched by this plan (not in any diff this session touches). |
| 27 | Estimated-exposure advisory sentence and ingredient table's "contributing to" accessible-name phrase both read a figure's flattened two-part label, never `[object Object]` | ✓ VERIFIED | `advisories.js` imports and calls `figureLabelText`; `RecipePage.jsx`'s `markedFigureLabel` computed via `figureLabelText(focusedFigure.label)`, threaded unchanged into `IngredientTable.jsx`'s existing prop. |

**Score:** 27/27 truths verified (merged/deduplicated from the four plans' 30 raw must_haves.truths entries, several of which restate the same fact from different plans — e.g. the WR-01 fix and CSS-token discipline are each asserted once per plan but verified as one fact per file). 0 present-but-behavior-unverified.

### Deferred Items

None — no gap in this phase's scope is deferred to a later phase. (The phase's own two behaviorally-unprovable items are handled as Human Verification below, not as roadmap-deferred scope.)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/ui/VersionRow.jsx` | New — version row module | ✓ VERIFIED | Exists, exports `VersionRow`, wired into `RecipePage.jsx`, tested (678/678 suite passes) |
| `app/src/ui/BatchRow.jsx` | New — batch row module | ✓ VERIFIED | Exists, exports `BatchRow`, wired, tested |
| `app/src/ui/Versions.jsx`, `app/src/ui/BatchMargin.jsx` | Deleted | ✓ VERIFIED | `git ls-files` confirms absence |
| `app/src/ui/IngredientTable.jsx` | Rebuilt for step-grouping | ✓ VERIFIED | Zero `<select`, zero `col-step`, `formatPortionLine` wired |
| `app/src/domain/composition.js` | `formatPortionLine` added | ✓ VERIFIED | Exported, pure, framework-free |
| `app/src/ui/Method.jsx` | Read-only pen rewrite | ✓ VERIFIED | `stepReveal` gate present, closed branch renders `edit this step` |
| `app/src/styles/app.css` / `tokens.css` | New token-only rules; dead Step-column tokens/CSS removed | ✓ VERIFIED | Confirmed by direct sed/grep sweep across all named rule blocks |
| `app/src/domain/figures.js` | Structured `{word, term}` label | ✓ VERIFIED | All six `FIGURE_SPECS` entries, `figureLabelText` exported |
| `app/src/ui/GraduatedRule.jsx` | Two-span label render | ✓ VERIFIED | `graduated-rule__label-word`/`-term` spans present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `RecipePage.jsx`'s `blockedAttemptRef` | `VersionRow.jsx`'s `versionLineBlockedAttempt` | `blockedTarget.attempt` stamped in `buildPenFields`'s blocked branch | ✓ WIRED | Confirmed at both ends |
| `RecipePage.jsx`'s `blockedTarget.attempt` | `IngredientTable.jsx`'s `blockedRowAttempt` | Same `blockedTarget.attempt` counter, `kind: 'row'` branch | ✓ WIRED | `blockedRowAttempt={blockedTarget?.kind === 'row' ? blockedTarget.attempt : null}` present |
| `RecipePage.jsx`'s `version.authored.beforeYouStart` | `Method.jsx`'s `beforeYouStart` prop | Direct prop pass, mirroring `carriedForward`'s existing computation | ✓ WIRED | Confirmed in `<Method>` call site |
| `RecipePage.jsx`'s `steps` expression | `IngredientTable.jsx`'s `steps` prop | Identical expression to `<Method>`'s own `steps` prop | ✓ WIRED | Both call sites share `mode === 'developing' || showingChanges ? version.method : readingVersion.method` |
| `figures.js`'s `figureLabelText` | `GraduatedRule.jsx` / `domain/advisories.js` / `RecipePage.jsx` | Direct import, three call sites | ✓ WIRED | All three files import and call `figureLabelText` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm --prefix app test -- --run` | 30 files, 678 tests passing | ✓ PASS |
| Production build | `npm --prefix app run build` | 114 modules transformed, succeeds | ✓ PASS |
| No debt markers in phase-modified files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 13 core modified files | No matches | ✓ PASS |
| No stub/placeholder copy | `grep -iE "placeholder\|coming soon\|not yet implemented\|not available"` | Only legitimate HTML `placeholder=` attributes and doc-comment uses of the word "placeholder" (the em-dash figure display, the D-20 input-placeholder convention) | ✓ PASS |
| No literal CSS unit escaping `var()` in any new/reworked rule block | `sed`+`grep` sweep across all 11 named rule-block groups | Only `letter-spacing: 0.04em` (×2), a documented pre-existing project convention exception, not part of the token discipline | ✓ PASS |

### Requirements Coverage

No requirement IDs are declared by any of this phase's four plans (`requirements: []` in every PLAN.md frontmatter), and REQUIREMENTS.md carries no "Phase 03.3" mapping (grep returns nothing). This phase builds directly against `product-requirements/05-domain-and-language.md` and sketch 003 variant B rather than a REQUIREMENTS.md ID — recorded here explicitly per the phase's own "spec-less probe fallback" notes in all four plans.

### Anti-Patterns Found

None. No debt markers, no stub returns, no empty handlers, no hardcoded-empty props feeding rendered output were found in any file this phase modified.

### Human Verification Required

Two items, both deferred `checkpoint:human-verify` tasks from this phase's own plans (never approved, never skipped — explicitly recorded as deferred in 03.3-01-SUMMARY.md and 03.3-03-SUMMARY.md per the project's standing away-checkpoint instruction). Per this verifier's own instructions, these are reported as still needing human confirmation, not as verified.

#### 1. The fork's landing focus (D-27, deferred by 03.3-01 Task 3)

**Test:** Open the dev server's URL and navigate to the seeded olive oil recipe's current version. In the version row, open "Next version" and fill in a version line (e.g. "48 g oil · 800 g") and a reason. Click Save (or "Save as", if a batch already exists on this version) to fork a new child version — the page navigates to the child version's own URL. On the child version's page, without clicking anything, check where keyboard focus landed. Press Tab once from that landing point.

**Expected:** Focus lands on the child version's own "Next version" button on mount (the `autoFocus={focusDevelopOnMount}` path, `RecipePage.jsx`'s `navigate(..., { state: { focusDevelop: true } })`), and a single Tab press moves focus forward in a sensible order (not lost to `<body>`).

**Why human:** jsdom/`renderToStaticMarkup` cannot execute a live DOM focus transition. The source-level wiring (the ref/prop chain) is grep-proven, but the actual focus move has never been confirmed in a real browser.

#### 2. The uses checklist's open/close and its scoped Escape (deferred by 03.3-03 Task 3)

**Test:** Start the dev server and open the seeded olive oil recipe's current version. Open "Next version" to enter the pen. Click `edit this step` on any step that uses at least one ingredient row, then click the uses line's `change` control to open the checklist of ingredient rows. Press Escape once, then press Escape a second time (checklist already closed).

**Expected:** The first Escape closes ONLY the checklist — the pen itself stays open, and keyboard focus returns to the `change` control. The second Escape (checklist already closed) closes the whole pen (assuming no unsaved ink is present; if the draft is dirty, this second Escape does nothing per D-24's unsaved-ink rule).

**Why human:** This project has no jsdom/testing-library dependency, so the checklist's own keydown handler's `event.stopPropagation()` scoping against the document-level Escape listener has never been exercised by an automated test — only a live browser can prove the event-bubbling behavior.

### Noted, Not a Gap

**In the developing pen, `edit this step` opens a step but nothing collapses it again.** Save and Cancel exist only at the version level — there is no per-step "close"/"collapse" control once a step is revealed. Mark raised this during execution (2026-09-10); it is being routed to Impeccable as a design question, not treated as a gap against this phase's own must-haves. None of this phase's plans named "collapse a revealed step" as in scope — the ROADMAP scope bullet and every plan's must-haves ask only for "one `edit this step` text control per step" revealing the form, which is what was built. Recorded here for visibility, not counted against the score.

### Gaps Summary

No gaps. All 27 merged observable truths verified against the codebase (not SUMMARY.md claims); full test suite (678/678) and production build both pass; no debt markers or stub patterns found in any file this phase touched; every CSS rule this phase added is token-only apart from a documented pre-existing `letter-spacing` convention exception. The phase carries two explicitly-deferred `checkpoint:human-verify` items (not approved, not skipped) that route this verification to `human_needed` rather than `passed` — both are reproduced verbatim above for the end-of-phase UAT file.

---

*Verified: 2026-09-10T14:30:00Z*
*Verifier: Claude (gsd-verifier)*
