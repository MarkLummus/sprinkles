---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/data/olive-oil.js
  - app/src/data/batch-2026-08-02.js
  - app/src/domain/axes.js
  - app/src/domain/axes.test.js
  - app/src/domain/battery.js
  - app/src/domain/battery.test.js
  - app/src/domain/batch.js
  - app/src/domain/batch.test.js
  - app/src/store/db.js
  - app/src/store/transfer.js
  - app/src/store/transfer.test.js
  - app/src/store/seed.test.js
autonomous: false
requirements: [BATCH2-01, BATCH1-01, BATCH1-02, OBS1-01]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A batch saved through the one-save assembly carries the full battery: the six churn-phase fields, a zero-or-one tasting object carrying 1–5 marks, defects, the declared toggle and the melt fields, and a changed date stamped on every save after the first (D-02, D-03, D-04)."
    - "The seeded 2 Aug record reads the battery value-for-value per D-07 — oil 4, sweetness 4, Bitter declared, melt test 3, time to draw temp. 20, out of machine −6, churn duration 30 — and is built through the same constructors a maker's own save calls."
    - "The store resets rather than lifts: DB_VERSION bumps, both object stores drop and recreate, and a store file exported before this phase is refused with every error named (D-09)."
    - "Every unmarked axis, unpicked chip and blank text stays absent in the stored record — never zero, never a manufactured empty tasting (D-02, D-10)."
  artifacts:
    - app/src/domain/axes.js
    - app/src/domain/battery.js
    - app/src/domain/batch.js
    - app/src/data/olive-oil.js
    - app/src/data/batch-2026-08-02.js
    - app/src/store/db.js
    - app/src/store/transfer.js
  key_links:
    - "createBatch/completeRecord carry the version's declaredAxes names and declaredFlaw into batch.snapshot via structuredClone at authoring time, once, never retaken (BATCH2-01)"
    - "transfer.js's validateStoreFile refuses any file whose schemaVersion is not the new value, and exportStore writes exactly that value (D-09)"
    - "seedIfEmpty reseeds the rewritten records through repository.saveVersion/saveBatch — the seeds are indistinguishable from typed records"
---

<objective>
The record's stored shape becomes the full battery. The domain modules are rewritten to sketch 007's fixed six-axis table with five whole goldilocks stops and the core/declared split; a new battery module carries the measured-field rules and the pure numeric parser; the batch record's assembly collapses to the one-save model (D-01–D-04); the store resets to the new shape (D-09); and the seeded olive oil version and its 2 Aug batch are rewritten onto the battery (D-07), with Mark reviewing the rewritten records before any further task of the phase executes (D-08).

Purpose: every later plan builds UI against this shape; the seed is the phase's first reviewable artifact and the proof that the shape carries a real record.
Output: rewritten `app/src/domain/axes.js`, new `app/src/domain/battery.js`, rewritten `app/src/domain/batch.js`, rewritten seed data files, bumped `app/src/store/db.js`, narrowed-and-extended `app/src/store/transfer.js`, Mark's review of the rewritten records.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery.md
@.impeccable/surfaces/route-recipe-batch.md
@app/src/domain/axes.js
@app/src/domain/batch.js
@app/src/store/transfer.js
@app/src/data/olive-oil.js
@app/src/data/batch-2026-08-02.js
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (the phase's stored-shape layer; later plans add the UI layer):

- `app/src/domain/axes.js`: constants `AXES` (six rows: key, name, low, high, group), `STOPS` (1–5), `CORE_AXIS_COUNT = 4`; functions `stopWordsFor(axis)`, `axesForBatch(batch)` (rewritten to the snapshot's declared-names shape), `markKeyFor(axis)`, `setMark(marks, axisKey, stop)` (unchanged).
- `app/src/domain/battery.js` (new file): constant `BATTERY_FIELDS` (six numeric field entries: key, label, unit, signed, error), `SEGMENT_OPTIONS` (three groups), `DEFECTS` (four chip strings), `DECLARED_FLAW = 'Bitter'`; function `parseMeasuredDraft(raw, { signed })`.
- `app/src/domain/batch.js`: `BATCH_SCHEMA_VERSION = 3`; rewritten `createBatch(version, churnFields, tastingFields, { id, now })`; new `completeRecord(batch, churnFields, tasting, { now })`; retired `addTasting`, `recordAmendment`, `isTastingSaveable`, `sortedTastings`, `hasTasting`; survivors unchanged (`formatRecordDate`, `readMeasured`, `sortedBatches`, `latestChurnDate`, the as-made and step-change readers, `asMadeTotals`).
- Stored batch shape (`BATCH_SCHEMA_VERSION` 3): `churn` gains `timeToDrawTempMinutes`, `outOfMachineTempC`, `churnDurationMinutes` (finite number or null), `exitConsistency`, `airiness` (enum-or-null), `atTheMachine` (string-or-null); retires the old numeric and notes names listed under Retirements; `tasting` replaces the tastings array — a single object or null carrying `tastedDate`, `temperingMinutes`, `tastingTempC`, `marks`, `note`, `defects`, `bitterDeclared`, `meltTestG`, `meltStyle`; batch-level `changed` (date string or null) replaces the amendment list.
- Stored version shape (`VERSION_SCHEMA_VERSION = 4` in `app/src/data/olive-oil.js`): `declaredAxes` becomes an array of declared-axis names (`['Body', 'Oil']` for the seed), plus `declaredFlaw: 'Bitter'`; `batch.snapshot` carries both.
- Store: `DB_VERSION = 5` (db.js); file `schemaVersion` 5 (transfer.js, both the gate and exportStore).
</artifacts_this_phase_produces>

## Retirements (this plan removes)

`addTasting`, `recordAmendment`, `isTastingSaveable`, `sortedTastings`, `hasTasting` (domain/batch.js); `CORE_AXES`, `MARK_STOPS` (domain/axes.js); churn fields `comeUpMinutes`, `drawTempC`, `overrunPercent`, `drawNotes` (replaced per the artifacts list); tasting fields `date`, `meltdownLossG`, `words`, per-tasting `nextTimeNote`, the `tastings` array, `amendedAt`, the tasting `id`; `TASTING_ID` in the seed file; `VERSION_SCHEMA_VERSION` 3's declared-axes object shape. Pitfall 10 of 03.3.1-RESEARCH.md: none of these survive "for consistency".

<tasks>

<task type="tracer">
  <name>Task 1: The battery's stored shape and the rewritten seed records (D-07, D-09 shape)</name>
  <files>app/src/domain/axes.js, app/src/domain/axes.test.js, app/src/domain/battery.js, app/src/domain/battery.test.js, app/src/domain/batch.js, app/src/domain/batch.test.js, app/src/data/olive-oil.js, app/src/data/olive-oil.test.js, app/src/data/batch-2026-08-02.js</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "Axes spec" (the AXES table verbatim, stop words, core/declared split) and "Controls spec" (the two verbatim error strings, the °C rule)
- `app/src/domain/axes.js` — the file being rewritten; `setMark` and the snapshot-only read survive
- `app/src/domain/batch.js` — the file being rewritten; the constructor docblock contract (lines 34-42) and the blank-is-absent discipline (79-88) are load-bearing
- `app/src/data/olive-oil.js` lines 1-125 — the declaredAxes block being replaced (Pitfall 3)
- `app/src/data/batch-2026-08-02.js` — the seed being rewritten through the constructors
- `03.3.1-RESEARCH.md` — "The axes module (domain/axes.js rewritten)", "The one-save model", Code Examples 1 and 2
  </read_first>
  <action>
Build the stored shape end-to-end at the data layer, with the seed rewrite as the visible deliverable (D-08 sequences it first; the domain modules land as the machinery the transcription runs through). Cite the contract sections "Axes spec" and "Controls spec".

1. Rewrite `app/src/domain/axes.js` (framework-free, node-env): replace `CORE_AXES`/`MARK_STOPS` with the contract's AXES table verbatim — six axes Hardness (soft–hard), Scoopability (crumbly–gummy), Smoothness (grainy–smooth), Sweetness (less–more), Body (thin–heavy), Oil (faint–strong) — each carrying a fixed lowercase `key`, its `name`, the two anchor words `low`/`high`, and a `group` of `core` (first four) or `declared` (Body, Oil). The middle anchor word is the fixed word `right` for every axis. Export `STOPS = [1, 2, 3, 4, 5]`, `CORE_AXIS_COUNT = 4`, and `stopWordsFor(axis)` returning the five words per the contract: the low anchor, `leaning {low}`, `right`, `leaning {high}`, the high anchor. Rewrite `axesForBatch(batch)` to build the four core axes from AXES followed by the batch's declared axes mapped through AXES by name from `batch.snapshot.declaredAxes` (now an array of names such as `['Body', 'Oil']`) — it still reads `batch.snapshot` only, never a live version (the existing docblock's rule stands; Pitfall 11). Simplify `markKeyFor(axis)` to return the axis's fixed key (all six are fixed now). Keep `setMark` byte-for-byte — its spread-and-delete discipline is exactly right for the new marks map.
2. Create `app/src/domain/battery.js` (pure, no imports, node-env): a `BATTERY_FIELDS` table with one entry per battery numeric field — key, visible label, unit, a `signed` flag true only on the two °C fields (Out of machine; Tasting temperature), and the field's own error string, verbatim from the contract's "Controls spec": the temperature sentence for signed fields, the zero-or-positive sentence for all others. Export `SEGMENT_OPTIONS` with the three groups and their option strings verbatim from the contract's "Controls spec" (Exit consistency: Smooth ribbon / Wet, soupy / Chunky, separated; Airiness (estimated): Low, dense / Medium, standard / High, airy; Melt style (optional): Watery, weeping / Creamy puddle / Stable foam), `DEFECTS` with the four comma-worded chips verbatim, and `DECLARED_FLAW = 'Bitter'`. Export `parseMeasuredDraft(raw, { signed = false })` per RESEARCH.md Code Example 2: blank always allowed; Unicode minus normalized to ASCII; decimal comma to point; a value malformed in any way returns not-ok — never a silent null (Pitfall 4); negatives rejected unless `signed`.
3. Rewrite `app/src/domain/batch.js`: `BATCH_SCHEMA_VERSION = 3`. Extend `createBatch` to `createBatch(version, churnFields, tastingFields, { id, now })` — the churn object carries the new field names (timeToDrawTempMinutes, outOfMachineTempC, churnDurationMinutes, exitConsistency, airiness, atTheMachine, ingredientNotes, nextTimeNote) with the same explicit `!= null` / presence-of-content discipline; `tastingFields` null produces `tasting: null`, otherwise the single tasting object (tastedDate, temperingMinutes, tastingTempC, marks copied through `setMark`'s own spread-and-delete discipline — a shallow own-key copy, defects as a copy of the supplied array or null when none, bitterDeclared true-or-null, meltTestG, meltStyle); `changed` starts null. Add `completeRecord(batch, churnFields, tasting, { now })`: churn replaced wholesale, tasting set to the supplied zero-or-one object — a null argument removes a stored tasting (RESEARCH.md Assumption A3, D-02's literal reading; note it for UAT) — and `changed` stamped with `now`, a YYYY-MM-DD date string the handler computes; recordedAt, snapshot and id untouched; the snapshot is never retaken (BATCH2-01). Delete the five retired functions named in this plan's Retirements section, including their docblocks, so a negative grep is clean. Keep the impure-call rule: no `crypto.randomUUID()` or `new Date()` anywhere in the module.
4. Rewrite `app/src/data/olive-oil.js`: `VERSION_SCHEMA_VERSION` 4; replace the declared-axes object block with `declaredAxes: ['Body', 'Oil']` and `declaredFlaw: 'Bitter'` (Pitfall 3; RESEARCH.md Assumption A6); everything else untouched.
5. Rewrite `app/src/data/batch-2026-08-02.js` as the D-07 transcription built through the new `createBatch` in one call (no tasting append step; `TASTING_ID` retires): churnDate 2026-08-02; asMade and stepChanges exactly as today; timeToDrawTempMinutes 20; outOfMachineTempC -6; churnDurationMinutes 30; exitConsistency null; airiness null; atTheMachine 'Soft, not greasy'; ingredientNotes 'Oil bottle open date 24 Jul 2026'; nextTimeNote null; tasting with tastedDate null, temperingMinutes null, tastingTempC -12, marks `{ sweetness: 4, oil: 4 }`, note null, defects null, bitterDeclared true, meltTestG 3, meltStyle null. Keep the fixed BATCH_ID and recordedAt so URLs and dates stay stable.
6. Update the four test files: `axes.test.js` asserts the AXES table byte-for-byte per the contract (names, anchors, group split, the five stop words per axis, STOPS 1–5); `battery.test.js` (new) asserts parseMeasuredDraft's cases — blank, digits, decimal point, decimal comma, Unicode minus on a signed field, a rejected stray letter, a rejected negative on an unsigned field, an accepted negative on a signed field, `.5` and `45.` accepted — and the field table's two error strings verbatim; `batch.test.js` asserts the new createBatch/completeRecord contract (zero-or-one tasting, no empty tasting manufactured for null fields, changed stamped only by completeRecord, snapshot never retaken, blank-is-absent on every new field including a written 0 melt test) and drops the retired functions' describes; `olive-oil.test.js` asserts the declared names and flaw.

Mark reviews the rewritten records before the remaining tasks of the phase execute — that is Task 2, immediately after this one (D-08). The app's UI layer still reads the old shape until plan 02 lands; this task's gates are the domain/data suites only.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/domain/axes.test.js src/domain/battery.test.js src/domain/batch.test.js src/data/olive-oil.test.js</automated>
    <fails_when>non-zero exit, or the summary line shows any test failed or was skipped</fails_when>
  </verify>
  <verify>
    <automated>grep -rn "MARK_STOPS\|CORE_AXES\|addTasting\|recordAmendment\|isTastingSaveable\|sortedTastings\|hasTasting\|comeUpMinutes\|overrunPercent" app/src/domain app/src/data --include='*.js' | grep -v '\.test\.js' | wc -l | tr -d ' '</automated>
    <fails_when>output is not 0 — a retired identifier still reachable in production domain or data code</fails_when>
  </verify>
  <acceptance_criteria>
- `app/src/domain/axes.js` exports AXES whose six name/anchor rows match the contract's "Axes spec" table exactly, in order, with Hardness through Sweetness group 'core' and Body/Oil group 'declared'; `stopWordsFor(AXES[0])` returns soft, leaning soft, right, leaning hard, hard.
- `app/src/domain/battery.js` exists, imports nothing, and exports BATTERY_FIELDS, SEGMENT_OPTIONS, DEFECTS, DECLARED_FLAW and parseMeasuredDraft; the two error strings in BATTERY_FIELDS match the contract's "Controls spec" strings character-for-character, and `signed: true` appears on exactly the two °C entries.
- `app/src/domain/batch.js` exports createBatch and completeRecord; `grep -c "addTasting\|recordAmendment\|isTastingSaveable\|sortedTastings\|hasTasting" app/src/domain/batch.js` returns 0.
- `app/src/data/olive-oil.js` carries VERSION_SCHEMA_VERSION 4, declaredAxes ['Body', 'Oil'] and declaredFlaw 'Bitter'.
- The seeded batch's tasting is a single object with marks { sweetness: 4, oil: 4 }, bitterDeclared true, meltTestG 3, tastingTempC -12; its churn carries timeToDrawTempMinutes 20, outOfMachineTempC -6, churnDurationMinutes 30.
- No `crypto.randomUUID` or `new Date()` call exists under app/src/domain.
  </acceptance_criteria>
  <done>The battery's stored shape exists in the domain modules; the seeded version and batch carry D-07's transcription through the same constructors a maker's save calls; the domain/data suites pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Mark reviews the rewritten seed records before execution continues (D-08)</name>
  <files>.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-01-SUMMARY.md</files>
  <what-built>The two rewritten seed records: `app/src/data/olive-oil.js` (declared pair Body and Oil, the declared flaw Bitter, VERSION_SCHEMA_VERSION 4) and `app/src/data/batch-2026-08-02.js` (the D-07 transcription built through the new createBatch). The domain/store machinery behind them is already committed.</what-built>
  <how-to-verify>Read the two rewritten data files beside D-07's mapping and confirm each value: oil character 4, sweetness 4, Bitter declared, hardness/smoothness/scoopability/body unmarked, melt test 3, melt style blank, tasted date blank, tasting temperature −12, tempering blank, time to draw temp. 20, out of machine −6, churn duration 30, exit consistency and airiness blank, At the machine "Soft, not greasy", ingredient notes "Oil bottle opened 24 Jul", note blank, defects none, Next time blank. Correct any value with the executor before continuing.</how-to-verify>
  <resume-signal>Mark's review outcome is recorded in 03.3.1-01-SUMMARY.md (approved, corrected, or deferred per the away-protocol) before Task 3 begins.</resume-signal>
  <action>
Pause the phase for Mark's review of the two rewritten seed records — `app/src/data/olive-oil.js` (the declared pair Body and Oil, the declared flaw Bitter, VERSION_SCHEMA_VERSION 4) and `app/src/data/batch-2026-08-02.js` (the D-07 transcription: oil 4, sweetness 4, Bitter declared, hardness/smoothness/scoopability/body unmarked, melt test 3, melt style blank, tasted date blank, temperature −12, tempering blank, time to draw temp. 20, out of machine −6, churn duration 30, exit consistency and airiness blank, At the machine "Soft, not greasy", ingredient notes "Oil bottle opened 24 Jul", note blank, defects none, Next time blank). Present the two files' contents and the D-07 mapping side by side; Mark approves or corrects values. If Mark corrects a value, apply the correction and re-run Task 1's verify commands before continuing. If Mark is away, the established deferral protocol applies: defer the review to end-of-phase UAT, record the deferral in this plan's SUMMARY, and keep executing (per the project's recorded practice for human-verify gates while Mark is away). Record the review outcome in the plan SUMMARY.
  </action>
  <verify>
    <human-check>Mark has seen the rewritten olive-oil.js declared block and the batch-2026-08-02.js transcription and confirmed each value against D-07, or the review is recorded as deferred to end-of-phase UAT.</human-check>
  </verify>
  <done>The review outcome (approved, corrected, or deferred) is recorded in 03.3.1-01-SUMMARY.md before Task 3 begins.</done>
</task>

<task type="auto">
  <name>Task 3: The store gate — reset to the new shape, refuse older files (D-09)</name>
  <files>app/src/store/db.js, app/src/store/transfer.js, app/src/store/transfer.test.js, app/src/store/seed.test.js</files>
  <read_first>
- `app/src/store/db.js` — the file being bumped; the drop-first-then-guarded-create order and the no-async-keyword rule are load-bearing and must not change
- `app/src/store/transfer.js` — the file being narrowed; the collect-all-errors style (lines 190-270), the schemaVersion gate (345-347), exportStore (363-374), `scanForUnsafeKeys` (31-44)
- `03.2-CONTEXT.md` — D-05/D-08: the reset pattern this task repeats; the no-second-ladder rule
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — section "Settled defaults" (what the stored shape must carry)
  </read_first>
  <action>
Cite the contract's "Settled defaults" section and D-09/D-10. (1) `app/src/store/db.js`: change DB_VERSION from 4 to 5 and touch nothing else — the drop/recreate upgrade already implements the reset; the guarded creates and the no-async rule are load-bearing. (2) `app/src/store/transfer.js`: raise the schemaVersion gate and exportStore's literal from 4 to 5 — no lift branch, no second ladder (the 03.2 D-05 pattern repeats; the header comment's shape story updates to name 5). Extend the validators in the same collect-all-errors, name-the-path style: validateBatch's churn loop gains the three new numerics (finite-number-or-null) and the two segment picks (enum-or-null against SEGMENT_OPTIONS's strings), and drops the retired numeric names; the amendedAt array check becomes a string-or-null check on the new changed date field; the tastings-array check becomes a single-tasting object-or-null check delegating to an extended validateTasting — marks each a 1–5 integer-or-absent (never 0, never a half), defects absent-or-an-array-of-the-four-known-chip-strings, bitterDeclared true-or-null, meltStyle enum-or-null, meltTestG and temperingMinutes finite-number-or-null, tastedDate string-or-null, note string-or-null; validateVersion gains the declaredAxes check (each entry a non-empty string naming a declared-pair axis) and a declaredFlaw string-or-null check, and the snapshot's declaredAxes/declaredFlaw get the same checks inside validateBatch. `scanForUnsafeKeys` and the refuse-whole-file contract stay as-is. (3) Update `transfer.test.js`: the schemaVersion fixtures, the batch fixture's new fields, an old-version refusal case, a marks 1–5 case (0 and 4.5 refused), a defects-list case (an unknown chip string refused), and the collect-three-errors style preserved. (4) Update `seed.test.js` for the rewritten seeds' shape. (5) Run the full store suite.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/store/transfer.test.js src/store/seed.test.js</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <verify>
    <automated>grep -n "schemaVersion !== 5\|schemaVersion: 5" app/src/store/transfer.js | wc -l | tr -d ' '</automated>
    <fails_when>output is less than 2 — the gate or the export literal did not move to 5</fails_when>
  </verify>
  <acceptance_criteria>
- `app/src/store/db.js` declares DB_VERSION 5 and its upgrade callback is byte-identical apart from nothing (the bump is the only change).
- `app/src/store/transfer.js`'s gate and exportStore both carry 5; a file declaring 4 produces a named error at `$.schemaVersion`.
- validateBatch refuses a churn carrying the retired numeric names' values in the new fields' places with named-path errors, accepts a written 0 meltTestG, and refuses a marks value of 0 or 4.5 with a named error.
- validateVersion accepts the seed version's declaredAxes ['Body', 'Oil'] and declaredFlaw 'Bitter'; an unknown declared-axis name produces a named error.
- The store suite passes with the rewritten seed files: the seeded version and batch round-trip through exportStore/importStore unchanged.
  </acceptance_criteria>
  <done>The store writes and accepts exactly the new shape, refuses every older one with named errors, and reseeds the reviewed records through the seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import file → store | A hand-edited or stale store file crosses the validator into the write path |
| stored keys → domain maps | Stored/imported axis keys and row ids become object keys in marks and draft maps |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1-01 | Tampering | transfer.js validateBatch/validateTasting/validateVersion | medium | mitigate | Every new stored field enters the collect-all-errors validator (enum lists, 1–5 integer marks, known chip strings); the refuse-whole-file contract and `scanForUnsafeKeys` stand unchanged |
| T-03.3.1-02 | Tampering | domain/axes.js marks maps, new draft maps | medium | mitigate | All new maps write through the `setMark` spread-and-delete pattern and `Object.fromEntries`, never bare bracket writes keyed by stored/imported strings (T-02-32 discipline) |
| T-03.3.1-03 | Repudiation | parseMeasuredDraft (battery.js) | low | mitigate | A malformed numeric is never silently coerced to null — it returns not-ok so the UI can speak (plan 02 wires the sentence) |
</threat_model>

<verification>
- The domain/data/store scoped suites pass: `npm --prefix app test -- src/domain/axes.test.js src/domain/battery.test.js src/domain/batch.test.js src/data/olive-oil.test.js src/store/transfer.test.js src/store/seed.test.js`
- The retired identifiers are gone from domain/data/store production code (the plan's Retirements list).
- Mark's review outcome is recorded in the plan SUMMARY (done, corrected, or deferred per D-08).
</verification>

<success_criteria>
The stored shape is the battery: a batch saved through the one-save assembly carries every battery field with blank-is-absent discipline; the seed reads D-07 value-for-value; the store resets to DB_VERSION 5, reseeds the rewritten records, and refuses pre-change export files with every error named; Mark has reviewed the rewritten records (D-08).
</success_criteria>

<output>
Create `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-01-SUMMARY.md` when done — including Mark's seed-review outcome and the final stored field names (Phase 4's PRINT planning reads them).
</output>