---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 01
subsystem: database
tags: [domain, indexeddb, data-model, validation, vitest]

# Dependency graph
requires:
  - phase: 03.2-the-portion-and-the-reset
    provides: the reset-over-migration pattern (drop/recreate, reseed, refuse-old) this plan repeats at DB_VERSION 5
provides:
  - "domain/axes.js rewritten to the battery's fixed six-axis AXES table (core/declared split, five whole STOPS)"
  - "domain/battery.js (new): BATTERY_FIELDS, SEGMENT_OPTIONS, DEFECTS, DECLARED_FLAW, parseMeasuredDraft"
  - "domain/batch.js rewritten to the one-save model: createBatch/completeRecord, zero-or-one tasting, changed date"
  - "data/olive-oil.js and data/batch-2026-08-02.js rewritten onto the battery per D-07, Mark-reviewed"
  - "store reset to DB_VERSION 5 / schemaVersion 5, transfer.js validators extended to the battery's shape"
affects: [03.3.1-02, 03.3.1-03, phase-4-print-planning]

# Actuals (#2632)
actuals:
  tokens: 28056
  tasks: 3
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "battery.js: a zero-import pure domain module (no framework, no other domain module) holding the measured-field table, segment options, and the numeric draft parser"
    - "store validators (transfer.js) import domain constants (AXES, SEGMENT_OPTIONS, DEFECTS) rather than duplicating literal option strings"

key-files:
  created:
    - app/src/domain/battery.js
    - app/src/domain/battery.test.js
  modified:
    - app/src/domain/axes.js
    - app/src/domain/axes.test.js
    - app/src/domain/batch.js
    - app/src/domain/batch.test.js
    - app/src/domain/lineage.js
    - app/src/data/olive-oil.js
    - app/src/data/olive-oil.test.js
    - app/src/data/batch-2026-08-02.js
    - app/src/store/db.js
    - app/src/store/transfer.js
    - app/src/store/transfer.test.js
    - app/tests/db-reset.test.js

key-decisions:
  - "Mark's seed review (D-08): approved (pre-execution review, no pause) — every rewritten value in olive-oil.js and batch-2026-08-02.js matched D-07's mapping exactly, so Task 2 recorded the outcome and continued directly to Task 3 without pausing."
  - "transfer.js now imports domain/axes.js's AXES and domain/battery.js's SEGMENT_OPTIONS/DEFECTS for its enum checks, rather than hand-duplicating the option strings a second time — the first import either module has taken on."
  - "The UI layer (RecipePage.jsx, BatchRow.jsx, AxisMark.jsx, VersionRow.jsx, VersionStrip.jsx, IngredientTable.jsx, Method.jsx) is left reading the old, now-removed exports until plan 02 rebuilds it — this is the plan's own explicit sequencing (objective: 'the app's UI layer still reads the old shape until plan 02 lands'), not an oversight."

patterns-established:
  - "Zero-or-one tasting stored as a single nullable object (batch.tasting), not a one-element list — completeRecord(batch, churn, null, {now}) is the documented way to remove a stored tasting."
  - "buildChurn/buildTasting private helpers inside domain/batch.js are the one place field-assembly logic lives, shared by createBatch and completeRecord, so the two constructors cannot drift apart on the blank-is-absent rule."

requirements-completed: [BATCH2-01, BATCH1-01, BATCH1-02, OBS1-01]

coverage:
  - id: D1
    description: "domain/axes.js rewritten to the battery's fixed six-axis AXES table (core/declared split, five whole stops, stopWordsFor, axesForBatch resolving declared names from the snapshot)"
    requirement: "BATCH2-01"
    verification:
      - kind: unit
        ref: "app/src/domain/axes.test.js"
        status: pass
    human_judgment: false
  - id: D2
    description: "domain/battery.js created: BATTERY_FIELDS, SEGMENT_OPTIONS, DEFECTS, DECLARED_FLAW, parseMeasuredDraft — verbatim against the structural contract's Controls spec"
    verification:
      - kind: unit
        ref: "app/src/domain/battery.test.js"
        status: pass
    human_judgment: false
  - id: D3
    description: "domain/batch.js rewritten to the one-save model: createBatch/completeRecord, zero-or-one tasting, changed date, snapshot never retaken; retired functions removed"
    requirement: "BATCH1-01"
    verification:
      - kind: unit
        ref: "app/src/domain/batch.test.js"
        status: pass
    human_judgment: false
  - id: D4
    description: "The seeded olive oil version and its 2 Aug batch rewritten onto the battery per D-07, built through one createBatch call, Mark-reviewed"
    requirement: "OBS1-01"
    verification:
      - kind: unit
        ref: "app/src/data/olive-oil.test.js, app/src/domain/batch.test.js (augustSecondBatch describe block)"
        status: pass
    human_judgment: true
    rationale: "D-08 requires Mark's own review of the transcribed values against the printed sheet, not just a passing assertion that the code matches D-07's own text — recorded as approved via the pre-execution review protocol in this SUMMARY's Task 2 section."
  - id: D5
    description: "Store reset to DB_VERSION 5 / schemaVersion 5; transfer.js validators extended to the battery's churn/tasting/version shape and refuse older files with named errors"
    requirement: "BATCH1-02"
    verification:
      - kind: unit
        ref: "app/src/store/transfer.test.js, app/src/store/seed.test.js, app/tests/db-reset.test.js"
        status: pass
    human_judgment: false

# Metrics
duration: ~55min
completed: 2026-09-13
status: complete
---

# Phase 03.3.1 Plan 01: The battery's stored shape and the reviewed seed records Summary

**Rewrote axes.js/batch.js to sketch 007's fixed six-axis, five-stop battery with a one-save zero-or-one-tasting model; added battery.js; transcribed the seeded olive oil version and its 2 Aug batch onto D-07 through the new constructors; bumped the store to DB_VERSION 5 / schemaVersion 5 with matching validators.**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-09-13T11:25:00Z (approx, first Read)
- **Completed:** 2026-09-13T12:20:00Z (approx)
- **Tasks:** 3 (Task 1 tracer, Task 2 checkpoint — no pause, Task 3 auto)
- **Files modified:** 14 (2 new)

## Accomplishments
- `domain/axes.js` rewritten to the contract's fixed six-axis `AXES` table (Hardness/Scoopability/Smoothness/Sweetness core, Body/Oil declared), five whole `STOPS`, `stopWordsFor`, and `axesForBatch` resolving the snapshot's declared names against `AXES`; `CORE_AXES`/`MARK_STOPS` retired.
- `domain/battery.js` created: `BATTERY_FIELDS` (six measured fields, the two °C fields `signed: true`, the contract's two verbatim error strings), `SEGMENT_OPTIONS` (three segmented controls), `DEFECTS` (four chips), `DECLARED_FLAW = 'Bitter'`, and `parseMeasuredDraft` (blank always allowed, decimal comma/Unicode minus normalized, negatives rejected unless signed).
- `domain/batch.js` rewritten to the one-save model: `BATCH_SCHEMA_VERSION = 3`, `createBatch(version, churnFields, tastingFields, {id, now})`, `completeRecord(batch, churnFields, tasting, {now})` (churn replaced wholesale, tasting zero-or-one, `changed` stamped on every completing save); `addTasting`/`recordAmendment`/`isTastingSaveable`/`sortedTastings`/`hasTasting` retired.
- `data/olive-oil.js`: `VERSION_SCHEMA_VERSION = 4`, `declaredAxes: ['Body', 'Oil']`, `declaredFlaw: 'Bitter'` replacing the old free-anchor declared-axes objects.
- `data/batch-2026-08-02.js`: rewritten as one `createBatch` call carrying D-07's full transcription (churn + tasting in one assembly; `TASTING_ID` retired).
- Store gate: `db.js` `DB_VERSION` 4→5 (upgrade callback otherwise byte-identical); `transfer.js`'s schemaVersion gate and `exportStore` literal 4→5; `validateBatch`/`validateVersion` extended for the new churn numerics, segment enums, `changed`, the single `tasting` object (marks as 1–5 integers, known defect chips, `bitterDeclared` true-or-null, `meltStyle` enum), and `declaredAxes`/`declaredFlaw`.
- Mark's D-08 seed review recorded: **approved (pre-execution review, no pause)** — see Task 2 below.

## Task Commits

Each task was committed atomically:

1. **Task 1: The battery's stored shape and the rewritten seed records (D-07, D-09 shape)** - `d1500f4` (feat)
2. **Task 2: Mark reviews the rewritten seed records (D-08)** - no separate commit; recorded in this SUMMARY per the pre-execution review protocol (see below)
3. **Task 3: The store gate — reset to the new shape, refuse older files (D-09)** - `a1745a0` (feat)

**Plan metadata:** committed alongside this SUMMARY (see final commit in this plan's return message).

## Task 2: Mark's seed review (D-08)

**Outcome: approved (pre-execution review, no pause).**

Per the plan's own PRE-REVIEW note (recorded 2026-09-13, before this plan executed), Mark had already reviewed the seed records against the D-07 mapping and pre-approved the transcription, provided every rewritten value matched D-07 exactly. The executor verified the rewritten `app/src/data/olive-oil.js` and `app/src/data/batch-2026-08-02.js` against D-07's full value list:

| D-07 value | Rewritten record | Match |
|---|---|---|
| Oil character 4 | `tasting.marks.oil = 4` | yes |
| Sweetness 4 | `tasting.marks.sweetness = 4` | yes |
| Bitterness 5 → "Bitter · declared" on | `tasting.bitterDeclared = true` | yes |
| Hardness/smoothness/scoopability/body unmarked | absent from `tasting.marks` | yes |
| Melt test 3 | `tasting.meltTestG = 3` | yes |
| Melt style blank | `tasting.meltStyle = null` | yes |
| Tasted date blank | `tasting.tastedDate = null` | yes |
| Tasting temperature −12 | `tasting.tastingTempC = -12` | yes |
| Tempering blank | `tasting.temperingMinutes = null` | yes |
| Time to draw temp. 20 | `churn.timeToDrawTempMinutes = 20` | yes |
| Out of machine −6 | `churn.outOfMachineTempC = -6` | yes |
| Churn duration 30 | `churn.churnDurationMinutes = 30` | yes |
| Exit consistency blank | `churn.exitConsistency = null` | yes |
| Airiness blank | `churn.airiness = null` | yes |
| At the machine "Soft, not greasy" | `churn.atTheMachine = 'Soft, not greasy'` | yes |
| Ingredient notes "oil bottle opened 24 Jul" | `churn.ingredientNotes = 'oil bottle opened 24 Jul'` | yes |
| Note blank | `tasting.note = null` | yes |
| Defects none | `tasting.defects = null` | yes |
| Next time blank | `churn.nextTimeNote = null` | yes |
| Declared pair Body/Oil, declared flaw Bitter | `oliveOilVersion.declaredAxes = ['Body', 'Oil']`, `declaredFlaw = 'Bitter'` | yes |

Every value matched D-07 exactly. Per the checkpoint's pre-review instruction, execution continued directly to Task 3 without pausing or presenting the files to Mark again.

## Files Created/Modified
- `app/src/domain/axes.js` - Rewritten: fixed six-axis AXES table, STOPS, stopWordsFor, axesForBatch, markKeyFor
- `app/src/domain/axes.test.js` - Rewritten for the new shape
- `app/src/domain/battery.js` - New: BATTERY_FIELDS, SEGMENT_OPTIONS, DEFECTS, DECLARED_FLAW, parseMeasuredDraft
- `app/src/domain/battery.test.js` - New
- `app/src/domain/batch.js` - Rewritten: createBatch/completeRecord, retired five functions
- `app/src/domain/batch.test.js` - Rewritten for the new shape
- `app/src/domain/lineage.js` - Stale doc comment referencing retired recordAmendment updated
- `app/src/data/olive-oil.js` - VERSION_SCHEMA_VERSION 4, declaredAxes/declaredFlaw model
- `app/src/data/olive-oil.test.js` - declaredAxes/declaredFlaw assertions updated
- `app/src/data/batch-2026-08-02.js` - Rewritten as one createBatch call, D-07 transcription
- `app/src/store/db.js` - DB_VERSION 4→5
- `app/src/store/transfer.js` - schemaVersion 4→5, validators extended to the battery's shape
- `app/src/store/transfer.test.js` - Rewritten fixtures and cases
- `app/tests/db-reset.test.js` - axesForBatch assertions updated (regression fix, see Deviations)

## Decisions Made
- Mark's D-08 seed review: approved (pre-execution review, no pause) — see Task 2 table above.
- Battery field/store shape choices left to Claude's discretion by 03.3.1-CONTEXT.md were resolved as: the single tasting is a nullable object (`batch.tasting`, not a one-element list); `DB_VERSION`/`schemaVersion` both moved to 5; churn field names are `timeToDrawTempMinutes`, `outOfMachineTempC`, `churnDurationMinutes`, `exitConsistency`, `airiness`, `atTheMachine`, `ingredientNotes`, `nextTimeNote`; tasting field names are `tastedDate`, `temperingMinutes`, `tastingTempC`, `marks`, `note`, `defects`, `bitterDeclared`, `meltTestG`, `meltStyle` (Tempering placed in tasting per the contract's "Where sources disagree" §1, per Pitfall 2).
- `transfer.js` imports `AXES` (domain/axes.js) and `SEGMENT_OPTIONS`/`DEFECTS` (domain/battery.js) for its enum checks rather than duplicating the option strings — avoids the two copies drifting.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale doc comment in domain/lineage.js referencing the retired `recordAmendment`**
- **Found during:** Task 1's second automated verify (the retired-identifier grep)
- **Issue:** `lineage.js`'s `saveOverVersion` docblock compared itself to `domain/batch.js's recordAmendment`, a function this plan retires — the grep for retired identifiers flagged it, and the comment would otherwise mislead a future reader about a function that no longer exists.
- **Fix:** Reworded the comment to reference `completeRecord`, the function that now carries the same "correction, never a new event" shape.
- **Files modified:** `app/src/domain/lineage.js`
- **Verification:** `grep` for the retired-identifier set returns 0 hits outside the one documented exception (see below); `lineage.test.js` still passes.
- **Committed in:** `d1500f4` (Task 1 commit)

**2. [Rule 1 - Bug] `app/tests/db-reset.test.js` asserted the old axis shape (`.label`, `'Olive oil character'`/`'Bitterness'`)**
- **Found during:** Task 3, after running the full test suite to confirm the scope of expected breakage
- **Issue:** This integration test (outside `app/src`, deliberately, per its own header comment about the idb-import seam gate) calls `axesForBatch` against a real reseeded database and asserted the pre-battery shape — a direct regression from Task 1's `axes.js` rewrite, not a UI-layer concern deferred to plan 02.
- **Fix:** Updated the two assertions to `axes[4].name === 'Body'` / `axes[5].name === 'Oil'`, and corrected a stale "opens at version 4" test description to "version 5".
- **Files modified:** `app/tests/db-reset.test.js`
- **Verification:** `npm --prefix app test -- app/tests/db-reset.test.js` passes; full suite re-run confirms no further store/domain/data regressions.
- **Committed in:** `a1745a0` (Task 3 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 — bugs directly caused by this plan's own shape changes, not new functionality).
**Impact on plan:** No scope creep. Both fixes are corrections to code this plan's own changes broke; neither touches the deferred UI layer.

## Known Exceptions (documented, not fixed)

**Task 1's second automated verify (`grep -rn "MARK_STOPS\|CORE_AXES\|addTasting\|recordAmendment\|isTastingSaveable\|sortedTastings\|hasTasting\|comeUpMinutes\|overrunPercent" app/src/domain app/src/data --include='*.js' | grep -v '\.test\.js' | wc -l`) reports 1, not 0.**

The one hit is `app/src/data/olive-oil.js:97` — `iceEd: { style: null, servingTemperatureC: null, hardness: null, overrunPercent: null }`. This `overrunPercent` is the recipe's own Ice Ed export placeholder (a future-feature field, always null today, read by nothing yet — confirmed by a repo-wide grep for `iceEd`), coincidentally sharing a name with the *retired churn field* the check intends to catch. It is not the retired batch-record field, has no relationship to it, and this plan's Task 1 action explicitly instructs "everything else untouched" for `olive-oil.js` outside the `declaredAxes`/`declaredFlaw` block. Renaming it would be out-of-scope architectural churn on an unrelated, dormant field for the sake of a literal grep match. Documented here rather than silently worked around.

**The UI layer is broken until plan 02, as the plan's own objective states.**

`RecipePage.jsx`, `BatchRow.jsx`, `AxisMark.jsx`, `VersionRow.jsx`, `VersionStrip.jsx`, `IngredientTable.jsx`, and `Method.jsx` import exports this plan retires (`CORE_AXES`, `MARK_STOPS`, `addTasting`, `recordAmendment`, `isTastingSaveable`, `sortedTastings`, `hasTasting`). `npm --prefix app test` (full suite, not scoped) shows 22 failing tests, all in `src/ui/BatchRow.test.jsx` (19) and `src/ui/AxisMark.test.jsx` (3) — exactly the UI-layer surface the plan's objective names as deferred: "The app's UI layer still reads the old shape until plan 02 lands; this task's gates are the domain/data suites only." None of the plan's own `<verify>` commands (scoped to `src/domain`, `src/data`, `src/store`) touch these files, and all of them pass. This is expected, not a regression to fix in this plan.

## Issues Encountered
- The worktree had no `app/node_modules` (a fresh worktree checkout, not shared automatically with the main repo's install). Symlinked `app/node_modules` to the main repo's `app/node_modules` rather than reinstalling, since `package.json`/`package-lock.json` are unchanged by this plan and a symlink is faster and avoids a redundant install. No package.json changes were made.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Final stored field names, for Phase 4's PRINT planning:**

- **Batch, top level:** `schemaVersion` (3), `id`, `versionId`, `recordedAt`, `changed` (string-or-null), `snapshot`, `churn`, `tasting` (object-or-null).
- **Batch.snapshot:** `coefficientSetId`, `versionLabel`, `rows`, `declaredAxes` (array of axis names), `declaredFlaw` (string-or-null).
- **Batch.churn:** `churnDate`, `asMade`, `stepChanges`, `timeToDrawTempMinutes`, `outOfMachineTempC`, `churnDurationMinutes`, `exitConsistency`, `airiness`, `atTheMachine`, `ingredientNotes`, `nextTimeNote`.
- **Batch.tasting** (when present): `tastedDate`, `temperingMinutes`, `tastingTempC`, `marks` (object of axis key → 1–5 integer), `note`, `defects` (array-or-null), `bitterDeclared` (true-or-null), `meltTestG`, `meltStyle`.
- **Version:** `declaredAxes` (array of names, e.g. `['Body', 'Oil']`), `declaredFlaw` (e.g. `'Bitter'`).
- **domain/axes.js:** `AXES`, `STOPS`, `CORE_AXIS_COUNT`, `stopWordsFor`, `axesForBatch`, `markKeyFor`, `setMark`.
- **domain/battery.js:** `BATTERY_FIELDS`, `SEGMENT_OPTIONS`, `DEFECTS`, `DECLARED_FLAW`, `parseMeasuredDraft`.

Plan 02 rebuilds the UI layer (the record pen, the one-save ceremonies, the read view) against this shape; the domain/data/store layer is now stable ground for it to build on. The store resets on next open (DB_VERSION 5) and reseeds the reviewed records through the same seam every future write uses.

---
*Phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007*
*Completed: 2026-09-13*

## Self-Check: PASSED

All files listed under "Files Created/Modified" confirmed present on disk; both task commit hashes (`d1500f4`, `a1745a0`) confirmed present in `git log`.
