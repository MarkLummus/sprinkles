---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 02
type: execute
wave: 2
depends_on: ["03.3.1-01"]
files_modified:
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/PenFoot.jsx
  - app/src/ui/PenFoot.test.jsx
autonomous: true
requirements: [UX1-01, UX1-03, BATCH1-01, BATCH1-02, BATCH2-01]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A maker can record a churn-only batch end-to-end: open the pen, write the battery's churn fields, press Save batch, and the read view reads the record back in the battery's words (Time to draw temp., Out of machine, Churn duration, Exit consistency, Airiness, At the machine, Ingredient notes)."
    - "The record pen has one save action rendered as two identical ceremonies — same component, same Save batch label, Cancel first — one at the end of the record just above the shared Next time, one at the foot; there are never three sets with different meanings (D-01)."
    - "A save persists everything the record currently holds; the tasting section's openness decides nothing about the label, and the Save control is never disabled by a completeness gate (D-02)."
    - "Save batch on a pen without a churn date stays enabled, shows one sentence beside both ceremonies, and moves focus to the churn date (D-05, press-to-block)."
    - "A malformed numeric stays in the field marked invalid with its contract sentence, and focus moves to the first invalid field; nothing is silently coerced (contract 'Controls spec')."
    - "Correct reopens the record with the battery's values in the fields and saving stamps the changed date (D-03, D-04)."
  artifacts:
    - app/src/ui/RecipePage.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/PenFoot.jsx
  key_links:
    - "the one save handler → parseMeasuredDraft → createBatch/completeRecord → repository.saveBatch (the seam is the only write path)"
    - "the blocked-date derivation → the sentence beside both ceremonies + the churn-date focus effect (one traversal, message and target never disagree)"
    - "isDraftDirty (extended) → the beforeunload and Escape guards read the same check, no fourth notion of dirtiness"
---

<objective>
The record pen's churn half becomes the battery. RecipePage's pen state machine folds the tasting pen away (the pens become plan / record / amend), carries one draft holding every field the record can take, and saves through the one-save model; BatchRow renders the churn section with the battery's labels and controls; the two identical Cancel | Save batch ceremonies mount at the end of the record and the foot (D-01); the churn date blocks in words (D-05); malformed numerics stay in place with the contract's sentences; and the read view reads the churn fields back. This plan restores the full suite and build after plan 01's shape flip.

Purpose: the churn half of the sheet's transcription becomes writable and readable on the battery; the one-save model is the foundation the tasting section (plan 03) completes.
Output: rebuilt record-pen state machine and churn section; the ceremony component; press-to-block and measured validation wired; the churn half of the read view.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@.impeccable/surfaces/route-recipe-batch.md
@app/src/ui/RecipePage.jsx
@app/src/ui/BatchRow.jsx
@app/src/ui/PenFoot.jsx
@app/src/domain/battery.js
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (the pen's state machine and churn half):

- RecipePage: draft shape `recordDraft` fields `churnDate, asMade, stepChanges, timeToDrawTempMinutes, outOfMachineTempC, churnDurationMinutes, exitConsistency, airiness, atTheMachine, ingredientNotes, nextTimeNote, tastingOpen, tastedDate, temperingMinutes, tastingTempC, marks, note, defects, bitterDeclared, meltTestG, meltStyle`; states `fieldErrors`, `blockedDateMessage`, `blockedDateTarget` (with attempt counter), `formStatus`; handlers `handleSaveBatch` (one save), `handleChangeRecordField`, `handleChangeSegment`, plus the surviving churn handlers; export `derivePenState` (pens plan/record/amend).
- PenFoot.jsx: exported `SaveCeremony` component (the one ceremony markup both mounts share); record/amend branch renders Cancel | Save batch, Save never disabled (the `Add tasting` beside it is plan 03's control).
- BatchRow.jsx: the churn pen section (Churn date; Time to draw temp. min; Out of machine °C; Churn duration min; Exit consistency; Airiness (estimated); At the machine; Ingredient notes), ceremony A at the end of the record, the shared Next time textarea, the `form-status` live region — the foot status region's one home; PenFoot mounts no status region — and the battery's churn read cells.
- CSS classes: `.field-error`, `.batch-row__summary` is NOT this plan's; this plan's classes: `save-ceremony`, `save-ceremony__hint`, `form-status` (styled in plan 06).
</artifacts_this_phase_produces>

## Retirements (this plan removes)

`tastingDraft`, `isTastingDraftDirty`, `handleStartTasting`, `handleCancelTasting`, `handleChangeTastingField`, `handleChangeTastingMark`, `handleUseAsExpectedShortcut`, `handleSaveTasting` (RecipePage); `derivePenState`'s tasting branch; the tasting completeness gate and its hint sentence (RecipePage, RecipePage.jsx lines ~494-498); PenFoot's tasting branch and its `disabled` Save; the old tasting ceremony block in BatchRow (including the retired shortcut button's only render site); read-view cells reading the retired churn field names and the Amended cell; the later-batches meta's read of the retired draw temperature name and the retired plural-tasting wording (Pitfall 7 — its replacement wording arrives in plan 05; this plan only makes the meta read the new shape).

<tasks>

<task type="tracer">
  <name>Task 1: The one record pen — churn section, one save, ceremonies, read-back (D-01, D-02)</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/PenFoot.jsx, app/src/ui/PenFoot.test.jsx</files>
  <read_first>
- `app/src/ui/RecipePage.jsx` — the file being rebuilt: derivePenState (214-228), the draft dirty checks (75-116), the guards (408-452), handleStartRecording/handleStartAmending/handleSaveBatch (602-759), the tasting handlers (776-835)
- `app/src/ui/BatchRow.jsx` — the pen body and read view being rebuilt (273-380 pen; 383-461 read)
- `app/src/ui/PenFoot.jsx` — the ceremony becoming the shared component
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "DOM order inventory", "Controls spec", "Blank stays blank"
- `03.3.1-01-SUMMARY.md` — the final stored field names and the new domain contract
- `03.3.1-RESEARCH.md` — "The one-save model", "Draft shape", "The one save (page-level handler shape)"
  </read_first>
  <action>
Wire one path end-to-end — open the record pen, write the churn battery, Save batch, read it back. Cite the contract's "DOM order inventory" and "Controls spec", and D-01/D-02/D-03/D-04.

1. RecipePage: replace the draft shape with the one record draft from this plan's artifacts list — the churn fields renamed to the stored names, the tasting fields folded into the same draft with a `tastingOpen` flag (false on a fresh recording; true when Correct pre-fills a stored tasting, per D-03). Extend `isDraftDirty` to every new field with the same presence-over-truthiness discipline (marks by own-key count, defects by length, bitterDeclared by boolean inequality) and fold the tasting dirty check away — the beforeunload and Escape guards read this one check, unchanged in mechanism. Retire the tasting pen state named in this plan's Retirements section (including `derivePenState`'s tasting branch — the pens become plan / record / amend). Retire the tasting completeness gate: the record pen's Save is never disabled; the plan pen's own gate and hint are untouched.
2. RecipePage: rewrite the save handler as the one save (the two impure calls stay in the handler). Order per RESEARCH.md Open Question 3's recommendation: parse every battery numeric through `parseMeasuredDraft` with each field's own `signed` flag from BATTERY_FIELDS; if any field is malformed, record per-field errors and abort (the full validation UI is Task 2 — here, storing the errors and aborting is enough); then assemble: text via empty-to-null, numerics from the parsed values, `exitConsistency`/`airiness`/`meltStyle` as the picked string or null, `marks` through `setMark`, `defects` as the array or null when empty, `bitterDeclared` true-or-null; a tasting object only when `tastingOpen` and the tasting side holds ink (any own-key mark, any picked chip, the declared toggle, or any non-empty tasting field) — an open-but-empty section persists churn only, never a manufactured empty tasting (D-02). Branch on the record operation, never the button: `amendingBatchId` set calls `completeRecord(batch, churnFields, tasting, { now })` with the YYYY-MM-DD date string computed here; otherwise `createBatch(version, churnFields, tasting, { id: crypto.randomUUID(), now })`. Update `handleStartAmending` to pre-fill every battery field including the tasting fields when the batch carries one — all own-property reads from the batch object, no computed-key writes, so no import-shaped prototype path can reach a draft map (the T-03-10 adjacency rule from 03.3.1-RESEARCH.md Security Domain); keep the separate `structuredClone` baseline for the dirty check.
3. BatchRow: rebuild the pen body's churn section to the battery per the contract's "DOM order inventory" — Churn date (type=date, the pen's first field, autofocus as today); Time to draw temp. (min); Out of machine (°C); Churn duration (min); Exit consistency and Airiness (estimated) as segmented radiogroups with the contract's option strings and no default; At the machine (textarea, no placeholder); Ingredient notes (textarea, placeholder verbatim "e.g. oil bottle opened 24 Jul"). Every non-date numeric input is text-mode with `inputMode="decimal"` — never `type="number"` (the contract requires stay-in-place malformed values). Give textareas `dir="auto"` and a height re-fit to scrollHeight on input. Keep the as-made column and step lines untouched (out of scope per the brief's § 7).
4. The two identical ceremonies (D-01): extract one `SaveCeremony` component exported from `app/src/ui/PenFoot.jsx` — hint paragraph slot, then Cancel first, then a Save batch button that is never disabled — and mount it twice for the record pen: ceremony A rendered by BatchRow at the end of the record (after the churn section while the tasting section is absent; the tasting-open position arrives with plan 03), just above the shared Next time textarea (placeholder verbatim "optional — for the batch, the tasting, or both"); ceremony B rendered by PenFoot's record/amend branch at the foot. Same markup, same label, Cancel first, differing only in placement (D-01). The imprint ceremony stays retired for this pen per the brief's 2026-09-12 revision (D-06: the contract's save-pair sections are superseded by D-01–D-04 — the sketch's `#churn-saves` row and its state-based footer label are NOT built, Pitfall 1). No Add tasting control yet (plan 03 opens the tasting section).
5. BatchRow's read view, churn half: cells for Time to draw temp. (min), Out of machine (signed °C), Churn duration (min), Exit consistency and Airiness as their picked words, each blank reading the row's own absence words per the house pattern (`readMeasured` via the display-only override that says "not measured" for this row's cells); At the machine, Ingredient notes and Next time as prose; the Recorded cell unchanged; the Amended cell removed (its data source retires; the changed date arrives in plan 05). Update the later-batches meta to read the new stored shape (drawn temperature from the new field name; tasted presence from the single tasting object) — its final wording lands in plan 05.
6. Update RecipePage.test.jsx, BatchRow.test.jsx and PenFoot.test.jsx: the pen ceremony's identical-pairs proof (Cancel index before Save index, both mounts carry the same label), the one-save create path and the completeRecord path with changed stamped, the extended dirty check, the absence words on the churn cells, the segmented groups' aria-labels and option strings.
  </action>
  <verify>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit, or the summary line shows any failed or skipped test</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit — the build no longer imports a retired export</fails_when>
  </verify>
  <acceptance_criteria>
- RecipePage's draft carries all twenty record fields with tastingOpen; `derivePenState` returns only plan/record/amend; the tasting completeness gate and its hint sentence exist nowhere under app/src.
- BatchRow's pen body renders the churn section with the labels Churn date, Time to draw temp., Out of machine, Churn duration, Exit consistency, Airiness (estimated), At the machine, Ingredient notes; the two numeric °C fields are the only signed ones; all three segmented groups render exactly the contract's option strings.
- SaveCeremony is one component with two mounts; both render Cancel before Save batch; no Save button on the record pen carries a disabled attribute.
- The read view renders Time to draw temp., Out of machine, Churn duration, Exit consistency and Airiness cells with absence in words; no read code references the retired churn field names.
- `handleSaveBatch` calls completeRecord when amending (changed stamped from the handler's own date string) and createBatch otherwise; both paths pass a null tasting when the section holds no ink.
- Full suite passes and the build compiles.
  </acceptance_criteria>
  <done>A churn-only record can be written and saved on the battery and read back in the battery's words; the two identical ceremonies and the one-save handler exist; the suite and build are green again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Press-to-block on the churn date and stay-in-place numeric validation (D-05, contract "Controls spec")</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx</files>
  <behavior>
- Save press with churnDate blank: the sentence "Enter the date you churned." appears beside ceremony A and beside ceremony B (both hint slots), Save stays enabled, focus moves to the churn date input; a second consecutive press re-fires the focus move (attempt counter); the sentence clears on the next draft edit.
- Save press with an unsigned field holding a stray letter: the field keeps the maker's characters, carries aria-invalid and a .field-error line wired by aria-describedby with the unsigned sentence verbatim, focus lands on the first invalid field in field order, and the status region reads "Check the marked measurements. Your entries have been kept."
- The same for a signed field holding a stray letter (temperature sentence verbatim) and for a negative typed into an unsigned field.
- Validation order locked: measurements are checked first, then the churn date (RESEARCH.md Open Question 3).
- A toast-bearing status written with self-clear clears after five seconds only if the text is still the message it wrote; validation and block statuses never self-clear (contract "Feedback and undo lifecycle").
  </behavior>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "Controls spec" (the two error strings, stay-in-place, aria wiring, focus rule) and "Feedback and undo lifecycle" (form-status, self-clear guard)
- `app/src/ui/RecipePage.jsx` — the plan pen's press-to-block machinery (blockedMessage/blockedTarget/blockedAttemptRef, 299-311 and 1031-1047) and the field-setter clear pattern (888-892); VersionRow.jsx lines 110-118 for the attempt-keyed focus effect
- `app/src/domain/battery.js` — BATTERY_FIELDS's error strings and signed flags (from plan 01)
- `app/src/domain/lineage.js` — the one-traversal rule: the sentence and the field it names can never disagree
  </read_first>
  <action>
Cite the contract's "Controls spec" and "Feedback and undo lifecycle", and D-05. (1) In RecipePage, build one derivation for the record pen's save gate: per-field `fieldErrors` keyed by battery field key from `parseMeasuredDraft(raw, { signed: field.signed })`, then the churn-date block. On a save press, walk BATTERY_FIELDS in order; on the first invalid field store its error string, focus it (a ref map keyed by field key, attempt-keyed so a second consecutive block re-fires — the VersionRow pattern, single-target variant for the date), write the validation status string verbatim to the form-status state, and abort. Only then apply the churn-date press-to-block: Save stays enabled; a blank churn date sets the blocked-date sentence (working words "Enter the date you churned." — planner's call within the plain-words rule) and the attempt-keyed focus target for the churn date's input, beside BOTH ceremonies; every draft-editing handler clears the blocked sentence and the field errors first (the 888-892 pattern). Never disable Save for either block (D-05, D-02). (2) Render the two hint slots through SaveCeremony's hint paragraph — the same sentence component beside both ceremonies, filled from the one blocked-date state so the two can never disagree. (3) Add the `form-status` live region (role="status", aria-live="polite") in BatchRow as the record body's last element — at the pen's foot, directly above PenFoot's ceremony B per the contract's "DOM order inventory" — its one home for the phase (PenFoot mounts no status region; plan 04's tasting-status joins it in BatchRow); the status state plus the announce helper live in RecipePage with the guarded five-second self-clear (a ref comparing the current text; RESEARCH.md Code Example 5); per-axis Clear and validation write persistently. (4) In BatchRow, wire each battery field's label to append the `.field-error` line inside the label element and `aria-describedby` to it, set `aria-invalid` from the per-field error, and render both hint slots. (5) Test via the RED-GREEN-REFACTOR loop: write the failing component tests for the behaviors above (renderToStaticMarkup for the markup facts; the blocked-sentence and error-string assertions are string-exact), then implement.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/RecipePage.test.jsx src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or any of the new blocked-date or field-error describes failed</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'role="status"' app/src/ui/BatchRow.jsx</automated>
    <fails_when>output is 0 — the form-status live region is absent</fails_when>
  </verify>
  <acceptance_criteria>
- Both ceremony hint slots render the same blocked-date sentence from one state; `grep -c "Enter the date you churned" app/src/ui/RecipePage.jsx` returns exactly 1 (one state, two renderings).
- Each battery field's markup carries aria-describedby pointing at its .field-error element when invalid; aria-invalid appears exactly on invalid fields.
- The two error strings in the components are character-identical to BATTERY_FIELDS's strings.
- A blank churn date never disables Save and always moves focus to the churn date on press.
- The validation status string matches the contract's sentence exactly.
  </acceptance_criteria>
  <done>Blocked saves speak in words beside both ceremonies with focus on the named field; malformed numerics stay in place with the contract's sentences and the validation status; the suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Correct reopens everything the record holds (D-03), focus returns to the openers</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx</files>
  <read_first>
- `app/src/ui/RecipePage.jsx` — handleStartAmending's pre-fill and baseline-clone pattern (678-703)
- `app/src/ui/BatchRow.jsx` — the focus-return ref/effect pattern for the row's openers (167-197) and the Correct/Add-tasting acts row (467-481)
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — section "How to cite this contract" (save-pair supersession note) and "Settled defaults"
- `03.1-CONTEXT.md` — D-27 (focus return), D-24 (Cancel discards the whole draft)
  </read_first>
  <action>
Cite the contract's "Settled defaults" section, and D-03/D-04/D-27. (1) `handleStartAmending` pre-fills the full battery draft from the batch: every churn field as its string form, `marks` copied own-key-by-own-key, `defects` as a copy of the stored list or empty, `bitterDeclared` from the stored value, `tastingOpen` true when the batch carries a tasting (D-03 — Correct reopens the same pen with everything editable, tasting fields in place). All reads go through own-property access on the batch; no computed-key writes anywhere in the pre-fill path (the T-03-10 adjacency rule). The amend baseline is the separate structuredClone it is today. (2) Saving the Correct pen stamps `changed` (already wired in Task 1); saving it with the tasting section collapsed (the Remove control arrives in plan 04) persists the removal per D-02's literal reading — surface this reading in the plan SUMMARY for end-of-phase UAT (RESEARCH.md Assumption A3). (3) Focus return (D-27): the Correct and Add-tasting openers keep their ref/effect pairs (refs above the conditional render); after any save or Cancel the page returns to reading and focus returns to whichever opener opened the pen — verify the existing two pairs still cover the rebuilt pen (the tasting opener's pair now keys on tastingOpen, not the retired tasting-pen state). (4) Escape closes only an untouched record pen — the extended dirty check decides; an open-but-empty tasting section counts as no ink (adding the section writes no ink per D-02), so Escape closes the pen and Cancel likewise discards it (RESEARCH.md Open Question 4, resolved here) — note this reading for end-of-phase UAT; the beforeunload guard reads the same check. (5) Update the two component test files for the amend pre-fill (every field, including a stored tasting's), the changed-stamping assertion, and the focus-return effects.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/RecipePage.test.jsx src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or the amend/dirty describes report failures</fails_when>
  </verify>
  <acceptance_criteria>
- Opening Correct on the seeded batch pre-fills churnDate 2026-08-02, timeToDrawTempMinutes 20, outOfMachineTempC -6, churnDurationMinutes 30, atTheMachine 'Soft, not greasy', tastingOpen true with tastingTempC -12, marks { sweetness: 4, oil: 4 }, bitterDeclared true, meltTestG 3.
- Saving the Correct pen without edits stamps changed with the handler's date string and leaves recordedAt and the snapshot untouched.
- The amend pre-fill contains no computed-key assignment against a stored id (own-property reads only).
- The beforeunload and Escape effects read the extended dirty check; a typed 0 counts as ink.
- Escape with the tasting section open but empty closes the pen (the empty section is no ink, D-02); Cancel likewise discards it.
  </acceptance_criteria>
  <done>Correct reopens the full record — churn and tasting in place — and saves through the one-save path with the changed date stamped; focus returns to the opener; the suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored batch → pre-fill draft | A stored record's keys and values flow into the pen's draft state on Correct |
| keystrokes → save path | Every raw typed string crosses parseMeasuredDraft before storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1-04 | Tampering | handleStartAmending pre-fill (RecipePage) | medium | mitigate | Pre-fill reads stored values through own-property access and never assigns through a computed key — the record-pen rebuild must not copy the plan pen's bare-bracket seeding pattern (the open T-03-10 carry) |
| T-03.3.1-05 | Repudiation | the save path's measured parsing | medium | mitigate | A malformed value stays in place, is named by its contract sentence, and blocks the save — nothing silently becomes null or NaN (contract "Controls spec") |
| T-03.3.1-06 | Tampering | prose rendering (At the machine, Ingredient notes, note) | low | accept | Prose renders as text via React's default escaping; the project's no-dangerous-markup convention stands; nothing in this plan renders stored prose as markup |
</threat_model>

<verification>
- `npm --prefix app test` passes (the full suite is green again after plan 01's shape flip).
- `npm --prefix app run build` compiles.
- A churn-only batch records end-to-end and reads back; both ceremonies are identical; D-05's block and the contract's validation surface work.
</verification>

<success_criteria>
The record pen's churn half is the battery: one draft, one save, two identical ceremonies, press-to-block on the churn date, stay-in-place measured validation, Correct reopening everything, and the churn half of the read view in the battery's words.
</success_criteria>

<output>
Create `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-02-SUMMARY.md` when done — note the A3 removal-on-save reading for end-of-phase UAT.
</output>