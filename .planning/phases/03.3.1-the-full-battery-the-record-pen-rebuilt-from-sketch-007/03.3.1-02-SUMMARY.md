---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 02
subsystem: ui
tags: [react, forms, validation, accessibility, vitest]

# Dependency graph
requires:
  - phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
    provides: "the battery's stored shape (domain/battery.js, domain/axes.js, domain/batch.js's one-save model) from plan 01"
provides:
  - "RecipePage.jsx's one record draft (churn + tasting fields, tastingOpen flag), derivePenState narrowed to plan/record/amend, the one save handler (handleSaveBatch), validateRecordDraft (measurements-then-churn-date press-to-block)"
  - "draftFromBatch: Correct's own full pre-fill (churn + tasting) from a stored batch"
  - "BatchRow.jsx's churn pen section rebuilt to the battery, the churn half of the read view, the two identical SaveCeremony mounts, the field-error/form-status UI"
  - "PenFoot.jsx's exported SaveCeremony component, shared by the foot (ceremony B) and BatchRow's end-of-record (ceremony A)"
affects: [03.3.1-03, 03.3.1-04, 03.3.1-05, 03.3.1-06]

# Actuals (#2632)
actuals:
  tokens: 40186
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SaveCeremony (PenFoot.jsx): one component, two mounts (D-01) — the pattern any future repeated-ceremony control in this codebase should follow"
    - "validateRecordDraft: one traversal returning fieldErrors + invalidFieldKey + blockedDateMessage + parsed, so a save handler never re-parses a field it already validated and the two blocks (measurement, date) can never both fire"
    - "Segmented radiogroups driven entirely by onClick (not onChange) with a no-op onChange, so click-again-clears and first-pick share one handler"

key-files:
  created: []
  modified:
    - app/src/ui/RecipePage.jsx
    - app/src/ui/RecipePage.test.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/BatchRow.test.jsx
    - app/src/ui/PenFoot.jsx
    - app/src/ui/PenFoot.test.jsx
    - app/src/ui/AxisMark.jsx
    - app/src/ui/AxisMark.test.jsx

key-decisions:
  - "AxisMark.jsx's broken import (MARK_STOPS, retired by 03.3.1-01's axes.js rewrite) was fixed to STOPS as a Rule 3 blocking-issue auto-fix — not in this plan's files_modified list, but required for 'npm test' to pass at all. AxisMark itself is not wired to any consumer in this plan (the tasting section that will use it is plan 03's build); its own test fixture still uses a hand-rolled axis shape (label, not name) unrelated to domain/axes.js's real AXES — left untouched beyond the minimal import fix, since rewiring it to the real battery shape is plan 03's job once it actually renders."
  - "Task 2's tdd=\"true\" requirement is honored as a genuine RED-then-GREEN split, corrected mid-execution: Task 1's first pass over-built the churn-date press-to-block and field-error UI (which the plan explicitly reserves for Task 2) directly into the tracer commit. Caught before the SUMMARY was written; the offending state/JSX was stripped back to Task 1's own stated minimum (store fieldErrors, abort — no field-level UI, no date block), a real RED test suite was written and confirmed to fail for the right reasons (10 targeted failures, TypeError on the not-yet-existing validateRecordDraft plus missing-markup assertions), then the full feature was re-implemented as the GREEN commit. See commits 0941f93 (Task 1, corrected), 5e2bcd1 (RED), 1193ee4 (GREEN)."
  - "validateRecordDraft supersedes parseAllMeasuredFields as the save-time gate but does not replace its export — parseAllMeasuredFields stays exported and used internally, since Task 1's own tests already exercised it directly and removing it would have meant rewriting passing coverage rather than adding new coverage, for no behavioral gain."
  - "Ingredient notes moved from a single-line input to a textarea, matching the contract's DOM order inventory ('Ingredient notes — textarea, placeholder...') — the old shape predates the battery rebuild."
  - "The 'plan 10–12 min' comparison annotation on the Time to draw temp. read-view cell (via targetValueFor(version, 'come-up')) is carried forward unchanged from the pre-battery code — not requested or forbidden by this plan, and removing working, harmless functionality would have been out of scope."

patterns-established:
  - "SaveCeremony (PenFoot.jsx, exported): the one component both mounts of a repeated Cancel|Save pair render, taking only onCancel/onSave/hint — no gate logic of its own."
  - "The record pen's one save handler branches on the record operation (amendingBatchId set vs not), never on which button was pressed — the same discipline the plan pen's buildPenFields already established."

requirements-completed: [UX1-01, UX1-03, BATCH1-01, BATCH1-02, BATCH2-01]

coverage:
  - id: D1
    description: "A maker can record a churn-only batch end-to-end (open the pen, write the churn fields, press Save batch) and read it back in the battery's words"
    requirement: "BATCH1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > buildChurnFieldsFromDraft / buildTastingFieldsFromDraft — the save assembly (D-02, D-10)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the record's reading state, measured values as cells"
        status: pass
    human_judgment: true
    rationale: "The full open-pen → type → save → read-back flow through the real rendered UI was not driven in a browser this session (no checkpoint task requested it); the assembly and read-view logic are proven at the unit/component level, but the maker-facing interaction itself needs a human pass at end-of-phase UAT."
  - id: D2
    description: "The record pen has one save action rendered as two identical ceremonies — same component, same 'Save batch' label, Cancel first — never three sets with different meanings (D-01)"
    requirement: "BATCH1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/PenFoot.test.jsx > SaveCeremony — the one component both mounts share (D-01)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the record and amend ceremony, the battery's churn section"
        status: pass
    human_judgment: false
  - id: D3
    description: "A save persists everything the record currently holds; the tasting section's openness decides nothing about the label, and Save is never disabled by a completeness gate (D-02)"
    requirement: "BATCH1-02"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > tastingHasInk — D-02: a save with the section open but empty persists no tasting"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > never renders a disabled Save batch button — the record pen has no completeness gate (D-02)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Save batch on a pen without a churn date stays enabled, shows one sentence beside both ceremonies, and moves focus to the churn date (D-05, press-to-block)"
    requirement: "UX1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > validateRecordDraft — the one traversal: measurements first, then the churn date"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the record pen's own blocked-date sentence, beside ceremony A (D-05)"
        status: pass
    human_judgment: true
    rationale: "The focus-move itself (churnDateRef.current.focus() on a blocked press) runs inside a useEffect and cannot be observed by this codebase's renderToStaticMarkup-only test style (no jsdom, no testing-library) — the markup and the underlying derivation are proven, but the actual keyboard-focus landing needs a browser pass."
  - id: D5
    description: "A malformed numeric stays in the field marked invalid with its contract sentence, and focus moves to the first invalid field; nothing is silently coerced"
    requirement: "UX1-03"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > validateRecordDraft — names the first invalid field in BATTERY_FIELDS order and carries its own verbatim contract sentence"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > marks an invalid field with aria-invalid and aria-describedby, and prints the contract's own error sentence verbatim (Task 2, D-05)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Correct reopens the record with the battery's values in the fields and saving stamps the changed date (D-03, D-04)"
    requirement: "BATCH2-01"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > draftFromBatch — Correct reopens everything the record holds (D-03, task 3)"
        status: pass
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > completeRecord without edits stamps changed and leaves recordedAt/snapshot untouched (task 3 acceptance)"
        status: pass
    human_judgment: false

# Metrics
duration: ~110min
completed: 2026-09-13
status: complete
---

# Phase 03.3.1 Plan 02: The record pen's churn half rebuilt to the battery — one save, two ceremonies, press-to-block Summary

**RecipePage's record pen rebuilt to the one-save model (churn + tasting draft, tastingOpen flag, derivePenState narrowed to plan/record/amend); BatchRow's churn section and read view rebuilt to the battery's fields with stay-in-place validation; PenFoot's SaveCeremony shared by both identical ceremonies (D-01); press-to-block on the churn date (D-05) built via genuine RED-then-GREEN TDD.**

## Performance

- **Duration:** ~110 min
- **Started:** 2026-09-13T11:55:00Z (approx, first Read)
- **Completed:** 2026-09-13T13:45:00Z (approx)
- **Tasks:** 3 (Task 1 tracer, Task 2 tdd — RED then GREEN, Task 3 auto)
- **Files modified:** 8

## Accomplishments
- `RecipePage.jsx`: the one record draft (`blankRecordDraft`) carries all twenty churn+tasting fields plus `tastingOpen`; `derivePenState` returns only `plan`/`record`/`amend`; `isDraftDirty` extends to every battery field (marks by own-key count, defects by length, `bitterDeclared` by boolean inequality, per the plan's own stated discipline).
- `draftFromBatch` (exported, pure): Correct's full pre-fill from a stored batch's own churn+tasting values, own-property reads only (T-03-10 adjacency rule) — verified against the seeded 2 Aug batch's exact D-07 values.
- `validateRecordDraft` (exported, pure): the record pen's one save gate — every `BATTERY_FIELDS` measurement validates first (RESEARCH.md Open Question 3), then the churn date (D-05); `CHURN_DATE_BLOCKED_MESSAGE`/`MEASURED_INVALID_STATUS` are the contract's own verbatim sentences, each written once.
- `handleSaveBatch`: the one save, branching on the record operation (`completeRecord` vs `createBatch`, never on which button), assembling a tasting object only when the section holds ink (D-02); a null tasting on `completeRecord` removes a stored one (RESEARCH.md Assumption A3 — see UAT note below).
- `BatchRow.jsx`: the churn pen section rebuilt to the battery (Churn date, Time to draw temp., Out of machine, Churn duration, Exit consistency, Airiness (estimated), At the machine, Ingredient notes) — every numeric field text-mode with `inputMode="decimal"`, never `type="number"`; `aria-invalid`/`aria-describedby`/`.field-error` wiring on a malformed field; the two segmented controls as native radiogroups with click-again-clears; the churn half of the read view in the battery's own words, absence read as "not measured"; the `form-status` live region as the record body's own last element.
- `PenFoot.jsx`: `SaveCeremony` extracted and exported — the one component both the foot (ceremony B) and BatchRow's end-of-record (ceremony A) mount, same "Save batch" label, Cancel first, never disabled.
- Retired: the tasting pen (`tastingDraft`, `handleStartTasting`/`handleCancelTasting`/`handleChangeTastingField`/`handleChangeTastingMark`/`handleUseAsExpectedShortcut`/`handleSaveTasting`), the tasting completeness gate and its hint, PenFoot's tasting branch, the old tasting ceremony block in BatchRow, the Amended read-view cell (its data source — `amendedAt[]` — retired in plan 01; the `changed` date's own read-view placement arrives in plan 05).
- Fixed a blocking build/test failure left over from plan 01's `axes.js` rewrite: `AxisMark.jsx` imported the retired `MARK_STOPS`; corrected to `STOPS` (Rule 3 auto-fix, not in this plan's own file list — see Decisions).

## Task Commits

Each task was committed atomically:

1. **Task 1: The one record pen — churn section, one save, ceremonies, read-back (D-01, D-02)** - `0941f93` (feat)
2. **Task 2: Press-to-block on the churn date and stay-in-place numeric validation (D-05, contract "Controls spec")** - RED `5e2bcd1` (test), GREEN `1193ee4` (feat)
3. **Task 3: Correct reopens everything the record holds (D-03), focus returns to the openers** - `3b014d1` (test)

**Plan metadata:** committed alongside this SUMMARY (see final commit in this plan's return message).

## Files Created/Modified
- `app/src/ui/RecipePage.jsx` - One record draft, derivePenState narrowed to three pens, draftFromBatch, validateRecordDraft, the one save handler
- `app/src/ui/RecipePage.test.jsx` - Pure-function coverage for every exported helper above, table-driven pen matrix
- `app/src/ui/BatchRow.jsx` - The churn pen section, the churn read view, ceremony A, field-error/form-status UI
- `app/src/ui/BatchRow.test.jsx` - Markup coverage for the rebuilt churn section, ceremony, validation UI, read view
- `app/src/ui/PenFoot.jsx` - SaveCeremony extracted and exported; the record/amend branch shares it
- `app/src/ui/PenFoot.test.jsx` - SaveCeremony's own coverage; the tasting branch's retirement confirmed
- `app/src/ui/AxisMark.jsx` - Blocking-import fix (MARK_STOPS -> STOPS), Rule 3 auto-fix
- `app/src/ui/AxisMark.test.jsx` - Fixture value corrected from a half-stop (4.5) to a whole stop (4)

## Decisions Made
- See `key-decisions` in the frontmatter for the AxisMark fix, the TDD self-correction, `validateRecordDraft`/`parseAllMeasuredFields` coexistence, the Ingredient notes textarea change, and the carried plan-comparison annotation.
- Field/store shape choices already settled by plan 01 (`timeToDrawTempMinutes`, `outOfMachineTempC`, etc.) were reused verbatim — no new naming decisions in this plan.
- Segmented control DOM: `role="radiogroup"` + native radio inputs restyled, per the contract's own "Don't Hand-Roll" guidance; click-again-clears implemented via `onClick` (not `onChange`) since a native radio's change event does not re-fire for a click on the already-checked option.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `AxisMark.jsx` imported the retired `MARK_STOPS` export**
- **Found during:** Task 1's first `npm test` run (full suite)
- **Issue:** Plan 01's `axes.js` rewrite retired `MARK_STOPS` (nine half-stops) in favor of `STOPS` (five whole stops), per its own documented objective ("the UI layer still reads the old shape until plan 02 lands"). `AxisMark.jsx` is not in this plan's `files_modified` list, but its broken import made the full suite fail — 3 tests in `AxisMark.test.jsx` — which this plan's own Task 1 `<verify>` requires to be green.
- **Fix:** Changed the import and its one usage from `MARK_STOPS` to `STOPS`; updated the header comment's stop-count claim; updated `AxisMark.test.jsx`'s own fixture value from a half-stop (4.5, no longer a valid `STOPS` member) to a whole stop (4). `AxisMark` itself is not consumed by any component in this plan (the tasting section that will render it is plan 03's build) — its own test fixture (`{ key, label, low, high }`) is unrelated to `domain/axes.js`'s real `AXES` shape (`name`, not `label`) and is left as-is, since wiring it to the real battery is out of this plan's scope.
- **Files modified:** `app/src/ui/AxisMark.jsx`, `app/src/ui/AxisMark.test.jsx`
- **Verification:** `npm --prefix app test` — full suite green.
- **Committed in:** `0941f93` (Task 1 commit)

**Total deviations:** 1 auto-fixed (Rule 3 — a blocking import from a retired export, left over from a prior plan's own documented sequencing).
**Impact on plan:** No scope creep; the fix is confined to the one broken import and the one now-invalid test fixture value.

## Issues Encountered
- **Self-corrected mid-execution, not a deviation from the plan's own rules but a correction to my own first pass:** Task 1's initial implementation over-built Task 2's own scope — the churn-date press-to-block state, the field-level `aria-invalid`/`aria-describedby`/`.field-error` UI, and the `form-status` region were all written into the Task 1 commit, even though the plan explicitly reserves them for Task 2 ("the full validation UI is Task 2 — here, storing the errors and aborting is enough") and marks Task 2 `tdd="true"`. Caught before finalizing: the Task 1 commit was reset and rebuilt to its own stated minimum (`parseAllMeasuredFields`: record per-field errors, abort — no date block, no field-level UI), then Task 2 was executed as a genuine RED-then-GREEN cycle (10 targeted test failures confirmed against the missing `validateRecordDraft`/`CHURN_DATE_BLOCKED_MESSAGE`/`MEASURED_INVALID_STATUS` and the not-yet-wired BatchRow markup, then implemented to green). No functional regression resulted — the corrected history simply produces the same final code through the plan's own required process.
- A fresh worktree checkout again had no `app/node_modules` (as plan 01's SUMMARY also noted) — symlinked to the main repo's `app/node_modules` rather than reinstalling, since `package.json`/`package-lock.json` are unchanged by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Two readings surfaced for end-of-phase UAT, per this plan's own `<output>` instruction:**

1. **RESEARCH.md Assumption A3 (D-02's literal reading, removal-on-save):** saving a Correct pen from which the tasting section was removed (`tastingOpen` collapsed back to `false`, or the section simply holds no ink) persists the removal — the stored tasting is gone, via `completeRecord`'s null-tasting argument. This is the D-02-consistent reading ("what a save persists is everything the record currently holds"), built and tested (`isDraftDirty` treats this as a real edit, "a removal is ink"), but the destructive-but-consistent outcome itself has not been confirmed with Mark. If he intends keep-on-removal instead, this is a one-function change plus a test flip, not a rebuild.
2. **RESEARCH.md Open Question 4 (Escape/Cancel on an open-but-empty tasting section):** since this plan builds no UI to open the tasting section at all (`tastingOpen` can only become `true` via Correct pre-filling an already-tasted batch), the "open but empty" scenario cannot yet be exercised through the app — only through direct calls to `isDraftDirty`. The underlying discipline is built and tested (an untouched `tastingOpen: true` draft compared against an identical baseline reads clean), ready for plan 03 to exercise through a real "Add tasting" control.

**Ready for plan 03 (opens the tasting section):**
- The record draft already carries every tasting field (`tastedDate`, `temperingMinutes`, `tastingTempC`, `marks`, `note`, `defects`, `bitterDeclared`, `meltTestG`, `meltStyle`) and the `tastingOpen` flag — plan 03 adds the UI (the Add tasting control, the tasting field-row, the axes grid, the defects checklist, the melt block) without touching the draft shape.
- `AxisMark.jsx` compiles again but is not wired to the real `AXES` shape (`name`/`low`/`high`/`group`) or the new five-stop `STOPS` semantics beyond the bare import fix — plan 03 should rebuild it against `domain/axes.js`'s real exports rather than assume the current header comment/props are final (its own test fixture still predates the battery).
- `validateRecordDraft` already walks all six `BATTERY_FIELDS` (three churn, three tasting) in order — plan 03/04 wiring the tasting numeric fields into the UI needs no change to the validation traversal, only to BatchRow's rendering.

**Not blocking, but worth plan 05's attention:**
- The later-batches meta line reads "tasted" (a boolean presence) rather than a richer phrase — this plan's own scope note said "its final wording lands in plan 05."
- The Amended read-view cell is gone; the `changed` date's own read-view placement is plan 05's build.

---
*Phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007*
*Completed: 2026-09-13*

## Self-Check: PASSED
