---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 03
type: execute
wave: 3
depends_on: ["03.3.1-02"]
files_modified:
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - app/src/ui/Segmented.jsx
  - app/src/ui/Segmented.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
autonomous: true
requirements: [UX1-01, UX1-03, OBS1-01]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Add tasting, beside the foot ceremony while the tasting section is absent, opens the section and lands focus on the Tasted date; the record opens with the entire Tasting section absent (hidden-until-added is the primary mode, D-01)."
    - "The tasting body reads in variant A's order — Tasted/Tempering/Tasting temperature field-row, the note before Texture & flavor, the defects row, the melt block at the foot (contract 'Tasting body')."
    - "Every axis is five whole goldilocks stops 1–5 with the contract's stop words as per-stop aria-labels, inline state beside the name, a per-axis Clear returning focus to the first stop, and click-again-clears; core and declared render side by side on desktop and stacked below 760px with the two DOM orders (contract 'Axes spec', 'Keyboard and tab order')."
    - "The defects are one standalone row: four comma-worded chips plus the declared Bitter chip with its declared-flaw helper, no defaults anywhere, click-again clears everything (contract 'Controls spec')."
    - "Marks live in page state keyed by axis key and survive the per-arrangement re-render; an unmarked axis is absent, never zero (D-10)."
  artifacts:
    - app/src/ui/Segmented.jsx
    - app/src/ui/AxisMark.jsx
    - app/src/ui/BatchRow.jsx
  key_links:
    - "Add tasting → tastingOpen true + focus on the Tasted date (contract 'Focus landings')"
    - "the axes grid's matchMedia listener on (max-width: 760px) → two DOM orders, marks carried by axis key"
    - "per-axis Clear → form-status announcement '{Axis name} cleared.' (wired through plan 02's status state)"
---

<objective>
The tasting section is added to the one record pen — hidden until added, variant A's order, the full texture block. Add tasting opens it; the field-row, the neutral note, the six goldilocks axes in their core/declared groups, the defects checklist with the declared toggle, and the melt block complete the battery inside the pen. The axes re-render per arrangement with marks surviving; nothing carries a default.

Purpose: the tasting half of the sheet — the part written a day later — becomes part of the same record and the same save (D-02, D-03).
Output: the tasting section's fields and note; the rebuilt AxisMark and the per-arrangement axes render; the Segmented control component; the defects chips and melt block.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery.md
@app/src/ui/BatchRow.jsx
@app/src/ui/AxisMark.jsx
@app/src/domain/axes.js
@app/src/domain/battery.js
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (the tasting half of the pen):

- `app/src/ui/Segmented.jsx` (new): the 3-way segmented control — `Segmented({ groupLabel, options, value, onChange })` — a native radiogroup, one tab stop, click-again clears, no default; used by Exit consistency, Airiness (estimated) and Melt style (plan 02 mounts the first two; this plan completes the component contract and adds the third).
- `app/src/ui/AxisMark.jsx` rebuilt: five numbered stops with per-stop aria-label "{n}: {word}", the anchors row (low, right, high, space-between, aria-hidden), inline state text "(Not recorded)" / "({n})", per-axis Clear with aria-label "Clear {name}", click-again-clears on the checked stop.
- BatchRow's tasting section: head (heading Tasting, helpers "· optional" and "— leave anything you did not record blank"), field-row (Tasted; Tempering min; Tasting temperature °C), note block ("How did it turn out?" eyebrow + textarea, placeholder verbatim), the axes grid with `useBelow760` (matchMedia on max-width 760px) driving the two DOM orders, the defects `role="group"` with five aria-pressed chips including the declared chip, and the melt block.
- RecipePage: `handleChangeRecordMark(axisKey, stop)`, `handleChangeSegment` already wired, `handleAddTasting` (opens the section, focuses the Tasted date), `handleChangeDefect` (toggle a chip), `handleToggleBitter` (the declared toggle).
- CSS classes this plan names (styled in plan 06): `axis-mark__state`, `axis-mark__anchors`, `axes-grid`, `axes-rule`, `axes-declared-caption`, `defects-row`, `chip-toggle`, `melt-block`, `tasting-head`, `tasting-field-row`, `note-block`.
</artifacts_this_phase_produces>

## Retirements (this plan removes)

`AxisMark.jsx`'s nine-stop rendering and its group aria-label "{label}, {low} to {high}"; the old anchors-flanking-stops layout (anchors become a separate row under the track). Nothing else retires here — this plan adds.

<tasks>

<task type="auto">
  <name>Task 1: Add tasting opens the section — head, field-row, and the note before Texture & flavor (D-03)</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/Segmented.jsx, app/src/ui/Segmented.test.jsx</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "DOM order inventory" (tasting head and body, variant A), "Settled defaults" (hidden-tasting primary mode), "Feedback and undo lifecycle" (adding writes no announcement)
- `app/src/ui/BatchRow.jsx` — the pen body from plan 02 (ceremony A placement, Next time, form-status)
- `app/src/ui/PenFoot.jsx` — SaveCeremony and the foot mount where Add tasting stands
- `app/src/domain/battery.js` — BATTERY_FIELDS's tasting-side entries (Tempering, Tasting temperature with its signed flag and sentence)
- `03.3.1-RESEARCH.md` — Pitfall 2 (Tempering is a tasting field — the contract wins over D-10's grouping), Pitfall 6 (hidden mode only)
  </read_first>
  <action>
Cite the contract's "DOM order inventory", "Settled defaults" and "Feedback and undo lifecycle", and D-01/D-02/D-03. (1) RecipePage gains `handleAddTasting`: sets `tastingOpen` true on the record draft, clears the status line (adding writes no announcement — the section opening and the focus landing are the evidence), and focuses the Tasted date input via a ref set beside ceremony B's Add tasting control. PenFoot's record/amend branch renders Add tasting beside the foot ceremony exactly while `tastingOpen` is false (D-01) — a text-styled control per the binder's control vocabulary, wired to `handleAddTasting`, hidden while the section is open. (2) BatchRow renders the tasting section when `tastingOpen`, after the churn section and before ceremony A (ceremony A moves below it per D-01 — "after the tasting section when it is open"). Section order per the contract's inventory and variant A: the Tasting head (heading word Tasting, then the two helper strings "· optional" and "— leave anything you did not record blank"); the field-row with Tasted (type=date), Tempering (numeric, unit min, unsigned), Tasting temperature (numeric, unit °C, signed — text-mode inputs with inputMode="decimal" and the per-field error wiring from plan 02); then the note block: the eyebrow "How did it turn out?" over a growing textarea with placeholder verbatim "e.g. flavor, texture, anything that stood out", dir="auto". (3) The Remove tasting control and the undo slots are plan 04's — do not build them here; the head renders its heading and helpers only in this plan. (4) Complete `app/src/ui/Segmented.jsx` (new file): a native radiogroup styled as the contract's segmented control — one accessible group name from `groupLabel`, options as the supplied strings, `checked` from value, `onChange(stop)` on selection, clicking the selected option again clears it (onChange(null)) — no default selection ever. Plan 02's Exit consistency and Airiness already exist; move their rendering onto this component (one home, three uses) and render Melt style with it in Task 3. (5) Ceremony A's position: while `tastingOpen`, BatchRow renders it after the tasting section — still just above the shared Next time (D-01). (6) Update BatchRow/RecipePage/Segmented tests: hidden-by-default section, Add tasting visible only while absent, focus lands on the Tasted date, the field-row's labels and units, variant A order (note before the texture block).
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx src/ui/Segmented.test.jsx</automated>
    <fails_when>non-zero exit, or any tasting-section or segmented describe fails</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'Tasting temperature\|Tempering' app/src/ui/BatchRow.jsx</automated>
    <fails_when>output is less than 2 — the tasting field-row's fields are absent</fails_when>
  </verify>
  <acceptance_criteria>
- A fresh record pen renders no tasting section at all — no heading, no helper, no rule (hidden-tasting primary mode).
- Add tasting renders beside the foot ceremony only while tastingOpen is false; activating it opens the section and moves focus to the Tasted date input.
- The tasting section's DOM order is field-row (Tasted, Tempering, Tasting temperature), then the note block, then the texture block, then the melt block; the note's eyebrow reads "How did it turn out?" and its placeholder is "e.g. flavor, texture, anything that stood out".
- Tempering renders in the tasting section, never in the churn section (contract "Where sources disagree (HTML wins)" § 1).
- Segmented.jsx is one component used for all three groups; clicking a selected option again clears it; nothing is pre-selected.
  </acceptance_criteria>
  <done>The tasting section exists as a hidden-until-added part of the one pen with variant A's head, field-row and note; Add tasting opens it and focuses the Tasted date.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The goldilocks axes — five stops, two arrangements, marks that survive (contract "Axes spec", "Keyboard and tab order")</name>
  <files>app/src/ui/AxisMark.jsx, app/src/ui/AxisMark.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx</files>
  <behavior>
- Each axis renders exactly five stops carrying digits 1–5; per-stop aria-label is "{n}: {word}" with the five words from stopWordsFor (Hardness: 1: soft, 2: leaning soft, 3: right, 4: leaning hard, 5: hard).
- Inline state beside the axis name: "(Not recorded)" unmarked, "(3)" marked — pen-faced when marked, muted italic when not.
- A marked axis shows its per-axis Clear (aria-label "Clear {name}"); activating it clears the mark, returns focus to the axis's first stop, and writes "{Axis name} cleared." to the form-status state; clicking the checked stop again also clears.
- The stops group carries aria-labelledby pointing at the axis name's element id; the anchors row shows low, right, high, aria-hidden.
- Desktop arrangement (above the 760px boundary): one row-major grid — Hardness, Scoopability, Body, Smoothness, Sweetness, Oil — with the vertical hairline drawn between columns 2 and 3 and the "Declared for this recipe" caption inside Body's box above its head.
- Stacked arrangement (at or below 760px): core-then-declared grouped containers — Hardness, Scoopability, Smoothness, Sweetness, then Body, Oil — the split a horizontal rule above the declared group.
- A mark set in one arrangement is still set after crossing the boundary (marks keyed by axis key in page state).
  </behavior>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "Axes spec" and "Keyboard and tab order" (the two DOM orders and the roving contract)
- `app/src/ui/AxisMark.jsx` — the recorded native-radio decision (lines 3-11): do not hand-roll a focus manager
- `app/src/domain/axes.js` — AXES, STOPS, stopWordsFor, CORE_AXIS_COUNT (from plan 01)
- `03.3.1-RESEARCH.md` — "The axes re-render per arrangement" and Code Example 6 (the matchMedia hook), Pitfall 5 and Pitfall 8
  </read_first>
  <action>
Cite the contract's "Axes spec" and "Keyboard and tab order" sections, and D-10. (1) Rebuild `app/src/ui/AxisMark.jsx` around native grouped radios exactly as the recorded decision prescribes — no keydown handler, no manual focus-index, no hand-rolled radiogroup. Per axis: the name element with an id, the inline state span after it, the per-axis Clear rendered only while marked, the five-stop group with aria-labelledby to the name's id, each stop a native radio labelled "{n}: {word}" via stopWordsFor, the anchors row (low, the fixed middle word, high) aria-hidden under the track, and an onClick on the checked radio calling onChange(null) (click-again-clears). Wire the per-axis Clear's announcement through plan 02's form-status announce helper passed down from the page. (2) In BatchRow, render the axes grid from `axesForBatch` with a `useBelow760` hook — one `window.matchMedia('(max-width: 760px)')` listener and state (RESEARCH.md Code Example 6; no matchMedia analog exists in the codebase — this is the first). Above the boundary render the row-major flat order; at or below it render the core group then the declared group under a horizontal rule (a real conditional re-render — one DOM order cannot serve both tab orders). Marks read from the record draft's marks map keyed by axis key, so they survive the re-render by construction (Pitfall 8's warning: the desktop tab order must be Hardness, Scoopability, Body, Smoothness, Sweetness, Oil). Draw the vertical hairline and the "Declared for this recipe" caption inside Body's box (desktop) — the caption moves with the stacked arrangement's rule as CSS in plan 06; the caption element renders in both arrangements. (3) Test via RED-GREEN-REFACTOR: the behaviors above as failing assertions first (static markup: five stops per axis, per-stop aria-labels, inline state text, Clear's aria-label and focus rule, both arrangements' axis orders, the caption's presence).
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/AxisMark.test.jsx src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or any axes-arrangement or stop-word assertion fails</fails_when>
  </verify>
  <verify>
    <automated>grep -c "matchMedia" app/src/ui/BatchRow.jsx</automated>
    <fails_when>output is 0 — the arrangement listener was not built</fails_when>
  </verify>
  <acceptance_criteria>
- AxisMark renders exactly five radio stops per axis with digits 1–5 and per-stop aria-labels built from stopWordsFor; the group's aria-labelledby resolves to the axis name's id.
- The inline state reads "(Not recorded)" when unmarked and "({n})" when marked; the Clear control exists only while marked and its activation clears plus refocuses the first stop.
- The two arrangements render the contract's two axis orders; the declared caption "Declared for this recipe" renders in both.
- Marks set before a boundary crossing remain set after it (state keyed by axis key).
- No keydown handler containing an arrow-key focus index exists in AxisMark.jsx (Pitfall 5).
  </acceptance_criteria>
  <done>The six axes render as five-stop goldilocks tracks with the contract's words, state, Clear and click-again-clears, in both arrangements, with marks surviving the re-render.</done>
</task>

<task type="auto">
  <name>Task 3: Defects checklist, the declared toggle, and the melt block (contract "Controls spec")</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "Controls spec" (the defects group, the chip wording with commas, the declared chip's helper and full aria-label) and "DOM order inventory" (the melt block's fields and units)
- `app/src/domain/battery.js` — DEFECTS, DECLARED_FLAW, SEGMENT_OPTIONS's melt-style group (from plan 01)
- `app/src/ui/BatchRow.jsx` — the tasting section from Task 1 (the texture block's position in variant A's order)
  </read_first>
  <action>
Cite the contract's "Controls spec" and "DOM order inventory", and D-10. (1) After the axes grid, render the defects group: a role="group" with aria-label "Any problems? Select all that apply", the caption "Any problems?" with the lowercase helper "select all that apply" beside it on one row, then five native toggle buttons with aria-pressed — the four chips from DEFECTS verbatim (comma wording per the contract's "Where sources disagree" § 2), then the declared chip labelled Bitter with the visible "· declared" helper suffix, its full aria-label "Declared for this recipe: Bitter", trailing the row after a wider gap (a spacing token from the declared-flaw margin, value landed in plan 06). Picked state is bold plus underline — never colour alone (UX1-03). Toggling writes through `handleChangeDefect` in RecipePage: a picked chip joins the draft's defects list, an unpicked one leaves it; blank stays absent in storage (plan 01's converter turns an empty list into null). The declared chip's toggle writes bitterDeclared true-or-null the same way. No chip carries a default; clicking a picked chip again clears it. (2) The melt block at the tasting body's foot: Melt test (optional) — text-mode numeric input, unit "g lost at 20 min", unsigned, with plan 02's error wiring; Melt style (optional) — the Segmented component with the contract's three options and group aria-label "Melt style (optional)". (3) Update the two test files: the group's aria-label, the five buttons' pressed semantics, the declared chip's helper and full aria-label, the melt block's labels and units, no defaults anywhere.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx</automated>
    <fails_when>non-zero exit, or the defects or melt describes fail</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'aria-pressed' app/src/ui/BatchRow.jsx</automated>
    <fails_when>output is less than 5 — the five toggle chips are absent</fails_when>
  </verify>
  <acceptance_criteria>
- The defects group carries aria-label "Any problems? Select all that apply" and five aria-pressed buttons; the four chip labels match DEFECTS character-for-character; the declared chip's visible label ends with the "· declared" helper and its aria-label is "Declared for this recipe: Bitter".
- No chip, option or stop carries a default selection on a fresh pen.
- The melt block renders Melt test (optional) with the "g lost at 20 min" unit and Melt style (optional) with the three contract options.
- A picked chip joins the draft's defects list; clearing every chip stores null, never an empty array (D-10's blank-is-absent).
  </acceptance_criteria>
  <done>The texture block's defects row and the melt block complete the tasting section; every control is blank-by-default with click-again-clears and spoken state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored/imported axis keys → marks map | Axis keys from the snapshot become mark keys on every mark write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1-07 | Tampering | marks writes (handleChangeRecordMark) | medium | mitigate | Every mark write goes through `setMark` (spread-and-delete, own properties only) — never a bare bracket write keyed by a stored axis key |
| T-03.3.1-08 | Repudiation | defects/declared toggle storage | low | mitigate | Chip state persists as known-string lists or true-or-null from plan 01's validator; nothing unpicked is stored |
| T-03.3.1-09 | Information disclosure | colour-only state | low | mitigate | Picked states carry bold-plus-underline affordances and explicit aria state, never colour alone (UX1-03) |
</threat_model>

<verification>
- `npm --prefix app test` passes; `npm --prefix app run build` compiles.
- The tasting section is hidden until added, opens to the Tasted date, and carries variant A's full body: field-row, note, axes grid, defects row, melt block.
- Both axis arrangements render their contract DOM orders with marks surviving the crossing.
</verification>

<success_criteria>
The tasting section is a hidden-until-added part of the one record pen, complete with the six goldilocks axes (core/declared split, two arrangements), the standalone defects checklist with the declared toggle, and the melt block — nothing defaulted, everything clearable, every state spoken.
</success_criteria>

<output>
Create `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-03-SUMMARY.md` when done.
</output>