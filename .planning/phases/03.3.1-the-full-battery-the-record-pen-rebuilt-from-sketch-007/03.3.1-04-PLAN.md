---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 04
type: execute
wave: 4
depends_on: ["03.3.1-03"]
files_modified:
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/PenFoot.jsx
  - app/src/ui/PenFoot.test.jsx
autonomous: true
requirements: [UX1-01, UX1-03]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The pen has two status channels exactly where the contract puts them — tasting-status beside the action in the tasting head, form-status at the foot — both role=status, aria-live=polite."
    - "Removing an empty tasting collapses the section, reads 'Tasting removed.' with no undo, and returns focus to Add tasting; removing a tasting with data reads 'Tasting removed. You can undo this.' and an undo exists (hidden mode only — the always-visible paths are never built, Pitfall 6)."
    - "Undo holds the removed tasting's values as React state, so it survives the axes re-render a breakpoint crossing triggers — the carried undo-retired-on-resize edge closes (RESEARCH.md Assumption A4)."
    - "Only an edit inside the tasting body retires a pending undo — never a churn-section edit or a Next-time edit; the Undo control lives in the tasting head slot while the section is visible and moves to the footer slot, trailing Add tasting, when it is absent."
    - "Undo restores the section with its values, re-fits the textareas, hides itself, focuses the Remove control, and reads 'Tasting restored.' to tasting-status; removal toasts self-clear after five seconds under the guarded timer."
  artifacts:
    - app/src/ui/RecipePage.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/PenFoot.jsx
  key_links:
    - "Remove tasting → the removal matrix's two hidden-mode paths → the undo payload state"
    - "undo → the restore sequence → focus on the Remove control → 'Tasting restored.' (tasting-status)"
    - "the guarded self-clear timer → a ref comparing the current message so a newer announcement is never wiped"
---

<objective>
The record pen's feedback and undo lifecycle, per the contract's "Feedback and undo lifecycle": the two live regions, the Remove tasting control and its two removal paths, the data-based undo that survives the axes re-render, the retirement scope, the real placement move, and the restore sequence.

Purpose: a tasting can be taken back safely — the reversal path the one-save record needs, with every announcement in the contract's words.
Output: the tasting head's live region and Remove control; the undo payload, placement and restore; the guarded self-clear on the removal toasts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@app/src/ui/RecipePage.jsx
@app/src/ui/BatchRow.jsx
@app/src/ui/PenFoot.jsx
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (the feedback and undo layer):

- RecipePage: state `pendingUndo` (the removed tasting's values, or null); `announce(message, { selfClear })` gains the tasting-status channel; handlers `handleRemoveTasting` (the two removal paths), `handleUndoRemove` (restore); `tastingStatus` state beside `formStatus`.
- BatchRow: the tasting head gains the tasting-status live region, the undo head slot, and the Remove tasting text control ("Remove tasting" — the hidden-mode label only); the pen's foot gains the undo footer slot trailing Add tasting.
- PenFoot: the undo footer slot rendering while a removal is pending and the section is absent.
- No new CSS classes beyond plan 03's list; `undo-control` text styling rides the existing text-control vocabulary.
</artifacts_this_phase_produces>

## Retirements (this plan removes)

Nothing pre-existing — the tasting head's undo/removal machinery is new (plan 03 deliberately deferred it). The contract's always-visible-mode strings and the visible-mode removal paths are NOT built (Pitfall 6): the two status strings belonging to the comparison mode and the visible-mode removal path are absent from the build, and the Clear tasting wording does not appear.

<tasks>

<task type="auto">
  <name>Task 1: The tasting head's live region and the two removal paths (contract "Feedback and undo lifecycle")</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "DOM order inventory" (tasting head: the status region, the undo head slot, the Remove control), "Feedback and undo lifecycle" (status strings verbatim, the removal path matrix, the retirement scope, the undo placement rule)
- `app/src/ui/BatchRow.jsx` — the tasting head from plan 03 (heading and helpers only today)
- `app/src/ui/RecipePage.jsx` — plan 02's announce helper and formStatus state (the guarded five-second self-clear)
- `03.3.1-RESEARCH.md` — "Feedback, undo, and live regions" and Pitfall 6
  </read_first>
  <action>
Cite the contract's "Feedback and undo lifecycle" and "DOM order inventory". (1) In the tasting head, render the tasting-status live region (role="status", aria-live="polite") beside the action, and the Remove tasting text control — the hidden-mode label only; the always-visible mode is a comparison device never built (Pitfall 6: the Clear tasting wording and the always-visible paths are absent). (2) RecipePage gains the two removal paths, exactly per the matrix's hidden-mode rows: a Remove press while the section holds no ink (no own-key mark, no picked chip, no declared toggle, no non-empty field) sets tastingOpen false, writes the empty-removal sentence verbatim to form-status with no undo, and moves focus to Add tasting (the opener's ref); a Remove press with data sets tastingOpen false, stores the tasting's values in a `pendingUndo` state (Task 2), writes the data-removal sentence verbatim to form-status, and moves focus to Add tasting. Both removal sentences carry the five-second self-clear through plan 02's guarded announce helper; focus lands per the contract's "Focus landings" (data removal → Add tasting). (3) Undo placement slots: the undo head slot element exists in the tasting head (rendered while the section is visible and a removal is pending — empty in practice while hidden, but the slot is the contract's static mount) and the footer slot renders in PenFoot's record branch trailing Add tasting while a removal is pending and the section is absent — a real DOM move between the two slots (the contract's "Undo placement rule"; the head slot stays in the markup so the restore path always finds a home). (4) Announcements: adding a tasting continues to clear the status line (plan 02's rule); removal sentences are exact; retirement scope lands in Task 2. (5) Update the two test files: the two hidden-mode paths' sentences, the collapse, the focus targets, undo's existence and placement per path.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx</automated>
    <fails_when>non-zero exit, or any removal-path describe fails</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'aria-live="polite"' app/src/ui/BatchRow.jsx app/src/ui/PenFoot.jsx | tr -d ' '</automated>
    <fails_when>either file's count is 0 — a status region is missing from its home</fails_when>
  </verify>
  <acceptance_criteria>
- The tasting head carries a role="status" region with aria-live="polite"; the form-status region sits at the foot above ceremony B.
- Remove tasting reads exactly "Remove tasting"; the always-visible mode's label and its two status strings appear nowhere under app/src.
- Empty removal: the section collapses, form-status reads the empty-removal sentence verbatim, no undo exists, focus lands on Add tasting.
- Data removal: the section collapses, form-status reads the data-removal sentence (with the undo clause) verbatim, an undo exists, focus lands on Add tasting.
- The status line is cleared when the tasting section is added (no stale removal toast beside a visible section).
  </acceptance_criteria>
  <done>The removal paths speak the contract's sentences to the right region, collapse the section, and land focus correctly; the undo slots exist in both homes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The undo — data-based, resize-proof, scoped retirement, restore (A4 closes the carried edge)</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/PenFoot.jsx, app/src/ui/PenFoot.test.jsx</files>
  <behavior>
- With a pending undo, the Undo control renders in the footer slot trailing Add tasting (hidden mode's only reachable home); activating it restores the tasting fields and selections exactly, reopens the section, hides the undo, focuses the Remove control, and reads the restore sentence to the tasting-status region.
- Editing any field inside the tasting body retires the pending undo — the tasted/tempering/temperature fields, the note, the marks, the chips, the declared toggle, and the per-axis Clear. Editing a churn-section field or the shared Next time never retires it.
- Crossing the 760px boundary (the axes re-render) never retires a pending undo — the payload is React state, not captured element references (A4; the carried undo-retired-on-resize edge closes).
- A removal toast self-clears after five seconds under the guarded timer; the undo control itself persists until retired or used.
  </behavior>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — sections "Feedback and undo lifecycle" (restore sequence, retirement scope, self-clear guard) and "Keyboard and tab order" (undo's focus landing)
- `app/src/ui/RecipePage.jsx` — the record draft and plan 02's announce helper (the guarded self-clear), plan 03's tasting-field handlers
- `03.3.1-RESEARCH.md` — "React realization of the carried edges" (React-state undo) and Assumption A4
  </read_first>
  <action>
Cite the contract's "Feedback and undo lifecycle" section, and A4. (1) `pendingUndo` holds the removed tasting's values as a plain object copy (the draft's tasting-side fields: marks, defects list, bitterDeclared, the dates, numerics and note) — never captured DOM references, so the payload survives the per-arrangement axes re-render (A4 closes the contract's "Deferred to the phase plan" edge). (2) `handleUndoRemove`: sets tastingOpen true, writes the payload back onto the record draft field-by-field, clears pendingUndo, focuses the Remove control, and announces the restore sentence verbatim to the tasting-status channel. (3) Retirement: every tasting-body edit handler (the tasting field-row's setters, the note setter, the mark setter, the chip and declared toggles, and the per-axis Clear) clears pendingUndo first; the churn-section setters and the shared Next time setter never touch it. The plan 03 axes re-render itself retires nothing — the payload is data. (4) The Undo control is a plain text control labelled per the contract's undo slot wording; it renders in the head slot while the section is visible, and in the footer slot trailing Add tasting while the section is absent (the DOM move from Task 1). (5) RED-GREEN-REFACTOR: the behaviors above as failing tests first — the retirement scoping (a churn edit does not retire, a tasting edit does), the restore sequence's field restoration, the resize survival (simulate the arrangement state change and assert the undo persists), and the restore sentence's target.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/RecipePage.test.jsx src/ui/BatchRow.test.jsx src/ui/PenFoot.test.jsx</automated>
    <fails_when>non-zero exit, or any undo/retirement/restore describe fails</fails_when>
  </verify>
  <verify>
    <automated>grep -c "pendingUndo" app/src/ui/RecipePage.jsx</automated>
    <fails_when>output is 0 — the undo payload state was not built</fails_when>
  </verify>
  <acceptance_criteria>
- Undo restores every tasting field and selection; after restore the Remove control holds focus and the tasting-status region reads the restore sentence.
- A churn-section edit leaves a pending undo intact; an edit to any tasting-body control retires it.
- A 760px boundary crossing leaves a pending undo intact.
- The undo control renders in the footer slot trailing Add tasting while the section is absent, and never in both slots at once.
- The removal toast's self-clear never wipes a newer message (the guarded timer compares the current text).
  </acceptance_criteria>
  <done>The tasting's reversal path is complete: removal speaks, undo restores exactly and survives re-renders, retirement is scoped to the tasting body, and every message lands in the contract's channel with the contract's words.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| undo payload state → draft | A stored payload is written back onto the live record draft on restore |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1-10 | Tampering | handleUndoRestore's write-back | low | mitigate | The payload is the pen's own draft copy (never a stored record's raw object); the write-back assigns named fields individually, never a computed key |
| T-03.3.1-11 | Repudiation | status announcements | low | mitigate | Every reversal speaks in the contract's verbatim words to the region the contract names; nothing self-evident is announced (adding writes none) |
</threat_model>

<verification>
- `npm --prefix app test` passes; `npm --prefix app run build` compiles.
- Both removal paths match the hidden-mode matrix rows; the undo restores and survives the arrangement crossing.
- The always-visible mode's paths and strings are absent from the build.
</verification>

<success_criteria>
Feedback and undo match the contract's lifecycle section: two live regions in their homes, the hidden-mode removal matrix, a data-based undo that survives the axes re-render, scoped retirement, and the full restore sequence.
</success_criteria>

<output>
Create `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-04-SUMMARY.md` when done.
</output>