---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 04
subsystem: ui
tags: [react, forms, accessibility, vitest, undo, live-regions]

# Dependency graph
requires:
  - phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
    provides: "the record pen's tasting section, axes, defects and melt block (plan 03), with tastingOpen the one flag Remove tasting toggles closed"
provides:
  - "BatchRow's tasting head: the tasting-status live region (role=status, aria-live=polite) beside the action, the Remove tasting text control (hidden-mode label only, Pitfall 6), and the undo head slot's static mount"
  - "The two hidden-mode removal paths (RecipePage.handleRemoveTasting): empty removal collapses with no undo; data removal captures the tasting's own values (tastingPayloadFromDraft) and keeps an undo pending — both self-clear their form-status toast after five seconds and move focus to Add tasting"
  - "The restore sequence (RecipePage.handleUndoRemove + restoreDraftFromUndo): reopens the section, writes the payload back field-by-field, clears pendingUndo, focuses Remove, and announces \"Tasting restored.\" to tasting-status"
  - "The retirement scope (isTastingBodyField + unconditional retirement in the marks/defects/declared-toggle/per-axis-Clear handlers): any tasting-body edit retires a pending undo; a churn-section edit or the shared Next time never does"
  - "PenFoot's undo footer slot, trailing Add tasting, while a removal is pending and the section is absent — the real DOM move whose other home is BatchRow's tasting head"
  - "announce()'s target param, so RecipePage can write either live region (form-status or tasting-status) under the same guarded five-second self-clear"
affects: [03.3.1-05, 03.3.1-06]

# Actuals (#2632)
actuals:
  tokens: 9870
  tasks: 2
  commits: 3
  plan_head_before: 370c644a51ec324674db9e77c8637dd443e84b8a

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "The undo payload (tastingPayloadFromDraft) is a plain-object copy of the tasting-side fields, never a captured DOM reference — proven structuredClone-safe in a dedicated test — so it survives the axes' own per-arrangement re-render by construction (RESEARCH.md A4). This is the reference pattern for any future undo/restore payload in this codebase: hold the data in React state, never an element ref."
    - "isTastingBodyField(field): a single exported Set-membership predicate is the one place the retirement scope's boundary lives, consulted by both generic setters (handleChangeRecordField, handleChangeSegment) that straddle the churn/tasting line — the dedicated tasting-only handlers (marks, per-axis Clear, defects, the declared toggle) retire unconditionally and need no entry in it."
    - "announce(message, { selfClear, target }): one guarded self-clear implementation serves two independent live regions (form-status, tasting-status), each against its own timer ref — extending a single-channel helper to a second channel by parameterizing the target rather than duplicating the guard logic."

key-files:
  created: []
  modified:
    - app/src/ui/RecipePage.jsx
    - app/src/ui/RecipePage.test.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/BatchRow.test.jsx
    - app/src/ui/PenFoot.jsx
    - app/src/ui/PenFoot.test.jsx

key-decisions:
  - "The vanilla-sketch 'static mount so init never finds it missing' concern for the two undo slots does not apply in React: conditional JSX rendering replaces the DOM-node-hunting risk the sketch's own querySelector-based script carried. The head slot still exists as real markup (rendered while draft.tastingOpen is true and pendingUndo is set) and is exercised by the one real scenario that produces it — pressing Add tasting again while an earlier data removal's undo is still pending, which reopens the section without retiring the undo (opening is not a tasting-body edit, so the retirement scope never touches it)."
  - "Task boundary: Task 1 built the removal paths, the pendingUndo *state*, both live regions, the Remove control, and PenFoot's removeTastingAttempt focus-effect on Add tasting — but not the Undo control's own rendering, restore handler, or retirement scoping, so that Task 2's TDD tests (isTastingBodyField, restoreDraftFromUndo, PenFoot's footer Undo) were genuinely RED against Task 1's own commit, not already-passing scaffolding."
  - "PenFoot.jsx's file list entry in Task 1 turned out to be exactly the removeTastingAttempt/addTastingRef focus effect — not the footer Undo control itself (that arrived in Task 2 alongside PenFoot.test.jsx, the file Task 1's own list omitted). Confirmed by re-reading which files carry test coverage in which task before writing any code, rather than assuming both tasks touch the same file identically."
  - "blankRecordDraft() was refactored to spread a new private blankTastingFields() helper, the same shape handleRemoveTasting's own blanking effect needed — avoiding a second, drifting copy of the tasting field's blank-shape list in the same file (Simplicity First; no behavior change, confirmed by the full suite staying green before any other edit)."
  - "The restore toast (\"Tasting restored.\") is given selfClear: true, matching the general phrase in the contract's own \"Feedback and undo lifecycle\" section (\"the removal/undo toasts self-clear after five seconds\") which groups removal and undo/restore toasts together, distinct from the per-axis Clear and validation announcements the same section explicitly calls out as persistent."

patterns-established:
  - "isTastingBodyField (RecipePage.jsx, exported): the reference pattern for scoping a cross-cutting side effect (here, undo retirement) across a shared generic setter that straddles two semantic regions of one draft — a membership predicate over field keys, not a per-call-site special case."

requirements-completed: [UX1-01, UX1-03]

coverage:
  - id: D1
    description: "The tasting head carries both live regions in their contract homes — tasting-status beside the action, form-status at the foot — and the Remove tasting control reads exactly \"Remove tasting\", with the always-visible mode's label and status strings absent from app/src (Pitfall 6)"
    requirement: "UX1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the tasting-status channel and the Remove/undo head slot (contract \"Feedback and undo lifecycle\", \"DOM order inventory\")"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the tasting section, hidden until added > renders the Remove tasting control, verbatim — the hidden-mode label only"
        status: pass
    human_judgment: false
  - id: D2
    description: "Both hidden-mode removal paths speak the contract's verbatim sentences: empty removal collapses with no undo, data removal collapses with an undo pending, both self-clearing after five seconds and moving focus to Add tasting"
    requirement: "UX1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > TASTING_REMOVED_EMPTY_STATUS / TASTING_REMOVED_DATA_STATUS — the contract's own verbatim removal sentences"
        status: pass
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > tastingPayloadFromDraft — the undo payload, a plain-object copy (Task 1, RESEARCH.md A4)"
        status: pass
    human_judgment: true
    rationale: "handleRemoveTasting's own focus-move to Add tasting runs inside PenFoot's useEffect on removeTastingAttempt and cannot be observed by this codebase's renderToStaticMarkup-only test style (no jsdom, no testing-library) — the sentence text, the payload shape, and the attempt-counter wiring are unit-proven; the actual keyboard-focus landing needs a browser pass (matching 03.3.1-02/03-SUMMARY's own precedent for churn-date/axis-Clear focus effects)."
  - id: D3
    description: "The undo is data-based (never a captured DOM reference) and survives the axes' own per-arrangement re-render; restoring writes every tasting field back exactly, reopens the section, focuses Remove, and announces \"Tasting restored.\" to tasting-status"
    requirement: "UX1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > tastingPayloadFromDraft — the resize-survival guarantee (Task 2, RESEARCH.md A4)"
        status: pass
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > restoreDraftFromUndo — the restore sequence's field write-back (Task 2, contract \"Feedback and undo lifecycle\", threat T-03.3.1-10)"
        status: pass
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > TASTING_RESTORED_STATUS — the contract's own verbatim restore sentence (Task 2)"
        status: pass
    human_judgment: true
    rationale: "The literal 760px breakpoint crossing in a real browser, and handleUndoRemove's own focus-move to the Remove control (a BatchRow useEffect), are both DOM/viewport behaviors this codebase's static-markup tests cannot execute — the payload's structuredClone-safety (the actual mechanism A4 relies on) and the restore assembly's correctness are unit-proven; the live crossing needs a browser pass (RESEARCH.md's own matchMedia-under-emulation caveat, carried from 03.3.1-03-SUMMARY)."
  - id: D4
    description: "Only a tasting-body edit retires a pending undo (the tasted/tempering/temperature fields, the note, the marks, the chips, the declared toggle, and the per-axis Clear); a churn-section edit or the shared Next time never does"
    requirement: "UX1-03"
    verification:
      - kind: unit
        ref: "app/src/ui/RecipePage.test.jsx > isTastingBodyField — the retirement scope's own boundary (Task 2, contract \"Feedback and undo lifecycle\")"
        status: pass
    human_judgment: false
  - id: D5
    description: "The undo control renders in the footer slot trailing Add tasting while a removal is pending and the section is absent, and never in both slots at once"
    requirement: "UX1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/PenFoot.test.jsx > PenFoot — the undo footer slot, trailing Add tasting while a removal is pending and the section is absent (Task 2, contract \"Feedback and undo lifecycle\", \"Undo placement rule\")"
        status: pass
    human_judgment: false

# Metrics
duration: ~55min
completed: 2026-09-13
status: complete
---

# Phase 03.3.1 Plan 04: The record pen's feedback and undo lifecycle Summary

**The tasting section gains a full reversal path — two live regions in their contract homes, the two hidden-mode removal sentences, a data-based undo that survives the axes' own arrangement re-render, retirement scoped to the tasting body, and a restore sequence that writes every field back exactly.**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-09-13T16:15:00Z (approx, first Read)
- **Completed:** 2026-09-13T17:09:00Z
- **Tasks:** 2 (Task 1 auto, Task 2 tdd — RED then GREEN)
- **Files modified:** 6 (0 created, 6 modified)

## Accomplishments
- `BatchRow.jsx`: the tasting head now carries the tasting-status live region (`role="status"`, `aria-live="polite"`) beside the action, the Remove tasting text control (the hidden-mode label only — Pitfall 6's "Clear tasting" and the always-visible mode's two status strings are absent from the build), and the undo head slot's static mount, plus the restore sequence's own focus-return ref to the Remove control.
- `RecipePage.jsx`: `pendingUndo` and `tastingStatus` state; `tastingPayloadFromDraft` (the undo payload, a plain-object copy proven `structuredClone`-safe); `handleRemoveTasting` (the two hidden-mode removal paths, per the removal path matrix); `restoreDraftFromUndo` and `handleUndoRemove` (the restore sequence); `isTastingBodyField` (the retirement scope's own boundary) wired into every tasting-body edit handler; `announce()` extended with a `target` param so it can write either live region under the same guarded five-second self-clear.
- `PenFoot.jsx`: the undo footer slot, trailing Add tasting, rendered exactly while a removal is pending and the section is absent — the real DOM move whose other home is BatchRow's tasting head; the `removeTastingAttempt`-driven focus effect on Add tasting.

## Task Commits

Each task was committed atomically:

1. **Task 1: The tasting head's live region and the two removal paths (contract "Feedback and undo lifecycle")** - `a53e7bc` (feat)
2. **Task 2: The undo — data-based, resize-proof, scoped retirement, restore (A4 closes the carried edge)** - RED `747342b` (test), GREEN `faabe31` (feat)

**Plan metadata:** committed alongside this SUMMARY (see final commit in this plan's return message).

## Files Created/Modified
- `app/src/ui/RecipePage.jsx` - pendingUndo/tastingStatus state, tastingPayloadFromDraft, handleRemoveTasting, restoreDraftFromUndo, handleUndoRemove, isTastingBodyField, announce()'s target param, retirement wiring
- `app/src/ui/RecipePage.test.jsx` - coverage for every new pure helper and constant above
- `app/src/ui/BatchRow.jsx` - the tasting-status region, the Remove tasting control, the undo head slot, the restore focus-return ref
- `app/src/ui/BatchRow.test.jsx` - coverage for the tasting-status region, the Remove control, and the head-slot Undo
- `app/src/ui/PenFoot.jsx` - the removeTastingAttempt focus effect on Add tasting, the undo footer slot
- `app/src/ui/PenFoot.test.jsx` - coverage for the undo footer slot's presence, absence, and placement

## Decisions Made
- See `key-decisions` in the frontmatter for the React-vs-vanilla-sketch static-mount reasoning, the Task 1/Task 2 boundary (which file changes belong to which task, confirmed by which task owns which `.test.jsx`), the `blankRecordDraft`/`blankTastingFields` refactor, and the restore toast's self-clear.
- Symbol names not already settled by plans 01–03 (`tastingPayloadFromDraft`, `restoreDraftFromUndo`, `isTastingBodyField`, `handleRemoveTasting`, `handleUndoRemove`, `TASTING_REMOVED_EMPTY_STATUS`, `TASTING_REMOVED_DATA_STATUS`, `TASTING_RESTORED_STATUS`) are this plan's own naming decisions, chosen to mirror the existing `CHURN_DATE_BLOCKED_MESSAGE`/`MEASURED_INVALID_STATUS`/`draftFromBatch` conventions already in the file.

## Deviations from Plan

None — plan executed exactly as written. The task-boundary interpretation questions noted under Decisions Made (which file changes belong to Task 1 vs. Task 2) were resolved by re-reading each task's own `<files>` list before writing code, not by deviating from either task's stated scope.

## Issues Encountered
- A fresh worktree checkout had no `app/node_modules` (the same condition plans 01–03 each noted) — symlinked to the main repo's `app/node_modules` rather than reinstalling, since `package.json`/`package-lock.json` are unchanged by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Surfaced for end-of-phase UAT** (per this plan's own read of the contract and RESEARCH.md's carried caveats):

1. **The matchMedia-under-emulation caveat (RESEARCH.md, carried through 03.3.1-03):** the resize-survival guarantee is proven at the payload level (`tastingPayloadFromDraft`'s `structuredClone`-safety, the actual mechanism A4 relies on) but the literal 760px crossing in a real browser, with a pending undo, still needs verification at real window widths.
2. **Focus landings not observable by this test style:** Remove tasting → Add tasting (both removal paths), and Undo → the Remove control, both run through `useEffect`/attempt-counter refs this codebase's `renderToStaticMarkup`-only tests cannot see. The attempt-counter wiring and the ref targets are unit-proven; the actual keyboard landing needs a browser pass (matching 03.3.1-02/03-SUMMARY's own precedent).
3. **The reopen-with-pending-undo scenario** (Add tasting pressed again after a data removal, before the undo is used) is the one real path that renders the tasting head's own Undo control — confirmed structurally in `BatchRow.test.jsx`, but the visual transition (footer Undo disappearing, head Undo appearing) is worth a browser look.

**Not blocking, but worth later plans' attention:**
- CSS classes this plan names for plan 06 to style: `tasting-status`, `undo-control` (riding the existing `.text-control` vocabulary, per this plan's own artifacts note — no new CSS classes beyond plan 03's list) — present in the DOM; no visual styling yet (tokens-only convention, deferred to plan 06).
- The always-visible tasting mode (its own two status strings, its own removal path, "Clear tasting") remains entirely unbuilt per Pitfall 6 — a deliberate, permanent exclusion, not a deferral.

---
*Phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007*
*Completed: 2026-09-13*

## Self-Check: PASSED
