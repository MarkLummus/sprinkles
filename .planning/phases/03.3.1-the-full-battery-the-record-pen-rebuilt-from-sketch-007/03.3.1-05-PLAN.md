---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 05
type: execute
wave: 5
depends_on: ["03.3.1-04"]
files_modified:
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/domain/batch.js
  - app/src/domain/batch.test.js
autonomous: true
requirements: [BATCH1-02, OBS1-01, UX1-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A saved tasting reads as the battery: only the marked axes, each as its goldilocks word with the stop number — Soft (2) style — the defects as their named chips with the declared flaw carrying its declared helper, and the melt block; no aggregate, average or overall figure is ever derived."
    - "The record's dates read churned, tasted, changed — and only the latest change shows; an absent tasted date reads date unknown; nothing is invented by the app."
    - "The batch list's small print reads changed {date} (was the amendment wording); no UI string can read 'twice' — a batch is tasted zero or one times (D-03, D-04)."
    - "Every field the maker left blank reads as its absence in words (BATCH1-02); the read view reads only the snapshot, never a live version (Pitfall 11)."
  artifacts:
    - app/src/ui/BatchRow.jsx
    - app/src/domain/batch.js
  key_links:
    - "read view → batch.tasting (the single object) → the AXES table's words for the marks"
    - "summary line → marked axes' words + the declared flaw's word — derived from stored marks only, never an aggregate"
    - "formatRecordDate → churned / tasted / changed everywhere (no Date, no locale)"
---

<objective>
The batch row's read view reads the full battery. The tasting reads as goldilocks marks in words with the tasting-head summary line; the defects read with their declared helper; the record's dates read churned, tasted, changed — only the latest change shows; the batch list's small print carries the changed date; and every absence reads in words.

Purpose: the record a maker reopens weeks later reads the same battery they wrote on, in plain words (D-04; the brief's § 3 read view).
Output: the tasting read view, the dates vocabulary, the summary line, and the retired plural/amendment wording.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@.impeccable/surfaces/route-recipe-batch.md
@app/src/ui/BatchRow.jsx
@app/src/domain/batch.js
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (the read view):

- `app/src/domain/batch.js`: `readMarkWords(axis, marks)` — no; the mark words derive from AXES's anchors: a new export `readMarkWord(axis, stop)` returning the low anchor for stops 1–2, the fixed middle word for stop 3, the high anchor for stops 4–5 (the brief's "Soft (2)" rule; flagged for Mark's eyes at UAT per RESEARCH.md Assumption A2).
- BatchRow's read view: the tasting head with the summary line ("{word} ({n})" per marked axis joined with " · ", plus the declared flaw's word when declared); the tasting's cells (Tasting temperature signed °C, Melt test g lost at 20 min, Melt style's picked words, the note as prose); the dates line churned / tasted / changed; the batch list's "changed {date}" small print.
- No new component files; `TastingReading` rewritten to the single tasting.
</artifacts_this_phase_produces>

## Retirements (this plan removes)

`TastingReading`'s old cell set (raw mark numbers, the melted-loss label); the plural-tasting map over `sortedTastings` remnants and the once/twice/N-times wording (Pitfall 7 — none of it may read "twice"); the per-tasting Next time prose (the note is shared on the churn); `hasTasting` reads in favour of the single tasting object's presence; the old tasting head's meta line format.

<tasks>

<task type="auto">
  <name>Task 1: The tasting read view — goldilocks words, defects read, summary line (brief § 3)</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/domain/batch.js, app/src/domain/batch.test.js, app/src/domain/axes.js, app/src/domain/axes.test.js</files>
  <read_first>
- `.impeccable/surfaces/route-recipe-batch.md` — § 3 "The record reads as the battery (read view)" and the summary line's recorded example
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — section "Axes spec" (the five stop words) and "Deferred to the phase plan" (the summary line belongs to the read view)
- `app/src/ui/BatchRow.jsx` — TastingReading's marked-axes-only rendering (lines 23-61) and the read-view cell pattern (383-439)
- `app/src/domain/axes.js` — AXES and stopWordsFor (from plan 01)
- `03.3.1-RESEARCH.md` — Assumption A2 and Open Question 2 (the read-view wording)
  </read_first>
  <action>
Cite the brief's § 3 read view and the contract's "Axes spec", and D-04. (1) Add `readMarkWord(axis, stop)` to `app/src/domain/axes.js` (pure): stop 1 or 2 returns the axis's low anchor, stop 3 the fixed middle word, stop 4 or 5 the high anchor — so a hardness mark at 2 reads Soft and the summary example "Soft (2) · grainy · bitter" reproduces exactly; the contract's full five stop words stay on the pen's stops (this rule is the read view's own wording; surface it to Mark at end-of-phase UAT per A2). (2) Rewrite the tasting read view onto the single tasting object, keeping the marked-axes-only principle (an unmarked axis is dropped entirely — never a blank judgment; no aggregate is ever derived): each marked axis as a cell labelled with the axis name and valued "{word} ({n})" via readMarkWord; the tasting's temperature with its signed unit, melt test with "g lost at 20 min", melt style as its picked words, the note as prose; defects read as a line of the picked chips' words with the declared flaw carrying "· declared" when bitterDeclared; every blank field reads its absence in words per the Saying-absence rule (the row's "not measured" wording for cells). (3) The tasting head carries the summary line: the marked axes' readMarkWord-with-number tokens joined by " · ", then the declared flaw's word (bitter) when declared — "Soft (2) · grainy · bitter" is the recorded shape; the line renders only when at least one token exists, and sits in the tasting head beside the date words. The tasting's date words read the tasted date or "date unknown" (never invented). (4) A batch with no tasting keeps its absence in words as today. (5) Update the domain test for readMarkWord's mapping and the component tests for the summary line, the defects line, and the absence words.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/domain/batch.test.js src/domain/axes.test.js src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or the read-view or readMarkWord describes fail</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'tasted once\|tasted twice\|times' app/src/ui/BatchRow.jsx | tr -d ' '</automated>
    <fails_when>output is not 0 — plural-tasting wording survives (Pitfall 7)</fails_when>
  </verify>
  <acceptance_criteria>
- `readMarkWord` maps stop 2 of Hardness to Soft, stop 3 to the fixed middle word, stop 4 to the high anchor; the read view's marked-axis cell reads "{word} ({n})".
- The seeded batch's read view shows sweetness "less (4)" and oil "strong (4)"-shaped tokens per the rule, with Bitter carrying the declared helper in the summary line (the working case marks 4 on both — high anchors).
- Only marked axes render; no cell, average or overall figure is derived from the marks.
- The summary line joins the marked axes' tokens and the declared flaw with " · " separators; it renders nothing when nothing is marked and nothing declared.
- Every blank tasting field reads its absence in words.
  </acceptance_criteria>
  <done>The tasting reads as the battery: goldilocks words with numbers, the summary line, the defects with their declared helper, and absence in words — from the single stored tasting only.</done>
</task>

<task type="auto">
  <name>Task 2: The record's dates — churned, tasted, changed; the list's small print (D-04)</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — section "Settled defaults" (dates vocabulary context) and "How to cite this contract" (supersession note)
- `app/src/ui/BatchRow.jsx` — the head line (253-270), the read cells, the later-batches list (487-530)
- `app/src/domain/batch.js` — formatRecordDate (the fixed month table; all new date rendering goes through it)
- `03.3.1-CONTEXT.md` — D-04 (the dates read churned, tasted, changed; only the latest change shows)
  </read_first>
  <action>
Cite D-04 and the contract's "Settled defaults" section. (1) The record's dates read churned, tasted, changed, and only the latest change shows: the batch row's head line keeps its churned date; the read view states the tasted date (the single tasting's, or "date unknown" when absent) and the changed date (the stored changed value — the latest change by construction, since every save after the first replaces it). Render them as dates cells in the read view's own vocabulary (planner's placement discretion: one dates line or cells beside the measured cells — either satisfies the vocabulary; state by weight, never position). The recordedAt line keeps its existing "recorded {date} against {label}" wording (the created date's home). (2) The later-batches list's meta small print: replace the tasted-count wording with "changed {date}" when the batch carries a changed date (formatRecordDate; D-04's recorded example "changed 5 Aug 2026"); keep the drawn-temperature and At-the-machine meta parts reading the new stored field names (plan 02's half); a batch with neither prints no small print. (3) The dates never invent values: a batch with no tasting reads its tasted absence in words; an unsaved record never carries dates it was not given. (4) Update BatchRow.test.jsx for the dates line, the changed small print, and the absence wordings.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or the dates describes fail</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'Amended\|amendedAt' app/src/ui/BatchRow.jsx app/src/domain/batch.js | tr -d ' '</automated>
    <fails_when>either count is not 0 — the amendment vocabulary survived the changed date</fails_when>
  </verify>
  <acceptance_criteria>
- The read view states the record's dates with the words churned, tasted and changed; only one changed date ever renders.
- An absent tasted date reads "date unknown"; the app never supplies a date.
- The later-batches small print reads "changed {D Mon YYYY}" for a batch carrying one, and the once/twice wording exists nowhere.
- Every date string is produced through formatRecordDate.
  </acceptance_criteria>
  <done>The record's dates read churned, tasted and changed with only the latest change shown, and the batch list's small print carries the changed date — the amendment list and its wording are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored marks/defects → read view | Stored values render as text in the reading state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1-12 | Tampering | read-view rendering of stored prose and marks | low | accept | All values render as text through React's escaping — the project's no-dangerous-markup convention; the read view reads only the snapshot, never a live version (Pitfall 11) |
| T-03.3.1-13 | Repudiation | absence wording | low | mitigate | A blank reads "unknown"/"date unknown"/"not measured" in words — silence is never presented as a value (BATCH1-02) |
</threat_model>

<verification>
- `npm --prefix app test` passes; `npm --prefix app run build` compiles.
- The seeded batch's read view reproduces the battery's read: goldilocks words, summary line with the declared helper, dates churned/tasted/changed, absence in words, changed small print.
</verification>

<success_criteria>
The record reads as the battery everywhere: marked axes as goldilocks words with numbers, defects with their declared helper, the tasting-head summary line, the churned/tasted/changed dates with only the latest change, and every absence in words.
</success_criteria>

<output>
Create `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-05-SUMMARY.md` when done — flag the readMarkWord rule for Mark's UAT eyes (A2).
</output>