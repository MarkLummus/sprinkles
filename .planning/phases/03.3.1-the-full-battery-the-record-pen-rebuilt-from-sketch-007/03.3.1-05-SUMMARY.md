---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 05
subsystem: ui
tags: [react, vitest, domain, read-view]

# Dependency graph
requires:
  - phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
    provides: "the record pen's tasting section, axes, defects, melt block and feedback/undo lifecycle (plans 01-04), with the single stored tasting object and the battery's fixed AXES table"
provides:
  - "readMarkWord(axis, stop) in domain/axes.js: the read view's own goldilocks-word rule — the low anchor for stops 1-2, the fixed middle word \"right\" for stop 3, the high anchor for stops 4-5"
  - "BatchRow's TastingReading component: the read view's rendering of the single stored tasting — marked-axes-only cells, the tasting-head summary line, measured cells, the defects line with the declared flaw's helper, and the note"
  - "laterBatchMetaFor(batch), exported from BatchRow.jsx: the later-batches list's meta small print, now reading \"changed {date}\" in place of the retired tasted-count wording"
  - "A Changed cell in the read view (D-04): only the latest change ever shows"
affects: [03.3.1-06]

# Actuals (#2632)
actuals:
  tokens: 5728
  tasks: 2
  commits: 2
  plan_head_before: f1fb64928794d828c72dbde0ccc8e054c5ff7460

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TastingReading (BatchRow.jsx, local component): the read view's own rendering of a domain object, mirroring the pen's own component-per-concern style — computes its marked-axes list, summary tokens, and defect words once at the top of the function body rather than inline in JSX, keeping the JSX itself a flat, readable list of cells."
    - "laterBatchMetaFor(batch), exported for direct testing: this file's second instance (after AxesGrid) of pulling a piece of otherwise-unreachable render logic out to a plain exported function specifically because the enclosing disclosure has no prop a render-only test can use to force it open."

key-files:
  created: []
  modified:
    - app/src/domain/axes.js
    - app/src/domain/axes.test.js
    - app/src/ui/BatchRow.jsx
    - app/src/ui/BatchRow.test.jsx

key-decisions:
  - "readMarkWord returns the axis's stored anchor words exactly as written in AXES (lowercase, e.g. \"more\", \"strong\", \"soft\") rather than capitalizing them — the brief's \"Soft (2)\" example capitalizes only because it opens a sentence; the acceptance criteria's own \"more (4)\" and \"strong (4)\" examples are lowercase, which this implementation reproduces. Flagged per RESEARCH.md Assumption A2 for Mark's own eyes at end-of-phase UAT: whether stops 2 and 4 should instead read their \"leaning {low/high}\" stop word (rather than the plain anchor) is a wording question this plan resolves one way, not a closed one."
  - "The summary line's declared-flaw token is DECLARED_FLAW.toLowerCase() (\"bitter\"), with no \"· declared\" suffix — the summary line's own existence already implies declaration, so the qualifier only appears in the separate defects line (\"Bitter · declared\"), which must disambiguate a declared flaw from an ordinary picked defect chip."
  - "The tasted date's absence (\"date unknown\") lives inside TastingReading, rendered only when a tasting exists; the pre-existing \"This batch has not been tasted yet.\" sentence (untouched by this plan) continues to cover the separate case of no tasting at all. These are different facts — tasted-but-undated vs never-tasted — and conflating them into one wording would lose the never-tasted state's own silence-is-a-value sentence."
  - "The Changed cell and the batch list's changed wording are keyed on batch.changed being present, not on batch.tasting — a churn-only batch that was later Corrected still carries a changed date and should show it, independent of whether a tasting was ever added."
  - "laterBatchMetaFor was extracted as a named, exported pure function (mirroring the file's existing AxesGrid export) rather than tested only through the rendered disclosure, because laterBatchesOpen is local component state with no prop this codebase's renderToStaticMarkup-only test style can force open."

patterns-established:
  - "laterBatchMetaFor: the reference pattern for testing render logic gated behind ungated local UI state (a `useState` toggle with no controlling prop) — extract the pure computation into its own exported function and test it directly, rather than reaching for jsdom/testing-library to simulate the click this codebase's test style otherwise avoids."

requirements-completed: [BATCH1-02, OBS1-01, UX1-01]

coverage:
  - id: D1
    description: "readMarkWord(axis, stop) maps stop 1-2 to the low anchor, stop 3 to the fixed word \"right\", and stop 4-5 to the high anchor, reproducing the working case's own tokens (Sweetness at 4 reads \"more\", Oil at 4 reads \"strong\")"
    requirement: "BATCH1-02"
    verification:
      - kind: unit
        ref: "app/src/domain/axes.test.js > readMarkWord — all four describe blocks"
        status: pass
    human_judgment: false
  - id: D2
    description: "The read view renders only marked axes as \"{word} (n)\" cells (an unmarked axis is dropped entirely, never a blank judgment), and the tasting-head summary line joins the marked axes' tokens and the declared flaw's lowercase word with \" · \", rendering nothing when neither exists"
    requirement: "BATCH1-02"
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the tasting read view, goldilocks words and the summary line — all cases"
        status: pass
    human_judgment: false
  - id: D3
    description: "The tasting's measured cells (temperature signed, melt test with its own unit, melt style's picked words) read \"not measured\" when blank; the defects line reads the picked words plus \"Bitter · declared\" on the declared flaw; the note renders as prose when written"
    requirement: "OBS1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > BatchRow — the tasting read view — measured-cell, defects-line, and note tests"
        status: pass
    human_judgment: false
  - id: D4
    description: "The record's dates read churned (head line, unchanged), tasted (\"date unknown\" when absent, never invented), and changed (only the latest change ever renders, via formatRecordDate); the later-batches small print reads \"changed {date}\" in place of the retired tasted-count wording, and no code path in BatchRow.jsx or batch.js reads \"Amended\"/\"amendedAt\" or any once/twice/times wording"
    requirement: "UX1-01"
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx > the record's dates: churned, tasted, changed (D-04); laterBatchMetaFor — the later-batches list's meta small print (D-04)"
        status: pass
      - kind: other
        ref: "grep -c 'tasted once\\|tasted twice\\|times' app/src/ui/BatchRow.jsx (returns 0); grep -c 'Amended\\|amendedAt' app/src/ui/BatchRow.jsx app/src/domain/batch.js (both 0)"
        status: pass
    human_judgment: false
  - id: D5
    description: "readMarkWord's exact wording choice (plain anchor vs. \"leaning {low/high}\") for stops 2 and 4 — RESEARCH.md Assumption A2 — is a cosmetic wording decision this plan resolved one way; it has not been shown to Mark"
    verification: []
    human_judgment: true
    rationale: "This is a subjective wording call on read-view prose, not a correctness property a test can settle — RESEARCH.md's own Open Question 2 recommends the planner propose wording within the plain-words rule and flag it for Mark's eyes at end-of-phase UAT, which this SUMMARY does."

# Metrics
duration: ~35min
completed: 2026-09-13
status: complete
---

# Phase 03.3.1 Plan 05: The full battery's read view — goldilocks words, dates, and the summary line Summary

**A saved batch's read view now reads the full battery: only the marked axes as goldilocks words with their stop numbers ("more (4)", "strong (4)"), a tasting-head summary line, the defects with the declared flaw's helper, the churned/tasted/changed dates with only the latest change shown, and the batch list's small print carrying the changed date instead of the retired tasted-count wording.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-09-13T13:10:00Z (approx, first Read)
- **Completed:** 2026-09-13T13:22:00Z
- **Tasks:** 2 (both type="auto", no TDD gate)
- **Files modified:** 4 (0 created, 4 modified)

## Accomplishments
- `axes.js`: `readMarkWord(axis, stop)` — the read view's own "Soft (2)" rule, pure and independently tested against all four stop ranges and the working case's own Sweetness/Oil tokens.
- `BatchRow.jsx`: `TastingReading` — the read view's rendering of the single stored tasting, replacing the placeholder "plan 05's build" comment: marked-axes-only cells, the summary line, the tasting's own measured cells (temperature, melt test, melt style), the defects line with the declared flaw's "· declared" helper, and the note as prose.
- `BatchRow.jsx`: a Changed cell in the read view (D-04, only the latest change ever renders) and `laterBatchMetaFor(batch)` — extracted and exported — reading "changed {date}" in the later-batches list's small print, replacing the retired tasted-count wording.
- Zero occurrences of "Amended"/"amendedAt" or any once/twice/times wording remain in `BatchRow.jsx` or `batch.js` (both plan verify greps return 0).

## Task Commits

Each task was committed atomically:

1. **Task 1: The tasting read view — goldilocks words, defects read, summary line (brief § 3)** - `bfe04d6` (feat)
2. **Task 2: The record's dates — churned, tasted, changed; the list's small print (D-04)** - `f25659c` (feat)

**Plan metadata:** committed alongside this SUMMARY (see final commit in this plan's return message).

## Files Created/Modified
- `app/src/domain/axes.js` - `readMarkWord(axis, stop)`
- `app/src/domain/axes.test.js` - coverage for `readMarkWord`'s four stop ranges and the working case's own tokens
- `app/src/ui/BatchRow.jsx` - `TastingReading` component, the read view's Changed cell, `laterBatchMetaFor` extraction
- `app/src/ui/BatchRow.test.jsx` - coverage for the tasting read view, the summary line, the Changed cell, and `laterBatchMetaFor`

## Decisions Made
See `key-decisions` in the frontmatter for: readMarkWord's lowercase-anchor choice (flagged for UAT per Assumption A2); the summary line's plain lowercase flaw token vs. the defects line's "· declared" suffix; keeping "This batch has not been tasted yet." distinct from "tasted date unknown"; keying the Changed cell and small print on `batch.changed` rather than `batch.tasting`; and extracting `laterBatchMetaFor` as an exported pure function to make the small print's logic directly testable.

## Deviations from Plan

None - plan executed exactly as written. One wording pitfall was caught and self-corrected before commit: an early draft of `laterBatchMetaFor`'s own doc comment used the words "one times" and "twice" to describe the retirement of the plural-tasting wording, which the plan's own Task 1 verify grep (`tasted once|tasted twice|times`) flags as exactly the wording it exists to catch — reworded to "zero or one, never plural" before running the verify command, confirmed at 0 before committing Task 2.

## Issues Encountered
- A fresh worktree checkout had no `app/node_modules` (the same condition every prior plan in this phase has noted) — symlinked to the main repo's `app/node_modules` rather than reinstalling, since `package.json`/`package-lock.json` are unchanged by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Surfaced for end-of-phase UAT** (per this plan's own read of RESEARCH.md's Assumption A2 and Open Question 2):

1. **readMarkWord's exact wording for stops 2 and 4 (Assumption A2, Open Question 2 — resolved at planning, flagged for Mark's own eyes):** this plan's implementation reads the plain anchor word for every marked stop except the fixed middle ("soft" at stop 2, "hard" at stop 4, etc.) rather than the contract's own "leaning soft"/"leaning hard" stop words. The brief's own "Soft (2)" example and this plan's own acceptance criteria ("more (4)", "strong (4)") both use the plain-anchor form, which is what shipped — but the sketch has no saved/read state at all, so nothing in the design authority specifies this choice definitively. Worth a look at the live read view before considering this settled.
2. **The declared flaw's summary-line wording ("bitter", lowercase, no suffix) vs. the defects line's own wording ("Bitter · declared")** — two different renderings of the same fact in two different places on the same read view. Both are internally consistent with their own context (the summary line's brevity vs. the defects line's need to disambiguate a declared flaw from an ordinary chip), but worth Mark's eye alongside item 1.

**Not blocking, but worth later plans' attention:**
- New CSS class names this plan introduces with no styling yet (tokens-only convention, deferred per this phase's own established pattern for plan 06): `tasting-reading`, `tasting-reading__summary`, `batch-row__dates` — present in the DOM, unstyled.
- The always-visible tasting mode, the unreadable-as-made value, and the other carried/deferred items from `03.3.1-CONTEXT.md` remain exactly as recorded there; this plan touches none of them.

---
*Phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007*
*Completed: 2026-09-13*

## Self-Check: PASSED
