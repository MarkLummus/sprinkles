---
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
plan: 06
type: execute
wave: 6
depends_on: ["03.3.1-02", "03.3.1-03", "03.3.1-04", "03.3.1-05"]
files_modified:
  - app/src/styles/tokens.css
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - .planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-06-SUMMARY.md
autonomous: true
requirements: [UX1-01, UX1-03]
estimate:
  tokens: 44000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The battery's verified numbers hold: the pen is 640px wide; desktop stops are 34×32 on the 186px track with anchors matched to it; below 760px stops grow to 40×44 on the 216px track and chip toggles, segmented options, text controls, buttons and ink fields all reach 44px; zero horizontal overflow at 393px; desktop untouched at 768px (contract 'Responsive ladder')."
    - "The record renders the verified sketch look at every width with nothing broken — the ladder's step-downs hold below 760px and at 600px, every visual value renders through its token, and the style suites pass unbroken after each change (Pitfall 9's one-level at-rule contract holds)."
    - "On screen, a picked chip, option or stop reads as picked through bold plus underline and outline, and a filled control carries no underline — the carried P3 lands visibly, at every width."
    - "The built record's conformance is verified in the browser against the structural contract at real widths, including the matchMedia-under-emulation caveat (real window widths, not emulation)."
    - "Phase 4's PRINT planning receives the final field list: every battery field the batch-log page prints is named in this plan's summary."
  artifacts:
    - app/src/styles/tokens.css
    - app/src/styles/app.css
  key_links:
    - "tokens.css battery tokens → every app.css rule and component dimension (the tokens-only convention)"
    - "the @media (max-width: 760px) block → AxisMark's matchMedia hook boundary (760px, one boundary, two names)"
    - "the browser conformance check → the contract section by section, at real widths 1280/768/759/600/393"
---

<objective>
The battery's responsive ladder and its visual contract land as tokens and rules — the verified numbers, the single-level at-rule discipline, the picked-state affordances, and the P3 underline drop — and the built record is verified in the browser against the structural contract. The phase's final deliverable names the fields Phase 4's batch-log page prints.

Purpose: the record must look and behave like the verified sketch at every width, and Phase 4's PRINT planning must read the final field list from this phase's output.
Output: tokens.css battery tokens; the app.css ladder and control styles; a browser conformance pass; the print field list.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery.md
@DESIGN.md
@app/src/styles/tokens.css
@app/src/styles/app.css
@app/src/styles/css-source.js
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (the CSS layer and the phase's closing deliverables):

- tokens.css battery tokens: `--pen-w` (640px), `--track-stop` (186px), `--track-stop-narrow` (216px), `--stop-w` (34px), `--stop-h` (32px), `--stop-gap` (4px), `--touch-stop-height` (44px, riding the existing touch block), `--gap-chip-row`, `--declared-flaw-gap`, `--axes-cols-clearance` (12px); the breakpoint literals live in app.css preludes only (759.98px prelude precedent).
- app.css rules: `.save-ceremony`, `.save-ceremony__hint`, `.form-status`, `.field-error`, the segmented control (shared borders, picked state), `.chip-toggle` and the declared-flaw margin, the axes grid's desktop 3-column rules with `.axes-rule` at `left: calc(200% / 3)`, the stacked-group rules, the 600px block, and the stops' desktop sizes.
- The style suites stay green: binder/columns/cross-cutting read tokens and resolve px; the one-level at-rule parser contract holds.
- The phase summary records the print field list for Phase 4.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Battery tokens and the desktop control styles (contract "Responsive ladder", "Axes spec")</name>
  <files>app/src/styles/tokens.css, app/src/styles/app.css, app/src/styles/binder.test.js</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — section "Responsive ladder" (the verified numbers, verbatim) and "Axes spec" (the anchors row, the hairline position)
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery.md` — sections "CSS Patterns" and "What to Avoid" (the picked-state affordance; the underline drop is the carried P3)
- `app/src/styles/tokens.css` — the touch-token block (lines 212-222, the px-literal precedent) and the existing spacing/rule tokens
- `app/src/styles/app.css` — the axis-mark section (1539-1630) and the pen-foot band (680-710)
- `DESIGN.md` — the Two-Ink, No-Verdict, Bookcloth rules and pen blue's role
  </read_first>
  <action>
Cite the contract's "Responsive ladder" and "Axes spec" sections. (1) tokens.css: add the battery tokens from this plan's artifacts list as named custom properties in the existing token groups — every value the contract verifies, nothing literal in app.css (the tokens-only convention; px literals live in the token declarations exactly as the touch block set the precedent). (2) app.css: style the pen's controls to the contract — the stops at 34×32 on the 186px track with a 4px gap, tabular-nums digits, the hairline ink border, no radius; the anchors row space-between across the track width, italic; the segmented control as shared-border options with the picked state bold plus underline (and the picked option's own outline-and-weight treatment per the binder), never colour alone; the chips row and the wider declared-flaw gap; `.save-ceremony` and its hint; `.form-status` small print; `.field-error` in the system's own words face (a named token value from the existing faces). (3) The picked-state affordance: checked/pressed states carry bold plus underline at 3px offset per the findings file's CSS pattern — and the carried P3 lands here: the underline on filled controls (the filled underline that reads on ink-field values) is dropped — the P3's scope is the filled-control underline only, so a filled control reads as filled through weight and outline instead, while picked states keep bold plus underline and outline. Do not copy the sketch's `[aria-checked], [aria-pressed]` underline block verbatim for the fill underline — the P3 governs (the findings file's "Carried for the phase plan" list). (4) Keep the parser contract: every new rule top-level; the style suites run.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/styles</automated>
    <fails_when>non-zero exit, or any binder/columns/cross-cutting suite fails</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'var(--pen-w)\|var(--track-stop)' app/src/styles/app.css</automated>
    <fails_when>output is 0 — the battery dimensions are not reading tokens</fails_when>
  </verify>
  <acceptance_criteria>
- tokens.css declares the battery tokens with the contract's verified values (640px pen, 186px track, 34×32 stops, 4px gaps, 12px clearance).
- Every new app.css rule's dimensions read tokens; no bare px declaration appears outside a prelude.
- The stops carry tabular digits on a hairline border with no radius; the anchors row is aria-hidden space-between across the track.
- Picked states carry bold-plus-underline-and-outline affordances; the filled-control underline is gone (the carried P3).
- The style suites pass (binder, columns, cross-cutting).
  </acceptance_criteria>
  <done>The battery's desktop face is styled through tokens: stops, tracks, segmented controls, chips, ceremonies, statuses and errors — with the P3 underline drop landed.</done>
</task>

<task type="auto">
  <name>Task 2: The responsive ladder — 760px step-down and the 600px block (contract "Responsive ladder")</name>
  <files>app/src/styles/app.css, app/src/styles/tokens.css, app/src/styles/cross-cutting.test.js</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — section "Responsive ladder" (every number: 761px-up grid, 768px clearance, sub-760 stacking, auto-fit grid, 216px track, 40×44 stops, 44px targets, the 600px block, zero overflow at 393px)
- `app/src/styles/app.css` — the existing single-level @media block (1603-1630) and the 759.98px prelude precedent
- `app/src/styles/css-source.js` — the parser contract: exactly one level of nesting; assertNoAtRules throws on any other at-rule (Pitfall 9)
  </read_first>
  <action>
Cite the contract's "Responsive ladder" section. (1) Extend the existing below-760px block (or add a second top-level block at the same breakpoint — multiple top-level @media blocks, never nested) with the contract's step-down: the axes groups stack on the horizontal rule with each group's grid at auto-fit minmax(min(100%, 280px), 1fr); the stops grow to 40×44 on the 216px track with anchors re-aligned; chip toggles, segmented options, text controls, buttons and ink fields all take the 44px minimum height (extend the existing touch block's selectors — the touch tokens already carry --touch-min and --touch-stop-width). (2) Add the 600px block: frame padding, field-row label widths, field font sizes, and the saves rows allowed to wrap (D-01's two identical ceremonies must wrap identically — one rule serving both). Every declared width keeps its token; the prelude is the only breakpoint literal in each block. (3) The desktop 3-column grid applies from 761px up with the hairline at the contract's calc position between columns 2 and 3, and the side-by-side split holds to 768px with the 12px minimum clearance (a grid-gap token the clearance arithmetic derives from). (4) Run the style suites after the change; the parser must still accept the file (one level of nesting only).
  </action>
  <verify>
    <automated>npm --prefix app test -- src/styles</automated>
    <fails_when>non-zero exit, or the parser/at-rule assertions fail</fails_when>
  </verify>
  <verify>
    <automated>grep -c '@media' app/src/styles/app.css</automated>
    <fails_when>output is less than 2 — the step-down blocks did not land as separate top-level rules</fails_when>
  </verify>
  <acceptance_criteria>
- The sub-760 rules land as top-level @media blocks (no at-rule nested inside an at-rule); the css-source parser accepts the whole file.
- Below 760px the stops are 40×44 on the 216px track and every interactive control reaches the 44px minimum.
- The 600px block tweaks frame padding, label widths, font sizes, and lets the saves rows wrap; both ceremonies wrap the same way.
- The desktop 3-column grid and the hairline's calc position match the contract's numbers.
  </acceptance_criteria>
  <done>The verified ladder holds: 3-column desktop with the hairline, the 760px step-down with grown stops and 44px targets, the 600px block, and zero overflow at 393px.</done>
</task>

<task type="auto">
  <name>Task 3: Browser conformance against the contract, and the print field list (phase notes)</name>
  <files>.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-06-SUMMARY.md, app/src/ui/AxisMark.jsx (only if the Home/End contingency fires), app/src/ui/AxisMark.test.jsx (only if the Home/End contingency fires)</files>
  <read_first>
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — every section; this is the check target ("Checkers verify the built record against this file in the browser")
- `.planning/ROADMAP.md` — § Phase 03.3.1 (the browser-verification requirement and the PRINT handoff bullet)
- The gsd-browser MCP surface — snapshot/act tools for driving the real app
  </read_first>
  <action>
Verify the built record's conformance in the browser against the structural contract, section by section, at real window widths (the matchMedia-under-emulation caveat: resize a real window, never an emulated viewport). Drive `npm --prefix app run dev` and check: (1) at 1280px — the pen at 640px, the 3-column axes grid with the hairline between columns 2 and 3, the row-major tab order Hardness, Scoopability, Body, Smoothness, Sweetness, Oil, stops at 34×32 on the 186px track; (2) at 768px — the side-by-side split still holds with its 12px clearance; (3) below 760px (resize to 700 then 393) — the groups stack on the horizontal rule, stops grow to 40×44 on the 216px track, every control reaches 44px, zero horizontal overflow at 393px (compare scrollWidth to innerWidth), and the matchMedia crossing re-renders the axes to the stacked order with marks carried; (4) the keyboard contract — arrows move and select with wrap, one tab stop per group, Home/End land per the contract (verify in Chrome; if the browser lacks Home/End, add the contract-sanctioned minimal Home/End keydown in this task — app/src/ui/AxisMark.jsx plus its test, a tiny addition with no arrow-key focus manager, so Pitfall 5's line still holds — and record the addition in the conformance record); click-again clears on stops, options and chips; per-axis Clear returns focus to the first stop and announces; Escape closes only an untouched pen; the churn-date block shows beside both ceremonies with focus on the date; a malformed numeric stays in place with its sentence and focus; the removal/undo lifecycle speaks and restores; the read view reads the seeded record as the battery. Record each section's pass/fail in the plan SUMMARY with the widths used. Then record the print handoff: the final field list Phase 4's batch-log page must print — the churn section's fields (Churn date; Time to draw temp. min; Out of machine °C; Churn duration min; Exit consistency; Airiness (estimated); At the machine; Ingredient notes; Next time), the tasting's fields (Tasted date; Tasting temperature °C; Tempering min; the six axes with their 1–5 marks and stop words; the note; the four defect chips plus the declared Bitter toggle; Melt test g lost at 20 min; Melt style), the record's dates (churned, tasted, changed) — into the plan SUMMARY under a "Fields Phase 4's batch-log prints" heading, sourced from the stored shape in 03.3.1-01-SUMMARY.md.
  </action>
  <verify>
    <automated>grep -c 'Fields Phase 4' .planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-06-SUMMARY.md</automated>
    <fails_when>non-zero exit, or a count of 0 — the print field list is absent from the summary</fails_when>
  </verify>
  <acceptance_criteria>
- The conformance record in the plan SUMMARY covers every contract section with the widths used and names any deviation with its section.
- Zero horizontal overflow at 393px was measured in a real window (scrollWidth ≤ 393).
- The two axis arrangements and their tab orders were both observed at real widths, with marks carried across a real boundary crossing.
- The summary carries a "Fields Phase 4's batch-log prints" heading listing the battery's churn fields, tasting fields, and dates.
  </acceptance_criteria>
  <done>The built record conforms to the structural contract at every verified width, and Phase 4's PRINT planning can read the final field list from the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server (localhost) → browser check | Verification drives the local app only; no network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1-14 | Information disclosure | visual state | low | mitigate | Every state affordance works without colour (weight, outline, underline per the contract); AA contrast is inherited from the existing tokens (pen blue 9.75:1 on ground, verified) |
| T-03.3.1-15 | DoS | CSS parser contract | low | accept | The one-level at-rule rule is enforced by the style suites; nesting would fail the build's own tests, not the user |
</threat_model>

<verification>
- `npm --prefix app test` (the full suite) passes; `npm --prefix app run build` compiles.
- The style suites (binder, columns, cross-cutting) pass with the new tokens and top-level at-rules.
- The browser conformance record and the print field list exist in the plan SUMMARY.
</verification>

<success_criteria>
The battery's verified geometry and control styles hold at every width from the ladder; the built record conforms to the structural contract in the browser; the filled-control underline is dropped; Phase 4's batch-log field list is recorded for PRINT planning.
</success_criteria>

<output>
Create `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-06-SUMMARY.md` when done — it must carry the conformance record and the "Fields Phase 4's batch-log prints" list.
</output>