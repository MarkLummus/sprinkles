---
status: complete
phase: 03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007
source: [03.3.1-VERIFICATION.md]
started: 2026-09-13T19:15:00Z
updated: 2026-09-13T21:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. True 393px horizontal-overflow check
expected: Resize a real (non-emulated) browser window to exactly 393px wide with a record pen open and a tasting section expanded, and compare document.scrollWidth to window.innerWidth. scrollWidth <= innerWidth (zero horizontal overflow) attributable to any battery element. (This session's browser floor was ~500px; the ~500px overflow measured traces to a pre-existing, unrelated page-shell issue — .recipe-band/.vmeta, tracked in STATE.md — not the battery pen.)
result: pass
reported: "window.innerWidth = 579, document.scrollWidth = 579 (browser floor still could not reach true 393px, same as prior session; measured widths equal — zero horizontal overflow at the achieved width)"

### 2. Add tasting → Tasted date focus landing
expected: Press "Add tasting" on a fresh record pen (no prior tasting) and confirm keyboard focus lands on the Tasted date input the instant the section opens. (Code is wired and unit-tested via an attempt-counter + ref pattern identical to the already-live-verified churn-date block focus; only the actual DOM focus landing itself was not re-observed live.)
result: pass
reported: "Corrected: the earlier screenshot was pre-click. After actually clicking Add tasting, the Tasted date input is focused correctly."

### 3. D-02's literal reading: does emptying a tasting on Correct delete it?
expected: Open Correct on a batch with a saved tasting, empty every tasting field/mark/chip, and press Save batch. Confirm with Mark whether this should delete the stored tasting (current, D-02-literal behavior — a null tasting on completeRecord removes the stored one) or instead leave the previously-saved tasting untouched.
result: pass

### 4. Read-view wording for goldilocks stops 2 and 4
expected: Show Mark a saved tasting's read view with an axis marked at stop 2 or 4 (e.g. Sweetness at 4 reading "more (4)"). Confirm whether the plain anchor word ("more") or the stop's own "leaning" word ("leaning more") is the intended read-view wording. Current code (readMarkWord) reads the plain anchor for every stop except the fixed middle.
result: pass
reported: "I don't like current implementation. The tasting section caption has the same dimensions as the hint: 'Tasting · soft (2) · gummy (4) · smooth (4) · less (2) · thin (2) · strong (4)' and then immediately below it are the axes showing the same values in a better looking layout. We should drop the hint on the section caption. fail."
severity: minor
resolution: "Gap G-03.3.1-4 closed by 03.3.1-07-PLAN.md (commit d8163c7) — the anchor-word question itself was answered by endorsing the cell grid's existing wording; the caption's duplicate restatement was dropped. Re-verified 31/31 in 03.3.1-VERIFICATION.md."

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-03.3.1-4
  truth: "The tasting read view's axis-value wording (plain anchor vs. leaning word) is settled and the section caption does not duplicate the hint shown by the axes below it."
  status: resolved
  reason: "User reported: I don't like current implementation. The tasting section caption has the same dimensions as the hint: 'Tasting · soft (2) · gummy (4) · smooth (4) · less (2) · thin (2) · strong (4)' and then immediately below it are the axes showing the same values in a better looking layout. We should drop the hint on the section caption. fail."
  severity: minor
  test: 4
  root_cause: "TastingReading() in app/src/ui/BatchRow.jsx builds summaryTokens/summaryLine (lines 211-216) from the same readMarkWord output the .batch-row__cells grid (lines 230-236) already renders per-axis, and puts it in the <h3> caption (lines 223-226) with no distinguishing CSS — so the caption duplicates the cell grid at the same visual weight."
  artifacts:
    - path: "app/src/ui/BatchRow.jsx"
      issue: "Lines 211-216 build summaryTokens/summaryLine; lines 223-226 render it in the <h3> caption, duplicating the per-axis words/numbers the cell grid at lines 230-236 already shows."
    - path: "app/src/ui/BatchRow.test.jsx"
      issue: "Lines 820-823 pin the summary span's exact content (encodes the now-rejected design); lines 825-830 already assert the bare <h3>Tasting</h3> no-marks case, which becomes the universal case."
  missing:
    - "Drop the caption's use of summaryLine (lines 214-216, 225) so <h3> renders as bare 'Tasting'; markedAxes (line 213) stays untouched since the cell grid depends on it independently."
    - "Update/remove BatchRow.test.jsx:820-823's summary-span assertion to match the bare caption; the no-marks case at 825-830 becomes the universal case."
    - "Check whether bitterDeclared's lowercase token (currently only fed the caption's summary) needs to surface elsewhere — the defects line already carries a declared-flaw entry via DECLARED_FLAW, so likely no additional surfacing is needed, but confirm."
  debug_session: ".planning/debug/tasting-caption-dup-hint.md"
  resolved_by: "03.3.1-07-PLAN.md"
  resolved_at: "2026-09-13"

## Triaged Notes (from Test 1 session, 393px width)

<!-- Reported alongside Test 1's pass, explicitly flagged by Mark as "may not be specific
to this test" — captured verbatim for triage after UAT completes, not auto-converted to
gaps or fix plans. -->

Triaged 2026-09-13 into Phase 03.3.1.1 (Record pen conformance polish, INSERTED after 03.3.1) — see `.planning/ROADMAP.md`.

- Version section is too narrow; should flow between recipe description and Batch section
- In Batch section, "Time to draw temp., min" caption wraps
- Field text boxes for Time to draw temp, Out of machine, Churn duration are too big for the values
- Exit consistency and Airiness segment controls have a hairline border not present in the sketch/contract; selected value is styled bold + underline instead of solid fill
- The Add tasting opener is missing from the top Save ceremony section
- On Tasting section caption, the hint is styled the same as "Tasting"
- Tempering min edit control is full width instead of sharing a row with Tasted date
- Tasting temperature edit control is full width instead of sharing a row with Tasted date
- Tabbing from "How did it turn out?" does not show focus on the first select under Hardness
- Selected axis value (e.g. 1) is bold underline, unlike sketch/contract
- Flaws under "Any problems?" are styled unlike sketch/contract
- Melt test edit control is full width and should share a row with Melt Style
- Melt style also has the outline — segmented control style is not like sketch/contract
- Caption for "Next time?" is missing
- In Ingredients section, the Ingredient section caption and Ingredients table heading overlap
- Column 2 (notes) should flow after the ingredients table because the ingredient name column is too narrow and wraps too much
- Mark noted "many other errors" beyond this list
- Batch row, Record Batch mode: sketch says "CHURN DATE", actual reads "Churned"
- Batch row, Record Batch mode: sketch has the Remove tasting opener right-aligned on the row with the TASTING section caption; actual has it below the Tasting section caption, left-aligned
- Batch row, Record Batch mode: sketch has all captions upper case; actual is sentence case
