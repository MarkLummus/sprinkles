---
context: phase
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
task: 9
total_tasks: 13
status: in_progress
last_updated: 2026-09-15T01:47:07.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: The sketch HTML is the single authority — 03.3.1 built to prose derivations and drifted; this phase exists to fix that. Every planner, checker, executor and verifier reads `.planning/sketches/007-full-battery/index.html` and `003-front-matter-rows/index.html` including their CSS, cites line numbers at the committed hash, and never answers an app-vs-sketch difference with an accepted-departure row: the sketch is revised instead (Mark, 2026-09-14).
- [ ] CONSTRAINT: Measure, never reason from CSS — four times this session the expectation was wrong (Clear "inline with the stops" overflowed a 3-column cell by 15px; an empty flex slot still held a 12px gap; the sheet's 393 preset applied no phone sizes; 1.5px borders render as 1px). Every claim about the sketch is a `getBoundingClientRect`/`getComputedStyle` reading at a real viewport (gsd-browser emulation for 393/360; Mark's Chrome for desktop), taken before and after the change.
- [ ] CONSTRAINT: Sketch line numbers are cited by research and the contract — after any commit that touches a sketch, the citations are re-cited before planning (this session: 978a507 → d574e53 → 9365383). Do not plan against stale numbers.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Departure rows | Treating a sketch-vs-app difference as "known, accepted" in CONFORMANCE.md instead of redrawing the sketch | blocking | D-22 aims at zero departure rows; every difference goes to Mark as a sketch edit, measured and shown before commit |
| Frame-width presets | A sketch toolbar that narrows the frame does not fire media queries; sizes looked wrong to Mark at "393" | blocking | Judge phone widths only at a real viewport (gsd-browser `browser_emulate_device`) or with the sheet's `syncTouch` class; never trust a preset alone |
| zsh echo of JSON | `echo "$INIT"` in zsh mangles `\\` escapes in gsd-tools JSON and breaks `JSON.parse` | advisory | Run GSD init/query pipelines from a `bash` script file with `printf '%s'` and `node -e` |
| Line drift under research | Editing a sketch while a researcher cites it by line | advisory | Keep edits line-stable (append to existing lines, use blank lines) or re-cite afterwards |

<current_state>
Phase 03.3.1.1 is in `/gsd-plan-phase`, paused before step 8 (planner spawn). Sketch 008 is finished and committed (3ed0c0f, 2026-09-14): the control language is settled — joined single-choice groups (segmented control, five-cell axis) as radios with Clear in a caption line that reserves its height; square-and-word independent on/offs (checkbox, defects in 007's two captioned groups, Show changes) with one focus rule (ring around square and word); a lone hairline box or underlined word is an action; 2px hover and focus ring with fixed outer dimensions; invalid as a sentence, no ring; high contrast as a Highlight fill (checked in Chrome's emulation). The sheet's "Settled values" section is the implementer's list of the twelve values; its README records every round with measurements.

Sketch 007 and 003 have not yet taken the rule. Carry-over list for 007: defects as square-and-word in the two captioned groups; joined stops on all six axes (38px cells on 186px; 44px on 216px); Clear on Exit consistency and Airiness in their caption lines, heads reserving 24px/44px; whole-label focus ring on Skipped and Show changes (Show changes 32px tall); 2px hover and ring; invalid without a ring; caption tracking 0.04em and axis names at weight 500; region-name gap 6px; numeric fields 56px; Clear moving focus only from the keyboard. 003: the Skipped checkbox's whole-label ring, 2px hover and ring, 0.04em. Then DESIGN.md's two leftover "bold plus outline" sentences (~212, ~292).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: D-21 DESIGN.md bridge edit — Done (fede1c8, before this session)
- Task 2: Research + contract visual layer (D-02, D-03: all 18 items are app-side) — Done (978a507; amended d574e53; re-cited 9365383)
- Task 3: Sketch 008 control sheet drafted by an unattended agent — Done (4a8dff6)
- Task 4: Pattern map — Done (b2c266e)
- Task 5: Sketch 007 ninth round, the D-22 revision, reviewed live with Mark and measured at 1188/393/360 — Done (977846a; every change in 007's README § "Ninth round")
- Task 6: Research re-cite against 977846a — Done (9365383)
- Task 7: Sketch 008 review — In progress: winner B square, phone-width fix, Table-as-written cleanup on disk, uncommitted; control-language rationalization mid-discussion
</completed_work>

<remaining_work>

- Task 7: commit the five files; settle the control language with Mark; answer the remaining decision-table rows
- Task 8–9: apply the settled standard to 003, 007 and 008 (007's flaws may redraw as square-and-word; captions/tracking/gaps/sizes per the answers), measure at 1188/393/360, commit the three together, mark 008's README/MANIFEST winner final
- Task 10: fix DESIGN.md's leftover "bold plus a 1px ink outline" picked-state sentences (≈ lines 212 and 292) as a docs commit
- Task 11: re-cite RESEARCH.md and the contract against the final sketch commits (a fresh researcher with the same brief, or `/gsd-plan-phase 03.3.1.1 --research`)
- Task 12: `/gsd-plan-phase 03.3.1.1` → planner (RESEARCH.md and PATTERNS.md exist, so it goes straight to step 8) → plan checker → `/gsd-execute-phase 03.3.1.1`
- Task 13: `/gsd-sketch --wrap-up` to refresh the skill's `sources/007-full-battery/` copy (D-20)
- Deferred, not this phase: Skipped checkbox → the chosen on/off shape in the app (quick task); Show changes pressed treatment (quick task); `/impeccable document` after the build
</remaining_work>

<decisions_made>

- The sketch is the single authority; app-side decisions get drawn back into the sketch (ceremonies to 03.3.1 D-01/D-14, region names "Batch"/"Tasting", G-03.3.1-4's bare Tasting head) — no departure rows.
- Sketch 007 ninth round: picked = fill only; helper out of Bitter; "(optional)" dropped; ceremonies "Add tasting | Cancel | Save batch" right-aligned at every width; "Restore tasting" replaces "Undo clear tasting" and hides Add tasting while pending (so the row fits a phone); removal toast at the end-of-record ceremony; Clear right-aligned with negative block margins (no row movement on pick; "inline with the stops" rejected by measurement); group captions "Every recipe"/"This recipe only" on their own grid row; the defects share the axes grid (one column "This recipe only" top to bottom); `.seg` row-gap 6px; prose placeholders all "e.g." examples that fit one line at 360px ("e.g. bowl frozen overnight", "e.g. oil bottle opened 24 Jul", "e.g. flavor, texture, what stood out", "e.g. churn 2 min longer").
- Sketch 008: winner B, leading ink square the checkbox's size; A kept for the record; D-21 narrows to "every boxed control fills".
- "Table as written" is not a thing (Phase 03.3: step order is the only order).
- Review 008 before planning; the planner runs on final values.
- Rows already answered: 1 (B square), 6 (pen blue for a recorded fact, ink for a setting — pending the shape decision), 13 (44px text controls below 760px), 16 (removed).
- Measured fact: 1.5px borders render as 1px in Mark's Chrome at DPR 1; underlines keep 1.5px. Row 2's recommendation: 2px hover border with compensating padding.
</decisions_made>

<blockers>
- Control-language rationalization: Mark's call (square+word for independent on/offs vs keep chips). Blocks the sketch edits and therefore the planner.
- Decision table rows 2–5, 7–12, 14, 15, 17, 18: Mark's calls; recommendations are in the 008 README's disagreement list plus the session table (2 → 2px hover; 3 → none on segments/chips; 4 → 007's 0.025em/0.035em; 5 → 12px; 7 → 128px/48px tokens; 8 → 0.8125rem; 9 → 007's; 10 → hairline baseline, one focus ring; 11 → sentence only, no ring; 12 → text face italic; 14 → 1rem; 15 → 65ch; 17 → no offset; 18 → fix DESIGN.md now).
</blockers>

## Required Reading (in order)
1. `.planning/phases/03.3.1.1-…/03.3.1.1-CONTEXT.md` — the 22 decisions; note D-08 and the "Clear confirmed as-sketch" bullet are superseded by the ninth round (the HTML governs).
2. `.planning/sketches/007-full-battery/README.md` § "Ninth round" — every sketch change with measurements; and `index.html` @977846a.
3. `.planning/sketches/008-control-sheet/README.md` — the winner, "Choices made unattended", "Where the sources disagree" (the decision table's source), "Phone-width honesty".
4. `.planning/phases/03.3.1.1-…/03.3.1.1-RESEARCH.md` § "Re-citation against commit 977846a" and § D-03 triage — the app-side implications and test pins.
5. `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` § "Visual treatment (from the sketch CSS)" — what plans cite by section name.
6. `.planning/phases/03.3.1.1-…/03.3.1.1-PATTERNS.md` — analogs and pins.
7. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses; apply before any assumption analysis

## Critical Anti-Patterns (do NOT repeat these)
- [DEPARTURE ROWS]: answering a sketch-vs-app gap in the conformance pass → redraw the sketch with Mark instead
- [PRESET ≠ VIEWPORT]: judging phone sizes under a frame-width preset → real viewport emulation or the sheet's `.touch` class
- [CSS REASONING]: predicting layout from source → measure before and after, both widths
- [STALE CITATIONS]: planning against sketch line numbers from before the last sketch commit → re-cite first

## Infrastructure State
- Sketch server: `python3 -m http.server 8077 --bind 127.0.0.1` rooted at `.planning/sketches/` (was up at pause; restart on resume: `cd .planning/sketches && python3 -m http.server 8077 --bind 127.0.0.1 &`). URLs: `/007-full-battery/index.html`, `/008-control-sheet/index.html`.
- gsd-browser daemon (Playwright): was emulating iPhone 15 (393); its live viewer was open in Mark's third Chrome tab (`browser_view` gives a fresh URL). A stale daemon refuses to connect: `gsd-browser daemon stop`, then retry.
- Mark's Chrome (claude-in-chrome): tabs for 007, 008 and the viewer were open; window ≈1188px wide, DPR 1. Tab ids do not survive a session — call `tabs_context_mcp` first.
- Research agent context (three amendments via SendMessage) does not survive the session.
- `/tmp` scratchpad scripts (`init.sh`, `pre.sh`) do not matter; the init pattern is: run gsd-tools from a bash script, write JSON with `printf`.

## Pre-Execution Critique Required
- Not applicable: paused inside planning, before the planner; no plan exists yet.

<context>
Mark drove a tight live loop all day: I edit the sketch, measure in his Chrome and in the emulated phone, show him, he corrects, repeat — then commit on his word. He thinks in terms of a coherent control language (shape = kind of control, colour = recorded vs system) and wants it rationalized across 003, 007 and 008 before anything is planned, so the plans build to final values once. The last exchange: I proposed five shapes with one job each (square+word on/off, lone box action, touching boxes pick-one, numbered track scale, underlined word inline action) and recommended redrawing 007's flaw chips as square-and-word; he had not answered. Whoever resumes should put that question first, in one message, with the decision table beneath it, then do the sketch edits in one pass and measure them.
</context>

<next_action>
Apply the settled rule to sketch 007 (and 003's checkbox) in one pass, measure at 1188/393/360 in Mark's Chrome and gsd-browser, show Mark, commit the two together; then the DESIGN.md docs fix; then re-cite RESEARCH.md and the contract against the new sketch commits (fresh researcher, same brief); then `/gsd-plan-phase 03.3.1.1`.
</next_action>
