---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/styles/tokens.css
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/columns.test.js
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
autonomous: true
requirements: []
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Below a 1100px viewport the recipe page is one column in the order band, ingredients, side, method, foot, and its 48px page padding holds down to 600px where it becomes 20px (sketch 003 @ 0fa1015 lines 47-48 and 178; UAT items 15 and 16; D-15)"
    - "The version row is two columns, the metadata column never narrower than 300px, above 760px, and one column below it (003 line 69 and 71; UAT item 1; D-15)"
    - "At a real 393px viewport document.scrollWidth equals window.innerWidth with a batch in view and the record pen open; the ingredient table may scroll inside its own region (D-15's acceptance criterion; the STATE.md 393 blocker retires)"
    - "Correct is the last child of the batch head line, right-aligned, and the row renders no foot acts and no read-view Add tasting (003 lines 84, 238, 256)"
    - "Both count disclosures carry aria-expanded and aria-controls naming the panel they open (003 lines 214 and 238)"
    - "A recorded figure in the batch row reads at the note size (16px) at weight 400 in pen blue with the unit word at the deviation size; an absent value reads 'not measured' in ink (003 line 88 at 0fa1015; CONTEXT 'The recorded figure', candidate D); the read view's tasting heading takes the region-name role (003 line 36 and 247; UAT item 6, read-view half)"
    - "Every text control is 13px, at least 24px tall at desktop and 44px tall below 760px (003 lines 32 and 178; CONTEXT D-15 supersession list 'text controls at the control size'; 007 lines 131 and 180)"
  artifacts:
    - app/src/styles/app.css
    - app/src/styles/tokens.css
    - app/src/ui/BatchRow.jsx
    - app/src/ui/VersionRow.jsx
  key_links:
    - "the new top-level @media (max-width: 1099.98px) block in app.css → the three @media enumerations in binder.test.js and cross-cutting.test.js (a condition string missing from either pin goes red on the first test run)"
    - ".recipe-band__row-version's new columns → --vmeta-min in tokens.css (the 300px literal never lands in app.css; binder.test.js's no-bare-px gate)"
    - ".batch-row__head's last child Correct → .batch-row__correct { margin-left: auto } (the head is already flex + baseline + wrap; the auto margin is what right-aligns it)"
    - ".text-control's min-height → the 759.98px block's selector enumeration in cross-cutting.test.js (the block gains .text-control; the 'exclusion is real' test inverts)"
---

<objective>
Close the three page-shell gaps and the batch row's read-view gaps against sketch 003 (`.planning/sketches/003-front-matter-rows/index.html` @ `0fa1015`; every 003 line number below is unchanged since `d89b758`, the commit RESEARCH.md re-cited): the page stacks to one column below 1100px with the side column following the ingredient table (UAT items 15, 16), the version row collapses below 760px and its metadata column is never squeezed (item 1), the true-393px horizontal overflow the STATE.md blocker records is closed (D-15), Correct moves onto the Batch head line with no foot acts, both count disclosures carry `aria-controls`, the recorded figure takes the settled note-size/prose-weight treatment (CONTEXT § "The recorded figure", candidate D), the read-view tasting heading takes the region-name role (item 6's read-view half), and text controls take the control size with the 24px/44px minimum heights (CONTEXT D-15 supersession list).

Purpose: D-15 puts the page shell in scope as one plan against sketch 003 cited line by line, exactly as the battery plans cite 007. These are the only items of the 18 that live outside the record pen, and the 393 fix is the acceptance criterion the roadmap had marked out of scope until CONTEXT D-15 pulled it in.

Output: `app.css` gains its third top-level `@media` block (1099.98px), `min-width: 0` on the four grid areas, the collapsed version row inside the existing 759.98px block, `overflow-x: auto` on the ingredient region inside the 600px block; `tokens.css` gains `--vmeta-min`, `--text-control-min` and `--leading-control`; BatchRow's head line carries Correct; both later-panels are named by `aria-controls`; the three style suites and the two component suites pass with their pins moved.

Spec-less probe fallback: skipped visibly — this phase has no requirement IDs to probe, and no UI-SPEC.md exists; the sketch HTML plus CONTEXT.md is the design contract (orchestrator's `<specless_probe_fallback>`).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-RESEARCH.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-PATTERNS.md
@.planning/sketches/003-front-matter-rows/index.html
@.claude/skills/sketch-findings-sprinkles/references/page-shell-front-matter.md
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@app/src/styles/tokens.css
@app/src/styles/app.css
@app/src/styles/css-source.js
</context>

## Artifacts this phase produces (this plan's share)

- Tokens (`app/src/styles/tokens.css`): `--vmeta-min: 300px` (003 line 69), `--text-control-min: 24px` (003 line 32; 007 line 131), `--leading-control: 1.3` (003 line 32; 007 lines 164-166; sketch 008 line 36).
- CSS (`app/src/styles/app.css`): the top-level `@media (max-width: 1099.98px)` block with `.recipe-page`, `.pen-foot`, `.pen-foot__controls` rules; `.recipe-band__row-version` in the 759.98px block; `.ingredient-table-region` in the 600px block; `.batch-row__correct`; `.batch-row__unit--absent`; `.text-control` in the 759.98px block; `min-width: 0` on `.recipe-band`, `.ingredient-table-region`, `.method-region`, `.side-region`. Retired: `.batch-row__acts`.
- Components: `BatchRow.jsx` — Correct rendered inside `.batch-row__head` with class `text-control batch-row__correct`; `aria-controls="batch-row-later"` on the later-batches control and `id="batch-row-later"` on the panel; `<h3 className="region-name">Tasting</h3>` in `TastingReading`; the absent-value modifier `batch-row__unit batch-row__unit--absent` on every "not measured" span. `VersionRow.jsx` — `aria-controls="version-row-later"` on the later-versions control and `id="version-row-later"` on its panel.
- Tests: pins moved in `binder.test.js`, `cross-cutting.test.js`, `columns.test.js`, `BatchRow.test.jsx`, `VersionRow.test.jsx` as named per task.

<tasks>

<task type="tracer">
  <name>Task 1: The stack below 1100 and the version row's collapse — one path from token to pin (D-15; UAT items 1, 15, 16; 003 lines 47-53, 69, 71, 178)</name>
  <files>app/src/styles/tokens.css, app/src/styles/app.css, app/src/styles/binder.test.js, app/src/styles/cross-cutting.test.js, app/src/styles/columns.test.js</files>
  <read_first>
- `.planning/sketches/003-front-matter-rows/index.html` @ `0fa1015` — lines 26-53 (the world, the page grid and the stacked `.frame.narrow .page`), 66-71 (the front matter's rows, `.row-version`'s columns and the 760 container collapse), 178 (the touch and 600px blocks, and its comment on the width presets), 510-511 (`setWidth`/`relayout`: the "full" preset bug — never measure the ladder with it).
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` § "Visual treatment (from the sketch CSS)", the paragraph headed "Page shell (sketch 003 @ d89b758, for the three page-shell items)".
- `.claude/skills/sketch-findings-sprinkles/references/page-shell-front-matter.md` § "CSS Patterns".
- `03.3.1.1-RESEARCH.md` § "Pattern 6: The page-shell ladder as top-level @media blocks", § "D-03 Triage" rows 1, 15, 16, § "Common Pitfalls" 5 and 7, § "Open Questions" 4 and 5.
- `03.3.1.1-PATTERNS.md` § "app.css — page-shell ladder and 393 overflow (D-15)" and § "cross-cutting.test.js" (the media-block pins and the first-match caveat on `mediaRuleFor`).
- `app/src/styles/app.css` lines 266-304 (`.recipe-page`, `.recipe-band`, `.recipe-band__row-version`), 659-701 (the four grid regions and `.pen-foot`), 1861-1928 (the two existing `@media` blocks and their comments).
- `app/src/styles/css-source.js` (the one-level `@media` contract; `mediaRuleFor` in the suites is first-match by selector).
- `app/src/styles/binder.test.js` lines 237-269; `app/src/styles/cross-cutting.test.js` lines 49-123; `app/src/styles/columns.test.js` lines 43-54.
  </read_first>
  <action>
Wire the page-shell ladder end to end — token, rule, pin — so one real viewport crossing (1100px) changes the page and the suites prove it. Per D-01 every value below is the sketch's, cited by line; per CLAUDE.md every visual value reads a token.

1. `tokens.css`: beside the page-shell tokens (the `--col-batch-cell-min` group is the nearest neighbour, before the touch block) add `--vmeta-min: 300px` with a one-line comment citing sketch 003 line 69 (`minmax(300px, 1fr)` — the version metadata column's floor).

2. `app.css`, the desktop rules (keep each rule's existing comment register — a paragraph naming the decision, the sketch line and the reason):
   - `.recipe-band__row-version` (currently `grid-template-columns: minmax(0, var(--measure-prose)) 1fr`): becomes `minmax(0, 1.6fr) minmax(var(--vmeta-min), 1fr)` per 003 line 69 (D-15; UAT item 1). Rewrite its comment: the headnote is no longer clamped to the prose measure by the grid (`.headnote` keeps its own `max-width`, 003 line 70); the metadata column never drops below its floor.
   - `.recipe-band`, `.ingredient-table-region`, `.method-region`, `.side-region`: each gains `min-width: 0` (003 lines 49-53 put `min-width: 0` on every area so a wide table or long word cannot force a grid track past the viewport — the direct cause of the 393 band overflow STATE.md records).

3. `app.css`, a NEW top-level `@media (max-width: 1099.98px)` block placed immediately BEFORE the existing `@media (max-width: 759.98px)` block (cascade order wide → narrow; both siblings, never nested — css-source.js parses one level). Give it the same long paragraph comment shape the 759.98 block carries: the 1099.98px prelude is the sketch's `relayout()` threshold (003 line 511, `want < 1100`) expressed as a viewport max-width, a prelude literal like the two existing ones, and the fractional value keeps exactly 1100px on the desktop side. Inside it, exactly three rules:
   - `.recipe-page { grid-template-columns: 1fr; gap: var(--gap-m); grid-template-areas: 'band' 'ingredients' 'side' 'method' 'foot'; }` — 003 line 48 (the stack changes columns and gap ONLY; NO padding declaration in this block — the paper's 48px margin holds until the 600px block, 003 line 48's comment and line 178). Side before method is UAT item 16.
   - `.pen-foot { grid-template-columns: 1fr; }` and `.pen-foot__controls { grid-column: 1; }` — the foot band mirrors `.recipe-page`'s columns (app.css 680-701) and must collapse with it, or the ceremony sits in a phantom second column.

4. `app.css`, the EXISTING `@media (max-width: 759.98px)` block gains one rule after `.axis-mark__stop`: `.recipe-band__row-version { grid-template-columns: 1fr; }` — 003 line 71 collapses the row at a 760px container; D-15 fixes it at the 760px viewport (Pitfall 5: never `@container`, css-source.js rejects it). Update the block's paragraph comment with one sentence naming the addition and 003 line 71; do not otherwise rewrite that comment in this task (Task 3 rewrites its text-control sentences).

5. `app.css`, the EXISTING `@media (max-width: 600px)` block gains `.ingredient-table-region { overflow-x: auto; }` with a comment: at 393px the table's sized columns (`--col-numeric` ×3 + `--col-data`, tokens.css 152-167) sum to 368px against a 353px content width, and `table-layout: fixed; width: 100%` cannot shrink a sized column (RESEARCH Pitfall 7); the sketch draws no rule for this width, so the table scrolls inside its own region and the page itself never overflows — D-15's criterion is zero PAGE overflow. This is the planner's call on RESEARCH Open Question 4 (recorded for Mark in the phase's planning return); it is one declaration and reversible.

6. Pins, in the same task (RESEARCH Pitfall 1):
   - `binder.test.js` 248-268: the test name and comment now say three named top-level blocks; `expect(stripped.match(/@media\b/g)).toHaveLength(3)`; `allowedMedia` becomes `['(max-width: 1099.98px)', '(max-width: 759.98px)', '(max-width: 600px)']`.
   - `cross-cutting.test.js` 80-87: the 759.98 enumeration becomes the four selectors in file order (`'button, select, .ink-field, .segmented__option'`, `'.axis-mark__stops, .axis-mark__anchors'`, `'.axis-mark__stop'`, `'.recipe-band__row-version'`); rename the test ("the four rules"). Lines 119-122: the sorted conditions become `['(max-width: 1099.98px)', '(max-width: 600px)', '(max-width: 759.98px)']` (lexical sort) and the test name says three. Add, in the 600px describe, a media-scoped test for `.ingredient-table-region` (`rule.media` is `'(max-width: 600px)'`, declarations match `/overflow-x:\s*auto/`). Add a new describe "the 1099.98px block — the page stacks (sketch 003 line 48, D-15)" with three tests that filter `rules` by `r.media === '(max-width: 1099.98px)'` (NOT `mediaRuleFor`, which is first-match across blocks — PATTERNS' caveat): the block carries exactly `['.recipe-page', '.pen-foot', '.pen-foot__controls']` in order; `.recipe-page`'s declarations match `/grid-template-columns:\s*1fr/` and `/'band' 'ingredients' 'side' 'method' 'foot'/` and do NOT match `/padding/`; `.recipe-band__row-version`'s desktop rule (`ruleFor`) matches `/minmax\(0, 1\.6fr\) minmax\(var\(--vmeta-min\), 1fr\)/` and `resolveTokenPx(tokens, '--vmeta-min')` is 300.
   - `columns.test.js` 43-54: comment-only — add two sentences noting that from Phase 03.3.1.1 the page stacks below 1100px (app.css's 1099.98 block), so `tableWidthAt(1024)` is a conservative lower bound (the table has the whole content width there), and the widths above 1100 are the ones the 2fr arithmetic still describes. No assertion changes.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/styles</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c "max-width: 1099.98px" app/src/styles/app.css` prints 1, and that prelude's line number is smaller than the line number of `max-width: 759.98px` (the block sits before the touch block): `grep -n "max-width: 1099.98px\|max-width: 759.98px" app/src/styles/app.css` lists the 1099.98 line first.
- In `app.css`, the rule for `.recipe-band__row-version` outside any `@media` declares `grid-template-columns: minmax(0, 1.6fr) minmax(var(--vmeta-min), 1fr)` (003 line 69), and inside the 759.98px block declares `grid-template-columns: 1fr` (003 line 71).
- `grep -c "min-width: 0" app/src/styles/app.css` is at least 4 higher than at HEAD `2fb0aba` (one per `.recipe-band`, `.ingredient-table-region`, `.method-region`, `.side-region`; 003 lines 49-53).
- The 1099.98 block's `.recipe-page` rule contains the areas string `'band' 'ingredients' 'side' 'method' 'foot'` and no `padding` declaration (003 line 48 and 178).
- `tokens.css` declares `--vmeta-min: 300px` and `binder.test.js`'s no-bare-px gate still passes (no `300px` literal in app.css).
- `npm --prefix app test -- src/styles` passes with the three `@media` pins updated (count 3; conditions list of three) and the new 1099.98 describe green.
  </acceptance_criteria>
  <done>A real viewport below 1100px lays the page out as band, ingredients, side, method, foot at 48px padding; below 760px the version row is one column; the ingredient region scrolls inside itself at 600px and below; every value reads a token; the style suites are green with their enumerations moved.</done>
</task>

<task type="auto">
  <name>Task 2: Correct onto the Batch head line, aria-controls on both count disclosures (003 lines 83-85, 214, 238, 256; sketch 008 box-or-word rubric)</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/VersionRow.jsx, app/src/styles/app.css, app/src/ui/BatchRow.test.jsx, app/src/ui/VersionRow.test.jsx</files>
  <read_first>
- `.planning/sketches/003-front-matter-rows/index.html` @ `0fa1015` — lines 83-85 (`.row-batch .head`, `.correct { margin-left: auto }`), 214 (the later-versions disclosure with `aria-expanded` + `aria-controls`), 238 (the batch head line: region name, date, count disclosure, Correct), 256 (the comment: the row has no foot acts and no read-view Add tasting), 459-460 (`closeLists`/`toggleList` maintaining `aria-expanded`).
- `.planning/sketches/008-control-sheet/index.html` @ `0fa1015` — line 230 (the header's box-or-word rule) and the Settled values line that names the disclosure (an underlined word that opens a panel beneath it; `aria-expanded` carries the state; the word does not change).
- `03.3.1.1-RESEARCH.md` § "The page shell (sketch 003 @ d89b758) — for the D-15 plan" rows S3 and S4; § "Shared Patterns › Box or word".
- `app/src/ui/BatchRow.jsx` lines 285-412 (the component's head line), 768-783 (the foot `.batch-row__acts` block to move), 785-822 (the later-batches panel); `app/src/ui/VersionRow.jsx` lines 183-197 (the later-versions control) and 332-340 (its panel).
- `app/src/styles/app.css` lines 1396-1448 (`.batch-row__head`, `.batch-row__acts`).
- `app/src/ui/BatchRow.test.jsx` lines 155-180 (the Correct/Add tasting placement tests), 930-945 (the closed-by-default panel tests); `app/src/ui/VersionRow.test.jsx` lines 320-385 (the later-versions control pins).
  </read_first>
  <action>
Move Correct onto the head line and name both disclosure panels, exactly as 003 draws the row (per D-01; the rubric: Correct is an underlined word because it acts on the record already in view, and the two count controls are disclosures).

1. `BatchRow.jsx`: delete the foot acts block (lines 768-783 — the `{openPen === null && openBatch && (…)}` fragment whose `<div>` wraps Correct at the row's foot) and render the same button — same `ref={amendButtonRef}`, same `onClick={() => onStartAmending(openBatch)}`, same text `Correct`, same guard `openPen === null && openBatch` — as the LAST child of `<div className="batch-row__head">`, with `className="text-control batch-row__correct"`. Rewrite the moved comment: Correct stands on the head line that names the record it acts on, right-aligned like Remove tasting on 007's Tasting head (003 line 84's comment); the row has no foot acts and no read-view Add tasting (003 line 256) — the pen is the one door (03.3.1 D-01/D-03). Add `aria-controls="batch-row-later"` to the later-batches count button (line 403-410) and `id="batch-row-later"` to the `<section className="batch-row__later">` it opens; keep the conditional render (the panel exists whenever `aria-expanded` is true, which is the state `aria-controls` describes) and the closed-by-default behaviour.

2. `VersionRow.jsx`: `aria-controls="version-row-later"` on the later-versions count button (line 187-194) and `id="version-row-later"` on the `<section className="recipe-band__full-row" aria-label="Later versions">` at line 336. Nothing else in the file changes.

3. `app.css`: replace the retired foot-acts rule and its comment (lines 1441-1448, the rule the deleted `<div>` read) with `.batch-row__correct { margin-left: auto; }` under a comment citing 003 line 84 (`.row-batch .head .correct { margin-left: auto }`) and the box-or-word rubric (sketch 008 line 230). `.batch-row__head` already carries `display: flex; align-items: baseline; gap: var(--gap-s); flex-wrap: wrap` (003 line 83) — leave it. Note: the head's `.text-control` buttons inherit no `margin-top` from `.batch-margin button` because the head is outside `.batch-margin`; no neutralisation is needed here.

4. Tests. `BatchRow.test.jsx` 165-171: the test that asserts Correct "at the row's foot" now asserts Correct inside the head — the `Correct` button's index is greater than the index of `class="batch-row__head"` and smaller than the index of `class="batch-margin` (the head precedes the margin container), its tag matches `/<button[^>]*class="text-control batch-row__correct"[^>]*>Correct<\/button>/`, and the markup contains no `batch-row__acts`. Add: with two batches and one in view, the count button's tag contains `aria-expanded="false"` and `aria-controls="batch-row-later"`; the closed-by-default tests at 934-943 stay as they are. `VersionRow.test.jsx` 324: the regex `class="text-control"[^>]*>1 later version` still matches (attribute order: React emits `class` before the aria attributes; if the executor's render shows otherwise, loosen only the attribute-order assumption, never the strings) — add an assertion that the same tag contains `aria-controls="version-row-later"`.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/VersionRow.test.jsx src/ui/RecipePage.test.jsx</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'className="text-control batch-row__correct"' app/src/ui/BatchRow.jsx` prints 1, and `grep -c 'batch-row__acts' app/src/ui/BatchRow.jsx` prints 0.
- `grep -c 'aria-controls="batch-row-later"' app/src/ui/BatchRow.jsx` prints 1 and `grep -c 'id="batch-row-later"' app/src/ui/BatchRow.jsx` prints 1; `grep -c 'aria-controls="version-row-later"' app/src/ui/VersionRow.jsx` prints 1 and `grep -c 'id="version-row-later"' app/src/ui/VersionRow.jsx` prints 1.
- In the rendered read-view markup (BatchRow.test.jsx), the `Correct` button is a descendant of `.batch-row__head`, after the count control in DOM order, and no element carries the class `batch-row__acts`.
- `app.css` has a `.batch-row__correct` rule declaring `margin-left: auto` and no `.batch-row__acts` rule (`ruleFor('.batch-row__acts')` is undefined through css-source.js).
- The UI suites pass.
  </acceptance_criteria>
  <done>The read view's one door, Correct, stands right-aligned on the Batch head line; both count controls describe the panel they open; no foot acts render.</done>
</task>

<task type="auto">
  <name>Task 3: The recorded figure at the note size, the read-view tasting heading, and the text control's size and height (003 lines 32, 36, 88, 178, 247; CONTEXT "The recorded figure" and D-15 supersession list)</name>
  <files>app/src/styles/tokens.css, app/src/styles/app.css, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/styles/cross-cutting.test.js</files>
  <read_first>
- `.planning/sketches/003-front-matter-rows/index.html` @ `0fa1015` — line 32 (`.text-control`: `--type-control`, line-height 1.3, `min-height: 24px`), line 36 (`.running-head, .region-name` at the section role), line 88 (`.cell .v`: `var(--type-note)`, weight 400, pen blue, tabular, line-height 1.1; `.cell .v .absent { color: var(--ink) }`), line 89 (`.cell .v .u` at the deviation size, 400), line 178 (`@media (max-width: 760px) { .btn, .text-control, .ink-field { min-height: 44px } }`), line 242 (the Air cell: `<span class="u absent">not measured</span>`), line 247 (the read-view tasting head as a `.running-head`).
- `03.3.1.1-CONTEXT.md` § "Superseded by sketch 008" — the bullets "New, the page shell (D-15 …)" (text controls at the control size) and "The recorded figure (008 § Captions and type roles; 003 line 88)".
- `03.3.1.1-RESEARCH.md` § "Open Questions" 7 (the recorded figure, candidate D, with the pin to assert), § "The record pen" row T17, § "The page shell" rows S1, S7.
- `app/src/styles/app.css` lines 58-90 (`.text-control` and its hover/disabled rules), 897-906 (`.region-name`), 1380-1394 (`.batch-row__cell-label`, `.batch-row__cell-value`), 1416-1422 (`.batch-row__unit`), 1861-1898 (the 759.98 block and its comment — the two sentences at 1878-1880 about text controls taking no min-height are what this task overrules).
- `app/src/ui/BatchRow.jsx` lines 208-266 (`TastingReading`, the bare `<h3>` at 218 and the "not measured" spans), 676-748 (the read view's churn cells and their "not measured" spans).
- `app/src/styles/cross-cutting.test.js` lines 80-95 (the 759.98 enumeration and the `.text-control` exclusion test), 305-309 (the batch-row registers); `app/src/ui/BatchRow.test.jsx` lines 266, 319 (the `<h3>Tasting` pins), 720-740 and 845-875 (the cell regexes with `batch-row__unit`).
  </read_first>
  <action>
Three settled values from the control sheet that the page shell carries, each cited to 003:

1. `tokens.css`: in the type-roles group add `--leading-control: 1.3;` with a comment (the control role's leading: 003 line 32, 007 lines 164-166, sketch 008 line 36) and, beside it, `--text-control-min: 24px;` (003 line 32; 007 line 131 — "an underlined word is an inline action", 24px tall at desktop; 44px below 760px rides the touch block's `--touch-min`). Update the `--type-control` comment's parenthetical to name text controls among its readers.

2. `app.css` `.text-control` (lines 64-77): `font-size` becomes `var(--type-control)` (003 line 32; was `--size-small-print`), add `line-height: var(--leading-control)` and `min-height: var(--text-control-min)`. Rewrite the rule's comment to cite 003 line 32 and 007 line 131 and the D-15 supersession bullet. In the 759.98px block add `.text-control { min-height: var(--touch-min); }` as its last rule (003 line 178; 007 line 180) and REWRITE the block comment's last two sentences (lines 1878-1880, which argue that an inline text control needs no minimum height and that padding-based growth would wreck its flow) to say the opposite with the citation: the sketches' text controls are `<button>`s, inline-block, and take 24px at desktop and 44px here (003 lines 32 and 178; 007 lines 131 and 180; settled on sketch 008, 2026-09-14). A comment contradicting the rule beneath it is a defect (PATTERNS "Paragraph-comment register").

3. `app.css` `.batch-row__cell-value` (1388-1394): `font-size: var(--type-note)` and `font-weight: 400` (003 line 88 @ `0fa1015`; CONTEXT "The recorded figure", Mark's candidate D — a recorded value reads at the weight of the maker's words; the balance figure keeps `--size-figure-value`/700 in ink as the page's one heavy element — do not touch `.graduated-rule` or any figure rule), plus `line-height: var(--leading-figure)` — a new token `--leading-figure: 1.1` in tokens.css beside `--leading-control`, citing 003 line 88 (the app has no 1.1 leading token; a leading is a size and reads a token). Rewrite the rule's comment. Add `.batch-row__unit--absent { color: var(--ink); }` after `.batch-row__unit` (003 line 88's `.absent`, line 242: an absence is not a record — ink, at the unit's size; Impeccable critique 2026-09-15 P2).

4. The read view's captions need no change for D-11 (uppercase reaches the batch row's read view): `.batch-row__cell-label` (app.css 1380-1386) already reads the caption role uppercase at 0.04em, and sketch 003 came up to that role (003 line 87; RESEARCH S2) — leave the rule; the conformance pass confirms the computed style. `BatchRow.jsx`: every `<span className="batch-row__unit">not measured</span>` (eight sites: 238, 251, 258, 688, 704, 717, 724, 730 — locate by the string, not the line) becomes `<span className="batch-row__unit batch-row__unit--absent">not measured</span>`; the unit-bearing spans (` min`, ` °C`, ` g lost at 20 min`) are untouched. Line 218's `<h3>Tasting</h3>` becomes `<h3 className="region-name">Tasting</h3>` (003 line 247 draws the read-view tasting head as a running head, and 003 line 36 puts running heads and region names on the same section role; RESEARCH S1). Do NOT touch the pen's tasting head at 507-528 (Plan 02's task) and do NOT change `.batch-row__date`'s lowercase "churned …" (RESEARCH Anti-Patterns).

5. Tests. `cross-cutting.test.js` 89-94: replace the "exclusion is real" test with its inverse — inside the 759.98 block a `.text-control` rule exists and declares `min-height: var(--touch-min)` (003 line 178, 007 line 180); 80-87: the enumeration gains `'.text-control'` as its last entry (after `.recipe-band__row-version` from Task 1); 305-309: unchanged (the `.batch-row__unit` register still reads `--size-deviation-words`); add, in the type-roles describe, a test that `ruleFor('.batch-row__cell-value')` matches `/font-size:\s*var\(--type-note\)/` and `/font-weight:\s*400/` and `/color:\s*var\(--pen-blue\)/`, and that `ruleFor('.batch-row__unit--absent')` matches `/color:\s*var\(--ink\)/`; and a test that `ruleFor('.text-control')` matches `/font-size:\s*var\(--type-control\)/` and `/min-height:\s*var\(--text-control-min\)/` and that `tokens['--text-control-min']` is `'24px'`. `binder.test.js` 214-226 still passes unchanged (border/underline/hover thickness untouched). `BatchRow.test.jsx`: the regexes at 736, 739, 861, 864, 867 that match `<span class="batch-row__unit">not measured` become `class="batch-row__unit batch-row__unit--absent"`; line 319's `indexOf('<h3>Tasting')` becomes `indexOf('<h3 class="region-name">Tasting')` (line 266 is the PEN head, changed by Plan 02 — leave it); the read-view assertions at 820-830 that look for a bare `<h3>Tasting</h3>` change to `<h3 class="region-name">Tasting</h3>`.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `ruleFor('.text-control')` in app.css declares `font-size: var(--type-control)`, `line-height: var(--leading-control)` and `min-height: var(--text-control-min)`; the 759.98px block carries a `.text-control` rule declaring `min-height: var(--touch-min)`; `resolveTokenPx(tokens, '--text-control-min')` is 24 and `resolveTokenPx(tokens, '--touch-min')` is 44.
- `ruleFor('.batch-row__cell-value')` declares `font-size: var(--type-note)` and `font-weight: 400`; `ruleFor('.batch-row__unit--absent')` declares `color: var(--ink)`; `ruleFor('.batch-row__unit')` still declares `font-size: var(--size-deviation-words)` and `font-weight: 400`.
- `grep -c 'batch-row__unit batch-row__unit--absent' app/src/ui/BatchRow.jsx` prints 8 (every "not measured" span) and `grep -c '<h3 className="region-name">Tasting</h3>' app/src/ui/BatchRow.jsx` prints 1.
- The block comment above `@media (max-width: 759.98px)` no longer claims text controls take no min-height (`grep -c "take no min-height" app/src/styles/app.css` prints 0) — the sentence is replaced, not deleted.
- `npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles` passes.
  </acceptance_criteria>
  <done>A recorded figure reads 16px at weight 400 in pen blue with its unit beside it and an absence in ink; the read-view tasting heading is a region name; every text control on the page is 13px and at least 24px tall, 44px below 760px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maker input → draft state | unchanged by this plan; no field, handler or store path is added |
| stylesheet text → style-contract suites | the suites read app.css/tokens.css as text; a new at-rule type would silently misattribute rules (css-source.js) |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1.1-01 | Tampering | BatchRow.jsx caption/heading text | low | mitigate | every caption and heading word is a JSX literal; maker prose still renders as text nodes; no dangerouslySetInnerHTML (CLAUDE.md convention, grep-verifiable) |
| T-03.3.1.1-02 | Denial of service (of the page) | app.css @media contract | low | mitigate | only a top-level `@media` is added; `assertNoAtRules` in every style suite throws on anything else, so the suites, not the maker, catch a parser-breaking at-rule |
| T-03.3.1.1-03 | Information disclosure | aria-controls ids | low | accept | the ids name UI panels only; no data is exposed by an id string |
| T-03.3.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package is installed in this phase (RESEARCH "Package Legitimacy Audit": not applicable); the legitimacy gate does not fire |
</threat_model>

<verification>
- `npm --prefix app test -- src/styles` and `npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/VersionRow.test.jsx src/ui/RecipePage.test.jsx` pass; `npm --prefix app test` (the full suite) passes at the end of the plan; `npm --prefix app run build` compiles.
- The three top-level `@media` blocks are the only at-rules in app.css (binder.test.js's at-rule pin at count 3).
- The browser facts — the one-column stack below 1100, the version row's collapse below 760, zero page overflow at 393, Correct's right edge on the head line, the 16px/400 figures — are the conformance pass's (Plan 06, D-17/D-18); this plan proves the rules and the markup, not the pixels.
</verification>

<success_criteria>
UAT items 1, 15 and 16 are closed in source against sketch 003's cited lines; the 393 overflow has a mechanism whose acceptance is zero page overflow; Correct, the disclosures, the recorded figure, the read-view tasting heading and the text control's size carry the sketch's values; every new value is a token; every moved pin is green.
</success_criteria>

<output>
Create `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-SUMMARY.md` when done — name the three `@media` blocks' order, the `overflow-x: auto` decision (Open Question 4) for Mark's acknowledgement, and every pin moved.
</output>
