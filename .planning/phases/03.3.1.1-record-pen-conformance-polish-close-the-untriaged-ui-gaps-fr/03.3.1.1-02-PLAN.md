---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 02
type: execute
wave: 2
depends_on: ["03.3.1.1-01"]
files_modified:
  - app/src/styles/tokens.css
  - app/src/styles/app.css
  - app/src/styles/cross-cutting.test.js
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/Segmented.test.jsx
autonomous: true
requirements: []
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every caption in the record pen is uppercase, 12px at weight 500 with 0.04em tracking and 6px to what it captions, done by text-transform so the source text stays sentence case (007 @ 109733d lines 36 and 156; D-10; UAT item 19, pen half)"
    - "The churn row is one wrapping flex row of four content-width labels — CHURN DATE (a 128px date field), TIME TO DRAW TEMP., OUT OF MACHINE, CHURN DURATION — each numeric field a 56px box with its unit word beside it, never inside the caption (007 lines 37-44, 211-216; D-12, D-13 as amended to 56px; UAT items 2, 3, 17)"
    - "Tasted, Tempering and Tasting temperature share one field row; Melt test and Melt style share another, top-aligned (007 lines 249-253 and 285-297; UAT items 7, 8, 12)"
    - "The tasting head is a bare region name 'Tasting' with the status beside it and Remove tasting right-aligned on the same line; no helper text remains (007 lines 242-246; UAT items 6 and 18; G-03.3.1-4 stands)"
    - "Next time carries its visible caption and the four prose fields carry the sketch's e.g. placeholders verbatim; the two melt captions carry no '(optional)' suffix (007 lines 237, 238, 256, 287, 289, 307; D-12; UAT item 14)"
    - "Both save ceremonies are right-aligned and the foot status line reads at the prose measure (007 lines 55 and 172; CONTEXT D-14 and the settled-values bullet)"
  artifacts:
    - app/src/styles/app.css
    - app/src/styles/tokens.css
    - app/src/ui/BatchRow.jsx
  key_links:
    - "MeasuredField's caption span (.pen-caption) and unit span (.field-unit__unit) → the .pen-caption rule and .field-unit rules in app.css (the unit word leaves the caption text; the aria-label keeps the spelled-out unit)"
    - ".field-unit .ink-field { width: var(--field-w-figure) } → --field-w-figure: 56px in tokens.css (the same token the retired [type='number'] rule read; redefined in place)"
    - ".pen-caption → cross-cutting.test.js's caption-role list (the rule must read --type-label at weight 500 or the pin goes red)"
---

<objective>
Close the pen's caption, field-row, width and wording gaps against sketch 007 (`.planning/sketches/007-full-battery/index.html` @ `109733d`; every 007 line number below is unchanged since `2a212be`, the commit RESEARCH.md re-cited): uppercase captions with 0.04em tracking (UAT item 19, D-10), the churn row's content-width labels with the unit beside a 56px box (items 2, 3, 17; D-12, D-13), the tasting and melt field rows (items 7, 8, 12), the bare tasting head with Remove tasting right-aligned (items 6, 18), the Next time caption and the sketch's placeholders (item 14), and the "(optional)" drop (D-12). Plus two ceremony-shape rules the sketch settled and later plans' markup relies on: right-aligned ceremonies (007 line 55) and the status line at the prose measure (007 line 172).

Purpose: these are the pen's structural gaps — wrappers with no CSS at all today (`.tasting-head`, `.tasting-field-row`, `.melt-block`, `.note-block` are unstyled, RESEARCH § "Three method facts") and captions that never read the caption role. The controls (Plans 03-04) and the ceremonies (Plan 05) build on the rows and captions this plan lays down.

Output: `tokens.css` carries the sketch's field widths and gaps as tokens; `app.css` carries `.pen-caption`, `.pen-helper`, the `.field-row` family, `.tasting-head`, the ceremony alignment; `BatchRow.jsx` renders the sketch's rows and words; the render-only and style suites pass with their pins moved.

Spec-less probe fallback: skipped visibly — this phase has no requirement IDs to probe; the sketch HTML plus CONTEXT.md is the design contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-RESEARCH.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-PATTERNS.md
@.planning/sketches/007-full-battery/index.html
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@app/src/styles/tokens.css
@app/src/styles/app.css
@app/src/ui/BatchRow.jsx
</context>

## Artifacts this phase produces (this plan's share)

- Tokens (`tokens.css`): `--field-w-date` redefined to `128px` (007 line 41), `--field-w-figure` redefined to `56px` (007 line 43), `--gap-field-unit: 4px` (007 line 42), `--caption-two-lines: 2.4em` (007 line 39; sketch 008 line 49), `--leading-helper: 1.4` (007 line 169; 008 line 36).
- CSS (`app.css`): `.pen-caption` (the sketch's `.lbl`, 007 lines 36 + 156), `.axis-mark__name` restyled to the caption role (007 line 157), `.pen-helper` (the sketch's `.helper`, 007 line 169), `.field-row`, `.field-row--start`, `.field-row__label`, `.field-row__label--date`, `.field-row .pen-caption`, `.field-unit`, `.field-unit .ink-field`, `.field-unit__unit`, `.tasting-head`, `.tasting-head .region-name`, `.tasting-head__remove`, `.tasting-status` (via `.pen-helper`), `.save-ceremony { justify-content: flex-end }`, `.save-ceremony__status`, `.form-status` at the prose measure; `.field-row__label { max-width: 100% }` inside the 600px block. Retired: `.batch-margin__field .ink-field[type='number']`, `.batch-margin__field .ink-field[type='date']`.
- Components (`BatchRow.jsx`): `MeasuredField` renders caption span + `.field-unit` (input + unit span); the churn row, tasting row and melt row are `.field-row`s; the tasting head is `.tasting-head` with `h3.region-name`, `p.tasting-status.pen-helper`, the undo control, and `button.tasting-head__remove`; the `SegmentedField` legend carries `pen-caption`; the Next time label carries a `pen-caption` span; placeholders per 007 lines 237, 256, 307; `MELT_TEST_FIELD` used unmodified and the melt legend reads "Melt style".

<tasks>

<task type="tracer">
  <name>Task 1: The churn row — CHURN DATE and three 56px boxes with their units beside them, one path from token to pin (D-10, D-12, D-13; UAT items 2, 3, 17, 19; 007 lines 34-44, 156, 210-216)</name>
  <files>app/src/styles/tokens.css, app/src/styles/app.css, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/styles/cross-cutting.test.js</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 32 (`.ink-field`), 34 (`label { display: block; margin: 0 0 var(--gap-s) }`), 36 (`.lbl`), 37-44 (`.field-row`, `.field-row label`, `.field-row label > span.lbl`, `.num-field`, `.f-date`, `.field-unit`, `.field-unit .num-field` 56px with its comment, `.field-unit .u`), 139 (`.field-row label { max-width: 100% }` below 600px), 156 (`.lbl`'s computed role: `--type-label`, 500, 1.35, 0.04em), 170 (`.field-unit .u { line-height: 1.3 }`), 210-216 (the churn section's markup: region name then the four-label field row).
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` § "Visual treatment (from the sketch CSS)" → "Captions" (the `.lbl` entry) and "Field rows, date and numeric fields".
- `03.3.1.1-RESEARCH.md` § "Pattern 2: The sketch's field row, ported", § "Pattern 3: One caption rule, text-transform, source stays sentence case", § "D-03 Triage" rows 2, 3, 17, 19, § "Common Pitfalls" 4 (textareas stay out of field rows).
- `03.3.1.1-PATTERNS.md` § "tokens.css" (the token-with-rationale-comment shape; redefine `--field-w-date`/`--field-w-figure` in place), § "app.css — captions, caption lines, field rows…" (the `.batch-row__cell-label` rule to copy the shape of; the dead width rules at 1347-1361), § "BatchRow.jsx" (MeasuredField and the churn-date cell), § "BatchRow.test.jsx" (the index-order and regex-on-markup patterns).
- `app/src/styles/tokens.css` lines 24-39 (type roles), 103-108 (the two field-width tokens), 224-259 (the battery group).
- `app/src/styles/app.css` lines 1299-1361 (`.ink-field`, `.batch-margin__field`, the two width rules), 1380-1386 (`.batch-row__cell-label`, the caption role already in the app), 1914-1928 (the 600px block).
- `app/src/ui/BatchRow.jsx` lines 20-26 (`CHURN_MEASURED_FIELDS`), 48-88 (`unitWords`, `MeasuredField`), 420-449 (the churn row markup).
- `app/src/ui/BatchRow.test.jsx` lines 183-223 (the DOM-order test and its regexes on the churn date); `app/src/styles/cross-cutting.test.js` lines 178-194 and 263-281 (the caption-role and caption-gap pins).
  </read_first>
  <action>
Port the sketch's field row for the churn section end to end — tokens, rules, markup, pins — so the four churn labels sit at content width on one wrapping row with their units beside 56px boxes.

1. `tokens.css`. Redefine in place (PATTERNS "Alternatives Considered": the `ch` tokens' only consumers are battery fields): `--field-w-date: 128px;` (007 line 41) and `--field-w-figure: 56px;` (007 line 43 — "48px clipped '−12.5' and '120.5'; 56px holds both"; CONTEXT D-13's 48px is superseded by the HTML, D-01). Rewrite their comment (lines 103-106) to cite the two sketch lines and the D-13 amendment. Add beside them `--gap-field-unit: 4px;` (007 line 42, the gap between a figure box and its unit word) and `--caption-two-lines: 2.4em;` (007 line 39; sketch 008 line 49 names this token — a field-row caption reserves two lines of height so short and long captions in one row keep their fields on one baseline). In the type-roles group add `--leading-helper: 1.4;` (007 line 169; 008 line 36) beside `--leading-control`.

2. `app.css`, in the battery section (between `.pen-hint` and `.axis-mark`), each rule under a paragraph comment naming the sketch line, the contract section ("Visual treatment › Captions" / "Field rows, date and numeric fields") and the decision:
   - `.pen-caption` — the sketch's `.lbl` (007 lines 36 + 156): `display: block; font-family: var(--face-grotesk); font-size: var(--type-label); font-weight: 500; line-height: var(--leading-section); text-transform: uppercase; letter-spacing: 0.04em; margin: 0 0 var(--gap-xs);` — copy `.batch-row__cell-label`'s shape exactly (D-11: uppercase reaches the pen exactly as it already reaches the read view's cell captions; the contract's "Visual treatment (from the sketch CSS)" section, written per D-02, is the citation aid — the HTML governs, D-01); `letter-spacing` in `em` is the file's sanctioned literal (app.css 829-833). The transform keeps the source sentence case for screen readers (D-10).
   - `.axis-mark__name` (app.css 1571-1575 — the six axis names): keep `font-family` and `font-size: var(--type-label)`; `font-weight` becomes 500; add `text-transform: uppercase; letter-spacing: 0.04em;` (007 lines 76 + 157: the tenth round put the axis name at the caption's weight 500 — 600 is for region names only; D-10's "the six axis names"; UAT item 19; RESEARCH Open Question 2's residual). Rewrite the rule's comment: the weight-600 "named exception" it describes was retired with the tenth round; the axis name is a caption like any other, uppercase by transform so the source stays sentence case.
   - `.pen-helper` — the sketch's `.helper` (007 line 169): `font-family: var(--face-grotesk); font-size: var(--type-control); line-height: var(--leading-helper); font-weight: 400; font-style: normal; text-transform: none; letter-spacing: 0; color: var(--ink); margin: 0;`. A helper is always a SIBLING of the uppercase element, never inside it (cross-cutting § Type roles). Task 2 and Plans 04-05 apply it.
   - `.field-row { display: flex; gap: var(--gap-s); flex-wrap: wrap; align-items: flex-end; }` (007 line 37).
   - `.field-row__label { display: block; flex: 0 0 auto; margin: 0 0 var(--gap-s); font-family: var(--face-grotesk); font-size: var(--size-table-body); }` (007 lines 34, 38 — a label at its own content width wraps to the next line whole; the face/size are what `.batch-margin__field` gave the label before).
   - `.field-row__label--date { width: var(--field-w-date); }` (007 line 41 — the width sits on the label; `.ink-field`'s own `width: 100%` fills it).
   - `.field-row .pen-caption { display: flex; align-items: flex-end; line-height: var(--leading-table); min-height: var(--caption-two-lines); }` (007 line 39: two lines reserved, caption bottom-aligned; `--leading-table` is the app's 1.2).
   - `.field-unit { display: flex; align-items: baseline; gap: var(--gap-field-unit); }` (007 line 42); `.field-unit .ink-field { width: var(--field-w-figure); flex: 0 0 auto; text-align: right; }` (007 lines 40, 43); `.field-unit__unit { font-size: var(--size-deviation-words); white-space: nowrap; line-height: var(--leading-control); }` (007 lines 44, 170).
   - In the 600px block add `.field-row__label { max-width: 100%; }` (007 line 139).
   - Delete the two dead width rules and their comments at 1347-1361 (`.batch-margin__field .ink-field[type='number']` matches nothing — the inputs are `type="text"` — and the date width now lives on `.field-row__label--date`). Leave `.batch-margin__field` itself (the prose fields keep it, RESEARCH Pitfall 4) and `.ink-field { width: 100% }` (bounded by the 128px label and the 56px rule).

3. `BatchRow.jsx`. `MeasuredField` renders: `<label className="field-row__label"><span className="pen-caption">{field.label}</span><span className="field-unit"><input type="text" inputMode="decimal" className="ink-field" aria-label={`${field.label}, ${unitWords(field.unit)}`} …unchanged attributes… /><span className="field-unit__unit">{field.unit}</span></span>{error && <span id={errorId} className="field-error">{error}</span>}</label>` — the unit word moves out of the caption into a sibling after the input (007 lines 213-215: the root cause of item 2), the `aria-label` keeps the spelled-out unit, `type="text"` + `inputMode="decimal"` and the error wiring stay exactly as they are. The churn row: `<div className="batch-row__cells">` becomes `<div className="field-row">`; the churn-date cell becomes `<label className="field-row__label field-row__label--date"><span className="pen-caption">Churn date</span><input type="date" className="ink-field" autoFocus ref={churnDateRef} …/></label>` (007 line 212; D-12 "Churn date" replaces "churned"; uppercase comes from the rule). `.batch-row__cells` stays for the read view only. Rewrite the comments at 57-64 and 422-424 in the file's register (contract section + sketch line + decision).

4. Pins. `BatchRow.test.jsx` 189-192 and 220: the churn-date regexes become `/<label class="field-row__label field-row__label--date"><span class="pen-caption">Churn date<\/span><input[^>]*type="date"[^>]*class="ink-field"[^>]*value="2026-08-09"/` (and the `2026-08-02` variant at 220 without the class prefix requirement); the `indexOf` anchor becomes `'<span class="pen-caption">Churn date</span>'`. Add two tests in the same describe: (a) "the unit word is a sibling after the box, never in the caption" — markup matches `/<span class="pen-caption">Time to draw temp\.<\/span><span class="field-unit"><input[^>]*aria-label="Time to draw temp\., minutes"[^>]*\/><span class="field-unit__unit">min<\/span><\/span>/` and does not contain the string `Time to draw temp., min<`; (b) "the churn row is one field row holding the date label and the three measured labels" — `markup.indexOf('<div class="field-row">')` is ≥ 0 and smaller than the Churn date caption's index, and the count of `class="field-row__label` occurrences between that index and the index of `Exit consistency` is 4. `cross-cutting.test.js` 178-194: the list gains `'.pen-caption'` and `'.axis-mark__name'` (rename "the ten caption rules"); 196-205: delete the axis-name weight-600 exception test outright (the exception it names no longer exists — 007 line 157); 263-281: add an assertion that `ruleFor('.pen-caption').declarations` matches `/margin:\s*0 0 var\(--gap-xs\)/` and `/text-transform:\s*uppercase/` and `/letter-spacing:\s*0\.04em/`; add a small describe "the sketch's field row (007 lines 37-44)" asserting `ruleFor('.field-unit .ink-field')` matches `/width:\s*var\(--field-w-figure\)/`, `ruleFor('.field-row__label--date')` matches `/width:\s*var\(--field-w-date\)/`, `resolveTokenPx(tokens, '--field-w-figure')` is 56 and `resolveTokenPx(tokens, '--field-w-date')` is 128, and `ruleFor('.field-row .pen-caption')` matches `/min-height:\s*var\(--caption-two-lines\)/`.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'className="pen-caption">Churn date<' app/src/ui/BatchRow.jsx` prints 1; `grep -c 'className="field-unit__unit"' app/src/ui/BatchRow.jsx` prints 1 (inside `MeasuredField`); `grep -c "field.label}, \${field.unit}" app/src/ui/BatchRow.jsx` prints 0 (the unit is no longer concatenated into the caption).
- `tokens.css`: `--field-w-date: 128px`, `--field-w-figure: 56px`, `--gap-field-unit: 4px`, `--caption-two-lines: 2.4em`, `--leading-helper: 1.4` are declared; `binder.test.js`'s no-bare-px gate still passes (no `128px`/`56px`/`4px` literal in app.css).
- `ruleFor('.pen-caption')` reads `--type-label` at weight 500, uppercase, 0.04em, `margin: 0 0 var(--gap-xs)`; `ruleFor('.axis-mark__name')` reads `--type-label` at weight 500, uppercase, 0.04em; `ruleFor('.batch-margin__field .ink-field[type=\'number\']')` and the `[type='date']` sibling are undefined.
- The rendered recording-mode markup has one `.field-row` for the churn section whose four `.field-row__label`s carry the captions Churn date, Time to draw temp., Out of machine, Churn duration in that order, and each numeric input is followed by a `.field-unit__unit` span with `min`, `°C`, `min`.
- `npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles` passes.
  </acceptance_criteria>
  <done>The churn row renders and is styled as 007 draws it — content-width uppercase captions, a 128px date, three 56px boxes with the unit word beside each — with every value a token and every pin green.</done>
</task>

<task type="auto">
  <name>Task 2: The tasting head, the tasting and melt rows, the note eyebrow, Next time, the placeholders, the "(optional)" drop, and the ceremonies' alignment (D-10, D-12, D-14; UAT items 6, 7, 8, 12, 14, 18; 007 lines 55, 73, 118, 172, 237-258, 285-297, 307)</name>
  <files>app/src/ui/BatchRow.jsx, app/src/styles/app.css, app/src/styles/tokens.css, app/src/ui/BatchRow.test.jsx, app/src/ui/Segmented.test.jsx</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 55 (`.pen .saves`: flex, gap 12, `justify-content: flex-end`, and the `#record-status { margin-right: auto }` rule), 73 + 118 (`.head`: flex, baseline, gap 12, wrap), 172 (`#form-status` at the prose measure, line-height 1.5), 237-238 (At the machine / Ingredient notes with their placeholders), 242-246 (the tasting head @ `109733d`: `margin-bottom: var(--gap-xs)`; the bare `h2.region-name` "Tasting" with `margin-bottom: 0`; the `.helper` status; the head undo slot; Remove/Clear tasting with `margin-left: auto`), 249-253 (the tasting field row), 255-257 (the note block and its placeholder), 285-297 (the melt row with `align-items: flex-start`, Melt test with its unit, the Melt style category), 307 (Next time's caption and placeholder), 141 (`.pen .saves { flex-wrap: wrap }` below 600px — the app's 600 block already wraps `.save-ceremony`).
- The contract § "Visual treatment" → "Pen shell and section rhythm" (the tasting head is a plain `.head`, no reserved min-height), "Prose fields" (placeholders verbatim), "Buttons and text controls" (the two ceremonies), "Superseded by the ninth round" (the tasting head's helpers are gone; "(optional)" is dropped).
- `03.3.1.1-RESEARCH.md` § "Pattern 4: The tasting head as the sketch's .head", § "Pattern 5" (only the `.save-ceremony` CSS lines; the props are Plan 05's), § "Re-citation against commit 977846a" items 3 and 6, § "D-03 Triage" rows 6, 7, 8, 12, 14, 18, § "The record pen" row T16.
- `03.3.1.1-PATTERNS.md` § "BatchRow.jsx" (tasting head, melt block, Next time) and § "Segmented.test.jsx".
- `app/src/ui/BatchRow.jsx` lines 28-46 (`TASTING_MEASURED_FIELDS`, `MELT_TEST_FIELD` and its "(optional)" comment), 90-105 (`SegmentedField`), 462-487 (At the machine / Ingredient notes), 495-565 (the tasting head, the tasting field row, the note block), 629-668 (the melt block, ceremony A, Next time).
- `app/src/styles/app.css` lines 1762-1785 (`.save-ceremony`, `.save-ceremony__hint`, `.form-status`), and the battery section where Task 1 placed `.pen-caption`/`.pen-helper`/`.field-row`.
- `app/src/ui/BatchRow.test.jsx` lines 266-268, 285-292, 316-320, 514-528, 629-645, 679-690, 700-715; `app/src/ui/Segmented.test.jsx` lines 46-63.
  </read_first>
  <action>
The rest of the pen's rows and words, each to its sketch line:

1. The tasting head (`BatchRow.jsx` 507-528; 007 lines 242-246; UAT items 6 and 18). The `<h3>` becomes `<h3 className="region-name">Tasting</h3>` with NO helper spans (the two helper spans inside the heading at lines 509-510 — "· optional" and "— leave anything you did not record blank" — and their class go; G-03.3.1-4 settled the bare caption and the ninth round removed the sketch's helpers). The status `<p>` becomes `<p className="tasting-status pen-helper" role="status" aria-live="polite">` (007 line 244 is a `.helper`). The undo button keeps its class and text for now (Plan 05 renames it). The Remove tasting button gains the class `text-control tasting-head__remove` (007 line 246, `margin-left: auto`). DOM order is already the sketch's; keep it. Rewrite the comment at 497-506. In `app.css` add, after `.pen-helper`: `.tasting-head { display: flex; align-items: baseline; gap: var(--gap-s); flex-wrap: wrap; margin-bottom: var(--gap-xs); }` (007 lines 73, 118, 242 — a plain `.head`; NO `min-height`, that reserve is the axes' and categories' caption lines, 007 line 75); `.tasting-head .region-name { margin: 0; }` (007 line 243); `.tasting-head__remove { margin-left: auto; }` (007 line 246). The `<h3>` is a `.region-name` now, so the section role (14px/600/uppercase/bookcloth) comes from the existing rule at app.css 897-906 — this is what fixes "the hint styled the same as Tasting": the `h3` had no rule at all.

2. The tasting field row (529-552; 007 lines 249-253; items 7, 8): `<div className="tasting-field-row">` becomes `<div className="field-row">`; the Tasted label becomes `<label className="field-row__label field-row__label--date"><span className="pen-caption">Tasted</span><input type="date" className="ink-field" ref={tastedDateRef} …/></label>`; the two `MeasuredField`s already render field-row labels (Task 1). Delete the `.tasting-field-row` class (it never had a rule).

3. The note block (553-565; 007 line 256): `<p className="note-block__eyebrow pen-caption">How did it turn out?</p>` (the caption role; the app keeps its `<p>` + `aria-label` structure); the textarea's placeholder becomes `e.g. flavor, texture, what stood out` (verbatim). At the machine (466-474) gains `placeholder="e.g. bowl frozen overnight"` (007 line 237); Ingredient notes keeps `e.g. oil bottle opened 24 Jul` (007 line 238).

4. The melt row (632-648; 007 lines 285-297; item 12; D-12): the `.melt-block` div wraps `<div className="field-row field-row--start">` holding, in order, `<MeasuredField field={MELT_TEST_FIELD} …/>` (the `{ ...MELT_TEST_FIELD, label: 'Melt test (optional)' }` override is removed — the domain label is already "Melt test"; rewrite the comment at 40-45) and `<SegmentedField legend="Melt style" …/>` (D-12; 007 line 289). In `app.css` add `.field-row--start { align-items: flex-start; }` (007 line 286's inline style). In `SegmentedField` (98-105) give the `<legend>` `className="pen-caption"` so all three segmented captions take the caption role (007 lines 220, 228, 289 are `.lbl`s; Plan 04 replaces the legend with the caption line, this is the interim that already reads right).

5. Next time (657-668; 007 line 307; item 14; D-12): the label's first child becomes `<span className="pen-caption">Next time</span>`; keep the `aria-label="Next time"` (the tests anchor on it); the placeholder becomes `e.g. churn 2 min longer`.

6. The ceremonies' alignment and the status line (007 lines 55, 172; D-14 "right-aligned at every width" via the ninth round; the settled-values bullet "the status line at the prose measure (65ch)"): `.save-ceremony` gains `justify-content: flex-end;` (rewrite its comment to cite 007 line 55 and D-14); add `.save-ceremony__status { margin-right: auto; }` under a comment citing 007 line 55's `#record-saves > #record-status` rule (the removal toast sits at the left of the end-of-record row; the controls stay flush right even when it is empty; Plan 05 renders the element); `.form-status` gains `max-width: var(--measure-prose); line-height: var(--leading-note);` (007 line 172 — 65ch, the status line's measure; the prose field keeps its own 70ch, the two are deliberately different). Leave `.save-ceremony`'s `margin-top` and `.batch-margin__field`'s spacing as they are — the row-to-row rhythm beyond the 18 is the conformance pass's log (D-19).

7. Pins. `BatchRow.test.jsx`: 266 → `expect(markup).toMatch(/<h3 class="region-name">Tasting<\/h3>/)` and `expect(markup).not.toContain('tasting-head__helper')`; 290 → `placeholder="e.g. flavor, texture, what stood out"`; 316-320 (the head order test): the status paragraph's class is now `tasting-status pen-helper` — update the regex; 514-528: the aria-label strings become `Melt test, g lost at 20 min` and `role="radiogroup" aria-label="Melt style"`, and the test names drop "(optional)"; 679-690: At the machine now HAS `placeholder="e.g. bowl frozen overnight"` (invert that assertion), Next time's placeholder is `e.g. churn 2 min longer`. Add tests: the tasting row is one `.field-row` whose first label is `field-row__label field-row__label--date` with caption `Tasted` and whose Tempering and Tasting temperature labels follow inside it (indexOf ordering between `Tasted` and `How did it turn out?`); the melt block contains `class="field-row field-row--start"` holding the Melt test label before the Melt style fieldset; the Remove tasting button's tag matches `/<button type="button" class="text-control tasting-head__remove"[^>]*>Remove tasting<\/button>/` and is the last child of `.tasting-head` (its index is greater than the status paragraph's); the Next time label's first child is `<span class="pen-caption">Next time</span>`. `Segmented.test.jsx` 48, 57, 59: `'Melt style (optional)'` becomes `'Melt style'` (the group label passes through to `aria-label`).
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/Segmented.test.jsx src/ui/RecipePage.test.jsx src/styles</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'tasting-head__helper' app/src/ui/BatchRow.jsx` prints 0; `grep -c '<h3 className="region-name">Tasting</h3>' app/src/ui/BatchRow.jsx` prints 2 (the pen head and the read view's, the latter from Plan 01).
- `grep -c 'className="text-control tasting-head__remove"' app/src/ui/BatchRow.jsx` prints 1; `grep -c 'className="field-row field-row--start"' app/src/ui/BatchRow.jsx` prints 1; `grep -c 'legend="Melt style"' app/src/ui/BatchRow.jsx` prints 1; `grep -c 'className="pen-caption">Next time<' app/src/ui/BatchRow.jsx` prints 1.
- `grep -c 'placeholder="e.g. bowl frozen overnight"\|placeholder="e.g. flavor, texture, what stood out"\|placeholder="e.g. churn 2 min longer"\|placeholder="e.g. oil bottle opened 24 Jul"' app/src/ui/BatchRow.jsx` prints 4.
- `ruleFor('.tasting-head')` declares `display: flex`, `align-items: baseline`, `flex-wrap: wrap` and `margin-bottom: var(--gap-xs)` and does NOT declare `min-height`; `ruleFor('.tasting-head__remove')` declares `margin-left: auto`; `ruleFor('.save-ceremony')` declares `justify-content: flex-end`; `ruleFor('.save-ceremony__status')` declares `margin-right: auto`; `ruleFor('.form-status')` declares `max-width: var(--measure-prose)`.
- The UI, Segmented, RecipePage and style suites pass.
  </acceptance_criteria>
  <done>The tasting head is a bare region name with Remove tasting flush right; Tasted/Tempering/Tasting temperature share a row and Melt test/Melt style share another; every prose field carries its e.g. placeholder; Next time has its caption; no "(optional)" survives; both ceremonies right-align and the status line reads at 65ch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maker input → draft state | unchanged: the same handlers, keys and validation (`parseMeasuredDraft` untouched); only the wrapper markup and caption words change |
| stylesheet text → style-contract suites | no new at-rule; only rules inside the existing 600px block and top-level rules |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1.1-04 | Tampering | caption and placeholder text (BatchRow.jsx) | low | mitigate | every caption, unit word and placeholder is a JSX literal; maker prose renders as text nodes; no dangerouslySetInnerHTML |
| T-03.3.1.1-05 | Denial of service (of a field) | MeasuredField's accessible name | low | mitigate | the `aria-label` keeps the spelled-out unit ("Time to draw temp., minutes") when the visible caption loses ", min"; the field-error `aria-describedby` wiring is unchanged and pinned |
| T-03.3.1.1-06 | Information disclosure | placeholders | low | accept | placeholders are examples, never defaults; a blank field still saves blank (unchanged) |
| T-03.3.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed in this phase; the legitimacy gate does not fire |
</threat_model>

<verification>
- `npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/Segmented.test.jsx src/ui/RecipePage.test.jsx src/styles` passes; `npm --prefix app test` passes at the end of the plan; `npm --prefix app run build` compiles.
- The pixel facts — 128px and 56px boxes, the 6px caption gap, the two-line reserve, the head's single line at every D-16 width — are Plan 06's to measure in both tabs (D-17/D-18).
</verification>

<success_criteria>
UAT items 2, 3, 6 (pen half), 7, 8, 12, 14, 17, 18 and 19 (pen half) are closed in source against 007's cited lines; every caption is the caption role by transform; every field width, gap and leading is a token; the ceremonies right-align; every pin is green.
</success_criteria>

<output>
Create `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-SUMMARY.md` when done — list every new class and token with its sketch line, and every pin moved.
</output>
