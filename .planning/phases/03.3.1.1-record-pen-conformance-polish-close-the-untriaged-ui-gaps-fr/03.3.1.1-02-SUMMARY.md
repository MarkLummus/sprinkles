---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 02
subsystem: ui
tags: [css, tokens, record-pen, sketch-conformance, captions, field-rows, batch-row]

# Dependency graph
requires:
  - phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr (plan 01)
    provides: the page-shell ladder, .text-control sizing (--text-control-min), the recorded-figure and read-view tasting-heading roles this plan's field-row/caption work builds on
provides:
  - "--field-w-date (128px) and --field-w-figure (56px) redefined in place, plus --gap-field-unit (4px), --caption-two-lines (2.4em), --leading-helper (1.4)"
  - ".pen-caption (the sketch's uppercase .lbl role, 0.04em tracking, 6px gap), .pen-helper (the sibling helper role), and the .field-row family (.field-row, .field-row__label, .field-row__label--date, .field-row .pen-caption, .field-unit, .field-unit .ink-field, .field-unit__unit, .field-row--start)"
  - "The churn row (Churn date, Time to draw temp., Out of machine, Churn duration) and the tasting field row (Tasted, Tempering, Tasting temperature) as .field-rows with content-width labels and 56px numeric boxes carrying their unit word beside them"
  - "The melt row (Melt test, Melt style) as a top-aligned field row with the '(optional)' suffix dropped from both captions"
  - "The tasting head as a bare 'Tasting' region-name with no residual helper spans, and Remove tasting right-aligned via .tasting-head__remove"
  - "The sketch's verbatim placeholders on At the machine, How did it turn out?, and Next time; Next time's own visible caption"
  - "Both save ceremonies right-aligned (.save-ceremony { justify-content: flex-end }), .save-ceremony__status (margin-right: auto, for Plan 05 to render), and .form-status at the 65ch prose measure"
  - ".axis-mark__name and SegmentedField's <legend> read the .pen-caption role (weight 500 uppercase, down from the retired weight-600 exception)"
affects: [03.3.1.1-03, 03.3.1.1-04, 03.3.1.1-05, 03.3.1.1-06]

# Actuals (#2632)
actuals:
  tokens: 11041
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "The sketch's field-row shape (label at content width, wrapping flex row, bottom-aligned) replaces the app's own fixed grid for every measured/date field in the record pen; .batch-row__cells stays only for the read view."
    - "A control's caption is always .pen-caption (uppercase by text-transform, source stays sentence case); a helper is always a sibling .pen-helper element, never nested inside the caption."
    - "The two dead .batch-margin__field .ink-field[type='number'/'date'] width rules are retired now that widths live on .field-row__label--date and .field-unit .ink-field."

key-files:
  created: []
  modified:
    - app/src/styles/tokens.css
    - app/src/styles/app.css
    - app/src/styles/cross-cutting.test.js
    - app/src/ui/BatchRow.jsx
    - app/src/ui/BatchRow.test.jsx
    - app/src/ui/Segmented.test.jsx

key-decisions:
  - "--field-w-date and --field-w-figure are redefined in place (not added as new tokens) per PATTERNS.md's own instruction; a third, non-battery consumer was discovered during execution — .versions__ceremony-field .ink-field[type='date'] (the recipe-version plan/amend/tasting ceremony's own date field, app.css line ~516) — which now also reads 128px instead of its prior ~12ch (~96px). This is outside this plan's scope (sketch 007/the record pen) and outside sketch 003's own ceremony rule (`.ceremony .ink-field { width: 100% }`, which the app already diverges from pre-existing this plan). No test pins its pixel width, and the widening is minor; flagged for Plan 06's conformance pass / Mark's awareness rather than treated as a blocker."

patterns-established:
  - "Pattern: a shared token redefined 'in place' must be grepped for ALL consumers across app.css before assuming the plan's own consumer count — PATTERNS.md's 'the ch tokens' only consumers are battery fields' claim was one consumer short."

requirements-completed: []

coverage:
  - id: D1
    description: "Every caption in the record pen is uppercase, 12px at weight 500 with 0.04em tracking and 6px to what it captions, by text-transform (source stays sentence case) — 007 lines 36 + 156; D-10; UAT item 19 (pen half)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/cross-cutting.test.js#the ten caption rules read --type-label at weight 500"
        status: pass
      - kind: unit
        ref: "app/src/styles/cross-cutting.test.js#.pen-caption carries the sketch's own uppercase/tracking/gap declarations (sketch 007 @ 2a212be lines 36 + 156, D-10)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The churn row is one wrapping flex row of four content-width labels — Churn date (128px), Time to draw temp., Out of machine, Churn duration — each numeric field a 56px box with its unit word beside it, never inside the caption (007 lines 37-44, 211-216; D-12, D-13 as amended to 56px; UAT items 2, 3, 17)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders the unit word as a sibling after the box, never in the caption (sketch 007 lines 42-44, UAT item 2)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders the churn row as one field row holding the date label and the three measured labels (sketch 007 lines 210-216)"
        status: pass
      - kind: unit
        ref: "app/src/styles/cross-cutting.test.js#the sketch's field row (007 @ 2a212be lines 37-44, D-13)"
        status: pass
    human_judgment: true
    rationale: "The rule text, token values and markup grouping are proven; the rendered 128px/56px pixel facts and the 6px caption gap are browser measurements Plan 06's conformance pass performs (D-17/D-18), per this plan's own <verification> section."
  - id: D3
    description: "Tasted, Tempering and Tasting temperature share one field row; Melt test and Melt style share another, top-aligned (007 lines 249-253 and 285-297; UAT items 7, 8, 12)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders the tasting row as one field row: the Tasted date label first, then Tempering and Tasting temperature, before the note (sketch 007 lines 249-253)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders the melt block as one top-aligned field row holding Melt test before the Melt style fieldset"
        status: pass
    human_judgment: false
  - id: D4
    description: "The tasting head is a bare region name 'Tasting' with the status beside it and Remove tasting right-aligned on the same line; no helper text remains (007 lines 242-246; UAT items 6 and 18; G-03.3.1-4 stands)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders a bare \"Tasting\" region-name heading, with no residual helper spans (sketch 007 line 243; G-03.3.1-4; UAT items 6, 18)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders Remove tasting as a right-aligned text control, the last child of the tasting head (sketch 007 line 246)"
        status: pass
    human_judgment: true
    rationale: "The DOM order and class wiring (.tasting-head { no min-height }, .tasting-head__remove { margin-left: auto }) are proven as text; the actual rendered single-line, right-aligned layout is a browser fact for Plan 06's conformance pass."
  - id: D5
    description: "Next time carries its visible caption and the four prose fields carry the sketch's e.g. placeholders verbatim; the two melt captions carry no '(optional)' suffix (007 lines 237, 238, 256, 287, 289, 307; D-12; UAT item 14)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders At the machine and Ingredient notes with the sketch's own verbatim placeholders (sketch 007 lines 237-238)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders the shared Next time textarea with its own caption and the sketch's verbatim placeholder (sketch 007 line 307; UAT item 14)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders the note block's eyebrow at the caption role and the sketch's own verbatim placeholder, dir=\"auto\" (sketch 007 line 256)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders Melt test with its own unit, after the defects row — the \"(optional)\" suffix is dropped"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#renders Melt style as a radiogroup with the contract's own three options — the \"(optional)\" suffix is dropped"
        status: pass
    human_judgment: false
  - id: D6
    description: "Both save ceremonies are right-aligned and the foot status line reads at the prose measure (007 lines 55 and 172; CONTEXT D-14 and the settled-values bullet)"
    requirement: null
    verification:
      - kind: manual_procedural
        ref: "grep on app/src/styles/app.css: .save-ceremony { justify-content: flex-end }, .save-ceremony__status { margin-right: auto }, .form-status { max-width: var(--measure-prose) }"
        status: pass
    human_judgment: true
    rationale: "No automated style-contract test pins these three declarations (the plan's own Task 2 action text scoped cross-cutting.test.js edits to Task 1 only); verified manually via the plan's acceptance criteria greps during execution. The actual rendered right-aligned layout at every width is Plan 06's browser measurement."

duration: 22min
completed: 2026-09-15
status: complete
---

# Phase 03.3.1.1 Plan 02: The pen's captions, field rows, widths and words against sketch 007 Summary

**Every record-pen caption reads the sketch's uppercase 0.04em role by text-transform; the churn, tasting and melt rows are content-width flex field-rows with 128px dates and 56px figures whose unit word sits beside the box; the tasting head is bare "Tasting" with Remove tasting right-aligned; every prose field carries its e.g. placeholder verbatim with no "(optional)" survivors; both save ceremonies right-align and the foot status reads at 65ch**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-15T12:20:00Z
- **Completed:** 2026-09-15T12:42:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- `tokens.css`: `--field-w-date`/`--field-w-figure` redefined in place to 128px/56px (D-13 amended by the live HTML per D-01); `--gap-field-unit` (4px), `--caption-two-lines` (2.4em), `--leading-helper` (1.4) added
- `app.css`: `.pen-caption` (the sketch's `.lbl`), `.pen-helper`, the `.field-row` family (`.field-row`, `.field-row__label`, `.field-row__label--date`, `.field-row .pen-caption`, `.field-unit`, `.field-unit .ink-field`, `.field-unit__unit`, `.field-row--start`), `.tasting-head`/`.tasting-head .region-name`/`.tasting-head__remove`, `.axis-mark__name` down to weight 500 uppercase, `.save-ceremony { justify-content: flex-end }`, `.save-ceremony__status`, `.form-status` at 65ch; the two dead `.batch-margin__field .ink-field[type='number'/'date']` rules removed
- `BatchRow.jsx`: `MeasuredField` renders a caption span plus a `.field-unit` wrapper (input + unit span, unit out of the caption); the churn row, tasting row and melt row are `.field-row`s; the tasting head is bare "Tasting" with the two helper spans removed and Remove tasting right-aligned; every prose field's placeholder matches the sketch verbatim; the "(optional)" suffix drops from Melt test/Melt style; Next time gets its own `.pen-caption` span; `SegmentedField`'s `<legend>` reads the caption role (all three segmented controls)
- Style and render-only test pins moved to match, with four new assertions covering the unit-sibling fact, the field-row grouping, the tasting field-row order, and the melt block's field-row-with-Clear-fieldset order

## Task Commits

Each task was committed atomically:

1. **Task 1: The churn row — CHURN DATE and three 56px boxes with their units beside them, one path from token to pin** - `1b35aa5` (feat)
2. **Task 2: The tasting head, the tasting and melt rows, the note eyebrow, Next time, the placeholders, the "(optional)" drop, and the ceremonies' alignment** - `67f0ac9` (feat)

**Plan metadata:** committed separately with this SUMMARY.

## Files Created/Modified
- `app/src/styles/tokens.css` - `--field-w-date`/`--field-w-figure` redefined, `--gap-field-unit`/`--caption-two-lines`/`--leading-helper` added
- `app/src/styles/app.css` - `.pen-caption`, `.pen-helper`, the field-row family, `.tasting-head`/`.tasting-head__remove`, `.axis-mark__name`, `.save-ceremony`/`.save-ceremony__status`/`.form-status`, the 600px block gains `.field-row__label { max-width: 100% }`, the two dead width rules removed
- `app/src/styles/cross-cutting.test.js` - the caption-role list grows to ten and drops the axis-name weight-600 exception test; a new `.pen-caption` assertion and a new "the sketch's field row" describe block
- `app/src/ui/BatchRow.jsx` - `MeasuredField`, the churn row, the tasting head/field-row/note block, the melt block, Next time, `SegmentedField`'s legend
- `app/src/ui/BatchRow.test.jsx` - pins moved for the new markup; six new tests
- `app/src/ui/Segmented.test.jsx` - `'Melt style (optional)'` → `'Melt style'` at the three call sites

## Decisions Made
- Redefining `--field-w-date`/`--field-w-figure` "in place" (per PATTERNS.md's own instruction, since the token's only stated consumers were battery fields) turned out to have a third consumer — `.versions__ceremony-field .ink-field[type='date']`, the recipe-version ceremony's own date field — which now also widens to 128px. Recorded under key-decisions above for Plan 06's/Mark's awareness; not treated as a blocker since no test pins the old width and sketch 003's own ceremony rule is full-width anyway (a pre-existing app/sketch divergence this plan did not introduce).
- No architectural changes; both tasks executed as planned, including the tracer feedback gate after Task 1 (automated-only `<verify>`, `human_verify_mode: end-of-phase`, interactive run — re-ran the verify command, it passed, logged and continued to Task 2 per the checkpoints.md precedence chain, row 3, with no checkpoint).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed the melt-block test assertion broken by Task 1's own MeasuredField restructure**
- **Found during:** Task 1, first `npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles` run
- **Issue:** `MeasuredField`'s caption/unit restructure (Task 1's own change, applying to every `MeasuredField` use including the melt block, which Task 2 has not yet touched) broke the pre-existing assertion `expect(markup).toContain('Melt test (optional), g lost at 20 min')` — the old concatenated `${field.label}, ${field.unit}` string no longer exists once the unit moves into a sibling `.field-unit__unit` span.
- **Fix:** Updated the assertion to check for the new caption span and unit span separately (`'<span class="pen-caption">Melt test (optional)</span>'` and `'<span class="field-unit__unit">g lost at 20 min</span>'`), keeping the "(optional)" wording intact since Task 2 (not yet run) owns dropping it.
- **Files modified:** `app/src/ui/BatchRow.test.jsx`
- **Verification:** `npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles` passes (194/194)
- **Committed in:** `1b35aa5` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — a test pin broken by this plan's own Task 1 change, corrected in the same task's commit; Task 2 then completed the wording change the fix deliberately deferred).
**Impact on plan:** No scope creep — the fix kept the "(optional)" wording exactly where Task 1 found it, and Task 2 finished the wording change on schedule per its own action text.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `npm --prefix app test` (910 tests) and `npm --prefix app run build` both pass at the end of the plan.
- Plans 03-05 (the shared control rules, the segmented caption line + defects groups, the ceremony wording) can proceed — this plan's `.pen-caption`/`.pen-helper`/`.field-row` foundation and the tasting-head/melt-row markup are the base they build on.
- Plan 06 (the conformance pass) needs to confirm the browser facts this plan's tests cannot: the actual rendered 128px/56px field widths, the 6px caption gap, the two-line caption reserve, the tasting head's single line at every D-16 width, and the right-aligned ceremonies.
- Mark should be aware of the `.versions__ceremony-field` date-field widening (128px, up from ~12ch) recorded under key-decisions above — a side effect of redefining the shared token in place, outside this plan's own sketch-007 scope.

## Self-Check: PASSED

- Key files verified on disk: `app/src/styles/tokens.css`, `app/src/styles/app.css`, `app/src/styles/cross-cutting.test.js`, `app/src/ui/BatchRow.jsx`, `app/src/ui/BatchRow.test.jsx`, `app/src/ui/Segmented.test.jsx`, this SUMMARY.md — all FOUND.
- Commits verified in `git log --oneline`: `1b35aa5` (Task 1), `67f0ac9` (Task 2) — both FOUND.
- Both tasks' `<acceptance_criteria>` re-run and PASS (grep/token/CSS-rule checks shown inline during execution).
- Plan-level `<verification>`: `npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/Segmented.test.jsx src/ui/RecipePage.test.jsx src/styles` passes (262/262); `npm --prefix app test` (full suite) passes (910/910); `npm --prefix app run build` compiles.

---
*Phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr*
*Completed: 2026-09-15*
