---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 03
type: execute
wave: 3
depends_on: ["03.3.1.1-01", "03.3.1.1-02"]
files_modified:
  - app/src/styles/tokens.css
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - app/src/ui/Segmented.jsx
  - app/src/ui/Segmented.test.jsx
  - app/src/ui/RecipePage.jsx
autonomous: true
requirements: []
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A picked stop or segment option is a solid pen-blue box with ground text and a pen-blue border, nothing bold, nothing underlined, no outline; a marked defect is a pen-blue 13px square before an ink word (007 @ 109733d lines 49, 83, 88, 129; D-04 as amended, D-05, D-09; UAT items 4, 10, 11, 13)"
    - "Tabbing into an axis lands on its first (or picked) stop and a 2px solid ink ring, offset 2px, draws on the visible box, not on the invisible radio (007 lines 24 and 153; D-07; UAT item 9)"
    - "Hovering a stop, an option, a defect's square or a button thickens its border to a whole 2px in place with nothing moving; a hovered joined box sits above its neighbours (z-index 2) and the picked one at z-index 1 (007 lines 28, 50, 82, 88; D-06 as amended)"
    - "The five stops are joined: 38px cells sharing four hairlines on the 186px track, 44px cells on the 216px track below 760px (007 lines 80-81, 124, 177-178; UAT item 10)"
    - "A joined group is a radio: a second click on the picked stop or option changes nothing; Clear is the only way back, and a keyboard-activated Clear lands focus on the first cell while a mouse Clear leaves the tab position on Clear (007 lines 362 and 379; CONTEXT D-04 supersession; sketch 008 'A joined group is a radio')"
    - "Every axis head reserves 24px (44px below 760px) marked or not, centred, with Clear right-aligned, so picking moves nothing (007 lines 75, 131, 158, 180)"
    - "Under forced colours a picked box keeps a Highlight fill through forced-color-adjust: none and its focus ring takes CanvasText (007 lines 143-145; CONTEXT D-05 as amended)"
  artifacts:
    - app/src/styles/tokens.css
    - app/src/styles/app.css
    - app/src/ui/AxisMark.jsx
    - app/src/ui/Segmented.jsx
    - app/src/ui/RecipePage.jsx
  key_links:
    - "--focus-outline-width: var(--rule-hover) in tokens.css → the global :focus-visible rule (app.css 21-24) and the new focus-on-box rule; binder.test.js's focus-width pin must read --rule-hover"
    - "the shared fill selector (.axis-mark__stop:has(input[type='radio']:checked), .segmented__option:has(input[type='radio']:checked)) → the forced-colours block's fill rule (same selectors; a selector missing from one leaves that control unfilled in one mode)"
    - "AxisMark.jsx's click handler (no toggle-off) + RecipePage.jsx's handleChangeSegment (no toggle-off) → the per-control Clear (AxisMark's today; Segmented's in Plan 04) — Pitfall 11: the radio rule and a reachable Clear must land together, which is why this plan removes the axis toggle only where Clear already exists and prepares handleClearSegment for Plan 04's Clear"
    - ".axis-mark__head, .segmented-field__head { min-height: var(--caption-line-h) } → cross-cutting.test.js's 759.98 enumeration and its axis-head gap pin (the selector is grouped; ruleFor must use the grouped string)"
---

<objective>
Close the control-treatment gaps against sketch 007 (`.planning/sketches/007-full-battery/index.html` @ `109733d`; every 007 line number below is unchanged since `2a212be`): the picked state as a fill (UAT items 4, 10, 11, 13; D-04, D-05, D-09), the focus ring on the visible stop box (item 9; D-07), the whole-2px hover and ring (D-06/D-07 as amended by sketch 008), the joined stops (item 10 as the tenth round now draws it), the radio rule with a keyboard-guarded Clear, the caption line that reserves Clear's height on the axes (and the rules for the segmented caption line Plan 04's markup lands on), the axes grid's own mechanism for keeping Clear off the next column, and the forced-colours fill.

Purpose: these three rules — one fill, one ring, one hover weight — are the phase's centre of gravity (RESEARCH Pattern 1); the 03.3.1 build certified the opposite treatment against a prose derivation. The joined stops and the radio rule are the tenth round's redraw of the same controls and land here so the pass compares one shape.

Output: `tokens.css` gains `--rule-hover`, repoints `--focus-outline-width`, redefines `--stop-w`/`--touch-stop-width`, retires `--stop-gap`, `--gap-chip-row`, `--declared-flaw-gap`, and gains `--seg-pad-y/x`, `--size-defect-mark`, `--gap-defect-col`, `--caption-line-h`, `--caption-line-h-touch`; `app.css` carries the shared fill, the focus-on-box rule, the 2px hovers, the joined stops, the caption lines, the defect square-and-word rules, and its fourth top-level `@media` block (`forced-colors: active`); `AxisMark.jsx` and `Segmented.jsx` lose toggle-off; `RecipePage.jsx` stores a pick without toggling and gains `handleClearSegment`; every pin moves with its rule.

Spec-less probe fallback: skipped visibly — no requirement IDs to probe; the sketch HTML plus CONTEXT.md is the design contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-RESEARCH.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-PATTERNS.md
@.planning/sketches/007-full-battery/index.html
@.planning/sketches/008-control-sheet/index.html
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@app/src/styles/tokens.css
@app/src/styles/app.css
@app/src/styles/binder.test.js
@app/src/styles/cross-cutting.test.js
</context>

## Artifacts this phase produces (this plan's share)

- Tokens: `--rule-hover: 2px`; `--focus-outline-width: var(--rule-hover)`; `--seg-pad-y: 5px`, `--seg-pad-x: 8px`; `--size-defect-mark: var(--size-mark-stop)`; `--gap-defect-col: var(--gap-m)`; `--stop-w: 38px`; `--touch-stop-width: 44px`; `--caption-line-h: var(--text-control-min)`; `--caption-line-h-touch: var(--touch-min)`. Retired: `--stop-gap`, `--gap-chip-row`, `--declared-flaw-gap`.
- CSS: the shared fill rule; the focus-on-box rule; `.axis-mark__stop:hover`; `.segmented__option:hover`; `.segmented { flex-wrap: wrap; row-gap }`; the joined `.axis-mark__stop` (margin-right, flex-basis, `:last-child`); `.axis-mark__head, .segmented-field__head` (the caption line); `.segmented-field`, `.segmented-field__caption`, `.batch-margin .segmented-field__clear` (rules for Plan 04's markup); `.axes-grid { column-gap: 0 }` with `.axis-mark--core { padding-right }` / `.axis-mark--declared { padding-left }`; the defect square-and-word rules on `.batch-margin .chip-toggle` and `.chip-toggle::before`; `.defects-row__chips` with the sketch's gaps; the `@media (forced-colors: active)` block; `button:hover` at `--rule-hover`. Retired rules: the three weight/underline/outline picked-state rules, `.chip-toggle--declared`, `.chip-toggle__helper`.
- Components: `AxisMark.jsx` root class `axis-mark axis-mark--{group}`, click without toggle-off, `handleClear(event)` guarded on `event.detail === 0`; `Segmented.jsx` click without toggle-off; `RecipePage.jsx` `handleChangeSegment` stores the pick, new `handleClearSegment(field, label)` passed to BatchRow as `onClearSegment`.

<tasks>

<task type="tracer">
  <name>Task 1: Every picked box fills, every hover is a whole 2px, focus is one 2px ring on the visible box, a defect is a square and a word — tokens, rules, forced colours, pins (D-04, D-05, D-06, D-07, D-09; UAT items 4, 9, 10, 11, 13; 007 lines 24, 28, 48-50, 82-83, 87-89, 121-130, 143-145, 153, 164-165)</name>
  <files>app/src/styles/tokens.css, app/src/styles/app.css, app/src/styles/binder.test.js, app/src/styles/cross-cutting.test.js</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 24 (the universal ring), 28 (`.btn:hover` 2px with compensating padding), 47-50 (`.seg`, `.seg .opt`, `.on`, `:hover`), 82-83 (stop hover and picked), 86-89 (the defect: a square and a word; `::before`; the row's gaps), 121-123 (`.seg` wrap + 6px row-gap; option min-height 32px), 128-130 (defect min-height 32; the word keeps its ink; the stacked declared group), 143-145 (forced colours: the fill and the CanvasText ring), 148-153 (`--focus-outline-width: 2px`), 164-165 (13px options/defects; option padding-inline 8px), 179 (44px options and defects below 760px).
- `.planning/sketches/008-control-sheet/index.html` @ `0fa1015` — lines 40-44 (`--rule-hover`, `--focus-outline-width`, `--hover-comp`), 80-100 (actions, the square-and-word rule for a defect), 111-121 (joined boxes), 150-157 (forced colours).
- The contract § "Visual treatment" → "Universal rules", "Segmented options (.seg .opt) and their caption line", "Axis rows" (the stop's hover and picked entries), "Defects — a square and a word".
- `03.3.1.1-RESEARCH.md` § "Pattern 1: One shared fill selector…" (the rules and the `:focus-visible` heuristics), § "Code Examples" → "Tokens to add or redefine", "Segmented option", "The defect, a square and a word", § "Common Pitfalls" 1, 2, 3, 10, § "Open Questions" 2c and 2d.
- `03.3.1.1-PATTERNS.md` § "app.css — picked / hover / focus / forced-colors" (the rules to replace, the comments to rewrite, the invisible-radio rule to keep, the global button box to neutralise on defects) and § "binder.test.js" (every pin, with its current form).
- `app/src/styles/tokens.css` lines 49-79 (rule weights and the focus token's comment), 171-173 (`--size-mark-stop`), 224-259 (the battery group; `--gap-chip-row` at 248, `--declared-flaw-gap` at 249-253).
- `app/src/styles/app.css` lines 18-48 (the ring and the button rest/hover pair), 1595-1649 (the stop rules and the picked-state block), 1676-1760 (the segmented rules, the picked-state rules, the chip rules), 1484-1486 (`.batch-margin button { margin-top }` — the rule a defect must outrank), 1914-1928 (the 600px block; the forced-colours block goes after it).
- `app/src/styles/binder.test.js` lines 42-131, 190-207, 237-269; `app/src/styles/cross-cutting.test.js` lines 119-122.
  </read_first>
  <action>
One path, end to end: a picked stop is a fill, its focus is a 2px ring on the box, its hover is a 2px border, and the same three rules reach the options and the defects — proven by the style suites.

1. `tokens.css`.
   - After `--rule-baseline` add `--rule-hover: 2px;` with a comment citing 007 lines 28, 50, 82, 88 and 153 and 008 lines 40-41: hover borders and the focus ring are a whole 2px because 1.5px renders as 1px at DPR 1 in Chrome; `--rule-baseline` keeps its 1.5px for table rules and the text control's underline (an underline does render at 1.5px, 007 line 30).
   - `--focus-outline-width: var(--rule-hover);` (007 line 153; 003 line 26). Rewrite the comment at 71-77: the ring means focus and nothing else; it is heavier than every other rule weight (2 > 1.5 > 1); a picked control carries no outline at all — its state is the fill (D-04/D-05). `resolveTokenPx` follows one level of `var()`, so nothing may point at `--focus-outline-width` in turn (PATTERNS).
   - In the battery group: `--seg-pad-y: 5px; --seg-pad-x: 8px;` (007 lines 48 + 165; the hover padding is derived by calc, never a second literal); `--size-defect-mark: var(--size-mark-stop);` (007 line 88 — the 13px square rides the app's existing 13px mark token); `--gap-defect-col: var(--gap-m);` (007 line 89 — 20px between a defect and the next; the row gap is `--gap-xs`). Delete `--gap-chip-row` (248) and `--declared-flaw-gap` with its comment (249-253) — nothing reads them after this task (PATTERNS: delete the token and its comment together).

2. `app.css`, the global pair (lines 26-48): the rest padding becomes `calc(var(--gap-xs) + var(--rule-hover) - var(--rule-ink-field))` and `button:hover, select:hover` becomes `border-width: var(--rule-hover)` (007 line 28; 003 line 30 — a whole pixel each way so the box holds exactly). Update the comment at 37-40 to cite the lines.

3. `app.css`, the picked state (D-04/D-21; 007 lines 49 and 83). Delete the declarations of `.axis-mark__stop:has(input[type='radio']:checked)` (1643-1649), the whole `.segmented__option input[type='radio']:checked ~ span` rule (1717-1721) and the whole `.chip-toggle[aria-pressed='true']` rule (1745-1751). Write ONE grouped rule in the axis-mark section, after the invisible-radio rule (which stays exactly as it is — 1627-1636; never `outline: none` on the input, binder 87-90):
   `.axis-mark__stop:has(input[type='radio']:checked), .segmented__option:has(input[type='radio']:checked) { background: var(--pen-blue); border-color: var(--pen-blue); color: var(--ground); z-index: 1; }` — both selectors already carry `position: relative` (Pitfall 2: without `z-index: 1` the picked pen-blue border hides under the neighbour's hairline). Rewrite the comment block at 1638-1642 (and the one at 1676-1683 and 1729-1733 when you reach them): the decision (D-04, D-21, sketch 008's settled control language), the sketch lines, and why — pen blue is the record's ink, not a verdict; the No-Verdict Rule governs verdict colour, and a picked state is none of uncertainty, estimate or error (UX1-03; `aria-checked`/`:checked` carry the fact). Do not leave a comment that says the opposite of the rule beneath it.
   Then the focus rule (D-07; 007 lines 24 + 153): `.axis-mark__stop:has(input[type='radio']:focus-visible), .segmented__option:has(input[type='radio']:focus-visible) { outline: var(--focus-outline-width) solid var(--ink); outline-offset: var(--focus-outline-offset); }` — the ring draws on the visible box, not on the opacity-0 radio; Chrome's `:focus-visible` heuristic keeps a mouse click on the label ring-free (RESEARCH Pattern 1's MDN/WebKit citations).
   Then the stop's hover (007 line 82): `.axis-mark__stop:hover { border-width: var(--rule-hover); z-index: 2; }` — the box is border-box (1624), so nothing moves.

4. `app.css`, the segmented option (007 lines 47-50, 121-123, 164-165; D-09; items 4 and 13): `.segmented { display: flex; flex-wrap: wrap; row-gap: var(--gap-xs); }`; `.segmented__option` keeps its border, negative right margin and `:last-child` reset and now reads `padding: var(--seg-pad-y) var(--seg-pad-x); box-sizing: border-box; min-height: var(--stop-h); font-family: var(--face-grotesk); font-size: var(--type-control); line-height: var(--leading-control); color: var(--ink);` (the `--stop-h` 32px is the shared cell height, 007 line 122; 13px is 007 line 164); add `.segmented__option:hover { border-width: var(--rule-hover); padding: calc(var(--seg-pad-y) - (var(--rule-hover) - var(--rule-ink-field))) calc(var(--seg-pad-x) - (var(--rule-hover) - var(--rule-ink-field))); z-index: 2; }` (007 line 50 — 4px 7px, derived so the box holds; D-06's "no hover on options" is superseded, RESEARCH 2c). The touch block already gives the option 44px.

5. `app.css`, the defect (007 lines 86-89, 128-130, 164, 179; item 11; the second half of D-04 as amended — only the square fills). Replace the chip section (1729-1760) with, under a comment citing those lines and sketch 008's "independent on/offs: square and word":
   - `.defects-row__chips { display: flex; flex-wrap: wrap; align-items: center; gap: var(--gap-xs) var(--gap-defect-col); margin-top: 0; }` (007 lines 89 and 130 — inside a group the row's own margin is 0; Plan 04 puts every row inside a group).
   - `.batch-margin .chip-toggle { display: inline-flex; align-items: center; gap: var(--gap-xs); border: 0; border-radius: 0; padding: 0; background: none; box-sizing: border-box; min-height: var(--stop-h); margin-top: 0; font-family: var(--face-grotesk); font-size: var(--type-control); line-height: var(--leading-control); letter-spacing: 0; color: var(--ink); cursor: pointer; }` — written with the `.batch-margin` ancestor (the precedent at 1665-1674) so it outranks `.batch-margin button { margin-top: var(--gap-s) }` (0,1,1) and the global `button` box (Pitfall 3); the pressed button itself never changes colour (007 line 129).
   - `.batch-margin .chip-toggle:hover { border: 0; padding: 0; }` — cancels the global `button:hover` thickening on the word; the thickening moves to the square.
   - `.chip-toggle::before { content: ''; display: inline-block; width: var(--size-defect-mark); height: var(--size-defect-mark); box-sizing: border-box; border: var(--rule-graduation) solid var(--ink); flex: 0 0 auto; }` (007 line 88); `.chip-toggle:hover::before { border-width: var(--rule-hover); }`; `.chip-toggle[aria-pressed='true']::before { background: var(--pen-blue); border-color: var(--pen-blue); }`.
   - Delete `.chip-toggle--declared` and `.chip-toggle__helper` (Plan 04 removes the markup; a class with no rule is inert until then).

6. `app.css`, the forced-colours block — a FOURTH top-level `@media`, placed LAST in the file after the 600px block, never nested, with the file's paragraph-comment shape (007 lines 143-145; CONTEXT D-05 as amended: selection stays a FILL in the system Highlight colour, the same shape as in normal colours, so state and focus never share one outline): `@media (forced-colors: active) { .axis-mark__stop:has(input[type='radio']:checked), .segmented__option:has(input[type='radio']:checked), .chip-toggle[aria-pressed='true']::before { forced-color-adjust: none; background: Highlight; border-color: Highlight; color: HighlightText; } .axis-mark__stop:has(input[type='radio']:checked):has(input[type='radio']:focus-visible), .segmented__option:has(input[type='radio']:checked):has(input[type='radio']:focus-visible) { outline-color: CanvasText; } }`. `Highlight`/`HighlightText`/`CanvasText` are system colour keywords, not hex — the no-hex gate is unaffected.

7. Pins (Pitfall 1), in this task:
   - `binder.test.js` 43-52: the outline list becomes `[':focus-visible', '.is-landing-focus:focus', ".axis-mark__stop:has(input[type='radio']:focus-visible), .segmented__option:has(input[type='radio']:focus-visible)"]` (css-source.js normalises a grouped selector's whitespace to single spaces — use exactly that string); rename the test to "exactly three rules" and extend the comment: the third is the ring drawn on a label-wrapped radio's visible box (D-07, 007 line 24). 54-61: `focusWidth` equals `resolveTokenPx(tokens, '--rule-hover')` (2), is greater than `--rule-baseline` (1.5) and than `--rule-graduation` (1); rename. 111-130: the padding regex reads `var\(--rule-hover\)` in the calc and the hover border regex reads `--rule-hover`; the arithmetic uses `resolveTokenPx(tokens, '--rule-hover')`. 191-207: the underline list becomes `['.text-control', 'a']` and the comment says a picked control is a fill (D-04) so nothing but a link or a text control is underlined. 248-268: count 4; `allowedMedia` gains `'(forced-colors: active)'`; rename ("the four named top-level @media blocks"). Add three tests in a new describe "the picked state is a fill (D-04, sketch 007 lines 49 and 83)": `ruleFor("<the grouped fill selector, normalised>")` is truthy and matches `/background:\s*var\(--pen-blue\)/`, `/border-color:\s*var\(--pen-blue\)/`, `/color:\s*var\(--ground\)/` and `/z-index:\s*1/`; no rule declares both `/font-weight:\s*700/` and `/text-decoration:\s*underline/`; no rule's selector includes `aria-invalid` (007 line 135 — invalid is the sentence, never a ring); and the `(forced-colors: active)` rules include one whose declarations match `/forced-color-adjust:\s*none/` and `/background:\s*Highlight/` and one matching `/outline-color:\s*CanvasText/`. Add a fourth: `ruleFor('.chip-toggle::before')` matches `/width:\s*var\(--size-defect-mark\)/` and `/border:\s*var\(--rule-graduation\)/`, and `ruleFor(".chip-toggle[aria-pressed='true']::before")` matches `/background:\s*var\(--pen-blue\)/`; and `ruleFor("[aria-pressed='true']")`-style leftovers: `ruleFor(".chip-toggle[aria-pressed='true']")` is undefined.
   - `cross-cutting.test.js` 119-122: the sorted conditions become `['(forced-colors: active)', '(max-width: 1099.98px)', '(max-width: 600px)', '(max-width: 759.98px)']`; rename ("four").
  </action>
  <verify>
    <automated>npm --prefix app test -- src/styles</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `resolveTokenPx(tokens, '--rule-hover')` is 2 and `resolveTokenPx(tokens, '--focus-outline-width')` is 2; `tokens['--gap-chip-row']` and `tokens['--declared-flaw-gap']` are undefined.
- `ruleFor` through css-source.js finds the grouped fill selector declaring `background: var(--pen-blue)`, `color: var(--ground)` and `z-index: 1`; finds the grouped focus selector declaring `outline: var(--focus-outline-width) solid var(--ink)`; finds `.axis-mark__stop:hover` and `.segmented__option:hover` declaring `border-width: var(--rule-hover)` and `z-index: 2`; finds no rule whose declarations carry both `font-weight: 700` and `text-decoration: underline`; finds no selector containing `aria-invalid`.
- `grep -c "forced-colors: active" app/src/styles/app.css` prints 1 and that prelude's line number is greater than the `max-width: 600px` prelude's (the block is last).
- `ruleFor('.batch-margin .chip-toggle')` declares `border: 0`, `padding: 0`, `background: none`, `min-height: var(--stop-h)`, `margin-top: 0`; `ruleFor('.chip-toggle::before')` exists; `ruleFor('.chip-toggle--declared')` and `ruleFor('.chip-toggle__helper')` are undefined.
- `npm --prefix app test -- src/styles` passes with the outline list at three, the underline list at two, the `@media` count at four.
  </acceptance_criteria>
  <done>The stylesheet says what the sketch's lines say for every control state: fill when picked, 2px ring when focused on the box, 2px border when hovered, a filled square for a marked defect, a Highlight fill under forced colours — and the suites prove it.</done>
</task>

<task type="auto">
  <name>Task 2: The joined stops, the caption line that reserves Clear's height, and the axes grid's own clearance mechanism (007 lines 35, 65-68, 75, 80-81, 118, 124, 131, 158, 177-178, 180; UAT item 10 as the tenth round draws it)</name>
  <files>app/src/styles/tokens.css, app/src/styles/app.css, app/src/ui/AxisMark.jsx, app/src/styles/cross-cutting.test.js</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 35 (`.category .head .lbl { margin-bottom: 0 }`), 64-70 (the 3-column grid: `column-gap: 0`, core `padding-right: var(--gap-m)`, declared `padding-left: var(--gap-s)`, the rule at `calc(200% / 3)`), 73-75 (`.head`; the caption line's `margin-bottom` and `min-height: 24px`), 80-81 (the joined track and cell; `:last-child`), 118 (`.head { flex-wrap: wrap }`), 120 (`.category { min-width: 0; max-width: 100% }`), 124 (`height: 32px; flex: 0 0 38px`), 131 (`.clear { margin-left: auto }`, no negative margin), 158 (`gap: var(--gap-xs); align-items: center`), 176-183 (below 760px: 44px cells on 216px, 44px caption lines).
- `.planning/sketches/007-full-battery/README.md` § "Tenth round" bullets "Joined stops on all six axes" and "A joined group is a radio; Clear in the caption line" (the measured numbers: track 186 / cells 38 ×5 / seams −1 ×4; head 24 → 24 and 44 → 44 before and after a pick).
- The contract § "Visual treatment" → "Captions" (the caption line entry), "Axis rows" (track, stop, per-axis Clear, the one grid).
- `03.3.1.1-RESEARCH.md` § "Amendment 2026-09-14: the per-axis Clear control", § "The record pen" rows T1, T2, T5, T18, § "Code Examples" → "The stop, joined and restyled", "The caption line with its Clear", § "Common Pitfalls" 10 and 12, § "Extras" (the axes-grid gap arithmetic: column-gap 12 vs 0 + padding).
- `03.3.1.1-PATTERNS.md` § "app.css — picked / hover / focus / forced-colors" → "Joined cells, not gapped ones" and § "cross-cutting.test.js" (the media-block pins, the axis-head gap pin at 283-289).
- `app/src/styles/tokens.css` lines 212-222 (the touch tokens), 224-259 (the battery group: `--stop-w`, `--stop-gap`, `--axes-cols-clearance`).
- `app/src/styles/app.css` lines 1559-1564 (`.axis-mark__head`), 1595-1626 (`.axis-mark__stops`, `.axis-mark__stop`), 1665-1674 (`.batch-margin .axis-mark__clear`), 1799-1825 (`.axes-grid`, `.axes-rule`), 1861-1898 (the touch block).
- `app/src/ui/AxisMark.jsx` line 58 (the root element) and `app/src/domain/axes.js` (each axis carries `group: 'core' | 'declared'`).
- `app/src/styles/cross-cutting.test.js` lines 49-95 and 283-289.
  </read_first>
  <action>
The tenth round's geometry for the stops and the caption line, with the rules Plan 04's segmented caption line will land on already in place:

1. `tokens.css`: `--stop-w: 38px;` (007 lines 81, 124 — five joined cells sharing four hairlines: 5 × 38 − 4 × 1 = 186, the unchanged `--track-stop`); `--touch-stop-width: 44px;` (007 line 177 — 5 × 44 − 4 × 1 = 216, the unchanged `--track-stop-narrow`); delete `--stop-gap` (242) — the cells share hairlines through a negative margin, not a gap. Add `--caption-line-h: var(--text-control-min);` (007 line 75 — the caption line reserves Clear's height, and Clear is a text control, 24px) and `--caption-line-h-touch: var(--touch-min);` (007 line 180 — 44px below 760px). Rewrite the battery group's comment (224-236) and the touch comment (212-220: "the validated 40x44 box" becomes the joined 44×44 cell) to the new arithmetic with the sketch lines. Rewrite the `--axes-cols-clearance` comment (254-258): it is now the declared column's `padding-left` (007 line 66's `var(--gap-s)`), the same 12px the contract verified between a stop and the hairline.

2. `app.css`, the track and the cell (007 lines 80-81, 124): `.axis-mark__stops { display: flex; width: var(--track-stop); }` (no `gap`); `.axis-mark__stop` gains `flex: 0 0 var(--stop-w); margin-right: calc(-1 * var(--rule-ink-field));` (the seam is one hairline, written as the token, never `-1px`) and a new `.axis-mark__stop:last-child { margin-right: 0; }`. In the 759.98 block the `.axis-mark__stop` rule gains `flex-basis: var(--touch-stop-width);` (007 line 177; without it the flex row keeps the 38px basis at 393). Rewrite the comment at 1595-1601: five joined cells sharing four hairlines; the picked cell sits on top (z-index 1, Task 1) and a hovered one above it (z-index 2).

3. `app.css`, the caption line (007 lines 75, 118, 131, 158, 180): the `.axis-mark__head` rule becomes the grouped `.axis-mark__head, .segmented-field__head { display: flex; align-items: center; gap: var(--gap-xs); margin-bottom: var(--gap-xs); min-height: var(--caption-line-h); flex-wrap: wrap; }` with a comment: the line reserves Clear's height marked or not, so picking moves nothing (README tenth round: head 24 → 24, 44 → 44 before and after a pick); no negative block margins. Add `.segmented-field { min-width: 0; max-width: 100%; }` (007 line 120) and `.segmented-field__caption { margin-bottom: 0; }` (007 line 35 — the line itself carries the 6px to the options; this rule must sit AFTER `.pen-caption` in the file so it wins at equal specificity). The Clear rule at 1665-1674 becomes `.batch-margin .axis-mark__clear, .batch-margin .segmented-field__clear { font-family: var(--face-grotesk); font-size: var(--size-mark-stop); margin-top: 0; margin-left: auto; }` (007 line 131; keep the `.batch-margin` ancestor for the reason its comment gives). In the 759.98 block add `.axis-mark__head, .segmented-field__head { min-height: var(--caption-line-h-touch); }` as the block's last rule (007 line 180). Plan 04 renders `.segmented-field__head` and `.segmented-field__clear`; until then these rules match nothing.

4. `app.css`, the grid's clearance mechanism (007 lines 65-68): `.axes-grid` reads `column-gap: 0` (was `var(--axes-cols-clearance)`); add `.axes-grid:not(.axes-grid--stacked) > .axis-mark--core { padding-right: var(--gap-m); }` (007 line 66 — keeps a right-aligned Clear 20px off the next column's caption and the hairline) and `.axes-grid:not(.axes-grid--stacked) > .axis-mark--declared { padding-left: var(--axes-cols-clearance); }` (007 line 66's `var(--gap-s)`, the app's own 12px clearance token). The rule stays at `left: calc(200% / 3)` (007 line 69). Rewrite the `.axes-grid` comment (1799-1809): the clearance is now padding on the declared column, the same token's arithmetic. `AxisMark.jsx` line 58: the root becomes `<div className={`axis-mark axis-mark--${axis.group}`}>` (every axis in `domain/axes.js` carries `group`); nothing else in the component changes in this task.

5. Pins. `cross-cutting.test.js` 50-54: `--touch-stop-width` resolves to 44 (rename "stops 44x44"); 64-71: the media `.axis-mark__stop` rule also matches `/flex-basis:\s*var\(--touch-stop-width\)/`; 80-87: the 759.98 enumeration, in file order, becomes `['button, select, .ink-field, .segmented__option', '.axis-mark__stops, .axis-mark__anchors', '.axis-mark__stop', '.recipe-band__row-version', '.text-control', '.axis-mark__head, .segmented-field__head']` (rename "six rules"); 283-289: `ruleFor('.axis-mark__head, .segmented-field__head')` (the grouped string) matches `/margin-bottom:\s*var\(--gap-xs\)/`, and add `/min-height:\s*var\(--caption-line-h\)/` and `/align-items:\s*center/`. Add a describe "joined stops (007 lines 80-81, 124)": `ruleFor('.axis-mark__stops').declarations` does not match `/gap:/`; `ruleFor('.axis-mark__stop')` matches `/flex:\s*0 0 var\(--stop-w\)/` and `/margin-right:\s*calc\(-1 \* var\(--rule-ink-field\)\)/`; `ruleFor('.axis-mark__stop:last-child')` matches `/margin-right:\s*0/`; `resolveTokenPx(tokens, '--stop-w')` is 38 and `tokens['--stop-gap']` is undefined; `resolveTokenPx(tokens, '--caption-line-h')` is 24 and `resolveTokenPx(tokens, '--caption-line-h-touch')` is 44; `ruleFor('.axes-grid')` matches `/column-gap:\s*0/`.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/styles src/ui/AxisMark.test.jsx src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `resolveTokenPx(tokens, '--stop-w')` is 38, `resolveTokenPx(tokens, '--touch-stop-width')` is 44, `tokens['--stop-gap']` is undefined; `--track-stop` and `--track-stop-narrow` are unchanged at 186px and 216px.
- `ruleFor('.axis-mark__stops')` has no `gap`; `ruleFor('.axis-mark__stop')` declares `flex: 0 0 var(--stop-w)` and `margin-right: calc(-1 * var(--rule-ink-field))`; the 759.98 `.axis-mark__stop` rule declares `flex-basis: var(--touch-stop-width)`.
- `ruleFor('.axis-mark__head, .segmented-field__head')` declares `align-items: center`, `min-height: var(--caption-line-h)`, `margin-bottom: var(--gap-xs)`, `flex-wrap: wrap`; the 759.98 block carries the same grouped selector at `min-height: var(--caption-line-h-touch)`; `ruleFor('.batch-margin .axis-mark__clear, .batch-margin .segmented-field__clear')` declares `margin-left: auto` and no `margin-block`.
- `grep -c 'className={`axis-mark axis-mark--${axis.group}`}' app/src/ui/AxisMark.jsx` prints 1; `ruleFor('.axes-grid')` declares `column-gap: 0`.
- The style, AxisMark and BatchRow suites pass.
  </acceptance_criteria>
  <done>Five joined cells on the same tracks, a caption line that is the same height before and after a pick with Clear flush right, and the grid's clearance carried by padding as the sketch draws it.</done>
</task>

<task type="auto">
  <name>Task 3: A joined group is a radio — no toggle-off on stops or options, Clear is the only way back, and a keyboard Clear lands on the first cell (007 lines 341-352, 362, 375-381; CONTEXT D-04 supersession; sketch 008 "A joined group is a radio; Clear on both")</name>
  <files>app/src/ui/AxisMark.jsx, app/src/ui/Segmented.jsx, app/src/ui/RecipePage.jsx, app/src/ui/AxisMark.test.jsx, app/src/ui/Segmented.test.jsx</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 341-352 (`selectRadio`: a click selects, never un-selects; Clear appears once something is picked), 354-373 (`wireRadioGroup`: the click handler with its comment "a joined group is a radio: a second click changes nothing"), 375-381 (`clearGroup`: `if (event.detail === 0)` focus the first cell; announce "{name} cleared.").
- `.planning/sketches/008-control-sheet/README.md` § "A joined group is a radio; Clear on both" and § "Mark's recording, frame by frame" (why the focus move is keyboard-only).
- The contract § "Visual treatment" → "Superseded by the tenth round" (the Axes spec and Controls spec sentences that no longer hold).
- `03.3.1.1-RESEARCH.md` § "The record pen" rows T3, T4 (the RecipePage half), T6; § "Common Pitfalls" 11 (the radio rule and a reachable Clear land together — the six axes already have Clear; the three segmented controls get theirs in Plan 04, so this task removes only the axis toggle and the segmented toggle in RecipePage's storage, and prepares `handleClearSegment` for Plan 04's control); § "Open Questions" 2b.
- `app/src/ui/AxisMark.jsx` (all 103 lines: the header comment's click-again sentences, `handleStopClick`, `handleClear`, the Home/End comment).
- `app/src/ui/Segmented.jsx` (all 34 lines).
- `app/src/ui/RecipePage.jsx` lines 1013-1062 (`handleChangeSegment`, `handleChangeRecordMark`, `handleClearAxisMark` and their comments), 1636-1664 (the BatchRow mount).
- `app/src/ui/AxisMark.test.jsx` lines 1-11 and `app/src/ui/Segmented.test.jsx` lines 1-8 (the header comments that name click-again-clears); `app/src/ui/RecipePage.test.jsx` — grep it for "click-again" or "clears it"; no test pins the toggle today, but confirm before editing.
  </read_first>
  <action>
A behaviour change, not a style one (RESEARCH 2b) — recorded here for Mark's one-line acknowledgement: CONTEXT D-04's "click-again clears" is superseded by the sketch's radio rule (007 line 362, sketch 008), and D-01 gives the HTML the call.

1. `AxisMark.jsx`: `handleStopClick(stop)` calls `onChange(stop)` unconditionally — a second click on the picked stop changes nothing (007 line 362). `handleClear(event)` calls `onClear()` and then moves focus to the first stop ONLY when `event.detail === 0` — a keyboard activation (Enter/Space report `detail` 0; a mouse click reports the click count) — per 007 line 379 and the 008 README's recording: a mouse Clear leaves the browser's sequential-focus starting point on Clear so the next Tab reaches the first cell with its ring; a script-focused cell after a pointer click would show no ring. Rewrite the header comment's two sentences that describe the retired re-click clearing (lines 19-26) and the Home/End comment (44-48: the reason it never uses `.click()` is now simply that Home/End must select, not toggle). Nothing else changes: native radios, `onChange={() => {}}`, the Home/End handler, `aria-label`s stay.

2. `Segmented.jsx`: the option's `onClick` calls `onChange(option)` unconditionally. Rewrite the header comment (lines 6-14): picking is final; Clear in the caption line (Plan 04 adds it) is the way back; a native radio's `onChange` does not re-fire for a click that leaves its value unchanged, which is why the handler stays on `onClick`.

3. `RecipePage.jsx`: `handleChangeSegment(field, value)` stores the pick — `setDraft((prev) => ({ ...prev, [field]: value }))` — with no toggle (rewrite the comment at 1013-1019 to cite 007 line 362 and D-04's supersession). Add, directly after `handleClearAxisMark`, `handleClearSegment(field, label)`: `setBlockedDateMessage(null); if (isTastingBodyField(field)) setPendingUndo(null); setDraft((prev) => ({ ...prev, [field]: '' })); announce(`${label} cleared.`);` — the same shape as `handleClearAxisMark`, with the retirement boundary `handleChangeSegment` already applies (only Melt style is a tasting-body field), announcing "{caption} cleared." to form-status as 007 line 381 does. Pass it to `<BatchRow … onClearSegment={handleClearSegment} />` (BatchRow wires it to the segmented Clear in Plan 04; an unread prop is harmless until then). Rewrite the comment at 1033-1039 (`handleChangeRecordMark` no longer receives a null from a re-click; a null arrives only from `handleClearAxisMark`).

4. Tests: rewrite the header comments of `AxisMark.test.jsx` (lines 10-11) and `Segmented.test.jsx` (lines 6-8) so neither describes the retired re-click clearing; the existing assertions (five radios, `checked` on the picked one, Clear present only when marked) already describe the radio shape and stay. Add to `AxisMark.test.jsx` one source-level guard in the existing style: none — render-only tests cannot click; the acceptance below reads the source. If `RecipePage.test.jsx` pins the segment toggle, replace that assertion with "a second call with the same value leaves the value" — expected: no such pin exists.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/AxisMark.test.jsx src/ui/Segmented.test.jsx src/ui/RecipePage.test.jsx src/ui/BatchRow.test.jsx</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c "event.detail === 0" app/src/ui/AxisMark.jsx` prints 1, and `grep -c "value === stop ? null : stop" app/src/ui/AxisMark.jsx` prints 0.
- `grep -c "value === option ? null : option" app/src/ui/Segmented.jsx` prints 0 and `grep -c "onClick={() => onChange(option)}" app/src/ui/Segmented.jsx` prints 1.
- `grep -c "prev\[field\] === value ? '' : value" app/src/ui/RecipePage.jsx` prints 0; `grep -c "function handleClearSegment" app/src/ui/RecipePage.jsx` prints 1; `grep -c "onClearSegment={handleClearSegment}" app/src/ui/RecipePage.jsx` prints 1.
- `grep -ci "click-again" app/src/ui/AxisMark.jsx app/src/ui/Segmented.jsx app/src/ui/AxisMark.test.jsx app/src/ui/Segmented.test.jsx app/src/ui/RecipePage.jsx | awk -F: '{s+=$2} END {print s}'` prints 0 (no comment still describes the retired behaviour).
- The four UI suites pass.
  </acceptance_criteria>
  <done>Picking is final on every stop and option; the per-axis Clear announces and moves focus only on a keyboard activation; RecipePage stores a pick without toggling and offers `handleClearSegment` for the segmented controls' Clear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maker input → draft state | the segmented pick and axis mark handlers change from toggle to store; the value set is unchanged (the option strings and stops 1-5); `setMark`'s discipline and `isTastingBodyField`'s retirement boundary are unchanged |
| pointer/keyboard → focus | `handleClear` moves focus only on `event.detail === 0`; a mouse Clear moves no focus |
| stylesheet text → style-contract suites | a fourth top-level `@media` (forced-colors) — the parser contract holds |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1.1-07 | Denial of service (of a control) | the radio rule with no Clear | medium | mitigate | the six axes already carry Clear; the three segmented controls receive theirs in Plan 04 (Pitfall 11) — until then their storage no longer toggles but the app's `handleChangeSegment` can still be driven to any option; the phase's must_have "Clear is the only way back" is met at Plan 04 and measured by Plan 06 |
| T-03.3.1.1-08 | Information disclosure | picked state carried by colour | low | mitigate | `:checked`/`aria-checked` carry the fact; forced colours keep a Highlight fill (the same shape), so the state is never colour-alone (UX1-03) |
| T-03.3.1.1-09 | Denial of service (of focus) | the ring on the box | low | mitigate | the ring is one extra `:has()` rule; the input keeps its own (invisible) ring — no `outline: none` anywhere (binder pin) |
| T-03.3.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed; the gate does not fire |
</threat_model>

<verification>
- `npm --prefix app test -- src/styles` and the four UI suites pass after each task; `npm --prefix app test` and `npm --prefix app run build` pass at the end of the plan.
- The browser facts — the pen-blue fill's computed `rgb(31, 61, 122)`, the ring's 2px on a tabbed-to stop, the unchanged box on hover, the 38/44px cells with 37/43px lefts, the head's height before and after a pick, the keyboard-vs-mouse Clear — are Plan 06's to measure (D-17/D-18).
</verification>

<success_criteria>
UAT items 4, 9, 10, 11 (control half), 13 are closed in source against 007's cited lines; D-04, D-05, D-06, D-07 and D-09 land as the sketch amended them; the radio rule holds with Clear reachable on the axes and prepared for the segmented controls; every token, rule and pin is in place.
</success_criteria>

<output>
Create `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-SUMMARY.md` when done — record the D-04 "click-again" supersession explicitly for Mark, the four `@media` blocks in order, and every pin moved.
</output>
