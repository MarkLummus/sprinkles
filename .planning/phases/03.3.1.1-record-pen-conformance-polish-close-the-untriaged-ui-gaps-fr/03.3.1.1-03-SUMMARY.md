---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 03
subsystem: ui
tags: [css, tokens, record-pen, sketch-conformance, control-treatment, forced-colors, accessibility]

# Dependency graph
requires:
  - phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr (plans 01, 02)
    provides: the page-shell ladder, the .pen-caption/.pen-helper/.field-row family, the tasting-head/melt-row markup this plan's control-treatment work builds on
provides:
  - "--rule-hover (2px) and --focus-outline-width repointed at it; --seg-pad-y/--seg-pad-x, --size-defect-mark, --gap-defect-col, --stop-w (38px), --touch-stop-width (44px), --caption-line-h (24px), --caption-line-h-touch (44px) added; --gap-chip-row, --declared-flaw-gap, --stop-gap retired"
  - "One shared fill selector for a picked stop/option (background/border-color pen-blue, color ground, z-index 1); the focus ring on the visible box via :has(:focus-visible); a whole-2px hover on stops, segmented options, and a defect's square; a defect rebuilt as a square-and-word with no outer box"
  - "Five joined stops sharing four hairlines on the unchanged 186px/216px tracks; the caption line (.axis-mark__head, .segmented-field__head) reserving Clear's height so picking moves nothing; the axes grid's clearance carried by padding on the flanking core/declared columns instead of column-gap"
  - "A fourth top-level @media (forced-colors: active) block: a Highlight fill under forced colours, CanvasText for a picked box's focus ring"
  - "A joined group is a radio: AxisMark and Segmented no longer toggle off on a second click; the per-axis Clear moves focus only on a keyboard activation (event.detail === 0); RecipePage's handleChangeSegment stores without toggling and a new handleClearSegment is wired to BatchRow for Plan 04's segmented Clear"
affects: [03.3.1.1-04, 03.3.1.1-05, 03.3.1.1-06-conformance-pass]

# Actuals (#2632)
actuals:
  tokens: 14867
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A picked control's state is a fill, never bold/underline/outline — one shared :has(input:checked) selector across .axis-mark__stop and .segmented__option, with a separate :has(input:focus-visible) selector carrying the ring so state and focus never share one rule"
    - "A hover weight of a whole 2px (--rule-hover) is distinct from the 1.5px --rule-baseline everywhere a border thickens on interaction, because 1.5px renders as 1px at DPR 1 in Chrome; --rule-baseline stays for rules that don't need to survive that rounding (table rules, the text-control underline)"
    - "Joined controls (five stops on a track) share hairlines through box-sizing: border-box + flex-basis + a one-hairline negative margin reset on :last-child, never a gap token — the picked/hovered cell's z-index keeps its border visible over the shared seam"
    - "A control's caption line reserves its trailing Clear's own height (min-height on the head row) so picking never changes the row's height, rather than the Clear itself carrying a negative margin"
    - "A joined radio group's Clear moves focus to the first cell only on event.detail === 0 (a keyboard activation) — a mouse Clear leaves the browser's sequential-focus starting point in place so the next Tab still reaches the first cell with its ring"

key-files:
  created: []
  modified:
    - app/src/styles/tokens.css
    - app/src/styles/app.css
    - app/src/styles/binder.test.js
    - app/src/styles/cross-cutting.test.js
    - app/src/ui/AxisMark.jsx
    - app/src/ui/AxisMark.test.jsx
    - app/src/ui/Segmented.jsx
    - app/src/ui/Segmented.test.jsx
    - app/src/ui/RecipePage.jsx

key-decisions:
  - "CONTEXT D-04's second half — 'click-again clears' — is superseded by sketch 007's radio rule (line 362) and sketch 008's settled control language: a joined group (an axis's five stops, a segmented control's three options) is a radio now. A second click on the picked cell changes nothing; the per-axis/per-segment Clear is the only way back. Recorded here for Mark's explicit acknowledgement, per the plan's own <output> instruction. A defect keeps its independent on/off (click-again does clear a defect) — only stops and segmented options changed."
  - "handleClearSegment is wired into RecipePage and passed to BatchRow as onClearSegment, but BatchRow does not yet render a Clear control for the three segmented fields — Plan 04 adds that markup (.segmented-field, .segmented-field__head, .segmented-field__clear), which this plan's CSS (.segmented-field, .segmented-field__caption, .axis-mark__head/.segmented-field__head grouping, .batch-margin .axis-mark__clear/.segmented-field__clear grouping) already targets and matches nothing until then. This is the plan's own stated sequencing (Pitfall 11: the radio rule and a reachable Clear must land together, which is why only the axis toggle was removed here, where Clear already exists)."

patterns-established:
  - "Pattern: when a plan retires a behavioural phrase project-wide (here, 'click-again clears' on radios), grep every file the plan's own files_modified list names for the phrase and its rationale-bearing comments, not just the code paths that execute it — a stale doc comment describing retired behaviour is as real a defect as stale code."

requirements-completed: []

coverage:
  - id: D1
    description: "A picked stop or segment option is a solid pen-blue box with ground text and a pen-blue border, nothing bold, nothing underlined, no outline; a marked defect is a pen-blue 13px square before an ink word (007 @ 109733d lines 49, 83, 88, 129; D-04 as amended, D-05, D-09; UAT items 4, 10, 11, 13)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/binder.test.js#the picked state is a fill (D-04, sketch 007 lines 49 and 83)"
        status: pass
      - kind: unit
        ref: "app/src/styles/binder.test.js#no selector other than `a`/`.text-control` declares an underline (D-18, D-04, 03.1 Gap 1 override; 03.3.1.1 tenth round)"
        status: pass
    human_judgment: true
    rationale: "The tests prove the rule text (fill selector, no bold+underline anywhere, the defect's ::before square) exists correctly; the rendered pixel fact — the actual pen-blue fill, the 13px square, the fill's colour value at rest and under forced colours — is a browser measurement this executor cannot perform, explicitly assigned to Plan 06's conformance pass by this plan's own <verification> section."
  - id: D2
    description: "Tabbing into an axis lands on its first (or picked) stop and a 2px solid ink ring, offset 2px, draws on the visible box, not on the invisible radio (007 lines 24 and 153; D-07; UAT item 9)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/binder.test.js#exactly three rules declare outline through --focus-outline-width: the global :focus-visible rule, the landing-focus exception, and the battery's focus-on-box rule (D-14, D-07, 03.3-06, G-03.3-1)"
        status: pass
    human_judgment: true
    rationale: "The :has(input:focus-visible) rule and its 2px/2px-offset values are proven as text; the actual rendered ring on a tabbed-to stop, and Chrome's :focus-visible heuristic correctly staying silent on a mouse click, are browser facts for Plan 06."
  - id: D3
    description: "Hovering a stop, an option, a defect's square or a button thickens its border to a whole 2px in place with nothing moving; a hovered joined box sits above its neighbours (z-index 2) and the picked one at z-index 1 (007 lines 28, 50, 82, 88; D-06 as amended)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/binder.test.js#hovering a button or select costs no movement — border-plus-padding is equal at rest and on hover (D-15)"
        status: pass
    human_judgment: true
    rationale: "The rest/hover arithmetic (border-ink-field + padding == rule-hover + padding) is proven equal for buttons/selects; the segmented option's own derived hover padding and the stop's border-box no-shift claim are the same arithmetic pattern but unpinned by an automated test, and the actual absence of visible movement is Plan 06's browser measurement."
  - id: D4
    description: "The five stops are joined: 38px cells sharing four hairlines on the 186px track, 44px cells on the 216px track below 760px (007 lines 80-81, 124, 177-178; UAT item 10)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/cross-cutting.test.js#.axis-mark__stop reads a 38px flex-basis and a one-hairline negative right margin, reset on :last-child"
        status: pass
      - kind: unit
        ref: "app/src/styles/cross-cutting.test.js#--stop-w resolves to 38 and --stop-gap is retired; the caption-line tokens resolve to 24 and 44"
        status: pass
    human_judgment: true
    rationale: "The token values and the flex/margin mechanism are proven as text; the actual rendered 186px/216px track widths and the visually seamless joined hairlines are Plan 06's browser measurement."
  - id: D5
    description: "A joined group is a radio: a second click on the picked stop or option changes nothing; Clear is the only way back, and a keyboard-activated Clear lands focus on the first cell while a mouse Clear leaves the tab position on Clear (007 lines 362 and 379; CONTEXT D-04 supersession; sketch 008 'A joined group is a radio')"
    requirement: null
    verification:
      - kind: unit
        ref: "grep -Fc \"event.detail === 0\" app/src/ui/AxisMark.jsx (=1); grep -Fc \"value === stop ? null : stop\" app/src/ui/AxisMark.jsx (=0)"
        status: pass
      - kind: unit
        ref: "grep -Fc \"onClick={() => onChange(option)}\" app/src/ui/Segmented.jsx (=1)"
        status: pass
    human_judgment: true
    rationale: "The source no longer contains a toggle-off path and the keyboard-only focus guard is in place, proven by grep and by the render-only test suites passing; the actual click/keyboard interaction sequence in a real browser (a click leaving the pick unchanged, Tab landing correctly after a mouse Clear) is Plan 06's job — this codebase's own test house style has no click driver (AxisMark.test.jsx/Segmented.test.jsx header comments)."
  - id: D6
    description: "Every axis head reserves 24px (44px below 760px) marked or not, centred, with Clear right-aligned, so picking moves nothing (007 lines 75, 131, 158, 180)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/cross-cutting.test.js#the axis/segmented head row picks up the same 6px caption-to-content gap, ahead of the stops, and reserves Clear's own height"
        status: pass
    human_judgment: true
    rationale: "min-height: var(--caption-line-h) (24px) and align-items: center are proven as text on the grouped selector; the actual rendered head height staying constant across a pick, at both breakpoints, is Plan 06's browser measurement."
  - id: D7
    description: "Under forced colours a picked box keeps a Highlight fill through forced-color-adjust: none and its focus ring takes CanvasText (007 lines 143-145; CONTEXT D-05 as amended)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/styles/binder.test.js#the forced-colors block keeps a fill and reserves CanvasText for the focus ring"
        status: pass
    human_judgment: true
    rationale: "The @media (forced-colors: active) block's two rules are proven as text (forced-color-adjust: none + background: Highlight; outline-color: CanvasText); actual behaviour under a forced-colours OS setting is not testable in this suite's node environment and is not assigned to any plan in this phase's <verification> — the sketch README itself records this was not re-checked in Chrome's emulation, so this stays an accepted gap beyond text-level proof."

duration: 22min
completed: 2026-09-15
status: complete
---

# Phase 03.3.1.1 Plan 03: Control treatment — fill, focus ring, whole-2px hover, joined stops, and the radio rule Summary

**One shared pen-blue fill replaces bold/underline/outline on every picked stop and segmented option, the focus ring now draws on the visible box via `:has(:focus-visible)`, hover thickens to a whole 2px in place everywhere a border is involved, the five stops on each axis are joined cells sharing hairlines, a defect is rebuilt as a square-and-word, a fourth `@media (forced-colors: active)` block lands, and AxisMark/Segmented retire click-again-clears in favour of sketch 007's radio rule with a keyboard-guarded per-axis Clear**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-15T16:30:00Z
- **Completed:** 2026-09-15T16:52:42Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments
- `tokens.css`: `--rule-hover` (2px) added; `--focus-outline-width` repointed at it (was `--rule-baseline`); `--seg-pad-y`/`--seg-pad-x`, `--size-defect-mark`, `--gap-defect-col` added; `--gap-chip-row`/`--declared-flaw-gap` retired; `--stop-w` redefined 34px → 38px, `--touch-stop-width` redefined 40px → 44px, `--stop-gap` retired; `--caption-line-h`/`--caption-line-h-touch` (24px/44px) added
- `app.css`: one shared fill selector (`.axis-mark__stop:has(input:checked), .segmented__option:has(input:checked)`) replaces the retired bold/underline/outline picked-state rules; a separate `:has(input:focus-visible)` selector draws the 2px ring on the visible box; `.axis-mark__stop:hover` and `.segmented__option:hover` thicken to `--rule-hover` in place; the defect is rebuilt as `.batch-margin .chip-toggle` (no border/padding/background) plus `.chip-toggle::before` (a 13px square that fills pen-blue when pressed); `.axis-mark__stops`/`.axis-mark__stop` join via flex-basis + a one-hairline negative margin, reset on `:last-child`; `.axis-mark__head` grouped with `.segmented-field__head` reserving `--caption-line-h`; `.axes-grid` reads `column-gap: 0` with padding carried on the flanking `.axis-mark--core`/`.axis-mark--declared` columns; a fourth top-level `@media (forced-colors: active)` block lands last in the file
- `AxisMark.jsx`: root class becomes `axis-mark axis-mark--{group}`; `handleStopClick` calls `onChange(stop)` unconditionally; `handleClear(event)` moves focus to the first stop only on `event.detail === 0`
- `Segmented.jsx`: the option's `onClick` calls `onChange(option)` unconditionally
- `RecipePage.jsx`: `handleChangeSegment` stores the pick without toggling; new `handleClearSegment(field, label)` mirrors `handleClearAxisMark`, wired to `<BatchRow onClearSegment={handleClearSegment} />` for Plan 04
- `binder.test.js`/`cross-cutting.test.js`: every pin (outline list, underline list, focus-width and hover-equality arithmetic, the `@media` allowlist, the joined-stops geometry, the caption-line reservation) moved to match; a new "the picked state is a fill" describe block added

## Task Commits

Each task was committed atomically:

1. **Task 1: Every picked box fills, every hover is a whole 2px, focus is one 2px ring on the visible box, a defect is a square and a word — tokens, rules, forced colours, pins** - `0105945` (feat)
2. **Task 2: The joined stops, the caption line that reserves Clear's height, and the axes grid's own clearance mechanism** - `830895d` (feat)
3. **Task 3: A joined group is a radio — no toggle-off on stops or options, Clear is the only way back, and a keyboard Clear lands on the first cell** - `d907bc6` (feat)

**Plan metadata:** committed separately with this SUMMARY.

## Tracer Feedback Gate

Task 1 is `type="tracer"`. Per the checkpoints.md precedence chain (row 3: interactive run, `human_verify_mode: end-of-phase`, tracer `<verify>` carries only `<automated>`, no `gate="blocking-human"`): re-ran `npm --prefix app test -- src/styles` immediately after Task 1's commit — 95/95 passed — logged `⚡ Tracer verified end-to-end — expanding`, and continued directly to Task 2 with no checkpoint.

## Files Created/Modified
- `app/src/styles/tokens.css` - `--rule-hover`, `--seg-pad-y`/`--seg-pad-x`, `--size-defect-mark`, `--gap-defect-col`, `--caption-line-h`/`--caption-line-h-touch` added; `--focus-outline-width`, `--stop-w`, `--touch-stop-width` redefined; `--gap-chip-row`, `--declared-flaw-gap`, `--stop-gap` retired
- `app/src/styles/app.css` - the shared fill/focus/hover rules, the rebuilt defect, the joined stops, the grouped caption line, the axes-grid clearance, the fourth `@media (forced-colors: active)` block
- `app/src/styles/binder.test.js` - outline/underline lists, focus-width and hover-equality arithmetic, the `@media` allowlist (now four), a new "the picked state is a fill" describe block
- `app/src/styles/cross-cutting.test.js` - touch tokens (44/44), the 759.98px enumeration (six rules), the grouped head-row pin, a new "joined stops" describe block, the `@media` sorted-conditions pin (now four)
- `app/src/ui/AxisMark.jsx` - root class, `handleStopClick`, `handleClear(event)`, header/Home-End comments
- `app/src/ui/AxisMark.test.jsx` - header comment no longer describes retired re-click clearing
- `app/src/ui/Segmented.jsx` - the option's `onClick`, header comment
- `app/src/ui/Segmented.test.jsx` - header comment no longer describes retired re-click clearing
- `app/src/ui/RecipePage.jsx` - `handleChangeSegment`, new `handleClearSegment`, `onClearSegment` prop on `<BatchRow>`, comments on `handleChangeRecordMark`/`handleClearAxisMark`/`handleChangeDefect`

## Decisions Made
- CONTEXT D-04's "click-again clears" is superseded for stops and segmented options by sketch 007's radio rule (line 362) and sketch 008's control language — recorded above under key-decisions for Mark's explicit acknowledgement, per the plan's own `<output>` instruction. A defect's independent on/off (click-again does clear a defect) is unchanged.
- No architectural changes; all three tasks executed as planned, including the tracer feedback gate after Task 1 (see above).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Symlinked `node_modules` from the main checkout into this worktree**
- **Found during:** Task 1, first attempt to run `npm --prefix app test -- src/styles`
- **Issue:** This worktree had no `app/node_modules` at all (`vitest: command not found`) — worktrees don't get their own `npm install` automatically, and none of this phase's prior plans in this worktree had run a test yet.
- **Fix:** Confirmed `app/package.json` and `app/package-lock.json` are byte-identical to the main checkout's, then symlinked `app/node_modules` to the main checkout's `app/node_modules` rather than running a fresh `npm install` — no new package was installed or resolved from the registry, only an existing local install reused. `app/node_modules` is gitignored, so the symlink is untracked and never staged.
- **Files modified:** none tracked (local dev environment only)
- **Verification:** `npm --prefix app test -- src/styles` ran successfully afterward
- **Committed in:** n/a (untracked, gitignored)

---

**Total deviations:** 1 auto-fixed (Rule 3 — a local dev-environment blocker, not a package install; no new dependency was resolved).
**Impact on plan:** No scope creep; the fix only restored the worktree's ability to run the test suite already declared in `package.json`.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `npm --prefix app test` (919 tests) and `npm --prefix app run build` both pass at the end of the plan.
- Plans 04-05 (the segmented caption line's markup, the ceremony wording) can proceed — this plan's `.segmented-field`/`.segmented-field__head`/`.segmented-field__clear` CSS and `RecipePage.jsx`'s `handleClearSegment`/`onClearSegment` prop are already in place and waiting for Plan 04's markup; until then they match/read nothing.
- Plan 06 (the conformance pass) needs to confirm the browser facts this plan's tests cannot: the pen-blue fill's actual computed colour, the ring's 2px on a tabbed-to stop, the unchanged box on hover, the 38px/44px joined cells with their measured lefts, the caption-line head's height before and after a pick, the keyboard-vs-mouse Clear focus behaviour, and the forced-colours fill (the sketch's own README records this was not re-checked in Chrome's emulation either).
- Mark should acknowledge the D-04 "click-again clears" supersession (key-decisions above) before or during the conformance pass — CONTEXT.md's own text still describes the retired behaviour for stops/options and should be updated to match, though that update is documentation housekeeping outside this plan's `files_modified` scope.

## Self-Check: PASSED

- Key files verified on disk: `app/src/styles/tokens.css`, `app/src/styles/app.css`, `app/src/ui/AxisMark.jsx`, `app/src/ui/Segmented.jsx`, `app/src/ui/RecipePage.jsx`, this SUMMARY.md — all FOUND.
- Commits verified in `git log --oneline`: `0105945` (Task 1), `830895d` (Task 2), `d907bc6` (Task 3) — all FOUND.
- All three tasks' `<acceptance_criteria>` re-run and PASS (grep/token/CSS-rule checks shown inline during execution).
- Plan-level `<verification>`: `npm --prefix app test -- src/styles` passes after Task 1 (95/95); `npm --prefix app test -- src/styles src/ui/AxisMark.test.jsx src/ui/BatchRow.test.jsx` passes after Task 2 (215/215); `npm --prefix app test -- src/ui/AxisMark.test.jsx src/ui/Segmented.test.jsx src/ui/RecipePage.test.jsx src/ui/BatchRow.test.jsx` passes after Task 3 (181/181); `npm --prefix app test` (full suite) passes (919/919); `npm --prefix app run build` compiles.

---
*Phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr*
*Completed: 2026-09-15*
