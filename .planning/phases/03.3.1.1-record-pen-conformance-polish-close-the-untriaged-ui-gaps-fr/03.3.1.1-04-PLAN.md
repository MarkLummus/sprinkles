---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 04
type: execute
wave: 4
depends_on: ["03.3.1.1-02", "03.3.1.1-03"]
files_modified:
  - app/src/ui/Segmented.jsx
  - app/src/ui/Segmented.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - app/src/styles/app.css
autonomous: true
requirements: []
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each of the three segmented controls — Exit consistency, Airiness (estimated), Melt style — carries a caption line holding its uppercase caption and, once something is picked, a right-aligned Clear that is the only way back; a keyboard Clear lands on the first option, a mouse Clear does not (007 @ 109733d lines 220, 228, 289, 350, 375-381; UAT items 4 and 13 as the tenth round draws them; CONTEXT D-04 supersession)"
    - "The defects are five square-and-word buttons in two labelled groups inside the axes grid — 'Every recipe' (Coarse, icy · Sandy, gritty · Gummy, elastic · Greasy film) and 'This recipe only' (Bitter, a plain defect with no helper and no aria-label) — under the head 'Any problems?' with 'select all that apply' as a sibling helper (007 lines 263-281; UAT item 11; D-08 as superseded by the ninth round)"
    - "Two cue rows, 'Every recipe' and 'This recipe only', head the axes: on their own grid row above the axes in the 3-column arrangement (columns 1-2 and 3) and heading each stacked group below 760px; no axis carries a caption of its own (007 lines 61, 67, 416-420; UAT item 19)"
    - "In the 3-column arrangement the defects take the axes' columns and the vertical hairline spans them; stacked, the declared defect group sits under its own hairline like the declared axes (007 lines 68-69, 90, 130)"
  artifacts:
    - app/src/ui/Segmented.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/styles/app.css
  key_links:
    - "Segmented.jsx's Clear → RecipePage's handleClearSegment (Plan 03) via BatchRow's onClearSegment prop (the announcement '{caption} cleared.' and the '' store happen there; Segmented owns only the keyboard-guarded focus)"
    - ".segmented-field__head → the grouped caption-line rule Plan 03 wrote (.axis-mark__head, .segmented-field__head) — the markup lands on rules already pinned"
    - "AxesGrid's children slot → the desktop placement rules (.axes-grid:not(.axes-grid--stacked) > .defect-group--core { grid-column: 1 / 3 } etc.) — the defects are grid items of the same grid as the axes, which is what lets the hairline span them"
---

<objective>
Land the two remaining control structures against sketch 007 (`.planning/sketches/007-full-battery/index.html` @ `109733d`; every line number unchanged since `2a212be`): the segmented controls' caption line with its Clear (the tenth round's redraw of UAT items 4 and 13, and Pitfall 11's other half — Plan 03 removed the toggle-off, this plan gives the three controls their way back), and the defects as square-and-word buttons in two labelled groups inside the axes grid with the two cue rows above the axes (UAT items 11 and 19; the ninth round's Pattern 7; the retirement of the "Declared for this recipe" caption, the Bitter helper and its aria-label — CONTEXT D-08 as superseded).

Purpose: the fill, hover and ring rules (Plan 03) are one shape for every joined box and every square; this plan gives the last two families the markup those rules address, and it retires the last derivation-era structures the sketch no longer has.

Output: `Segmented.jsx` renders `.segmented-field` with a head and a Clear; `BatchRow.jsx` renders the three controls directly with `onClear`, the cue rows, and the defects inside `AxesGrid`; `AxisMark.jsx` loses `declaredCaption`; `app.css` gains the grid placement rules and loses `.axes-declared-caption`; the render-only suites pass with their pins moved.

Spec-less probe fallback: skipped visibly — no requirement IDs to probe; the sketch HTML plus CONTEXT.md is the design contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-RESEARCH.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-PATTERNS.md
@.planning/sketches/007-full-battery/index.html
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@app/src/ui/BatchRow.jsx
@app/src/ui/Segmented.jsx
@app/src/ui/AxisMark.jsx
@app/src/styles/app.css
</context>

## Artifacts this phase produces (this plan's share)

- Components: `Segmented.jsx` — props `{ groupLabel, options, value, onChange, onClear }`; renders `div.segmented-field > div.segmented-field__head (span.segmented-field__caption.pen-caption#…-caption, button.text-control.segmented-field__clear when picked) + div.segmented[role=radiogroup]`; `handleClear(event)` guarded on `event.detail === 0`. `BatchRow.jsx` — `onClearSegment` prop; the three `<Segmented>` call sites with `onClear`; `AxesGrid` with `children` and the two cue rows; the defects head and two `role="group"` blocks rendered as `AxesGrid`'s children; Bitter as a plain `.chip-toggle`. `AxisMark.jsx` — no `declaredCaption` prop.
- CSS (`app.css`): `.axes-grid__group > .axes-cue`, the desktop placement rules for `.axes-cue--core`, `.axes-cue--declared`, `.defects-head`, `.defect-group--core`, `.defect-group--declared`, the `.defects-head` row rule, `.defects-head > .pen-caption, .defects-head > .pen-helper { margin: 0 }`, `.axes-grid--stacked > .defect-group--declared`. Retired: `.axes-declared-caption`; the `SegmentedField` fieldset/legend markup.
- Ids: `defects-caption`, `defects-core-cue`, `defects-declared-cue`; per-control caption ids `segment-<slug>-caption`.

<tasks>

<task type="tracer">
  <name>Task 1: The segmented caption line with its Clear — three controls, one component, wired to RecipePage's clear (007 lines 35, 120, 220, 228, 289, 350, 375-381; UAT items 4, 13; Pitfall 11)</name>
  <files>app/src/ui/Segmented.jsx, app/src/ui/BatchRow.jsx, app/src/ui/Segmented.test.jsx, app/src/ui/BatchRow.test.jsx</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 218-235 (the two churn categories: `.category > .head (.lbl#lbl-… + button.text-control.clear[hidden]) + .seg`), 288-295 (Melt style, the same shape), 341-352 (`selectRadio` — line 350 shows/hides the category's Clear), 375-381 (`clearGroup`: the keyboard-only focus move and the "{caption} cleared." announcement), 612-616 (the three groups' `aria-label`s).
- The contract § "Visual treatment" → "Segmented options (.seg .opt) and their caption line" (the caption-line entry and the radio rule).
- `03.3.1.1-RESEARCH.md` § "The record pen" rows T4 and T6, § "Common Pitfalls" 11, § "Validation Architecture" (the render-only pins this round adds: a caption line with a right-aligned Clear on each of the three controls).
- `03.3.1.1-PATTERNS.md` § "BatchRow.jsx" → "Segmented caption line".
- `app/src/ui/Segmented.jsx` (all of it, as Plan 03 left it: no toggle-off); `app/src/ui/AxisMark.jsx` lines 39-42 and 60-70 (the head + Clear shape and the guarded focus this component mirrors); `app/src/ui/BatchRow.jsx` lines 90-105 (`SegmentedField`, to retire), 291-319 (the props list), 450-461 and 642-647 (the three call sites); `app/src/styles/app.css` (Plan 03's `.segmented-field`, `.segmented-field__head` (grouped with the axis head), `.segmented-field__caption`, `.batch-margin .segmented-field__clear` rules — read them so the class names match exactly).
- `app/src/ui/Segmented.test.jsx` (all), `app/src/ui/BatchRow.test.jsx` lines 183-212, 514-528, 650-676.
  </read_first>
  <action>
One component, three uses, one path to the store: the caption line becomes Segmented's own head, with Clear as its last child.

1. `Segmented.jsx`: the component takes `{ groupLabel, options, value, onChange, onClear }`. It renders `<div className="segmented-field"><div className="segmented-field__head"><span className="segmented-field__caption pen-caption" id={captionId}>{groupLabel}</span>{picked && <button type="button" className="text-control segmented-field__clear" aria-label={`Clear ${groupLabel}`} onClick={handleClear}>Clear</button>}</div><div className="segmented" role="radiogroup" aria-label={groupLabel}>…the options, unchanged…</div></div>` where `captionId` is `` `${groupName}-caption` `` (the existing slugged `groupName`), `picked` is `options.includes(value)`, the first option's `<input>` takes a `ref` (`firstOptionRef`), and `handleClear(event)` calls `onClear()` then `if (event.detail === 0) firstOptionRef.current?.focus()` — the same keyboard-only guard AxisMark carries (007 line 379; sketch 008 "Mark's recording, frame by frame"). The radiogroup keeps `aria-label={groupLabel}` (the tests pin it; 007 line 614 names the group the same way). Rewrite the header comment: one home for the three controls' caption line, options and Clear; picking is final; Clear is the only way back (007 lines 220, 228, 289, 350, 362).

2. `BatchRow.jsx`: delete `SegmentedField` (90-105) and its comment. Add `onClearSegment` to the props list beside `onChangeSegment` (RecipePage already passes it, Plan 03). The three call sites become `<Segmented groupLabel="Exit consistency" options={SEGMENT_OPTIONS.exitConsistency} value={draft.exitConsistency} onChange={(option) => onChangeSegment('exitConsistency', option)} onClear={() => onClearSegment('exitConsistency', 'Exit consistency')} />`, the same for `"Airiness (estimated)"` / `airiness`, and for `"Melt style"` / `meltStyle` inside the melt row's `.field-row--start` (the `.segmented-field` div is now the row's second item — 007 line 288's `.category`). No `<fieldset>` or `<legend>` remains in the pen.

3. Tests. `Segmented.test.jsx`: rewrite the header comment (the caption line and Clear are this component's now); keep every existing assertion (radiogroup role, `aria-label`, verbatim options, nothing checked by default, the picked one checked, one shared name); add a describe "the caption line with its Clear (007 lines 220, 228, 289; Pitfall 11)": (a) the markup starts with `<div class="segmented-field"><div class="segmented-field__head"><span class="segmented-field__caption pen-caption" id="segment-exit-consistency-caption">Exit consistency</span>` and the radiogroup's index is greater than the head's; (b) with `value=""` the markup does not contain `segmented-field__clear`; (c) with `value="Wet, soupy"` the markup contains a button matching `/<button type="button" class="text-control segmented-field__clear" aria-label="Clear Exit consistency">Clear<\/button>/` whose index is greater than the caption's and smaller than the radiogroup's (`role="radiogroup"`). `BatchRow.test.jsx`: in the "two segmented controls" describe (650-676) add: with the empty draft the recording markup contains three `class="segmented-field__head"` occurrences once the tasting is open (Exit consistency, Airiness (estimated), Melt style — assert the three caption strings each appear inside a `segmented-field__caption` span) and no `segmented-field__clear`; with `exitConsistency: 'Wet, soupy'` it contains `aria-label="Clear Exit consistency"` exactly once; the melt-block test at 522-528 keeps its `role="radiogroup" aria-label="Melt style"` assertion and gains: the melt row's `.segmented-field` follows the Melt test label inside `class="field-row field-row--start"`. The DOM-order test at 183-212 needs no change (it anchors on the caption strings, which still appear in order).
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/Segmented.test.jsx src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'className="segmented-field__head"' app/src/ui/Segmented.jsx` prints 1; `grep -c 'className="text-control segmented-field__clear"' app/src/ui/Segmented.jsx` prints 1; `grep -c "event.detail === 0" app/src/ui/Segmented.jsx` prints 1.
- `grep -c "onClear={() => onClearSegment(" app/src/ui/BatchRow.jsx` prints 3; `grep -c "function SegmentedField" app/src/ui/BatchRow.jsx` prints 0; `grep -c "<fieldset" app/src/ui/BatchRow.jsx` prints 0.
- In the rendered recording markup with the tasting open, each of the three caption strings appears inside a `segmented-field__caption` span, the Clear button appears only for a control whose draft value matches an option, and its `aria-label` is `Clear <caption>`.
- The Segmented, BatchRow and RecipePage suites pass.
  </acceptance_criteria>
  <done>Every segmented control has its caption line with a right-aligned Clear that appears once something is picked, clears through RecipePage's announcing handler, and moves focus only on a keyboard activation — the radio rule now has its way back on all nine groups.</done>
</task>

<task type="auto">
  <name>Task 2: The defects as two labelled groups inside the axes grid, the cue rows above the axes, and the retirement of the declared caption, helper and aria-label (007 lines 58-70, 86-90, 128-130, 261-282, 416-420; UAT items 11, 19; D-08 as superseded)</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/AxisMark.jsx, app/src/styles/app.css, app/src/ui/BatchRow.test.jsx, app/src/ui/AxisMark.test.jsx</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 58-70 (the one grid: `#axes`, `.axes-core`/`.axes-declared`, the cue rows' `grid-column`, the 3-column placements at 65-69 including the defects' columns at 68 and the rule at 69), 86-90 (`.defect`, `::before`, `.defects`, `.defects-head { grid-column: 1 / -1 }`), 113-117 (the stacked grid below 760px), 128-130 (defect min-height; the stacked declared group's rule), 261-282 (the markup: `#axes-rows` then the head and the two groups), 416-420 (the render order: cue rows first in 3-column mode, each cue heading its group when stacked).
- `.planning/sketches/007-full-battery/README.md` § "Ninth round" bullets "Group captions on their own row" and "The defects share the axes' grid and captions" (the measured facts: all three first-row heads at the same y; the rule spanning the chips; both group cues at the same y).
- The contract § "Visual treatment" → "Captions" (the group cues entry), "Axis rows" (the one grid entry), "Defects — a square and a word".
- `03.3.1.1-RESEARCH.md` § "Pattern 7: One grid for the axes and the defects, with cue rows", § "Re-citation against commit 977846a" item 4, § "Anti-Patterns" (the Bitter aria-label/helper/gap), § "Extras" (the Greasy film wrap at the 640px pen is Mark's call — expected, not a gap).
- `03.3.1.1-PATTERNS.md` § "BatchRow.jsx" → "Defects caption" and "Declared chip"; § "app.css — picked / hover / focus / forced-colors" (Plan 03 already wrote the `.batch-margin .chip-toggle` and `::before` rules — read them, do not rewrite them).
- `app/src/ui/BatchRow.jsx` lines 138-193 (`AxesGrid`), 586-628 (the defects block), 208-266 (`TastingReading` — the read view's "Bitter · declared" line stays; no sketch 007 counterpart, D-19); `app/src/ui/AxisMark.jsx` lines 27 and 59; `app/src/styles/app.css` lines 1799-1859 (`.axes-grid`, `.axes-rule`, `.axes-declared-caption`, the stacked rules) and the defects section Plan 03 wrote.
- `app/src/ui/BatchRow.test.jsx` lines 370-410 (the arrangement tests), 462-530 (the defects and melt tests); `app/src/ui/AxisMark.test.jsx` lines 101-119.
  </read_first>
  <action>
One grid for the axes and the defects, with the cue rows the ninth round drew:

1. `AxisMark.jsx`: remove the `declaredCaption` prop (line 27's default and line 59's render) — no axis carries a caption any more (README "Group captions on their own row").

2. `BatchRow.jsx`, `AxesGrid`: signature `AxesGrid({ axes, marks, below, onChangeMark, onClearMark, children })`; `renderAxis(axis)` takes one argument. Desktop (the `below === false` branch): `<div className="axes-grid"><div className="axes-rule" aria-hidden="true" /><p className="pen-caption axes-cue axes-cue--core">Every recipe</p><p className="pen-caption axes-cue axes-cue--declared">This recipe only</p>{Hardness}{Scoopability}{Body}{Smoothness}{Sweetness}{Oil}{children}</div>` (007 line 419: cue core, cue declared, the six axes row-major, then the defects). Stacked: `<div className="axes-grid axes-grid--stacked"><div className="axes-grid__group"><p className="pen-caption axes-cue">Every recipe</p>{the four core axes}</div><div className="axes-grid__group axes-grid__group--declared"><p className="pen-caption axes-cue">This recipe only</p>{Body}{Oil}</div>{children}</div>` (007 line 420) — the "Declared for this recipe" paragraph and the `declaredCaption` argument go. Rewrite the comments at 138-146 and 177-181.

3. `BatchRow.jsx`, the defects (586-628) move INSIDE the `<AxesGrid …>` element as its children, replacing the `role="group" aria-label` wrapper with (007 lines 263-281, verbatim strings): `<div className="defects-head"><p className="pen-caption" id="defects-caption">Any problems?</p><p className="pen-helper defects-head__helper">select all that apply</p></div>` (the helper is a SIBLING `<p>`, never inside the uppercase caption — RESEARCH Anti-Patterns); `<div className="defect-group defect-group--core" role="group" aria-labelledby="defects-caption defects-core-cue"><p className="pen-caption axes-cue" id="defects-core-cue">Every recipe</p><div className="defects-row__chips">{DEFECTS.map(… the existing four buttons, unchanged …)}</div></div>`; `<div className="defect-group defect-group--declared" role="group" aria-labelledby="defects-caption defects-declared-cue"><p className="pen-caption axes-cue" id="defects-declared-cue">This recipe only</p><div className="defects-row__chips"><button type="button" className="chip-toggle" aria-pressed={draft.bitterDeclared} onClick={onToggleBitter}>{DECLARED_FLAW}</button></div></div>` — Bitter is a plain defect: no helper span, no `aria-label`, no declared modifier class (007 line 279; the group cue carries the meaning through `aria-labelledby`, so a screen reader hears "Any problems? This recipe only, Bitter"). Rewrite the comments at 586-593, 599-602 and 614-617 (a marked defect's square fills; the word keeps its ink; `aria-pressed` is the fact — UX1-03).

4. `app.css`: delete `.axes-declared-caption` and its comment (1827-1836). Under a comment citing 007 lines 61, 67-69, 90, 130 and 263-281, add: `.axes-grid__group > .axes-cue { grid-column: 1 / -1; }` (007 line 61 — a cue spans its group's auto-fit columns; type and the 6px beneath come from `.pen-caption`); `.axes-grid:not(.axes-grid--stacked) > .axes-cue--core, .axes-grid:not(.axes-grid--stacked) > .defects-head, .axes-grid:not(.axes-grid--stacked) > .defect-group--core { grid-column: 1 / 3; }` (007 lines 67-68); `.axes-grid:not(.axes-grid--stacked) > .defect-group--core { padding-right: var(--gap-m); }` (007 line 68); `.axes-grid:not(.axes-grid--stacked) > .axes-cue--declared, .axes-grid:not(.axes-grid--stacked) > .defect-group--declared { grid-column: 3; padding-left: var(--axes-cols-clearance); }` (007 lines 67-68 — the same 12px the declared axes take, Plan 03); `.defects-head { display: flex; align-items: baseline; gap: var(--gap-s); flex-wrap: wrap; margin-bottom: var(--gap-xs); }` (007 lines 73, 118, 263); `.defects-head > .pen-caption, .defects-head > .pen-helper { margin: 0; }` (007 lines 264-265's inline margins); `.axes-grid--stacked > .defect-group--declared { border-top: var(--rule-graduation) solid var(--ink); padding-top: var(--gap-s); }` (007 line 130 — the 20px above the rule is `.axes-grid--stacked`'s existing `gap: var(--gap-m)`, the same distance 007 draws with a margin). The `.axes-rule` at `calc(200% / 3)` spans the whole grid's height, defects included, by construction (it is absolutely positioned top 0 / bottom 0).

5. Tests. `AxisMark.test.jsx`: delete the describe at 101-119 (the declared caption). `BatchRow.test.jsx`: replace 398-407 with (a) desktop: `class="axes-cue axes-cue--core"`-bearing "Every recipe" precedes `axes-cue--declared`-bearing "This recipe only", which precedes the first `class="axis-mark` — assert with `indexOf` on `>Every recipe<` and `>This recipe only<`; (b) stacked: "Every recipe" sits inside the first `.axes-grid__group` (its index is greater than the group's opening tag and smaller than the first `axis-mark`) and "This recipe only" inside `.axes-grid__group--declared`; (c) neither arrangement contains the retired "Declared for this recipe" caption text. Rewrite 462-511: the recording markup with the tasting open contains `role="group" aria-labelledby="defects-caption defects-core-cue"` and `role="group" aria-labelledby="defects-caption defects-declared-cue"`; five `aria-pressed` buttons; the head matches `/<div class="defects-head"><p class="pen-caption" id="defects-caption">Any problems\?<\/p><p class="pen-helper defects-head__helper">select all that apply<\/p><\/div>/`; the four DEFECTS strings verbatim; Bitter matches `/<button type="button" class="chip-toggle" aria-pressed="false">Bitter<\/button>/` (and `aria-pressed="true"` with `bitterDeclared: true`); the defects head's index is greater than the last `axis-mark__anchors` index and smaller than the `melt-block` index (inside the grid, after the axes); the pressed-state tests at 489-510 update `>Bitter ` to `>Bitter<`. At 516, the melt-block ordering anchor `defects-row` becomes `defects-head`.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/AxisMark.test.jsx src/ui/RecipePage.test.jsx src/styles</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'className="pen-caption axes-cue axes-cue--core">Every recipe<' app/src/ui/BatchRow.jsx` prints 1 and `grep -c '>This recipe only<' app/src/ui/BatchRow.jsx` prints 3 (the desktop cue, the stacked cue, the declared defect group's cue).
- `grep -c 'aria-labelledby="defects-caption defects-core-cue"' app/src/ui/BatchRow.jsx` prints 1 and `grep -c 'aria-labelledby="defects-caption defects-declared-cue"' app/src/ui/BatchRow.jsx` prints 1; `grep -c 'className="chip-toggle" aria-pressed={draft.bitterDeclared}' app/src/ui/BatchRow.jsx` prints 1.
- `grep -c "declaredCaption" app/src/ui/AxisMark.jsx app/src/ui/BatchRow.jsx app/src/ui/AxisMark.test.jsx | awk -F: '{s+=$2} END {print s}'` prints 0.
- `ruleFor('.axes-declared-caption')` is undefined; `ruleFor('.axes-grid:not(.axes-grid--stacked) > .defect-group--core')` declares `padding-right: var(--gap-m)`; `ruleFor('.axes-grid--stacked > .defect-group--declared')` declares `border-top: var(--rule-graduation) solid var(--ink)`; `ruleFor('.defects-head')` declares `display: flex`.
- The BatchRow, AxisMark, RecipePage and style suites pass.
  </acceptance_criteria>
  <done>The defects live in the axes grid as two labelled groups under the head, every defect a square and a word, Bitter plain in "This recipe only"; the cue rows head the axes in both arrangements; nothing derivation-era ("Declared for this recipe", "· declared") renders in the pen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maker input → draft state | the segmented Clear stores `''` through RecipePage's existing handler shape; the defect toggles are unchanged (`onChangeDefect`, `onToggleBitter`) |
| pointer/keyboard → focus | Segmented's Clear moves focus only on `event.detail === 0` |
| assistive tech → names | the defect groups' names come from `aria-labelledby` pairs of ids that must exist once per render |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1.1-10 | Denial of service (of a name) | duplicate or dangling ids (`defects-caption`, `segment-…-caption`) | low | mitigate | ids are rendered once per pen; the render-only tests assert each `aria-labelledby` target id appears in the markup |
| T-03.3.1.1-11 | Information disclosure | the declared flaw's accessible name | low | accept | "Declared for this recipe: Bitter" is retired for the sketch's group cue; the read view's "Bitter · declared" line is unchanged (no 007 counterpart, D-19) |
| T-03.3.1.1-12 | Tampering | option and defect strings | low | mitigate | every string is a domain constant or a JSX literal (`SEGMENT_OPTIONS`, `DEFECTS`, `DECLARED_FLAW`); no markup is built from maker text |
| T-03.3.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed; the gate does not fire |
</threat_model>

<verification>
- The UI and style suites pass after each task; `npm --prefix app test` and `npm --prefix app run build` pass at the end of the plan.
- The browser facts — the caption line's 24/44px before and after a pick on Exit consistency, the Clear's right edge, the two group cues at the same y with the five defects at the same y (Greasy film wrapping at the 640px pen is expected — Mark's call), the rule spanning the defects, the 13×13 squares, the stacked declared rule 20px below the row — are Plan 06's to measure.
</verification>

<success_criteria>
UAT items 4 and 13 (the caption line half), 11 and 19 (the cue rows) are closed in source against 007's cited lines; Pitfall 11 is closed (nine groups, nine Clears); D-08's structures are gone as the ninth round superseded them; every pin is green.
</success_criteria>

<output>
Create `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-SUMMARY.md` when done — list the ids and classes introduced and the tests replaced.
</output>
