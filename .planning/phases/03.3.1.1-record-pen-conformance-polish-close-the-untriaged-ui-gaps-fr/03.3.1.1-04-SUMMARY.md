---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 04
subsystem: ui
tags: [css, record-pen, sketch-conformance, segmented-control, defects, axes-grid]

# Dependency graph
requires:
  - phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr (plans 02, 03)
    provides: the .pen-caption/.pen-helper family, the .segmented-field/.segmented-field__head/.segmented-field__clear CSS Plan 03 wrote in advance, RecipePage's handleClearSegment/onClearSegment wiring, the axes-grid clearance mechanism and the joined-radio rule (no toggle-off) on stops and segmented options
provides:
  - "Segmented.jsx owns its own caption line: a .segmented-field__head holding the caption and a right-aligned Clear that appears once something is picked, reachable on all three controls (Exit consistency, Airiness (estimated), Melt style) and wired end to end to RecipePage's handleClearSegment via BatchRow's new onClearSegment prop"
  - "The defects live inside AxesGrid as two role=group children — Every recipe (four DEFECTS chips) and This recipe only (Bitter, plain, no helper, no aria-label) — under a defects-head caption/helper pair, sharing the axes grid's columns and vertical hairline"
  - "Two cue rows (Every recipe / This recipe only) head the axes on their own grid row (desktop) or atop each stacked group; no axis or defect group carries its own caption any more"
  - "AxisMark.jsx's declaredCaption prop and app.css's .axes-declared-caption rule are retired"
affects: [03.3.1.1-05, 03.3.1.1-06-conformance-pass]

# Actuals (#2632)
actuals:
  tokens: 9185
  tasks: 2
  commits: 2
plan_head_before: e334c3b624a32af7aa37b24d510648616a3d3558

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A joined control's caption line is the control's own component concern (Segmented.jsx), not a wrapping caller's fieldset/legend — SegmentedField's fieldset wrapper retired now that Segmented renders its own head and Clear, matching AxisMark.jsx's own established shape"
    - "AxesGrid takes a children prop so a caller can render grid items that share the grid's own columns and hairline (the defects) without AxesGrid needing to know what they are"
    - "A control group's Clear moves focus to the first item only on a keyboard activation (event.detail === 0) — now shared verbatim between AxisMark.jsx and Segmented.jsx"

key-files:
  created: []
  modified:
    - app/src/ui/Segmented.jsx
    - app/src/ui/Segmented.test.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/BatchRow.test.jsx
    - app/src/ui/AxisMark.jsx
    - app/src/ui/AxisMark.test.jsx
    - app/src/styles/app.css

key-decisions:
  - "Segmented.jsx's slug function (groupLabel.replace(/[^a-z0-9]+/gi, '-').toLowerCase(), pre-existing since before this plan, used for the radio group's `name`) produces a caption id with a cosmetic double hyphen for 'Airiness (estimated)' — segment-airiness-estimated--caption — because the trailing ')' collapses to a trailing '-' before '-caption' is appended. The id is still unique and valid HTML; no test asserts the exact string, so this is recorded as an observation, not fixed, since the slug function itself is out of this plan's scope."

requirements-completed: []

coverage:
  - id: D1
    description: "Each of the three segmented controls carries a caption line with a right-aligned Clear that appears once something is picked, wired to RecipePage's clear through BatchRow's onClearSegment prop; a keyboard Clear lands focus on the first option, a mouse Clear does not (007 @ 109733d lines 220, 228, 289, 350, 375-381; UAT items 4, 13; CONTEXT D-04 supersession)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/Segmented.test.jsx#Segmented — the caption line with its Clear (007 lines 220, 228, 289; Pitfall 11)"
        status: pass
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#BatchRow — the two segmented controls (contract \"Controls spec\") — the caption-line and Clear tests"
        status: pass
    human_judgment: true
    rationale: "The caption line's markup, the Clear's appearance/aria-label, and the keyboard-only focus guard are proven as text and DOM order; the actual rendered 24px/44px caption-line height before and after a pick, and the keyboard-vs-mouse focus behavior in a real browser, are Plan 06's conformance pass per this plan's own <verification> section."
  - id: D2
    description: "The defects are five square-and-word buttons in two labelled groups inside the axes grid — Every recipe (Coarse, icy · Sandy, gritty · Gummy, elastic · Greasy film) and This recipe only (Bitter, plain, no helper, no aria-label) — under Any problems? with select all that apply as a sibling helper (007 lines 263-281; UAT item 11; D-08 as superseded)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#BatchRow — the defects, two labelled groups inside the axes grid (contract \"Controls spec\")"
        status: pass
    human_judgment: false
  - id: D3
    description: "Two cue rows, Every recipe and This recipe only, head the axes: on their own grid row above the axes in the 3-column arrangement and heading each stacked group below 760px; no axis carries a caption of its own (007 lines 61, 67, 416-420; UAT item 19)"
    requirement: null
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#AxesGrid — the two DOM orders, one per arrangement (contract \"Keyboard and tab order\") — the cue-row tests"
        status: pass
      - kind: unit
        ref: "app/src/ui/AxisMark.test.jsx (the declared-caption describe block deleted; no axis renders a caption)"
        status: pass
    human_judgment: false
  - id: D4
    description: "In the 3-column arrangement the defects take the axes' columns and the vertical hairline spans them; stacked, the declared defect group sits under its own hairline like the declared axes (007 lines 68-69, 90, 130)"
    requirement: null
    verification:
      - kind: unit
        ref: "ruleFor('.axes-grid:not(.axes-grid--stacked) > .defect-group--core') declares padding-right: var(--gap-m); ruleFor('.axes-grid--stacked > .defect-group--declared') declares border-top: var(--rule-graduation) solid var(--ink); ruleFor('.defects-head') declares display: flex — verified live against css-source.js's readAllRules during execution"
        status: pass
    human_judgment: true
    rationale: "The grid-placement rule text and the padding/border arithmetic are proven directly against the parsed stylesheet; the actual rendered column alignment, the hairline spanning the defects, and the 20px stacked clearance in a real browser are Plan 06's conformance pass, per this plan's own <verification> section."

duration: 15min
completed: 2026-09-15
status: complete
---

# Phase 03.3.1.1 Plan 04: The segmented caption line and the defects as two labelled groups inside the axes grid Summary

**Segmented.jsx renders its own caption line with a right-aligned Clear wired to RecipePage's handleClearSegment on all three controls; the defects move inside AxesGrid as two role=group children (Every recipe / This recipe only) sharing the axes grid's columns and hairline, headed by two cue rows that replace AxisMark's retired declaredCaption prop**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-15T16:52:42Z
- **Completed:** 2026-09-15T17:07:25Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- `Segmented.jsx`: takes a new `onClear` prop; renders `.segmented-field > .segmented-field__head (.segmented-field__caption + Clear when picked) + .segmented[role=radiogroup]`; `handleClear(event)` moves focus to the first option only on `event.detail === 0`, mirroring `AxisMark.jsx`
- `BatchRow.jsx`: `SegmentedField` (fieldset/legend wrapper) retired; the three `<Segmented>` call sites (Exit consistency, Airiness (estimated), Melt style) pass `onClear`; new `onClearSegment` prop threaded from RecipePage (already wired there since Plan 03); `AxesGrid` gains a `children` prop and renders the two group cues on their own row/heading each stacked group; the defects render as `AxesGrid`'s children — a `defects-head` (caption + sibling helper) plus two `role="group" aria-labelledby` blocks; Bitter is a plain `.chip-toggle` with no helper and no aria-label of its own
- `AxisMark.jsx`: `declaredCaption` prop removed — no axis carries a caption any more
- `app.css`: the axes-grid placement rules extended so the defects head and groups share the axes' desktop columns and padding; a new `.axes-grid--stacked > .defect-group--declared` rule gives the stacked declared defects their own top rule, matching the declared axes; `.axes-declared-caption` retired
- `Segmented.test.jsx`, `AxisMark.test.jsx`, `BatchRow.test.jsx`: pins moved to the new caption-line/Clear markup, the cue rows, and the two labelled defect groups; the declared-caption describe block in `AxisMark.test.jsx` deleted outright

## Task Commits

Each task was committed atomically:

1. **Task 1: The segmented caption line with its Clear — three controls, one component, wired to RecipePage's clear** - `3d9f7f1` (feat)
2. **Task 2: The defects as two labelled groups inside the axes grid, the cue rows above the axes, and the retirement of the declared caption, helper and aria-label** - `0967695` (feat)

**Plan metadata:** committed separately with this SUMMARY.

## Tracer Feedback Gate

Task 1 is `type="tracer"`. Per the checkpoints.md precedence chain (row 3: interactive run, `human_verify_mode: end-of-phase`, tracer `<verify>` carries only `<automated>`, no `gate="blocking-human"`): re-ran `npm --prefix app test -- src/ui/Segmented.test.jsx src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx` immediately after Task 1's commit — 177/177 passed — logged `⚡ Tracer verified end-to-end — expanding`, and continued directly to Task 2 with no checkpoint.

## Files Created/Modified
- `app/src/ui/Segmented.jsx` - the `.segmented-field` head with its caption and Clear; the keyboard-guarded `handleClear`
- `app/src/ui/Segmented.test.jsx` - the caption-line/Clear describe block; header comment
- `app/src/ui/BatchRow.jsx` - `SegmentedField` deleted; the three `Segmented` call sites gain `onClear`; `onClearSegment` prop; `AxesGrid`'s `children` prop and cue rows; the defects moved inside `AxesGrid` as two labelled groups
- `app/src/ui/BatchRow.test.jsx` - the caption-line/Clear tests, the cue-row tests, the two-labelled-groups defects tests, the melt-block ordering anchor
- `app/src/ui/AxisMark.jsx` - `declaredCaption` prop and its render removed
- `app/src/ui/AxisMark.test.jsx` - the declared-caption describe block deleted; the now-unused `bodyAxis` fixture removed
- `app/src/styles/app.css` - `.axes-declared-caption` retired; the axes-grid placement rules extended to `.defects-head`/`.defect-group--core`/`.defect-group--declared`/`.axes-cue`; the stacked declared defect group's own top rule

## Decisions Made
- See key-decisions above (the caption id's cosmetic double hyphen for "Airiness (estimated)" — an observation on pre-existing slug logic, not a defect).
- No architectural changes; both tasks executed as planned, including the tracer feedback gate after Task 1.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `npm --prefix app test` (925 tests) and `npm --prefix app run build` both pass at the end of the plan.
- Ids introduced: `defects-caption`, `defects-core-cue`, `defects-declared-cue`; per-control caption ids `segment-<slug>-caption` (`segment-exit-consistency-caption`, `segment-airiness-estimated--caption`, `segment-melt-style-caption`).
- Classes introduced: `.segmented-field`, `.segmented-field__head`, `.segmented-field__caption`, `.segmented-field__clear` (all already styled by Plan 03; this plan supplies the matching markup); `.defects-head`, `.defects-head__helper`, `.defect-group`, `.defect-group--core`, `.defect-group--declared`, `.axes-cue`, `.axes-cue--core`, `.axes-cue--declared`.
- Tests replaced: `AxisMark.test.jsx`'s declared-caption describe block (deleted, no replacement — the fact it tested no longer exists); `BatchRow.test.jsx`'s "Declared for this recipe" caption test (replaced by the two cue-row tests); `BatchRow.test.jsx`'s defects-checklist describe (rewritten for the two `role="group"` blocks and the plain Bitter button); the melt-block ordering test's `defects-row` anchor (renamed `defects-head`).
- Plan 05 (ceremony wording) and Plan 06 (the conformance pass) can proceed — this plan's markup and CSS are in place for both.
- Plan 06 needs to confirm the browser facts this plan's tests cannot: the caption line's 24/44px before and after a pick on Exit consistency, the Clear's right edge, the two group cues at the same y with the five defects at the same y (Greasy film wrapping at the 640px pen is expected — Mark's call), the rule spanning the defects, and the stacked declared rule 20px below the row.

## Self-Check: PASSED

- Key files verified on disk: `app/src/ui/Segmented.jsx`, `app/src/ui/BatchRow.jsx`, `app/src/ui/AxisMark.jsx`, `app/src/styles/app.css`, this SUMMARY.md — all FOUND.
- Commits verified in `git log --oneline`: `3d9f7f1` (Task 1), `0967695` (Task 2) — both FOUND.
- Both tasks' `<acceptance_criteria>` re-run and PASS (grep/ruleFor checks shown inline during execution).
- Plan-level `<verification>`: `npm --prefix app test -- src/ui/Segmented.test.jsx src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx` passes after Task 1 (177/177); `npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/AxisMark.test.jsx src/ui/RecipePage.test.jsx src/styles` passes after Task 2 (278/278); `npm --prefix app test` (full suite) passes (925/925); `npm --prefix app run build` compiles.

---
*Phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr*
*Completed: 2026-09-15*
