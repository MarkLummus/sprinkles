---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 05
type: execute
wave: 5
depends_on: ["03.3.1.1-02", "03.3.1.1-04"]
files_modified:
  - app/src/ui/PenFoot.jsx
  - app/src/ui/PenFoot.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
autonomous: true
requirements: []
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Both save ceremonies read, in DOM order, Add tasting | Cancel | Save batch while the tasting section is absent and no restore is pending, and Cancel | Save batch otherwise — the same component, right-aligned at every width (007 @ 109733d lines 55, 301-306, 310-314, 484; CONTEXT D-14; UAT item 5; 03.3.1 D-01 not reopened)"
    - "While a removed tasting can still be restored, Restore tasting stands in Add tasting's place in the end-of-record ceremony and Add tasting is hidden in both ceremonies; the foot ceremony never carries a restore control (007 lines 245, 303, 484, 505-510; the ninth round)"
    - "The removal toasts — 'Tasting removed.' and 'Tasting removed. You can restore it.' — appear in the end-of-record ceremony's own live region at the left of its row, not at the foot; 'Tasting restored.' stays on the tasting head's region (007 lines 303, 543, 561, 582)"
    - "Focus after an empty removal lands on the end-of-record Add tasting; after a data removal on Restore tasting; after a restore on Remove tasting (007 lines 542, 560, 578)"
  artifacts:
    - app/src/ui/PenFoot.jsx
    - app/src/ui/BatchRow.jsx
    - app/src/ui/RecipePage.jsx
  key_links:
    - "SaveCeremony's onAddTasting/onRestore props → the two mounts' guards (BatchRow: !draft.tastingOpen && !pendingUndo for the opener, pendingUndo && !draft.tastingOpen for the restore; PenFoot: !tastingOpen && !pendingUndo for the opener, never a restore) — the guard is what makes the two ceremonies identical in every state (03.3.1 D-01)"
    - "RecipePage's announce({ target: 'record' }) → BatchRow's recordStatus prop → SaveCeremony's status paragraph (.save-ceremony__status, whose margin-right: auto Plan 02 wrote) — the toast lives where the action was"
    - "RecipePage's removeTastingAttempt → BatchRow's focus effect choosing restoreRef or addTastingRef by pendingUndo (the effect moves out of PenFoot; the foot's button is no longer a landing)"
---

<objective>
Close UAT item 5 and the ninth round's ceremony redraw against sketch 007 (`.planning/sketches/007-full-battery/index.html` @ `109733d`; every line number unchanged since `2a212be`): Add tasting stands beside BOTH ceremonies as the first control of the right-aligned set (D-14, without reopening 03.3.1 D-01 — the two mounts render one component); Restore tasting replaces "Undo clear tasting" and takes Add tasting's place while a restore is pending; the removal toasts move to the end-of-record ceremony's own `record-status` live region; the focus landings follow the sketch's lines 542, 560 and 578.

Purpose: the sketch's ninth round drew D-14 into the reference and reversed the 03.3.1 footer order, so the app follows the sketch (D-01); the toast and the restore control move to where the action happened, which is the feedback rule the cross-cutting findings already carry.

Output: `SaveCeremony` gains `onAddTasting`, `addTastingRef`, `onRestore`, `restoreRef`, `status`; PenFoot's separate opener and undo slot retire; BatchRow's ceremony A carries the region, the restore and the focus effect; RecipePage gains `recordStatus` with the guarded self-clear and routes the removal toasts to it; the render-only suites pass with their pins moved.

Spec-less probe fallback: skipped visibly — no requirement IDs to probe; the sketch HTML plus CONTEXT.md is the design contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-RESEARCH.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-PATTERNS.md
@.planning/sketches/007-full-battery/index.html
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@app/src/ui/PenFoot.jsx
@app/src/ui/BatchRow.jsx
@app/src/ui/RecipePage.jsx
</context>

## Artifacts this phase produces (this plan's share)

- `PenFoot.jsx`: `SaveCeremony({ onCancel, onSave, hint, onAddTasting = null, addTastingRef = null, onRestore = null, restoreRef = null, status })` rendering, in order, the status paragraph (`p.save-ceremony__status.pen-helper[role=status][aria-live=polite]`, only when `status` is a string), the hint, `button.text-control.undo-control` "Restore tasting" (when `onRestore`), `button.text-control.save-ceremony__add-tasting` "Add tasting" (when `onAddTasting`), Cancel, Save batch. `PenFoot` passes `onAddTasting` guarded by `!tastingOpen && !pendingUndo`; its own opener, undo slot, `removeTastingAttempt` prop and focus effect retire.
- `BatchRow.jsx`: props `onAddTasting`, `recordStatus`, `removeTastingAttempt`; refs `addTastingRef`, `restoreRef`; the removal focus effect; ceremony A's mount with `status`, `onAddTasting`, `onRestore`; the head's restore control reads "Restore tasting".
- `RecipePage.jsx`: `TASTING_REMOVED_DATA_STATUS = 'Tasting removed. You can restore it.'`; `recordStatus` state + timer ref; `announce(…, { target: 'record' })`; the removal toasts routed to it; `recordStatus` cleared wherever `tastingStatus` is; `onAddTasting`, `recordStatus`, `removeTastingAttempt` passed to BatchRow.

<tasks>

<task type="tracer">
  <name>Task 1: Add tasting | Cancel | Save batch in both ceremonies, and Restore tasting in the end-of-record set (D-14; UAT item 5; 007 lines 245, 301-306, 310-314, 484, 505-510)</name>
  <files>app/src/ui/PenFoot.jsx, app/src/ui/BatchRow.jsx, app/src/ui/RecipePage.jsx, app/src/ui/PenFoot.test.jsx, app/src/ui/BatchRow.test.jsx</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 55 (`.pen .saves`: right-aligned; the slots are `display: contents`; `#record-status { margin-right: auto }`), 245 (Restore tasting in the head slot), 301-306 (the end-of-record ceremony: status, record slot, Add tasting, Cancel, Save batch), 310-314 (the foot ceremony: Add tasting, Cancel, Save batch — no slot), 480-485 (`renderAddTasting`: hidden while the section is open OR a restore is pending), 505-510 (`placeUndo`: the restore control moves between the head slot and the record slot).
- The contract § "Visual treatment" → "Buttons and text controls" (the two ceremonies; Restore tasting) and "Superseded by the ninth round" (the Save and save-pair matrix; the undo lifecycle).
- `03.3.1.1-RESEARCH.md` § "Pattern 5: The two ceremonies", § "Re-citation against commit 977846a" items 1 and 2, § "Common Pitfalls" 8, § "D-03 Triage" row 5.
- `03.3.1.1-PATTERNS.md` § "PenFoot.jsx" and § "PenFoot.test.jsx".
- `app/src/ui/PenFoot.jsx` (all 119 lines); `app/src/ui/BatchRow.jsx` lines 291-319 (props), 507-528 (the head with its undo control), 651-656 (ceremony A); `app/src/ui/RecipePage.jsx` lines 1636-1664 (the BatchRow mount) and 1747-1761 (the PenFoot mount); `app/src/styles/app.css` (Plan 02's `.save-ceremony { justify-content: flex-end }` and `.save-ceremony__status`).
- `app/src/ui/PenFoot.test.jsx` (all); `app/src/ui/BatchRow.test.jsx` lines 183-212, 330-360 (the undo pins at 340 and 349-350).
  </read_first>
  <action>
One component, two mounts, one order:

1. `PenFoot.jsx`, `SaveCeremony`: signature `({ onCancel, onSave, hint, onAddTasting = null, addTastingRef = null, onRestore = null, restoreRef = null })` (Task 2 adds `status`). Render, in this DOM order inside `.save-ceremony`: the hint paragraph (when `hint`), `{onRestore && <button type="button" className="text-control undo-control" ref={restoreRef} onClick={onRestore}>Restore tasting</button>}` (007 lines 245, 303 — the record slot; while pending it takes Add tasting's place so the set fits one row at 360px, README ninth round), `{onAddTasting && <button type="button" className="text-control save-ceremony__add-tasting" ref={addTastingRef} onClick={onAddTasting}>Add tasting</button>}` (007 lines 304, 311 — the text control FIRST), then Cancel, then Save batch (unchanged). Rewrite the header comment (3-11): the ninth round reversed the 03.3.1 footer order; both mounts read Add tasting | Cancel | Save batch, right-aligned (007 line 55; Plan 02's rule), and Restore tasting stands in the opener's place while a restore is pending.

2. `PenFoot.jsx`, the foot: the record/amend branch renders only `<SaveCeremony onCancel={onCancelRecording} onSave={onSaveBatch} hint={penHint} onAddTasting={!tastingOpen && !pendingUndo ? onAddTasting : null} />` (007 line 484's guard; the foot has NO restore slot — 007 lines 310-314). Delete the separate Add tasting button (99-103) and the undo footer slot (104-113) with their comments. Keep the `addTastingRef` effect (57-66) for this task but attach the ref to the ceremony via `addTastingRef={addTastingRef}` so the existing empty-removal focus landing still works until Task 2 moves it to BatchRow's mount; rewrite the header comment at 37-41.

3. `BatchRow.jsx`: add `onAddTasting` to the props (beside `onRemoveTasting`); ceremony A (line 656) becomes `<SaveCeremony onCancel={onCancelRecording} onSave={onSaveBatch} hint={blockedDateMessage} onAddTasting={!draft.tastingOpen && !pendingUndo ? onAddTasting : null} onRestore={pendingUndo && !draft.tastingOpen ? onUndoRemove : null} />` (007 lines 301-306, 484, 505-510); the head's undo button (517) reads `Restore tasting` (007 line 245) — same class, same handler. Rewrite the comment at 651-655 and the head comment's undo sentences (497-506). `RecipePage.jsx`: pass `onAddTasting={handleAddTasting}` to `<BatchRow>` (the same handler PenFoot receives).

4. Tests. `PenFoot.test.jsx` 105-133: keep "renders Add tasting for the record/amend pen when tastingOpen is false" and "no Add tasting once the section is open"; the order test (121-127) inverts — Add tasting's index is greater than `class="save-ceremony"`'s and smaller than `Cancel`'s (inside the ceremony, first); add "renders no Add tasting while a restore is pending" (`pendingUndo` set, `tastingOpen` false). 135-178 becomes "the foot ceremony carries no restore control in any state": with `pendingUndo` set and `tastingOpen` false or true, the markup contains neither `Restore tasting` nor `undo-control`, and (for Task 2's shape) no `save-ceremony__status`. 192-218 (`SaveCeremony` unit tests): add — with `onAddTasting`: `Add tasting` precedes `Cancel` which precedes `Save batch`; with `onRestore` and no `onAddTasting`: `Restore tasting` precedes `Cancel` and `Add tasting` is absent; with neither: neither string. `BatchRow.test.jsx` 330-360: every pin on the retired undo label becomes `Restore tasting` (the head slot when `tastingOpen` and `pendingUndo`); add in the ceremony describe: with `tastingOpen: false` and `pendingUndo: null`, ceremony A's markup has `Add tasting` after `class="save-ceremony"` and before `Cancel`; with `pendingUndo` set and `tastingOpen: false`, it has `Restore tasting` before `Cancel` and no `Add tasting`; with `tastingOpen: true`, neither.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/PenFoot.test.jsx src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'className="text-control save-ceremony__add-tasting"' app/src/ui/PenFoot.jsx` prints 1 and `grep -c '>Add tasting<' app/src/ui/PenFoot.jsx` prints 1 (the one opener lives in SaveCeremony); `grep -c '>Restore tasting<' app/src/ui/PenFoot.jsx` prints 1 and `grep -c '>Restore tasting<' app/src/ui/BatchRow.jsx` prints 1 (the head slot).
- `grep -c "onAddTasting={!draft.tastingOpen && !pendingUndo ? onAddTasting : null}" app/src/ui/BatchRow.jsx` prints 1; `grep -c "onRestore={pendingUndo && !draft.tastingOpen ? onUndoRemove : null}" app/src/ui/BatchRow.jsx` prints 1; `grep -c "onAddTasting={handleAddTasting}" app/src/ui/RecipePage.jsx` prints 2 (BatchRow and PenFoot).
- In the rendered markup, both ceremonies list Add tasting before Cancel before Save batch while the section is absent and no restore is pending; the foot never renders Restore tasting; ceremony A renders Restore tasting in the opener's place while a restore is pending.
- The PenFoot, BatchRow and RecipePage suites pass.
  </acceptance_criteria>
  <done>The two ceremonies are one component reading Add tasting | Cancel | Save batch, right-aligned, with Restore tasting standing in the opener's place while a restore is pending — the visible half of item 5 and the ninth round's ceremony rule.</done>
</task>

<task type="auto">
  <name>Task 2: The record-status live region, the removal toasts' new home, and the focus landings (007 lines 303, 484, 503, 542-543, 560-561, 578, 582)</name>
  <files>app/src/ui/RecipePage.jsx, app/src/ui/BatchRow.jsx, app/src/ui/PenFoot.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/BatchRow.test.jsx</files>
  <read_first>
- `.planning/sketches/007-full-battery/index.html` @ `109733d` — lines 303 (`#record-status`, a `.helper` live region first in the end-of-record row), 334-339 (`announceTransient`: the guarded five-second clear, per target), 498-504 (`addTasting` clears both statuses), 531-567 (`removeTasting`: the empty path focuses the end-of-record Add tasting and announces "Tasting removed." to record-status; the data path focuses Restore tasting and announces "Tasting removed. You can restore it." to record-status), 568-584 (`undoTasting`: focus to the Remove control; "Tasting restored." to tasting-status).
- The contract § "Visual treatment" → "Superseded by the ninth round" (Feedback and undo lifecycle).
- `03.3.1.1-RESEARCH.md` § "Pattern 5" (the toast routing paragraph), § "Re-citation against commit 977846a" item 2, § "Common Pitfalls" 8, § "Open Questions" 3, § "Anti-Patterns" (routing the removal toast to the foot).
- `app/src/ui/RecipePage.jsx` lines 41-54 (the status constants), 600-637 (the status/attempt state), 933-960 (`announce`), 968-983 (`handleStartRecording`), 1170-1181 (`handleAddTasting`), 1183-1219 (`handleRemoveTasting`, `handleUndoRemove`), every `setTastingStatus('')` site (grep it: 978, 1160, 1178, 1196, 1307), 1636-1664 and 1747-1761 (the two mounts).
- `app/src/ui/BatchRow.jsx` lines 337-362 (the existing attempt-keyed focus effects — the pattern to copy), 651-656 (ceremony A); `app/src/ui/PenFoot.jsx` lines 1, 42-66 (the effect and prop to remove).
- `app/src/ui/RecipePage.test.jsx` lines 436-442 (the string pins); `app/src/ui/BatchRow.test.jsx` lines 310-320 (the status paragraph regex pattern to copy), 629-645.
  </read_first>
  <action>
The toast lives where the action was, and focus lands where the sketch says:

1. `RecipePage.jsx`: `TASTING_REMOVED_DATA_STATUS` becomes `'Tasting removed. You can restore it.'` (007 line 561; rewrite the comment at 41-46 to cite the ninth round). Beside `tastingStatus` add `const [recordStatus, setRecordStatus] = useState('')` and `recordStatusTimerRef` with a comment: the end-of-record ceremony's own live region (007 line 303) — the removal toasts' channel; `formStatus` stays the foot's save/validation channel (007 line 309). `announce` gains a `target === 'record'` branch identical in shape to the `tasting` branch (the guarded five-second self-clear against its own timer). `handleRemoveTasting` announces with `{ selfClear: true, target: 'record' }` (007 lines 543, 561) — rewrite its comment (the focus landing is now BatchRow's effect: the end-of-record Add tasting after an empty removal, Restore tasting after a data removal). `handleAddTasting` also clears `recordStatus` (007 line 503); add `setRecordStatus('')` beside every `setTastingStatus('')` reset (start recording, cancel, the save path, add tasting, remove tasting). Pass `recordStatus={recordStatus}` and `removeTastingAttempt={removeTastingAttempt}` to `<BatchRow>`; remove `removeTastingAttempt` from `<PenFoot>`.

2. `BatchRow.jsx`: props `recordStatus = ''` and `removeTastingAttempt = null`; refs `addTastingRef` and `restoreRef` (`useRef(null)`); a new effect beside the three existing attempt-keyed effects: when `removeTastingAttempt` changes and is not null, focus `restoreRef.current` if `pendingUndo` is set, else `addTastingRef.current` (007 lines 542 and 560; the same single-dependency attempt-counter pattern as `blockedDateAttempt` — the effect reads `pendingUndo` from the render that carried the attempt, which RecipePage sets in the same batch). Ceremony A's mount gains `status={recordStatus} addTastingRef={addTastingRef} restoreRef={restoreRef}`.

3. `PenFoot.jsx`: `SaveCeremony` gains a `status` prop and renders, FIRST inside `.save-ceremony`, `{typeof status === 'string' && <p className="save-ceremony__status pen-helper" role="status" aria-live="polite">{status}</p>}` (007 line 303 — a `.helper` live region; Plan 02's `.save-ceremony__status { margin-right: auto }` keeps the controls flush right even when it is empty); the foot's mount passes no `status`, so the foot renders no region. Delete `PenFoot`'s `removeTastingAttempt` prop, `addTastingRef` and its effect (57-66), and drop `useEffect`/`useRef` from the import if nothing else in the file uses them. Rewrite the header comment's focus-landing sentences.

4. Tests. `RecipePage.test.jsx` 441: `'Tasting removed. You can restore it.'`; the describe name at 439 cites the ninth round. `BatchRow.test.jsx`: in the ceremony describe add — with `recordStatus: 'Tasting removed. You can restore it.'` and `pendingUndo` set, `tastingOpen: false`, the markup matches `/<div class="save-ceremony"><p class="save-ceremony__status pen-helper" role="status" aria-live="polite">Tasting removed\. You can restore it\.<\/p>/` and the `Restore tasting` button follows it; with `recordStatus: ''` the empty paragraph still renders first (`<p class="save-ceremony__status pen-helper" role="status" aria-live="polite"></p>`) so the live region exists before it speaks; the form-status test at 629-645 stays (the foot channel is unchanged). `PenFoot.test.jsx` already asserts (Task 1) that the foot renders no `save-ceremony__status`.
  </action>
  <verify>
    <automated>npm --prefix app test -- src/ui/RecipePage.test.jsx src/ui/BatchRow.test.jsx src/ui/PenFoot.test.jsx</automated>
    <fails_when>non-zero exit, or the Vitest summary line reports any failed tests or test files</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c "TASTING_REMOVED_DATA_STATUS = 'Tasting removed. You can restore it.'" app/src/ui/RecipePage.jsx` prints 1; `grep -c "target === 'record'" app/src/ui/RecipePage.jsx` prints 1; `grep -c "target: 'record'" app/src/ui/RecipePage.jsx` prints 1 (the removal toast); `grep -c "setRecordStatus('')" app/src/ui/RecipePage.jsx` prints at least 4.
- `grep -c "recordStatus={recordStatus}" app/src/ui/RecipePage.jsx` prints 1 and `grep -c "removeTastingAttempt={removeTastingAttempt}" app/src/ui/RecipePage.jsx` prints 1 (BatchRow's mount only).
- `grep -c 'className="save-ceremony__status pen-helper"' app/src/ui/PenFoot.jsx` prints 1; `grep -c "removeTastingAttempt" app/src/ui/PenFoot.jsx` prints 0; `grep -c "restoreRef.current" app/src/ui/BatchRow.jsx` prints 1.
- The RecipePage, BatchRow and PenFoot suites pass; `npm --prefix app test` (the full suite) passes; `npm --prefix app run build` compiles.
  </acceptance_criteria>
  <done>The removal toasts speak from the end-of-record ceremony's own region with the sketch's words; focus lands on the end-of-record Add tasting, on Restore tasting, or on Remove tasting exactly as 007 lines 542, 560 and 578 do; the foot keeps only the save and validation channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maker input → draft state | the removal/restore payload path (`tastingPayloadFromDraft`, `restoreDraftFromUndo`, T-03.3.1-10's by-value copies) is unchanged; only which region announces and which control receives focus change |
| status text → live regions | every status string is a constant in RecipePage.jsx; no maker text is announced |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1.1-13 | Denial of service (of the restore) | the pending restore with no visible control | medium | mitigate | Task 1 renders Restore tasting in ceremony A while `pendingUndo && !tastingOpen` and in the head slot while open, BEFORE the foot's slot is removed in the same task; the render-only tests pin both placements |
| T-03.3.1.1-14 | Denial of service (of focus) | the removal focus effect reading a ref that did not render | low | mitigate | the effect chooses the ref by the same `pendingUndo` guard that renders the button; optional chaining leaves focus where it was if the button is absent |
| T-03.3.1.1-15 | Tampering | live-region text | low | mitigate | constants only; `role="status"`/`aria-live="polite"` on a `<p>` rendering a string |
| T-03.3.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed; the gate does not fire |
</threat_model>

<verification>
- The three UI suites pass after each task; `npm --prefix app test` and `npm --prefix app run build` pass at the end of the plan (the last build plan before the pass).
- The browser facts — the ceremony flush right at every width, the toast at the left of the end-of-record row, Restore tasting | Cancel | Save batch on one row at 393, the three focus landings — are Plan 06's sequence checks (RESEARCH § "Conformance pass" 5b).
</verification>

<success_criteria>
UAT item 5 is closed against 007 lines 301-314 without reopening 03.3.1 D-01; the ninth round's Restore tasting, `record-status` and focus landings are in source with their strings verbatim; the full suite and the build are green going into the conformance pass.
</success_criteria>

<output>
Create `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-SUMMARY.md` when done — record the three status channels (form, tasting, record) and which sentence each carries.
</output>
