---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
plan: 06
type: execute
wave: 6
depends_on: ["03.3.1.1-01", "03.3.1.1-02", "03.3.1.1-03", "03.3.1.1-04", "03.3.1.1-05"]
files_modified:
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-conformance-probe.js
autonomous: false
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "03.3.1.1-CONFORMANCE.md exists with one row per contract element per width per applicable state, each row naming the app selector, the sketch selector, the contract section, the property, the sketch value with its line, the app value, the delta and a verdict (D-17)"
    - "Every measurement is a computed-style or bounding-rect reading taken in both tabs by getComputedStyle / getBoundingClientRect at the ten D-16 widths — 1280, 1180, 1080, 980, 880, 780, 680, 580, 480, 393 — in the three states; screenshots are never the verdict (D-18)"
    - "No row for any of the 18 UAT items carries the verdict gap when the pass closes; a gap on one of the 18 was fixed and re-measured before UAT (D-19)"
    - "Every divergence beyond the 18 is logged with the verdict extra and left for Mark's post-UAT triage, never auto-closed (D-19); the reference is the live sketch in .planning/sketches at the cited commits, not the skill's frozen copy (D-20)"
    - "document.scrollWidth equals window.innerWidth at 393px with a batch in view and the record pen open (D-15's criterion; the STATE.md blocker retires)"
  artifacts:
    - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md
    - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-conformance-probe.js
  key_links:
    - "CONFORMANCE.md → 03.3.1.1-UAT.md (D-23: one UAT test per gap of the 18 plus one per state-and-width pair Mark walks is scripted from this file; the pass gates UAT)"
    - "the probe helper → both tabs (the same function reads both, so a difference is the sketch's versus the app's, never the reader's)"
    - "the sketch commit hashes in the file's head → git log -1 on the two sketch files (a moved sketch invalidates every line citation; D-20)"
---

<objective>
Run the side-by-side conformance pass that gates UAT (D-17 to D-20): the orchestrator serves the live sketches and the app, walks every contract element at the ten D-16 widths in the three states (blank pen; fully marked pen with hover and focus spot checks; the record saved and read back against sketch 003), measures with `getComputedStyle` and `getBoundingClientRect` in both tabs (D-18), and writes `03.3.1.1-CONFORMANCE.md`. A gap on one of the 18 blocks UAT until fixed and re-measured; divergences beyond the 18 are logged as extras for Mark's post-UAT triage (D-19).

Purpose: 03.3.1 self-certified against a prose derivation; this pass measures the build against the sketch HTML itself, in a browser, at real widths — the method fix the phase exists for (CONTEXT § "Why this phase exists"). The contract's "Visual treatment (from the sketch CSS)" section (research wrote it per D-02) names each element's contract section for the rows; the HTML at the cited commits is the value (D-01, D-20); the read view's uppercase captions are checked under D-11.

Output: `03.3.1.1-CONFORMANCE.md` (the measurement record UAT is scripted from) and `03.3.1.1-conformance-probe.js` (the helper both tabs run). No `app/` file changes unless a gap on one of the 18 needs a fix commit, applied and re-measured inside this plan.

Who does what: `gsd-executor` has no browser tools (03.3.1-06 precedent), so Task 1 (an executor task) prepares the artifacts and the preflight, and Task 2 is a blocking checkpoint the ORCHESTRATOR performs with `gsd-browser` (Playwright: exact viewports down to 393) and, for interactive spot checks at 500px and above, `claude-in-chrome`. The checkpoint is typed `checkpoint:human-action` deliberately: this project's `human_verify_mode` is `end-of-phase`, which would fold a `human-verify` task into UAT — and D-17 says the pass gates UAT, so it must halt execution here instead. Mark's own confirmation is the UAT that follows, not this checkpoint.

Spec-less probe fallback: skipped visibly — no requirement IDs to probe; the sketch HTML plus CONTEXT.md is the design contract.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-RESEARCH.md
@.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-PATTERNS.md
@.planning/sketches/007-full-battery/index.html
@.planning/sketches/003-front-matter-rows/index.html
@.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-06-SUMMARY.md
@.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-UAT.md
</context>

## Artifacts this phase produces (this plan's share)

- `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md` — YAML head (`status`, `phase`, `sketch_007`, `sketch_003`, `sketch_008`, `app_commit`, `tools`, `widths`, `states`), sections "How the pass was run", "Pre-declared departures (never gaps)", "The 18 items" (I-01 … I-18 mapped to elements), "Element checklist", "Measurements" (the table), "Sequence checks", "Extras log (D-19)", "Result".
- `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-conformance-probe.js` — `probe(selector)`, `shareRow(a, b)`, `follows(a, b)`, `lines(el)`, the sketch state-setting snippets, the frame-equalisation snippet, the app↔sketch selector map, and a `row(...)` markdown formatter.
- No app code, unless a fix commit closes a gap on one of the 18 (recorded in the file's "Fixes applied" list and re-measured).

<tasks>

<task type="auto">
  <name>Task 1: Preflight and the pass's artifacts — the full suite, the build, the sketch commits, the CONFORMANCE.md skeleton and the probe helper (D-17, D-18, D-20)</name>
  <files>.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md, .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-conformance-probe.js</files>
  <read_first>
- `03.3.1.1-RESEARCH.md` § "Conformance pass: recommended procedure (D-16–D-20)" (all seven points, the element list in 5, the sequence checks in 5b, the row layout in 6), § "Code Examples" → "Conformance probe" and "Setting the sketch's state", § "Common Pitfalls" 6, 7, 9, 12, § "Open Questions" 4, 5, 6, § "Extras already visible from source", § "The page shell" row S10 and S11, § "Amendment 2026-09-14 … Conformance pass addition".
- `03.3.1.1-PATTERNS.md` § "No Analog Found" (the row layout and the two nearest artifact shapes).
- `03.3.1-06-SUMMARY.md` lines 125-145 and 205-215 (how the previous browser pass was run and what it could not reach); `03.3.1-UAT.md` lines 1-45 (the YAML head and numbered-test shape UAT will take).
- `.planning/sketches/007-full-battery/index.html` @ `109733d` lines 319-632 (the functions the state snippets call: `selectRadio` 341, `clearGroup` 375, `renderAxes` 394, `toggleDefect` 428, `setCols` 436, `setVariant` 445, `setTastingMode` 488, `addTasting` 498, `removeTasting` 531, `undoTasting` 568; init 627-629) and line 109 (`.frame` width — Pitfall 9); `.planning/sketches/003-front-matter-rows/index.html` @ `0fa1015` lines 510-511 (the "full" preset bug), 316-320 (the numeric presets), 526 (init).
- `.planning/sketches/themes/default.css` (the colour values the probe compares: `--pen-blue` → `rgb(31, 61, 122)`, `--ground` → `rgb(247, 247, 244)`, `--ink` → `rgb(20, 20, 20)`, `--bookcloth` → `rgb(51, 81, 59)`).
  </read_first>
  <action>
Everything the pass needs that an executor can produce without a browser:

1. Preflight, recorded in the skeleton's head: run `npm --prefix app test` and `npm --prefix app run build`; run `git log -1 --format=%h -- .planning/sketches/007-full-battery/index.html` (expected `109733d`) and `… -- .planning/sketches/003-front-matter-rows/index.html` (expected `0fa1015`) and `git status --porcelain .planning/sketches/` (expected empty). If a sketch file has moved past the cited commit, STOP and report — every line citation in the plans and the contract is that commit's (D-20; RESEARCH "Valid until").

2. Write `03.3.1.1-CONFORMANCE.md` with: a YAML head — `status: pending`, `phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr`, `sketch_007: 109733d`, `sketch_003: 0fa1015`, `sketch_008: 0fa1015`, `app_commit: TBD-at-pass-time` (the orchestrator replaces it with the merged main HEAD), `tools: gsd-browser (Playwright, exact viewports incl. 393); claude-in-chrome for interactive spot checks at 500px and above`, `widths: [1280, 1180, 1080, 980, 880, 780, 680, 580, 480, 393]`, `states: [1-blank, 2-marked, 3-read]`. Then these sections, with the prose written out (not "see RESEARCH"), so the file is self-sufficient for the orchestrator and later for UAT scripting (D-23):
   - "How the pass was run": the seven-point procedure from RESEARCH § "Conformance pass" (serve `.planning/sketches` on a throwaway `python3 -m http.server`, the app on `npm --prefix app run dev`; hard-reload each sketch tab before a session's first reading — no cache headers; one browser tool for both tabs at a given width; per width per state set the sketch by script and drive the app by real clicks/typing; equalise the sketch frame to the app pen's width below 784px (Pitfall 9: `document.querySelector('.frame').style.width = penWidth + 2 * framePadding + 'px'`); never measure 003 with its "full" preset — `parseInt('100%')` is 100 and the frame is always classed narrow (003 lines 510-511) — set a real viewport and record the frame width and `.narrow` class beside every 003 reading; measure the axis heads and segmented caption lines BEFORE and AFTER a pick (Pitfall 12: 24/44 both times is conformance); record the pen's full height at 393 in both tabs (Open Question 6); at 780 the version row is single-column in the sketch's container rule and two-column in the app's viewport rule — arithmetic, not a gap (Open Question 5)).
   - "Pre-declared departures (never gaps)": typeface differences (D-18); 03.3.1 D-01's ceremony visibility; Show changes and the Skipped checkbox (CONTEXT Deferred — a later quick-batch; RESEARCH S10); the read view's "Bitter · declared" line (no 007 counterpart); Greasy film wrapping to a second row at the 640px pen in the 3-column arrangement (README tenth round — Mark kept the 20px gap); the 44px caption lines below 760 (expected, Pitfall 12); and the RESEARCH "Extras already visible from source" list, each named with its sketch line, to be logged as `extra`, never `gap`: the churn categories' side-by-side `.category-row`, the `.pen section + section` rhythm, the prose field's always-drawn hairline versus the app's blank-only one, placeholder size (the app's 12px small print versus the field's own size), the axis state text (roman ink 12px in the sketch), the anchors' face, the axes' gap arithmetic where it still differs, the button's padding (6.5px versus 6px/12px), the ink field at 1rem below 600, the ceremonies' `margin-top` and Next time's 32px.
   - "The 18 items": a table `| Item | UAT wording (verbatim) | Element(s) and property | Widths/states where it must pass |` for I-01 … I-18 in the order of `03.3.1-UAT.md`'s Untriaged Notes (skipping the "many other errors" bullet), so UAT can cite `I-nn`.
   - "Element checklist": the list from RESEARCH § "Conformance pass" point 5 and the Amendment's "Conformance pass addition", one bullet per element with its app selector, sketch selector and the properties to read (from the probe's keys) — every bullet becomes rows in the table below.
   - "Measurements": the header `| # | Width | State | Element (app selector / sketch selector) | Contract section | Property | Sketch value (line) | App value | Δ | Verdict | Item |` and a "Verdict key" — `pass` (within 1px / exact per D-18), `gap` (one of the 18 — blocks UAT until fixed and re-measured), `extra` (beyond the 18 — D-19 log), `sketch-round` (the sketch draws the same thing Mark dislikes — D-03; joins a later sketch round), `pre-declared` (listed above).
   - "Sequence checks": the state 2 → 2b → back list from RESEARCH 5b (record-status text, Restore tasting focused and placed, both Add tasting hidden, the foot reading Cancel | Save batch; after Restore the section back with values and focus on Remove tasting and "Tasting restored." on the head region; an empty removal focusing the end-of-record Add tasting), plus state 2c (a second click on the picked stop and option changes nothing; Clear stays visible) and 2d (keyboard Clear lands focus on cell 1 with the ring; mouse Clear does not; each head's height before and after the pick), an invalid field showing `outlineStyle: none`, and the forced-colours emulation (Chrome DevTools `forced-colors: active`, or recorded as unverified — RESEARCH A7).
   - "Extras log (D-19)" (empty table with the same header), "Fixes applied" (empty list: commit, item, what), "Result" (blank: pass count, gap count, extra count, the date, and the sentence UAT is scripted from).

3. Write `03.3.1.1-conformance-probe.js` (ES module syntax is unnecessary — plain browser JS the orchestrator pastes into `browser_evaluate`): the `probe(sel)` helper exactly as RESEARCH "Conformance probe" gives it (bounding rect, background, color, border width/colour, text-transform, letter-spacing, font-weight/size, line-height, text-decoration-line, outline width/style, min-height, gap, z-index, forced-color-adjust, the `::before` mark, and the `lines` count via `Range.getClientRects`); `shareRow(a, b)` (`Math.abs(top_a - top_b) <= 1`); `follows(a, b)` (`compareDocumentPosition & DOCUMENT_POSITION_FOLLOWING`); `overflow()` (`{ scrollWidth: document.documentElement.scrollWidth, innerWidth: window.innerWidth }`); the sketch state snippets from RESEARCH "Setting the sketch's state" (states 1, 2, 2b, 2c, 2d and the 003 state-3 note); the frame equalisation snippet; a `SELECTORS` object mapping each element name to `{ app, sketch }` selector pairs (e.g. `stop: { app: '.axis-mark__stop', sketch: '.axis .stop5' }`, `option: { app: '.segmented__option', sketch: '.seg .opt' }`, `defect: { app: '.chip-toggle', sketch: '.defect' }`, `axisHead: { app: '.axis-mark__head', sketch: '.axis .head' }`, `segHead: { app: '.segmented-field__head', sketch: '.category .head' }`, `caption: { app: '.pen-caption', sketch: '.lbl' }`, `regionName: { app: '.region-name', sketch: '.region-name' }`, `tastingHead: { app: '.tasting-head', sketch: '.tasting .head' }`, `ceremony: { app: '.save-ceremony', sketch: '.saves' }`, `recordStatus: { app: '.save-ceremony__status', sketch: '#record-status' }`, `formStatus: { app: '.form-status', sketch: '#form-status' }`, `dateField: { app: '.field-row__label--date .ink-field', sketch: '.f-date .num-field' }`, `figureField: { app: '.field-unit .ink-field', sketch: '.field-unit .num-field' }`, `unit: { app: '.field-unit__unit', sketch: '.field-unit .u' }`, `cue: { app: '.axes-cue', sketch: '.axes-cue' }`, `defectsHead: { app: '.defects-head', sketch: '.defects-head' }`, page-shell pairs for `.recipe-page`/`.page`, `.recipe-band__row-version`/`.row-version`, `.batch-row__head`/`.row-batch .head`, `.batch-row__correct`/`.correct`, `.batch-row__cell-value`/`.cell .v`, `.text-control`/`.text-control`); and `row(n, width, state, element, section, prop, sketchValue, appValue, verdict, item)` returning one markdown table row with `Δ` computed for numeric values. Run `node --check` on the file.
  </action>
  <verify>
    <automated>npm --prefix app test && npm --prefix app run build && node --check ".planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-conformance-probe.js" && grep -c "| # | Width | State | Element" ".planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md"</automated>
    <fails_when>non-zero exit from any command in the chain (a failed test or test file in the Vitest summary, a build error, a syntax error in the probe file), or the final grep printing 0</fails_when>
  </verify>
  <acceptance_criteria>
- `npm --prefix app test` and `npm --prefix app run build` pass on the merged main the pass will measure.
- `git log -1 --format=%h -- .planning/sketches/007-full-battery/index.html` prints `109733d` and the 003 file prints `0fa1015`; `git status --porcelain .planning/sketches/` prints nothing.
- `03.3.1.1-CONFORMANCE.md` has the YAML head with `status: pending`, the three sketch hashes, the ten widths and three states; `grep -c "^| I-" 03.3.1.1-CONFORMANCE.md` prints 18; the "Pre-declared departures" section names Show changes, the typeface exclusion and Greasy film; the "Verdict key" names pass, gap, extra, sketch-round, pre-declared.
- `node --check 03.3.1.1-conformance-probe.js` exits 0 and `grep -c "function probe" 03.3.1.1-conformance-probe.js` prints 1.
  </acceptance_criteria>
  <done>The build under test is green, the reference is pinned, and the orchestrator has a self-sufficient record to fill and a helper to fill it with.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: The conformance pass — the orchestrator measures both tabs at the ten widths in the three states and writes the record (D-16, D-17, D-18, D-19, D-20; the gate before UAT)</name>
  <precondition>The two sketch files are at their cited commits and unmodified: `git log -1 --format=%h -- .planning/sketches/007-full-battery/index.html` prints 109733d, the 003 file prints 0fa1015, and `git status --porcelain .planning/sketches/` prints nothing (D-20; D-22's sketch session is committed).</precondition>
  <action>The orchestrator (not gsd-executor, which has no browser tools) runs the side-by-side pass with gsd-browser and writes 03.3.1.1-CONFORMANCE.md; a gap on one of the 18 is fixed and re-measured before this checkpoint resolves.</action>
  <instructions>
Task 1 prepared: a green build on merged main, the CONFORMANCE.md skeleton (its head, the 18-item map, the element checklist, the verdict key, the pre-declared list, the empty tables), and the probe helper. What remains needs a browser:

1. Serve. `python3 -m http.server 8787 --directory .planning/sketches` (throwaway) and `npm --prefix app run dev`; note the app's port from its output. Open, per width, two tabs with gsd-browser at the exact viewport: `http://localhost:8787/007-full-battery/index.html` (or `/003-front-matter-rows/index.html` for state 3) and the app's recipe page with the seeded olive oil version. Hard-reload each sketch tab once per session (no cache headers). Record the merged main HEAD as `app_commit` in the file's head.

2. Per width (1280, 1180, 1080, 980, 880, 780, 680, 580, 480, 393), per state:
   - State 1 (blank): sketch `setVariant('A'); setCols(3); setTastingMode('hidden'); addTasting();` — app: press Record batch, then Add tasting; every field blank. Below 784px equalise the sketch frame to the app pen's width (Pitfall 9) and say so in the row.
   - State 2 (marked): sketch — click stop `data-i="2"` in every `.stops5`, the first `.opt` in every `.seg`, every `.defect`, and fill every `input.num-field` (`'2026-08-02'` for dates, `'12'` otherwise); app — the same by real clicks and typing (English input values). Then the spot checks: hover a stop, an option, a defect and Save batch (2px border, box unchanged, z-index 2 on the hovered joined box); Tab from the note textarea into Hardness (a 2px ring, offset 2px, on the picked or first cell's box); state 2c (a second click on the picked stop and option changes nothing, Clear stays); state 2d (each axis head's and segmented caption line's height before and after the pick — 24 desktop / 44 below 760, both times; keyboard Clear lands focus on cell 1 with the ring, mouse Clear does not); an invalid value ("cold" in Out of machine, then Save) shows `outlineStyle: none` and the sentence; state 2b (Remove tasting with data → record-status text, Restore tasting focused in the end-of-record row, both Add tasting hidden, the foot reading Cancel | Save batch; Restore → section back, focus on Remove tasting, "Tasting restored." on the head; Remove on an empty section → "Tasting removed." and focus on the end-of-record Add tasting). Forced colours: emulate `forced-colors: active` in DevTools if the tool allows and read the Highlight fill and the CanvasText ring; otherwise mark the forced-colours rows "unverified" (RESEARCH A7).
   - State 3 (read back): save the marked record in the app; compare the batch row against sketch 003 (a real viewport; never the "full" preset; record the frame width and `.narrow` class): the head line (Batch, the pen-blue date, the count disclosure with aria-expanded and aria-controls, Correct right-aligned on the same row), the cells (12px/500/uppercase/0.04em captions with 6px to the figure; figures 16px/400 pen blue; "not measured" in ink; unit words at 13px/400), the tasting heading as a region name, no foot acts; then the page shell: the column count and area order at each width, the padding (48px stacked at 780-1080, 20px at 580 and below), the version row's columns and the vmeta width, the ingredients caption clear of the table, and `document.scrollWidth === window.innerWidth` — the 393 reading is D-15's acceptance criterion; the table may scroll inside its region.

3. Probe every element in the checklist in both tabs with `probe()` from the helper; compare per D-18 (≤ 1px on lengths; `rgb()` strings equal on colours; `textTransform`, `fontWeight`, `textDecorationLine`, `outlineStyle` exact; `gap`/`minHeight` numeric; `lines` for every caption — a value above 1 is a wrapped caption). Write one row per element × width × state into "Measurements", with the verdict, and the `I-nn` item where one applies. Record the pen's full height at 393 in both tabs.

4. Verdicts. `pass` / `gap` / `extra` / `sketch-round` / `pre-declared` per the file's key. A `gap` on any of I-01 … I-18 blocks UAT: apply the fix as a `fix(03.3.1.1): …` commit on main (English message), re-measure the affected rows at every width they apply to, and list the commit under "Fixes applied". Everything beyond the 18 is `extra` (D-19) — logged in the "Extras log", never fixed here. A sketch-matches-app finding is `sketch-round` (D-03) — logged for a later sketch round and quick task.

5. Close the file: `status: complete`, the "Result" section (counts, date, the one sentence UAT is scripted from), and no remaining `gap` verdict. Commit `docs(03.3.1.1): conformance pass — …` with the file and the helper.
  </instructions>
  <verification>`grep -c "^status: complete" 03.3.1.1-CONFORMANCE.md` prints 1; a grep for the gap verdict cell — the literal pipe-space-gap-space-pipe — over the Measurements table prints 0; for every item I-01 … I-18, `grep -c "| I-nn |" 03.3.1.1-CONFORMANCE.md` prints at least 1 with the verdict pass on that row; the 393 row for `document.scrollWidth` reads equal to `innerWidth`; the app_commit in the head is a real short hash on main.</verification>
  <resume-signal>Type "conformance recorded" once CONFORMANCE.md is complete with no gap verdict on the 18; or describe which of the 18 still fails and why, if a fix needs planning beyond a fix commit.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost sketch server and dev server → the measuring browser | the pass drives local files and the local app only; no network surface beyond the machine |
| the record → UAT | CONFORMANCE.md's rows are what UAT is scripted from (D-23); a false pass row would ship a gap into UAT |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.3.1.1-16 | Repudiation | a measurement taken against a moved sketch | low | mitigate | the precondition pins both sketch commits and a clean tree; the file's head records the hashes and the app commit |
| T-03.3.1.1-17 | Tampering | a screenshot standing in for a value | low | mitigate | D-18: computed style and bounding rects only; the probe helper is the one reader for both tabs |
| T-03.3.1.1-18 | Information disclosure | the throwaway http.server exposing .planning/sketches | low | accept | bound to localhost for the session and stopped after the pass; the sketches are committed project files |
| T-03.3.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed; python3's http.server is the standard library |
</threat_model>

<verification>
- `03.3.1.1-CONFORMANCE.md` is complete: head filled, every element row present at every applicable width and state, no `gap` verdict on the 18, the extras logged, the 393 overflow row reading equal widths, the sequence checks recorded, the forced-colours rows read or marked unverified.
- `npm --prefix app test` and `npm --prefix app run build` still pass after any fix commit.
- `/gsd-verify-work` scripts `03.3.1.1-UAT.md` from the record (D-23) — outside this plan.
</verification>

<success_criteria>
The build is measured against the live sketches at the ten widths in the three states with computed values on every row; every one of the 18 items reads `pass`; the STATE.md 393 blocker has its measured closure; extras and sketch-round items are logged for Mark's triage; UAT can be scripted from the file.
</success_criteria>

<output>
Create `.planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-SUMMARY.md` when done — the counts from the Result section, the fixes applied, the extras and sketch-round items for Mark, and the forced-colours verification status.
</output>
