---
status: pending
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
sketch_007: 109733d
sketch_003: 59f7948
sketch_008: 0fa1015
app_commit: TBD-at-pass-time
tools: gsd-browser (Playwright, exact viewports incl. 393); claude-in-chrome for interactive spot checks at 500px and above
widths: [1280, 1180, 1080, 980, 880, 780, 680, 580, 480, 393]
states: [1-blank, 2-marked, 3-read]
---

# 03.3.1.1 Conformance Pass

This record is what `03.3.1.1-UAT.md` is scripted from (D-23). Task 1 (`gsd-executor`, no browser tools) prepared this skeleton — the preflight, the 19-item map, the element checklist, the verdict key and the pre-declared list — and the empty tables below. Task 2 (the orchestrator, `gsd-browser` + `claude-in-chrome`) fills the Measurements table, the Sequence checks, the Extras log and the Result, then flips `status` to `complete`.

## Preflight (Task 1, recorded before any browser work)

- `npm --prefix app test` — 934/934 tests passed, 33/33 test files.
- `npm --prefix app run build` — built in 82ms, no errors.
- `git log -1 --format=%h -- .planning/sketches/007-full-battery/index.html` → `109733d` (matches the pinned reference).
- `git log -1 --format=%h -- .planning/sketches/003-front-matter-rows/index.html` → `59f7948` (matches the pinned reference — a same-line edit of line 249 after `0fa1015`; every `0fa1015` line citation in the plans and this file still holds).
- `git log -1 --format=%h -- .planning/sketches/008-control-sheet/index.html` → `0fa1015` (the control-language source both other sketches carry).
- `git status --porcelain .planning/sketches/` → empty (clean tree).

The build under test is green and both sketch references are pinned at the commits every line citation in the phase's plans, RESEARCH.md and this file assumes. `app_commit` above is filled by the orchestrator with the merged main HEAD at the moment the browser pass begins.

## How the pass was run

1. **Serve.** A throwaway `python3 -m http.server 8787 --directory .planning/sketches` serves the sketch tab; `npm --prefix app run dev` serves the app (note the printed port — Vite's default is 5173, but the pass reads whatever the running instance actually prints). The reference is sketch 007 at `109733d` and sketch 003 at `59f7948` (D-20) — both hashes and the app's merged-main HEAD are recorded in this file's YAML head. The sketches are served from disk with no cache headers, so hard-reload each sketch tab (`Cmd+Shift+R`) once before the first measurement of a session; a stale tab silently measures yesterday's HTML.
2. **One browser tool per width.** `gsd-browser` (Playwright-backed) sets the viewport exactly, including 393 — the 03.3.1-06 precedent found the interactive Chrome window floors around ~500px, so `gsd-browser` is the primary tool for every one of the ten D-16 widths and `claude-in-chrome` is used only for optional interactive re-checks (hover, real Tab-key focus) at 500px and above. Both tabs at a given width are read with the *same* tool in the *same* session, so font rendering and DPR match between them.
3. **Per width, per state**, both tabs are opened at that width. The sketch's state is set by script (the snippets in `03.3.1.1-conformance-probe.js`'s `SKETCH_STATE_SNIPPETS`, sourced from RESEARCH "Setting the sketch's state"). The app is driven by real clicks and typing in English (Record batch → Add tasting for state 1; the same marks and field values for state 2; save and read back for state 3, compared against sketch 003).
4. **Frame equalisation below 784px (Pitfall 9).** Below the width where the sketch's pen stops being capped at 640px, the sketch's `.frame` padding makes its pen narrower than the app's `.batch-margin--pen` at the same viewport unless corrected. The pass calls `equaliseSketchFrame(appPenWidthPx)` from the probe helper — it reads the app pen's measured width, reads the sketch frame's own computed padding, and sets `.frame`'s width so the two pens measure the same — and records that the equalisation ran, in the row for that width.
5. **Never measure sketch 003 with its "full" preset.** `setWidth(0)` writes `width: 100%` and `relayout()` reads it back with `parseInt`, so `parseInt('100%') === 100` and the frame is classed `.narrow` at every real width regardless of the preset chosen (003 lines 510-511). Every state-3 reading sets a real viewport (or one of the numeric presets — 834, 1024, 1280, 1440) and records `.frame`'s measured width and whether it carries the `.narrow` class beside the reading, so a mis-set preset is visible in the record rather than silently invalidating the row.
6. **Measure caption/axis-head lines before AND after a pick (Pitfall 12).** Below 760px every axis head and every segmented caption line reserves 44px whether or not Clear is showing (24px above 760px). Read naively, the app's head growing on first mark could look like a spacing bug; the correct reading is 24-before/24-after (desktop) or 44-before/44-after (narrow) in both tabs. A head that measures 24 before and 44 after IS the bug.
7. **Two open questions the procedure resolves by measurement, not assumption.** At 393, record the pen's full height in both tabs (Open Question 6) — a taller app pen than the sketch is not itself a gap, since the 44px caption-line reserve alone adds roughly 9 × 20px; if Mark objects after seeing the measured number it is a sketch round, not an app fix. At 780px, the sketch's `.row-version` collapse is keyed to a *container* width (760px inside a padded `.front`) while D-15 (and the app) key the same collapse to a *viewport* width (759.98px) — the two disagree by roughly the page's padding, so a 780px divergence on this one element is recorded as container-vs-viewport arithmetic (Open Question 5), not a gap, and Mark decides which number is the intent after seeing it.

## Pre-declared departures (never gaps)

These carry the verdict `pre-declared` wherever the pass encounters them — they are known, named divergences the phase's decisions already accepted, not findings:

- **Typeface differences** (D-18) — the app keeps DESIGN.md's own text/grotesk faces; the sketch's system-ui stand-in is excluded from every comparison.
- **03.3.1 D-01's ceremony visibility** — both save ceremonies render regardless of pen state, per 03.3.1's own D-01, which this phase does not reopen.
- **Show changes and the Skipped checkbox** (sketch 003 S10; CONTEXT Deferred) — the square-and-word treatment for these two controls is deferred to a later quick-batch once DESIGN.md carries the app-wide picked-fill rule (D-21); the pass will see the retired bold+underline/outline treatment on `.headnote__show-changes` and the Skipped checkbox and must not treat it as a gap.
- **The read view's "Bitter · declared" line** — has no 007 counterpart (007 draws the pen, not the read view); any divergence here is out of this pass's scope.
- **Greasy film wrapping to a second row** at the 640px pen in the 3-column arrangement (README tenth round) — Mark kept the 20px column gap deliberately; a wrap here is expected, not a gap.
- **The 44px caption lines below 760px** (Pitfall 12) — expected growth, not a spacing regression; see "How the pass was run" point 6.
- **RESEARCH "Extras already visible from source"** — named divergences already known from reading the source, each logged with the verdict `extra` (not `pre-declared`, not `gap`) when the pass encounters them, with the sketch line cited:
  - The churn categories' side-by-side `.category-row` (007 lines 119, 218-235) vs. the app's stacked fieldsets.
  - The `.pen section + section` rhythm (007 line 53) vs. the app's unstyled section boundary.
  - The prose field's always-drawn hairline (007 line 33) vs. the app's blank-only underline.
  - Placeholder size — the app's 12px small print vs. the sketch field's own size (007 lines 168, 003:40).
  - The axis state text — roman ink 12px in the sketch (007 lines 125, 159-162) vs. the app's italic 13px pen-blue.
  - The anchors' face — sketch text-face italic vs. the app's grotesk italic (both 13px).
  - The axes' gap arithmetic where it still differs (007 lines 65-68, 74 vs. `app.css:1814, 1851-1855, 1556`).
  - The button's padding — sketch 6px/12px (007 line 27) vs. the app's 6.5px all round (`app.css:41`).
  - The ink field at 1rem below 600px (007 line 140, 003:178) vs. the app's `--type-control` 13px.
  - The ceremonies' `margin-top` (007 line 54) and Next time's 32px spacing.

## The 19 items

The roadmap's and CONTEXT's "18" undercounts by one — research's own D-03 triage table already carried 19 rows (the ROADMAP's list has 19 bullets including the "many other errors" note, which is not itself a numbered item). This record carries all 19, `I-01` through `I-19`, in the order of RESEARCH § "D-03 Triage" rows 1-19, which is the order of `03.3.1-UAT.md`'s Triaged Notes with the "many other errors" bullet skipped.

| Item | UAT wording (verbatim) | Element(s) and property | Widths/states where it must pass |
|------|-------------------------|--------------------------|-----------------------------------|
| I-01 | Version section is too narrow; should flow between recipe description and Batch section | `rowVersion` (`.recipe-band__row-version` / `.row-version`) — `grid-template-columns`; `page` (`.recipe-page` / `.page`) — column stack | All 10 widths; page shell, not pen-state-dependent (visible in every state) |
| I-02 | In Batch section, "Time to draw temp., min" caption wraps | `caption` (`.pen-caption` / `.lbl`) — `lines` (`getClientRects().length`), text content (unit word beside the field, not inside the caption) | 580, 480, 393 primarily, all 10 recorded; states 1, 2 |
| I-03 | Field text boxes for Time to draw temp, Out of machine, Churn duration are too big for the values | `figureField` (`.field-unit .ink-field` / `.field-unit .num-field`) — `w` (bounding-rect width, expect 56px) | All 10 widths; states 1, 2 |
| I-04 | Exit consistency and Airiness segment controls have a hairline border not present in the sketch/contract; selected value is styled bold + underline instead of solid fill | `option` (`.segmented__option` / `.seg .opt`) — `bg`, `bc`, `td` (text-decoration), `os` (outline-style, expect `none`) | All 10 widths; state 1 (rest), state 2 (picked + hover) |
| I-05 | The Add tasting opener is missing from the top Save ceremony section | `ceremony` (`.save-ceremony` / `.saves`) — DOM order (`follows()`), presence of the Add tasting child | All 10 widths; state 1 (ceremony visible from the start) |
| I-06 | On Tasting section caption, the hint is styled the same as "Tasting" (superseded — verify no residual artifact) | `tastingHead` (`.tasting-head` / `.tasting .head`) — `fw`, `td` (no bold/underline residue from an unstyled `<h3>`) | All 10 widths; state 1 |
| I-07 | Tempering min edit control is full width instead of sharing a row with Tasted date | `dateField` / `figureField` — `shareRow()` on the Tempering row | All 10 widths; states 1, 2 |
| I-08 | Tasting temperature edit control is full width instead of sharing a row with Tasted date | `dateField` / `figureField` — `shareRow()` on the Tasting temperature row | All 10 widths; states 1, 2 |
| I-09 | Tabbing from "How did it turn out?" does not show focus on the first select under Hardness | `stop` (`.axis-mark__stop` / `.axis .stop5`) — `ow`, `os` after a real Tab from the note textarea | All 10 widths (interactive spot check ≥500px via `claude-in-chrome`; 393 via `gsd-browser` keyboard events); state 2 |
| I-10 | Selected axis value (e.g. 1) is bold underline, unlike sketch/contract | `stop` — `bg`, `color`, `td`, `fw` | All 10 widths; state 1 (rest), state 2 (picked + hover) |
| I-11 | Flaws under "Any problems?" are styled unlike sketch/contract | `defect` (`.chip-toggle` / `.defect`) — `bw`, cs padding, `bg`, `mark` (the `::before` square) | All 10 widths; states 1, 2 |
| I-12 | Melt test edit control is full width and should share a row with Melt Style | `dateField` / `figureField` — `shareRow()` on the Melt test row | All 10 widths; states 1, 2 |
| I-13 | Melt style also has the outline — segmented control style is not like sketch/contract | `option` (Melt style's segmented control) — same properties as I-04 | All 10 widths; state 1 (rest), state 2 (picked + hover) |
| I-14 | Caption for "Next time?" is missing | `caption` (Next time) — presence, `tt` (text-transform) | All 10 widths; states 1, 2 |
| I-15 | In Ingredients section, the Ingredient section caption and Ingredients table heading overlap | `regionName` (Ingredients) — margin/position; page shell table region — `overflow()`, which element overlaps if any | 580, 480, 393 primarily, all 10 recorded; state 3 (a batch in view) |
| I-16 | Column 2 (notes) should flow after the ingredients table because the ingredient name column is too narrow and wraps too much | `page` — `grid-template-areas` per width (the 1099.98px stack) | All 10 widths; state 3 (page shell layout, independent of pen state) |
| I-17 | Batch row, Record Batch mode: sketch says "CHURN DATE", actual reads "Churned" | `caption` (churn date) — text content, `tt` | All 10 widths; state 1 |
| I-18 | Batch row, Record Batch mode: sketch has the Remove tasting opener right-aligned on the row with the TASTING section caption; actual has it below the Tasting section caption, left-aligned | `tastingHead` — `shareRow()` between the region name and Remove tasting, `left`/`right` alignment | All 10 widths; state 1 (tasting open, no data) |
| I-19 | Batch row, Record Batch mode: sketch has all captions upper case; actual is sentence case | `caption`, `regionName`, `cue` — `tt` (expect `uppercase`), `ls` (expect `0.04em` at 12px / `0.04em` at 14px) | All 10 widths; states 1, 2 (pen) and 3 (read view — already `uppercase` in source per RESEARCH; the pass confirms by computed style) |

## Element checklist

One bullet per contract element; each becomes rows in the Measurements table below at every applicable width and state. App selector / sketch selector pairs and the properties read are the probe helper's `SELECTORS` map and `probe()` keys (`03.3.1.1-conformance-probe.js`).

- **BATCH region name** — `regionName` (`.region-name` / `.region-name`): `fs` (14px), `fw` (600), `ls` (0.04em), `color` (bookcloth `rgb(51, 81, 59)`), 6px beneath (gap to the next element's top).
- **CHURN DATE label + input** — `caption` + `dateField` (`.field-row__label--date .ink-field` / `.f-date .num-field`): `w` (128px), caption `tt`/`ls`.
- **The three churn numeric labels, boxes and unit words** — `caption` + `figureField` + `unit` (`.field-unit__unit` / `.field-unit .u`): `w` (56px boxes), unit word placement beside the box.
- **Exit consistency and Airiness caption lines** — `segHead` (`.segmented-field__head` / `.category .head`): `h` (24px / 44px, before and after a pick), right-aligned Clear.
- **Each segmented option** — `option`: rest/picked/hover states (`bg`, `bc`, `ow`, cs padding, `z`), 6px row gap where an option wraps (≤480).
- **At the machine, Ingredient notes placeholders** — prose field elements: placeholder `fs`, `font-style` (italic).
- **TASTING head** — `tastingHead`: bare name, status, Remove right-aligned.
- **Tasted / Tempering / Tasting temperature row** — `dateField`/`figureField` `shareRow()`.
- **How did it turn out? caption, field and placeholder** — prose field: caption presence, placeholder text/size.
- **The two cue rows (Every recipe / This recipe only)** — `cue` (`.axes-cue` / `.axes-cue`): `top` alignment (desktop: cols 1-2 and 3 same row; stacked: heading each group).
- **The six axis heads** — `axisHead` (`.axis-mark__head` / `.axis .head`): name `fw` (500), state text, Clear, `h` before/after pick.
- **30 joined stops** — `stop`: `w` (38/44), consecutive `left` deltas (37/43), track (186/216), no gap; rest/picked (`z:1`)/hovered (`z:2`, `bw:2px`)/focused (`ow:2px`, offset 2px).
- **Six anchor rows and the hairline** — anchor text elements; the rule spanning the defects too.
- **ANY PROBLEMS? caption + helper** — `caption` + helper element.
- **The two defect groups with their cues** — `cue` + `defectsHead` (`.defects-head` / `.defects-head`): same-y alignment; wrap at the 640px pen (pre-declared).
- **Five defects as square-and-word** — `defect`: `bw` (0 on the button), `mark` (13×13 `::before`, `bw:1px` rest / `2px` hover, `bg` pen-blue when pressed), row gaps (6px/20px).
- **MELT TEST label/box/unit + MELT STYLE caption line and options row** — `figureField`/`unit` + `segHead` + `option`.
- **End-of-record ceremony** — `ceremony` + `recordStatus` (`.save-ceremony__status` / `#record-status`): DOM order (Add tasting → Cancel → Save batch), right edge alignment, `record-status` empty/filled, Restore tasting present with Add tasting hidden in state 2b.
- **NEXT TIME caption/field/placeholder** — `caption` + prose field.
- **form-status** — `formStatus` (`.form-status` / `#form-status`): `max-width` (65ch).
- **Foot ceremony** — `ceremony`: same order/alignment, no restore slot.
- **Sequence-only checks** (no static selector; see "Sequence checks" below): a click on an already-picked stop/option changes nothing (state 2c); a keyboard Clear lands focus on cell 1, a mouse Clear does not (state 2d); an invalid field shows no outline.
- **Page shell (sketch 003)** — `page`: column count, area order, padding (48px stacked at 768, 20px at 393); `rowVersion`: columns, vmeta width; ingredients caption vs. table top; `batchHead` + `correct` (`.batch-row__correct` / `.correct`): Correct on the head line, right-aligned, no `.batch-row__acts`; the two count disclosures' `aria-expanded`/`aria-controls`; `overflow()` (`document.scrollWidth` vs. `window.innerWidth`).
- **Skip** `.recipe-placeholder` (007 line 308) — sketch chrome with no app counterpart.
- **Both ceremonies' `justify-content`** is `flex-end` at every width (007 line 55), not left below 761.

## Measurements

| # | Width | State | Element (app selector / sketch selector) | Contract section | Property | Sketch value (line) | App value | Δ | Verdict | Item |
|---|-------|-------|-------------------------------------------|-------------------|----------|----------------------|-----------|---|---------|------|

**Verdict key:**

- `pass` — within 1px on lengths / exact on discrete values (D-18).
- `gap` — a divergence on one of the 19 items (`I-01` … `I-19`) — **blocks UAT** until fixed and re-measured.
- `extra` — a divergence beyond the 19 — logged in "Extras log (D-19)" below, never auto-closed, left for Mark's post-UAT triage.
- `sketch-round` — the sketch draws the same thing Mark dislikes (D-03) — never fixed in `app/` during this pass; logged for a later sketch round and quick task.
- `pre-declared` — one of the named departures in "Pre-declared departures" above.

## Sequence checks

The state 2 → 2b → back sequence and three additional state variants (RESEARCH § "Conformance pass" 5b), recorded once per width where feasible (interactive checks are spot-checked at a representative subset of widths, at minimum 1280 and 393):

- **2 → 2b → back (Remove tasting, data present):** after Remove tasting with data — `record-status` reads "Tasting removed. You can restore it." (both tabs); Restore tasting is focused and sits in the end-of-record row; both Add tasting buttons are hidden; the foot reads Cancel | Save batch. After Restore — the section is back with its values, focus on Remove tasting, `tasting-status` reads "Tasting restored."
- **Empty removal:** Remove tasting on an empty section → "Tasting removed." in `record-status`, focus on the end-of-record Add tasting.
- **2c (radio, not toggle):** a second click on the already-picked stop and option changes nothing — `aria-checked`/`aria-pressed` unchanged, Clear stays visible.
- **2d (Clear, keyboard vs. mouse):** each axis/segmented head's height measured before and after the pick (expect equal — 24 desktop / 44 below 760, both times); a keyboard-activated Clear (`event.detail === 0`) moves focus to cell 1 with the ring; a mouse-activated Clear does not.
- **Invalid field:** an invalid value (e.g. "cold" in Out of machine, then Save) shows `outlineStyle: none` — never a ring — with the invalid sentence beneath the field.
- **Forced colours:** emulate `forced-colors: active` in Chrome DevTools if the browser tool allows it, and read the Highlight fill and the CanvasText ring on a picked control; otherwise mark the forced-colours rows "unverified" (RESEARCH A7 — the sketch's own README records this was not re-checked in Chrome's emulation either).

| Check | Width(s) | Result | Notes |
|-------|----------|--------|-------|

## Extras log (D-19)

Divergences found by the pass that are NOT among the 19 items and were NOT already named in "Pre-declared departures" above. Logged here, never auto-closed — Mark triages these after UAT into a quick-batch or a later phase.

| # | Width | State | Element | Sketch value (line) | App value | Notes |
|---|-------|-------|---------|----------------------|-----------|-------|

## Fixes applied

Commits that closed a `gap` verdict on one of the 19 items during this pass, with the affected rows re-measured at every width they apply to.

- (none yet)

## Result

- Pass count: —
- Gap count: —
- Extra count: —
- Sketch-round count: —
- Date: —
- Summary sentence UAT is scripted from: —
