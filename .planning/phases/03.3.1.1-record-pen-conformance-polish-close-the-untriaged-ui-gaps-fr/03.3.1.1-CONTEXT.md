# Phase 03.3.1.1: Record pen conformance polish - Context

**Gathered:** 2026-09-14
**Status:** Ready for planning — after the two prerequisites in "Sequencing" (D-21 before plan-phase; D-22 before execute-phase)

<domain>
## Phase Boundary

Phase 03.3.1.1 closes the 18 conformance gaps Mark logged during 03.3.1's UAT between the shipped record pen / batch row and sketch 007, plus the three page-shell items among them against sketch 003, and ends with a side-by-side sketch-versus-app conformance pass that gates UAT. No new capability. 03.3.1's D-01–D-24 and the closed G-03.3.1-4 caption fix are not reopened.

**Why this phase exists (the method failure it corrects):** in 03.3.1 no planner, checker, executor or verifier read the sketch HTML. They built to two prose derivations (the structural contract and the findings), which carry behaviour but not visual treatment; where DESIGN.md's binder prose disagreed with the sketch, the planner picked DESIGN.md silently; and verification checked the build against the derivation, so it self-certified. This context fixes the method as much as the pixels: the sketch HTML is the authority, the contract gains its visual layer, conflicts come to Mark, and conformance is measured side by side in a browser.

Out of scope: the page's other pressed controls (Show changes, the Skipped checkbox) — a quick-batch after DESIGN.md carries the new rule; gaps the pass finds beyond the 18 — logged, triaged by Mark after UAT; the brief and design.json — refreshed by `/impeccable document` after the build; Phase 4 print.

</domain>

<decisions>
## Implementation Decisions

### The sketch is the authority (method)

- **D-01:** **The live sketch HTML is the design authority for this phase**: `.planning/sketches/007-full-battery/index.html` for the record pen and batch row, `.planning/sketches/003-front-matter-rows/index.html` for the page shell. The structural contract, the findings files, the brief and DESIGN.md are citation aids; where any of them disagrees with the HTML, the HTML governs unless a decision below says otherwise. The researcher, planner, plan checker, every executor and the verifier read the HTML including its CSS; each plan task cites sketch line numbers as well as contract section names. Acceptance criteria assert DOM order, placement and computed style, never label presence alone.
- **D-02:** **Research writes the contract's missing visual layer.** The RESEARCH step adds a section "Visual treatment (from the sketch CSS)" to `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md`: per control and caption, the sketch's declarations with `index.html` line numbers (fill, borders, hover, focus, case, tracking, widths, row grouping, gaps), plus the departures decided here (D-04, D-08, D-12). Plans cite it by section name. The file is GSD's (written by `/gsd-sketch --wrap-up`), so no Impeccable round is needed for it.
- **D-03:** **Sketch-matches-app items go back to the sketch first.** Research triages each of the 18 against the sketch at the relevant widths into "app diverges from the sketch → fix here" and "sketch draws the same thing Mark dislikes → the app follows the sketch as drawn; the item joins the pre-execution sketch session (D-22) or, if found later by the pass, a later sketch round and quick task". Known candidate: the sketch reserves two-line caption height (`.field-row label > span.lbl { min-height: 2.4em }`, line 39), so "Time to draw temp., min" wraps in the sketch at 579px too.

### Picked state (revises DESIGN.md's binder rule)

- **D-04:** ⚠ *Amended — read the "Superseded by sketch 008" block below before using this decision.* **One picked treatment, everywhere in the battery: solid pen-blue fill with ground text, no bold, no underline.** Stops (`.axis .stop5.on`, sketch line 80), segment options (`.seg .opt.on`, line 49), the four defect chips and the declared toggle all fill pen blue when picked or pressed; the picked stop's or option's border turns pen blue. The sketch's `[aria-checked="true"], [aria-pressed="true"]` bold-plus-underline rule (line 127) is **not** carried — chips filling is a departure from the sketch as drawn (Mark's call), sent to the sketch session (D-22). Recorded as an **app-wide rule** in DESIGN.md (D-21): every picked or pressed control fills, inline text toggles and the checkbox included; this phase applies it to the battery only, the rest follows in a quick-batch (see Deferred). Blank stays visibly blank: nothing is pre-picked; click-again clears (unchanged from 03.3.1). The brief's anti-goal "no colour on a mark" means verdict colour; pen blue is the record's ink and is not a verdict.
- **D-05:** ⚠ *Amended — read the "Superseded by sketch 008" block below before using this decision.* **The app's added ink outline on picked controls goes** (`app.css` lines 1647 and 1724). The only outline on a picked control is a `forced-colors: active` fallback, so a picked state survives Windows high-contrast where backgrounds are stripped (the fallback the cross-cutting findings already name).
- **D-06:** ⚠ *Amended — read the "Superseded by sketch 008" block below before using this decision.* **Hover follows the sketch's own rules per element, and there are no transitions.** Stops: border 1.5px on hover with no layout shift (line 79); buttons: 1.5px border with compensating padding (line 28); text controls: thicker underline (line 30). Segment options and chips have no hover rule in the sketch and get none. A filled control shows no hover change. The sketch has zero transitions; the app adds none.
- **D-07:** ⚠ *Amended — read the "Superseded by sketch 008" block below before using this decision.* **Focus reads as the sketch draws it.** Tabbing into an axis lands on its first stop, or the picked stop, and that stop's box shows the universal focus-visible ring (line 24: `--focus-outline-width` solid ink at `--focus-outline-offset`). The app currently loses the ring because focus sits on the invisible radio inside the label (`app.css` 1627); the plan fixes that so the ring draws on the visible box. Native radiogroup keyboard semantics plus 03.3.1's Home/End handler stay.
- **D-08:** **The "· declared" helper leaves the Bitter toggle.** Mark: "I actually do not like the declared helper suffix inside the toggle." It becomes a sibling helper element beside the button (helper face — grotesk, control size, ink, lowercase, per cross-cutting § Type roles); the button's text is "Bitter" and it keeps `aria-label="Declared for this recipe: Bitter"`. Default placement: after the toggle with the small gap; Mark tweaks the exact location later in a quick task. A departure from sketch line 277 (D-22).
- **D-09:** **Segmented controls stay as the sketch draws them**: each option a hairline ink box, adjacent options sharing one border (`margin-right: -1px`, line 48), the picked option solid pen blue edge to edge with a pen-blue border (line 49), no outline. The UAT note "hairline border not present in the sketch" was the app's extra outline, not the hairlines.
- **Confirmed as-sketch (no change):** the per-axis inline state "(3)" in pen blue / "(Not recorded)" muted italic and the per-axis Clear control (lines 376–380); a filled stop prints as drawn in Phase 4.

### Superseded by sketch 008 (Mark, 2026-09-14 — added after the control sheet settled)

The decisions above were taken on 2026-09-14 morning against sketch 007's ninth round. That evening sketch 008 (the control sheet, `3ed0c0f`; rubric `3a5beed`) settled the control language app-wide, and the settlement was carried into sketch 007 (`2a212be`), sketch 003 (`d89b758`) and DESIGN.md (`a0126a2`). D-01 stands: the HTML governs. Where a decision above disagrees with the HTML at those commits, the HTML wins and the decision reads as amended here; RESEARCH.md § "Re-citation against commits 2a212be / d89b758 / 3a5beed" carries the values, lines and test pins.

- **Superseded — D-04, "click-again clears"** → a joined group (stops, segment options) is a radio: a second click on the pick changes nothing; Clear in the caption line is the only way back, on the six axes and on Exit consistency, Airiness and Melt style alike (007 lines 220, 228, 289, 350, 362, 375–380). Clear moves focus to the first cell only when activated by keyboard (`event.detail === 0`).
- **Superseded — D-04, "the four defect chips and the declared toggle fill pen blue"** → the defects are a square and a word (`<button class="defect" aria-pressed>` with a 13px `::before` square, no box; 007 lines 87–89, 269–279): only the square fills pen blue; the word keeps its ink. The same rule as the checkbox and Show changes (one focus ring around square and word).
- **Superseded — D-05, "the only outline on a picked control is a forced-colors fallback"** → in forced colours a picked box keeps a fill in the system Highlight colour through `forced-color-adjust: none`, and a picked box's focus ring takes `CanvasText` (007 line 144). There is no outline stand-in.
- **Superseded — D-06, hover** → every box thickens to a whole 2px on hover in place, segment options and the defect's square included, with padding giving the pixel back so nothing moves; a hovered joined box stacks above its neighbours (z-index 2, the picked one 1). 1.5px is retired: it renders as 1px at DPR 1 (007 lines 28, 50, 82, 88).
- **Superseded — D-07, the ring** → `--focus-outline-width: 2px` (007 line 153); the ring means focus only — the `[aria-invalid]` ring is gone (line 135), invalid is the sentence beneath the field.
- **Superseded — D-13, "a numeric field 48px"** → 56px (007 line 43): 48px clipped "−12.5" and "120.5".
- **New, from the twelve settled values (007 lines 31, 33, 131, 155–157, 172, 180):** 0.04em on every uppercase caption; axis names at weight 500 (600 is for region names only); 6px beneath a region name (the Tasting head too, line 242); the prose field's baseline a hairline with no thickening on focus; the status line at the prose measure (65ch); text controls 24px tall at desktop and 44px below 760px; the caption line of every axis and segmented control reserves 24px / 44px marked or not (no negative block margins).
- **New, the page shell (D-15, sketch 003 @ `d89b758`):** region names and running heads at the section role (0.875rem/600, 6px beneath); every uppercase caption 0.75rem/500 with 6px to what it captions; text controls at the control size; the page's 48px margin holds until 600px (the stack below 1100px changes only the columns); italic ink placeholders; Correct on the Batch head line, right-aligned, and no Add tasting in the read view (the pen is the one door, 03.3.1 D-01/D-03); `aria-expanded` on the two count disclosures. 003's "full" preset always takes the narrow layout (its width check reads "100%" as 100): measure the ladder at real viewports.
- **The box-or-word rubric (008 header, Settled values; DESIGN.md § The binder):** a hairline box commits, discards, or starts a new record and stands apart; an underlined word acts on what is already on the page and stands beside it; a word that opens a panel beneath it is a disclosure whose open state is the panel. Any new control in the plans takes its shape from this.
- **The recorded figure (008 § Captions and type roles; 003 line 88):** the batch row's read-view figure is the note size (1rem) at the prose weight (400), pen blue, tabular, the unit word at the deviation size, the plan sub-line beneath — not the balance figure's 1.25rem/700, which stays in ink for the page's assessment (Mark, 2026-09-14, candidate D). The app's `.batch-row__cell-value` (`app.css` 1388) takes it in the D-15 plan.

### Captions

- **D-10:** **Every caption in the record pen is uppercase small print with 0.04em tracking, as the sketch's `.lbl` (line 36), axis names (line 73) and region names (line 31).** Field captions (CHURN DATE, TIME TO DRAW TEMP., MIN … NEXT TIME), the six axis names, ANY PROBLEMS?, the melt captions, and the section names. Done with `text-transform: uppercase` so the source text stays sentence case for screen readers. Helpers ("select all that apply") stay lowercase sentence case as sibling elements, never inside the uppercase element (cross-cutting § Type roles).
- **D-11:** **Uppercase reaches the pen and the batch row's read view** (`.batch-row__cell-label`, matching sketch 003's `.cell .k`, page-shell findings). The rest of the recipe page is untouched; remaining sentence-case captions elsewhere are noted for Impeccable, not changed here.
- **D-12:** **Caption words are the sketch's, verbatim, except the "(optional)" suffixes are dropped.** "Churn date" replaces "Churned"; the "Next time" caption is restored (sketch line 298); "Tasted", "Tempering", "Tasting temperature", "Melt test", "Melt style", with the unit word after the field ("min", "°C", "g lost at 20 min"). Dropping "(optional)" from Melt test and Melt style is a departure from lines 284 and 286 (D-22). Neither "Churned" nor "Churn date" is a settled label in `product-requirements/05-domain-and-language.md`, so no product decision is reopened.

### Layout, widths and scope

- **D-13:** ⚠ *Amended — read the "Superseded by sketch 008" block below before using this decision.* **Fields are sized by the sketch's pixels, as tokens.** A date field 128px (`.f-date`, line 41); a numeric field 48px with its unit word beside it (`.field-unit .num-field`, line 43); labels at content width inside the sketch's wrapping flex `.field-row` with the small gap and bottom alignment (lines 37–39). The pen's `.ink-field { width: 100% }` (`app.css` 1303) no longer applies to battery fields. Tasted, Tempering and Tasting temperature share one field-row (lines 251–255); Melt test and Melt style share another (lines 284–286). DESIGN.md's "a date at 12ch, a short figure at 6ch" sentence is amended with the binder paragraph (D-21).
- **D-14:** **Add tasting sits beside both ceremonies while the tasting section is absent** — 03.3.1 D-01 says the two ceremonies are identical regardless of position, which resolves the UAT note without reopening D-01. **Remove tasting is right-aligned on the TASTING caption row**, as the sketch draws it; the app's below-caption, left-aligned placement is a gap.
- **D-15:** **The page shell is in scope as one plan against sketch 003.** The version section's width, column two flowing after the ingredients table, and the Ingredients caption overlapping the table heading are closed by applying sketch 003's ladder — the page stacks to one column below 1100px, the version row goes single-column below 760px (`page-shell-front-matter.md` CSS patterns; `003-front-matter-rows/index.html`), ingredient table column widths per 003 — cited to 003's HTML like the battery is to 007's. **This plan also closes the true-393px horizontal overflow** (`.recipe-band` / `.vmeta`) the roadmap had marked out of scope: zero horizontal overflow at 393 becomes its acceptance criterion and the STATE.md blocker retires when it passes.
- **D-16:** **Widths that define done: every 100px from 1280 down to 393** (1280, 1180, 1080, 980, 880, 780, 680, 580, 480, 393) for the conformance pass and the scripted UAT.
- **Confirmed scope (no change):** the Correct-reopened pen gets the same treatment; 03.3.1's D-01–D-24 stay closed; the G-03.3.1-4 caption stays bare "Tasting" with no residual styling.

### The conformance pass

- **D-17:** **The orchestrator runs the pass as the phase's last, gating plan before UAT.** `gsd-executor` has no browser tools. The orchestrator serves the live sketch (throwaway `python3 -m http.server` on `.planning/sketches/`) and the app dev server in two tabs, walks every contract element at each width of D-16 in three states — (1) blank pen: variant A, default columns, tasting opened via Add tasting, every field blank; (2) fully marked pen: every stop, segment and chip picked, every field filled, with hover and focus spot checks; (3) the record saved and read back in the batch row, checked against sketch 003's cells and D-10–D-12 (no sketch 007 counterpart) — and writes `03.3.1.1-CONFORMANCE.md` with one row per element × width: element, contract section, sketch value with line, app value, verdict (pass / gap / sketch-round). The `live-dom-uat` capability stays off (execute:wave:post carries only drift gates today).
- **D-18:** **"Conforms" means computed-style equality on named properties within 1px**: DOM order and row placement, background, border, text-transform, font-weight and size, width and height, gaps — read with `getComputedStyle` and `getBoundingClientRect` in both tabs. Typeface differences are excluded (the app keeps DESIGN.md's faces). Screenshots are not the verdict.
- **D-19:** **Gaps the pass finds beyond the 18 are logged, not auto-closed.** UAT proceeds on the 18; Mark triages the extras after UAT into a quick-batch or a later phase. A pass row marked "gap" for one of the 18 blocks UAT until fixed and re-measured.
- **D-20:** **The reference is the live sketch in `.planning/sketches/`**, not the skill's frozen `sources/` copy; a sketch revision lands there first and the pass picks it up. The skill's copy is refreshed at the next `/gsd-sketch --wrap-up`.

### Sequencing outside the plans (prerequisites Mark set)

- **D-21:** **DESIGN.md is amended before `/gsd-plan-phase` runs.** Claude edits the binder paragraphs directly — § binder (line 132: "no fill … a picked or open thing turns weight and outline"), § components (lines 229 and 236), and the ink-field sizing sentence (line 244) — to say: every picked or pressed control fills pen blue with ground text, app-wide, with no weight or underline affordance and a forced-colors outline fallback; measured fields are sized by the sketch's width tokens. Mark approves the diff; committed as docs. This is a bridge: `/impeccable document` re-derives DESIGN.md, the brief and `design.json` from shipped code after the build. The brief (`route-recipe-batch.md` line 110 already says pen-blue picked state; line 108's "no fill" is about text fields, which stay unfilled) and `design.json` are not edited now.
- **D-22:** **Sketch 007 is revised before `/gsd-execute-phase` runs, by Claude in a sketch session Mark reviews** — direct edits to `.planning/sketches/007-full-battery/index.html` (not `app/`, so no GSD command), served and measured in a real tab, shown to Mark before commit (the 2026-09-12 loop). Contents: chips and the declared toggle fill when pressed and the line-127 bold+underline rule goes (D-04); the helper moves outside the toggle (D-08); "(optional)" suffixes dropped (D-12); plus any sketch-matches-app items research triages under D-03. Planning proceeds in parallel; only execution waits, so the pass compares against a matching reference with no accepted-departure rows.
- **D-23:** **UAT is scripted from CONFORMANCE.md**: one test per gap of the 18 plus one per state-and-width pair Mark chooses to walk, each naming the element, the expected sketch value and how to see it; extras go to Untriaged Notes for post-UAT triage (D-19). **True 393px is reached in Chrome DevTools device mode on the desktop** during UAT (real engine, emulated viewport); no phone step.

### Claude's Discretion

- How the focus ring is restored on the stop box (D-07): styling the label via `:has(input:focus-visible)` versus moving to the sketch's button-with-roving-tabindex — either is acceptable if native arrow-key semantics and the Home/End handler survive.
- Token names and homes for the new values (field widths, fill colours, forced-colors block), and whether the fill rule is one shared selector for stops, segments and chips.
- The default placement of the relocated "· declared" helper (D-08) until Mark tweaks it.
- The exact CONFORMANCE.md layout, which browser tool measures which check (the Playwright-backed gsd-browser sets exact viewports down to 393; claude-in-chrome's window floors near 500px and suits interactive spot checks), and how the sketch's toolbar state is set for each of the three states.
- Plan count and order, within: the page-shell plan and the battery plans first, the conformance pass last and gating; research's triage table (D-03) feeds both the plans and the sketch session.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The design authority (read the HTML, including its CSS)
- `.planning/sketches/007-full-battery/index.html` — the record pen and the battery; the authority for every battery decision above (line numbers cited throughout). Live file; revised per D-22 before execution.
- `.planning/sketches/007-full-battery/README.md` — the sketch's own design question, variants and what to look for.
- `.planning/sketches/003-front-matter-rows/index.html` — the page shell and front matter; the authority for D-15 (ladder, version row, table column widths).
- `.planning/sketches/MANIFEST.md` — which sketches won and which are superseded.

### Derivations (citation aids; the HTML governs where they disagree)
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md` — the structural contract (DOM order, axes, controls, status strings, keyboard, responsive ladder, "Where sources disagree (HTML wins)"); gains the "Visual treatment (from the sketch CSS)" section in research (D-02). Cite by section name in every task.
- `.claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery.md` — design decisions and CSS patterns (the `.stop5.on` / `.seg .opt.on` fill rules are here too).
- `.claude/skills/sketch-findings-sprinkles/references/cross-cutting-type-spacing-feedback.md` — type roles (uppercase only wraps a short caption; helpers plain), 6px caption gap, controls (hover 1.5px no shift; selected fill; forced-colors fallback), touch targets and ladder, measure-the-DOM verification.
- `.claude/skills/sketch-findings-sprinkles/references/page-shell-front-matter.md` — sketch 003's decisions and CSS (stack below 1100px; version row 1fr below 760px; cells uppercase).
- `.impeccable/surfaces/route-recipe-batch.md` — the brief; § controls (line 110: pen-blue picked state), § fields (108), the 2026-09-08 revision (115: measured fields sized to what they hold). Not edited in this phase (D-21).
- `DESIGN.md` — the Formulation Cookbook; amended per D-21 before planning (binder line 132, components 229/236, ink-field 244).

### Delivery record and prior decisions
- `.planning/ROADMAP.md` § Phase 03.3.1.1 — goal, the 18 items verbatim, the "many other errors" note.
- `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-UAT.md` — the Untriaged Notes the 18 come from.
- `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-CONTEXT.md` — D-01–D-24 (one save, two identical ceremonies, Add tasting placement, single tasting, dates, press-to-block) — closed, not reopened.
- `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-06-SUMMARY.md` — how the previous browser pass was run (orchestrator, executors have no browser), the Home/End and box-sizing fixes, `--pen-w` cap.
- `.planning/phases/03.3.1-the-full-battery-the-record-pen-rebuilt-from-sketch-007/03.3.1-UI-REVIEW.md` — the Impeccable audit that certified the contract-conformant (sketch-divergent) picked state; the method failure this phase corrects.
- `.planning/STATE.md` — Blockers/Concerns: the `.recipe-band`/`.vmeta` 393px overflow this phase closes (D-15).
- `.claude/CLAUDE.md` — conventions: tokens-only styling, no `dangerouslySetInnerHTML`, repository seam, English agent prose.

### Product authority
- `.planning/REQUIREMENTS.md` — UX1-01 (keyboard, labels, focus, AA) and UX1-03 (uncertainty, estimates, errors never by colour alone — a picked state is none of these) as build constraints.
- `product-requirements/05-domain-and-language.md` — settled interface labels (neither "Churned" nor "Churn date" is settled there); saying absence in words.
- `product-requirements/03-decision-register.md` — D11 familiar words, D12 open labels.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `app/src/ui/AxisMark.jsx` — the five-stop track: a label per stop wrapping a hidden native radio (`app.css` 1627 stretches it invisible), with the Home/End keydown handler from 03.3.1-06. D-04/D-07 restyle it; the mechanism stays.
- `app/src/ui/Segmented.jsx` — the 3-way radiogroup restyled as shared-border options (`app.css` 1676–1727); D-04/D-05/D-09 change only its picked and outline rules.
- `app/src/ui/BatchRow.jsx` — the pen body and the read view: `.tasting-field-row` (529), `.defects-row__caption` (595), the Remove tasting control (526), the read view's `.batch-row__cell-label` cells (680–744). D-08, D-10–D-14 land here.
- `app/src/ui/PenFoot.jsx` — the shared ceremony component (`SaveCeremony`, mounted twice per 03.3.1 D-01); Add tasting mounts beside it (101). D-14 makes both mounts carry it.
- `app/src/styles/tokens.css` — `--pen-w`, `--track-stop`, `--stop-w`/`--stop-h`, `--stop-gap`, touch aliases; new width and fill tokens join these groups. `app/src/styles/app.css` — the picked-state block (1638–1649, 1717–1727) and its "never a fill (No-Verdict Rule)" comments are what D-04 replaces; `.ink-field { width: 100% }` at 1303 is what D-13 scopes out of the pen; `text-transform: uppercase` already appears at 15 sites, so the caption class pattern exists.
- `app/src/ui/RecipePage.jsx` — the page grid whose ladder D-15 adds (sketch 003 patterns); `.batch-margin--pen { max-width: var(--pen-w) }` from 03.3.1.

### Established Patterns
- Every visual value through a `tokens.css` custom property; one-level at-rule contract (top-level `@media` blocks only, with the 759.98px prelude precedent); style test suites under `src/styles` pin selectors.
- Test style is render-only (`renderToStaticMarkup`), which cannot observe focus or computed style — hence the browser pass (D-17) and the scripted UAT (D-23) as the phase's real acceptance.
- Browser verification is the orchestrator's (executors have no browser tools; 03.3.1-06 Task 3 precedent). The Playwright-backed `gsd-browser` MCP sets exact viewports, including 393; the `claude-in-chrome` window floors near 500px.
- Measure the real DOM (`getBoundingClientRect`, `getComputedStyle`), never reason from CSS source alone (sketch-findings verification method; two spacing bugs in 03.3.1 were only visible that way).

### Integration Points
- Routes unchanged; the pen is page state in `RecipePage.jsx`; the batch row read view renders the saved record.
- The conformance pass reads both `.planning/sketches/007-full-battery/index.html` (served) and the running app; its output `03.3.1.1-CONFORMANCE.md` feeds `03.3.1.1-UAT.md`.
- Follow-ups outside `app/`: DESIGN.md (D-21, before planning), the sketch (D-22, before execution), `/impeccable document` after the build.

</code_context>

<specifics>
## Specific Ideas

- Mark, opening this work: "I'm having trouble getting the planner & executor to implement my designs which are well-speced using the /gsd-sketch command; what is going wrong?" — the diagnosis is recorded in the domain section; this phase is its first application.
- On the declared toggle: "I actually do not like the declared helper suffix inside the toggle: let's place it outside the toggle that is consistent with the design and then I will tweak the location in a quick task later."
- On focus: "in the sketch, tabbing into the Hardness the first stop got the focus ring." — the sketch's behaviour is the spec even where Mark cannot name the CSS.
- Mark's picked-state call is stronger than the sketch: fill everywhere, "no bold+underline anywhere", and app-wide.
- The three states of the pass (blank / fully marked / saved) exist because the fill decisions cannot be verified on a blank pen.

</specifics>

<deferred>
## Deferred Ideas

- **Quick-batch after this phase:** apply the app-wide picked-fill rule (D-04) to the recipe page's other pressed controls — Show changes (`app.css` 396), the step Skipped checkbox — once DESIGN.md carries the rule (D-21).
- **Quick task after this phase:** Mark tweaks the exact placement of the "· declared" helper now outside the Bitter toggle (D-08).
- **`/impeccable document` after the build** — re-derives DESIGN.md, `.impeccable/design.json` and `route-recipe-batch.md` from what shipped (the fill rule, caption case, field widths, the helper's new home), replacing the D-21 bridge edit; also absorbs 03.3.1's D-06 one-save revision, still pending.
- **Later sketch round** for any sketch-matches-app items the pass discovers after the D-22 session (D-03), each followed by a quick task.
- **Post-UAT triage** of gaps the pass logs beyond the 18 (D-19) into a quick-batch or a later phase.
- **Shared size tokens stay shared (Mark, 2026-09-15):** a width token like `--field-w-date` holds one value app-wide; a surface that genuinely needs a different width gets its own token on an exception basis, named for the exception. Raised when plan 02 moved `--field-w-date` from 12ch to 128px for the pen's churn date and the change was thought to have carried into the recipe-version ceremony — in fact `.versions__ceremony-field` is dead CSS (no JSX renders it), so nothing moved on screen. The rule stands for the next shared-token change, which will have a live second consumer. For `/impeccable document` to carry into DESIGN.md.
- **Remaining sentence-case captions elsewhere on the recipe page** (D-11) — noted for Impeccable, not this phase.
- **`/gsd-sketch --wrap-up`** to refresh the skill's `sources/007-full-battery/` copy after the D-22 revision (D-20).
- **`live-dom-uat` capability** — considered for per-wave DOM checks and left off; the orchestrator's gating pass covers it for this phase.
- **Sketch round for the ingredient table at phone width (Open Question 4, Mark 2026-09-15):** at 393 the table's sized columns total 368px in 353px of content, and neither `--col-numeric` (~1.4px of slack over the "% of batch" header) nor `--col-data` (~0.5px over "unreviewed") can give the 15px back without clipping. Plan 01 shipped `overflow-x: auto` on `.ingredient-table-region` so the table scrolls alone and the page does not (D-15 passes). **Mark's preference is to drop the Source column (`--col-data`, header rendered at `IngredientTable.jsx:608`) at that width instead — 86px freed against a 15px shortfall — decided in a later sketch session, not in code.** Sketch 003 never drew the table below 834px, so the sketch is drawn first and the app follows (D-01/D-22); until then `overflow-x: auto` stands and is NOT a departure row for the conformance pass.

</deferred>

---

*Phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr*
*Context gathered: 2026-09-14*
