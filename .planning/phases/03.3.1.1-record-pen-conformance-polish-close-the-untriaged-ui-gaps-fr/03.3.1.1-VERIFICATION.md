---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
verified: 2026-09-15T23:10:00Z
status: gaps_found
score: 6/12 must-haves verified
covered_files:
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/.continue-here.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-REVIEW.md
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/tokens.css
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/PenFoot.jsx
  - app/src/ui/PenFoot.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/Segmented.jsx
  - app/src/ui/Segmented.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
covered_digest: "v1:sha256:e3917637535cfb0e17b5a26ff3013aca430b32b607e1db3e42821193430f095b"
gaps:
  - truth: "DOM-driving JS breakpoints agree with the CSS breakpoints they must move in lockstep with (the 759.98px convention)"
    status: failed
    reason: "BatchRow.jsx's useBelow760 hook still reads window.matchMedia('(max-width: 760px)') — one pixel off every corresponding CSS rule (759.98px) that the axes grid's DOM shape must agree with. At exactly 760px this produces a hybrid state: React renders the stacked mobile DOM while the axis stop/track CSS keeps desktop 186px/38px geometry inside it. Documented as CR-01 in 03.3.1.1-REVIEW.md; unfixed as of the review's own commit (2b15331) and confirmed still present by direct grep."
    artifacts:
      - path: "app/src/ui/BatchRow.jsx"
        issue: "Lines 116 and 119 use '(max-width: 760px)'; every sibling CSS rule in app.css for the same feature uses '759.98px'"
    missing:
      - "Change both matchMedia literals in useBelow760 to '(max-width: 759.98px)' and update the header comment (also flagged as WR-02)"
  - truth: "Every className rendered by the record pen has a matching CSS rule (no unstyled sketch-conformance regression)"
    status: failed
    reason: "The tasted-date line in the read view renders <p className=\"batch-row__dates\"> — plural, with an 's' — which has no matching rule anywhere in app.css. Its singular sibling, .batch-row__date, is styled with the pen-blue text-face treatment every other recorded date in this component uses. The tasted-date paragraph silently falls through to page-body defaults instead. Documented as CR-02 in 03.3.1.1-REVIEW.md; the test suite (BatchRow.test.jsx:1062,1068) asserts the broken class verbatim, so it does not catch the mismatch. Unfixed as of the review's own commit and confirmed still present by direct grep."
    artifacts:
      - path: "app/src/ui/BatchRow.jsx"
        issue: "Line 218 renders className=\"batch-row__dates\" (plural); no such rule exists in app.css"
      - path: "app/src/ui/BatchRow.test.jsx"
        issue: "Lines 1062 and 1068 assert the broken markup verbatim, pinning the typo"
    missing:
      - "Change BatchRow.jsx:218 to className=\"batch-row__date\" (singular) and update the two test assertions to match"
  - truth: "SR-6 (the WebKit empty-date-field collapse on the churn row) is resolved in the running app, not just on a probe page"
    status: failed
    reason: "Commit 9df9d58 is recorded as fixing SR-6 and a probe page confirmed the fix on Mark's iPad (date box 26.0px vs text 26.3px, was -22.5px). But per .continue-here.md's remaining_work item 3, Mark subsequently reported the real app's churn row still shows the unchanged (collapsed) date box while the text fields beside it did take touch height. The probe replicates only the input pair, not the surrounding flex/label structure, so the fix's real-app applicability is unconfirmed and, per Mark's own report, apparently not actually landing."
    artifacts:
      - path: "app/src/styles/app.css"
        issue: "The 9df9d58 ink-field leading/floor rule exists but the churn row's real cascade (`.field-row__label--date > .pen-caption + .ink-field`) has not been confirmed to receive it in the running app"
    missing:
      - "Reproduce with a faithful replica of the real cascade (not the standalone probe pair) served to the device, per .continue-here.md's own prescription"
  - truth: "Melt row caption/control alignment matches the sketch (Melt test caption vs Melt style caption vertical alignment)"
    status: failed
    reason: "Measured on Mark's device at 1366/coarse: Melt test caption 28.8 / gap 6 / control top 1389.4 vs Melt style caption 16.2 / gap 19.9 / control top 1404.6 — 15px out of alignment. At 1280 the app matches the sketch for Melt test exactly, but the sketch's Melt style control did not resolve under the probe selector, so whether the sketch itself carries the same misalignment is still unknown — this is an open, unresolved item per .continue-here.md remaining_work item 4, not merely a documentation gap."
    artifacts:
      - path: "app/src/styles/app.css"
        issue: "Melt test / Melt style caption-to-control vertical rhythm diverges by ~15px at 1366px coarse-pointer; root cause (app-fix vs sketch-round) not yet established"
    missing:
      - "Establish whether the sketch has the same misalignment before deciding app-fix vs sketch-round (.continue-here.md's own next step)"
  - truth: ".method-step__edit avoids the column-flex stretch defect the same way .method-step__on-demand was fixed to"
    status: failed
    reason: "app.css's own comment at .method-step__edit (line 1103-1106) claims 'same shape as .method-step__on-demand above' — but .method-step__on-demand (lines 1092-1101) carries align-self: flex-start specifically to stop a column-flex item from stretching to the full column width, while .method-step__edit (lines 1107-1110) has display: block and margin-top only, with no align-self. This is a statically verifiable defect (not merely 'unconfirmed' as .continue-here.md's remaining_work item 6 characterizes it): the two rules are not actually the same shape, and .method-step__edit almost certainly stretches to the full step column exactly as .method-step__on-demand did before 03.3.1.1 fixed it there."
    artifacts:
      - path: "app/src/styles/app.css"
        issue: "Lines 1107-1110: .method-step__edit lacks align-self: flex-start, unlike its sibling .method-step__on-demand (lines 1092-1101) which the comment claims it matches"
    missing:
      - "Add align-self: flex-start to .method-step__edit, matching .method-step__on-demand, and confirm on a saved step (the only state it renders in)"
deferred: []
advisory: []
human_verification:
  - test: "Forced-colours (Windows high-contrast) rendering of picked stops/options/chips"
    expected: "A picked box keeps a fill in Highlight; the ring takes CanvasText, per the cross-cutting design rule"
    why_human: "CONFORMANCE.md records this row as 'unverified' — the headless driver used for the pass cannot emulate forced-colors: active (RESEARCH A7). No browser was used in this verification pass either."
  - test: "Touch mode at a wide viewport (1366px, pointer:coarse) — axis stop sizing"
    expected: "A drawn, Mark-approved touch-at-wide-viewport treatment for the axis stops; currently they fall back to 32px (the pre-touch main tap target) because 44px stops plus a 216px track would overflow the wide grid's 213.3px columns"
    why_human: "No sketch has ever drawn this combination (touch AND ≥1280px) — this is an open design question that blocks further app CSS per .continue-here.md's own blocker note, not a code defect to grep for"
  - test: "008's touch font bump (.touch .ink-field, .touch .prose-field { font-size: var(--type-note) }, 16px) — implement or confirm departure"
    expected: "Mark's explicit yes/no on whether the app's 13px touch-mode field text is an accepted departure from sketch 008's 16px rule"
    why_human: "Deliberately left unimplemented pending a product decision (.continue-here.md remaining_work item 5); confirmed via grep that --type-note (16px) is never applied to .touch .ink-field — this is a pre-declared, not-yet-resolved departure, not a bug"
---

# Phase 03.3.1.1: Record pen conformance polish Verification Report

**Phase Goal:** Close the 18 (research counts 19) untriaged UI gaps Mark logged during 03.3.1's UAT (393px-width session) between the shipped record pen/batch row and the sketch 007 / structural contract, without reopening any already-settled decision (D-01–D-24) or the just-closed G-03.3.1-4 caption fix.
**Verified:** 2026-09-15T23:10:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-15: below 1100px the page is one column, padding steps to 20px at 600px | ✓ VERIFIED | Plan 01 shipped, conformance pass I-01 rows all pass; `document.scrollWidth === window.innerWidth` acceptance criterion recorded met in CONFORMANCE.md Result section |
| 2 | D-10: every record-pen caption is uppercase 12px/500/0.04em via text-transform | ✓ VERIFIED | `grep -n "text-transform: uppercase" app.css` shows the rule applied consistently across caption classes (lines 210-211, 362-363, 393-394, 433-434, 852-853, 877-878, 917-918, 1017-1018) |
| 3 | D-04/05/09: picked stops/options/chips render solid pen-blue fill, no bold/underline/outline; defect is a 13px square before an ink word | ✓ VERIFIED | Plan 03 shipped; CONFORMANCE.md I-04/I-10/I-11/I-13 rows all pass; commit `f6690a1` fixed the one gap the pass found (defect touch-target specificity) |
| 4 | D-04 supersession: each segmented control carries a caption line with a Clear text control that reserves 24px/44px correctly | ✓ VERIFIED | `grep` confirms `AxisMark.jsx:72` uses `className="text-control axis-mark__clear"`; CONFORMANCE.md "Fixes applied" documents `7f1e415` giving the axis Clear the same class the segmented Clear already had |
| 5 | D-14: both save ceremonies read Add tasting \| Cancel \| Save batch (or Cancel \| Save batch) in DOM order, right-aligned | ✓ VERIFIED | Plan 05 shipped; no review finding or conformance gap against this element |
| 6 | D-17: 03.3.1.1-CONFORMANCE.md exists with one row per contract element per width per applicable state, naming selectors, values, delta and verdict | ✓ VERIFIED (with caveat) | File exists, 653 rows, 19/19 triaged items pass structurally. **Caveat, not a fail of this must-have's own text:** the file's own banner marks the Measurements table SUPERSEDED — five CSS commits landed after it was written, so the numbers are stale even though the verdicts hold in direction. Re-measurement is explicitly deferred to a follow-up phase, not silently dropped. |
| 7 | JS breakpoints agree with the CSS breakpoints they must move in lockstep with (759.98px convention) | ✗ FAILED | CR-01 (03.3.1.1-REVIEW.md): `BatchRow.jsx`'s `useBelow760` still reads `760px`, not `759.98px`, unlike every corresponding CSS rule. Confirmed present via direct grep after the review; no fix commit follows the review commit |
| 8 | Every rendered className has a matching CSS rule (no unstyled sketch-conformance regression) | ✗ FAILED | CR-02 (03.3.1.1-REVIEW.md): `batch-row__dates` (plural) has no CSS rule; the tasted-date line renders unstyled instead of matching its sibling `.batch-row__date`'s pen-blue text-face treatment. Confirmed present via direct grep; test suite pins the broken class rather than catching it |
| 9 | SR-6 (WebKit empty-date-field collapse) is resolved in the real running app | ✗ FAILED | `.continue-here.md` remaining_work item 3: the fix (`9df9d58`) was confirmed on a standalone probe page but Mark reports the real app's churn row still collapses. Not resolved by this phase's own account |
| 10 | Melt test / Melt style caption-control alignment matches the sketch | ✗ FAILED | `.continue-here.md` remaining_work item 4: measured 15px misalignment at 1366/coarse; whether the sketch shares it is unestablished. Open |
| 11 | `.method-step__edit` avoids the column-flex stretch defect its sibling `.method-step__on-demand` was fixed for | ✗ FAILED | Statically confirmed by this verification (not just "suspected" as `.continue-here.md` characterizes it): `app.css` lines 1107-1110 lack `align-self: flex-start`, which lines 1092-1101 carry specifically to prevent this stretch. The two rules are not the same shape despite the comment's claim |
| 12 | Touch mode at a wide viewport (≥1280px, pointer:coarse) has a drawn, approved treatment | ? UNCERTAIN | No sketch has ever drawn touch at this width; `.continue-here.md` names this an open DESIGN question blocking further app CSS, not something code can resolve. Routed to human verification |

**Score:** 6/12 truths verified (1 verified-with-caveat counted as verified since its own text — artifact existence and structure — holds)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/.../03.3.1.1-CONFORMANCE.md` | 19-item x 10-width x 3-state measurement record | ✓ VERIFIED (superseded numbers, flagged) | 653 rows, 19/19 structural pass; banner honestly marks it superseded |
| `.planning/phases/.../03.3.1.1-REVIEW.md` | Code review gate output | ✓ VERIFIED | 2 critical, 2 warning, 1 info findings recorded; both criticals confirmed still unfixed in the codebase by this verification |
| `app/src/ui/BatchRow.jsx` | Battery/read-view markup | ⚠️ CONTAINS UNFIXED DEFECTS | CR-01 (breakpoint) and CR-02 (unstyled date line) both present |
| `app/src/styles/app.css` | Contract CSS | ⚠️ CONTAINS UNFIXED DEFECT | `.method-step__edit` missing `align-self: flex-start` |
| `03.3.1.1-UAT.md` | End-of-phase UAT script (D-23) | ✗ DOES NOT EXIST | D-23 scripts UAT from the CONFORMANCE Measurements table, which is superseded — correctly not written rather than written against stale numbers. This is an honest absence, not a silent drop; the follow-up phase's first task is re-measurement, after which UAT can be scripted |

### Key Link Verification

Not applicable in the traditional sense (this is a CSS/markup conformance phase, not a data-flow feature). The relevant "link" verified is JS-state-to-CSS-rule agreement (breakpoint literals), which is where CR-01 was found — see Observable Truth #7.

### Behavioral Spot-Checks

SKIPPED — per the task's explicit instruction, this verification runs no browser. All checks in this report are static (grep against CSS/JSX, cross-referencing REVIEW.md/CONFORMANCE.md/.continue-here.md against the current tree). Where hardware/browser confirmation is required, the item is routed to human verification instead of asserted.

### Probe Execution

Not applicable. `03.3.1.1-conformance-probe.js` is a browser-context helper library (function definitions meant to be evaluated inside a live tab via `new Function`), not a standalone `scripts/*/tests/probe-*.sh` executable. No such probe scripts exist for this phase.

### Requirements Coverage

No requirement IDs are mapped to this phase — every PLAN's frontmatter carries `requirements: []`, matching the ROADMAP's own phase note ("None new — every item is a conformance gap..."). `grep` of REQUIREMENTS.md for "03.3.1.1" returns no matches. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/src/ui/BatchRow.jsx` | 116, 119 | Breakpoint literal disagrees with sibling CSS (CR-01) | 🛑 Blocker | Hybrid DOM/CSS state at exactly 760px |
| `app/src/ui/BatchRow.jsx` | 218 | Orphaned className, unstyled element (CR-02) | 🛑 Blocker | Sketch-conformance regression on the tasted-date line |
| `app/src/styles/app.css` | 1107-1110 | `.method-step__edit` missing `align-self: flex-start` present on its documented-identical sibling | 🛑 Blocker | Column-flex stretch defect, almost certainly visible whenever a step is saved |
| `app/src/ui/Segmented.jsx` | 25, 37, 51 | Dead `captionId`/`id`, never wired via `aria-labelledby` unlike the sibling `AxisMark` pattern (WR-01) | ⚠️ Warning | No functional break (aria-label already supplies the name) but a maintenance trap |
| `app/src/ui/BatchRow.jsx` | 106 | Comment documents the same wrong breakpoint as CR-01 (WR-02) | ℹ️ Info | Will recur silently if CR-01 is fixed without updating the comment |
| `.planning/phases/.../03.3.1.1-06-SUMMARY.md` | 158, 178 | Frontmatter says `status: complete` but the body text says "Not ready to advance — halted at Task 2" and "Completed (Task 1 only; Task 2 pending)" | ℹ️ Info | Stale documentation — Task 2 (the browser pass) did in fact run, per CONFORMANCE.md and `.continue-here.md`. Does not affect code correctness but is a factual inconsistency in the phase's own record |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file this phase touched (`app.css`, `tokens.css`, `AxisMark.jsx`, `BatchRow.jsx`, `PenFoot.jsx`, `RecipePage.jsx`, `Segmented.jsx`, `VersionRow.jsx`).

### Human Verification Required

See frontmatter `human_verification`. Three items: forced-colours rendering (driver limitation), touch-at-wide-viewport axis sizing (open design question, needs a sketch round with Mark before any more app CSS), and the 008 touch-font-bump departure (needs Mark's explicit yes/no).

### Gaps Summary

The six plans (01-05 built the battery/page-shell/controls/ceremonies against sketch 007/003/008; 06 ran a 653-row conformance pass) did land real, verified work — six of the phase's twelve observable truths hold, the full 936-test suite is green, and the build is clean. That is real progress and should not be minimized.

But the phase goal is "close the 18/19 untriaged gaps... without reopening any already-settled decision" — and it cannot be marked achieved while:

1. **Two unresolved critical defects** (CR-01, CR-02) sit in the exact files this phase modified, caught by this phase's own code-review gate and never fixed — the review is the newest commit in the phase's history, with nothing after it.
2. **One newly-discovered defect** (`.method-step__edit`'s missing `align-self: flex-start`) was found by this verification pass through direct comparison with its documented-identical sibling, which this phase's own review did not flag.
3. **Three device-only defects** (SR-6 real-app collapse, wide-viewport touch sizing, Melt row alignment) surfaced by Mark on his own hardware are explicitly open per `.continue-here.md`, which itself states the team agreed to pause rather than keep patching — this is not a hidden gap, it is an honestly-documented one, but it is still a gap against the goal as stated.
4. **No UAT.md exists** — correctly, since D-23 scripts it from a now-superseded Measurements table, but the absence still means the phase's own closing artifact was never produced.
5. **One stale summary** (06-SUMMARY.md's body contradicts its own frontmatter and the actual CONFORMANCE.md record) — a documentation-integrity issue, not a code defect, but worth fixing so the phase's history is auditable.

The team's own decision on 2026-09-15 to pause and hand items 1-6 to a follow-up phase (recorded in `.continue-here.md`) is the right call, and this verification agrees with that read of the situation — it does **not** recommend more ad-hoc patching of 03.3.1.1. But per this workflow's own status contract, a phase with unresolved must-haves cannot be marked `passed`; it is `gaps_found`, with the gaps structured above so a `/gsd-plan-phase --gaps` follow-up (or the already-planned follow-up phase) can consume them directly. CR-01 and CR-02 in particular are small, mechanical, low-risk fixes (a string literal change and a class-name typo) that do not require a sketch round or device access — there is no reason they need to wait for the follow-up phase's sketch work.

---

_Verified: 2026-09-15T23:10:00Z_
_Verifier: Claude (gsd-verifier)_
