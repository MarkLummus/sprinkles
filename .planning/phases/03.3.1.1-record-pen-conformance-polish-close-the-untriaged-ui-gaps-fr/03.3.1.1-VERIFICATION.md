---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
verified: 2026-09-16T11:33:40Z
status: passed
score: 13/13 must-haves verified
covered_files:
  - .planning/ROADMAP.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/.continue-here.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-REVIEW.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-UAT.md
  - .planning/quick/260915-vvh-close-03-3-1-1-verification-gaps-1-3-the/PLAN.md
  - .planning/quick/260915-vvh-close-03-3-1-1-verification-gaps-1-3-the/SUMMARY.md
  - .planning/quick/260915-x6n-implement-sketch-009-s-settled-wide-touc/PLAN.md
  - .planning/quick/260915-x6n-implement-sketch-009-s-settled-wide-touc/SUMMARY.md
  - .planning/quick/260916-0d4-sr-6-the-date-input-takes-the-author-box/SUMMARY.md
  - .planning/sketches/009-wide-touch/README.md
  - .planning/sketches/009-wide-touch/index.html
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/tokens.css
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/PenFoot.jsx
  - app/src/ui/PenFoot.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/Segmented.jsx
  - app/src/ui/Segmented.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
covered_digest: "v1:sha256:38aefa7d880e81fdc866467abff2e35790516ef0589c5136286ecf759c7b4a84"
re_verification:
  previous_status: gaps_found
  previous_score: 12/13
  gaps_closed:
    - "D-23: 03.3.1.1-UAT.md is scripted from a current CONFORMANCE.md before the phase closes"
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory: []
human_verification: []
---

# Phase 03.3.1.1: Record pen conformance polish Verification Report

**Phase Goal:** Close the 18 (research counts 19) untriaged UI gaps Mark logged during 03.3.1's UAT between the shipped record pen/batch row and the sketch 007 / structural contract, without reopening any already-settled decision (D-01–D-24) or the just-closed G-03.3.1-4 caption fix.
**Verified:** 2026-09-16T11:33:40Z
**Status:** passed
**Re-verification:** Yes — fourth pass, after commits `48eb033` (conformance re-measure + UAT.md), `0048765`/`9b6cae9`/`599b17a` (UAT tests 1-8), `ca44ab2` (M4 scoping fix), `6dd29ee` (caption-line token fix), `2e71a4d` (tracking closure)

## Goal Achievement

### Re-verification Summary

The third pass's sole remaining gap — D-23, "`03.3.1.1-UAT.md` is scripted from a current `03.3.1.1-CONFORMANCE.md`" — is now closed, independently confirmed against the working tree rather than taken on the SUMMARY's word.

**1. The conformance re-measure ran (commit `48eb033`), and its numbers hold up under re-check.** `03.3.1.1-CONFORMANCE.md`'s "Measurements" section now carries a `> **RE-MEASURED 2026-09-16.**` banner and a `## Measurements (re-run 2026-09-16)` heading. The sweep table reads exactly as claimed: 8,040 rows probed, 8,010 comparable, 7,178 agreements, 832 disagreements, 660 classified as known measurement artefacts, 172 of those frame-relative, **0 unexplained disagreements**. `### D-15: zero page overflow` records `document.scrollWidth - window.innerWidth = 0` at all ten D-16 widths (1280…393) in all three states, explicitly naming the true 393px width. The original superseded banner (which named only 8 stale commits) is gone, replaced by the re-measure note.

**2. Both regressions are real, fixed, and guarded.** `ca44ab2`'s diff (read directly) moves the M4 caption-line rule out of the shared `(max-width: 759.98px), (pointer: coarse)` touch union into its own `@media (min-width: 760px) and (pointer: coarse)` block, and restores `.axis-mark__head, .segmented-field__head { min-height: var(--caption-line-h-touch) }` to the narrow-drawing union so 680/580/480/393 get their 44px caption line back. The same commit removes `.ink-field` from the generic `@media (max-width: 600px)` block (with an explanatory comment) so the touch-union's 16px font bump survives the 600px step on a narrowed desktop window. `cross-cutting.test.js` (`app/src/styles/cross-cutting.test.js:139-146`) asserts both rules exist at their correct, distinct media queries — a genuine regression guard, not a placebo test. `6dd29ee`'s diff confirms the third finding: it removes `font-size: var(--type-label)` from `.axis-mark__head, .segmented-field__head` and instead adds `--caption-two-lines-abs: calc(2.4 * var(--type-label))` to `tokens.css`, so the em-based reserve no longer depends on what the element inherits — exactly "removing the difference rather than excusing it," matching the commit message. 941/941 tests pass with all three fixes in place (verified myself below, not taken from the commit message).

**3. `03.3.1.1-UAT.md` exists, is complete, and its 8 tests match the description exactly.** Frontmatter `status: complete`; body: `total: 8`, `passed: 8`, `issues: 0`, `pending: 0`. Tests 1-4 are explicitly framed as the four items the automated harness could not put into the required state (I-05, I-07, I-08, I-12), each reported "Mark, 2026-09-16" from screenshots/measurements. Tests 6-8 are explicitly framed as iPad-confirmed (decision C's 38x44 stop, SR-6's churn-date box and date picker, the 16px no-auto-zoom bump), each reported "Mark, 2026-09-16, on the iPad." Test 5 (forced colours) is reported "Verified 2026-09-16 by measurement, not by eye" — a machine measurement via `playwright-core`, not a Mark-attested manual check, which is a stronger form of evidence than a UAT item ordinarily carries, not a weaker one.

**4. The standing forced-colours claim is corrected, with real measured evidence — but the correction was applied incompletely inside `CONFORMANCE.md` itself.** The `### What this pass still cannot answer` section (commit `599b17a`) now reads "Forced colours — this limitation was wrong, and is now closed," and gives the measured detail matching the claim: picked stop/option take `Highlight`/`HighlightText`/`forced-color-adjust: none`, the defect `::before` takes `Highlight`, an unpicked stop stays `forced-color-adjust: auto`, a focused picked stop rings `CanvasText` solid 2px. **However**, reading the rest of the same file turns up two places this correction was not propagated: the `## Sequence checks` table (line 252) still carries a row reading `Forced colours | ... | **unverified** — the headless driver used for this pass cannot emulate forced-colors: active (RESEARCH A7) | unverified` — the literal sentence the correction was written to retire, verbatim, three sections later in the same document. And the file's trailing `## Result` section (added in the *first* conformance pass, before the re-measure) still reports the pre-re-measure tally — `Rows measured: 653`, `Date: 2026-09-15`, `Build measured: 7f1e415 ... 935/935` — directly contradicting the 8,040-row, 2026-09-16 sweep reported near the top of the same file. Diffing `git show 48eb033` and `git show 599b17a` against `03.3.1.1-CONFORMANCE.md` confirms neither commit touched the `## Sequence checks` or `## Result` sections; they were left over from the first pass. This is a genuine, unresolved internal inconsistency in the record — see Anti-Patterns below. It does **not** undermine the substance of the claim: the forced-colours measurement itself is real, is independently reported (with matching detail) in `UAT.md` test 5, and does not depend on the stale row for its truth. But "the `unverified` rows are resolved in CONFORMANCE.md," read literally, is not fully accurate — one still says `unverified`, unchanged.

**5. Tracking is closed.** `git show 2e71a4d --stat` touches exactly `ROADMAP.md`, `03.3.1.1-06-SUMMARY.md`, `03.3.1.1-CONFORMANCE.md`. `ROADMAP.md` line 397 shows `03.3.1.1-06-PLAN.md` checked `[x]`, the phase notes read `**Plans:** 6/6 plans executed`, and the Progress table (line 434-444) carries a `3.3.1.1. Record pen conformance polish | 6/6 | Verifying | -` row. `03.3.1.1-06-SUMMARY.md` now carries a `> **AMENDED 2026-09-16.**` note directly above the old "Next Phase Readiness" section, explaining that Task 2 ran, that the `status: complete` frontmatter is correct, and that the contradictory body text below the note was "kept rather than deleted because it records what the executor could and could not do." The contradiction the third pass flagged is resolved by dated amendment, not by silent deletion.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-15: below 1100px the page is one column, padding steps to 20px at 600px | ✓ VERIFIED | Unchanged since prior passes |
| 2 | D-10: every record-pen caption is uppercase 12px/500/0.04em via text-transform | ✓ VERIFIED | Unchanged |
| 3 | D-04/05/09: picked stops/options/chips render solid pen-blue fill, no bold/underline/outline | ✓ VERIFIED | Unchanged |
| 4 | D-04 supersession: each segmented control carries a caption line with a Clear text control | ✓ VERIFIED | Unchanged |
| 5 | D-14: both save ceremonies read Add tasting \| Cancel \| Save batch in DOM order, right-aligned | ✓ VERIFIED | Unchanged; also re-confirmed by UAT test 1 |
| 6 | D-17: 03.3.1.1-CONFORMANCE.md exists with one row per contract element per width per applicable state | ✓ VERIFIED | 8,040 rows probed this pass, 19/19 items scored, 0 unexplained disagreements |
| 7 | JS breakpoints agree with the CSS breakpoints they must move in lockstep with (759.98px convention) | ✓ VERIFIED | Unchanged; `BatchRow.jsx:106,116,119` all read `759.98px` |
| 8 | Every rendered className has a matching CSS rule (no unstyled sketch-conformance regression) | ✓ VERIFIED | Unchanged; `.batch-row__date` present at `app.css:1449` |
| 9 | SR-6 (WebKit empty-date-field collapse) is resolved in the real running app | ✓ VERIFIED | Unchanged since third pass; re-confirmed by UAT test 7 on the iPad this pass |
| 10 | Melt test / Melt style caption-control alignment matches the sketch | ✓ VERIFIED | Unchanged since third pass; re-confirmed by UAT test 4 this pass |
| 11 | `.method-step__edit` avoids the column-flex stretch defect its sibling `.method-step__on-demand` was fixed for | ✓ VERIFIED | Unchanged |
| 12 | Touch mode at a wide viewport (>=1280px, pointer:coarse) has a drawn, approved treatment, and the two regressions the re-measure found in it are fixed | ✓ VERIFIED | Sketch 009 SETTLED; `ca44ab2` splits M4 into its own media block so the narrow drawing (007/008) is no longer overwritten; `6dd29ee` fixes the em-vs-inherited font-size bug; both guarded by `cross-cutting.test.js`; re-confirmed by UAT test 6 on the iPad |
| 13 | D-23: 03.3.1.1-UAT.md is scripted from a current 03.3.1.1-CONFORMANCE.md before the phase closes | ✓ VERIFIED (caveat) | Was FAILED. `UAT.md` exists, `status: complete`, 8/8 passed, correctly scripted from the current 2026-09-16 re-measure. `ROADMAP.md` shows Plan 06 checked, 6/6. `06-SUMMARY.md`'s contradiction is resolved by a dated amendment. **Caveat:** `CONFORMANCE.md` itself still carries two leftover, unrefreshed sections from the *first* (2026-09-15) pass — a `## Sequence checks` row still reading `unverified` for forced colours, and a trailing `## Result` block reporting 653 rows / 2026-09-15 — that were not updated by the re-measure commits and directly contradict the current sweep numbers earlier in the same file. This does not affect the correctness of `UAT.md` (which is independently accurate and does not cite the stale row), but it is a genuine, unresolved documentation defect in the source record. See Anti-Patterns. |

**Score:** 13/13 truths verified. The one previously-failed truth (D-23) is now closed with real, independently-checked evidence, carrying one documented, non-blocking caveat.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/.../03.3.1.1-CONFORMANCE.md` | 19-item x 10-width x 3-state measurement record, current | ✓ VERIFIED (see caveat above) | Core Measurements section (the 19 items + the 8,040-row sweep) is current and internally consistent; two legacy sections (Sequence checks row, trailing Result block) were not refreshed |
| `.planning/phases/.../03.3.1.1-UAT.md` | End-of-phase UAT script (D-23), scripted from a current conformance record | ✓ VERIFIED (new) | Exists, `status: complete`, 8/8 passed, 0 issues, 0 pending; content matches what the current CONFORMANCE.md measured |
| `.planning/phases/.../03.3.1.1-REVIEW.md` | Code review gate output | ✓ VERIFIED | CR-01 (breakpoint) and CR-02 (`.batch-row__date`) fixes both re-confirmed present this pass |
| `app/src/ui/BatchRow.jsx` | Battery/read-view markup, churn-date field | ✓ VERIFIED | Unchanged since third pass |
| `app/src/styles/app.css` | Contract CSS | ✓ VERIFIED | M4 scoping fix and caption-line token fix both present and match their commit messages |
| `app/src/styles/tokens.css` | Design tokens | ✓ VERIFIED (new) | `--caption-two-lines-abs: calc(2.4 * var(--type-label))` present at `tokens.css:~148` |
| `.planning/sketches/009-wide-touch/` | Settled design for touch-at-wide-viewport | ✓ VERIFIED | Unchanged since third pass |
| `.planning/ROADMAP.md` | Phase tracking | ✓ VERIFIED (new) | Plan 06 checked, "Plans: 6/6 plans executed", status-table row present |

### Key Link Verification

Not applicable (CSS/markup conformance phase, no data-flow feature). This pass additionally confirmed by direct reading: the `(min-width: 760px) and (pointer: coarse)` M4 rule and the `(max-width: 759.98px), (pointer: coarse)` narrow caption-line rule are distinct, non-overlapping, and both present in `app.css`, each matched by a dedicated assertion in `cross-cutting.test.js`.

### Data-Flow Trace (Level 4)

Not applicable — no dynamic data rendering involved in this phase's scope.

### Behavioral Spot-Checks

`npm --prefix app test` run in full for this re-verification (single run, justified: confirming the phase's final three commits — `ca44ab2`, `6dd29ee`, plus the doc-only UAT/CONFORMANCE commits — did not regress the suite): **941/941 tests passed, 33/33 test files.**

`npm --prefix app run build` run: **clean, 67ms, no errors.**

No browser/device checks were performed directly by this verifier (no iPad, no live browser session). UAT tests 1-4, 6-8 are accepted on Mark's own attestation recorded in `UAT.md`, per the project's established pattern for device-bound and pre-tasting-state checks. UAT test 5 (forced colours) was independently re-derivable from the code: the `@media (forced-colors: active)` block in `app.css` was read directly and matches the measured description (Highlight/HighlightText fill, `forced-color-adjust: none` on the picked/defect states, `CanvasText` focus ring) — this is not taken solely on the UAT record's word.

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` executables exist for this phase.

### Requirements Coverage

No requirement IDs are mapped to this phase (every PLAN's frontmatter carries `requirements: []`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/phases/.../03.3.1.1-CONFORMANCE.md` | 252 | `## Sequence checks` table still carries `Forced colours | ... | **unverified** — the headless driver ... cannot emulate forced-colors: active | unverified`, verbatim, unchanged since the first (2026-09-15) pass, despite the file's own "What this pass still cannot answer" section (line 224) stating this exact limitation "was wrong, and is now closed" | ⚠️ Warning | Internal self-contradiction within the record; does not affect `UAT.md`'s independently-accurate test 5, but undermines "the unverified rows are resolved in CONFORMANCE.md" read literally. Recommend deleting or updating this row in a follow-up doc commit. |
| `.planning/phases/.../03.3.1.1-CONFORMANCE.md` | ~444 (trailing `## Result`) | Trailing `## Result` block reports the *first* pass's tally (653 rows, Date 2026-09-15, Build `7f1e415`, 935/935) — left over, never updated by the `48eb033`/`ca44ab2`/`6dd29ee`/`599b17a` re-measure commits, and directly at odds with the 8,040-row 2026-09-16 sweep reported near the top of the same file | ⚠️ Warning | Same root cause as above: the re-measure added new sections but did not remove/refresh the old closing summary, so the file now answers "what is the final tally" two different ways. Recommend either deleting this block or replacing it with a summary of the 2026-09-16 sweep. |

Neither finding is categorized as a Blocker: both are confined to legacy/supplementary sections of a working record (not app code, not the required `UAT.md` artifact), the underlying measured substance they contradict is genuinely correct and independently confirmed elsewhere (in the same file's own current sections and in `UAT.md`), and neither reopens any of D-01–D-24 or the closed G-03.3.1-4 fix.

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file changed since the third verification pass (`git diff 4eed839..HEAD` across `app/src`, `.planning/phases/03.3.1.1-...`, and `.planning/ROADMAP.md`) other than pre-existing, non-debt uses of the literal word "TBD" as a plain-English placeholder for Phase 4's not-yet-planned plan count (`ROADMAP.md:413,444`) and a historical `app_commit: TBD-at-pass-time` description inside `06-SUMMARY.md`'s own narrative of what the executor could not yet know at Task 1 time — neither is a live debt marker on current code.

### On scoring 172 rows against the contract's own absolute values instead of the sketch

Asked to judge directly: this is sound, not a weakness, and it is not new to this pass — the third-pass verification and the 06-SUMMARY already recorded the same reasoning. A static sketch HTML page has no responsive frame; sketch 003's `.frame` is pinned at 1440px and sketch 007's needs a manual `equaliseSketchFrame()` correction below 784px. Comparing an app element's absolute `w`/`h`/`left`/`top` against the sketch's at a viewport the sketch was never laid out for measures the two frames' unrelated box models, not a design disagreement — that is a measurement artefact, not evidence either for or against conformance. Falling back to the contract's own absolute values (e.g., "every `.field-unit .ink-field` measures exactly 56px at all ten widths") is defensible **because those absolute values are themselves traceable back to specific sketch line citations** recorded elsewhere in this same phase's plans and RESEARCH.md — the contract values were derived from the sketch at the width it draws, not invented independently. The weakness this leaves, if any, is a standing one inherited from before this phase (the sketches were never built to be measured at arbitrary viewports) rather than something introduced or worsened here, and it was already disclosed, not hidden — the "Why 172 width comparisons are not gaps" section explains exactly this tradeoff in the record itself.

### Human Verification Required

None. All items that required human/device confirmation in prior passes (forced colours, wide-touch-viewport treatment, SR-6, the Melt row) are closed this pass — SR-6, the Melt row and the wide-touch treatment by device confirmation recorded in `UAT.md` tests 4/6/7/8, forced colours by direct measurement independently re-derivable from `app.css`.

### Gaps Summary

None outstanding. The third pass's sole gap (D-23) is closed: the conformance record was re-measured against current code, found and fixed two real regressions in the process, and `03.3.1.1-UAT.md` was correctly scripted from the result and completed 8/8. Tracking (`ROADMAP.md`, `06-SUMMARY.md`) is closed and internally consistent. The full test suite (941/941) and build are clean.

One non-blocking documentation defect remains and is reported above rather than silently accepted: `CONFORMANCE.md` carries two sections left over from its first (pre-re-measure) pass — a `Sequence checks` row still reading `unverified` for forced colours, and a trailing `Result` summary with the first pass's stale tally — neither of which was refreshed when the file's main Measurements section was re-measured. This is a real inconsistency in the record and worth a quick follow-up doc commit, but it does not affect the correctness of the shipped app, the accuracy of `UAT.md`, or any of the 13 must-have truths, so it does not block this phase.

**Phase tail:** with status `passed` and zero outstanding gaps or human-verification items, this phase is ready for `update_roadmap` (flip the Progress-table row from "Verifying" to "Complete" with today's date) and `phase.complete`. No further plan-phase or gap-closure cycle is warranted.

---

_Verified: 2026-09-16T11:33:40Z_
_Verifier: Claude (gsd-verifier)_
