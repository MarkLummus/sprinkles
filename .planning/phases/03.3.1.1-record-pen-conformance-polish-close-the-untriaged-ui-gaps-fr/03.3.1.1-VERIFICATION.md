---
phase: 03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr
verified: 2026-09-16T05:00:00Z
status: gaps_found
score: 12/13 must-haves verified
covered_files:
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/.continue-here.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-01-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-02-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-03-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-04-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-05-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-PLAN.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-SUMMARY.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONTEXT.md
  - .planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-REVIEW.md
  - .planning/quick/260915-vvh-close-03-3-1-1-verification-gaps-1-3-the/PLAN.md
  - .planning/quick/260915-vvh-close-03-3-1-1-verification-gaps-1-3-the/SUMMARY.md
  - .planning/quick/260915-x6n-implement-sketch-009-s-settled-wide-touc/PLAN.md
  - .planning/quick/260915-x6n-implement-sketch-009-s-settled-wide-touc/SUMMARY.md
  - .planning/quick/260916-0d4-sr-6-the-date-input-takes-the-author-box/SUMMARY.md
  - .planning/sketches/009-wide-touch/README.md
  - .planning/sketches/009-wide-touch/index.html
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/tokens.css
  - app/src/ui/AxisMark.jsx
  - app/src/ui/AxisMark.test.jsx
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/PenFoot.jsx
  - app/src/ui/PenFoot.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/Segmented.jsx
  - app/src/ui/Segmented.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
covered_digest: "v1:sha256:220826b9d1b0254df83bcc05dbdccdfd7230da2cc767da79799084ff496e0707"
re_verification:
  previous_status: gaps_found
  previous_score: 9/12
  gaps_closed:
    - "SR-6 (the WebKit empty-date-field collapse on the churn row) is resolved in the real running app"
    - "Melt test / Melt style caption-control alignment matches the sketch"
  gaps_remaining:
    - "D-23: 03.3.1.1-UAT.md is scripted from a current CONFORMANCE.md before the phase closes — newly elevated from a documentation caveat to a blocking gap now that both device-bound defects are fixed and nothing else stands between this phase and `passed`"
  regressions: []
gaps:
  - truth: "D-23: 03.3.1.1-UAT.md is scripted from a current CONFORMANCE.md before the phase closes"
    status: failed
    reason: "03.3.1.1-UAT.md does not exist anywhere in the phase directory. ROADMAP.md still lists Plan 06 unchecked ('Plans: 5/6 plans executed'), so the phase's own roadmap tracking has not been closed even though 03.3.1.1-06-SUMMARY.md's frontmatter claims status: complete — the SUMMARY's own body text contradicts that frontmatter (see Anti-Patterns). CONFORMANCE.md's superseded banner names only 8 commits as postdating its Measurements table (5a9169f, d2e4149, 9df9d58, f975ec0, 99d89c3, 748e0dc, c5085f4, 3c7a3cd); two more now also postdate it and are unlisted (246f172, 1ee6c37) — both change measurable values (stop height, axis-head font-size, .ink-field font-size at touch). The banner's own instruction — 'Do not script UAT from the Measurements table until it is re-measured' — has not been followed."
    artifacts:
      - path: ".planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-CONFORMANCE.md"
        issue: "Measurements table (653 rows) reflects a pre-246f172/1ee6c37 app.css; its own superseded banner has not been widened to name the two newest commits, so it now understates its own staleness"
      - path: ".planning/ROADMAP.md"
        issue: "Phase 03.3.1.1 Plan 06 checkbox is still unchecked; phase-notes line reads 'Plans: 5/6 plans executed'"
      - path: ".planning/phases/03.3.1.1-record-pen-conformance-polish-close-the-untriaged-ui-gaps-fr/03.3.1.1-06-SUMMARY.md"
        issue: "Frontmatter status: complete contradicts body text ('Not ready to advance — halted at Task 2', 'Completed (Task 1 only; Task 2 pending)')"
    missing:
      - "Re-run the browser conformance pass (Plan 06 Task 2) against the current app.css/BatchRow.jsx and refresh the Measurements table, widening the superseded banner to name 246f172 and 1ee6c37 (or removing it once re-measured)"
      - "Write 03.3.1.1-UAT.md per D-23, scripted from the re-measured CONFORMANCE.md"
      - "Check off Plan 06 in ROADMAP.md and reconcile 03.3.1.1-06-SUMMARY.md's body with its status: complete frontmatter"
deferred: []
advisory: []
human_verification:
  - test: "Forced-colours (Windows high-contrast) rendering of picked stops/options/chips"
    expected: "A picked box keeps a fill in Highlight; the ring takes CanvasText, per the cross-cutting design rule"
    why_human: "CONFORMANCE.md still records this row as 'unverified' — the headless driver used for the pass cannot emulate forced-colors: active (RESEARCH A7). Unchanged across all three verification passes; no browser capable of this emulation has been used yet."
---

# Phase 03.3.1.1: Record pen conformance polish Verification Report

**Phase Goal:** Close the 18 (research counts 19) untriaged UI gaps Mark logged during 03.3.1's UAT (393px-width session) between the shipped record pen/batch row and the sketch 007 / structural contract, without reopening any already-settled decision (D-01–D-24) or the just-closed G-03.3.1-4 caption fix.
**Verified:** 2026-09-16T05:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — third pass, after commits `246f172` (sketch 009's settled decisions) and `1ee6c37` (SR-6, closed on-device)

## Goal Achievement

### Re-verification Summary

Both truths that failed in the second verification pass (2026-09-16T03:15:00Z) are now independently confirmed closed:

| Gap | Prior status | This pass | Evidence |
|-----|--------------|-----------|----------|
| SR-6 — WebKit empty-date-field collapse on the churn row | FAILED | ✓ Closed | `app/src/styles/app.css:203-206` now carries `input[type='date'].ink-field { -webkit-appearance: none; appearance: none; }`. The churn date's real markup (`app/src/ui/BatchRow.jsx:465-474`) renders exactly `type="date"` + `className="ink-field"` — the selector matches the real element, not a stand-in. `binder.test.js:176-189` pins both declarations and states in its own comment that the test engine cannot reproduce the defect it guards. Commit `1ee6c37`'s message records a same-origin-proxy device session (iPadOS 18.7, WebKit 605.1.15) establishing the cause and a post-fix re-measurement on the shipped app itself (not a probe page): `h=44 w=128 minH=44px box=border-box delta=0`, versus `35/142/29px/content-box/−9` before. Mark also confirmed the date picker still opens. |
| Melt test / Melt style caption-control alignment | FAILED | ✓ Closed | `app/src/styles/app.css:1728-1743` — `.axis-mark__head, .segmented-field__head` now share `align-items: flex-end` and `font-size: var(--type-label)` (the em-reserve trap the commit message describes and the code comment confirms). The touch block (`app.css:2261-2310`) replaces the retired 44px-both-heads rule with an overflowing `::after` hit area for Clear (`min-height: 0` + absolutely-positioned `::after`) and reserves `--caption-two-lines` on both heads instead of `--caption-line-h-touch`. `cross-cutting.test.js:141-144` explicitly asserts the retired token is gone from this rule. Commit `246f172`'s message and `.planning/sketches/009-wide-touch/README.md`'s M4 table give measured values matching the code: 1366 coarse both gaps 6/6, captions level, controls level; 1366 fine returns to −4.8, which sketch 007 itself draws. |

Both fixes are engine-agnostic CSS (flexbox, `font-size`, custom properties) rather than a native-control override like SR-6, so the recorded Chromium-based device measurements (`playwright-core`, DPR 2, pointer emulation) are trustworthy evidence for this class of fix — pointer-media-query matching and box arithmetic do not vary by rendering engine the way WebKit's native `<input type="date">` override does. The SR-6 fix specifically required and received an on-device (WebKit) re-measurement, per its own commit message, because that defect is a Chromium-blind, WebKit-only rendering behavior.

**A third, previously human-verification item is also resolved this pass, though it was not one of the two named gaps:** "Touch mode at a wide viewport — axis stop sizing" was routed to human verification in both prior passes because no sketch had drawn touch-at-≥1280px. Sketch 009 (`.planning/sketches/009-wide-touch/`) now exists, is marked `Status: SETTLED` by Mark (2026-09-15) for both decisions it carries, and both are implemented in `app.css` (commit `246f172`): Decision C (stop grows in height only — `.axis-mark__stop { height: var(--touch-stop-height); }` inside the `(pointer: coarse)` union block, with `width`/`--track-stop` deliberately confined to the width-only block below it, confirmed by reading `app.css:2312-2349`) and Decision M4 (above). A regression-guard test (`cross-cutting.test.js`, referenced in the commit message as "a guard that fails if a width or flex-basis ever reappears on the touch-union stop rule") is present — `cross-cutting.test.js:115` carries the comment "If a width or flex-basis ever appears here, that regression is [caught]". This closes the item; it is no longer listed under Human Verification below.

**One gap does not close, and is newly the sole blocker.** With both device-bound defects fixed, the remaining thing standing between this phase and `passed` is the conformance re-measurement mandated by D-23. This was treated as a documentation caveat in the second verification pass (Truth #6 stayed VERIFIED-with-caveat); this pass elevates it to a blocking gap because it is now the only thing left, and because the roadmap itself — not just the CONFORMANCE.md banner — confirms it is unresolved: **ROADMAP.md still shows Plan 06 unchecked ("Plans: 5/6 plans executed")**, and `03.3.1.1-06-SUMMARY.md`'s body text still says "Not ready to advance — halted at Task 2" directly beneath a `status: complete` frontmatter line. `03.3.1.1-UAT.md` does not exist. CONFORMANCE.md's own superseded banner (last widened by commit `93ba53b`, before `246f172`/`1ee6c37` existed) names 8 stale commits and is itself now short two more.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-15: below 1100px the page is one column, padding steps to 20px at 600px | ✓ VERIFIED | Unchanged since prior passes; Plan 01 shipped, CONFORMANCE.md I-01 rows pass |
| 2 | D-10: every record-pen caption is uppercase 12px/500/0.04em via text-transform | ✓ VERIFIED | Unchanged; confirmed present in app.css |
| 3 | D-04/05/09: picked stops/options/chips render solid pen-blue fill, no bold/underline/outline | ✓ VERIFIED | Unchanged; Plan 03 shipped, CONFORMANCE.md I-04/I-10/I-11/I-13 pass |
| 4 | D-04 supersession: each segmented control carries a caption line with a Clear text control | ✓ VERIFIED | Unchanged; confirmed via `AxisMark.jsx` and `app.css` |
| 5 | D-14: both save ceremonies read Add tasting \| Cancel \| Save batch in DOM order, right-aligned | ✓ VERIFIED | Unchanged; Plan 05 shipped |
| 6 | D-17: 03.3.1.1-CONFORMANCE.md exists with one row per contract element per width per applicable state | ✓ VERIFIED (caveat widened again) | File exists, 653 rows, 19/19 triaged items pass structurally. Its own superseded banner is now stale by 2 more commits than it names (`246f172`, `1ee6c37`) — see Truth #13 below, which is where this caveat now surfaces as a blocking gap rather than a footnote |
| 7 | JS breakpoints agree with the CSS breakpoints they must move in lockstep with (759.98px convention) | ✓ VERIFIED | Unchanged since second pass; `BatchRow.jsx` lines 106, 116, 119 all read `759.98px` |
| 8 | Every rendered className has a matching CSS rule (no unstyled sketch-conformance regression) | ✓ VERIFIED | Unchanged since second pass; `batch-row__date` (singular) confirmed throughout |
| 9 | SR-6 (WebKit empty-date-field collapse) is resolved in the real running app | ✓ VERIFIED | Was FAILED. Now closed — see Re-verification Summary above. `-webkit-appearance: none; appearance: none;` present at `app.css:203-206`, matches real markup, pinned by `binder.test.js`, 941/941 tests pass. On-device re-measurement is recorded in commit `1ee6c37`'s message (h=44/w=128/border-box/delta 0), which I cannot personally re-run without the physical iPad but accept as recorded audit evidence, consistent with how the earlier, now-superseded probe-only fix was correctly rejected for lacking exactly this kind of on-device record |
| 10 | Melt test / Melt style caption-control alignment matches the sketch | ✓ VERIFIED | Was FAILED. Now closed — see Re-verification Summary above. Shared `.axis-mark__head`/`.segmented-field__head` rule, `--caption-two-lines` reserve, `::after` hit area for Clear, and `font-size: var(--type-label)` em-fix all present and match the commit's description exactly; `cross-cutting.test.js` guards the retired-token removal |
| 11 | `.method-step__edit` avoids the column-flex stretch defect its sibling `.method-step__on-demand` was fixed for | ✓ VERIFIED | Unchanged since second pass; `app.css:1131-1135` still carries `align-self: flex-start` |
| 12 | Touch mode at a wide viewport (>=1280px, pointer:coarse) has a drawn, approved treatment | ✓ VERIFIED (newly closed) | Was UNCERTAIN/routed to human verification in both prior passes. Sketch 009 now exists, drawn and SETTLED by Mark; both its decisions (C, M4) are implemented in `app.css` and guarded by tests. See Re-verification Summary |
| 13 | D-23: 03.3.1.1-UAT.md is scripted from a current CONFORMANCE.md before the phase closes | ✗ FAILED (newly elevated) | `03.3.1.1-UAT.md` does not exist. `ROADMAP.md` shows Plan 06 unchecked, "5/6 plans executed". `03.3.1.1-06-SUMMARY.md` frontmatter (`status: complete`) contradicts its own body text. CONFORMANCE.md's superseded banner is stale by 2 more commits than it names. This was treated as a caveat under Truth #6 while other gaps remained open; now that those are closed, it is the sole blocker and is reported as a FAILED must-have rather than a footnote |

**Score:** 12/13 truths verified. Two previously-failed truths (SR-6, Melt row) closed this pass; one previously-uncertain truth (touch-at-wide-viewport) closed this pass; one truth (D-23/UAT.md) is newly elevated to FAILED, carrying forward what was previously recorded only as a documentation caveat.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/.../03.3.1.1-CONFORMANCE.md` | 19-item x 10-width x 3-state measurement record, current | ⚠️ STALE | 653 rows, 19/19 structural pass, but reflects pre-`246f172`/`1ee6c37` CSS; superseded banner does not name the two newest commits |
| `.planning/phases/.../03.3.1.1-REVIEW.md` | Code review gate output | ✓ VERIFIED | 2 critical findings (CR-01, CR-02), both confirmed fixed in prior passes, unchanged this pass |
| `app/src/ui/BatchRow.jsx` | Battery/read-view markup, churn-date field | ✓ VERIFIED | Churn date renders `type="date"` + `className="ink-field"`, matching the SR-6 CSS selector exactly |
| `app/src/styles/app.css` | Contract CSS | ✓ VERIFIED | Both remaining device-bound defects (SR-6, Melt row) now have matching, tested rules; no regressions found in the 941-test full run |
| `.planning/sketches/009-wide-touch/` | Settled design for touch-at-wide-viewport | ✓ VERIFIED (new) | `README.md` + `index.html` exist, `Status: SETTLED`, decisions C and M4 both implemented in `app.css` |
| `03.3.1.1-UAT.md` | End-of-phase UAT script (D-23) | ✗ DOES NOT EXIST | Still not written — correctly not written against stale numbers, but this is now the phase's only remaining blocker |

### Key Link Verification

Not applicable (CSS/markup conformance phase, no data-flow feature). The two links that matter for this pass — `input[type='date'].ink-field` selector to `BatchRow.jsx`'s real churn-date markup, and `.axis-mark__head`/`.segmented-field__head` shared rule to both `AxisMark.jsx`'s axis heads and the Melt row's segmented-field head — are both confirmed wired by direct reading, not just presence.

### Data-Flow Trace (Level 4)

Not applicable — no dynamic data rendering involved in this phase's scope.

### Behavioral Spot-Checks

`npm --prefix app test` run in full for this re-verification (justified: confirming two fix commits plus one feature commit did not regress the suite, single run): **941/941 tests passed, 33/33 test files** — matches the count expected for this verification pass.

`npm --prefix app run build` run: **clean, 68ms, no errors.**

No browser/device checks were performed directly by this verifier (no iPad access). SR-6 and the Melt-row/touch-viewport fixes are accepted on the strength of: (a) code that precisely matches the real markup and the commit messages' described mechanism, (b) tests that pin the exact declarations and guard against regression, (c) a full green test suite, and (d) for SR-6 specifically, an on-device re-measurement recorded in the commit's own message — the same standard the earlier, correctly-rejected probe-only attempt failed to meet.

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` executables exist for this phase.

### Requirements Coverage

No requirement IDs are mapped to this phase (every PLAN's frontmatter carries `requirements: []`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/phases/.../03.3.1.1-06-SUMMARY.md` | ~93, ~158, ~178 | Frontmatter says `status: complete` but body text still says "Not ready to advance — halted at Task 2" and "Completed (Task 1 only; Task 2 pending)" | ℹ️ Info → now load-bearing | Unchanged across all three passes. No longer purely cosmetic: it is direct documentary evidence, alongside ROADMAP.md's unchecked Plan 06, that the conformance pass was never formally re-closed after later fixes landed |
| `.planning/phases/.../03.3.1.1-CONFORMANCE.md` | 15-30 | Superseded banner names 8 commits; 2 more (`246f172`, `1ee6c37`) now also postdate the Measurements table and are unlisted | ℹ️ Info | Compounds the existing superseded state; both new commits change measurable values (stop height, axis-head font-size, touch field font-size) |
| `.planning/ROADMAP.md` | ~397 | Phase 03.3.1.1 Plan 06 checkbox unchecked, "Plans: 5/6 plans executed" | ℹ️ Info | Confirms the phase's own tracking has not been closed |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file changed since the second verification pass (`git diff bb05569..HEAD` across `app/src` and `.planning/sketches`).

### Human Verification Required

See frontmatter `human_verification`. One item remains (down from two — the wide-touch-viewport item is now resolved by decision + implementation, see above):

1. **Forced-colours rendering** — unchanged across all three passes, headless driver limitation, needs a real Chrome-with-forced-colors pass.

### Gaps Summary

Both truths that blocked the phase after the second verification pass are now genuinely closed, confirmed independently against the working tree rather than taken on SUMMARY.md's word:

1. **SR-6 — the WebKit empty-date-field collapse.** The fix (`appearance: none` on `input[type='date'].ink-field`) is present, matches the real churn-date markup exactly, is pinned by a test that documents its own blind spot, and was re-measured on the actual device against the shipped app (not a probe page) after the earlier probe-only attempt was correctly rejected for that exact gap.
2. **Melt row caption/control alignment.** The fix (a shared caption-line rule with a corrected em basis, plus an overflowing hit area so Clear's touch target no longer grows the line box) is present, matches the commit's own described mechanism, and is guarded by a test that pins the retired token's removal.

A third item — touch mode at a wide viewport — moved from "no sketch has drawn this" to "drawn, settled by Mark, and implemented" in the same window of work, closing a human-verification item that had stood open since the first verification pass.

**What remains is the conformance re-measurement, and it is now the only thing between this phase and `passed`.** D-23 requires `03.3.1.1-UAT.md` to be scripted from a current `03.3.1.1-CONFORMANCE.md`. That file's own Measurements table predates all eight fix commits it names in its superseded banner, plus the two most recent ones (`246f172`, `1ee6c37`) that the banner does not even name yet. `03.3.1.1-UAT.md` does not exist. This is not a documentation nicety this pass is choosing to flag pedantically — `ROADMAP.md` itself still carries Plan 06 as unchecked and "5/6 plans executed," and `03.3.1.1-06-SUMMARY.md`'s own body text says the plan halted at Task 2, directly under a frontmatter line claiming it completed. Three independent sources (the roadmap, the SUMMARY body, and the CONFORMANCE banner's own math) agree this step was never finished.

Closing it requires exactly what Plan 06 Task 2 already specifies: a browser-driven conformance pass (`gsd-browser` + `claude-in-chrome`, per the plan) at the ten D-16 widths and three states against the current app, refreshing the Measurements table, then scripting `03.3.1.1-UAT.md` from it. This is orchestrator/browser-tool work this verifier cannot perform, but it is not device-bound in the way SR-6 and the Melt row were — it does not require Mark's iPad, only a browser automation pass against the now-stable CSS.

Given one must-have truth remains FAILED (D-23 / conformance re-measurement), this phase's status is **`gaps_found`**, not `passed`. Do not inflate this: two real, previously device-blocked defects were closed cleanly and are confirmed genuinely fixed in the working tree — that is real, verified progress — but the phase's own declared acceptance gate (D-23's UAT.md, scripted from a current conformance record) has not been met, and the roadmap agrees.

---

_Verified: 2026-09-16T05:00:00Z_
_Verifier: Claude (gsd-verifier)_
