---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
plan: 05
type: execute
wave: 5
depends_on: ["03.4-04"]
files_modified:
  - app/src/styles/tokens.css
  - app/src/styles/tokens.test.js
  - app/src/styles/home.test.js
files_deleted:
  - app/src/ui/recipe-colour.js
  - app/src/ui/recipe-colour.test.js
autonomous: true
requirements: [D-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Sprinkles Jar is gone from the code: the six jar hues, the twelve recipe hues, recipe-colour.js, its test and the row's per-recipe custom property no longer exist (D-04)."
    - "Nothing in the tracked source reads a retired token, and tokens.test.js's retiring list is empty — so the prefix-discipline test now proves every declared token belongs to a named world or the shared middle."
    - "Every suite is green and the bundle builds with the removal in place."
  artifacts:
    - app/src/styles/tokens.css
    - app/src/styles/tokens.test.js
  key_links:
    - "tokens.test.js's emptied retiring list -> the prefix-discipline test, which from now on fails on any context-free new token"
  prohibitions:
    - "Nothing is removed that a rendered rule or a live import still reads — the removal follows the rewrite, it does not lead it."
    - "No unrelated dead code is deleted in this plan; the retirement is exactly what D-04 names."
---

<objective>
Retire the Sprinkles Jar as the phase's last step: the jar hues, the twelve recipe hues, the colour-dealing module and its test, and the per-recipe custom property (D-04).

Purpose: colour now says where a thing lives, never which recipe it is. Leaving the retired palette in the file would let a future rule pick it up by accident, and D-04 says the removal comes after every other change has landed.
Output: a tokens.css with two named worlds and nothing else, and a prefix-discipline test with no exemptions left.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-CONTEXT.md
@.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The colour-dealing module goes, with its one import proved absent first</name>
  <files>app/src/ui/recipe-colour.js, app/src/ui/recipe-colour.test.js, app/src/ui/RecipeList.jsx</files>
  <read_first>
    - app/src/ui/recipe-colour.js (the whole file — `recipeHueByRecipeId`, `RECIPE_HUE_COUNT`, and what else it exports)
    - app/src/ui/RecipeList.jsx and app/src/ui/RecipeList.test.jsx as rewritten in plan 04 (confirm neither imports the module any more)
  </read_first>
  <action>
Prove first, delete second. Search the tracked source for any import of the colour-dealing module and for any reader of the per-recipe custom property the rows used to set inline; if either still exists, stop and report it rather than deleting — plan 04 was meant to remove the last one, and a survivor means the rewrite missed a spot.

With the search clean, delete `app/src/ui/recipe-colour.js` and `app/src/ui/recipe-colour.test.js`. Remove any comment left elsewhere that explains the retired inline custom property, since the thing it explains is gone.
  </action>
  <acceptance_criteria>
    - `git ls-files app/src/ui/recipe-colour.js app/src/ui/recipe-colour.test.js` prints nothing after the change is staged.
    - `grep -rn "recipe-colour\|recipeHueByRecipeId\|RECIPE_HUE_COUNT" app/src` prints nothing.
    - `npm --prefix app test` passes, with two fewer suites than before and no suite reporting a missing module.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit, "failed" in the Vitest summary line, or any suite reporting a failed import resolution</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit, or the build output contains "error"</fails_when>
  </verify>
  <done>The module that dealt a colour per recipe, and its test, are gone, and nothing imported them.</done>
</task>

<task type="auto">
  <name>Task 2: The hues go, and the discipline test loses its last exemption</name>
  <files>app/src/styles/tokens.css, app/src/styles/tokens.test.js, app/src/styles/home.test.js</files>
  <read_first>
    - app/src/styles/tokens.css, the Sprinkles Jar block at the foot of the file (the banner comment, the six jar hues, the twelve recipe hues and the closing note)
    - app/src/styles/tokens.test.js (the retiring list added in plan 01 and the prefix-discipline test that reads it)
    - app/src/styles/home.test.js (any assertion still naming a retired token)
  </read_first>
  <action>
Delete the Sprinkles Jar block from tokens.css: the banner comment, the six jar hues, the twelve recipe hues and the closing note about the row's last words. Leave the App block that replaced it, with its own comment citing DESIGN.md.

Empty the retiring list in tokens.test.js to an empty array and leave a one-line comment saying the Jar was retired in this phase under D-04, so the prefix-discipline test now admits only a `--sheet-` name, an `--app-` name or a member of the shared list. Remove any assertion in home.test.js that still names a retired token.

Then record what this phase hands to Impeccable rather than to code, as a single pending todo: DESIGN.md's Hand entry can leave *pending* once the two-mode fallback has been seen (D-17), the five text companions and the filled action wait on Mark seeing them in the real app (D-19), and route.md still describes the shared action colour as open while DESIGN.md governs. Each of these is an `/impeccable document` job; no GSD plan edits DESIGN.md.
  </action>
  <acceptance_criteria>
    - `grep -c "jar-\|recipe-hue" app/src/styles/tokens.css` returns 0.
    - `grep -rn "jar-pink\|jar-gold\|jar-teal\|jar-green\|jar-orange\|jar-violet\|recipe-hue" app/src` prints nothing.
    - tokens.test.js's retiring list is an empty array literal.
    - A new file exists under `.planning/todos/pending/` naming the three Impeccable follow-ups.
    - `npm --prefix app test` passes in full, the prefix-discipline test included.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test</automated>
    <fails_when>non-zero exit, "failed" in the Vitest summary line, or the prefix-discipline test naming a token that matches neither prefix nor the shared list</fails_when>
    <automated>npm --prefix app run build</automated>
    <fails_when>non-zero exit, or the build output contains "error"</fails_when>
    <human-check>End-of-phase UAT: nothing on any surface lost its colour — the Sheet unchanged, the App reading its own palette. Recorded as a UAT window.</human-check>
  </verify>
  <done>tokens.css declares two named worlds and one shared middle, with no retired palette left, and the discipline test has no exemptions to hide behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> browser bundle | Removal only; no input, storage or network surface is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.4-16 | Denial of Service (usability) | a rule left reading a deleted token would render no colour at all, silently | medium | mitigate | Task 1 searches for every consumer before deleting anything, and tokens.test.js's unresolved-reference test fails the build if a read survives its declaration |
| T-03.4-17 | Tampering | over-deletion beyond what D-04 names | low | mitigate | the deletion list is exactly D-04's: six jar hues, twelve recipe hues, one module, one test, one inline custom property; pre-existing dead code elsewhere is reported, not removed |
</threat_model>

<verification>
- `npm --prefix app test` and `npm --prefix app run build` both green after the removal.
- `grep -rn "jar-\|recipe-hue\|--c:" app/src` returns nothing.
- Deferred to end-of-phase UAT: a look at the Sheet and the App confirming nothing lost a colour it should have kept.
</verification>

<success_criteria>
- The jar hues, the recipe hues, `recipe-colour.js`, its test and the per-recipe custom property are gone (D-04).
- Nothing reads a retired token; the unresolved-reference test proves it.
- tokens.test.js's retiring list is empty, so the prefix-discipline test now binds without exception.
- The phase's remaining design-document work is recorded as an Impeccable follow-up rather than left implicit.
</success_criteria>

## Artifacts this plan produces

**Deleted**
- `app/src/ui/recipe-colour.js`, `app/src/ui/recipe-colour.test.js`
- tokens: `--jar-pink`, `--jar-gold`, `--jar-teal`, `--jar-green`, `--jar-orange`, `--jar-violet`, `--recipe-hue-01` through `--recipe-hue-12`
- the row's per-recipe inline custom property

**New**
- one pending todo naming the `/impeccable document` follow-ups (D-17's Hand entry, D-19's companions, route.md's action colour)

<output>
Create `.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-SUMMARY.md` when done
</output>
