---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
plan: 05
subsystem: styles
tags: [css-custom-properties, design-tokens, tokens.css, vitest, cleanup]

# Dependency graph
requires:
  - phase: 03.4-01
    provides: "tokens.css split into --sheet-/--app- worlds, the App palette, tokens.test.js's unresolved-var/dead-token/prefix-discipline gates with the RETIRING exemption and the --c allowlist entry this plan closes"
  - phase: 03.4-04
    provides: "RecipeList.jsx rewritten over the App marks grammar with no import of recipe-colour.js and home.css with no read of --c/--jar-*/--recipe-hue-*, leaving the retired system provably unreferenced and safe to delete"
provides:
  - "The Sprinkles Jar retired from the code entirely: recipe-colour.js and its test deleted, the six jar hues and twelve recipe hues deleted from tokens.css, the row's --c custom property's last documented trace (tokens.test.js's allowlist entry) removed"
  - "tokens.test.js's RETIRING array emptied to [] and its LOCALLY_SET_CUSTOM_PROPERTIES allowlist emptied to an empty Set — the prefix-discipline gate now binds with zero exemptions, so a future context-free token declaration fails the build immediately"
  - "A pending todo naming the phase's three remaining /impeccable document follow-ups (DESIGN.md's Hand entry, the five text companions and the filled action, route.md's stale action-colour note)"
affects: []

# Actuals (#2632)
actuals:
  tokens: 5619
  tasks: 2
  commits: 2
plan_head_before: b430d381628b1781d02373c5e53932a5e29033e9

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Prove-then-delete discipline for a retirement task: grep the tracked source for every named consumer of the module/property before deleting anything, and stop rather than delete if a survivor turns up — the same discipline the plan's own threat register (T-03.4-16) names as the mitigation for a silent, colourless render."

key-files:
  created: []
  modified:
    - app/src/styles/tokens.css
    - app/src/styles/tokens.test.js
    - app/src/styles/home.test.js
  deleted:
    - app/src/ui/recipe-colour.js
    - app/src/ui/recipe-colour.test.js

key-decisions:
  - "Task 2's banner comment ('The Sprinkles Jar — ...') was deleted outright rather than reworded into a new section header, matching the plan's literal 'delete the banner comment' instruction; the paper/App boundary knowledge it carried is not lost, since home.css's own header comment (lines 1-8) already documents the identical route.md § 1 boundary independently."
  - "--app-size-meta's own inline comment ('...the last words') was trimmed alongside the standalone closing note the plan names, since both described the same retired per-row quotation (D-20) and leaving one half fixed while the other kept naming the removed feature would be an inconsistent cleanup of the same fact."
  - "tokens.test.js's LOCALLY_SET_CUSTOM_PROPERTIES allowlist was emptied to an empty Set (not deleted) alongside the RETIRING array — D-04 names 'the row's --c custom property' as retired by this plan, and the allowlist's own comment explained it existed only for --c; kept as an empty Set with an explanatory comment rather than removed outright, so a future inline custom property has a documented place to be excepted rather than silently widening the unresolved-token gate."
  - "home.test.js's 'no rule reads a jar hue or a recipe hue (D-04 in progress...)' test was deleted rather than left passing vacuously — its title's own 'in progress' language and its assertions naming --jar-/--recipe-hue- directly matched the plan's instruction to remove any assertion still naming a retired token, and the tokens it named no longer exist anywhere in tokens.css to declare."

requirements-completed: [D-04]

coverage:
  - id: D1
    description: "The Sprinkles Jar is gone from the code: the six jar hues, the twelve recipe hues, recipe-colour.js, its test and the row's per-recipe custom property no longer exist (D-04)"
    requirement: D-04
    verification:
      - kind: unit
        ref: "git ls-files app/src/ui/recipe-colour.js app/src/ui/recipe-colour.test.js (empty)"
        status: pass
      - kind: unit
        ref: "grep -c \"jar-\\|recipe-hue\" app/src/styles/tokens.css (0)"
        status: pass
      - kind: unit
        ref: "grep -rn \"jar-pink\\|jar-gold\\|jar-teal\\|jar-green\\|jar-orange\\|jar-violet\\|recipe-hue\" app/src (empty)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Nothing in the tracked source reads a retired token, and tokens.test.js's retiring list is empty — the prefix-discipline test now proves every declared token belongs to a named world or the shared middle"
    requirement: D-04
    verification:
      - kind: unit
        ref: "src/styles/tokens.test.js#the prefix-discipline gate (RETIRING = [])"
        status: pass
      - kind: unit
        ref: "src/styles/tokens.test.js#the unresolved-token gate (LOCALLY_SET_CUSTOM_PROPERTIES = new Set())"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every suite is green and the bundle builds with the removal in place"
    verification:
      - kind: unit
        ref: "npm --prefix app test -- --run (1093 passed, 0 failed — 1101 baseline minus 8 tests in the two files this plan retires: 7 in recipe-colour.test.js, 1 in home.test.js's stale D-04 test)"
        status: pass
      - kind: other
        ref: "npm --prefix app run build (succeeds, 126 modules transformed)"
        status: pass
    human_judgment: false
  - id: D4
    description: "The phase's remaining design-document work is recorded as an Impeccable follow-up rather than left implicit"
    verification:
      - kind: other
        ref: "test -f .planning/todos/pending/2026-09-22-three-impeccable-document-jobs-this-phase-hands-off.md"
        status: pass
    human_judgment: false
  - id: D5
    description: "Nothing on any surface lost its colour — the Sheet unchanged, the App reading its own palette"
    verification: []
    human_judgment: true
    rationale: "The plan's own <verify> block names this a human-check deferred to end-of-phase UAT, per workflow.human_verify_mode: end-of-phase — a text-only removal (no rendered rule changed value, only retired declarations and their sole consumers were deleted) is not that verdict."

# Metrics
duration: ~15min
completed: 2026-09-22
status: complete
---

# Phase 03.4 Plan 5: The Sprinkles Jar retired — tokens.css to two named worlds, no exemptions left Summary

**Deleted `recipe-colour.js`/its test and the eighteen jar/recipe-hue tokens from `tokens.css` after proving no live consumer read them, then emptied `tokens.test.js`'s RETIRING array and `--c` allowlist so the prefix-discipline gate binds with zero exemptions.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-09-21T20:53:00-04:00 (approx.)
- **Completed:** 2026-09-21T21:08:08-04:00 (Task 2 commit)
- **Tasks:** 2 completed
- **Files modified:** 5 (2 deleted, 3 modified), 1 new pending todo

## Accomplishments

- Proved first, deleted second: grepped the tracked source for any import of `recipe-colour.js` (`recipeHueByRecipeId`, `RECIPE_HUE_COUNT`) and any reader of the inline `--c` custom property before touching anything — both were already absent, confirming plan 04's row rewrite left no survivor. Deleted `app/src/ui/recipe-colour.js` and `app/src/ui/recipe-colour.test.js`.
- Deleted the Sprinkles Jar block from `tokens.css`: the banner comment, the six `--jar-*` hues, the twelve `--recipe-hue-*` tokens, and the closing note about the row's last words (plus the same stale phrase trimmed from `--app-size-meta`'s own inline comment). The App palette block that replaced it keeps its own comment citing DESIGN.md, untouched.
- `tokens.test.js`'s `RETIRING` array emptied to `[]` with a one-line D-04 comment — the prefix-discipline test now admits only a `--sheet-` name, an `--app-` name, or a member of the shared list, with no exemption left to hide a future context-free token behind. Its `LOCALLY_SET_CUSTOM_PROPERTIES` allowlist (which existed solely for `--c`) emptied to a documented empty `Set`, and the dead-token soft list's eighteen jar/recipe-hue entries removed (they are no longer declared anywhere, so no longer eligible to be "unread").
- `home.test.js`'s stale `"no rule reads a jar hue or a recipe hue (D-04 in progress...)"` test deleted — the tokens it named no longer exist anywhere to declare.
- Recorded the phase's three remaining `/impeccable document` follow-ups (DESIGN.md's Hand entry per D-17, the five text companions and the filled action per D-19, and `route.md`'s stale open note on the shared action colour) as one pending todo — GSD plans do not edit DESIGN.md.
- Full suite: 1093 passed (1101 baseline minus 8: 7 tests in the deleted `recipe-colour.test.js`, 1 stale test deleted from `home.test.js`), 0 failed. Build succeeds, 126 modules transformed.

## Task Commits

Each task was committed atomically:

1. **Task 1: The colour-dealing module goes, with its one import proved absent first** — `774a9dc` (feat)
2. **Task 2: The hues go, and the discipline test loses its last exemption** — `201ac30` (feat)

**Plan metadata:** committed alongside this SUMMARY.

## Files Created/Modified

- `app/src/ui/recipe-colour.js` — deleted: the colour-dealing module (`recipeHueByRecipeId`, `RECIPE_HUE_COUNT`).
- `app/src/ui/recipe-colour.test.js` — deleted: its seven-test suite.
- `app/src/styles/tokens.css` — the Sprinkles Jar banner, the six jar hues, the twelve recipe hues, and the row's-last-words closing note all deleted; `--app-size-meta`'s inline comment trimmed to match.
- `app/src/styles/tokens.test.js` — `RETIRING` emptied to `[]`; `LOCALLY_SET_CUSTOM_PROPERTIES` emptied to a documented empty `Set`; the dead-token soft list's eighteen jar/recipe-hue entries removed and its explanatory comment rewritten.
- `app/src/styles/home.test.js` — the stale "no rule reads a jar hue or a recipe hue (D-04 in progress...)" test deleted.
- `.planning/todos/pending/2026-09-22-three-impeccable-document-jobs-this-phase-hands-off.md` — new: the three `/impeccable document` follow-ups this phase hands off.

## Decisions Made

See `key-decisions` in the frontmatter above: the banner comment deleted outright (not reworded) since the paper/App boundary it explained is independently documented in `home.css`'s own header; `--app-size-meta`'s stale "last words" phrase trimmed alongside the standalone closing note for the same underlying fact; the `--c` allowlist emptied rather than deleted, keeping a documented place for a future exception; `home.test.js`'s stale D-04-in-progress test deleted rather than left passing vacuously.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] tokens.test.js's own explanatory comment inflated the plan's literal `<verification>` grep count**
- **Found during:** Task 2, plan-level `<verification>` check after both tasks committed
- **Issue:** The plan's own verification is `grep -rn "jar-\|recipe-hue\|--c:" app/src` returns nothing; the comment I wrote above the now-empty `RETIRING` array used the literal substrings `--jar-*` and `--recipe-hue-*` in prose to explain what was removed, tripping the same grep it was meant to satisfy
- **Fix:** Reworded the comment to "The Jar's six hues and the twelve per-recipe hues" — same meaning, no literal token-prefix substring
- **Files modified:** `app/src/styles/tokens.test.js`
- **Verification:** `grep -rn "jar-\|recipe-hue\|--c:" app/src` returns nothing
- **Committed in:** `201ac30` (Task 2 commit, fixed before commit — not a separate follow-up commit)

---

**Total deviations:** 1 auto-fixed (a self-inflicted grep-gate trip in the executor's own prose, the same pattern documented in this phase's plans 01/03/04).
**Impact on plan:** No behaviour changed; the fix only reworded a comment so the plan's own literal verification command passes as written.

### Documented Acceptance-Criterion Timing Note (not a discrepancy)

Task 1's acceptance criterion `grep -rn "recipe-colour\|recipeHueByRecipeId\|RECIPE_HUE_COUNT" app/src` prints nothing was **not yet true immediately after Task 1's own commit** — `tokens.css` (lines 408-414, pre-Task-2) and `tokens.test.js` (a comment) still named `recipe-colour.js`/`RECIPE_HUE_COUNT` in prose, since those files are Task 2's scope, not Task 1's (`files: app/src/ui/recipe-colour.js, app/src/ui/recipe-colour.test.js, app/src/ui/RecipeList.jsx`). By the end of Task 2, `tokens.css`'s twelve-hue block (and its comment) is deleted and `tokens.test.js`'s comment was rewritten as part of the deviation above, so the grep prints nothing at plan completion, satisfying the criterion in full. No code or test needed changing to close this — it resolved naturally once Task 2's own deletions landed.

## Issues Encountered

None beyond the deviation documented above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `tokens.css` now declares exactly two named worlds (`--sheet-`, `--app-`) plus the shared middle and the hand's four D-17-named tokens — nothing else. `tokens.test.js`'s prefix-discipline gate has no exemptions left, so any future token declared without one of those three shapes fails the build immediately.
- This is the last plan in Phase 03.4 (`depends_on: ["03.4-04"]`, no plan depends on 03.4-05). Phase 03.4's own remaining verification is entirely the end-of-phase UAT window the plan's `<verification>` section names: a look at the Sheet and the App confirming nothing lost a colour it should have kept, alongside the UAT windows plans 01-04 already deferred (the hand's rendering and forced-colors/print fallback, the lead block and row grammar at 1280px/393px, keyboard operation with visible focus, the App palette's exact hex values against DESIGN.md).
- The pending todo (`.planning/todos/pending/2026-09-22-three-impeccable-document-jobs-this-phase-hands-off.md`) names the three `/impeccable document` jobs this phase hands off once that UAT is complete — not GSD scope, and not blocking phase completion.
- No blockers for phase completion.

## Self-Check: PASSED

- `app/src/styles/tokens.css`, `app/src/styles/tokens.test.js`, `app/src/styles/home.test.js`: FOUND, all modified as described
- `app/src/ui/recipe-colour.js`, `app/src/ui/recipe-colour.test.js`: MISSING (deleted, as intended) — `git ls-files` confirms neither is tracked
- `.planning/todos/pending/2026-09-22-three-impeccable-document-jobs-this-phase-hands-off.md`: FOUND
- Commits `774a9dc`, `201ac30` both present in `git log --oneline`
- `npm --prefix app test -- --run`: 1093 passed, 0 failed
- `npm --prefix app run build`: succeeded, no errors
- `grep -c "jar-\|recipe-hue" app/src/styles/tokens.css` (0); `grep -rn "jar-pink\|jar-gold\|jar-teal\|jar-green\|jar-orange\|jar-violet\|recipe-hue" app/src` (empty); `grep -rn "recipe-colour\|recipeHueByRecipeId\|RECIPE_HUE_COUNT" app/src` (empty); `grep -rn "jar-\|recipe-hue\|--c:" app/src` (empty)

---
*Phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t*
*Plan: 05*
*Completed: 2026-09-22*
