---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
plan: 08
subsystem: docs
tags: [conformance, gap-closure, css-custom-properties, home, shell]

# Dependency graph
requires:
  - phase: 03.4-06
    provides: "The App ground, the zero-specificity visited rule, the typed placeholder, and the phone-fit tab row — half of this record's rows read these rules"
  - phase: 03.4-07
    provides: "The row's column grammar and the board's lead block — the other half of this record's rows read these rules"
provides:
  - "03.4-GAPS-CONFORMANCE.md: a 38-row side-by-side record, board line versus shipped rule/class/token, at 1280 (board 170) and 393 (board 171)"
  - "03.4-gaps-conformance-probe.js: a re-runnable plain-browser-JS reader and per-width selector lists, ready to fill the record's judgments with real measurements at UAT"
  - "All six of VERIFICATION.md's gaps traced to conformance rows, a plan, a task and a test pin"
  - "The four already-recorded board departures (12px tab label, rod/tally kept at phone width, absent page-preview glyph, no versions-count cell) each with their authority named, open for Mark at UAT"
  - "Three new, previously-unflagged findings (the rail's missing padding/border-right, the active rail item's missing pill treatment, RowActions always showing two actions where board 171 shows one) recorded as open rows, not resolved by this plan"
affects: []

# Actuals (#2632)
actuals:
  tokens: 8703
  tasks: 2
  commits: 2
plan_head_before: 85799f30859dc161d25124661184c68695596e78

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A conformance record judged from source when no browser tool is available: every Verdict cell reached by comparing the shipped CSS rule/class/token to the board's own hand-copied markup, with the pass's own preamble stating once, never per-cell, that no rendered pixel informed the judgment — so a later browser-driven pass can correct a cell without the record ever having claimed more than it measured."
    - "A verdict vocabulary of exactly four words (matches/differs/departure/unmeasured), with 'departure' reserved for cross-referencing a named, already-decided departure in its own section — keeping a genuinely new finding (a 'differs') distinct from a decision already made and awaiting only Mark's confirmation."

key-files:
  created:
    - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-GAPS-CONFORMANCE.md
    - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js
  modified:
    - .planning/WINDOWS.md

key-decisions:
  - "The rail's six places (Home plus the five destinations) each get their own row, not the plan's literal 'five places' phrasing — Home is itself part of the rail's own markup on both boards, and a missing element being a visible empty cell (the plan's own stated goal) argues for the fuller list over the narrower one."
  - "The 62px/56px/44px rail-height numbers (03.4-07's own decision #5, 'sizes read tokens, board governs structure') are marked `differs` in the table, not `departure` — the departures section is reserved for the four items this plan's own action text names explicitly, so a verdict of `departure` always cross-references one of those four rather than any other already-decided divergence."
  - "Three genuinely new findings surfaced by this side-by-side pass (the rail's missing padding/border-right, the active item's missing pill, RowActions always showing two actions at 393px) are recorded as open rows in the Verdict section and in .planning/WINDOWS.md, not folded into the four named departures and not resolved by this plan — matching the plan's own prohibition against this plan resolving anything it finds."

requirements-completed: [D-06, D-07, D-09, D-10, D-11, D-16, D-18, D-19, UX1-01, UX1-03]

coverage:
  - id: D1
    description: "A side-by-side conformance record exists naming, for each width and element, what the board draws and what the app renders"
    requirement: D-09
    verification:
      - kind: other
        ref: 'grep -c "| # | Width | Element | Board says | App shows | Verdict |" 03.4-GAPS-CONFORMANCE.md (1)'
        status: pass
      - kind: other
        ref: 'grep -c "^| [0-9]" 03.4-GAPS-CONFORMANCE.md (38, >= 24 required)'
        status: pass
    human_judgment: false
  - id: D2
    description: "Every element of board 170 at 1280 and board 171 at 393 has a row — header, tools row, rail and its places, lead block children in order, recipe row children in order, and the bottom nav's five items"
    verification:
      - kind: other
        ref: "manual inspection of the 38-row table against both board files' own markup, line by line"
        status: pass
    human_judgment: false
  - id: D3
    description: "Each of the six verification gaps is cited by at least one row, with the plan and the pin that closes it"
    requirement: D-16
    verification:
      - kind: other
        ref: "the gap-to-evidence table (03.4-GAPS-CONFORMANCE.md): six rows, G1-G6, each naming conformance rows, a plan/task and a test pin"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every knowing departure from a board is its own row with its authority named — the 12px tab label, the rod/tally kept at phone width, the absent page-preview glyph, and the dropped versions count"
    verification:
      - kind: other
        ref: "the departures section (03.4-GAPS-CONFORMANCE.md): four rows, each citing the 03.4-06/03.4-07 key-decision that authorized it"
        status: pass
    human_judgment: false
  - id: D5
    description: "A re-runnable probe script sits beside the record, syntactically valid, so the measured column can be filled at UAT"
    verification:
      - kind: other
        ref: "node --check 03.4-gaps-conformance-probe.js (exit 0); grep -c '^import \\|^export ' (0)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Rendered side by side against both boards at 1280 and 393, in the browser that matters: the record's judged verdicts hold up against real measurements, and the four departures plus the three new findings read as intended rather than as defects"
    verification: []
    human_judgment: true
    rationale: "This run had no browser tool; every verdict above was judged from source, never from a rendered pixel. The probe this plan ships is what a browser-equipped pass runs at UAT to confirm or correct each cell, per this project's own working note that a source-level read has previously missed device-specific defects (WebKit, coarse pointer) a rendered pass would catch."

# Metrics
duration: ~25min
completed: 2026-09-22
status: complete
---

# Phase 03.4 Plan 08: The gaps conformance record — a 38-row side-by-side check against both boards Summary

**Wrote the side-by-side record VERIFICATION.md's own "Added 2026-09-22" lesson demanded — 38 rows reading board 170 (1280px) and board 171 (393px) line by line against the shipped shell.css/home.css rules, tracing all six verification gaps to their closing plan/task/pin, naming the four already-decided board departures for Mark to rule on, and surfacing three new, previously-unflagged findings as open rows rather than resolving them.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-09-22 (session start, following 03.4-07)
- **Completed:** 2026-09-22T03:12Z
- **Tasks:** 2 completed
- **Files modified:** 3 (2 created, 1 modified — no file under `app/` touched)

## Accomplishments

- `03.4-gaps-conformance-probe.js` ships a plain-browser-JS `probe(sel)`/`readWidth(selectors)` reader, following 03.3.1.1's own precedent, plus `SELECTORS_1280` and `SELECTORS_393` constants naming every element this record's table cites — ready to paste into the running app at UAT to fill or correct any cell with a real measurement.
- `03.4-GAPS-CONFORMANCE.md`'s conformance table carries 38 numbered rows: 21 at 1280 (header, tools row, rail, the rail's six places, the lead block and its five children, the first recipe row and its five children) and 17 at 393 (header, the lead block and its three visible children, the first row and its four children, the bottom nav and its five items). Every `Board says` cell cites the board's own markup by line number; every `App shows` cell names the shipped rule, class or token; every `Verdict` reads one of exactly four words (matches/differs/departure/unmeasured).
- All six of VERIFICATION.md's gaps (the tab row, the placeholder page, the App ground, the visited rule, the lead block, the row's columns) trace to a gap-to-evidence table row naming the conformance rows that cover them, the plan and task that closed them, and the test pin that holds each one closed.
- The four board departures already decided by plans 06 and 07 (the 12px tab label against the board's hand-copied 10px, the rod and tally kept at phone width where board 171 draws neither, the unbuilt page-preview glyph, and the row shipping without a versions-count cell) each get their own departures-section row, citing the exact key-decision that authorized them, marked open for Mark at UAT.
- The pass surfaced three genuinely new findings no prior gap or decision named: `.shell__rail` carries no padding or `border-right` rule at all (board 170 draws both); the rail's active item has no `border-radius` pill or bold weight (only its background swaps); and `RowActions` always renders both the filled and the secondary action at every width, where board 171's own row shows only the filled one at 393px. All three are recorded as open rows in the Verdict section and appended to `.planning/WINDOWS.md` — not resolved by this plan, per its own prohibition against making a new decision.
- No file under `app/` was touched — confirmed by `git status --porcelain app/` returning empty both before and after Task 2, and by the full test suite (1119 passed) and production build (126 modules) matching plan 07's own final numbers exactly.

## Task Commits

Each task was committed atomically:

1. **Task 1: The probe and the record's skeleton, with every board cell filled from the board's own markup** — `ce6e894` (feat, tracer)
2. **Task 2: Fill the app's column, map every gap to its evidence, and record the departures** — `b1f4e28` (feat)

**Plan metadata:** committed alongside this SUMMARY.

**Tracer feedback gate (Task 1 → Task 2):** Task 1 is `type="tracer"`. Its `<verify>` block carries only `<automated>` commands (no `<human-check>`), and `workflow.human_verify_mode` is `end-of-phase` (the project default) — per the tracer feedback gate's precedence chain (checkpoints.md), both automated verify commands (`node --check` on the probe, and the table-header grep) were re-run after Task 1's commit and passed, so execution continued straight to Task 2's expansion with no checkpoint synthesized.

## Files Created/Modified

- `.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js` — a re-runnable plain-browser-JS conformance reader and the per-width selector lists.
- `.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-GAPS-CONFORMANCE.md` — the 38-row side-by-side record, the six-row gap-to-evidence table, the four-row departures section, and the verdict.
- `.planning/WINDOWS.md` — four new open entries: the four departures as one deviation entry, plus the three newly-surfaced findings, each citing its record row.

## Decisions Made

See `key-decisions` in the frontmatter above: including all six rail places (not the plan text's literal "five") since Home is drawn on both boards and omitting it would leave a visible element with no row; keeping the token-governed rail-height numbers (03.4-07 decision #5) as `differs` rather than `departure`, since `departure` is reserved for the four items this plan's own action text names; and recording the three newly-surfaced findings as open rows rather than resolving them, per this plan's own prohibition.

## Deviations from Plan

None — plan executed exactly as written. The three newly-surfaced findings and the "six rail places, not five" reading are within this plan's own stated scope (a side-by-side record that surfaces every difference it finds, resolving none of them) rather than deviations from it.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- This is the final plan of Phase 03.4. All six of VERIFICATION.md's gaps now trace to a plan, a task, a test pin, and a conformance row; the four already-decided departures and three new findings are recorded for Mark's end-of-phase UAT pass, per `workflow.human_verify_mode: end-of-phase`.
- `03.4-gaps-conformance-probe.js` is ready to run at UAT: paste it into the running app at 1280px and at 393px, call `readWidth(SELECTORS_1280)` / `readWidth(SELECTORS_393)`, and use the returned numbers to confirm or correct this record's judged verdicts — particularly the one `unmeasured` row (the hand's exact rendered size at 393px) and any row this record marked `differs` from a token-governed value.
- No blockers for milestone completion; the phase's own UAT is the last remaining step.

## Self-Check: PASSED

- `.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js`, `03.4-GAPS-CONFORMANCE.md`: FOUND, both created as described
- Commits `ce6e894`, `b1f4e28` both present in `git log --oneline`
- `node --check 03.4-gaps-conformance-probe.js`: exit 0; `grep -c "^import \|^export "`: 0
- `grep -c "| # | Width | Element | Board says | App shows | Verdict |" 03.4-GAPS-CONFORMANCE.md`: 1
- `grep -c "^| [0-9]" 03.4-GAPS-CONFORMANCE.md`: 38 (>= 24 required)
- `grep -c "line " 03.4-GAPS-CONFORMANCE.md`: 38 (>= 24 required)
- `grep -c "not measured" 03.4-GAPS-CONFORMANCE.md`: 1 (preamble only, never both claims)
- No empty `App shows` cell; every `Verdict` cell one of matches/differs/departure/unmeasured (checked by awk over all 38 rows)
- Gap-to-evidence table: 6 rows, one per VERIFICATION.md gap
- Departures section: 4 rows, each open for Mark
- `git status --porcelain app/`: empty
- `npm --prefix app test`: 1119 passed, 0 failed
- `npm --prefix app run build`: succeeded, 126 modules transformed

---
*Phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t*
*Plan: 08*
*Completed: 2026-09-22*
