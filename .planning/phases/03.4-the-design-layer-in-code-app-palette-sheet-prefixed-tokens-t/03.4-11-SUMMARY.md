---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
plan: 11
subsystem: ui
tags: [react, react-router, details-disclosure, focus-management, shell]

# Dependency graph
requires:
  - phase: 03.4-08
    provides: the shell's tab row and More disclosure (03.4-03), and the phase's conformance audit that surfaced the UAT gap this plan closes
provides:
  - More's open state moved from an uncontrolled native <details> into React, following History.jsx's HistoryDisclosure precedent
  - The panel closes on any of its five items being chosen (links and buttons alike) and on any route change, including ones the Shell did not start
  - Focus returns to the summary after dismissal, so the tab order is never lost to the document body
  - A dated note on 03.4-03-PLAN.md decision 8 recording that it reasoned about opening and never about dismissal
affects: [shell navigation, phone-width UAT, any future phase touching Shell.jsx's More disclosure]

# Actuals (#2632)
actuals:
  tokens: 2032
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns: [controlled <details> via open/onToggle props (History.jsx's HistoryDisclosure precedent), close-on-route-change via a useEffect keyed on useLocation().pathname]

key-files:
  created: []
  modified:
    - app/src/ui/Shell.jsx
    - app/src/ui/Shell.test.jsx
    - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-PLAN.md

key-decisions:
  - "The disclosure stays a <details>; only its open state moves into React — open becomes a prop, onToggle keeps React and the DOM in step when the maker toggles from the summary itself."
  - "One click handler on the <ul>, not five — covers all five items and both activation paths (pointer tap, and Enter/Space which fire a click)."
  - "Focus returns to the summary for every item, not only Import and Export — the same focus-loss problem exists for the three navigation items."
  - "Half B (import feedback/navigation from a non-Home route) is out of scope — Mark deferred it 2026-09-22; recorded in 03.4-UAT.md's Deferred Follow-Ups."
  - "Shell.test.jsx's new assertions read Shell.jsx's own source as text (readFileSync, the home.test.js/RecipeList.jsx convention) since this suite is renderToStaticMarkup in the node environment and cannot click, toggle a <details>, navigate or run an effect."

requirements-completed: [D-15, D-16, UX1-01]

coverage:
  - id: D1
    description: "Choosing any of More's five items (Ingredients, Kitchen, Search links; Import, Export buttons) at phone width closes the More panel"
    requirement: "D-16"
    verification:
      - kind: unit
        ref: "app/src/ui/Shell.test.jsx#the More list carries a click handler closing the panel"
        status: pass
    human_judgment: true
    rationale: "The shell suite is renderToStaticMarkup in vitest's node environment and cannot click, toggle a <details>, or navigate — the unit test pins that the click handler exists and is wired to all five items structurally, but real click-through behavior at 393px needs a browser. Deferred to end-of-phase UAT (Mark away during execution, per standing practice)."
  - id: D2
    description: "A route change closes the panel even when the change came from somewhere other than a More item (e.g. the rail, browser back)"
    requirement: "D-16"
    verification:
      - kind: unit
        ref: "app/src/ui/Shell.test.jsx#an effect closes the panel keyed on the route pathname"
        status: pass
    human_judgment: true
    rationale: "The effect's dependency array is pinned structurally, but exercising a real navigation and observing the panel close needs a browser — deferred to end-of-phase UAT."
  - id: D3
    description: "Focus lands on More's own summary after any of the five items is activated, not on the document body"
    requirement: "UX1-01"
    verification: []
    human_judgment: true
    rationale: "Focus management after a DOM node is hidden cannot be observed by a static-markup render; needs a real browser check. Deferred to end-of-phase UAT."
  - id: D4
    description: "The rendered <details> still emits no open attribute at rest, and the three pre-existing More assertions still pass"
    requirement: ""
    verification:
      - kind: unit
        ref: "app/src/ui/Shell.test.jsx (14 tests, full file)"
        status: pass
    human_judgment: false
  - id: D5
    description: "03.4-03-PLAN.md decision 8 gets a dated, additions-only note recording what it did not consider"
    requirement: ""
    verification:
      - kind: other
        ref: "grep -c 2026-09-22 03.4-03-PLAN.md == 1; git diff --stat shows additions only"
        status: pass
    human_judgment: false

# Metrics
duration: ~15min
completed: 2026-09-22
status: complete
---

# Phase 03.4 Plan 11: More Panel Dismissal Summary

**More's open state moves from an uncontrolled native `<details>` into React (History.jsx's HistoryDisclosure pattern) — closes on item activation and on route change, returns focus to the summary.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-09-22T16:53:58Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `useLocation` (react-router) and `useEffect` (React) to Shell.jsx's imports; added a `moreOpen` boolean state, a `moreSummaryRef`, and a `closeMore` helper that sets `moreOpen` false and focuses the summary
- Wired the `<details className="shell__more">` with controlled `open`/`onToggle` props, the `<summary>` with the ref, and the `<ul>` with one `onClick={closeMore}` handler covering all five items (links and buttons alike)
- Added an effect keyed on `useLocation().pathname` that closes the panel on any route change, including ones the Shell did not itself start
- Extended the existing comment above the tab row to record why the open state is React's now
- Pinned all five structural facts plus the at-rest no-`open`-attribute markup assertion in a new `describe` block in Shell.test.jsx, reading Shell.jsx's own source as text (the home.test.js/RecipeList.jsx convention), since this suite's `renderToStaticMarkup` harness cannot click, toggle a `<details>`, navigate, or run an effect
- Appended a dated note to 03.4-03-PLAN.md decision 8 recording that it reasoned about opening and never about dismissal (additions only, no deletions — verified by `git diff --stat`)

## Task Commits

Each task was committed atomically:

1. **Task 1: More closes on choosing an item and on a route change, and hands focus back to its summary** - `83ea44f` (feat)
2. **Task 2: Pin the dismissal in the shell's own suite, and date the decision that excluded it** - `eef5b06` (test)

**Plan metadata:** committed separately per worktree convention (STATE.md/ROADMAP.md excluded; orchestrator owns those after merge)

## Files Created/Modified
- `app/src/ui/Shell.jsx` - More's open state moved into React: `moreOpen` state, `moreSummaryRef`, `closeMore` helper, an effect keyed on `pathname`, controlled `<details>`/`<summary>`/`<ul>` props
- `app/src/ui/Shell.test.jsx` - a new source-text `describe` block pinning the five facts the static-markup harness cannot show, plus one markup assertion that the rendered `<details>` still emits no `open` attribute at rest
- `.planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-PLAN.md` - decision 8 gains a dated note; no other line touched

## Decisions Made
See `key-decisions` in frontmatter — all five carried over from the plan's own `<decisions_recorded>` block; no new decisions made during execution, and no deviation from the plan's stated approach.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The worktree had no `node_modules` (a fresh worktree checkout); ran `npm --prefix app install` (existing `package.json`/`package-lock.json`, no new dependency added) before the first test run. `npm`/`node` were resolved via `/Users/mark/.nvm/versions/node/v24.21.0/bin` (the known Homebrew-symlink PATH drift recorded in project memory).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- G-03.4-3 half A is closed: More's dismissal is structurally proven (14 Shell.test.jsx assertions pass) and both `npm --prefix app test` (1125 tests) and `npm --prefix app run build` pass clean.
- The `<human-check>` in Task 1's `<verify>` block (real-browser click-through at 393px on the Notebook route: Ingredients, Export, Import each close the panel; focus resumes at More afterward) is deferred to end-of-phase UAT per `workflow.human_verify_mode: end-of-phase` and the standing practice of deferring human-verify checkpoints while Mark is away during execution.
- Half B (import feedback/navigation from a non-Home route) remains out of scope, deferred by Mark 2026-09-22 and already recorded in 03.4-UAT.md's Deferred Follow-Ups — untouched by this plan.
- No blockers for the rest of the wave.

---
*Phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t*
*Completed: 2026-09-22*

## Self-Check: PASSED

- FOUND: app/src/ui/Shell.jsx
- FOUND: app/src/ui/Shell.test.jsx
- FOUND: .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-SUMMARY.md
- FOUND commit 83ea44f (Task 1)
- FOUND commit eef5b06 (Task 2)
- FOUND commit 39b7940 (SUMMARY metadata)
- Re-ran `npm --prefix app test` (1125 passed) and `npm --prefix app run build` (succeeded) — both acceptance criteria confirmed after the metadata commit
