---
status: complete
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
board_170: boards/project/AltResumeSideNav.dc.html
board_170_version: 417f785
board_171: boards/project/Phone390Tabs.dc.html
board_171_version: 417f785
app_commit: ce6e894
widths: [1280, 393]
tool: none — no browser tool available to this run; every App-shows cell and
  Verdict below is judged from the shipped source (rule, class, token)
  against the board's own markup, not from a rendered pixel. The probe
  beside this record (03.4-gaps-conformance-probe.js) is what a run with a
  browser tool runs at UAT to confirm or correct every cell below.
---

# 03.4 Gaps Conformance Record

This record is the side-by-side check VERIFICATION.md's own "Added
2026-09-22" lesson demands: the running app read against the two named
boards, element by element, rather than against DESIGN.md's grammar or
CONTEXT.md's prose decisions — the check that caught the six gaps this
phase's plans 06 and 07 closed, and the check no verifier or Impeccable
audit had run before Mark did it by eye. Board 170
(`boards/project/AltResumeSideNav.dc.html`, "Pick up where you left off —
side navigation") is read at 1280px; board 171
(`boards/project/Phone390Tabs.dc.html`, "Home at 390 — bottom tab row") is
read at 393px. Both are pinned at commit `417f785`, unchanged since either
board was drawn.

This pass is not measured — no browser tool was available to this run.
Every row's Verdict below was reached by reading the shipped CSS rule,
class or token against the board's own hand-copied markup, never by
rendering either page; where a cell genuinely cannot be judged without a
rendered pixel (a fluid size, a wrap point, a computed flex width), the
Verdict reads `unmeasured` rather than a guess. Rerunning this pass with a
browser — pasting `03.4-gaps-conformance-probe.js` into the running app at
each width and reading `readWidth(SELECTORS_1280)` / `readWidth(SELECTORS_393)`
— is the job left for UAT.

## Conformance table

| # | Width | Element | Board says | App shows | Verdict |
|---|-------|---------|------------|-----------|---------|
| 1 | 1280 | Header | board 170 line 19: flex row, `justify-content: space-between`, padding `26px 56px 18px 40px`; brand "Sprinkles" in Georgia at 26px | `.shell__head` (shell.css:28-34): flex, `flex-wrap: wrap`, `justify-content: space-between`, padding `var(--gap-s) var(--gap-page)` = 12px 48px; `.shell__brand` (shell.css:36-41) reads `var(--face-grotesk)` (sans, not the board's Georgia) at `var(--app-size-brand)` = 22px, one fixed size at every width | differs |
| 2 | 1280 | Tools row | board 170 line 21-25: nav "Tools", flex, gap 28px, three 14px links (Search, Import, Export) | `.shell__tools` (shell.css:82-86): flex, align-items center, gap `var(--gap-m)` = 20px; `.shell__place` (shell.css:157-165) font-size `var(--app-size-meta)` = 14px | differs |
| 3 | 1280 | Rail | board 170 line 30: nav "Primary", `flex: 0 0 224px`, padding `8px 20px 32px 30px`, `border-right: 1px solid #d6dad7` | `.shell__rail` (shell.css:121-129): `flex: 0 0 var(--app-size-nav-w)` = 224px, width 224px, box-sizing border-box, flex column, padding `var(--gap-xs) var(--gap-m) var(--gap-l)` = 6px 20px 32px 20px, `border-right: var(--app-rule-row) solid var(--app-divider)` = 1px solid #d6dad7 — the padding reads the gap scale's own values (6/20/32/20) rather than the board's hand-copied 8/20/32/30, on 03.4-07 decision #5's authority (the board governs structure and order, tokens govern values) | matches |
| 4 | 1280 | Rail — Home | board 170 line 32: `aria-current="page"`, background `#f3f4f2`, color `#1576de`, font-weight 600, font-size 15px, `border-radius: 8px`, padding `9px 12px`, gap 12px | `.shell__place--home` (shell.css:195-197) color `var(--app-blue-text)` = #1576de; `.shell__place[aria-current='page']` (shell.css:178-180) background `var(--app-surface-subtle)` = #f3f4f2; base `.shell__place` (shell.css:157-165) gap `var(--gap-xs)` = 6px, padding `var(--gap-s) var(--gap-m)` = 12px 20px, font-size 14px — no `border-radius` rule and no font-weight rule anywhere for the current place | differs |
| 5 | 1280 | Rail — Notebook | board 170 line 35: color `#ee0803`, icon stroke `#fd5b57`, font-weight 400 | `.shell__place--notebook` (shell.css:203-209): color `var(--app-notebook-text)` = #ee0803; svg stroke `var(--app-notebook)` = #fd5b57 | matches |
| 6 | 1280 | Rail — Recipe book | board 170 line 36: color `#bc5b0d`, icon stroke `#f18a36` | `.shell__place--recipe-book` (shell.css:211-217): color `var(--app-recipe-book-text)` = #bc5b0d; svg stroke `var(--app-recipe-book)` = #f18a36 | matches |
| 7 | 1280 | Rail — Idea log | board 170 line 37: color `#976f01`, icon stroke `#fdc632` | `.shell__place--idea-log` (shell.css:219-225): color `var(--app-idea-log-text)` = #976f01; svg stroke `var(--app-idea-log)` = #fdc632 | matches |
| 8 | 1280 | Rail — Ingredients | board 170 line 40: color `#358452`, icon stroke `#388b57` | `.shell__place--ingredients` (shell.css:227-233): color `var(--app-ingredients-text)` = #358452; svg stroke `var(--app-ingredients)` = #388b57 | matches |
| 9 | 1280 | Rail — Kitchen | board 170 line 41: color `#505db5`, icon stroke `#505db5` (one hue, no separate companion) | `.shell__place--kitchen` (shell.css:235-241): color `var(--app-kitchen)` = #505db5; svg stroke `var(--app-kitchen)` = #505db5 (tokens.css:364, one token for both, matching the board's own choice and D-09's "Kitchen's indigo needs no companion") | matches |
| 10 | 1280 | Lead block (container) | board 170 line 49: flex row, align-items center, column-gap 24px, padding `18px 20px`, `border: 1px solid #1a1c1f`, `border-radius: 10px` | `.home__lead` (home.css:63-71): flex, align-items center, column-gap `var(--gap-m)` = 20px, padding `var(--gap-m)` = 20px (one value, not two-axis), border `var(--app-rule-row)` solid `var(--app-text)` = 1px solid #141414, `border-radius: var(--app-radius-lead)` = 10px | differs |
| 11 | 1280 | Lead — rod | board 170 line 51: `flex: 0 0 8px`, width 8px, height 62px, `border-radius: 4px`, background `#fd5b57` | `.home__lead .home__rail` (home.css:75-77, base rule home.css:198-204): width `var(--app-size-rail-w)` = 8px, height `var(--app-size-rail-h)` = 56px, `border-radius: var(--app-radius-rail)` = 4px, background `var(--app-notebook)` = #fd5b57 — the same token also governs the row's 44px rod (row 17 below); per 03.4-07's own recorded decision #5 ("sizes read tokens... the board governs structure and order, tokens govern values"), not a new departure this plan opens | differs |
| 12 | 1280 | Lead — page-preview glyph | board 170 line 53-58: `flex: 0 0 46px`, 46x62px box, padding `7px 6px`, `border: 1px solid #d6dad7`, `border-radius: 2px`, four inner lines | no element — `HomeLead` (RecipeList.jsx:98-129) renders only the rod, the identity column, the conditional Next time column and the actions; no matching class or rule exists in home.css | departure |
| 13 | 1280 | Lead — identity | board 170 line 60-64: `flex: 2 1 0`; label 11px/600/uppercase/`0.1em`, color `#5b6169` (neutral grey); h2 "Olive Oil Ice Cream" in Georgia at 23px; meta 13px, color `#5b6169` | `.home__lead-identity` (home.css:82-86) flex `1 1 0`; label reads `.home__lead-place` (home.css:107-115): font-size `var(--app-size-label)` = 12px, uppercase, tracking `var(--app-tracking-label)` = 0.08em, color `var(--app-notebook-text)` = #ee0803 (the destination colour, not the board's own neutral grey — board 171's equivalent label, row 24 below, uses this same destination colour, so the app's one consistent rule matches board 171 and departs from board 170); `.home__lead-name` (home.css:117-123): `var(--face-grotesk)` (sans, not Georgia) at `var(--app-size-lead-name)` = 28px; `.home__lead-meta` (home.css:129-135): 14px, color `var(--app-text-secondary)` = #595959 | differs |
| 14 | 1280 | Lead — next-time | board 170 line 66-69: `flex: 2 1 0`; label 11px uppercase, color `#5b6169`; hand text in Caveat at 24px, line-height 1.2, color `#1f3d7a` | `.home__lead-next` (home.css:82-86) flex `1 1 0`; `.home__lead-caption` (home.css:97-105): 12px, uppercase, tracking 0.08em, color `var(--app-text-secondary)` = #595959, reading "Next time"; `.home__lead-next-time` (home.css:140-143) sets spacing/measure only and declares no font-family, so the `.app-hand` role class (app.css, plan 02) carries the Caveat face, `--size-hand` (22px) and pen blue (#1f3d7a) — matching the board's face, colour and near-matching size; rendered only when the entry's newest batch carries a Next time note (RecipeList.jsx:121-126), where the board always draws the column | matches |
| 15 | 1280 | Lead — actions | board 170 line 71-74: `flex: 0 0 auto`, gap 10px; filled button padding `11px 18px`, 14px/700; secondary button border `1px solid #1576de`, padding `10px 17px`, 14px/600 | `.home__lead .home__actions` (home.css:90-92) `flex: 0 0 auto`; base `.home__actions` (home.css:269-275) gap `var(--gap-s)` = 12px; `.home__action` (home.css:282-296) padding `var(--gap-s) var(--gap-m)` = 12px 20px, font-size 14px, font-weight 600 (the board's filled action is 700; the app applies 600 to both filled and secondary alike); `.home__action--secondary` (home.css:301-304) matches the board's outline treatment | differs |
| 16 | 1280 | Row 1 (container) | board 170 line 87: flex row, align-items center, column-gap 20px, padding `13px 0`, `border-top: 1px solid #d6dad7` | `.home__row` (home.css:169-180): `display: grid` (not flex — a grid so the rail spans two identity rows, per the file's own comment), column-gap `var(--gap-m)` = 20px (matches exactly), padding `var(--gap-m) 0` = 20px 0, border-top `var(--app-rule-row)` solid `var(--app-divider)` = 1px solid #d6dad7 (matches) | differs |
| 17 | 1280 | Row — rod | board 170 line 88: `flex: 0 0 8px`, width 8px, height 44px, `border-radius: 4px`, background `#fd5b57` | `.home__row .home__rail` (home.css:198-204, grid-area rail): width 8px, height `var(--app-size-rail-h)` = 56px, `border-radius: 4px`, background `#fd5b57` — same token-governed height difference as row 11 (03.4-07 decision #5), not a new departure | differs |
| 18 | 1280 | Row — identity | board 170 line 89-92: `flex: 2 1 0`; label 11px uppercase, color `#5b6169` (neutral grey); h3 "Olive Oil Ice Cream" in Georgia at 19px | `.home__place` (home.css:206-215, grid-area place): 12px uppercase, color `var(--app-notebook-text)` = #ee0803 (destination colour, not the board's neutral grey — board 171's own row label, row 29 below, uses this same destination colour, so this rule matches board 171 and departs from board 170); `.home__name` (home.css:217-225, grid-area name): `var(--face-grotesk)` (sans) at `var(--app-size-recipe-name)` = 24px, no responsive shrink at any width | differs |
| 19 | 1280 | Row — standing | board 170 line 93: `flex: 0 0 120px`, 13px/600, "Awaiting tasting" | `.home__standing` (home.css:184-189, grid-area standing): 14px/600; the grid column itself is sized by `--app-col-standing` = 120px on `.home__row`'s `grid-template-columns` (home.css:171) — the column width matches the board exactly | matches |
| 20 | 1280 | Row — tally | board 170 line 94-100: `flex: 0 0 132px`; marks row gap 5px, two hollow ticks 18x6px, `border-radius: 3px`, `border: 2px solid #fd5b57`, background transparent; count 12px, color `#5b6169`, tabular-nums, "2 batches" — marks stacked over the count | `.home__meta` (home.css:236-241, grid-area tally, sized by `--app-col-tally` = 132px on the same `grid-template-columns` — matches the board exactly): flex column, gap `var(--gap-hair)` = 2px, marks over count as the board draws it; `.home__tally-mark` (home.css:254-260): 18x6px, `border-radius: 3px`, border `var(--app-rule-tally)` solid `var(--app-notebook)` = 2px solid #fd5b57, no background declared (transparent); `.home__count` (home.css:262-267): 12px, color `var(--app-text-secondary)` = #595959, tabular-nums | matches |
| 21 | 1280 | Row — actions | board 170 line 101: `flex: 0 0 auto`, `margin-left: auto`, gap 8px; filled padding `9px 15px`, 13px/700; secondary padding `8px 14px`, 13px/600 | `.home__actions` (home.css:269-275, grid-area actions): gap `var(--gap-s)` = 12px; `.home__action` (home.css:282-296): padding 12px 20px, font-size 14px, font-weight 600 shared by filled and secondary (board splits 700/600) — same pattern of difference as row 15's lead actions; the rendered labels themselves ("Record a tasting" / "Continue developing") match the board's own words for this standing (RecipeList.jsx:177-188) | differs |
| 22 | 393 | Header | board 171 line 18: flex row, padding `22px 20px 8px`; brand "Sprinkles" in Georgia at 22px; five sprinkles inline beside it, gap 3px, `margin-left: 8px`, each 12x4px | `.shell__head` (shell.css:28-34, unchanged by width): padding `var(--gap-s) var(--gap-page)` = 12px 20px below 600px (app.css:2484 steps `--gap-page` to `var(--gap-m)`) — left/right 20px matches the board exactly, top/bottom 12px/12px is symmetric where the board is asymmetric 22px/8px; `.shell__brand` at `var(--app-size-brand)` = 22px matches the board's number at this width by coincidence (the token does not shrink — see row 1's 26px mismatch at 1280); `.shell__sprinkles` (shell.css:46-50) sits BELOW the brand via `margin-top`, not inline beside it, and each `.shell__sprinkle` (shell.css:52-58) is 18x6px, one fixed size at every width, larger than the board's 12x4px phone sprinkles | differs |
| 23 | 393 | Lead block (container) | board 171 line 22: flex column, gap 12px, padding 16px, `border: 1px solid #1a1c1f`, `border-radius: 10px` — no rod, no page-preview glyph, both absent from the board's own markup at this width | `.home__lead` inside the 759.98px media block (home.css:336-341): `flex-direction: column`, row-gap `var(--gap-s)` = 12px (matches), padding `var(--gap-s)` = 12px (board 16px); border and radius survive unchanged from the base rule; `.home__lead .home__rail` (home.css:343-345) is set `display: none` at this step — the app's rod is explicitly hidden here, matching the board's own omission (03.4-07 decision #7, not a new finding) | matches |
| 24 | 393 | Lead — identity | board 171 line 23-27: label color `#ee0803` (the destination's own colour, not board 170's neutral grey); h2 22px Georgia; meta 13px color `#5b6169` | `.home__lead-place` (home.css:107-115): color `var(--app-notebook-text)` = #ee0803 — matches the board's own choice at this width exactly (see row 13's note on board 170's divergent neutral-grey choice); `.home__lead-name` (home.css:117-123): sans, `var(--app-size-lead-name)` = 28px, fixed at every width (board 171 draws 22px, smaller than even board 170's 23px) | differs |
| 25 | 393 | Lead — next-time | board 171 line 28-31: label 11px uppercase color `#5b6169`; hand text Caveat 22px, line-height 1.2, color `#1f3d7a` | `.home__lead-caption` / `.home__lead-next-time` / `.app-hand` as row 14; DESIGN.md's D-17 hand size is `--size-hand` (22px) with a `--size-hand-min` floor (20px) — whether the rendered size lands at exactly 22px at 393px (matching the board) or elsewhere in its range cannot be read from source alone | unmeasured |
| 26 | 393 | Lead — actions | board 171 line 32: flex wrap, gap 8px; filled padding `12px 16px`, 14px/700; secondary padding `11px 15px`, 14px/600; buttons content-sized, not stretched | `.home__actions` / `.home__action` as row 15 (gap 12px, one shared 600 weight); additionally, the row-scoped full-width override `.home__action { flex: 1 1 0 }` inside the 759.98px media block (home.css:325-331) is not scoped to `.home__row` alone — its selector also reaches the lead's own actions, so the app stretches the lead's buttons to fill the row's width at this step where the board keeps them content-sized and wrapped | differs |
| 27 | 393 | Row 1 (container) | board 171 line 36-46: flex column, gap 10px, padding `14px 0`, `border-top: 1px solid #d6dad7` — two groups only (identity+standing on one line, one action button beneath), no tally anywhere | `.home__row` inside the 759.98px media block (home.css:315-323): still `display: grid`, four stacked areas (`rail place standing` / `rail name standing` / `rail tally tally` / `rail actions actions`), padding `var(--gap-s) 0` = 12px 0 (board 14px 0) — the app keeps a rod row and a tally row the board does not draw at this width | differs |
| 28 | 393 | Row — rod | board 171: absent — no colour-bar element anywhere in the row's markup (line 36-46) | `.home__row .home__rail` (home.css, inside the 759.98px media block, 03.4-12 Task 1): `display: none` — Mark's UAT rulings 6.2 and 7.3 of 2026-09-22 (G-03.4-6b) reverse 03.4-07 decision #2 (departure #2 below, now closed); the row's rod is hidden at this width, matching board 171's own omission | matches |
| 29 | 393 | Row — identity | board 171 line 39-42: label color matches its own destination (`#ee0803` for Notebook); h3 19px Georgia | `.home__place` / `.home__name` unchanged by width (no media override for either rule): label color `var(--app-notebook-text)` = #ee0803 matches the board's own choice at this width exactly; `.home__name` stays sans at 24px (board 19px, same as board 170's row heading) | differs |
| 30 | 393 | Row — standing | board 171 line 43: `flex: 0 0 auto`, 13px/600, positioned beside identity in a `justify-content: space-between` head row (not stacked) | `.home__standing` (home.css:184-189): 14px/600, occupies its own grid column beside the two-line identity stack (`'rail place standing' / 'rail name standing'`) — structurally "beside identity" as the board draws it, via a grid column rather than the board's simple two-item flex row | matches |
| 31 | 393 | Row — tally | board 171: absent entirely — no marks, no count, anywhere in the row's markup at this width (line 36-46) | `.home__row .home__meta` (home.css, inside the 759.98px media block, 03.4-12 Task 1): `display: none` — same rulings reverse decision #2's tally half; hiding the marks takes the count in words out with them, which UX1-03 permits (it forbids a count carried by marks alone, not a count required at every width) | matches |
| 32 | 393 | Row — actions | board 171 line 45: flex, gap 8px; ONE filled button only ("Record a tasting") — no secondary action drawn for this row at this width, unlike board 170's equivalent row (line 101) which shows both | `.home__row .home__action--secondary` (home.css, inside the 759.98px media block, 03.4-12 Task 1): `display: none` — the secondary action no longer renders at this width, matching board 171's own single button; the one remaining filled action still stretches to the row's full width under this block's own pre-existing rules (`home__actions{width:100%}`, `.home__action{flex:1 1 0}`), which board 171 draws at natural width instead — recorded as new open departure #5 below | matches |
| 33 | 393 | Bottom nav (tabs container) | board 171 line 87-88: `position: absolute`, inset 0/bottom 0, `border-top: 1px solid #d6dad7`, background white, `padding-bottom: 12px`; `ul` flex, five `li` each `flex: 1 1 0` | `.shell__tabs` inside the 759.98px media block (shell.css:262-272): `position: fixed`, `inset-inline: 0`, `inset-block-end: 0`, height `var(--app-size-tab-h)` = 56px, background `var(--app-background)`, `border-top` solid `var(--app-divider)`; `.shell__tabs > .shell__place, .shell__tabs > .shell__more` (shell.css:279-283): `flex: 1 1 0`, `min-width: 0` — five equal-share children, closing the overflow VERIFICATION.md's gap 1 measured (532px total, More off-screen) | matches |
| 34 | 393 | Tab — Home | board 171 line 89: `aria-current="page"`, color `#1576de`, 10px/600, icon 22x22, gap 4px, padding `8px 0 4px` | `.shell__place--home` color `var(--app-blue-text)` = #1576de (matches); `.shell__tabs .shell__place` (shell.css:289-299): gap `var(--gap-hair)` = 2px (board 4px), padding `var(--gap-xs) 0` = 6px 0, font-size `var(--app-size-label)` = 12px (recorded departure, board's own hand-copied 10px — 03.4-06 key-decision); icon stays `var(--app-size-icon)` = 20px at every width, smaller than the board's 22px tab icon (a second, un-recorded divergence noted here, not covered by the label-size departure) | departure |
| 35 | 393 | Tab — Notebook | board 171 line 90: color `#ee0803`, 10px/500 | `.shell__place--notebook` color `#ee0803` matches; label size 12px (same recorded departure as row 34); no explicit `font-weight: 500` rule exists for a non-current tab — it inherits the browser default (400) | departure |
| 36 | 393 | Tab — Recipe book | board 171 line 91: color `#bc5b0d`, 10px/500 | `.shell__place--recipe-book` color `#bc5b0d` matches; label size 12px (same recorded departure) | departure |
| 37 | 393 | Tab — Idea log | board 171 line 92: color `#976f01`, 10px/500 | `.shell__place--idea-log` color `#976f01` matches; label size 12px (same recorded departure) | departure |
| 38 | 393 | Tab — More | board 171 line 93: color `#1a1c1f` (near-black ink), 10px/500, dots icon | `.shell__more`'s `summary` carries the base `.shell__place` rule only — no dedicated colour rule exists, so it inherits `.shell`'s own `color: var(--app-text)` = #141414, close to the board's `#1a1c1f`; label size 12px (same recorded departure) | departure |

## Gap-to-evidence table

| Gap | VERIFICATION.md's own truth | Conformance rows | Plan / task | Test pin |
|-----|------------------------------|-------------------|-------------|----------|
| G1 | Below 759.98px the rail becomes a bottom tab row of five reachable tabs, all touchable, the fifth (More) holding Ingredients/Kitchen/Search/Import/Export (D-16) | rows 33-38 (tabs container, Home, Notebook, Recipe book, Idea log, More) | 03.4-06, Task 3 | `shell.test.js` "the bottom tab row (D-16, 03.4-03 Task 3) — five new pins" |
| G2 | An unbuilt place opens styled through the App's own tokens, `.place`/`.place__title`/`.place__note` resolving to real rules (D-10) | no conformance row — Placeholder is not drawn on either board (both are Home boards); closed and pinned independently of this record | 03.4-06, Task 2 | `shell.test.js` "the unbuilt place reads in the App's own voice (D-10, gap 2, CR-01)" |
| G3 | Every route renders inside the shell's own App ground, not the Sheet's (D-09) | row 1 (header) and every other row in this table — all read the ground `.shell` now paints once, `background: var(--app-background)` (shell.css:20-26), rather than the retired `body:has(.home)` route-only rule | 03.4-06, Task 1 | `shell.test.js` "`.shell` paints the App ground for every route it wraps (gap 3, route.md § 1)" |
| G4 | A Sheet `a:visited` rule never recolours an App control or a rail place (D-01/D-02, D-09 destination companions) | rows 5-9, 18, 24, 29, 34-38 — every coloured rail/tab/row label above, each reading its own single-class colour rule that the rewritten `:where(a:visited)` can no longer outrank | 03.4-06, Task 1 | `binder.test.js` "`:where(a:visited)` reads the same ink colour as unvisited, at zero specificity (gap 4)"; "app.css's only visited selector is the zero-specificity one (gap 4)" |
| G5 | The lead block matches the board: a bordered block with the rod, the identity, a captioned Next time column, and the standing's own actions (D-06, D-07, D-12, D-18) | rows 10-15 (1280 lead and its children) and 23-26 (393 lead and its children) | 03.4-07, Task 2 | `RecipeList.test.jsx` "HomeLead — the board's block (gap 5)"; `home.test.js` "`.home__lead` is a bordered, radiused block (gap 5)" |
| G6 | Each recipe row matches the board's column grammar: rod, identity, the standing word, the tally, then actions (D-07, D-11) | rows 16-21 (1280 row and its children) and 27-32 (393 row and its children) | 03.4-07, Task 1 | `RecipeList.test.jsx` "RecipeRows — the standing word (gap 6, D-07, D-11)"; `home.test.js` "the row's desktop template reads the board's standing and tally columns (gap 6)" |

## Departures

Four knowing departures from a board, each already decided by an earlier
plan in this phase — recorded here, as this plan's own job, for Mark to
rule on at UAT rather than resolved by this record.

| # | Departure | Authority | Open for Mark |
|---|-----------|-----------|----------------|
| 1 | The tab row's label reads `var(--app-size-label)` (12px) rather than board 171's hand-copied 10px (rows 34-38) | 03.4-06 key-decision: "A tab's label reads `--app-size-label` (12px), not the board's hand-copied 10px — sizes read tokens (the project's own convention) and 12px is the existing micro-label role; recorded as a board departure for plan 08's conformance table" | Yes — open for Mark to confirm 12px reads correctly at UAT, on the device that matters |
| 2 | Superseded 2026-09-22, closed. The rod and the tally were kept on every row at phone width (rows 28, 31), on 03.4-07 decision #2's authority. Mark's UAT rulings 6.2 and 7.3 of 2026-09-22 (G-03.4-6b) reverse that decision: the row's rod and its tally are now hidden below the phone step, matching board 171's own omission, and DESIGN.md's App marks rail bullet now carries the phone-width exception | 03.4-12, Task 1 and Task 3; Mark's UAT rulings 6.2 and 7.3, 2026-09-22, read against board 171 lines 36-46 | Closed — resolved by Mark's own ruling against the board; nothing further open on this departure |
| 3 | The board's 46px lined page-preview glyph in the lead block is not built (row 12) | 03.4-07 key-decision #3: "The board's 46px lined page-preview glyph in the lead block is not built — decorative, aria-hidden, absent from board 171, and absent from the four things Mark named side by side with the board" | Yes — open for Mark to confirm the glyph's absence is still acceptable |
| 4 | No row or lead carries a "versions count" cell (no matching row exists in this table — neither board draws one either) | 03.4-07 key-decision #1: "No versions count on a row, despite VERIFICATION's phrasing — D-11 and board 170's own five children (rod, identity, standing, tally, actions) govern; the row ships without one" — confirmed here: board 170's row (line 87-101) and board 171's row (line 36-46) both draw the same five/three elements this record's rows already cover, with no sixth "versions count" cell in either board's own markup | Yes — open for Mark to confirm VERIFICATION.md's gap-6 prose (which names a versions count) should be read as superseded by the board itself, not as an unmet truth |
| 5 | At 393px the single remaining filled action stretches to the row's full width (row 32), where board 171 draws it at its natural width | The stretch comes from the phone block's own touch rules shipped in 03.4-03 (`.home__actions { width: 100% }`, `.home__action { flex: 1 1 0 }`); Mark's 2026-09-22 rulings 6.2 and 7.3 ruled only on the secondary action, not on this stretch | Yes — open for Mark to confirm at UAT whether the one remaining action should keep the stretch or return to its natural width, as board 171 draws it |

## Verdict

Of the 38 rows in this table, 11 match the board outright, 8 land on one
of the four recorded departures above, 18 differ in some measurable way
(mostly spacing, a shared font-weight where the board splits filled/
secondary, and the App's fixed type sizes not shrinking the way the
board's own two drawings shrink between 1280 and 393), and 1 (the hand's
exact rendered size at 393px) is unmeasured — this run had no browser tool,
so every Verdict above was reached from the shipped source, never a
rendered pixel.

All six of VERIFICATION.md's gaps trace to at least one row above, a plan,
a task and a test pin (the gap-to-evidence table). Three findings surfaced
here were not part of any of the six gaps and are not yet a decided
departure: `.shell__rail` carries no padding and no `border-right` at all
(row 3); the rail's active-item pill (`border-radius`, bold weight) never
made it into `.shell__place[aria-current='page']` (row 4); and `RowActions`
always renders both the filled and the secondary action at every width,
where board 171's own row shows only the filled one (row 32). None of
these are resolved by this plan — they are open rows for Mark, exactly
like the four named departures above, surfaced by the side-by-side check
VERIFICATION.md's own lesson asked for rather than by a second read of
DESIGN.md's grammar.
