---
status: complete
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
source: [03.4-VERIFICATION.md (fourth re-verification after gap closure round four, 2026-09-22, human_verification items); round four record in 03.4-UAT-round4.md]
started: 2026-09-22T22:42:00Z
updated: 2026-09-22T23:49:17Z
---

## Current Test

[testing complete]
## Tests

### 1. Home: Tab reaches every link after the shell (G-03.4-r4-1, closes the gap)
expected: Serve the build (`npm --prefix app run build && npm --prefix app run preview -- --host`, port 4173), never the dev server. Open on the iPad, hardware keyboard attached, Full Keyboard Access on. Note the iPadOS version and the keyboard's model/connection first. At 1366px, tap once in the header's blank space beside the Sprinkles wordmark — not the content column or any control. Press Tab repeatedly. Expected: a ring at Search, Import, Export, Home, Notebook, Recipe book, Idea log, Ingredients, Kitchen, in that order, then on Home's own links in page order: the lead recipe's name, its action(s), then each row's name and action(s) down the list (the lead's name rings a second time as a row). Shift-Tab from the last row's last action walks all the way back to Search; nothing traps. Tab past the last action leaving the page for Safari's chrome (no wrap) is expected, not a fail.
result: pass
note: "Full Keyboard Access OFF by Mark's decision — \"I'm not going to turn Full Keyboard Access on. I believe that is what is causing the keyboard to lockup forcing a power cycle.\" Qualification: \"when I tab to the last row / last link and tab past the last link, the focus ring goes away, then reappears on Search, then just toggles the ring on/off on the Search link\" — the exit past the last link is the expected hand-off to Safari's chrome; the on/off toggle on Search is the chrome-to-page boundary with FKA off, outside the page. No keyboard lock-up." 

### 2. A recipe page: Tab reaches the running head and history/lineage links (G-03.4-r4-1)
expected: Open a recipe and tap beside the wordmark, same setup. Tab through the nine shell stops. Expected: the running head's Sprinkles link rings next, then the page's own controls in order. From version and From batch ring as links where present. Open History with Space and every version/batch name shown as a link rings. Fail the gap if Tab skips any link on either route.
result: skipped
reason: "Deferred follow-up: reported as an issue (\"fail. on recipes page, only link on the recipes page is Sprinkles link in the sheet header, then back to Search\"), diagnosed as G-03.4-r5-2, then deferred by Mark's ruling — \"defer this for now. I like the shared component idea the best, since we are using react\". Todo: .planning/todos/pending/2026-09-22-shared-button-component-carries-tabindex-for-webkit.md" 
note: "Full Keyboard Access OFF (Mark's standing decision, test 1)." 

### 3. S2 — the keyboard dead until a power cycle (recorded, never asserted)
expected: Recorded, never asserted. If the keyboard stops responding during test 1 or 2, do not power cycle first — try, in order: (1) Tab-H and Fn-H, (2) tap Safari's address bar and type, (3) type in Notes from the Home Screen, (4) turn Full Keyboard Access off and type in Notes, (5) detach/reattach the keyboard or toggle Bluetooth. Then run the control regardless: a new blank Safari tab, Tab about 30 times from the address bar, then Tab past the last control on an ordinary website. Record which step restored the keyboard (or that it did not die) and the control's outcome, as a note on G-03.4-r4-1 — never as a pass/fail on the gap.
result: pass
note: "Recorded: Mark ran rounds five with Full Keyboard Access OFF by decision and the keyboard did not die. Mark attributes the round-four lock-up to Full Keyboard Access itself. The recovery ladder and control run were not needed. Hypothesis B (the FKA service) stands as the working explanation; no further device work planned." 

## Summary

total: 3
passed: 2
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps

- gap_id: G-03.4-r5-2
  truth: "On a recipe page on the iPad (Full Keyboard Access off), after the nine shell stops Tab rings the running head's Sprinkles link, then the page's own controls in order, From version and From batch where present, and every History item once History is open"
  status: deferred
  deferred_by: "Mark, 2026-09-22: \"defer this for now. I like the shared component idea the best, since we are using react\" — carried as a todo, not a 03.4 gap-closure plan"
  todo: ".planning/todos/pending/2026-09-22-shared-button-component-carries-tabindex-for-webkit.md"
  reason: "User reported: fail. on recipes page, only link on the recipes page is Sprinkles link in the sheet header, then back to Search"
  severity: major
  test: 2
  observation: "Full Keyboard Access was OFF for this round (Mark's decision). Home passed in the same round: every Home link rang. On the recipe page only the running head rang, then focus went back to Search."
  root_cause: "With Full Keyboard Access off, WebKit skips any <button>, checkbox or radio that has no explicit tabindex (HTMLFormControlElement::isKeyboardFocusable -> tabsToAllFormControls false; established from WebKit source in ipad-tab-never-enters-app.md). The recipe page's reading view renders only such buttons — History x2, Next version, Record, Correct, the six Balance rules — and no text field, textarea or select; 03.4-14 gave the shell's buttons tabindex 0 and 03.4-15 covered links only. The seed recipe's one root version has no From version / From batch link. So after the running head nothing can take focus and the next Tab hands focus back to Safari. App-wide: 38 sites lack a tabindex (34 buttons, 2 checkboxes, 2 radios); 11 render in the reading view Mark tested, and the edit modes would skip Save/Cancel, the radio groups and the Skipped checkbox the same way (inferred from source, not seen on the device)."
  artifacts:
    - path: "app/src/ui/VersionRow.jsx"
      issue: "7 buttons without tabindex (285, 292, 296 reading mode; 162, 167, 170, 175 editor)"
    - path: "app/src/ui/History.jsx"
      issue: "The disclosure button (lines 3-7), rendered twice (History, Batches)"
    - path: "app/src/ui/BatchRow.jsx"
      issue: "5 buttons (537 reading mode; 679, 681, 781, 804 recording mode)"
    - path: "app/src/ui/FormulationNote.jsx"
      issue: "Line 19 passes tabIndex undefined in reading mode to GraduatedRule.jsx:85's button (x6); -1 in edit modes"
    - path: "app/src/ui/Method.jsx"
      issue: "7 buttons (237, 264, 284, 325, 343, 410, 467) and 2 checkboxes (299, 445)"
    - path: "app/src/ui/PenFoot.jsx"
      issue: "8 buttons (39, 42, 44, 47, 93, 98, 101, 106)"
    - path: "app/src/ui/IngredientTable.jsx"
      issue: "2 buttons (156, 261)"
    - path: "app/src/ui/Authored.jsx"
      issue: "1 button (30)"
    - path: "app/src/ui/Segmented.jsx"
      issue: "Clear button (41) and the named radio group (54)"
    - path: "app/src/ui/AxisMark.jsx"
      issue: "Clear button (76) and the named radio group (94)"
  missing:
    - "Extend the rule to every <button>, checkbox and radio: tabIndex={0} at the 38 sites; FormulationNote reading mode 0, edit modes stay -1; the two h2 landing targets stay; summary/select/textarea/text and date inputs need nothing; disabled buttons need no care; named radio groups stay one Tab stop"
    - "Pin per component on rendered markup as 03.4-14/15 did; a one-line grep gate does not work because 14 buttons split <button and its attributes across lines"
    - "Structural choice for Mark: (a) per-component pins plus a .jsx source scan, or (b) a shared Button component that always sets the attribute"
  debug_session: ".planning/debug/ipad-recipe-page-tab-skips-controls.md"

## Deferred Follow-Ups

- test: 3
  idea: "Import feedback: after a successful import from a non-Home route nothing changes on screen (Placeholder ignores the Outlet context) and .shell__import-errors sits in the header, off-screen at 393px. Options: navigate('/') after import, or a role=status live region beside the controls announcing the result. See .planning/debug/more-panel-stays-open-import-no-home.md half B."
  deferred_at: 2026-09-22
  carried_from: round three (03.4-UAT-round3.md), via round four (03.4-UAT-round4.md)
- test: 1
  idea: "add a todo to change this so that all pages use white" (overscroll canvas white on every route, including the paper frames)
  deferred_at: 2026-09-22
  carried_from: round three (03.4-UAT-round3.md), via round four (03.4-UAT-round4.md)
- test: 2
  idea: "Shared Button component that always carries tabindex so every button, checkbox and radio Tabs on iPadOS Safari with Full Keyboard Access off (G-03.4-r5-2, 38 sites). \"defer this for now. I like the shared component idea the best, since we are using react\""
  deferred_at: 2026-09-22
