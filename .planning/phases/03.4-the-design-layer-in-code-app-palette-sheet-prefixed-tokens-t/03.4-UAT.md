---
status: diagnosed
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
source: [03.4-VERIFICATION.md (re-verification after gap closure, 2026-09-22)]
started: 2026-09-22T03:31:20Z
updated: 2026-09-22T15:47:52Z
---

## Current Test

[testing complete]

## Tests

### 1. The bottom tab row at 393px and on the iPad (WebKit, coarse pointer)
test: Load the app at 393px and confirm all five tabs (Home, Notebook, Recipe book, Idea log, More) are fully on-screen and touchable, that More's opened panel does not cover the page's last control, and that the header's Search/Import/Export controls are folded away. Repeat on Mark's iPad.
expected: No overflow (the original measured 532px/off-screen-More defect does not recur).
result: issue
reported: "pass on iPhone. fail on iPad - the page load time is measured in seconds"
severity: major

### 2. The hand's rendering and its two-mode fallback with real data
test: Record a real "Next time" note on a batch, return to Home, confirm the lead block renders it in Caveat/pen blue — then activate `forced-colors: active` and print preview and confirm the fallback to the text face in italic.
expected: Legible script by default; text-face italic under forced colours and in print (D-17).
result: pass

### 3. Real Import/Export click-through
test: Use the tools-row Import button to choose a JSON file, and Export to trigger a download, from more than one route.
expected: File picker opens, JSON round-trips, Home reloads via `storeRevision`, Export downloads.
result: issue
reported: "fail - when on Notebook tab in iphone width, More menu doesn't close when one of the menu options is clicked; export works (json file is written); import doesn't navigate back to home"
severity: major

### 4. Keyboard operation of the tools row, rail, and tab row + More
test: Tab through Search/Import/Export, the rail (or tab row + More at narrow widths), open More via keyboard.
expected: Visible focus ring at every stop, correct order, no trap (UX1-01).
result: issue
reported: "pass on desktop; fail on iPad with keyboard" / "details on test 4: no focus ring anywhere."
severity: major

### 5. The Notebook text companion against the No-Verdict Rule
test: View the rail's "NOTEBOOK" label and a Notebook row's place name in the real app.
expected: Mark confirms the red companion reads as intended.
result: pass

### 6. Rule on the four already-decided board departures (03.4-GAPS-CONFORMANCE.md)
test: Review the Departures section: the 12px tab label vs. the board's 10px; the rod/tally kept at every width; the absent 46px page-preview glyph; the row shipping with no versions-count cell.
expected: Mark confirms each is acceptable as shipped, or requests a follow-up plan.
result: issue
reported: "add a note about home: at 760px width, the lead row is unusable due to compression of name column and Next time column while action button stay on a single line centered in the row - need to adopt iPhone layout ealier in responsive ladder"
severity: major
rulings (2026-09-22): 6.1 keep — "test 6 text was 12px on iphone width". 6.2 fix — "we should remove the batch counter, the color rod and the Continue Developer button to be consistent with the artboard; we'll need to revisit the decision to include the color rod on all destination rows no matter the size". 6.3 keep — "we should remove that from the artboard - I don't believe that an arbitrary doc icon there adds any value" (todo filed). 6.4 fix — "it's not needed here, but there's another problem - the subtext below recipe name contains recipe grams - that's not needed either".

### 7. Rule on the three new conformance findings (03.4-GAPS-CONFORMANCE.md)
test: Review: `.shell__rail` has no padding/`border-right` at all; the rail's active item has no pill/bold treatment; `RowActions` always shows both actions at 393px where the board shows one.
expected: Mark confirms whether these are acceptable or should be closed later.
result: issue
reported: "test 7, #1 - fix. we should add to the app / test 7, #2 - keep. we have not settled radius in the design system for app context, only for sheet context; add that as a todo and we will revisit the rail once we have determined / test 7, #3 - fix. mentioned earlier, secondary button should be removed"
severity: minor
rulings (2026-09-22): 7.1 fix (rail padding + border-right). 7.2 keep (todo filed: settle App-context radius, then revisit the rail's active item). 7.3 fix (folded into G-03.4-6b).

### 8. REVIEW.md WR-01: the More disclosure's native marker on Safari/WebKit
test: On Safari/WebKit, check whether a native ▶/▼ triangle renders beside the custom MoreIcon and "More" label.
expected: No native marker glyph.
result: pass

### 9. REVIEW.md IN-03: elastic-overscroll colour on iOS/Safari
test: On iOS/Safari, scroll past the top/bottom of a non-Sheet route far enough to trigger the rubber-band effect and check its colour.
expected: The App's white, not the Sheet's cream.
result: issue
reported: "Fail. It's cream on all pages that I tested."
severity: cosmetic

## Summary

total: 9
passed: 3
issues: 6
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-03.4-1
  truth: "The app loads promptly on the iPad (WebKit, coarse pointer); the tab row shows no overflow"
  status: failed
  reason: "User reported: pass on iPhone. fail on iPad - the page load time is measured in seconds"
  severity: major
  test: 1
  root_cause: "Environment plus code. (1) The iPad is served the Vite dev server over the LAN (192.168.1.133:5173, vite --host): a cold Home load is 64 unbundled modules, 6.22 MiB uncompressed, 68% of it inline base64 sourcemaps; the production build is 3 requests, ~134 KB gzipped. (2) Nothing paints until the whole graph resolves: index.html has no stylesheet link and an empty #root; main.jsx:13-18 top-level-awaits seedIfEmpty (an IndexedDB open) before createRoot; RecipeList returns null through two more IDB reads. Viewport width contributes nothing — the iPhone/iPad split is device throughput and CPU on ~2 MiB of unminified dev React, not a wide-width code path. Amplifier: two dev servers share app/node_modules/.vite/deps, so a stale ?v= hash 504s and forces a full reload."
  artifacts:
    - path: "app/index.html"
      issue: "No stylesheet link, no shell markup, empty #root — blank until JS lands"
    - path: "app/src/main.jsx"
      issue: "Six stylesheets imported as JS; top-level await seedIfEmpty gates createRoot"
    - path: "app/src/ui/RecipeList.jsx"
      issue: "return null while versions === null keeps Home blank after mount"
    - path: "app/vite.config.js"
      issue: "No dev sourcemap or compression tuning; LAN devices get dev, not preview"
  missing:
    - "Standing rule: device UAT runs against `npm --prefix app run build && npm --prefix app run preview -- --host`, never dev; kill the duplicate dev server"
    - "Give the shell something to paint immediately (linked/inlined critical CSS and minimal shell markup in index.html)"
    - "Move seedIfEmpty off the first-paint path and replace RecipeList's return null with a loading state"
    - "Re-run the tab-row overflow clause of test 1 on the iPad once load clears — it was untested, not failed"
  debug_session: ".planning/debug/ipad-page-load-seconds.md"

- gap_id: G-03.4-3
  truth: "At iPhone width, choosing Search/Import/Export inside More closes the More panel; a completed import from a non-Home route returns to Home with the imported data showing"
  status: failed
  reason: "User reported: fail - when on Notebook tab in iphone width, More menu doesn't close when one of the menu options is clicked; export works (json file is written); import doesn't navigate back to home"
  severity: major
  test: 3
  root_cause: "Two halves. (A) Defect: More is an uncontrolled native <details> (Shell.jsx:270) with no open-state code at all — a <details> toggles only via its own <summary>, so activating a child never closes it; the details sits outside the Outlet so a route change reconciles it in place, still open. Traces to 03.4-03-PLAN.md decision 8 ('open/closed without any JavaScript'), which reasoned about opening, never dismissal. (B) Not a regression: no plan line asks Import to navigate — 03.4-03-PLAN.md:149 says bump storeRevision 'so a route below can reload its own data; do not reach into any route's state'. The UAT truth over-read 'Home reloads' as 'navigates to Home'. The real defect under (B) is silence: /notebook renders Placeholder, which never reads the Outlet context, so a successful import from a non-Home route changes nothing on screen; and .shell__import-errors lives in the header (Shell.jsx:226-232), unreachable at 393px, contradicting shell.css:327-331's own rationale."
  artifacts:
    - path: "app/src/ui/Shell.jsx"
      issue: "Uncontrolled <details> (270-301); handleImportChange has no success signal (171-191); error list stranded in the header (226-232)"
    - path: "app/src/ui/Placeholder.jsx"
      issue: "Ignores useOutletContext, so no non-Home route reflects an import"
    - path: "app/src/styles/shell.css"
      issue: "Narrow block folds the controls into More (327-334) while the feedback channel stays in the header"
  missing:
    - "(A) Close More on item activation (one onClick on the <ul>, ref or controlled open) AND on useLocation().pathname change; return focus to the summary after Import/Export; amend 03.4-03-PLAN.md decision 8; follow History.jsx's HistoryDisclosure pattern"
    - "(B) Mark rules: navigate('/') after a successful import (Option 1), or a role=status live region beside the controls announcing 'Imported N recipes' / errors and stay put (Option 2, also fixes invisible failures)"
    - "(B) Either way, move or mirror .shell__import-errors so it is on-screen at 393px"
  debug_session: ".planning/debug/more-panel-stays-open-import-no-home.md"

- gap_id: G-03.4-4
  truth: "On the iPad with a hardware keyboard, tabbing through the tools row, tab row and More shows a visible focus ring at every stop in the correct order with no trap"
  status: failed
  reason: "User reported: pass on desktop; fail on iPad with keyboard. details on test 4: no focus ring anywhere."
  severity: major
  test: 4
  root_cause: "Most likely a device behaviour, not the app: iPadOS Safari routes Tab only to text inputs and selects unless Full Keyboard Access is on (Settings > Accessibility > Keyboards); every test-4 stop is an <a>, <button> or <summary>, so focus never lands and no ring can paint. Two app-side facts compound it: rings are authored only through the single :focus-visible rule (app.css:21-24) with no :focus fallback, so 'no ring' and 'focus never arrived' are indistinguishable; and the tab row + More are display:none above 760px (shell.css:247-263), so that arm of test 4 cannot be exercised on a 1366/1024 iPad at all. No stylesheet hides outlines under coarse-pointer media (binder.test.js:114-117 forbids outline:none)."
  artifacts:
    - path: "app/src/styles/app.css"
      issue: "Single :focus-visible mechanism, no documented fallback for a platform that never sets keyboard modality (one prior scoped :focus escape exists at app.css:593-607)"
    - path: "app/src/styles/shell.css"
      issue: "Tab row + More are width-keyed hidden above 760px, so the narrow arm of test 4 is unexecutable on the iPad"
  missing:
    - "Device check first: confirm Full Keyboard Access on the iPad; if off, record a platform note against UX1-01 rather than a code change (product decision for Mark)"
    - "If focus does land but :focus-visible is false on WebKit: add a narrow, documented second :focus exception in the shape app.css:604 already establishes — not blanket :focus"
    - "Split test 4's truth by device so the tab row + More arm is not asserted on a 1366px iPad"
  debug_session: ".planning/debug/ipad-keyboard-no-focus-ring.md"

- gap_id: G-03.4-6
  truth: "The Home lead row stays usable just above the 760px step: the name and Next time columns are not crushed while the actions sit on one centred line; the stacked (phone) layout takes over earlier on the responsive ladder"
  status: failed
  reason: "User reported: add a note about home: at 760px width, the lead row is unusable due to compression of name column and Next time column while action button stay on a single line centered in the row - need to adopt iPhone layout ealier in responsive ladder"
  severity: major
  test: 6
  root_cause: "Three conditions across the whole 760-1100px band: home.css has exactly one width step (759.98px), so the four-child flex row must hold from 760px up; the actions are flex 0 0 auto (home.css:90-92) and never yield — measured constant at 219px (Tasted) or 344px (Awaiting tasting) from 760 to 1440px — while identity and next are flex 1 1 0 residue; and 430px is spent before either column lays out (224px rail + 96px gutters + padding, border, rod, gaps). column = (W - 430 - A) / 2, matching measurements to 0.01px. At 760px identity gets 55px (Tasted) or 0px (Awaiting); the name's min-content is 86px, so it overflows into the hand note. The row form only reads from ~1040-1160px. Also: .home__lead-name lacks the overflow-wrap: anywhere its row sibling has (home.css:117-123), and separately .home__row's desktop grid overflows the page at 760-820px (scrollWidth 778 at 760) — a distinct defect the lead fix does not touch."
  artifacts:
    - path: "app/src/styles/home.css"
      issue: "Lead flex contract (63-92) has no stack step above 759.98px; .home__lead-name missing overflow-wrap (117-123); .home__row grid (169-180) overflows at 760-820px"
    - path: "app/src/styles/home.test.js"
      issue: "Line 51 asserts the only media condition is 759.98px — must be updated deliberately with a positive pin for the new step"
    - path: "app/src/styles/shell.css"
      issue: "224px rail stays visible down to 760px (line 115), part of the 430px fixed cost"
  missing:
    - "Stack the lead below the ladder's already-named 1099.98px step (DESIGN.md 348-364, app.css:2321): flex-direction column, align-items stretch, row-gap — verified by injection to give one-line name and note at every width in the band"
    - "Decide the rod in a stacked lead (hide it as the 759.98px step does, consistent with ruling 6.2) and the stacked actions' alignment (natural width left-aligned vs full-width) explicitly"
    - "Add overflow-wrap: anywhere to .home__lead-name"
    - "Decide the rows' behaviour in the 760-900px band or file the row overflow as its own gap"
    - "Update home.test.js:51 with a positive assertion for the 1099.98px lead rule"
  debug_session: ".planning/debug/home-lead-row-crushed-near-760.md"

- gap_id: G-03.4-6b
  truth: "At 393px a Home row matches board 171: no colour rod, no batch tally, and only the filled action — the secondary action is not rendered (rulings 6.2 and 7.3); DESIGN.md's 'rail on every list' rule is revisited for phone width"
  status: failed
  reason: "User reported: we should remove the batch counter, the color rod and the Continue Developer button to be consistent with the artboard; we'll need to revisit the decision to include the color rod on all destination rows no matter the size / secondary button should be removed"
  severity: major
  test: 6
  root_cause: "Three width-agnostic pieces render at 393px where board 171 draws none: the row's rod span (RecipeList.jsx:227, .home__rail) has no phone-width override — only the lead's rod is hidden (home.css:344); the tally (RecipeList.jsx:233, .home__meta, grid area 'rail tally tally' at home.css:316-323) is kept by 03.4-07 decision #2; and RowActions (RecipeList.jsx:166-206) always renders the secondary Link (lines 184, 196) at every width, with the 759.98px block only stretching both (home.css:325-331). Ruled by Mark 2026-09-22: the board wins at phone width; DESIGN.md's 'rail on every list' rule needs a phone-width exception."
  artifacts:
    - path: "app/src/ui/RecipeList.jsx"
      issue: "RowActions renders the secondary action unconditionally; the row's rod and tally have no width awareness (CSS decides)"
    - path: "app/src/styles/home.css"
      issue: "The 759.98px media block hides only .home__lead .home__rail, keeps the tally grid row, and stretches both actions instead of dropping the secondary"
    - path: "DESIGN.md"
      issue: "App marks rule says the rail stays on every list at every width — needs the phone-width exception Mark ruled"
  missing:
    - "At 393px hide the row's rod and the tally row (CSS in the existing 759.98px block, grid areas reduced to place/name/standing/actions)"
    - "At 393px render only the filled action — hide .home__action--secondary in the same media block (CSS) rather than branching JSX on width"
    - "Amend DESIGN.md's App marks rule and 03.4-GAPS-CONFORMANCE.md's departure #2 to record the phone-width exception"
    - "Update the style-contract tests in home.test.js and RecipeList.test.jsx that pin the kept rod/tally/both-actions behaviour"
  debug_session: ""  # cause known from 03.4-GAPS-CONFORMANCE.md rows 28, 31, 32; no debug agent needed

- gap_id: G-03.4-6c
  truth: "The lead block's meta line under the recipe name does not carry the recipe's gram figures"
  status: failed
  reason: "User reported: the subtext below recipe name contains recipe grams - that's not needed either"
  severity: minor
  test: 6
  root_cause: "HomeLead (RecipeList.jsx:100-113) computes a mass line from computeBalance and appends it to the meta paragraph: `{versionIdentity(...)} · {massLine}` renders e.g. 'Version 1 · 50 g oil · 800 g · 799.7 g'. Board 170's meta is 'Version 3 · Less oil · awaiting tasting · last batch 14 Sep' — version, label, standing, last batch date, no grams. (The seed version's own label '50 g oil · 800 g' also carries grams; that is data, not code.)"
  artifacts:
    - path: "app/src/ui/RecipeList.jsx"
      issue: "massLine computed and rendered in .home__lead-meta (lines 100-101, 111-113)"
  missing:
    - "Drop massLine (and the now-unused computeBalance/activeRows imports if nothing else uses them) from HomeLead's meta line"
    - "Decide whether to add the board's standing and last-batch date to the meta line, or leave it as version · label"
    - "Update RecipeList.test.jsx assertions that expect the gram figure"
  debug_session: ""  # cause read directly from source; no debug agent needed

- gap_id: G-03.4-7
  truth: "At desktop width the rail carries board 170's inner padding and a hairline right border dividing it from the page"
  status: failed
  reason: "User reported: test 7, #1 - fix. we should add to the app"
  severity: minor
  test: 7
  root_cause: ".shell__rail (shell.css:115-120) declares only flex-basis/width/flex-direction; no padding rule and no border-right rule exist anywhere in shell.css. Board 170 line 30 draws padding 8px 20px 32px 30px and border-right 1px solid #d6dad7 (the --app-divider token)."
  artifacts:
    - path: "app/src/styles/shell.css"
      issue: ".shell__rail has no padding and no border-right"
  missing:
    - "Add border-right: var(--app-rule-row) solid var(--app-divider) and rail padding read from gap tokens to .shell__rail"
    - "Pin both declarations in shell.test.js"
    - "Update 03.4-GAPS-CONFORMANCE.md row 3 to 'matches'"
  debug_session: ""  # cause known from 03.4-GAPS-CONFORMANCE.md row 3; no debug agent needed

- gap_id: G-03.4-9
  truth: "On iOS Safari the elastic-overscroll colour behind a non-Sheet route is the App's white, not the Sheet's cream"
  status: failed
  reason: "User reported: Fail. It's cream on all pages that I tested."
  severity: cosmetic
  test: 9
  root_cause: "The overscroll region paints the CSS canvas, and the canvas is cream on every route: no html background exists anywhere in app/src/styles, so body's background propagates to the canvas; app.css:5-11 sets body { background: var(--sheet-ground) } unconditionally; and the App white sits only on .shell (shell.css:20-26), a descendant that can never reach the canvas. Commit 4105848 (03.4-06) retired home.css's body:has(.home) rule — the only canvas-level App ground — and moved the paint to .shell (regression for Home; pre-existing gap for the other six App routes). Verified in Chromium by pixel sampling: body's background fills the canvas outside any descendant; a body:has() switch re-propagates on SPA route change."
  artifacts:
    - path: "app/src/styles/app.css"
      issue: "body background is the Sheet cream unconditionally (lines 5-11)"
    - path: "app/src/styles/shell.css"
      issue: ".shell's --app-background is correct in-page but structurally cannot paint the canvas (20-26)"
    - path: "app/src/styles/shell.test.js"
      issue: "Line 59 only asserts some rule declares --app-background; the deleted home.test.js viewport-ground guard is not replaced"
  missing:
    - "Make body's background per-context: body { background: var(--app-background) } as default, body:has(.recipe-page, .page-head, .not-found) { background: var(--sheet-ground) } for the paper frames; keep html unstyled; rule lives in app.css's prelude (home.test.js forbids home.css rules outside .home)"
    - "Design confirmation: the per-context rule keeps cream behind the three paper frames; a flat body default would turn their overscroll white — check whether iOS also tints the status-bar band from the canvas"
    - "Add a CSS test pinning that the App ground is declared on body, not only on a descendant; verify the fix on the iPhone/iPad (WebKit only shows rubber-band)"
  debug_session: ".planning/debug/ios-overscroll-cream-on-app-routes.md"
