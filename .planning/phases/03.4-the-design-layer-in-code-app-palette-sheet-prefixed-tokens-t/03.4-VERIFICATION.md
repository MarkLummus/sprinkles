---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
verified: 2026-09-22T20:15:00Z
status: human_needed
score: 33/33 truths verified (0 present, behaviour-unverified)
covered_files:
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/debug/ipad-tab-never-enters-app.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-09-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-09-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-10-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-10-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-12-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-12-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-13-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-13-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-14-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-14-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-CONTEXT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-DISCUSSION-LOG.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-GAPS-CONFORMANCE.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-PATTERNS.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-RESEARCH.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-SECURITY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UAT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UI-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js
  - CLAUDE.md
  - DESIGN.md
  - app/public/fonts/OFL.txt
  - app/public/fonts/caveat-regular.woff2
  - app/src/domain/lastEvent.js
  - app/src/domain/lastEvent.test.js
  - app/src/main.jsx
  - app/src/router.jsx
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/fonts.css
  - app/src/styles/history.css
  - app/src/styles/home.css
  - app/src/styles/home.test.js
  - app/src/styles/shell.css
  - app/src/styles/shell.test.js
  - app/src/styles/tokens.css
  - app/src/styles/tokens.test.js
  - app/src/ui/GraduatedRule.jsx
  - app/src/ui/Placeholder.jsx
  - app/src/ui/RecipeList.jsx
  - app/src/ui/RecipeList.test.jsx
  - app/src/ui/Shell.jsx
  - app/src/ui/Shell.test.jsx
covered_digest: "v1:sha256:e4c7a94167612b1cae4e396a51b83cf8d755b5a028d4c5364e3c444f7fea26c8"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 27/28 truths verified (1 present, behaviour-unverified — the More panel's open/close state transition)
  gaps_closed:
    - "G-03.4-r3-3 (iPad Tab never enters the app — the TabsToLinks/Full-Keyboard-Access link gate on WebKit): every shell__place JSX site (ten sites, nineteen rendered stops) now carries an explicit tabIndex={0}, routing WebKit's HTMLAnchorElement::isKeyboardFocusable and HTMLFormControlElement::isKeyboardFocusable straight to Element::isKeyboardFocusable, past both platform gates. Shell.test.jsx's new describe block pins the rendered markup (not source text): 19 stops at tabindex=\"0\", the hidden file input alone at tabindex=\"-1\". Confirmed by direct grep of the current source and by re-running the two-test acceptance criteria; unaffected by trusting 03.4-14-SUMMARY.md's own claims."
    - "The falsified round-two premise is corrected, not silently left standing: shell.css's comment above .shell__place:focus no longer attributes a measurement to Mark and states plainly that the rule is a ring for when focus arrives, not what brings it in; 03.4-09-SUMMARY.md gained a dated Correction section naming G-03.4-r3-3 without rewriting its original frontmatter, key-decisions, or coverage. Both confirmed by direct grep and read, not by SUMMARY claim."
    - "G-03.4-9 (iOS overscroll cream), G-03.4-7 (rail's missing edge), G-03.4-6/6b/6c (Home lead/row crush 760-1100px and the 393px phone row), and G-03.4-3 half A (More's dismissal) — all four were left in the prior VERIFICATION.md as structurally-correct-but-device-unconfirmed. 03.4-UAT.md's round-three device pass (tests 1, 2, 4, 5) now confirms all four pass on Mark's own device, closing them end to end rather than only at the code level. This is device evidence recorded in 03.4-UAT.md, not a claim from any plan's SUMMARY."
    - "G-03.4-1 (iPad cold load) — 03.4-UAT.md round-three test 6 confirms pass on the actual device, closing the last of the code-level fixes end to end."
    - "The still-open conformance departure #5 (single filled action stretching full-width at 393px) — 03.4-UAT.md round-three test 7 records Mark's ruling: keep, as shipped. No longer an open human-verification item."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory:
  - finding: "REVIEW.md's WR-01 (carried forward, unaffected by plan 14): the comment above Shell.jsx's tab row still claims focus returns to the summary 'in both cases' (item activation and route change), but the pathname-keyed effect (Shell.jsx:161-163) only calls setMoreOpen(false) — it never calls .focus(). The click path (closeMore) does call moreSummaryRef.current?.focus(); the route-change path does not."
    category: other
    reason: "Plan 03.4-14 did not touch this comment or this effect (confirmed by diff — only tabIndex attributes and the Shell() doc comment changed). The finding is unaffected by this round; still a documentation-accuracy defect, not functional, and does not block the phase goal."
    evidence_status: "grep-confirmed: the comment text (Shell.jsx:289-290) and the effect body (Shell.jsx:161-163) are unchanged since the prior verification"
  - finding: "REVIEW.md's WR-02 (carried forward): shell.css has no rule suppressing summary::-webkit-details-marker on the More disclosure."
    category: other
    reason: "Unaffected by plan 14. UAT round two's test 8 found no native marker on Mark's device; the code-level risk is still technically present but contradicted by that device observation. Carried forward so the discrepancy is not lost."
    evidence_status: "grep-confirmed absence of a suppression rule in shell.css; contradicted by Mark's own device pass at 03.4-UAT.md (round two) test 8"
behavior_unverified_items: []
coincidental_reliance_items: []
human_verification:
  - test: "The truth split at the Safari boundary, G-03.4-r3-3 (plan 03.4-14's own <verification> UAT note for the re-verifier) — (a) assertable on the iPad: serve the build (`npm --prefix app run build && npm --prefix app run preview -- --host`, port 4173), open on the iPad with the hardware keyboard attached and Full Keyboard Access on. On Home, tap once on the page body away from any control, then press Tab repeatedly. (b) recorded from the device, not asserted: force-quit Safari with FKA already on, reopen the preview build, tap the address bar, and press Tab until focus leaves the chrome."
    expected: "(a) A ring at Search, Import, Export, Home, Notebook, Recipe book, Idea log, Ingredients, Kitchen, in that order, then on into the page's own controls; Shift-Tab walks back; nothing traps. Fail the gap only on (a) — the tab row and More are display:none above 760px, so that arm is exercised at phone width, not on the 1366px iPad. (b) Record, do not assert, which of three things happens: a ring on Search (hand-off works, fix complete end to end); rings on Import/Export only (the link attribute did not ship — a code failure if seen, impossible after this plan); or focus cycles the chrome and never enters the page (Safari does not hand focus to the web view in this configuration on this device — (a) still holds if tap-then-Tab works; write the observation as a note on the gap, do not fail it)."
    why_human: "This is the device-side half of G-03.4-r3-3 the debug session (.planning/debug/ipad-tab-never-enters-app.md) could not resolve from WebKit source alone: whether Safari's UIKit focus loop (`_webViewCanBecomeFocused:`, closed-source) actually hands focus to the WKContentView in Mark's configuration, and whether Full Keyboard Access's KeyboardAccessFull value reached Safari's web process. The code-level fix (explicit tabindex on every stop, giving the hand-off a focus candidate) is confirmed present and test-pinned in this verification; its actual result on Mark's iPad — the only environment this gap was ever reported from — is unmeasured since the fix shipped. The device probe Mark ran before the fix (recorded in the debug session, 2026-09-22) found the page received no DOM keydown/focus event at all under FKA, which is exactly consistent with 'nothing keyboard-focusable to hand off to' — the state this plan's fix was built to change; that prediction has not yet been re-tested."
---

# Phase 03.4: The design layer in code: App palette, Sheet-prefixed tokens, the hand, and Home — Verification Report

**Phase Goal:** The approved design layer of 2026-09-21 lands in code: tokens.css carries the App palette and the Sheet tokens under the `sheet-` prefix (contract suites included), the five `app-*-text` companions, and the hand (Caveat, with forced-colours and print fallback); then Home is reworked per route.md "Active work first" using the App marks grammar in DESIGN.md.
**Verified:** 2026-09-22T20:15:00Z
**Status:** human_needed
**Re-verification:** Yes — third gap-closure round, after 03.4-UAT.md's round-three device pass (6 of 7 tests pass, 1 issue) surfaced G-03.4-r3-3 (Tab never enters the app on the iPad), diagnosed in `.planning/debug/ipad-tab-never-enters-app.md`, and closed by 03.4-14 (`gap_closure: true`, commits `6e2dff8`..`82f7cbd`).

## Re-verification Summary

The prior VERIFICATION.md (2026-09-22T17:45:00Z) recorded `human_needed`, 27/28 truths verified, with one truth (the More panel's open/close state transition) present-but-behaviour-unverified and seven human-verification items outstanding — four confirmed-broken defects awaiting re-test since their code fixes, two rendered-spacing facts, and one open design ruling.

Mark then ran a third UAT pass against that report (03.4-UAT.md, this phase's current file): 6 of 7 items passed outright — the iOS overscroll colour (G-03.4-9), the rail's edge at 1280px (G-03.4-7), the Home lead/row band from 760-1100px and the 393px phone row (G-03.4-6/6b/6c), More's dismissal at 393px end to end (G-03.4-3 half A), the cold load on the iPad (G-03.4-1), and the departure #5 ruling (kept as shipped) — and 1 surfaced a real regression: "fail. I see the focus move on the browser chrome only — focus never moves into the app" (test 3). That is a *harder* failure than the G-03.4-4 gap 03.4-09 had closed in round two (then: focus landed with no visible ring; now: focus never entered the document at all), so it was logged as a new gap, G-03.4-r3-3, and root-caused in a dedicated debug session by reading WebKit's own source: seven of the nine desktop-arm stops are `<a href>` NavLinks with no explicit tabindex, and WebKit makes such a link keyboard-focusable only under the embedder's TabsToLinks preference — off on every Apple platform, with no iPadOS switch, and never touched by Full Keyboard Access, which widens only the form-control gate. Mark's own on-device probe (bookmarklet logging keydown/focusin against `document.activeElement`) found the page received no DOM event at all under Full Keyboard Access — consistent with there being no keyboard-focusable element to hand off to.

Plan 03.4-14 closed G-03.4-r3-3 at the code level: an explicit `tabIndex={0}` on all ten `shell__place` JSX sites (nineteen rendered stops), bypassing both the TabsToLinks and Full-Keyboard-Access gates per WebKit's own commit `89bf60bf9f`; a new `Shell.test.jsx` describe block pinning this on **rendered markup**, not source text (19 stops at `tabindex="0"`, the hidden file input alone at `tabindex="-1"`); a corrected `shell.css` comment that no longer attributes the falsified "focus lands" premise to a measurement; and a dated Correction section in `03.4-09-SUMMARY.md` naming the gap without rewriting the original record. **This re-verification re-checked every one of these claims against the current codebase, not against 03.4-14-SUMMARY.md's own claims** — by reading the actual JSX (`grep -cF 'tabIndex={0}'` = 10, `grep -cF 'tabIndex={-1}'` = 1), the actual test file, the actual CSS comment (`! grep -q "Mark measured"` succeeds, `grep -c ipad-tab-never-enters-app` = 1), the actual 03.4-09-SUMMARY.md correction, the actual commit diffs (`git diff 6e2dff8~1 82f7cbd` touches exactly the four declared files, and Shell.jsx's diff is exactly ten `tabIndex={0}` insertions plus a comment extension — no `focus()`, no `autoFocus`, no `onKeyDown`, no reordering), then running the full suite and the production build directly.

**All five of plan 03.4-14's must-haves are verified present, correct, and pinned in the current source.** What remains open is exactly what the plan's own `<verification>` section says must remain open until measured: whether Safari's chrome-to-page hand-off actually lands on the shell now that it has a focus candidate to find. That is the sole human-verification item below, phrased as the plan's own (a)/(b) split so it can be answered honestly rather than conflating the app's own behaviour with a closed-source Safari mechanism.

**No regressions found.** The full test suite passes cleanly at HEAD (41 files, 1147 tests — up from 1145 before this round's two new tests); the production build succeeds (126 modules, ~126 KB gzipped JS). `git diff` across the three plan-14 commits touches only the four files the plan declared (`app/src/ui/Shell.jsx`, `app/src/ui/Shell.test.jsx`, `app/src/styles/shell.css`, `03.4-09-SUMMARY.md`) — every one of the 27 previously-verified truths is unaffected by this round's code changes.

**One previously present-but-behaviour-unverified truth is upgraded to fully VERIFIED this round by direct device evidence**: truth 27, the More panel's open/close state transition (G-03.4-3 half A) — Mark's own UAT round-three test 5 ("More closes on item choice and on route change at 393px") reported `pass`, the strongest form of evidence this verifier can cite for a state-transition truth no unit test in this codebase's `renderToStaticMarkup` harness can exercise.

**Four other truths move from "structural, device-unconfirmed" to fully VERIFIED by the same round of device testing**, closing G-03.4-9 (iOS overscroll colour), G-03.4-7 (rail's edge), G-03.4-6/6b/6c (Home lead/row band and the 393px phone row), and G-03.4-1 (cold load) end to end. The single still-open design departure (#5, the filled action's full-width stretch at 393px) was ruled by Mark: keep, as shipped — no longer an open item.

Both advisory findings from the prior round (WR-01's Shell.jsx comment overclaim, WR-02's unsuppressed `<summary>` marker) are carried forward unchanged: plan 03.4-14's diff does not touch either location.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every Sheet-frame token in tokens.css takes the `--sheet-` prefix; every consumer follows it (D-01) | ✓ VERIFIED | unchanged by plan 14; regression-checked via the full suite pass |
| 2 | Shared sizes keep bare names; the App reads them directly (D-02) | ✓ VERIFIED | unchanged; regression-checked |
| 3 | tokens.css declares the approved App palette and the five `app-*-text` companions under `--app-` (D-03, D-19) | ✓ VERIFIED | unchanged; regression-checked |
| 4 | `tokens.test.js` fails the build if any `var()` read resolves to an undeclared token, or a token lacks its prefix | ✓ VERIFIED | unchanged; regression-checked |
| 5 | Caveat Regular ships as one vendored woff2 with its OFL licence, one `@font-face`, a floor-guarded `.app-hand` role class (D-17) | ✓ VERIFIED | unchanged; files present, build emits the font |
| 6 | Under `forced-colors: active` and in print, `.app-hand` falls back to the text face in italic (D-17) | ✓ VERIFIED | unchanged since prior round's device confirmation (03.4-UAT.md round-two test 2: pass) |
| 7 | Every route renders inside a shell that paints its own App ground rather than the Sheet's (D-09) | ✓ VERIFIED (device-confirmed this round) | mechanism unchanged since prior round (shell.css:23, app.css:23/31); **upgraded** — 03.4-UAT.md round-three test 1: "pass" — a real device confirms the rubber-band region is App white on Home/placeholder routes and Sheet cream on a recipe route |
| 8 | All five destinations are links; an unbuilt place opens a page styled through the App's own tokens (D-10) | ✓ VERIFIED | unchanged; regression-checked |
| 9 | Search appears in the tools row as a link and no search logic is built (D-13) | ✓ VERIFIED | unchanged; regression-checked |
| 10 | Import and Export keep their present behaviour via `storeRevision` (D-15) | ✓ VERIFIED (structural); real click-through partially confirmed | `handleImportChange`/`handleExport` untouched by plan 14 (confirmed by diff); Export confirmed working on-device in round-two UAT; Import's non-navigation from a non-Home route remains explicitly deferred by Mark, not a must-have |
| 11 | Below 759.98px the rail becomes a bottom tab row of five reachable tabs, More holding the rest (D-16) | ✓ VERIFIED | unchanged since prior round's device confirmation (03.4-UAT.md round-two test 1: pass on iPhone) |
| 12 | The recipe route's page-scoped notice still anchors to its own head — the shell introduces no new containing block | ✓ VERIFIED | `shell.test.js`'s containing-block guard still passes; plan 14 added no property among the five the guard checks |
| 13 | Home orders recipes by recency, newest first, with no stored flag (D-05) | ✓ VERIFIED | unchanged; regression-checked |
| 14 | The most recently touched recipe reads as a larger lead block above the list, and also appears in the list below (D-06, D-12) | ✓ VERIFIED | unchanged; regression-checked |
| 15 | The lead block shows the maker's Next time in the hand, in Sheet pen blue, the only place the hand appears (D-18, D-20) | ✓ VERIFIED | unchanged since prior round's device confirmation |
| 16 | Every recipe row carries its next action at its right edge, chosen by standing (D-07, D-11) | ✓ VERIFIED | unchanged; regression-checked |
| 17 | Home's Sprinkles Jar is fully retired (D-04) | ✓ VERIFIED | unchanged; regression-checked |
| 18 | A Sheet rule never recolours an App control or a rail place once a route has been visited (D-01/D-02 boundary, D-07, D-09, D-19) | ✓ VERIFIED | unchanged; regression-checked |
| 19 | The lead block matches the board: a bordered block with the rod, identity, a captioned Next time column, and the standing's own actions (D-06, D-07, D-12, D-18) | ✓ VERIFIED (structural, extended prior round) | unchanged by plan 14; regression-checked |
| 20 | Each recipe row matches the board's column grammar: rod, place-over-name, the standing word, the tally, then actions (D-07, D-11) | ✓ VERIFIED (structural, extended prior round) | unchanged by plan 14; regression-checked |
| 21 | On an App-context route the CSS canvas — what iOS Safari paints during elastic overscroll — is the App's white; on the three paper frames it stays the Sheet's cream, declared on `body` (G-03.4-9, D-01/D-02/D-03) | ✓ VERIFIED (device-confirmed this round) | mechanism unchanged (app.css:23/31); **upgraded** alongside truth 7 by the same 03.4-UAT.md round-three test 1 pass |
| 22 | At desktop width the rail is divided from the page by a hairline right border and carries the board's own inner padding, without growing past its 224px basis (G-03.4-7, D-09) | ✓ VERIFIED (device-confirmed this round) | mechanism unchanged (shell.css:121-129); **upgraded** — 03.4-UAT.md round-three test 2: "pass" |
| 23 | The `.shell__place:focus` fallback rule paints a ring once focus arrives at a shell stop; on its own it does nothing to bring focus into the document (G-03.4-4, corrected by G-03.4-r3-3) | ✓ VERIFIED | rule byte-for-byte unchanged since 03.4-09 (`shell.css:216-220`); its comment is now corrected (this round) to state exactly this scope, after round-three UAT (03.4-UAT.md test 3) falsified the original "focus lands without :focus-visible" premise the comment used to assert. The actual gate-bypass mechanism — what brings focus in — is the explicit tabindex, truth 29 below |
| 24 | Between 760px and 1100px the lead block reads with the recipe name on one line and the Next time note on one line, neither column crushed nor spilling; in the same band the rows take a stacked form so Home does not scroll sideways (G-03.4-6, D-06, D-11, D-18) | ✓ VERIFIED (device-confirmed this round) | mechanism unchanged (home.css:350-368); **upgraded** — 03.4-UAT.md round-three test 4: "pass" |
| 25 | At 393px a recipe row matches board 171 exactly: identity beside the standing word, then one filled action — no colour rod, no batch tally, no secondary action; the lead above it is untouched and keeps both its actions (G-03.4-6b, rulings 6.2/7.3, D-07, D-19, UX1-03) | ✓ VERIFIED (device-confirmed this round) | mechanism unchanged (home.css:404-414); **upgraded** alongside truth 24 by the same test 4 pass |
| 26 | The lead block's meta line under the recipe name carries the version identity and no computed gram figure (G-03.4-6c, ruling 6.4, D-19) | ✓ VERIFIED | unchanged; regression-checked |
| 27 | At phone width, choosing any of More's five items closes the panel; a route change closes it even when the change did not originate inside More; focus returns to the summary rather than the document body (G-03.4-3 half A, D-16, UX1-01) | ✓ VERIFIED (device-confirmed this round) | wiring unchanged (Shell.jsx:157-168, 291-295, untouched by plan 14's diff); **upgraded from PRESENT_BEHAVIOR_UNVERIFIED** — 03.4-UAT.md round-three test 5: "pass" (all four sub-cases: Ingredients, Export, Import, and a sibling-tab route change all closed the panel). A genuine state-transition truth, now confirmed by direct device observation rather than presence-and-wiring alone |
| 28 | Device UAT is served from a production build (documented), and the page paints something in the App's own voice before IndexedDB opens, with Home showing a loading line rather than nothing while its own reads are in flight (G-03.4-1 code half, D-06, D-08) | ✓ VERIFIED (device-confirmed this round) | mechanism unchanged (main.jsx, RecipeList.jsx); **upgraded** — 03.4-UAT.md round-three test 6: "pass" |
| 29 | Every shell stop — the tools row's Search/Import/Export, the rail's Home and five places, the tab row's Home and three places, More's summary, and the three More items — is keyboard-focusable on WebKit with no platform preference set: each carries an explicit `tabindex="0"`, the one page-side condition that routes `HTMLAnchorElement::isKeyboardFocusable` and `HTMLFormControlElement::isKeyboardFocusable` past the TabsToLinks and Full Keyboard Access gates (G-03.4-r3-3, UX1-01) | ✓ VERIFIED | `grep -cF 'tabIndex={0}' app/src/ui/Shell.jsx` = 10 (all ten JSX sites); `Shell.test.jsx`'s new describe block renders the page and asserts all 19 rendered `shell__place` opening tags (`<a>`/`<button>`/`<summary>`) contain `tabindex="0"` — test passes |
| 30 | Search is the document's first keyboard-focusable element on every route, so WebKit's chrome-to-page hand-off (`FocusController::setInitialFocus`) has something to land on | ✓ VERIFIED (structural) | confirmed by direct read of Shell.jsx's render order: the tools row (Search first, then Import, then Export) is the first focusable content inside `.shell__head`, itself the first child rendered; no focusable element precedes it in the DOM. The actual chrome-to-page hand-off on Mark's device is the open human-verification item below |
| 31 | The pin is on rendered markup, not source text: every element carrying the `shell__place` class in the static render carries `tabindex="0"`, and the hidden file input alone keeps `tabindex="-1"` | ✓ VERIFIED | `Shell.test.jsx`'s new tests call `renderAt('/')` (`renderToStaticMarkup`) and regex the actual HTML output, not `Shell.jsx`'s source — confirmed by direct read of the test file; both tests pass |
| 32 | No record still cites the falsified premise as measured: `shell.css`'s comment at the `:focus` fallback and `03.4-09`'s SUMMARY both state that round two's "focus lands without `:focus-visible`" was an inference, that round three showed focus never entered the page, and that the rule is the ring for when focus does arrive | ✓ VERIFIED | `! grep -q "Mark measured" app/src/styles/shell.css` succeeds; `grep -c ipad-tab-never-enters-app app/src/styles/shell.css` = 1; `03.4-09-SUMMARY.md`'s new `## Correction — 2026-09-22 (G-03.4-r3-3)` section states the inference/falsification narrative directly (confirmed by direct read), while its original frontmatter, key-decisions and coverage blocks are untouched (`grep -c "The focus fallback is one scoped"` = 1, `grep -c "human_judgment: true"` = 3) |
| 33 | The UAT truth is split so it can pass or fail honestly: (a) with focus already in the page, Tab rings every stop in order with no trap — assertable on the iPad; (b) Tab from Safari's own chrome entering the page — recorded from the device probe, not asserted | ✓ VERIFIED (documented) | 03.4-14-PLAN.md's own `<verification>` section contains the split verbatim, addressed to "the re-verifier"; carried into this report's sole human-verification item below, worded to preserve the (a)/(b) distinction rather than conflating them into one pass/fail |

**Score:** 33/33 truths verified (0 present, behaviour-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/ui/Shell.jsx` | explicit tabindex on every shell stop | ✓ VERIFIED | ten `tabIndex={0}` sites (nineteen rendered stops) confirmed by grep and direct read; the hidden file input's `tabIndex={-1}` untouched; comment above `export function Shell()` extended in prose, no code |
| `app/src/ui/Shell.test.jsx` | rendered-markup pin for 19 stops + the -1 input | ✓ VERIFIED | new describe block confirmed present and passing (`findShellPlaceTags` + two tests) |
| `app/src/styles/shell.css` | corrected `.shell__place:focus` comment, rule unchanged | ✓ VERIFIED | comment rewritten (confirmed by diff); selector/declarations byte-for-byte unchanged; still exactly one `@media` block, one `:focus` rule |
| `03.4-09-SUMMARY.md` | dated Correction section, original record untouched | ✓ VERIFIED | `## Correction — 2026-09-22 (G-03.4-r3-3)` present; original frontmatter/key-decisions/coverage confirmed unchanged |
| `.planning/debug/ipad-tab-never-enters-app.md` | diagnosis + device probe result | ✓ VERIFIED | present, status `diagnosed`, contains the device probe result Mark ran before the fix shipped |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `tabIndex={0}` on each JSX stop | `tabindex="0"` on the rendered `<a>`/`<button>`/`<summary>` | React's attribute serialization | ✓ WIRED | confirmed on rendered markup via `renderToStaticMarkup`, not source inspection |
| `RailPlace`'s one `tabIndex={0}` | the ten rail/tab-row/More place links | shared component | ✓ WIRED | `RailPlace` (Shell.jsx:127-135) is invoked ten times (rail: 3 + 2 sliced across the divider; tab row: 3 sliced; More: 2, PLACES[3]/[4]) and each call renders the attribute; confirmed by the 19-stop total matching the plan's own derivation (3 tools + 6 rail + 4 tab row + 1 summary + 5 More items) |
| the corrected `shell.css` comment | the next reader of `.shell__place:focus` | prose | ✓ WIRED | comment now states the rule paints after arrival and does nothing to bring focus in; confirmed by direct read |
| Search's position in the DOM | the chrome-to-page hand-off (`FocusController::setInitialFocus`) | render order | ✓ WIRED (structural; device hand-off is the open human-verification item) | confirmed: no focusable element precedes Search in Shell.jsx's render tree |

### Requirements Coverage

No requirement IDs are mapped to this phase in REQUIREMENTS.md (a design-delivery phase governed by CONTEXT.md's D-01..D-20, confirmed by grep — no "03.4" row exists there). UX1-01 and UX1-03 apply as build constraints, verified end-to-end in Phase 4:

| Requirement | Description | Status | Evidence |
|-------------|--------------|--------|----------|
| UX1-01 | Keyboard-operable, visible focus, WCAG 2.2 AA contrast | ? NEEDS HUMAN | the tabindex fix (G-03.4-r3-3) is now present, pinned on rendered markup, and structurally correct (Search is first in DOM order); whether Safari's chrome-to-page hand-off actually lands on it on Mark's iPad remains the sole open device check, carried to human verification |
| UX1-03 | No colour-only signal | ✓ SATISFIED | unchanged from prior round; every standing states its word explicitly |

No orphaned requirements found for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/src/ui/Shell.jsx` | comment above the tab row (~289-290) | Comment claims focus always returns to More's summary "in both cases"; the route-change effect (161-163) does not call `.focus()`, only the click path does | ⚠️ Warning (WR-01, carried forward — 03.4-REVIEW.md, confirmed still present, untouched by plan 14) | documentation-accuracy defect, not functional; recorded as advisory above |
| `app/src/styles/shell.css` | `.shell__more`/`.shell__more > ul` | The More disclosure's native `<summary>` marker (`::-webkit-details-marker`) is still never suppressed | ℹ️ Info (WR-02, carried forward, still technically present) | round-two UAT found no visible marker on Mark's device — a contradicting real observation recorded as advisory, not a live defect |
| `app/src/ui/RecipeList.jsx` | 109, 229 (unchanged) | `entry.name` rendered with no fallback for a blank/whitespace name | ⚠️ Warning (WR-03/prior WR-02, carried forward) | unchanged; no upstream validation reviewed |
| `app/src/styles/home.css` | 357-359 and 434-436 | `.home__lead .home__rail { display: none; }` declared identically in both the 1099.98px and 759.98px blocks — the narrower query is a strict subset, so the second declaration is a dead duplicate | ℹ️ Info (IN-01, 03.4-REVIEW.md) | harmless today; unaffected by plan 14 |
| `app/src/ui/RecipeList.test.jsx` | 266, 274 (unchanged) | greedy, unanchored regex assertions | ℹ️ Info (IN-02, carried forward) | currently correct by coincidence of markup shape |
| `app/src/ui/RecipeList.jsx` | 43 | the `.home__loading` paragraph carries no `aria-live`/`role="status"` | ℹ️ Info (IN-03, 03.4-REVIEW.md) | a screen-reader user gets no announcement of the loading-to-loaded transition; minor |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK` markers found in `app/src/ui/Shell.jsx`, `app/src/ui/Shell.test.jsx`, or `app/src/styles/shell.css` — the three source files plan 14 changed (confirmed by direct grep). The one `PLACEHOLDER` match in the codebase remains `Placeholder.jsx`'s component name, not a debt marker.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm --prefix app test -- --run` | 1147 passed, 0 failed, 41 files | ✓ PASS |
| Production build | `npm --prefix app run build` | 126 modules transformed, succeeded, ~126 KB gzipped JS | ✓ PASS |
| Explicit tabindex count (G-03.4-r3-3) | `grep -cF 'tabIndex={0}' app/src/ui/Shell.jsx` | 10 | ✓ PASS |
| Negative tabindex count (G-03.4-r3-3) | `grep -cF 'tabIndex={-1}' app/src/ui/Shell.jsx` | 1 | ✓ PASS |
| Rendered-markup pin (G-03.4-r3-3) | `npm --prefix app test -- src/ui/Shell.test.jsx src/styles/shell.test.js` | 49/49 pass | ✓ PASS |
| Falsified premise removed | `! grep -q "Mark measured" app/src/styles/shell.css` | succeeds (no match) | ✓ PASS |
| Debug session named in the comment | `grep -c ipad-tab-never-enters-app app/src/styles/shell.css` | 1 | ✓ PASS |
| Focus rule unchanged | `grep -c "^.shell__place:focus {" app/src/styles/shell.css` | 1 | ✓ PASS |
| One @media block preserved | `grep -c "@media" app/src/styles/shell.css` | 1 | ✓ PASS |
| 03.4-09 record correction | `grep -c "G-03.4-r3-3" 03.4-09-SUMMARY.md` | 1 | ✓ PASS |
| 03.4-09 original record intact | `grep -c "The focus fallback is one scoped" 03.4-09-SUMMARY.md`; `grep -c "human_judgment: true" 03.4-09-SUMMARY.md` | 1; 3 | ✓ PASS |
| Diff scope | `git diff 6e2dff8~1 82f7cbd --stat` | exactly the 4 declared files (79 insertions, 15 deletions) | ✓ PASS |
| Commits present | `git log --oneline` for 6e2dff8, 7092f67, 82f7cbd | all 3 present | ✓ PASS |

### Probe Execution

`03.4-gaps-conformance-probe.js` is unchanged this round — a browser-paste reader, not a `scripts/*/tests/probe-*.sh` shell probe. Not executed by this verifier, matching prior rounds' own finding — this is not a gap.

## Human Verification Required

### 1. The truth split at the Safari boundary (G-03.4-r3-3)

**Test — (a), assertable, closes the gap:** Serve the build (`npm --prefix app run build && npm --prefix app run preview -- --host`, port 4173) and open it on the iPad with the hardware keyboard attached and Full Keyboard Access on. On Home, tap once on the page body away from any control, then press Tab repeatedly.
**Expected (a):** A ring at Search, Import, Export, Home, Notebook, Recipe book, Idea log, Ingredients, Kitchen, in that order, then on into the page's own controls; Shift-Tab walks back; nothing traps.

**Test — (b), recorded, not asserted:** Force-quit Safari with FKA already on, reopen the preview build, tap the address bar, and press Tab until focus leaves the chrome.
**Expected (b):** One of three outcomes, recorded as a note rather than a pass/fail: a ring on Search (hand-off works, fix complete end to end); rings on Import/Export only (the tabindex fix did not ship — a code failure, impossible after this round); or focus cycles the chrome and never enters the page (a Safari/device behaviour outside the app's control — (a) still holds if tap-then-Tab works).

**Why human:** The code-level fix — an explicit tabindex giving WebKit's chrome-to-page hand-off something to find — is confirmed present, pinned on rendered markup, and structurally sound (Search is first in DOM order). Whether Safari's closed-source `_webViewCanBecomeFocused:`/`_webView:takeFocus:` mechanism actually uses that candidate on Mark's own iPad, the only device this gap was ever reported from, is unmeasured since the fix shipped. Mark's pre-fix device probe (recorded in the debug session) found zero DOM events under FKA — consistent with "nothing to hand off to," which is exactly the state this fix changes; the probe has not been re-run.

## Gaps Summary

All items from the prior round's human-verification list are now resolved, except one:

1. **G-03.4-9 (iOS overscroll cream)** — closed, device-confirmed (03.4-UAT.md round-three test 1: pass).
2. **G-03.4-7 (rail's missing edge)** — closed, device-confirmed (test 2: pass).
3. **G-03.4-4 → G-03.4-r3-3 (focus ring / focus never entering the app)** — round three found a harder failure than round two's fix addressed (focus never reached the document, not just no ring). Root-caused to WebKit's TabsToLinks/Full-Keyboard-Access link gate via direct source reading. Closed at the code level by 03.4-14's explicit tabindex on every shell stop, confirmed present and pinned by this verification. **The device-side half — whether Safari's hand-off actually uses it — remains the one open item, listed above.**
4. **G-03.4-6/6b/6c (lead/row crush 760-1100px, 393px phone row)** — closed, device-confirmed (test 4: pass).
5. **G-03.4-3 half A (More stays open after selection)** — closed, device-confirmed (test 5: pass). This truth is now fully VERIFIED, upgraded from present-but-behaviour-unverified in the prior round.
6. **G-03.4-1 (iPad cold load)** — closed, device-confirmed (test 6: pass).
7. **Conformance departure #5 (filled action stretches full-width at 393px)** — ruled by Mark: keep, as shipped (test 7). No longer an open item.

Two advisory findings (WR-01, WR-02) carry forward unchanged — plan 03.4-14's diff does not touch either location, and neither blocks the phase goal.

This phase's status remains **human_needed**, not `passed`, for exactly one reason: the device-side confirmation of G-03.4-r3-3's fix. Every other item this phase has ever raised is now either fully verified in source and on-device, or explicitly ruled/deferred by Mark. This is the narrowest the human-verification list has been across all three rounds of this phase's verification.

---

_Verified: 2026-09-22T20:15:00Z_
_Verifier: Claude (gsd-verifier)_
