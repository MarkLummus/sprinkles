---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
verified: 2026-09-22T04:30:00Z
status: human_needed
score: 19/20 truths verified (1 present, behaviour-unverified — the tab row's fit at 393px)
covered_files:
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-CONTEXT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-DISCUSSION-LOG.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-GAPS-CONFORMANCE.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-PATTERNS.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-RESEARCH.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-SECURITY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UI-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js
  - CLAUDE.md
  - DESIGN.md
  - app/public/fonts/OFL.txt
  - app/public/fonts/caveat-regular.woff2
  - app/src/domain/lastEvent.js
  - app/src/domain/lastEvent.test.js
  - app/src/main.jsx
  - app/src/router.jsx
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/fonts.css
  - app/src/styles/history.css
  - app/src/styles/home.css
  - app/src/styles/home.test.js
  - app/src/styles/shell.css
  - app/src/styles/shell.test.js
  - app/src/styles/tokens.css
  - app/src/styles/tokens.test.js
  - app/src/ui/GraduatedRule.jsx
  - app/src/ui/Placeholder.jsx
  - app/src/ui/RecipeList.jsx
  - app/src/ui/RecipeList.test.jsx
  - app/src/ui/Shell.jsx
  - app/src/ui/Shell.test.jsx
covered_digest: "v1:sha256:43c8f1e4f813688937de4fd8d8afab22b6f147859e6a498be967a81ff112aad8"
behavior_unverified: 1
re_verification:
  previous_status: gaps_found
  previous_score: 14/20
  gaps_closed:
    - "Gap 3: every route the shell wraps paints the App's own ground — `.shell` now declares `background: var(--app-background)` (shell.css:24), the route-only `body:has(.home)` hack is deleted from home.css, and `.recipe-page`, `.page-head` and `.not-found` each explicitly repaint `--sheet-ground` (app.css:353, 376-377, 421) so the three Sheet frames keep their paper."
    - "Gap 2: the unbuilt place renders in the App's own type — `.place`, `.place__title` and `.place__note` all resolve to real rules in shell.css (lines 138-155), and `Placeholder.jsx`'s root now carries `className=\"list-page place\"`."
    - "Gap 4: a Sheet `a:visited` rule can no longer recolour an App control — app.css's selector is rewritten `:where(a:visited)` (app.css:165-167), a zero-specificity wrapper around the same declaration; every single-class App rule (`.shell__place--*`, `.home__action`, `.home__action--secondary`) now outranks it by CSS's own cascade rules (a class selector is always higher specificity than a zero-specificity selector, independent of source order), and binder.test.js pins both the rewritten selector and that it is the only `:visited` selector in the file."
    - "Gap 5: the lead block now matches the must-have's structural description — `.home__lead` is a bordered, radiused flex block (home.css:63-71, reading `--app-rule-row`/`--app-text`/`--app-radius-lead`) carrying the destination rod, an identity column, a Next-time column captioned \"Next time\" (rendered only when a note exists), and `RowActions` over the same entry (RecipeList.jsx:98-130)."
    - "Gap 6: every row states its standing in words — `STANDING_WORDS` (RecipeList.jsx:156-160) looked up from lastEvent.js's own exported constants, rendered in `.home__standing` (RecipeList.jsx:232) — and `.home__row`'s desktop grid reads the board's five columns via two new tokens, `--app-col-standing`/`--app-col-tally` (home.css:171, tokens.css:411-412)."
    - "Gap 1 (partial — CSS mechanics closed, rendered fit still needs a device pass): the tab row's five items are now `flex: 1 1 0; min-width: 0` (shell.css:279-283), laid out icon-over-label, inside a bar pinned to `--app-size-tab-h` with `box-sizing: border-box` (shell.css:262-266), and More opens as an absolutely-positioned panel above the bar (shell.css:316-324) rather than inline in the fixed row. This closes the *mechanism* VERIFICATION.md's gap 1 named as missing; whether five real labels plus icons now fit 393px with no overflow is a rendered-pixel fact this run cannot read from source and is carried to human verification below (unchanged in kind from the plan's own `<human-check>`)."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory:
  - finding: "REVIEW.md's WR-01: the More disclosure's native `<summary>` marker is not suppressed (`summary::-webkit-details-marker` is unset), so Safari/WebKit may render an extra native triangle beside the custom MoreIcon+\"More\" label."
    category: other
    reason: "Discovered by the phase's own code review (03.4-REVIEW.md), not part of any of the six named gaps or any plan's must_haves, so it does not block this phase's goal; it is a genuine, unresolved rendering risk on the WebKit engine this project's own notes flag as the one that catches device-specific defects. No deterministic evidence beyond the review's own grep (no `-webkit-details-marker` rule anywhere in the codebase)."
    evidence_status: "grep-confirmed absence of the suppression rule; not rendered"
  - finding: "REVIEW.md's IN-03: `body` still paints `--sheet-ground` unconditionally; `.shell` (not `html`/`body`) is what paints `--app-background`, so iOS/Safari elastic-overscroll past the top/bottom of any non-Sheet route may briefly show the Sheet's cream instead of the App's white."
    category: other
    reason: "A WebKit-only rendering edge case flagged by code review, not one of the six named gaps or any plan's must_haves. Recorded here so it is not lost, per this project's own pattern of catching WebKit-only defects only on-device."
    evidence_status: "source-level reasoning only; not rendered on any device"
behavior_unverified_items:
  - truth: "Below the existing 759.98px step the rail becomes a bottom tab row of five coloured tabs, all reachable by touch, with More opening as a panel that does not cover the page's last control (D-16, gap 1)"
    test: "Load the app at 393px (and on Mark's iPad — WebKit, 1366, coarse pointer) and confirm all five tab items (Home, Notebook, Recipe book, Idea log, More) are fully on-screen and touchable, that More's opened panel does not cover the page's last control, and that the header's Search/Import/Export controls are folded away at this width."
    expected: "No overflow, no off-screen tab, More reachable and its panel legible above the bar."
    why_human: "The CSS mechanics (`flex: 1 1 0; min-width: 0` on all five items, a bar pinned to `--app-size-tab-h` with border-box sizing, More's panel absolutely positioned) are in place and pinned by shell.test.js, but whether five real icon+label items actually fit 393px with no wrap or clipping is a rendered-pixel fact — font metrics and icon rendering are not visible to a Vitest `node`-environment test, and this project's own notes record that a Chromium-only pass has previously missed WebKit/device-specific defects."
coincidental_reliance_items: []
human_verification:
  - test: "The bottom tab row at 393px and on Mark's iPad (WebKit, 1366, coarse pointer): confirm all five tabs are on-screen and touchable, More's panel opens without covering page content, and the header's Search/Import/Export are folded away."
    expected: "No overflow (VERIFICATION's original measured 532px/off-screen-More regression does not recur), the bar sits at the pinned tab-height, More's panel is legible."
    why_human: "Rendered layout fact; the CSS mechanics are structurally in place and test-pinned but font/icon metrics at a real width cannot be read from source. Named as a `<human-check>` in 03.4-06-PLAN.md Task 3 itself."
  - test: "The hand's rendering and its two-mode fallback with real data: record a real Next-time note on a batch, return to Home, confirm the lead block renders it in Caveat/pen blue, then activate forced-colors and print preview and confirm the fallback to the text face in italic."
    expected: "Legible script rendering by default; text-face italic under forced colours and in print (D-17)."
    why_human: "The seed data carries `nextTimeNote: null`, so this path has never rendered under real data in any automated run; the forced-colours/print fallback rules exist unchanged in app.css but were not exercised."
  - test: "Real Import/Export click-through from more than one route."
    expected: "File picker opens, JSON round-trips, Home reloads via `storeRevision` with no page reload, Export downloads."
    why_human: "File-API interaction cannot be exercised by `renderToStaticMarkup` tests."
  - test: "Keyboard operation of the tools row, the rail, and the tab row + More at narrow widths."
    expected: "Visible focus ring at every stop, correct tab order, no keyboard trap (UX1-01)."
    why_human: "Native semantics and the global `:focus-visible` rule are structurally in place; actual focus-ring rendering and traversal order need a real browser."
  - test: "The Notebook text companion (`#EE0803`) against the No-Verdict Rule, viewed on the rail and a Notebook row."
    expected: "Mark confirms the red companion reads as intended beside the red rail mark."
    why_human: "DESIGN.md itself flags this pairing as an open design judgment, not a code defect."
  - test: "Rule on the four already-decided board departures recorded in 03.4-GAPS-CONFORMANCE.md's Departures section: the 12px tab label (vs. the board's hand-copied 10px), the rod and tally kept at every width (board 171 draws neither), the absent 46px lined page-preview glyph in the lead block, and the row shipping with no versions-count cell."
    expected: "Mark confirms each departure is acceptable as shipped, or requests a follow-up plan."
    why_human: "Each is a knowing, already-authorized deviation from the board's exact pixels — a product/design call, not a code defect — per the plan's own recorded decisions."
  - test: "Rule on the three new findings 03.4-GAPS-CONFORMANCE.md's side-by-side pass surfaced (not part of any of the six original gaps): `.shell__rail` has no padding or `border-right` at all (board 170 draws both); the rail's active item has no pill/bold-weight treatment (only its background swaps); and `RowActions` always renders both the filled and secondary action at 393px, where board 171's own row shows only the filled one."
    expected: "Mark confirms whether these are acceptable simplifications or should be closed in a follow-up plan."
    why_human: "These are genuinely new, unresolved findings from a side-by-side board read, not resolved by any plan in this phase (by design — 03.4-08's own prohibition against this plan resolving anything it finds)."
  - test: "REVIEW.md's WR-01: on Safari/WebKit specifically, check whether the More `<summary>` disclosure shows a native ▶/▼ triangle beside the custom MoreIcon and 'More' label."
    expected: "No native marker glyph — only the custom icon and label, matching the project's 'every control drawn by the page, not the browser' convention."
    why_human: "`::-webkit-details-marker` is a WebKit-only pseudo-element invisible to a text-only CSS-contract reader; no automated check in this codebase can see it."
  - test: "REVIEW.md's IN-03: on iOS/Safari, scroll past the top or bottom of a non-Sheet route (Home, an unbuilt place) far enough to trigger elastic overscroll, and check the rubber-band region's colour."
    expected: "The App's white/`--app-background`, not the Sheet's cream `--sheet-ground`."
    why_human: "The overscroll region is a WebKit-only rendering artifact outside any element's own box; only `body`'s own background paints it, and `body` still declares `--sheet-ground` unconditionally."
---

# Phase 03.4: The design layer in code: App palette, Sheet-prefixed tokens, the hand, and Home — Verification Report

**Phase Goal:** The approved design layer of 2026-09-21 lands in code: tokens.css carries the App palette and the Sheet tokens under the `sheet-` prefix (contract suites included), the five `app-*-text` companions, and the hand (Caveat, with forced-colours and print fallback); then Home is reworked per route.md "Active work first" using the App marks grammar in DESIGN.md.
**Verified:** 2026-09-22T04:30:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plans 03.4-06, 03.4-07, 03.4-08, commits `4105848`..`2470926`)

## Re-verification Summary

The prior VERIFICATION.md (2026-09-22T01:27:14Z) recorded `gaps_found`, 14/20 must-haves verified, with six failed truths: the bottom tab row overflowing at 393px (gap 1), the unbuilt-place page shipping with zero CSS (gap 2), the shell never painting its own App ground (gap 3), a Sheet `a:visited` rule recolouring App links once visited (gap 4), the lead block missing a border/rod/captioned column/actions (gap 5), and recipe rows missing the board's standing word and column grammar (gap 6).

Three gap-closure plans have since executed: **03.4-06** (gaps 1, 2, 3, 4 — the shell's own world), **03.4-07** (gaps 5, 6 — the row and lead block), and **03.4-08** (a 38-row side-by-side conformance record against both boards, tracing all six gaps to evidence and recording four already-decided board departures plus three newly-surfaced findings). All eight plans' `must_haves` were re-checked against the current codebase, not against SUMMARY.md's claims.

**All six gaps are closed in the source**, five of them to a level provable by direct inspection (CSS rule existence, selector specificity, DOM class attributes — none of which require rendering to confirm). The sixth (gap 1, the tab row) is closed at the *mechanism* level — the CSS that was missing before (`flex: 1 1 0; min-width: 0` on all five items, a pinned bar height, More as an absolute panel) is now present and test-pinned — but whether five real labels and icons visually fit 393px with no overflow remains a rendered-pixel fact this run cannot read from source, exactly as the plan itself flagged it (`<human-check>` in 03.4-06-PLAN.md Task 3). This is not a regression from "verified" to "unverified" — the original gap-1 truth was **✗ FAILED** (a measured 532px overflow); it is now **present and structurally correct**, carried to human verification rather than left failing.

**No regressions found.** The full test suite (1119 tests, 41 files) and the production build both pass; every one of the previously-verified 14 truths was re-checked and still holds — `lastEvent.js`, the token-prefix discipline, the Caveat font, the Sprinkles Jar retirement and the forced-colours/print fallback are all unmodified by plans 06-08.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every Sheet-frame token in tokens.css takes the `--sheet-` prefix; every consumer follows it (D-01) | ✓ VERIFIED | `tokens.test.js` passes (3/3); `grep -rn "var(--ink)\|var(--ground)\|var(--pen-blue)\|var(--bookcloth)" app/src` returns nothing; unchanged by plans 06-08 |
| 2 | Shared sizes keep bare names; the App reads them directly (D-02) | ✓ VERIFIED | `tokens.test.js`'s prefix-discipline test passes with zero exemptions; new tokens `--app-col-standing`, `--app-col-tally`, `--app-radius-lead`, `--app-size-tab-h` all carry the `--app-` prefix correctly (tokens.css:411-428) |
| 3 | tokens.css declares the approved App palette and the five `app-*-text` companions under `--app-` (D-03, D-19) | ✓ VERIFIED | unchanged by plans 06-08; still holds |
| 4 | `tokens.test.js` fails the build if any `var()` read resolves to an undeclared token, or a token lacks its prefix | ✓ VERIFIED | gate exercised by plan 07's four new tokens (all read, all prefixed correctly) with no new exemption added; `npm --prefix app test -- src/styles/tokens.test.js` passes (3/3) |
| 5 | Caveat Regular ships as one vendored woff2 with its OFL licence, one `@font-face`, a floor-guarded `.app-hand` role class (D-17) | ✓ VERIFIED | unchanged; files present, build emits the font, no network reference |
| 6 | Under `forced-colors: active` and in print, `.app-hand` falls back to the text face in italic (D-17) | ⚠️ Present, not naturally exercised | unchanged from prior verification: `app.css:2571-2620` still declares both fallback blocks unmodified by plans 06-08; still routed to human verification (seed data carries no Next-time note) |
| 7 | Every route renders inside one shell that paints its own App ground rather than the Sheet's (D-09) | ✓ VERIFIED (gap 3 closed) | `.shell { background: var(--app-background); }` (shell.css:20-26); home.css's route-only `body:has(.home)` rule is deleted (confirmed absent by grep); `.recipe-page`, `.page-head`, `.not-found` each explicitly repaint `--sheet-ground` (app.css:353, 376-377, 421); `shell.test.js` and `home.test.js` pin both sides; this is a declarative CSS fact, not a rendered one, so no human check is needed for the rule's existence |
| 8 | All five destinations are links; an unbuilt place opens a page styled through the App's own tokens (D-10) | ✓ VERIFIED (gap 2 closed) | `.place`, `.place__title`, `.place__note` all resolve to rules in shell.css (lines 138-155); `Placeholder.jsx`'s root now carries `className="list-page place"` (confirmed by direct read); `shell.test.js` pins the rule and the renderer; REVIEW.md confirms CR-01 fixed |
| 9 | Search appears in the tools row as a link and no search logic is built (D-13) | ✓ VERIFIED | unchanged; still holds |
| 10 | Import and Export keep their present behaviour via `storeRevision` (D-15) | ✓ VERIFIED (structural); real click-through deferred | unchanged; still routed to human verification |
| 11 | Below 759.98px the rail becomes a bottom tab row of five reachable tabs, More holding the rest (D-16) | ⚠️ Present, mechanism closed, rendered fit unverified | `flex: 1 1 0; min-width: 0` on all five tab items (shell.css:279-283, replacing the old rule that had none), bar pinned to `--app-size-tab-h` with `box-sizing: border-box` (shell.css:262-266), More opens as an absolutely-positioned panel above the bar (shell.css:316-324) instead of inline — the exact mechanism VERIFICATION's gap 1 named as missing is now present and pinned by 5 new shell.test.js assertions; whether this actually fits 393px with real font/icon metrics is a rendered fact — routed to human verification |
| 12 | The recipe route's page-scoped notice still anchors to its own head — the shell introduces no new containing block | ✓ VERIFIED | `shell.test.js`'s containing-block guard still passes; `.shell`'s new `background` declaration is not one of the five containing-block-triggering properties the guard checks (`transform`/`filter`/`perspective`/`will-change`/`contain`) |
| 13 | Home orders recipes by recency, newest first, with no stored flag (D-05) | ✓ VERIFIED | `lastEvent.js` untouched by plans 06-08 (confirmed by git log); 14 tests still pass |
| 14 | The most recently touched recipe reads as a larger lead block above the list, and also appears in the list below (D-06, D-12) | ✓ VERIFIED | `RecipeList.test.jsx`'s `HomeBody`/`HomeLead` tests still pass; unaffected by plan 07's markup rebuild of `HomeLead`'s internals |
| 15 | The lead block shows the maker's Next time in the hand, in Sheet pen blue, the only place the hand appears (D-18, D-20) | ✓ VERIFIED (structural); natural rendering unexercised | `home.test.js` still asserts the hand role class appears exactly once in RecipeList.jsx's source (now inside the new `.home__lead-next` column, `grep -c "app-hand" app/src/ui/RecipeList.jsx` = 1); still routed to human verification for natural (non-injected) rendering |
| 16 | Every recipe row carries its next action at its right edge, chosen by standing (D-07, D-11) | ✓ VERIFIED | `RecipeList.test.jsx` tests all three standings; `RowActions` unchanged in its branching logic, now also reused by `HomeLead` (RecipeList.jsx:127) |
| 17 | Home's Sprinkles Jar is fully retired (D-04) | ✓ VERIFIED | `grep -rn "jar-\|recipe-hue\|recipe-colour" app/src` returns nothing; `git ls-files app/src/ui/recipe-colour.js app/src/ui/recipe-colour.test.js` empty; unchanged by plans 06-08 |
| 18 | A Sheet rule never recolours an App control or a rail place once a route has been visited (D-01/D-02 boundary, D-07, D-09, D-19) | ✓ VERIFIED (gap 4 closed) | `a:visited` rewritten to `:where(a:visited)` (app.css:165-167) — zero specificity by CSS's own cascade rules, which single-class App rules (`.shell__place--*`, `.home__action`, `.home__action--secondary`) always outrank regardless of source order; `binder.test.js` pins both the rewrite and that it is the only `:visited` selector in the file; this is a deterministic specificity fact, provable from source without rendering |
| 19 | The lead block matches the board: a bordered block with the rod, identity, a captioned Next time column, and the standing's own actions (D-06, D-07, D-12, D-18) | ✓ VERIFIED (gap 5 closed) | `.home__lead` declares `border: var(--app-rule-row) solid var(--app-text)` and `border-radius: var(--app-radius-lead)` (home.css:69-70); `HomeLead` renders the rod, `.home__lead-identity`, a conditional `.home__lead-next` with `.home__lead-caption` reading "Next time", and `RowActions` (RecipeList.jsx:98-130); `home.test.js`/`RecipeList.test.jsx` pin each element. Exact pixel match to the board (padding axes, meta colour, font-weight split, the absent page-preview glyph) is recorded separately in 03.4-GAPS-CONFORMANCE.md as departures/differences, not part of this truth's own structural claim |
| 20 | Each recipe row matches the board's column grammar: rod, place-over-name, the standing word, the tally, then actions (D-07, D-11) | ✓ VERIFIED (gap 6 closed) | `STANDING_WORDS` lookup keyed on lastEvent.js's own exported constants (RecipeList.jsx:156-160), rendered in `.home__standing` (RecipeList.jsx:232); `.home__row`'s desktop grid reads `var(--app-col-standing)` and `var(--app-col-tally)` (home.css:171); `home.test.js`/`RecipeList.test.jsx` pin both |

**Score:** 19/20 truths verified (1 present, behaviour-unverified — gap 1's rendered fit at 393px)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/styles/shell.css` | shell chrome, App ground, `.place` rules, phone-fit tab row | ✓ VERIFIED | all four gap-3/2/1 fixes confirmed present by direct read |
| `app/src/styles/home.css` | Home world, lead block, row grammar | ✓ VERIFIED | gap-5/6 fixes confirmed present; route-level ground rule confirmed removed |
| `app/src/styles/app.css` | Sheet frames, visited-rule rewrite | ✓ VERIFIED | `:where(a:visited)`, three `--sheet-ground` repaints confirmed present |
| `app/src/ui/Placeholder.jsx` | unbuilt-place page, styled | ✓ VERIFIED | `className="list-page place"` confirmed |
| `app/src/ui/RecipeList.jsx` | Home rebuild: lead block, rows, standing word | ✓ VERIFIED | `STANDING_WORDS`, `.home__standing`, `HomeLead`'s rebuilt markup all confirmed present |
| `app/src/styles/tokens.css` | four new App tokens, all read | ✓ VERIFIED | `--app-col-standing`, `--app-col-tally`, `--app-radius-lead`, `--app-size-tab-h` all declared and consumed; `tokens.test.js`'s declared-and-never-read gate passes |
| `.planning/.../03.4-GAPS-CONFORMANCE.md` | side-by-side record, probe script | ✓ VERIFIED | 38-row table, 6-row gap-to-evidence table, 4-row departures section all present; `node --check` on the probe passes; spot-checked two board-line citations (rows 87-101 and 87-93 of the two board files) against the actual board markup — both accurate, not paraphrased |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `.shell` | `--app-background` | CSS declaration | ✓ WIRED | confirmed present; the App ground is now painted once for every route the shell wraps |
| `Placeholder.jsx`'s `.place`/`.place__title`/`.place__note` | shell.css rules | className → stylesheet | ✓ WIRED | confirmed present, no longer orphaned |
| `.shell__tabs` items | `flex: 1 1 0; min-width: 0` | CSS | ✓ WIRED (mechanism); rendered fit unverified | the constraint that was missing is now present; whether it visually resolves the 532px overflow needs a device pass |
| app.css's `a:visited` | zero specificity | `:where()` | ✓ WIRED | confirmed by direct read and by binder.test.js's cardinality pin |
| `lastEvent.js`'s `standingFor` | the rendered standing word and `RowActions` | shared constants (`NOT_YET_CHURNED`/`AWAITING_TASTING`/`TASTED`) | ✓ WIRED | `STANDING_WORDS` keys off the same imported constants `RowActions` branches on, so a word can never disagree with the action beside it |
| `RowActions` | `HomeLead` and `RecipeRows` | shared component, same `entry` | ✓ WIRED | confirmed at RecipeList.jsx:127 (lead) and :241 (row) |

### Requirements Coverage

No requirement IDs are mapped to this phase in REQUIREMENTS.md (a design-delivery phase governed by CONTEXT.md's D-01..D-20, confirmed by grep — no "03.4" row exists there). UX1-01 and UX1-03 apply as build constraints:

| Requirement | Description | Status | Evidence |
|-------------|--------------|--------|----------|
| UX1-01 | Keyboard-operable, visible focus, WCAG 2.2 AA contrast | ? NEEDS HUMAN | unchanged from prior verification — native elements and the global `:focus-visible` rule apply; actual traversal and ring visibility need a real browser |
| UX1-03 | No colour-only signal | ✓ SATISFIED | every standing now states its word explicitly (gap 6 closure strengthens this — the standing word is a second, independent words-based signal alongside the action label); the tally states its count in words |

No orphaned requirements found for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/src/ui/RecipeList.jsx` | 109, 230 | `entry.name` rendered with no fallback for a blank/whitespace name | ⚠️ Warning (WR-02, carried forward from the prior review) | unchanged from the original verification; still no upstream validation reviewed |
| `app/src/styles/shell.css` | 246-334 | More's native `<summary>` disclosure marker (`::-webkit-details-marker`) is never suppressed | ⚠️ Warning (WR-01, new — 03.4-REVIEW.md) | Safari/WebKit may render an extra native triangle beside the custom MoreIcon+"More" label; no test can see this (text-only CSS contract reader); routed to human verification |
| `app/src/styles/app.css` | 5-11 vs shell.css:20-26 | `body` still paints `--sheet-ground` unconditionally; only `.shell` (not `html`/`body`) paints `--app-background` | ℹ️ Info (IN-03, new — 03.4-REVIEW.md) | iOS/Safari elastic-overscroll past a non-Sheet route's box may briefly show the Sheet's cream; routed to human verification |
| `app/src/ui/RecipeList.jsx` | 156-160, 166-206 | `STANDING_WORDS`/`RowActions` render silently empty for any standing outside the current three | ℹ️ Info (IN-01, 03.4-REVIEW.md) | documented, current-phase scope limit; not reachable today |
| `app/src/ui/RecipeList.test.jsx` | 263, 271 | greedy, unanchored regex assertions for block-shape tests | ℹ️ Info (IN-02, 03.4-REVIEW.md) | currently correct by coincidence of markup shape; a latent test fragility, not a production defect |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file this phase modified (confirmed by direct grep across all shell/home/app CSS, RecipeList.jsx, Placeholder.jsx and their test files).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm --prefix app test -- --run` | 1119 passed, 0 failed, 41 files | ✓ PASS |
| Production build | `npm --prefix app run build` | 126 modules transformed, succeeded | ✓ PASS |
| tokens.test.js gate (new tokens all read, all prefixed) | `npm --prefix app test -- --run src/styles/tokens.test.js` | 3 passed | ✓ PASS |
| `.shell` paints the App ground | `grep -n "background: var(--app-background)" app/src/styles/shell.css` | line 24 | ✓ PASS |
| home.css carries no route-only ground rule | `grep -n "body:has" app/src/styles/home.css` | empty | ✓ PASS |
| Three Sheet frames repaint their own paper | `grep -c "background: var(--sheet-ground)" app/src/styles/app.css` | 5 (≥3 required) | ✓ PASS |
| Visited rule is zero-specificity | `grep -n ":where(a:visited)" app/src/styles/app.css` | line 165 | ✓ PASS |
| Placeholder carries the `.place` class | `grep -c 'className="list-page place"' app/src/ui/Placeholder.jsx` | 1 | ✓ PASS |
| Placeholder rules exist | `grep -n "^\.place" app/src/styles/shell.css` | `.place`, `.place__title`, `.place__note` all present | ✓ PASS |
| Tab-row items constrained | `grep -n "flex: 1 1 0;" app/src/styles/shell.css` (within the media block) | present, direct-child selector | ✓ PASS |
| Standing word rendered | `grep -c "Awaiting tasting\|Not yet churned\|Tasted" app/src/ui/RecipeList.jsx` | 1 each | ✓ PASS |
| New tokens declared and read | `grep -n "app-col-standing\|app-col-tally\|app-radius-lead\|app-size-tab-h" app/src/styles/tokens.css` | all 4 present, each consumed elsewhere | ✓ PASS |
| Sprinkles Jar retired | `grep -rn "jar-\|recipe-hue\|recipe-colour" app/src` | empty | ✓ PASS |
| No debt markers in phase files | grep across all touched files | empty | ✓ PASS |
| Commits present | `git log --oneline 4105848^..2470926` | all 13 commits (06/07/08's tasks, docs and metadata) present | ✓ PASS |
| Board-line citations spot-check | direct read of both board files at the cited lines | both spot-checked citations (row 16/87-101 and row 33/87-93) matched the record's claims exactly | ✓ PASS |

### Probe Execution

`03.4-gaps-conformance-probe.js` is a browser-paste reader, not a `scripts/*/tests/probe-*.sh` shell probe — `node --check` confirms it is syntactically valid (exit 0, no `import`/`export`), but it is designed to be pasted into a running browser at UAT, not executed by this verifier. This matches the plan's own design (decision 1: "the record is written to be fillable with or without a browser") and is not a gap.

## Human Verification Required

### 1. The bottom tab row at 393px and on the iPad (WebKit, coarse pointer)

**Test:** Load the app at 393px and confirm all five tabs (Home, Notebook, Recipe book, Idea log, More) are fully on-screen and touchable, that More's opened panel does not cover the page's last control, and that the header's Search/Import/Export controls are folded away. Repeat on Mark's iPad.
**Expected:** No overflow (the original measured 532px/off-screen-More defect does not recur).
**Why human:** The CSS mechanics are structurally in place and test-pinned, but real font/icon metrics at a real width are a rendered fact.

### 2. The hand's rendering and its two-mode fallback with real data

**Test:** Record a real "Next time" note on a batch, return to Home, confirm the lead block renders it in Caveat/pen blue — then activate `forced-colors: active` and print preview and confirm the fallback to the text face in italic.
**Expected:** Legible script by default; text-face italic under forced colours and in print (D-17).
**Why human:** The seed data carries `nextTimeNote: null`; unexercised in any automated run.

### 3. Real Import/Export click-through

**Test:** Use the tools-row Import button to choose a JSON file, and Export to trigger a download, from more than one route.
**Expected:** File picker opens, JSON round-trips, Home reloads via `storeRevision`, Export downloads.
**Why human:** File-API interaction cannot be exercised by `renderToStaticMarkup` tests.

### 4. Keyboard operation of the tools row, rail, and tab row + More

**Test:** Tab through Search/Import/Export, the rail (or tab row + More at narrow widths), open More via keyboard.
**Expected:** Visible focus ring at every stop, correct order, no trap (UX1-01).
**Why human:** Native semantics are in place; ring visibility and traversal order need a real browser.

### 5. The Notebook text companion against the No-Verdict Rule

**Test:** View the rail's "NOTEBOOK" label and a Notebook row's place name in the real app.
**Expected:** Mark confirms the red companion reads as intended.
**Why human:** DESIGN.md itself flags this as an open design judgment.

### 6. Rule on the four already-decided board departures (03.4-GAPS-CONFORMANCE.md)

**Test:** Review the Departures section: the 12px tab label vs. the board's 10px; the rod/tally kept at every width; the absent 46px page-preview glyph; the row shipping with no versions-count cell.
**Expected:** Mark confirms each is acceptable as shipped, or requests a follow-up plan.
**Why human:** Each is a knowing, already-authorized design deviation, not a code defect.

### 7. Rule on the three new conformance findings (03.4-GAPS-CONFORMANCE.md)

**Test:** Review: `.shell__rail` has no padding/`border-right` at all; the rail's active item has no pill/bold treatment; `RowActions` always shows both actions at 393px where the board shows one.
**Expected:** Mark confirms whether these are acceptable or should be closed later.
**Why human:** Genuinely new findings, not resolved by design (plan 08's own prohibition against resolving what it finds).

### 8. REVIEW.md WR-01: the More disclosure's native marker on Safari/WebKit

**Test:** On Safari/WebKit, check whether a native ▶/▼ triangle renders beside the custom MoreIcon and "More" label.
**Expected:** No native marker glyph.
**Why human:** `::-webkit-details-marker` is invisible to a text-only CSS-contract reader.

### 9. REVIEW.md IN-03: elastic-overscroll colour on iOS/Safari

**Test:** On iOS/Safari, scroll past the top/bottom of a non-Sheet route far enough to trigger the rubber-band effect and check its colour.
**Expected:** The App's white, not the Sheet's cream.
**Why human:** A WebKit-only rendering artifact outside any element's own box.

## Gaps Summary

All six gaps from the prior verification (2026-09-22T01:27:14Z) are closed in the source and confirmed by direct code inspection, not by trusting the plans' SUMMARY.md claims:

1. **Gap 3 (App ground)** — closed. `.shell` now paints `--app-background`; the route-only hack is gone; the three Sheet frames repaint their own paper.
2. **Gap 2 (Placeholder styling)** — closed. `.place`/`.place__title`/`.place__note` all resolve to rules; `Placeholder.jsx` carries the class.
3. **Gap 4 (visited-rule recolouring)** — closed, and provably so by CSS specificity rules alone (no rendering needed): `:where(a:visited)` is zero-specificity, so every single-class App rule outranks it unconditionally.
4. **Gap 5 (lead block)** — closed at the structural level named by the must-have (border, radius, rod, captioned column, actions); exact pixel parity with the board is tracked separately as recorded departures/differences in 03.4-GAPS-CONFORMANCE.md, which is the correct place for it per this phase's own lesson.
5. **Gap 6 (row column grammar)** — closed at the structural level; same caveat as gap 5.
6. **Gap 1 (tab-row overflow)** — closed at the *mechanism* level (the missing flex constraint, the pinned bar height, More as an overlay panel are all now present and test-pinned); the rendered outcome (does it actually fit 393px) is a pixel fact this run cannot read from source and is carried forward as human verification, exactly as the plan itself flagged it. This is not a lingering gap in the sense of "known broken" — it is the one truth in this phase whose correctness genuinely depends on rendering, same as the phase's other already-accepted human-verification items (the hand's rendering, Import/Export click-through, keyboard traversal).

No regressions were found in the 14 previously-verified truths, the full test suite (1119/1119) and the production build. Two new findings surfaced by the phase's own code review (WR-01, the More disclosure's unsuppressed native marker on WebKit; IN-03, the overscroll colour edge case) are neither part of the six named gaps nor resolved by any plan — they are recorded here as advisory/human-verification items rather than silently dropped, alongside the four board departures and three new conformance findings 03.4-GAPS-CONFORMANCE.md already surfaced.

Because a substantial set of human-verification items remain (nine items above, none of which are automatable), this phase's status is **human_needed**, not `passed` — per the verification framework's own rule that human-verification items always take precedence over an otherwise-clean automated pass.

---

_Verified: 2026-09-22T04:30:00Z_
_Verifier: Claude (gsd-verifier)_
