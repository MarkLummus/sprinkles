---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
verified: 2026-09-22T23:15:00Z
status: passed
score: 39/39 truths verified (0 present, behaviour-unverified)
covered_files:
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/debug/ipad-keyboard-locks-after-tab.md
  - .planning/debug/ipad-tab-never-enters-app.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-09-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-09-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-10-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-10-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-12-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-12-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-13-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-13-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-14-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-14-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-15-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-15-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-CONTEXT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-DISCUSSION-LOG.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-GAPS-CONFORMANCE.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-PATTERNS.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-RESEARCH.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-SECURITY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/uat-rounds/03.4-UAT-round2.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/uat-rounds/03.4-UAT-round3.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/uat-rounds/03.4-UAT-round4.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UAT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UI-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js
  - CLAUDE.md
  - DESIGN.md
  - app/public/fonts/OFL.txt
  - app/public/fonts/caveat-regular.woff2
  - app/src/domain/lastEvent.js
  - app/src/domain/lastEvent.test.js
  - app/src/main.jsx
  - app/src/router.jsx
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/fonts.css
  - app/src/styles/history.css
  - app/src/styles/home.css
  - app/src/styles/home.test.js
  - app/src/styles/shell.css
  - app/src/styles/shell.test.js
  - app/src/styles/tokens.css
  - app/src/styles/tokens.test.js
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/GraduatedRule.jsx
  - app/src/ui/Placeholder.jsx
  - app/src/ui/RecipeHistory.jsx
  - app/src/ui/RecipeHistory.test.jsx
  - app/src/ui/RecipeList.jsx
  - app/src/ui/RecipeList.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - app/src/ui/Shell.jsx
  - app/src/ui/Shell.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
covered_digest: "v1:sha256:fcd82f7c48cda7f9c8f63e64ef1890b82013e47f79e30350b52c3d4f8c57d138"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 33/33 truths verified (0 present, behaviour-unverified)
  gaps_closed:
    - "G-03.4-r4-1 S1 (every non-shell app link is unreachable by Tab, so after the shell's ninth stop WebKit finds nothing to focus and relinquishes to Safari — 'then I lose focus on everything'): plan 03.4-15 added an explicit `tabIndex={0}` to all seventeen react-router Link sites outside the shell (RecipeList.jsx 9, RecipeHistory.jsx 3, VersionRow.jsx 2, BatchRow.jsx 1, RecipePage.jsx 1, router.jsx 1). Confirmed by direct grep of current source (counts match exactly: 9/3/2/1/1/1), by an app-wide gate that finds zero untagged `<Link>`/`<NavLink>` under app/src, by four rendered-markup tests with exact link counts (HomeBody 2 and 11, RecipeHistory 3, VersionRow 2, BatchHistoryPanel 1) that this verifier ran directly, and by two source-text tests for the two sites the node harness cannot render (router.jsx, RecipePage.jsx). Not trusted from 03.4-15-SUMMARY.md's own claims."
    - "The device-side half of G-03.4-r3-3 (whether Safari's chrome-to-page hand-off actually lands on Search), the sole human-verification item carried from the previous round, was tested in 03.4-UAT.md round four (test 1: rings appeared on the tools row and rail; test 2: outcome (3), recorded per its own contract). That specific hand-off question is answered by device evidence and is not re-listed; what round four surfaced instead was the new, distinct fault G-03.4-r4-1 now closed at the code level above."
    - "The every-link convention is recorded once in .claude/CLAUDE.md's Conventions block (confirmed present: exactly one 'TabsToLinks' mention, eight bullets total) so the next link-rendering component inherits the rule."
    - "S2 (the keyboard dead until a power cycle) confirmed to have received no code change: git diff against the plan's base commit shows zero added lines outside `tabIndex={0}` across the six source files, and no CSS, key handler, focus() call or new element was added anywhere in the plan's diff."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory:
  - finding: "REVIEW.md's WR-01 (carried forward, unaffected by plan 15): Shell.jsx's tab-row comment still claims focus returns to the summary 'in both cases', but the route-change effect only calls setMoreOpen(false), never .focus()."
    category: other
    reason: "Plan 03.4-15 does not touch Shell.jsx (confirmed: not in the plan's files_modified list, and `git diff` since the prior VERIFICATION.md's round shows no Shell.jsx change). Still a documentation-accuracy defect, not functional, and does not block the phase goal."
    evidence_status: "grep-confirmed: comment text and effect body unchanged since the prior verification round"
  - finding: "REVIEW.md's WR-02 (carried forward): shell.css has no rule suppressing summary::-webkit-details-marker on the More disclosure."
    category: other
    reason: "Unaffected by plan 15 (shell.css not modified). UAT round two's test 8 found no native marker on Mark's device; the code-level risk is still technically present but contradicted by that device observation."
    evidence_status: "grep-confirmed absence of a suppression rule in shell.css; contradicted by Mark's own device pass at 03.4-UAT-round2.md test 8"
  - finding: "RecipeList.jsx renders entry.name (recipe name) with no fallback for a blank/whitespace name, at the same two sites as before (now lines 110 and 229 after plan 15's tabIndex insertion shifted line numbers by one)."
    category: other
    reason: "Carried forward from 03.4-REVIEW.md (WR-03/prior WR-02). Plan 15's diff at these two lines is exactly one attribute insertion each; no fallback logic was added or removed."
    evidence_status: "grep-confirmed: `entry.name` rendered directly at both sites, no `||` or `??` fallback present"
behavior_unverified_items: []
coincidental_reliance_items: []
human_verification:
  - test: "(a) Home, assertable on the iPad — closes G-03.4-r4-1. Serve the build (`npm --prefix app run build && npm --prefix app run preview -- --host`, port 4173), never the dev server. Open on the iPad, hardware keyboard attached, Full Keyboard Access on. Note the iPadOS version and the keyboard's model/connection first (Settings > General > About). At 1366px, tap once in the header's blank space beside the Sprinkles wordmark — not the content column or any control, since a tap sets WebKit's Tab starting point at the tapped node and a content-column tap starts after every shell stop. Press Tab repeatedly."
    expected: "A ring at Search, Import, Export, Home, Notebook, Recipe book, Idea log, Ingredients, Kitchen, in that order, then on Home's own links in page order: the lead recipe's name, its action(s), then each row's name and action(s) down the list (the lead's name rings a second time as a row, per D-12). Shift-Tab from the last row's last action walks all the way back to Search; nothing traps. The Tab past the last action leaving the page for Safari's own chrome (no wrap) is expected WebKit behaviour, not a fail."
    why_human: "The code-level fix — an explicit tabindex on all seventeen non-shell Link sites — is confirmed present, pinned on rendered markup with exact link counts, present in the built bundle (27 tabIndex:0 sites), and structurally sound (no reordering, no focus() call). Whether WebKit's FocusController actually advances Tab into Home's own links after Kitchen, rather than relinquishing to Safari as it did before this fix, is unmeasured on Mark's iPad — the only device this gap was ever reported from — since the fix shipped."
  - test: "(a2) A recipe page, assertable, same setup. Open a recipe and tap beside the wordmark. Tab through the nine shell stops."
    expected: "The running head's Sprinkles link rings next, then the page's own controls in order. On a version developed from another, From version and From batch ring as links. Open History with Space and every version and batch name shown as a link rings. Fail the gap if Tab skips any link on either route."
    why_human: "Same reasoning as (a): the running head (router.jsx), the history outline (RecipeHistory.jsx) and the version row's lineage links (VersionRow.jsx) are confirmed present and pinned (rendered-markup tests for the outline and version row; source-text pins for the running head), but whether WebKit's Tab order actually reaches them on a real recipe page on Mark's iPad is unmeasured since the fix shipped."
  - test: "(b) S2 — the keyboard dead until a power cycle — recorded, never asserted. If the keyboard stops responding at any point in (a) or (a2), do not power cycle first: try, in order, (1) Tab-H and Fn-H, (2) tap Safari's address bar and type a letter, (3) swipe to the Home Screen and type in Notes, (4) turn Full Keyboard Access off and type in Notes, (5) detach/reattach the keyboard or toggle Bluetooth. Then, regardless of whether it died, run the control: a new blank Safari tab, tap the address bar, Tab about 30 times; then Tab past the last control on an ordinary website."
    expected: "Record which step (if any) restored the keyboard, or that it did not die, plus the control's outcome, as a note on G-03.4-r4-1 — never as a pass/fail on the gap itself. The app runs no key, focus, timer or observer code on Home and this plan added none, so the app cannot cause this fault; the note only places which layer (Safari's focus state, the Full Keyboard Access service, or the keyboard transport/iPadOS) it lives in, and the control separates 'any page' from 'Sprinkles only'."
    why_human: "This is an on-device, closed-source platform behaviour (Safari's UI-process focus hand-off, the Full Keyboard Access service, or iPadOS's external-keyboard input stack) that no code in this repository can reach, reproduce, or fix. It reproduced in round four with focus never having entered the page at all, which is exactly the state this plan's fix changes — whether it still reproduces once Home's links give WebKit something to focus is the open question."
---

# Phase 03.4: The design layer in code: App palette, Sheet-prefixed tokens, the hand, and Home — Verification Report

**Phase Goal:** The approved design layer of 2026-09-21 lands in code: tokens.css carries the App palette and the Sheet tokens under the `sheet-` prefix (contract suites included), the five `app-*-text` companions, and the hand (Caveat, with forced-colours and print fallback); then Home is reworked per route.md "Active work first" using the App marks grammar in DESIGN.md.
**Verified:** 2026-09-22T23:15:00Z
**Status:** human_needed
**Re-verification:** Yes — fourth gap-closure round, after 03.4-UAT.md's round-four device pass surfaced G-03.4-r4-1 (every non-shell app link unreachable by Tab, plus the unrelated S2 keyboard lock-up), diagnosed in `.planning/debug/ipad-keyboard-locks-after-tab.md`, and closed at the code level by plan 03.4-15 (`gap_closure: true`, commits `cbbc4b1`..`0009e12`).

## Re-verification Summary

The prior VERIFICATION.md (2026-09-22T20:15:00Z) recorded `human_needed`, 33/33 truths verified, with one human-verification item outstanding: whether Safari's chrome-to-page hand-off actually lands on the shell's Search link on Mark's iPad, the device-side half of G-03.4-r3-3 that 03.4-14's code fix could not itself confirm.

Mark then ran a fourth UAT pass against that report (03.4-UAT.md, current file). Test 1 (the hand-off truth) reported `issue`: "rings on tabs, search and import/export, then into the page, and then I lose focus on everything. keyboard stops working until I recycle power." Test 2 (the recorded, never-asserted chrome-hand-off truth) reported `pass` with outcome (3) — focus cycles the chrome and never enters the page — but noted the same keyboard lock-up. Read together, these results actually **confirm** the shell hand-off this round's carried-forward human item was about — the ring did reach Search, Import and Export — while surfacing a **new, distinct fault**: once Tab reaches Home's own controls, which are all tabindex-less react-router `<Link>`s, WebKit's `TabsToLinks` gate makes them unreachable, so after the ninth shell stop (Kitchen) there is nothing left to focus and WebCore relinquishes focus to Safari, which does not hand it back. A debug session (`.planning/debug/ipad-keyboard-locks-after-tab.md`, status `diagnosed`) root-caused this from WebKit's own source and separated it from an unrelated, environment-only fault (S2, the keyboard staying dead until a power cycle) that the app has no code path to cause or fix.

Plan 03.4-15 closed the app-side fault (S1, filed as G-03.4-r4-1) by extending 03.4-14's shell-only remedy to every remaining `<a>` the app renders: an explicit `tabIndex={0}` on all seventeen non-shell react-router `Link` sites (`RecipeList.jsx` 9, `RecipeHistory.jsx` 3, `VersionRow.jsx` 2, `BatchRow.jsx` 1, `RecipePage.jsx` 1, `router.jsx` 1), pinned on rendered markup with exact link counts for the four components the test harness can render, and on source text (matching the whole `Link` opening tag) for the two it cannot. It added no code for S2, since the app runs no key/focus/timer/observer code on Home and cannot cause a fault that reproduced with focus never in the page at all. It recorded one Conventions bullet in `.claude/CLAUDE.md` and reworded the UAT truth so the tap position (beside the wordmark, not in the content column) no longer decides the result.

**This re-verification re-checked every one of these claims directly against the current codebase, not against 03.4-15-SUMMARY.md's own claims** — by reading the actual JSX and running the actual greps (`tabIndex={0}` counts: RecipeList.jsx 9, RecipeHistory.jsx 3, VersionRow.jsx 2, BatchRow.jsx 1, RecipePage.jsx 1, router.jsx 1, Shell.jsx still 10, all confirmed independently), by running the app-wide grep gate itself (clean — zero untagged `Link`/`NavLink` lines under `app/src`), by running the actual test files (all five, 300/300 passing) rather than reading the SUMMARY's reported numbers, by running the full suite myself (41 files, 1154 tests, all passing) and the production build myself (126 modules, 27 `tabIndex:0` sites in the bundle), and by diffing the six source files against the plan's own declared base commit (`5f25d16`): equal added/removed line counts per file (1/1, 1/1, 3/3, 9/9, 1/1, 2/2) and zero added lines outside `tabIndex={0}`, confirming the "attribute-only" prohibition held. `.claude/CLAUDE.md`'s Conventions block was read directly and confirmed to carry exactly one `TabsToLinks` mention among eight bullets.

**All six of plan 03.4-15's must-haves are verified present, correct, and pinned in the current source, with zero regressions.** No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers exist in any of the twelve files this plan touched. What remains open is exactly what the plan's own `<verification>` section says must remain open until measured: whether Tab now actually advances into Home's own links (and a recipe page's own links) on Mark's iPad once WebKit has something to focus after the shell, plus the S2 recovery-ladder procedure, which the plan explicitly never asserts as pass/fail. Both are carried into this report's human-verification section, worded from the plan's own `<verification>` section rather than restated from memory.

**No regressions found.** The full test suite passes cleanly at HEAD (41 files, 1154 tests). The production build succeeds (126 modules, ~126 KB gzipped JS, 27 `tabIndex:0` sites). `git diff` against the plan's base commit touches only the twelve files the plan declared; the six source files carry attribute-only changes. Every one of the prior round's 33 previously-verified truths is unaffected by this round's code changes, confirmed by regression-checking the files each depends on (Shell.jsx, shell.css, tokens.css, home.css, app.css, main.jsx — none touched by plan 15).

**The one previously-outstanding human-verification item (G-03.4-r3-3's chrome-to-page hand-off) is resolved by round-four device evidence** and is not re-listed: Mark's own report ("rings on tabs, search and import/export") directly confirms the hand-off lands on Search. What replaces it is the new, narrower human-verification need this round's fix creates — whether Home's and a recipe page's own links are now reachable too, plus S2's recorded-not-asserted procedure — exactly as plan 03.4-15's own `<verification>` section anticipates.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every Sheet-frame token in tokens.css takes the `--sheet-` prefix; every consumer follows it (D-01) | ✓ VERIFIED | unchanged by plan 15; regression-checked via the full suite pass |
| 2 | Shared sizes keep bare names; the App reads them directly (D-02) | ✓ VERIFIED | unchanged; regression-checked |
| 3 | tokens.css declares the approved App palette and the five `app-*-text` companions under `--app-` (D-03, D-19) | ✓ VERIFIED | unchanged; regression-checked |
| 4 | `tokens.test.js` fails the build if any `var()` read resolves to an undeclared token, or a token lacks its prefix | ✓ VERIFIED | unchanged; regression-checked |
| 5 | Caveat Regular ships as one vendored woff2 with its OFL licence, one `@font-face`, a floor-guarded `.app-hand` role class (D-17) | ✓ VERIFIED | unchanged; files present, build emits the font |
| 6 | Under `forced-colors: active` and in print, `.app-hand` falls back to the text face in italic (D-17) | ✓ VERIFIED | unchanged since device confirmation (03.4-UAT-round2.md test 2: pass) |
| 7 | Every route renders inside a shell that paints its own App ground rather than the Sheet's (D-09) | ✓ VERIFIED (device-confirmed) | unchanged; mechanism unaffected by plan 15 |
| 8 | All five destinations are links; an unbuilt place opens a page styled through the App's own tokens (D-10) | ✓ VERIFIED | unchanged; regression-checked |
| 9 | Search appears in the tools row as a link and no search logic is built (D-13) | ✓ VERIFIED | unchanged; regression-checked |
| 10 | Import and Export keep their present behaviour via `storeRevision` (D-15) | ✓ VERIFIED (structural) | `handleImportChange`/`handleExport` untouched by plan 15; Export confirmed on-device in round-two UAT |
| 11 | Below 759.98px the rail becomes a bottom tab row of five reachable tabs, More holding the rest (D-16) | ✓ VERIFIED | unchanged since device confirmation (03.4-UAT-round2.md test 1: pass) |
| 12 | The recipe route's page-scoped notice still anchors to its own head — the shell introduces no new containing block | ✓ VERIFIED | `shell.test.js`'s containing-block guard still passes; plan 15 touched no CSS |
| 13 | Home orders recipes by recency, newest first, with no stored flag (D-05) | ✓ VERIFIED | unchanged; regression-checked |
| 14 | The most recently touched recipe reads as a larger lead block above the list, and also appears in the list below (D-06, D-12) | ✓ VERIFIED | unchanged; regression-checked |
| 15 | The lead block shows the maker's Next time in the hand, in Sheet pen blue, the only place the hand appears (D-18, D-20) | ✓ VERIFIED | unchanged since device confirmation |
| 16 | Every recipe row carries its next action at its right edge, chosen by standing (D-07, D-11) | ✓ VERIFIED | unchanged; regression-checked |
| 17 | Home's Sprinkles Jar is fully retired (D-04) | ✓ VERIFIED | unchanged; regression-checked |
| 18 | A Sheet rule never recolours an App control or a rail place once a route has been visited (D-01/D-02 boundary, D-07, D-09, D-19) | ✓ VERIFIED | unchanged; regression-checked |
| 19 | The lead block matches the board: a bordered block with the rod, identity, a captioned Next time column, and the standing's own actions (D-06, D-07, D-12, D-18) | ✓ VERIFIED (structural) | unchanged by plan 15; regression-checked |
| 20 | Each recipe row matches the board's column grammar: rod, place-over-name, the standing word, the tally, then actions (D-07, D-11) | ✓ VERIFIED (structural) | unchanged by plan 15; regression-checked |
| 21 | On an App-context route the CSS canvas is the App's white; on the three paper frames it stays the Sheet's cream (G-03.4-9, D-01/D-02/D-03) | ✓ VERIFIED (device-confirmed) | unchanged; mechanism unaffected by plan 15 |
| 22 | At desktop width the rail is divided from the page by a hairline right border, without growing past its 224px basis (G-03.4-7, D-09) | ✓ VERIFIED (device-confirmed) | unchanged; shell.css untouched by plan 15 |
| 23 | The `.shell__place:focus` fallback rule paints a ring once focus arrives at a shell stop; on its own it does nothing to bring focus into the document (G-03.4-4, corrected by G-03.4-r3-3) | ✓ VERIFIED | rule and comment unchanged since 03.4-14; plan 15 touched no CSS |
| 24 | Between 760px and 1100px the lead block reads with the recipe name and Next time each on one line; rows take a stacked form (G-03.4-6, D-06, D-11, D-18) | ✓ VERIFIED (device-confirmed) | unchanged; home.css untouched by plan 15 |
| 25 | At 393px a recipe row matches board 171: identity beside the standing word, one filled action, no rod, no tally (G-03.4-6b, D-07, D-19, UX1-03) | ✓ VERIFIED (device-confirmed) | unchanged; home.css untouched by plan 15 |
| 26 | The lead block's meta line carries the version identity and no computed gram figure (G-03.4-6c, D-19) | ✓ VERIFIED | unchanged; regression-checked |
| 27 | At phone width, choosing any of More's five items closes the panel; a route change closes it; focus returns to the summary (G-03.4-3 half A, D-16, UX1-01) | ✓ VERIFIED (device-confirmed) | wiring unchanged; Shell.jsx untouched by plan 15 |
| 28 | Device UAT is served from a production build; the page paints before IndexedDB opens; Home shows a loading line while reads are in flight (G-03.4-1, D-06, D-08) | ✓ VERIFIED (device-confirmed) | unchanged; main.jsx/RecipeList.jsx's loading branch untouched by plan 15's diff (confirmed: RecipeList.jsx's 9 added lines are attribute-only) |
| 29 | Every shell stop is keyboard-focusable on WebKit with no platform preference set: each carries an explicit `tabindex="0"` (G-03.4-r3-3, UX1-01) | ✓ VERIFIED (device-confirmed) | `grep -cF 'tabIndex={0}' app/src/ui/Shell.jsx` = 10, re-confirmed this round; 03.4-UAT.md round-four test 1's report ("rings on tabs") directly confirms the shell stops ring on Mark's device |
| 30 | Search is the document's first keyboard-focusable element on every route, so WebKit's chrome-to-page hand-off has something to land on | ✓ VERIFIED (device-confirmed this round) | **upgraded from "structural"**: 03.4-UAT.md round-four test 1 ("rings on tabs, search and import/export") and test 2 (outcome (3), recorded) together confirm the hand-off reaches Search on Mark's own iPad — the previously open human-verification item for G-03.4-r3-3 is resolved by this device evidence |
| 31 | The shell's pin is on rendered markup: every `shell__place` element carries `tabindex="0"`, the hidden file input alone keeps `tabindex="-1"` | ✓ VERIFIED | `Shell.test.jsx` unchanged, still passing; plan 15 touched no shell files |
| 32 | No record still cites the falsified premise as measured (shell.css comment, 03.4-09-SUMMARY.md correction) | ✓ VERIFIED | unchanged; regression-checked |
| 33 | The prior round's UAT truth was split so it could pass or fail honestly ((a) assertable, (b) recorded) | ✓ VERIFIED (documented) | 03.4-14-PLAN.md's split was carried into round-four UAT and produced exactly the evidence this report cites above |
| 34 | Every `<a>` the app renders carries `tabindex="0"`: the seventeen non-shell Link sites plus the shell's ten, 27 total (G-03.4-r4-1 S1, UX1-01) | ✓ VERIFIED | `grep -cF 'tabIndex={0}'` returns 9/3/2/1/1/1 across the six files (RecipeList, RecipeHistory, VersionRow, BatchRow, RecipePage, router.jsx), re-confirmed by this verifier directly, matching the plan's own derivation exactly; app-wide gate (`grep -rn -E '<(Link\|NavLink)\b' app/src --include='*.jsx' \| grep -v '\.test\.jsx' \| grep -v 'tabIndex={0}'`) returns clean |
| 35 | On Home at 1366px the Tab after Kitchen goes to the lead recipe's name, then its actions, then each row's name and actions in page order — the document is never left without a next keyboard-focusable element after the shell (D-06, D-07, D-11, D-12) | ✓ VERIFIED (structural; device confirmation pending) | code-level: HomeBody's 11 links (three-recipe fixture) are each pinned with `tabIndex={0}` in DOM render order matching the page's visual order (lead, then rows), confirmed by direct read of RecipeList.jsx and its passing test; WebKit's actual Tab traversal on Mark's device is this report's human-verification item (a) |
| 36 | The pin is on rendered markup for every component the node harness can render (HomeBody, RecipeHistory, VersionRow, BatchHistoryPanel), each asserting an exact link count so none can pass with zero links; only router.jsx's running head and RecipePage.jsx's not-found link are pinned on source text | ✓ VERIFIED | ran all four rendered-markup test files directly (not from SUMMARY claims): `RecipeList.test.jsx` (2 and 11 links), `RecipeHistory.test.jsx` (3 links), `VersionRow.test.jsx` (2 links), `BatchRow.test.jsx` (1 link) — all pass; the two source-text tests in `RecipePage.test.jsx` (router.jsx, RecipePage.jsx) also confirmed present and passing |
| 37 | The rule is recorded once in `.claude/CLAUDE.md`'s Conventions, stating the rule, the WebKit TabsToLinks reason, and where it is pinned | ✓ VERIFIED | `awk '/GSD:conventions-start/,/GSD:conventions-end/' .claude/CLAUDE.md \| grep -c 'TabsToLinks'` = 1; same region `grep -c '^- '` = 8; bullet text read directly and states all three required elements |
| 38 | S2 (the keyboard dead until a power cycle) gets no code change; the plan's verification section carries the on-device steps, recorded never asserted | ✓ VERIFIED | `git diff` against base `5f25d16` for the six source files shows zero added lines outside `tabIndex={0}`; no `focus()`, `autoFocus`, `onKeyDown`, key handler, timer or observer added anywhere; the plan's `<verification>` section carries the ordered recovery ladder and neutral-page control, carried verbatim into this report's human-verification item (b) |
| 39 | The UAT truth's tap goes in the header's blank space beside the Sprinkles wordmark, not the page body, since a tap sets WebKit's Tab starting point at the tapped node | ✓ VERIFIED (documented) | 03.4-15-PLAN.md's own `<verification>` section states this explicitly and reworded the (a)/(a2) procedure; carried verbatim into this report's human-verification items |

**Score:** 39/39 truths verified (0 present, behaviour-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/ui/RecipeList.jsx` | `tabIndex={0}` on all nine Link sites | ✓ VERIFIED | grep confirms 9; `<Link` count also 9, all carrying the attribute |
| `app/src/ui/RecipeList.test.jsx` | rendered-markup pin, 2 and 11 links | ✓ VERIFIED | new describe block ran directly, both tests pass |
| `app/src/ui/RecipeHistory.jsx` | `tabIndex={0}` on all three Link sites | ✓ VERIFIED | grep confirms 3 |
| `app/src/ui/RecipeHistory.test.jsx` | rendered-markup pin (3 links); two literals updated | ✓ VERIFIED | new test passes; both literals confirmed updated (`grep -cF` = 2) |
| `app/src/ui/VersionRow.jsx` | `tabIndex={0}` on both lineage links | ✓ VERIFIED | grep confirms 2; computed identity-heading tabIndex untouched |
| `app/src/ui/VersionRow.test.jsx` | rendered-markup pin (2 links) | ✓ VERIFIED | new describe block passes |
| `app/src/ui/BatchRow.jsx` | `tabIndex={0}` on the batch register's one link | ✓ VERIFIED | grep confirms 1; computed h2 tabIndex untouched |
| `app/src/ui/BatchRow.test.jsx` | rendered-markup pin (1 link) | ✓ VERIFIED | new test passes |
| `app/src/ui/RecipePage.jsx` | `tabIndex={0}` on the not-found link | ✓ VERIFIED | grep confirms 1 |
| `app/src/router.jsx` | `tabIndex={0}` on the running head's link | ✓ VERIFIED | grep confirms 1 |
| `app/src/ui/RecipePage.test.jsx` | two source-text tests (router.jsx, RecipePage.jsx) | ✓ VERIFIED | both tests present and passing, confirmed by direct read |
| `.claude/CLAUDE.md` | one Conventions bullet recording the rule | ✓ VERIFIED | present, one bullet, states rule/reason/pin |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `tabIndex={0}` on each of the seventeen non-shell Link sites | `tabindex="0"` on the rendered `<a>` | React's attribute serialization via react-router's `Link` (rest props spread before `href`) | ✓ WIRED | confirmed on rendered markup via `renderToStaticMarkup` for the four renderable components; on source text for the two that cannot be rendered |
| HomeBody's lead and rows | the next keyboard-focusable elements after Kitchen | render order | ✓ WIRED (structural; device Tab order is the open human-verification item) | confirmed: no code between the shell and Home's own links removes or reorders anything; the 11-link fixture's DOM order matches the page's visual order |
| the `.claude/CLAUDE.md` convention bullet | the next component that renders a link | prose, no test-time enforcement | ✓ WIRED | present, stating the rule, the WebKit rationale, and where it is pinned; the plan's own "not taken here" section notes a source-scan enforcement was considered and left as a follow-up for Mark, not a gap |
| the app-wide grep gate | every `Link`/`NavLink` under `app/src` | construction-time proof | ✓ WIRED | ran directly by this verifier: zero untagged lines found |

### Requirements Coverage

No requirement IDs are mapped to this phase in REQUIREMENTS.md (a design-delivery phase governed by CONTEXT.md's D-01..D-20, confirmed by grep — no "03.4" row exists there, unchanged since the prior round). UX1-01 and UX1-03 apply as build constraints, verified end-to-end in Phase 4:

| Requirement | Description | Status | Evidence |
|-------------|--------------|--------|----------|
| UX1-01 | Keyboard-operable, visible focus, WCAG 2.2 AA contrast | ? NEEDS HUMAN | the every-link tabindex fix (G-03.4-r4-1) is now present, pinned on rendered/source markup, present in the built bundle, and structurally correct (Home's links follow the shell in DOM order with no reordering); whether Tab actually advances into them on Mark's iPad is the open device check, carried to human verification |
| UX1-03 | No colour-only signal | ✓ SATISFIED | unchanged from prior rounds; every standing states its word explicitly |

No orphaned requirements found for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/src/ui/Shell.jsx` | comment above the tab row (~289-290) | Comment claims focus always returns to More's summary "in both cases"; the route-change effect does not call `.focus()` | ⚠️ Warning (WR-01, carried forward — unaffected by plan 15, Shell.jsx not in its files_modified list) | documentation-accuracy defect, not functional; recorded as advisory above |
| `app/src/styles/shell.css` | `.shell__more`/`.shell__more > ul` | The More disclosure's native `<summary>` marker is still never suppressed | ℹ️ Info (WR-02, carried forward, unaffected by plan 15) | contradicted by Mark's own device pass (round two); recorded as advisory |
| `app/src/ui/RecipeList.jsx` | 110, 229 | `entry.name` rendered with no fallback for a blank/whitespace name | ⚠️ Warning (WR-03, carried forward; line numbers shifted by one from the tabIndex insertion, no logic change) | unchanged; no upstream validation reviewed |
| `app/src/styles/home.css` | 357-359 and 434-436 | `.home__lead .home__rail { display: none; }` declared identically in two breakpoint blocks — a dead duplicate | ℹ️ Info (IN-01, carried forward, unaffected by plan 15) | harmless; home.css not touched this round |
| `app/src/ui/RecipeList.test.jsx` | ~266, ~274 | greedy, unanchored regex assertions | ℹ️ Info (IN-02, carried forward) | currently correct by coincidence of markup shape; unaffected by plan 15's appended tests |
| `app/src/ui/RecipeList.jsx` | 43 | the `.home__loading` paragraph carries no `aria-live`/`role="status"` | ℹ️ Info (IN-03, carried forward) | minor; unaffected by plan 15's diff at this line |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any of the twelve files this plan modified (confirmed by direct grep across `RecipeList.jsx`, `RecipeList.test.jsx`, `RecipeHistory.jsx`, `RecipeHistory.test.jsx`, `VersionRow.jsx`, `VersionRow.test.jsx`, `BatchRow.jsx`, `BatchRow.test.jsx`, `RecipePage.jsx`, `RecipePage.test.jsx`, `router.jsx`, `.claude/CLAUDE.md`).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm --prefix app test -- --run` | 1154 passed, 0 failed, 41 files | ✓ PASS |
| Production build | `npm --prefix app run build` | 126 modules transformed, succeeded, ~126 KB gzipped JS | ✓ PASS |
| Five affected test files | `npm --prefix app test -- --run src/ui/RecipeList.test.jsx src/ui/RecipeHistory.test.jsx src/ui/VersionRow.test.jsx src/ui/BatchRow.test.jsx src/ui/RecipePage.test.jsx` | 300/300 pass | ✓ PASS |
| Explicit tabindex counts (G-03.4-r4-1) | `grep -cF 'tabIndex={0}'` per file | RecipeList.jsx 9, RecipeHistory.jsx 3, VersionRow.jsx 2, BatchRow.jsx 1, RecipePage.jsx 1, router.jsx 1, Shell.jsx 10 | ✓ PASS |
| App-wide untagged-link gate | `grep -rn -E '<(Link\|NavLink)\b' app/src --include='*.jsx' \| grep -v '\.test\.jsx' \| grep -v 'tabIndex={0}'` | empty (no matches) | ✓ PASS |
| Built-bundle tabindex count | `npm --prefix app run build && grep -o 'tabIndex:0' app/dist/assets/*.js \| wc -l` | 27 | ✓ PASS |
| Conventions bullet present | `awk '/GSD:conventions-start/,/GSD:conventions-end/' .claude/CLAUDE.md \| grep -c 'TabsToLinks'` | 1 | ✓ PASS |
| RecipeHistory literal updates | `grep -cF 'From batch · <a tabindex="0" href="/recipe/v1/batch/b1"' app/src/ui/RecipeHistory.test.jsx` | 2 | ✓ PASS |
| Diff scope (attribute-only) | `git diff --numstat 5f25d16 -- <six source files>` | equal added/removed per file (1/1, 1/1, 3/3, 9/9, 1/1, 2/2); zero non-`tabIndex={0}` additions | ✓ PASS |
| Commits present | `git log --oneline` for cbbc4b1, 8da3337, 37ff04a, 1131b4b, 1d20159, f4d14fa, 0009e12 | all 7 present | ✓ PASS |
| Debt markers | grep across all 12 files this plan touched | none found | ✓ PASS |

### Probe Execution

`03.4-gaps-conformance-probe.js` is unchanged this round — a browser-paste reader, not a `scripts/*/tests/probe-*.sh` shell probe. Not executed by this verifier, matching prior rounds' own finding — this is not a gap.

## Completion ruling (2026-09-22)

Round five UAT (03.4-UAT.md): item 1 passed on the iPad with Full Keyboard Access off; item 3 recorded (no lock-up with it off); item 2 failed, was diagnosed as G-03.4-r5-2 (every button, checkbox and radio without an explicit tabindex is skipped by WebKit with Full Keyboard Access off, 38 sites) and deferred by Mark to a shared Button todo (.planning/todos/pending/2026-09-22-shared-button-component-carries-tabindex-for-webkit.md). Mark: "mark 03.4 complete now". Status set to passed on that ruling; the recipe page's keyboard reachability on iPadOS is carried as debt, not claimed.

## Human Verification Required (as issued before round five)

### 1. Home: Tab reaches every link after the shell (G-03.4-r4-1, closes the gap)

**Test:** Serve the build (`npm --prefix app run build && npm --prefix app run preview -- --host`, port 4173), never the dev server. Open on the iPad, hardware keyboard attached, Full Keyboard Access on. Note the iPadOS version and the keyboard's model/connection first. At 1366px, tap once in the header's blank space beside the Sprinkles wordmark — not the content column or any control. Press Tab repeatedly.
**Expected:** A ring at Search, Import, Export, Home, Notebook, Recipe book, Idea log, Ingredients, Kitchen, in that order, then on Home's own links in page order: the lead recipe's name, its action(s), then each row's name and action(s) down the list (the lead's name rings a second time as a row). Shift-Tab from the last row's last action walks all the way back to Search; nothing traps. Tab past the last action leaving the page for Safari's chrome (no wrap) is expected, not a fail.
**Why human:** The code-level fix is confirmed present, pinned on rendered markup, present in the built bundle, and structurally sound (no reordering, no focus call). Whether WebKit actually advances Tab into Home's own links after Kitchen — rather than relinquishing to Safari, as it did before this fix — is unmeasured on Mark's iPad since the fix shipped.

### 2. A recipe page: Tab reaches the running head and history/lineage links (G-03.4-r4-1)

**Test:** Open a recipe and tap beside the wordmark, same setup. Tab through the nine shell stops.
**Expected:** The running head's Sprinkles link rings next, then the page's own controls in order. From version and From batch ring as links where present. Open History with Space and every version/batch name shown as a link rings. Fail the gap if Tab skips any link on either route.
**Why human:** Same reasoning as item 1 — the running head, history outline and lineage links are confirmed present and pinned, but WebKit's actual Tab order on a real device is unmeasured since the fix shipped.

### 3. S2 — the keyboard dead until a power cycle (recorded, never asserted)

**Test:** If the keyboard stops responding during item 1 or 2, do not power cycle first — try, in order: (1) Tab-H and Fn-H, (2) tap Safari's address bar and type, (3) type in Notes from the Home Screen, (4) turn Full Keyboard Access off and type in Notes, (5) detach/reattach the keyboard or toggle Bluetooth. Then run the control regardless: a new blank Safari tab, Tab about 30 times from the address bar, then Tab past the last control on an ordinary website.
**Expected:** Record which step restored the keyboard (or that it did not die) and the control's outcome, as a note on G-03.4-r4-1 — never as a pass/fail on the gap.
**Why human:** An on-device, closed-source platform behaviour (Safari's focus hand-off, the Full Keyboard Access service, or iPadOS's keyboard input stack) that no code in this repository can reach or fix. It reproduced in round four with focus never in the page at all — exactly the state this plan's fix changes — so whether it still reproduces is the open question this note answers.

## Gaps Summary

No code-level gaps remain. Every must-have plan 03.4-15 declared is verified present, correct, pinned, and regression-safe against the full suite and production build, all re-run directly by this verifier rather than trusted from the SUMMARY.

This phase's status remains **human_needed**, not `passed`, for exactly the reason plan 03.4-15's own `<verification>` section anticipated: the code-level remedy for G-03.4-r4-1 cannot itself prove that WebKit's Tab order reaches Home's and a recipe page's own links on Mark's iPad, and S2's on-device recovery procedure is by design recorded, never asserted. Both are the sole items above. The previously-outstanding human item (G-03.4-r3-3's shell hand-off) is resolved by round-four device evidence and is not re-listed.

---

_Verified: 2026-09-22T23:15:00Z_
_Verifier: Claude (gsd-verifier)_
