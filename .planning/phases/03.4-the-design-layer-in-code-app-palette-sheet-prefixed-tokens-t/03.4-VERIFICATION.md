---
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
verified: 2026-09-22T17:45:00Z
status: human_needed
score: 27/28 truths verified (1 present, behaviour-unverified — the More panel's open/close state transition)
covered_files:
  - .claude/CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-01-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-02-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-03-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-04-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-05-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-06-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-07-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-08-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-09-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-09-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-10-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-10-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-11-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-12-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-12-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-13-PLAN.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-13-SUMMARY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-CONTEXT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-DISCUSSION-LOG.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-GAPS-CONFORMANCE.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-PATTERNS.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-RESEARCH.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-SECURITY.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UAT.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UI-REVIEW.md
  - .planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-gaps-conformance-probe.js
  - CLAUDE.md
  - DESIGN.md
  - app/public/fonts/OFL.txt
  - app/public/fonts/caveat-regular.woff2
  - app/src/domain/lastEvent.js
  - app/src/domain/lastEvent.test.js
  - app/src/main.jsx
  - app/src/router.jsx
  - app/src/styles/app.css
  - app/src/styles/binder.test.js
  - app/src/styles/columns.test.js
  - app/src/styles/cross-cutting.test.js
  - app/src/styles/fonts.css
  - app/src/styles/history.css
  - app/src/styles/home.css
  - app/src/styles/home.test.js
  - app/src/styles/shell.css
  - app/src/styles/shell.test.js
  - app/src/styles/tokens.css
  - app/src/styles/tokens.test.js
  - app/src/ui/GraduatedRule.jsx
  - app/src/ui/Placeholder.jsx
  - app/src/ui/RecipeList.jsx
  - app/src/ui/RecipeList.test.jsx
  - app/src/ui/Shell.jsx
  - app/src/ui/Shell.test.jsx
covered_digest: "v1:sha256:9e8e754706a8d2aa10f0b3685b1fd38b1b5e0fa87be872caf8163860b3a7f771"
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 19/20 truths verified (1 present, behaviour-unverified — the tab row's fit at 393px)
  gaps_closed:
    - "G-03.4-9 (elastic-overscroll cream on every App route on iOS): body's own background is now the App ground per context, with body:has(.recipe-page, .page-head, .not-found) handing the canvas back to the Sheet's cream for the three paper frames — declared where the UA can actually propagate it to the canvas, replacing the .shell-only fix the prior round shipped."
    - "G-03.4-7 (rail missing its own edge): .shell__rail now carries box-sizing: border-box, a padding reading the gap scale, and border-right: var(--app-rule-row) solid var(--app-divider); the rail's 224px footprint is unchanged; conformance row 3 reads matches."
    - "G-03.4-4 (no focus ring on Mark's iPad with Full Keyboard Access on): one new .shell__place:focus rule, scoped to the shell's own stops, added beside the project's single global :focus-visible rule."
    - "G-03.4-6 (the Home lead row unusable from 760px to ~1100px): a new @media (max-width: 1099.98px) block — reusing the ladder's own number — stacks the lead (rod hidden, actions at natural width) and the rows (their fixed standing/tally tracks dropped), closing both the text-crush and the page's own 760-820px sideways scroll."
    - "G-03.4-6b (rulings 6.2/7.3 — the phone row should match board 171 exactly): the 759.98px block now hides the row's rod, its tally, and its secondary action, each rule scoped under .home__row so the lead's own two actions (sharing the same class) cannot be lost."
    - "G-03.4-6c (ruling 6.4 — the lead's meta line should not carry recipe grams): HomeLead's meta paragraph now renders versionIdentity(...) alone; the mass computation and its two now-orphaned domain imports are gone."
    - "G-03.4-3 half A (More stays open after choosing an item or navigating away, at phone width): More's open state moved into React (History.jsx's HistoryDisclosure pattern) — the <ul>'s own click handler closes it on item activation, an effect keyed on useLocation().pathname closes it on any route change, and focus returns to the summary in both cases. Half B (import feedback/navigation from a non-Home route) was explicitly deferred by Mark on 2026-09-22 and remains open, recorded in 03.4-UAT.md's Deferred Follow-Ups — it is not part of this phase's must-haves."
    - "G-03.4-1 code half (iPad load 'measured in seconds'): .claude/CLAUDE.md now states device UAT must be served from the production build, never the dev server, with the measured 48x transport-cost reason and a one-Vite-process operator note; main.jsx renders an .app-boot line through the root before awaiting seedIfEmpty, so the first paint no longer waits on IndexedDB; RecipeList renders a .home__loading line inside its one existing wrapper while its own two store reads are in flight, replacing an early return null."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory:
  - finding: "REVIEW.md's WR-01 (new, post-gap-closure): the comment above Shell.jsx's tab row still claims focus returns to the summary 'in both cases' (item activation and route change), but the pathname-keyed effect (Shell.jsx:152-154) only calls setMoreOpen(false) — it never calls .focus(). The click path (closeMore) does return focus; the route-change path does not, and is independently reachable (a sibling tab tap, or browser back/forward, while More is open)."
    category: other
    reason: "REVIEW.md's own recommended fix was to narrow the comment rather than change the behaviour (forcing focus back after a route change the user didn't initiate via More would steal focus from whatever they just used) — a reasonable call, but the comment in the shipped source (Shell.jsx, around the tab-row block) was not actually narrowed to match. This is a documentation-accuracy defect, not a functional one; it does not block the phase goal, but a future reader trusting the comment could misjudge the focus-return guarantee's scope."
    evidence_status: "grep-confirmed: the comment text and the effect body are as REVIEW.md describes them, unchanged since that review"
  - finding: "REVIEW.md's WR-02 (carried forward): shell.css has no rule suppressing summary::-webkit-details-marker on the More disclosure, so Safari/WebKit could in principle render a native ▶/▼ triangle beside the custom MoreIcon and the word 'More'."
    category: other
    reason: "UAT round two's own test 8 ('On Safari/WebKit specifically, check whether the More <summary> disclosure shows a native marker') reported result: pass — Mark did not see a native marker on his device. The code-level risk REVIEW.md flagged is still technically present in source (no suppression rule was added by any of plans 09-13, confirmed by grep), but it has been superseded by a real, contradicting device observation. Recorded so the discrepancy between the static prediction and the device result is not lost, not because it blocks anything."
    evidence_status: "grep-confirmed absence of a suppression rule in shell.css; contradicted by Mark's own device pass at 03.4-UAT.md test 8"
  - finding: "03.4-GAPS-CONFORMANCE.md's departure #5 (the single remaining filled action stretches to the row's full width at 393px, where board 171 draws it at natural width) was never ruled on during UAT round two — test 7's rulings cover items #1 (rail padding, fixed) and #3 (secondary action, fixed, folded into G-03.4-6b) and #2 (rail active-item pill, kept/deferred), but no ruling addresses the stretch. It remains open in the conformance record exactly as plan 12 left it."
    category: other
    reason: "A known, already-surfaced, non-blocking design departure — not a new finding by this re-verification, and not part of any of the eight gap_ids this round closed. Carried forward here so it is not silently dropped."
    evidence_status: "confirmed by direct read of 03.4-GAPS-CONFORMANCE.md's Departures table and 03.4-UAT.md's rulings for tests 6 and 7 — no matching ruling exists"
behavior_unverified_items:
  - truth: "At phone width, choosing any of More's five items closes the panel; a route change closes it even when the change did not originate inside More; focus lands on More's own summary rather than the document body (G-03.4-3 half A, D-16, UX1-01)"
    test: "At 393px on a non-Home route (e.g. Notebook): open More, choose Ingredients — confirm the panel closes and Ingredients opens. Open More again, choose Export — confirm the panel closes and the file downloads. Open More again, choose Import — confirm the panel closes and the file picker opens. After each, press Tab and confirm focus resumes at More's summary, not the top of the page. Separately: open More, then tap Home (a sibling tab, not inside More's own list) and confirm the panel still closes."
    expected: "The panel closes on every one of the five items and on a route change from any source; focus is on the summary afterward, never lost to the document body."
    why_human: "Shell.test.jsx runs `renderToStaticMarkup` in Vitest's node environment, which cannot click, toggle a `<details>`, run an effect, or navigate — this suite's own new describe block reads Shell.jsx's source as text to pin that the wiring (onToggle, the pathname-keyed effect, the <ul>'s onClick) exists, but no test in this codebase exercises the actual open→close transition or the focus hand-off. This is a genuine state-transition truth (open state, route dependency, focus target) that presence-and-wiring checks cannot resolve on their own — it was broken before this round's fix (03.4-UAT.md test 3) and has not been re-confirmed since."
coincidental_reliance_items: []
human_verification:
  - test: "iOS/Safari elastic-overscroll colour on a non-Sheet route (G-03.4-9)"
    expected: "Scrolling past the top or bottom of Home or a placeholder route far enough to trigger the rubber-band effect reveals the App's white, not the Sheet's cream; a recipe route (a paper frame) still reveals cream."
    why_human: "The rubber-band region is a WebKit-only rendering artifact painted from body's own background, invisible to any DOM/CSS test; this was confirmed broken ('Fail. It's cream on all pages that I tested.', 03.4-UAT.md test 9) before the fix and has not been re-tested on a device since. The fix (body defaults to --app-background, body:has(...) hands the three paper frames back their cream) is present in app.css and pinned by binder.test.js's new describe block, but the mechanism's actual on-device result is unconfirmed."
  - test: "The rail's edge at 1280px (G-03.4-7)"
    expected: "The rail is divided from the page by a hairline in the divider grey, its places sit in from both edges, and the page has not shifted — the rail is still 224px wide overall."
    why_human: "A rendered spacing/border fact; shell.css declares box-sizing: border-box, the padding tokens, and border-right: var(--app-rule-row) solid var(--app-divider) (confirmed present, grep-verified), but whether it renders at exactly the board's edge with no width growth needs a browser."
  - test: "Focus rings on Mark's iPad with a hardware keyboard and Full Keyboard Access on (G-03.4-4)"
    expected: "Tabbing through the tools row, the rail's places, and (at phone width) the tab row and More shows a visible ring at every stop, in order, with no trap."
    why_human: "This closes a device-specific defect (03.4-UAT.md test 4: 'no focus ring anywhere' even after Mark turned Full Keyboard Access on) attributed to WebKit not setting :focus-visible's keyboard modality on iPadOS in that configuration. The fix is a plain, unconditioned `.shell__place:focus` fallback (confirmed present in shell.css) — its actual painting on the iPad has not been re-tested."
  - test: "The Home lead and rows from 760px to 1100px, and the phone-width row at 393px (G-03.4-6, G-03.4-6b, G-03.4-6c)"
    expected: "From 760 to ~1090px: the lead's recipe name reads on one line, the Next time note reads on one line, no column is crushed, nothing spills, and the page does not scroll sideways; at 1100px and above the row/lead forms return unchanged. At 393px: a row shows the place name over the recipe name beside the standing word and one filled action only — no colour rod, no batch tally, no secondary action; the lead above it still shows both of its actions; the lead's meta line reads the version identity with no trailing gram figure."
    why_human: "This closes a confirmed defect ('at 760px width, the lead row is unusable due to compression...', 03.4-UAT.md test 6) with a new CSS step (home.css's 1099.98px block) and three row-scoped hides in the existing 759.98px block — both present and pinned by home.test.js, but the rendered result across the 760-1100px band and at 393px has not been re-measured on a device since the fix."
  - test: "More's dismissal at 393px, end to end (G-03.4-3 half A)"
    expected: "See the behavior_unverified_items entry above — the same test, repeated here because status: human_needed routes every behavior-unverified truth into this section."
    why_human: "See behavior_unverified_items — no test in this codebase can exercise a `<details>` toggle, a route change, or a focus hand-off."
  - test: "Cold load on Mark's iPad, served from the production build (G-03.4-1)"
    expected: "`npm --prefix app run build && npm --prefix app run preview -- --host`, opened on the iPad: an .app-boot line ('Opening your recipes…') appears promptly on the App's own ground, then the shell and Home follow with Home showing 'Loading your recipes…' before the list appears — no multi-second blank white page."
    why_human: "The prior failure (03.4-UAT.md test 1: 'fail on iPad - the page load time is measured in seconds') was root-caused to two things: the iPad being served the unbundled Vite dev server over the LAN (6.22 MiB vs. the build's ~134 KB gzipped), and nothing painting until IndexedDB opened. Both are addressed in code (the CLAUDE.md convention, and main.jsx's two-phase render) and covered by unit tests for the parts a test can see (the render-order guarantee, the loading branch), but perceived load time on the actual device, served the actual way, is a rendered/timed fact no test in this codebase can measure."
  - test: "03.4-GAPS-CONFORMANCE.md's open departure #5: the single remaining filled action stretches to the row's full width at 393px, where board 171 draws it at natural width"
    expected: "Mark rules on whether the stretch should stay (it comes from 03.4-03's own pre-existing touch rules, untouched by this round) or be corrected to match the board."
    why_human: "A known, already-recorded design departure that UAT round two's rulings (tests 6 and 7) did not address — carried forward rather than silently dropped, not a code defect."
---

# Phase 03.4: The design layer in code: App palette, Sheet-prefixed tokens, the hand, and Home — Verification Report

**Phase Goal:** The approved design layer of 2026-09-21 lands in code: tokens.css carries the App palette and the Sheet tokens under the `sheet-` prefix (contract suites included), the five `app-*-text` companions, and the hand (Caveat, with forced-colours and print fallback); then Home is reworked per route.md "Active work first" using the App marks grammar in DESIGN.md.
**Verified:** 2026-09-22T17:45:00Z
**Status:** human_needed
**Re-verification:** Yes — second gap-closure round, after 03.4-UAT.md's round-two device pass logged nine test results (three pass, six issue) and 03.4-09 through 03.4-13 (all `gap_closure: true`, all with SUMMARY.md) closed eight distinct gap IDs (G-03.4-9, -7, -4, -6, -6b, -6c, -3 half A, -1 code half).

## Re-verification Summary

The prior VERIFICATION.md (2026-09-22T04:30:00Z) recorded `human_needed`, 19/20 truths verified, with one truth (the tab row's rendered fit at 393px) present-but-behaviour-unverified and nine human-verification items outstanding, including four already-decided board departures and three new conformance findings.

Mark then ran UAT round two against that report (03.4-UAT.md): 3 of 9 items passed outright (the hand's rendering and fallback, the Notebook text companion, the WR-01 native-marker check), and 6 surfaced real defects, each root-caused in its own debug session and ruled on by Mark directly: the iPad's multi-second load (test 1), the More panel staying open after any item was chosen (test 3), no focus ring anywhere on the iPad even with Full Keyboard Access on (test 4), the Home lead row unusable from 760-900px plus three board-departure rulings that fold into it (test 6), the rail's missing padding and border (test 7), and the iOS elastic-overscroll region staying cream on every route (test 9). Test 6 and test 7's sub-rulings produced two further gap IDs (G-03.4-6b for the phone row's rod/tally/secondary-action, G-03.4-6c for the lead's meta-line grams).

Five gap-closure plans (03.4-09 through 03.4-13) then executed, closing eight distinct gap IDs. **This re-verification re-checked every one of those eight against the current codebase, not against any plan's SUMMARY.md claims** — by reading the actual CSS rules, the actual React wiring, and the actual test pins, then running the full suite and the production build directly.

**All eight gaps are closed in the source, each confirmed present and each pinned by at least one new test.** One of them — G-03.4-3 half A, the More panel's open/close state transition — is a genuine behaviour-dependent truth (an open state, a route dependency, and a focus hand-off) that no test in this codebase's `renderToStaticMarkup`/node-environment harness can exercise; it is marked present-and-wired but behaviour-unverified, per this verifier's own rule that presence and wiring are not sufficient for a state-transition truth. The other seven are declarative CSS/structural facts, verified present, wired, and test-pinned, with their rendered/on-device outcome carried to human verification because a confirmed prior UAT failure has not yet been re-tested since the code fix — this verifier does not mark a confirmed-broken behaviour "passed" on a SUMMARY's own claim of correctness.

**No regressions found.** The full test suite grew monotonically through the five plans (1119 → 1128 → 1123 → 1125 → 1140 → 1145) and passes cleanly at HEAD (41 files, 1145 tests); the production build succeeds (126 modules, ~126 KB gzipped JS). Every one of the 19 previously-verified truths was re-checked directly against the current source and still holds. One previously-behaviour-unverified truth (the tab row's fit at 393px, D-16) is **upgraded to VERIFIED** this round: Mark's own UAT test 1 reported "pass on iPhone" — the tab row's fit at the width the board draws it, board 171's own 393px, is now confirmed by a real device rather than left as a rendered fact this run could not read from source. The iPad-specific failure test 1 also reported ("fail on iPad — the page load time is measured in seconds") is a *distinct* concern (load performance, not tab-row layout — the iPad's own viewport is well above 760px, where the tab row does not even render) and is what G-03.4-1 addresses.

One new documentation-accuracy issue surfaced by this round's own code review (03.4-REVIEW.md's WR-01) is recorded as advisory: a Shell.jsx comment still overclaims that focus always returns to More's summary on both the click and route-change paths, when only the click path actually does. This does not affect behaviour — the reviewer's own recommendation was to narrow the comment, not change the code — but the comment was not in fact narrowed, so it is flagged here rather than silently dropped.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Every Sheet-frame token in tokens.css takes the `--sheet-` prefix; every consumer follows it (D-01) | ✓ VERIFIED | unchanged by plans 09-13; `tokens.test.js` passes; `grep -rn "var(--ink)\|var(--ground)\|var(--pen-blue)\|var(--bookcloth)" app/src` returns nothing |
| 2 | Shared sizes keep bare names; the App reads them directly (D-02) | ✓ VERIFIED | unchanged; `tokens.test.js`'s prefix-discipline test passes with zero exemptions |
| 3 | tokens.css declares the approved App palette and the five `app-*-text` companions under `--app-` (D-03, D-19) | ✓ VERIFIED | unchanged by plans 09-13; still holds |
| 4 | `tokens.test.js` fails the build if any `var()` read resolves to an undeclared token, or a token lacks its prefix | ✓ VERIFIED | unchanged; gate re-exercised by this round's new `--focus-outline-width`/`--focus-outline-offset` reads (already-declared, shared tokens) with no new exemption |
| 5 | Caveat Regular ships as one vendored woff2 with its OFL licence, one `@font-face`, a floor-guarded `.app-hand` role class (D-17) | ✓ VERIFIED | unchanged; files present, build emits the font |
| 6 | Under `forced-colors: active` and in print, `.app-hand` falls back to the text face in italic (D-17) | ✓ VERIFIED | **upgraded this round** — 03.4-UAT.md test 2 ("Record a real Next-time note...activate forced-colors and print preview and confirm the fallback") result: `pass`. A real device confirmed both the hand's default rendering and its two-mode fallback; no longer merely structural |
| 7 | Every route renders inside a shell that paints its own App ground rather than the Sheet's (D-09) | ✓ VERIFIED (mechanism moved to body, gap 3 + G-03.4-9) | `.shell` still declares `background: var(--app-background)` (shell.css:23); `body` now also declares it by default (app.css:23), with `body:has(.recipe-page, .page-head, .not-found)` handing the canvas back to `--sheet-ground` (app.css:31) — the descendant-level paint (in-page pixels) and the canvas-level paint (elastic-overscroll) are now both covered, closing the gap the first fix (`.shell`-only) structurally could not reach; binder.test.js pins the new mechanism. The rendered on-device colour is carried to human verification (see below) since UAT test 9 found it still cream before this fix |
| 8 | All five destinations are links; an unbuilt place opens a page styled through the App's own tokens (D-10) | ✓ VERIFIED | unchanged by plans 09-13; `.place`/`.place__title`/`.place__note` still resolve, `Placeholder.jsx` still carries the class |
| 9 | Search appears in the tools row as a link and no search logic is built (D-13) | ✓ VERIFIED | unchanged; still holds |
| 10 | Import and Export keep their present behaviour via `storeRevision` (D-15) | ✓ VERIFIED (structural); real click-through partially confirmed | `handleImportChange`/`handleExport` untouched by plans 09-13 (confirmed by diff — only the More disclosure's open state changed in Shell.jsx); 03.4-UAT.md test 3 confirmed Export genuinely works on a device ("export works (json file is written)"); Import's non-navigation from a non-Home route was clarified as never a spec requirement (03.4-03-PLAN.md:149) and Mark explicitly deferred that follow-up on 2026-09-22 — it is not a must-have of this phase |
| 11 | Below 759.98px the rail becomes a bottom tab row of five reachable tabs, More holding the rest (D-16) | ✓ VERIFIED | **upgraded this round** — the CSS mechanism (`flex: 1 1 0; min-width: 0`, pinned bar height, More as an overlay) closed last round is now confirmed by a real device: 03.4-UAT.md test 1 result "pass on iPhone" is a direct confirmation that all five tabs fit and are touchable at 393px, the width board 171 draws. The iPad's separate load-time failure is tracked as G-03.4-1, below, and does not bear on this truth (the iPad's own viewport, 1366 landscape / 1024 portrait per the debug session, is above 760px, where the tab row does not render at all — the rail does) |
| 12 | The recipe route's page-scoped notice still anchors to its own head — the shell introduces no new containing block | ✓ VERIFIED | `shell.test.js`'s containing-block guard still passes; none of `.shell`'s, `body`'s, or `.shell__rail`'s new declarations (`background`, `box-sizing`, `padding`, `border-right`) are among the five containing-block-triggering properties the guard checks |
| 13 | Home orders recipes by recency, newest first, with no stored flag (D-05) | ✓ VERIFIED | `lastEvent.js` untouched by plans 09-13 |
| 14 | The most recently touched recipe reads as a larger lead block above the list, and also appears in the list below (D-06, D-12) | ✓ VERIFIED | unchanged by plans 09-13; `RecipeList.test.jsx`'s lead/list tests still pass |
| 15 | The lead block shows the maker's Next time in the hand, in Sheet pen blue, the only place the hand appears (D-18, D-20) | ✓ VERIFIED | **upgraded this round alongside truth 6** — 03.4-UAT.md test 2 confirmed natural rendering with real data, on a device, for the first time; `home.test.js` still pins the role class appearing exactly once |
| 16 | Every recipe row carries its next action at its right edge, chosen by standing (D-07, D-11) | ✓ VERIFIED | `RecipeList.test.jsx` still tests all three standings; `RowActions` unchanged by plans 09-13's own edits (only the phone-width CSS visibility of its secondary action changed, in home.css) |
| 17 | Home's Sprinkles Jar is fully retired (D-04) | ✓ VERIFIED | unchanged; `grep -rn "jar-\|recipe-hue\|recipe-colour" app/src` returns nothing |
| 18 | A Sheet rule never recolours an App control or a rail place once a route has been visited (D-01/D-02 boundary, D-07, D-09, D-19) | ✓ VERIFIED | unchanged by plans 09-13; `:where(a:visited)` still zero-specificity, `binder.test.js` still pins it |
| 19 | The lead block matches the board: a bordered block with the rod, identity, a captioned Next time column, and the standing's own actions (D-06, D-07, D-12, D-18) | ✓ VERIFIED (structural, extended this round) | base structure unchanged; now additionally stacks below 1099.98px (rod hidden, actions at natural width, `home.css:350-368`) and its meta line no longer carries a computed mass figure (`RecipeList.jsx:112`, G-03.4-6c) — both new declarations present and pinned by `home.test.js`/`RecipeList.test.jsx`; rendered result across the crush band carried to human verification below |
| 20 | Each recipe row matches the board's column grammar: rod, place-over-name, the standing word, the tally, then actions (D-07, D-11) | ✓ VERIFIED (structural, extended this round) | base structure unchanged; now additionally drops the rod, the tally, and the secondary action at 393px (`home.css:404-414`, G-03.4-6b), matching board 171's own three-element row exactly; rendered result carried to human verification below |
| 21 | On an App-context route the CSS canvas — what iOS Safari paints during elastic overscroll — is the App's white; on the three paper frames it stays the Sheet's cream, declared on `body` (the element the UA propagates to the canvas), not a descendant (G-03.4-9, D-01/D-02/D-03) | ✓ VERIFIED (structural) | `body { background: var(--app-background); }` (app.css:23); `body:has(.recipe-page, .page-head, .not-found) { background: var(--sheet-ground); }` (app.css:31), naming all three paper-frame classes; no `html` selector exists anywhere in app.css (confirmed by grep); `binder.test.js`'s new describe block (`the canvas is per context, on body, not a descendant (G-03.4-9, commit 4105848)`) pins all three facts. On-device rubber-band colour carried to human verification — confirmed cream/broken by UAT test 9 before this fix, not yet re-tested |
| 22 | At desktop width the rail is divided from the page by a hairline right border and carries the board's own inner padding, without growing past its 224px basis (G-03.4-7, D-09) | ✓ VERIFIED (structural) | `.shell__rail` declares `box-sizing: border-box`, `padding: var(--gap-xs) var(--gap-m) var(--gap-l)`, `border-right: var(--app-rule-row) solid var(--app-divider)` (shell.css:121-129); `shell.test.js` pins all three; conformance row 3 reads `matches`. Rendered edge carried to human verification |
| 23 | Every keyboard stop in the shell paints a focus ring on a platform (iPadOS Safari with Full Keyboard Access) that never sets `:focus-visible`'s keyboard modality (G-03.4-4, UX1-01) | ✓ VERIFIED (structural) | `.shell__place:focus { outline: var(--focus-outline-width) solid var(--app-text); outline-offset: var(--focus-outline-offset); }` (shell.css:207-210), a single scoped rule beside the project's one global `:focus-visible` rule; `shell.test.js` pins its existence, its selector, and that `shell.css` declares no `outline: none` anywhere. On-device ring painting carried to human verification — confirmed absent on the iPad (even with the accessibility setting on) before this fix |
| 24 | Between 760px and 1100px the lead block reads with the recipe name on one line and the Next time note on one line, neither column crushed nor spilling; in the same band the rows take a stacked form so Home does not scroll sideways (G-03.4-6, D-06, D-11, D-18) | ✓ VERIFIED (structural) | `home.css`'s new `@media (max-width: 1099.98px)` block (home.css:350-368), reusing DESIGN.md's own ladder number, stacks `.home__lead` and hides its rod, and gives `.home__row` a fixed-track-free `auto 1fr auto` template; `.home__lead-name` gained `overflow-wrap: anywhere`; `home.test.js` names both of the file's two `@media` steps deliberately and pins each new rule, including a negative pin that no actions rule exists in the new block (the stacked lead's buttons stay at natural width, left-aligned, by the *absence* of a stretch rule). Rendered crush-free reading across 760-1090px carried to human verification — confirmed broken by UAT test 6 before this fix |
| 25 | At 393px a recipe row matches board 171 exactly: identity beside the standing word, then one filled action — no colour rod, no batch tally, no secondary action; the lead above it is untouched and keeps both its actions (G-03.4-6b, rulings 6.2/7.3, D-07, D-19, UX1-03) | ✓ VERIFIED (structural) | inside the existing `(max-width: 759.98px)` block, `.home__row .home__rail`, `.home__row .home__meta`, `.home__row .home__action--secondary` all declare `display: none` (home.css:404-414), each scoped under `.home__row` specifically so the lead's own two actions (sharing `.home__action--secondary`) cannot be caught by a widened selector; `.home__row`'s template drops the `rail`/`tally` areas; `home.test.js` pins all three hides and a negative assertion that no bare `.home__action--secondary` hide exists. Rendered result carried to human verification alongside truth 24 |
| 26 | The lead block's meta line under the recipe name carries the version identity and no computed gram figure (G-03.4-6c, ruling 6.4, D-19) | ✓ VERIFIED | `HomeLead`'s meta paragraph renders `{versionIdentity(entry.versions, entry.latestVersion)}` alone (RecipeList.jsx:112); `computeBalance`/`activeRows` imports removed (confirmed absent by grep); `RecipeList.test.jsx` asserts both the version-identity string and the absence of a computed one-decimal gram figure |
| 27 | At phone width, choosing any of More's five items closes the panel; a route change closes it even when the change did not originate inside More; focus returns to the summary rather than the document body (G-03.4-3 half A, D-16, UX1-01) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | More's open state moved into React (`moreOpen` state, `moreSummaryRef`, controlled `open`/`onToggle`, `<ul onClick={closeMore}>`, an effect keyed on `useLocation().pathname`) — all present and wired at Shell.jsx:148-159, 290-295. `Shell.test.jsx` gained a source-text describe block pinning every one of these facts exists in the file, but this suite is `renderToStaticMarkup` in Vitest's node environment and cannot click, toggle a `<details>`, run an effect, or navigate — no test in this codebase exercises the actual open→close transition or the focus hand-off. A genuine state-transition truth, not resolvable from presence and wiring alone; routed to human verification, not counted toward the verified score |
| 28 | Device UAT is served from a production build (documented), and the page paints something in the App's own voice before IndexedDB opens, with Home showing a loading line rather than nothing while its own reads are in flight (G-03.4-1 code half, D-06, D-08) | ✓ VERIFIED | `.claude/CLAUDE.md` states the build/preview convention and the one-Vite-process note (lines 47-48, grep-confirmed); `main.jsx` calls `root.render(<p className="app-boot">...)` BEFORE `await seedIfEmpty(repository)` (confirmed by direct read — this ordering is guaranteed by JS's synchronous-execution-before-await semantics, not merely asserted); the failure path renders the same class through the same root rather than writing `textContent`; `RecipeList` renders `<p className="home__loading">Loading your recipes…</p>` when `versions === null`, inside the same `.list-page`/`.home` wrapper (confirmed exactly one `className="list-page"` in the file); `RecipeList.test.jsx`'s new describe block actually exercises this — `renderToStaticMarkup` runs no effects, so the component genuinely renders in its loading state, and the test passes. The perceived on-device load-time improvement (the reason this gap existed) is carried to human verification, since a test cannot measure wall-clock feel |

**Score:** 27/28 truths verified (1 present, behaviour-unverified — the More panel's open/close state transition, G-03.4-3)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/styles/app.css` | per-context canvas on `body` | ✓ VERIFIED | `body`/`body:has(...)` rules confirmed present, no `html` selector |
| `app/src/styles/shell.css` | rail edge, focus fallback | ✓ VERIFIED | `.shell__rail`'s three new declarations and `.shell__place:focus` confirmed present |
| `app/src/styles/home.css` | 1099.98px stack step, phone-row hides | ✓ VERIFIED | both confirmed present; `home.css` carries exactly two `@media` blocks, in the documented order |
| `app/src/ui/Shell.jsx` | controlled More disclosure | ✓ VERIFIED (wired; behaviour unverified — see truth 27) | `useLocation`, `useEffect`, `moreOpen`, `closeMore`, controlled `open`/`onToggle` all confirmed present |
| `app/src/ui/RecipeList.jsx` | loading state, trimmed lead meta | ✓ VERIFIED | `home__loading` branch and `versionIdentity`-only meta line confirmed present |
| `app/src/main.jsx` | two-phase render | ✓ VERIFIED | boot line before the awaited seed, confirmed by direct read |
| `.claude/CLAUDE.md` | device-UAT convention | ✓ VERIFIED | build/preview line and one-Vite-process note confirmed present |
| `.planning/.../03.4-GAPS-CONFORMANCE.md` | rows 28/31/32 resolved, departures updated | ✓ VERIFIED | rows read `matches`; departure #2 closed; departure #5 still open (not part of any of the eight gap IDs this round closed) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `body`'s own background | the CSS canvas / iOS rubber-band region | UA propagation from a transparent `html` | ✓ WIRED (structural) | confirmed: no `html` rule exists; `body` declares the App ground by default |
| `.shell__rail` | `--gap-*`, `--app-rule-row`, `--app-divider` | CSS `var()` reads | ✓ WIRED | confirmed, no literals |
| `.shell__place:focus` | `--focus-outline-width`/`--focus-outline-offset` | CSS `var()` reads | ✓ WIRED | confirmed; shared, unprefixed tokens per D-02 |
| the `1099.98px` block | DESIGN.md's own responsive ladder | the same number, not a new one | ✓ WIRED | `grep -c "@media (max-width: 1099.98px)"` is 1; DESIGN.md line 352 already names it |
| `.home__row .home__rail`/`.home__meta`/`.home__action--secondary` | the phone-width row | scoped hides, never bare | ✓ WIRED | `home.test.js`'s negative pin confirms the lead's own second action cannot be caught |
| the `<ul>`'s `onClick={closeMore}` | all five More items | one handler, not five | ✓ WIRED (untested transition — see truth 27) | confirmed at Shell.jsx:295 |
| `useLocation().pathname` | the effect that closes More | dependency array | ✓ WIRED (untested transition — see truth 27) | confirmed at Shell.jsx:152-154 |
| `root.render(...)` (boot line) | the awaited `seedIfEmpty` | statement order, before the `await` | ✓ WIRED | confirmed by direct read of main.jsx |
| `versions === null` | `RecipeList`'s loading branch | conditional render inside the one wrapper | ✓ WIRED | confirmed and behaviourally tested (`RecipeList.test.jsx`, `renderToStaticMarkup` naturally captures the pre-effect state) |

### Requirements Coverage

No requirement IDs are mapped to this phase in REQUIREMENTS.md (a design-delivery phase governed by CONTEXT.md's D-01..D-20, confirmed by grep — no "03.4" row exists there). UX1-01 and UX1-03 apply as build constraints, verified end-to-end in Phase 4:

| Requirement | Description | Status | Evidence |
|-------------|--------------|--------|----------|
| UX1-01 | Keyboard-operable, visible focus, WCAG 2.2 AA contrast | ? NEEDS HUMAN | the focus-fallback rule (G-03.4-4) is now present and pinned; actual ring visibility, traversal order, and no-trap confirmation on the iPad remain a device check, carried to human verification |
| UX1-03 | No colour-only signal | ✓ SATISFIED | unchanged: every standing states its word explicitly; the phone-width row's tally removal (G-03.4-6b) takes the count's marks AND its words out together — UX1-03 forbids a count carried by marks alone, not a count required at every width, so this does not weaken the requirement |

No orphaned requirements found for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/src/ui/Shell.jsx` | comment above the tab row (~274-280) | Comment claims focus always returns to More's summary "in both cases"; the route-change effect (152-154) does not call `.focus()`, only the click path does | ⚠️ Warning (WR-01, new — 03.4-REVIEW.md, confirmed still present) | documentation-accuracy defect, not functional; recorded as advisory above |
| `app/src/styles/shell.css` | `.shell__more`/`.shell__more > ul` | The More disclosure's native `<summary>` marker (`::-webkit-details-marker`) is still never suppressed | ℹ️ Info (WR-02, carried forward, still technically present) | UAT test 8 found no visible marker on Mark's device — a contradicting real observation recorded as advisory, not a live defect |
| `app/src/ui/RecipeList.jsx` | 109, 229 (unchanged) | `entry.name` rendered with no fallback for a blank/whitespace name | ⚠️ Warning (WR-03/prior WR-02, carried forward) | unchanged by plans 09-13; no upstream validation reviewed |
| `app/src/styles/home.css` | 357-359 and 434-436 | `.home__lead .home__rail { display: none; }` declared identically in both the 1099.98px and 759.98px blocks — the narrower query is a strict subset, so the second declaration is a dead duplicate | ℹ️ Info (IN-01, 03.4-REVIEW.md) | harmless today; a future edit to one block could silently drift from the other |
| `app/src/ui/RecipeList.test.jsx` | 266, 274 (unchanged) | greedy, unanchored regex assertions | ℹ️ Info (IN-02, carried forward) | currently correct by coincidence of markup shape |
| `app/src/ui/RecipeList.jsx` | 43 | the new `.home__loading` paragraph carries no `aria-live`/`role="status"` | ℹ️ Info (IN-03, new — 03.4-REVIEW.md) | a screen-reader user gets no announcement of the loading-to-loaded transition; minor, transition is typically brief |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK` markers found in any file this round's five plans modified (confirmed by direct grep; the one `PLACEHOLDER` match is the pre-existing `Placeholder.jsx` component name, not a debt marker).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite | `npm --prefix app test -- --run` | 1145 passed, 0 failed, 41 files | ✓ PASS |
| Production build | `npm --prefix app run build` | 126 modules transformed, succeeded, ~126 KB gzipped JS | ✓ PASS |
| Canvas per context (G-03.4-9) | `grep -n "^body {" -A3 app/src/styles/app.css` and `grep -n "body:has" app/src/styles/app.css` | `body` declares `--app-background`; `body:has(.recipe-page, .page-head, .not-found)` declares `--sheet-ground`; no `html` selector | ✓ PASS |
| Rail edge (G-03.4-7) | `sed -n '/\.shell__rail {/,/^}/p' app/src/styles/shell.css` | `box-sizing: border-box`, padding, `border-right` all present | ✓ PASS |
| Focus fallback (G-03.4-4) | `grep -n "shell__place:focus" app/src/styles/shell.css` | rule present, reads both focus tokens | ✓ PASS |
| 1099.98px stack step (G-03.4-6) | `grep -n "@media" app/src/styles/home.css` | exactly two blocks, 1099.98px before 759.98px | ✓ PASS |
| Phone-row hides (G-03.4-6b) | `grep -n "home__row .home__rail\|home__row .home__meta\|home__row .home__action--secondary" app/src/styles/home.css` | all three present, scoped under `.home__row` | ✓ PASS |
| Lead meta line (G-03.4-6c) | `grep -n "versionIdentity\|computeBalance" app/src/ui/RecipeList.jsx` | `versionIdentity` present; `computeBalance` absent | ✓ PASS |
| More disclosure wiring (G-03.4-3) | `grep -n "useLocation\|useEffect\|onToggle\|onClick={closeMore}" app/src/ui/Shell.jsx` | all four present | ✓ PASS (wiring only — see truth 27 for the untested transition) |
| Two-phase render (G-03.4-1) | direct read of `app/src/main.jsx` | boot line rendered before `await seedIfEmpty`, failure path rendered through the same root | ✓ PASS |
| Home loading state (G-03.4-1) | `npm --prefix app test -- --run src/ui/RecipeList.test.jsx` | the new loading-state describe block passes | ✓ PASS |
| Device-UAT convention (G-03.4-1) | `grep -n "run preview" .claude/CLAUDE.md` | present, line 47 | ✓ PASS |
| No debt markers in round-two files | grep across all nine files the five plans touched | empty (one `Placeholder.jsx` name false-positive excluded) | ✓ PASS |
| Commits present | `git log --oneline` for all 14 task commits across plans 09-13 | all 14 present | ✓ PASS |

### Probe Execution

`03.4-gaps-conformance-probe.js` is unchanged this round — a browser-paste reader, not a `scripts/*/tests/probe-*.sh` shell probe. `node --check` confirms it is still syntactically valid. Not executed by this verifier, matching the prior round's own finding — this is not a gap.

## Human Verification Required

### 1. iOS/Safari elastic-overscroll colour (G-03.4-9)

**Test:** On the iPhone or iPad, scroll past the top or bottom of Home or a placeholder route far enough to trigger the rubber-band effect.
**Expected:** The revealed colour is the App's white, not the Sheet's cream; a recipe route still reveals cream.
**Why human:** WebKit-only rendering artifact from `body`'s own background, invisible to any DOM/CSS test. Confirmed broken ("cream on all pages") before this fix; not re-tested since.

### 2. The rail's edge at 1280px (G-03.4-7)

**Test:** At desktop width, look at the rail's right edge and its inner spacing.
**Expected:** A hairline border in the divider grey, the board's own inner padding, and an unchanged 224px overall width.
**Why human:** A rendered spacing fact; the CSS is confirmed present but not confirmed to render correctly.

### 3. Focus rings on Mark's iPad, Full Keyboard Access on (G-03.4-4)

**Test:** Tab through the tools row, the rail's places, and (at phone width) the tab row and More.
**Expected:** A visible ring at every stop, in order, no trap.
**Why human:** Confirmed absent on the iPad (even with the accessibility setting on) before this fix; the new fallback rule is present but not re-tested on the device.

### 4. The Home lead and rows from 760px to 1100px, and the phone-width row at 393px (G-03.4-6, G-03.4-6b, G-03.4-6c)

**Test:** Resize through 760, 820, 900, 1000, 1090, and 1100px, then view at 393px.
**Expected:** No crushed columns, no spilling text, no sideways scroll anywhere in the band, and the row/lead forms return unchanged at 1100px; at 393px a row shows only identity, standing, and one filled action, with the lead above it keeping both of its actions and a meta line with no trailing gram figure.
**Why human:** Confirmed broken at 760px before this fix; the new CSS step and the phone-width hides are present and pinned by tests, but not re-measured on a device since.

### 5. More's dismissal at 393px, end to end (G-03.4-3 half A)

**Test:** See `behavior_unverified_items` above — open More from a non-Home route, choose each of its five items in turn, and separately trigger a route change from outside More while it is open.
**Expected:** The panel closes in every case; focus lands on the summary, never the document body.
**Why human:** A genuine state transition (open state, route dependency, focus target) that no test in this codebase's static-markup harness can exercise. This is not merely deferred out of caution — it is structurally untestable with the current suite.

### 6. Cold load on Mark's iPad, served from the production build (G-03.4-1)

**Test:** `npm --prefix app run build && npm --prefix app run preview -- --host`, opened on the iPad from a cold load.
**Expected:** An `.app-boot` line appears promptly on the App's own ground, then the shell and Home follow, with Home showing its loading line before the list appears — no multi-second blank page.
**Why human:** Perceived load time on the actual device, served the actual way, is a timed/rendered fact no unit test can measure. The code-level fixes (two-phase render, loading state, the documented convention) are all present and test-pinned.

### 7. The still-open conformance departure: the single action's stretch at 393px

**Test:** Review `03.4-GAPS-CONFORMANCE.md`'s departure #5 — the one remaining filled action still stretches to the row's full width at 393px, where board 171 draws it at natural width.
**Expected:** Mark rules on whether the stretch stays (it is 03.4-03's own pre-existing touch rule, untouched by this round) or should be corrected.
**Why human:** A known, already-recorded design departure UAT round two's rulings (tests 6 and 7) did not reach — not a code defect, not part of any of this round's eight gap IDs, carried forward so it is not lost.

## Gaps Summary

All eight gap IDs UAT round two logged are closed in the source, each confirmed by direct inspection of the current codebase — not by trusting any of plans 09-13's SUMMARY.md claims:

1. **G-03.4-9 (iOS overscroll cream)** — closed. The App/Sheet split now lives on `body`, the element the UA actually propagates to the canvas, replacing the `.shell`-only mechanism that could never reach it.
2. **G-03.4-7 (rail's missing edge)** — closed. Padding and a hairline border added, footprint unchanged.
3. **G-03.4-4 (no focus ring on the iPad)** — closed at the mechanism level: a scoped `:focus` fallback beside the project's one `:focus-visible` rule.
4. **G-03.4-6 (lead/row crush 760-1100px)** — closed. A new step, reusing the ladder's own 1099.98px number, stacks both the lead and the rows in that band.
5. **G-03.4-6b (phone row should match board 171 exactly)** — closed. Rod, tally, and secondary action all hidden at 393px, scoped so the lead cannot lose its own second action.
6. **G-03.4-6c (lead meta line shouldn't carry grams)** — closed. The computed mass line and its two now-orphaned imports are gone.
7. **G-03.4-3 half A (More stays open after selection)** — closed at the *code* level: the panel's open state is now React's, closing on item activation and on route change, with focus returned to the summary. **This is the one gap of the eight that remains behaviourally unverified** — no test in this codebase can exercise a `<details>` toggle, an effect, or a route change, so the actual transition has not been proven to work, only shown to be wired. Half B (import feedback/navigation from a non-Home route) was explicitly out of scope, deferred by Mark on 2026-09-22, and is not a must-have of this phase.
8. **G-03.4-1 code half (iPad load "measured in seconds")** — closed. A documented build-serving convention, a two-phase render that paints before IndexedDB opens, and a loading line for Home's own reads — all three present and, where testable, actually tested (the loading-state branch has a real passing behavioural test; the render-order guarantee is a deterministic JS fact, not an assertion of trust).

**Two truths were upgraded this round by direct device evidence, not source inspection:** truth 6/15 (the hand's rendering and its forced-colours/print fallback) and truth 11 (the tab row's fit at 393px) both moved from "structural, unexercised" to fully VERIFIED because Mark's own UAT round two tested them directly and they passed — the strongest form of evidence this verifier can cite.

**One new documentation-accuracy issue** (REVIEW.md's WR-01: a comment overclaiming focus-return coverage) is recorded as advisory — it does not affect the phase goal, but it is not silently dropped either.

Because six of the eight gap closures (all but G-03.4-1's documentation half and G-03.4-6c's meta-line trim, which are fully confirmed) rest on a confirmed prior UAT failure that has not yet been re-tested since the code fix, and because G-03.4-3 half A is genuinely untestable by this codebase's own harness, this phase's status remains **human_needed**, not `passed`. This is not a regression from the prior round's own human_needed status — it reflects the same discipline the prior verification applied to the tab row (present, structurally correct, rendered outcome not yet proven), now applied consistently across all eight of this round's closures, with two of them already promoted to fully verified by Mark's own direct testing.

---

_Verified: 2026-09-22T17:45:00Z_
_Verifier: Claude (gsd-verifier)_
