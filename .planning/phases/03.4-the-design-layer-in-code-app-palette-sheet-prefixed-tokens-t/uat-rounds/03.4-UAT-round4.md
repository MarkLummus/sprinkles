---
status: resolved
phase: 03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t
source: [03.4-VERIFICATION.md (third re-verification after gap closure round three, 2026-09-22, human_verification items); round three record in 03.4-UAT-round3.md]
started: 2026-09-22T19:43:38Z
updated: 2026-09-22T22:42:00Z
---

## Current Test

[testing complete]
## Tests

### 1. Tab rings every shell stop on the iPad once focus is in the page (G-03.4-r3-3, truth a)
expected: Serve the build (`npm --prefix app run build && npm --prefix app run preview -- --host`, port 4173) on the iPad, hardware keyboard attached, Full Keyboard Access on. On Home, tap once on the page body away from any control, then press Tab repeatedly. A ring at Search, Import, Export, Home, Notebook, Recipe book, Idea log, Ingredients, Kitchen, in that order, then on into the page's own controls; Shift-Tab walks back; nothing traps. Pass or fail the gap on this alone.
result: issue
reported: "fail. rings on tabs, search and import/export, then into the page, and then I lose focus on everything. keyboard stops working utnil I recycle power."
severity: blocker

### 2. Recorded, not asserted: Tab from Safari's own chrome entering the page (G-03.4-r3-3, truth b)
expected: Force-quit Safari with Full Keyboard Access already on, reopen the preview build, tap the address bar, and press Tab until focus leaves the chrome. Record which happens, as a note, never as a fail — (1) a ring on Search: the hand-off works and the fix is complete end to end; (2) rings on Import and Export only: the link attribute did not ship, a code failure and impossible after 03.4-14; (3) focus cycles the chrome and never enters the page: Safari does not hand focus to the web view here, tap-then-Tab is the procedure on this device, and test 1 still holds. Optionally re-run the bookmarklet probe from .planning/debug/ipad-tab-never-enters-app.md.
result: pass
note: "outcome (3) — focus cycles the chrome and never enters the page; and now I need to power cycle the device to regain use of the keyboard" (recorded as a note per the test's own contract; the keyboard lock-up reproduced here with focus never having entered the page, which is evidence for G-03.4-r4-1)

## Summary

total: 2
passed: 1
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-03.4-r4-1
  truth: "On the iPad with Full Keyboard Access on, after a tap into the page, Tab rings Search, Import, Export and every rail place in order, then the page's own controls; Shift-Tab walks back; nothing traps and the keyboard keeps working"
  status: resolved
  resolved_by: "03.4-15 (commits cbbc4b1..835c2ae): tabIndex={0} on all 17 Link sites outside the shell, pinned on rendered markup; convention recorded in .claude/CLAUDE.md. Re-verified 2026-09-22 (39/39 truths). S1 device confirmation is round five tests 1 and 2 in 03.4-UAT.md; S2 (keyboard dead until power cycle) is environmental, recorded there as test 3, never asserted."
  resolved_at: 2026-09-22
  reason: "User reported: fail. rings on tabs, search and import/export, then into the page, and then I lose focus on everything. keyboard stops working utnil I recycle power."
  severity: blocker
  test: 1
  observation: "Test 2 (Tab from Safari's own chrome, focus never entering the page) also ended with the keyboard dead until a power cycle — the lock-up reproduces without page focus"
  root_cause: "Two faults. S1 (app + platform, proven from WebKit source): every control on Home is a react-router <Link> rendering <a href> with no tabindex; WebKit only lets Tab reach such a link when TabsToLinks is on (off on Apple platforms, unaffected by Full Keyboard Access), so after the ninth shell stop (Kitchen) there is no reachable element and FocusController::relinquishFocusToChrome clears the focused element and hands focus to Safari, which never hands it back — 'then I lose focus on everything'. The 03.4-14 tabindex covered the shell only (device now confirms those nine ring). Also, a tap sets WebKit's Tab starting point at the tapped node, so tapping in the content column starts after every shell stop. S2 (keyboard dead until power cycle): the app cannot cause it — Home runs no key handlers, focus calls, timers or observers, no keydown reaches the page under FKA, and it reproduced in test 2 with focus never in the page. Three candidates remain below the page: A Safari resting focus somewhere invisible after takeFocus; B the Full Keyboard Access service stalling device-wide; C iPadOS 26.x external-keyboard input failure (publicly reported). One on-device step separates them."
  artifacts:
    - path: "app/src/ui/RecipeList.jsx"
      issue: "All nine Home controls are tabindex-less <Link>s (lines 63, 67, 110, 170, 180, 183, 192, 195, 229); Tab cannot reach them on iPadOS Safari"
    - path: "app/src/router.jsx"
      issue: "Running-head 'Sprinkles' <Link> on recipe pages (line 63) has the same gap; RecipeHistory.jsx, VersionRow.jsx, BatchRow.jsx, RecipePage.jsx <Link> sites unchecked"
    - path: "app/src/ui/Shell.jsx"
      issue: "03.4-14 tabindex is correct and device-confirmed; it covers the shell only"
    - path: ".planning/phases/03.4-the-design-layer-in-code-app-palette-sheet-prefixed-tokens-t/03.4-UAT.md"
      issue: "Test 1's 'tap once on the page body' sets the Tab start after the shell; should read 'tap beside the Sprinkles wordmark'"
  missing:
    - "S1: explicit tabindex on Home's links (and, if Mark rules so, every <a> the app renders), pinned by a rendered-markup test as 03.4-14 did"
    - "S1: reword the UAT step to tap beside the wordmark, not the page body"
    - "S2: no code change; on-device discrimination next round — note iPadOS version and keyboard; when the keyboard dies try in order Tab-H/Fn-H, address bar typing, Notes typing, FKA off, keyboard reattach; control run on a blank tab and an ordinary site (procedure at the end of the debug file)"
  debug_session: ".planning/debug/ipad-keyboard-locks-after-tab.md"

## Deferred Follow-Ups

- test: 3
  idea: "Import feedback: after a successful import from a non-Home route nothing changes on screen (Placeholder ignores the Outlet context) and .shell__import-errors sits in the header, off-screen at 393px. Options: navigate('/') after import, or a role=status live region beside the controls announcing the result. See .planning/debug/more-panel-stays-open-import-no-home.md half B."
  deferred_at: 2026-09-22
  carried_from: round three (03.4-UAT-round3.md)
- test: 1
  idea: "add a todo to change this so that all pages use white" (overscroll canvas white on every route, including the paper frames)
  deferred_at: 2026-09-22
  carried_from: round three (03.4-UAT-round3.md)
