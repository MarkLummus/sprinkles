---
phase: 03.5-separate-the-recipe-from-the-sheet
plan: 07
type: execute
wave: 6
depends_on: ["03.5-06"]
files_modified:
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/useBelowDesktop.js
  - app/src/styles/notebook.css
  - app/src/styles/tokens.css
  - app/src/styles/app.css
  - app/src/styles/cross-cutting.test.js
  - .planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md
autonomous: false
requirements: [UX1-01, UX1-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The batch in view reads in App context in the log column: beside the Sheet at desktop (340px) and iPad landscape (300px), below it at iPad portrait and phone (layout C, settled decision 1; the batch brief's 2026-09-23 amendment)."
    - "The log's head carries the 'Batch' caption and 'churned <date>' on the left. On the right are the Batches (n) chooser, Correct and Record another as underlined companion-blue controls; Record another moves here from the version column (1600-batch.html; route-recipe.md § 6's open item on where the chooser sits is settled by the board)."
    - "The churn cells read as the board draws them: a two-column grid of caps labels, 20px values with units, 'plan …' beneath the draw-temperature cell, and 'not measured' where a value is blank. The maker's own words are in the hand: At the machine, Ingredient notes, Problems and Next time. 'Tasted <date>' or 'tasted date unknown' heads the Tasting section, and 'Recorded <date> against <version identity>' closes the log. A blank stays visibly blank and never falls back to the plan (No-Verdict and blank-stays-blank rules)."
    - "A version with no batch shows 'Not yet churned. Print the sheet, make it, then record what happened.' and 'Record a batch' as the filled action (1600-no-batch.html). The board's 'Print sheet' control is Phase 4's and is not built here."
    - "The log stays visible while the version pen is open, read-only: its controls and links are withheld, as every other opener is while a pen is open (1600-pen.html draws the log unchanged beside the pen)."
    - "Recording and correcting a batch still work end to end, with the record pen placed in layout C as Mark decides at this plan's checkpoint. The battery's grammar (joined stops, segmented controls, defect toggles, Clear, the two save ceremonies, blank-stays-blank, no defaults) is unchanged."
    - "Every link the log renders carries tabIndex={0}, and BatchRow's test pins the exact count (UX1-01)."
  artifacts:
    - path: app/src/ui/BatchRow.jsx
      provides: "the App-context reading log, the no-batch state, the head's three controls, and the record pen's placement"
    - path: app/src/styles/notebook.css
      provides: ".notebook-log rules for the reading log (scoped `.notebook-log .batch-row…`)"
  key_links:
    - from: app/src/ui/BatchRow.jsx
      to: app/src/ui/RecipePage.jsx
      via: "onStartRecording and the Record opener's focus-return now live in BatchRow; RecipePage renders BatchRow in every mode"
      pattern: "onStartRecording"
  prohibitions:
    - "No 'Print sheet' control (print is Phase 4; CONTEXT.md Out)."
    - "No new visual treatment is invented for the record pen: its App-context restyle is 'drawn on the 03.5 canvas, not invented by a builder' (route-recipe-batch.md amendment), and the canvas did not draw it. The checkpoint below decides what ships."
    - "No phone logging work (deferred to the phone-jobs brief, settled decision 14)."
---

<objective>
Move the batch log into App context, built to sketch 011. The batch in view reads as the boards draw it in the log column, the no-batch state offers Record a batch, and the record pen goes where Mark decides, because the canvas drew the log's reading state only.

Purpose: a batch and its tastings are a process, and on screen they are App context (route-recipe.md § 03.5 revision part 2). Only the as-made grams and Instructions changes stay on the Sheet, in the hand (plan 06).
Output: BatchRow's App reading state and head controls, the no-batch state, the Record opener moved from the version column, the log visible under the version pen, and the record pen's placement per the decision.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.5-separate-the-recipe-from-the-sheet/03.5-CONTEXT.md
@.planning/phases/03.5-separate-the-recipe-from-the-sheet/03.5-RESEARCH.md
@.planning/phases/03.5-separate-the-recipe-from-the-sheet/03.5-06-SUMMARY.md
@.planning/sketches/011-recipe-route-c/README.md
@.impeccable/surfaces/route-recipe-batch.md
@.claude/skills/sketch-findings-sprinkles/SKILL.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Where |
|---|---|---|
| `BatchRow` props `versionName` (the identity text for the Recorded line) and `onStartRecording` (moved from VersionRow) | component props | app/src/ui/BatchRow.jsx |
| `NO_BATCH_PROSE = 'Not yet churned. Print the sheet, make it, then record what happened.'` | constant | app/src/ui/BatchRow.jsx |
| `useLogBesideSheet()`, `LOG_BESIDE_SHEET_QUERY '(min-width: 1100px)'` (option A only) | hook | app/src/ui/useBelowDesktop.js |
| `.notebook-log .batch-row…` rules and the log's App tokens | App CSS | notebook.css, tokens.css |

<decisions_recorded>
1. **Keep BatchRow's class names; restyle in App context by scope.** The reading state keeps its `batch-row__*` classes, which its 1393-line test pins. notebook.css restyles them under `.notebook-log` with --app-* tokens. app.css's Sheet rules for the same classes stay, because the record pen still uses them.
2. **The Recorded line names the version by identity** ('against Version 1 · 50 g oil · 800 g'), as the board does. RecipePage passes versionIdentity(...) as `versionName`; batches are listed per version, so the batch's version is the page's version. The Changed line keeps its existing words beneath it in the same style.
3. **Next time with no note** reads 'nothing written yet' in the secondary colour (1600-batch.html). With no tasting, the existing sentence 'This batch has not been tasted yet.' stays, restyled.
4. **Why the checkpoint.** route-recipe-batch.md's 2026-09-23 amendment says the battery's visual treatment "moves to App context and is drawn on the 03.5 canvas, not invented by a builder". Sketch 011 draws the log's reading state at four widths and never its recording state. The record pen in today's Sheet grammar is capped at a 640px frame with a wide axes arrangement, and does not fit a 340px or 300px column. That is missing information, not a difficulty, so it is Mark's call. The orchestrator should put the question to Mark before execution starts; if his answer is recorded here as a line "Record pen placement: A", "B" or "C", the executor takes it and continues.
Record pen placement: A (Mark, 2026-09-25: open the record pen in the column using sketch 008's approved narrow arrangement)
</decisions_recorded>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): The batch in view reads in App context in the log column end to end — head controls, cells, the hand, Tasting, and the no-batch state</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/VersionRow.jsx, app/src/ui/VersionRow.test.jsx, app/src/ui/RecipePage.jsx, app/src/styles/notebook.css, app/src/styles/tokens.css, app/src/styles/cross-cutting.test.js</files>
  <read_first>
    - .planning/sketches/011-recipe-route-c/1600-batch.html: the `<aside aria-label="Batch">` subtree (head row, Batches (1) / Correct / Record another as underlined 14px/600 companion-blue buttons with 18px gaps, the two-column cells grid at 16px 20px gaps, the caps labels, the 20px/600 values with 14px units, 'plan 10–12 min', 'not measured', the two Caveat 22px notes, the Tasting section's top rule and 14px padding, 'tasted date unknown' at 13px, Problems with 'Bitter' in Caveat, Melt test and Melt style, Next time 'nothing written yet', and 'Recorded 4 Aug 2026 against Version 1 · 50 g oil · 800 g' at 12px)
    - .planning/sketches/011-recipe-route-c/1600-no-batch.html: the aside's no-batch section (12px gap, the 15px secondary prose, Record a batch filled and Print sheet outlined)
    - .planning/sketches/011-recipe-route-c/1600-pen.html: the aside while the pen is open
    - app/src/ui/BatchRow.jsx (the head at 496-550, the reading state at 880-1004, TastingReading at 222-316, BatchHistoryPanel at 328-360)
    - app/src/ui/VersionRow.jsx (the Record opener and its focus-return ref/effect)
    - app/src/ui/RecipePage.jsx (the BatchRow render and its `mode !== 'developing'` condition; handleStartRecording)
    - app/src/ui/BatchRow.test.jsx (the reading-state, head and register-link assertions)
  </read_first>
  <behavior>
    - BatchRow.test.jsx (reading, the 2 Aug batch): the head's h2 reads 'Batch' and the date 'churned 2 Aug 2026'. The head's buttons appear in order: 'Batches (1)', 'Correct', 'Record another'. The cells read 'Time to draw temp.' 20 min with 'plan 10–12 min', 'Out of machine' −6 °C, 'Churn duration' 30 min, and 'Exit consistency' and 'Airiness' as 'not measured'. 'Soft, not greasy' and 'oil bottle opened 24 Jul' render with className app-hand. The Tasting head reads 'tasted date unknown'. 'Bitter' renders with app-hand under a 'Problems' caption. Next time reads 'nothing written yet'. The closing line reads 'Recorded 4 Aug 2026 against Version 1 · 50 g oil · 800 g'.
    - BatchRow.test.jsx (no batch): the prose NO_BATCH_PROSE and one button 'Record a batch'; no text 'Print sheet'.
    - BatchRow.test.jsx (openPen 'plan'): the reading content renders, and no Correct, Record another or Batches control renders.
    - BatchRow.test.jsx: the Batches register's links keep their exact count, and each carries tabindex="0".
    - VersionRow.test.jsx: the version column renders no Record button.
  </behavior>
  <action>
RED first; commit. Then GREEN:
- BatchRow.jsx head: keep the h2 with its landing focus and aria-label. The date stays. The right-hand group holds, in order, the Batches (n) disclosure (HistoryDisclosure, unchanged behaviour), Correct (only with no pen open and a batch in view) and the Record opener, moved from VersionRow with its ref and focus-return effect. It reads 'Record another' when a batch is in view and is rendered only with no pen open.
- The reading state (decisions_recorded 1-3): the five churn cells as they are; atTheMachine and ingredientNotes as `.app-hand` spans; TastingReading with its head reading `tasted ${recordDateWords(tastedDate)}` (so 'tasted date unknown' when blank), its cells, a 'Problems' caption with each defect word and the declared flaw as `.app-hand`, and Melt test and Melt style. Next time always renders its caption, with the note in `.app-hand` or 'nothing written yet'. The provenance line reads `Recorded ${recordDateWords(recordedAt)} against ${versionName}`, with the Changed line beneath it when present.
- The no-batch state (no batch and no pen): replace 'no batch yet' with the NO_BATCH_PROSE paragraph and a filled 'Record a batch' button that calls onStartRecording. No Print sheet.
- While any pen other than this batch's own is open, the reading state renders without its controls or links. The existing openPen checks already suppress them; extend them to the moved opener.
- VersionRow.jsx: delete the Record opener, its ref and effect, and the openBatch and onStartRecording props.
- RecipePage.jsx: render BatchRow in every mode (drop the `mode !== 'developing'` condition), passing versionName (versionIdentity over the recipe's sorted versions) and onStartRecording.
- notebook.css: the reading log's App rules, scoped `.notebook-log .batch-row…` and `.notebook-log .tasting-reading…`, value by value from the aside's inline styles, with the log's --app-* tokens (20px value, 13px, 15px sizes; the 16px/20px cell gaps; the 18px head gap). The record pen's own rules are not touched.
  </action>
  <acceptance_criteria>
    - `npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/VersionRow.test.jsx src/styles` passes.
    - `grep -c "Print sheet" app/src/ui/BatchRow.jsx` prints 0.
    - `grep -cE "onStartRecording|recordButtonRef" app/src/ui/VersionRow.jsx` prints 0.
    - `npm --prefix app test && npm --prefix app run build` pass.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test -- src/ui/BatchRow.test.jsx src/ui/VersionRow.test.jsx src/styles</automated>
    <fails_when>non-zero exit, or "failed" in the Vitest summary line</fails_when>
    <automated>npm --prefix app test && npm --prefix app run build</automated>
    <fails_when>either command exits non-zero</fails_when>
  </verify>
  <done>The log column reads the 2 Aug batch as 1600-batch.html draws it, and a version with no batch offers Record a batch. The log stays in view, read-only, under the version pen.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Where the record pen opens in layout C (sketch 011 draws the log's reading state only)</name>
  <read_first>
    - .planning/sketches/011-recipe-route-c/README.md (the boards table: no recording-state board exists)
    - .impeccable/surfaces/route-recipe-batch.md lines 14-24 (the 2026-09-23 amendment, "What restyles")
    - this plan's decisions_recorded item 4
  </read_first>
  <acceptance_criteria>
    - The chosen option id (option-a, option-b or option-c) is written into this plan's SUMMARY with Mark's words, and Task 3 builds that option only.
  </acceptance_criteria>
  <decision>Where and how the batch record pen (Record a batch / Record another / Correct) opens now that the log is a 340px or 300px column beside the Sheet at desktop and iPad landscape</decision>
  <context>
The record pen today is sketch 007/008's battery in Sheet grammar: joined stops, segmented controls, defect toggles, two save ceremonies, a 640px frame, and a wide axes arrangement above 760px with a stacked one below. Sketch 011 draws the log's reading state at every width and never its recording state. route-recipe-batch.md's 2026-09-23 amendment says the battery's visual treatment "moves to App context and is drawn on the 03.5 canvas, not invented by a builder". So the App-context record pen has no drawing to build to, and the Sheet-grammar pen does not fit the column as it stands.
Whatever is chosen, the as-made grams and step changes are still entered on the Sheet (plan 06), and the batch's own ceremony B stays at the Sheet's foot. If this plan's decisions_recorded already holds a line "Record pen placement: A", "B" or "C", take that answer and continue without stopping.
  </context>
  <options>
    <option id="option-a">
      <name>A — In the column, in its approved narrow arrangement (recommended)</name>
      <pros>No new drawing is invented: the pen keeps sketch 007/008's grammar and uses the stacked arrangement sketch 008 already drew for narrow widths whenever the log is a column. The log never moves while recording. A small change.</pros>
      <cons>The record pen stays in Sheet styling inside an App column until its App treatment is drawn (captured as a todo). At 1366 the column is 300px, tighter than the 353px phone content the stacked arrangement was drawn for, so plan 08 must measure for overflow.</cons>
    </option>
    <option id="option-b">
      <name>B — The log opens full width below the Sheet while a batch pen is open</name>
      <pros>The pen keeps its drawn wide arrangement and 640px frame unchanged at desktop, and nothing is squeezed.</pros>
      <cons>It adds an undrawn layout move: the log jumps from beside the Sheet to below it while recording and back on save or cancel. It is still Sheet-styled in App context, and still needs a todo for the App treatment.</cons>
    </option>
    <option id="option-c">
      <name>C — Draw it first</name>
      <pros>It honours the brief literally: the App-context record pen is drawn on the Claude Design canvas, snapshotted into sketch 011, and built to that drawing.</pros>
      <cons>This plan stops at this point. Option A's placement ships in the meantime so recording keeps working, and a follow-up gap-closure plan builds the drawing, which adds a design session and a plan to 03.5.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The record pen opens where the decision placed it, with its grammar and saves unchanged</name>
  <files>app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/ui/RecipePage.jsx, app/src/ui/useBelowDesktop.js, app/src/styles/notebook.css, app/src/styles/app.css, app/src/styles/tokens.css, .planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md</files>
  <read_first>
    - the Task 2 answer (the checkpoint response, or the "Record pen placement" line in decisions_recorded)
    - app/src/ui/BatchRow.jsx: useBelow760 and AxesGrid's `below` prop (108-220), the recording branch (553-880), `batch-margin--pen`
    - app/src/styles/app.css: `.batch-margin--pen`, the axes grid rules, and the (max-width: 759.98px) and (pointer: coarse) blocks that switch the narrow arrangement
    - app/src/ui/BatchRow.test.jsx: the AxesGrid arrangement cases
    - .planning/sketches/007-full-battery/ and 008 findings via .claude/skills/sketch-findings-sprinkles/references/batch-record-tasting-battery-structure.md (the narrow arrangement's DOM order)
  </read_first>
  <behavior>
    - Option A: BatchRow in recording mode, with the log beside the Sheet (useLogBesideSheet() true), renders AxesGrid with below={true} (the stacked core-then-declared order). With the log below the Sheet and a viewport of 760px or more it renders the wide order, as today.
    - Option B: RecipePage renders the `.notebook-body` element with the class `notebook-body--log-below` while openPen is 'record' or 'amend', and without it otherwise. A test of the exported class-name helper pins both.
    - Option C: option A's behaviour, plus a pending todo file for the canvas drawing.
    - Every option: the existing recording, amending, tasting add/remove/restore and save tests in BatchRow.test.jsx pass unchanged apart from a placement assertion.
  </behavior>
  <action>
RED first on the chosen option's behaviour; commit. Then GREEN for the chosen option only:
- Option A:
  - Add `LOG_BESIDE_SHEET_QUERY = '(min-width: 1100px)'` and `useLogBesideSheet()` to useBelowDesktop.js, node-guarded like useBelowDesktop.
  - In BatchRow, compute `below = below760 || logBesideSheet` for AxesGrid.
  - In notebook.css, scope the pen frame to the column: `.notebook-log .batch-margin--pen` fills the column rather than capping at --sheet-pen-w. Reapply the narrow arrangement's width rules (the stop track, field-row label widths, the churn row's wrap) under `.notebook-log` at the default, desktop condition, and hand back to app.css's viewport rules inside (max-width: 1099.98px), where the log is full width.
  - Record a pending todo, `.planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md`, stating that the brief's App treatment of the battery awaits a canvas drawing.
- Option B:
  - RecipePage renders `.notebook-body` with `notebook-body--log-below` while openPen is 'record' or 'amend', through a small exported pure helper `notebookBodyClass(openPen)`.
  - In notebook.css, `.notebook-body--log-below` stacks the log below the Sheet at every width with the log full width, mirroring the (max-width: 1099.98px) rules.
  - Record the same pending todo.
- Option C: ship option A's change, record the todo naming a canvas session, and state in the SUMMARY that a gap-closure plan builds the drawing.
In every option, focus on open and close, the churn-date block, Add tasting, Remove and Restore tasting, and both save ceremonies keep their existing behaviour and tests.
  </action>
  <acceptance_criteria>
    - `npm --prefix app test` passes in full, and `npm --prefix app run build` exits 0.
    - `ls .planning/todos/pending/ | grep -c "draw-the-record-pen-in-app-context"` prints a number >= 1.
    - The SUMMARY names the option taken and quotes the checkpoint answer.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix app test && npm --prefix app run build</automated>
    <fails_when>either command exits non-zero, or "failed" in the Vitest summary line</fails_when>
    <human-check>On the built app (`npm --prefix app run build && npm --prefix app run preview`, one Vite process) at 1600 and 1366: open Record another on the olive oil version. The record pen opens where the decision placed it, nothing in the log overflows its column horizontally (measure the real DOM: the log's scrollWidth equals its clientWidth), and Save batch still lands on the new batch.</human-check>
  </verify>
  <done>Recording, correcting and tasting work in layout C with the pen where Mark chose, the battery's grammar untouched, and the App-context drawing tracked as a todo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored batch text (maker's words) -> the log render | Rendered as text in the hand |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-21 | Tampering / XSS | log notes, defects, Next time | low | mitigate | React text only; no dangerouslySetInnerHTML |
| T-03.5-22 | Tampering (integrity) | the log visible under the version pen | low | mitigate | every control and link in the log is withheld while another pen is open, so no batch write can start mid-draft (existing openPen discipline, tested) |
</threat_model>

<verification>
- `npm --prefix app test` and `npm --prefix app run build` pass.
- The human-check in Task 3 is recorded for end-of-phase UAT; plan 08 measures the log against the boards at every width.
</verification>

<success_criteria>
- The log reads in App context as the boards draw it, at every rung.
- The no-batch state offers Record a batch; Print sheet waits for Phase 4.
- The record pen works where Mark decided, its grammar unchanged.
</success_criteria>

<output>
Create `.planning/phases/03.5-separate-the-recipe-from-the-sheet/03.5-07-SUMMARY.md` when done
</output>
