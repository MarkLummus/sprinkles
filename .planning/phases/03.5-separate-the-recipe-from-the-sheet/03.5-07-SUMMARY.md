---
phase: 03.5-separate-the-recipe-from-the-sheet
plan: 07
subsystem: ui
tags: [react, batch-log, app-context, css-tokens, sketch-011, media-query]

# Dependency graph
requires:
  - phase: 03.5-06
    provides: "IngredientTable/Method in style 6, the Sheet's own .sheet-hand, the step pen's Cancel/Done — this plan's read_first list, unchanged by this plan"
provides:
  - "BatchRow.jsx: the App-context reading log (head controls, churn cells, the hand, Tasting, the no-batch state), the Record opener moved from VersionRow into the head's own right-hand group"
  - "notebook.css/tokens.css: .notebook-log .batch-row…/.notebook-log .tasting-reading… — the reading log's own App-context restyle, scoped by class, four new tokens"
  - "useBelowDesktop.js: useLogBesideSheet()/LOG_BESIDE_SHEET_QUERY — the record pen's own arrangement query, feeding BatchRow's AxesGrid"
  - "The record pen opens inside the log column (option A) — sketch 007/008's grammar, saves and battery unchanged; its own App-context visual treatment is a pending todo, not built here"
affects: ["08"]

# Actuals (#2632)
actuals:
  tokens: 10478
  tasks: 3
  commits: 3
  plan_head_before: 75df60f5cf23cf11ef5d2b44eed4af9055278859

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A control that must be withheld while ANY pen is open (not only its own) reads a broader predicate (logControlsVisible = openPen === null || openPen === 'amend') than a control that opens its OWN pen (openPen === null alone) — the same component can carry both gates side by side for two different controls with two different exposure rules."
    - "One opener, two renderings: BatchRow's Record button shares a single ref/handler pair (recordButtonRef, onStartRecording) across two mutually-exclusive DOM sites — an underlined head-line link when a batch is in view, a filled .notebook-action CTA in the no-batch state — since exactly one of the two ever mounts for a given openBatch/batches.length combination."
    - "A CSS override scoped narrower than its target (.notebook-log .batch-margin--pen vs app.css's bare .batch-margin--pen) must hand itself back inside the SAME breakpoint app.css already declares, merged into that file's existing named @media block rather than a new one — notebook.css's own GUARD-05 contract pins exactly three named steps in file order, so a fourth or reordered block fails a style-contract test even though the rules inside it are correct."

key-files:
  created:
    - app/src/ui/useBelowDesktop.test.js
    - .planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md
  modified:
    - app/src/ui/BatchRow.jsx
    - app/src/ui/BatchRow.test.jsx
    - app/src/ui/VersionRow.jsx
    - app/src/ui/VersionRow.test.jsx
    - app/src/ui/RecipePage.jsx
    - app/src/ui/useBelowDesktop.js
    - app/src/styles/notebook.css
    - app/src/styles/tokens.css

key-decisions:
  - "Task 1's Batches disclosure and the churned date got two DIFFERENT visibility gates, not one: the churned date is text naming the batch in view, so it keeps its pre-existing openPen !== 'record' guard; the Batches control is an interactive disclosure, so it gained the broader logControlsVisible predicate (openPen === null || openPen === 'amend') that also hides it while the version pen ('plan') is open — closing a gap the must_haves called out (\"its controls and links are withheld\" while any pen is open) that the pre-existing code only handled for the record pen."
  - "TastingReading's defect words moved out of the shared \"Observations\" heading into their own \"Problems\" caption, matching the sketch's own separate block for it, while the marked-axis cells kept \"Observations\" unchanged — the plan's own behavior text names \"a 'Problems' caption\" specifically, and splitting only the piece it names avoids restructuring the axis-marks rendering the plan never asked to change."
  - "The Recorded line's version-identity text (versionName prop) falls back to openBatch.snapshot.versionLabel when absent (versionName ?? …) rather than assuming every caller supplies it — RecipePage.jsx always does, but the fallback keeps the component usable from a caller that has not computed it, at no cost to the real render path."
  - "Task 3's option A CSS override resets itself inside app.css's own EXISTING (max-width: 1099.98px) block (merged, not a new block) after notebook.test.js's GUARD-05 cardinality test caught a first pass that added a fourth, out-of-order @media step — corrected before it reached a task boundary."
  - "Task 3's useLogBesideSheet contract test was written after its implementation, not before, since the hook and the BatchRow union it feeds (below = below760 || logBesideSheet) were built alongside the CSS/DOM investigation the option required; documented as a deviation below rather than staged as a fake RED, following the 03-05 SUMMARY's own precedent for MVP mode (workflow.tdd_mode: false)."

patterns-established:
  - "The shared-opener-two-renderings pattern (above) — reused wherever a single action needs a different visual weight depending on whether its target already exists."

requirements-completed: [UX1-01, UX1-03]

coverage:
  - id: D1
    description: "The batch in view reads in App context in the log column: head controls (Batches, Correct, Record another), churn cells, the hand (At the machine, Ingredient notes, Problems, Next time), Tasting, and the Recorded line naming the version by identity"
    requirement: UX1-01
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx — the head/openers, the record's reading state, the tasting read view, and the Next time describe blocks"
        status: pass
      - kind: other
        ref: "npm --prefix app test (1256/49) && npm --prefix app run build (exit 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "A version with no batch shows NO_BATCH_PROSE and a filled Record a batch action; no Print sheet control"
    requirement: UX1-01
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#BatchRow — the no-batch state offers Record a batch (1600-no-batch.html)"
        status: pass
      - kind: other
        ref: "grep -c \"Print sheet\" app/src/ui/BatchRow.jsx == 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "The log stays visible, read-only, while the version pen is open: its Batches/Correct/Record controls are withheld, its links are withheld (existing openPen discipline extended)"
    requirement: UX1-01
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#BatchRow — the openers, present only with no pen open (D-05) — 'renders no Correct, Record another or Batches control while the plan pen is open'"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every link the log renders carries tabIndex={0}, with the exact rendered count pinned"
    requirement: UX1-01
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx#BatchHistoryPanel — the revealed Batches register — 'renders exactly 1 <a> opening tag, carrying tabindex=\"0\"'"
        status: pass
    human_judgment: false
  - id: D5
    description: "Recording, correcting and tasting work in layout C with the record pen opened in the log column (option A), the battery's grammar and saves unchanged"
    requirement: UX1-03
    verification:
      - kind: unit
        ref: "app/src/ui/BatchRow.test.jsx — the record and amend ceremony, the tasting section, the two segmented controls, ceremony A describe blocks (all pre-existing tests, unchanged, still passing)"
        status: pass
      - kind: other
        ref: "npm --prefix app test (1256/49) && npm --prefix app run build (exit 0)"
        status: pass
    human_judgment: true
    rationale: "Task 3's own <human-check> — at 1600/1366, open Record another, confirm the pen opens in the log column with no horizontal overflow (scrollWidth == clientWidth) and Save batch lands on the new batch — needs a real layout engine this worktree has no browser-automation tool for. Deferred to end-of-phase UAT per workflow.human_verify_mode: end-of-phase, logged to .planning/WINDOWS.md (id 12, kind unrun-verify)."

# Metrics
duration: ~28min
completed: 2026-09-25
status: complete
---

# Phase 03.5 Plan 07: The batch log in App context, the Record opener moved, the record pen in the column Summary

**BatchRow.jsx now renders the App-context reading log (head controls, churn cells, the maker's words in the hand, Tasting, the no-batch state) with the Record opener moved out of VersionRow.jsx, and the record pen opens inside the log column — option A, Mark's 2026-09-25 checkpoint answer — using sketch 008's already-approved narrow arrangement, the battery's grammar and saves untouched.**

## Performance

- **Duration:** ~28 min
- **Started:** 2026-09-25T12:48:54Z (the phase's prior wave-5 commit)
- **Completed:** 2026-09-25T13:14:48Z
- **Tasks:** 3 (Task 1 tracer/TDD, Task 2 checkpoint:decision pre-answered, Task 3 auto/TDD)
- **Files modified:** 10 (2 new, 8 modified)

## Accomplishments

- `app/src/ui/BatchRow.jsx` — the head's right-hand group now reads Batches (n), Correct, Record another, in that order, each gated `openPen === null` (Correct/Record) or the new `logControlsVisible` predicate (`openPen === null || openPen === 'amend'`, Batches/date); the version pen ('plan') now withholds the Batches control too, closing a gap the pre-existing `openPen !== 'record'` guard left open. The no-batch state renders the exported `NO_BATCH_PROSE` constant plus a filled `.notebook-action` "Record a batch" button sharing the head's own opener ref. `atTheMachine`, `ingredientNotes`, and the tasting's defect words render as `.app-hand` spans; TastingReading's Problems words split into their own caption, separate from the marked-axis "Observations" block. Next time always renders its caption, reading "nothing written yet" (the same `batch-row__unit--absent` voice as an absent measured value) when blank. The Recorded line reads the new `versionName` prop (`Recorded 4 Aug 2026 against Version 1 · 50 g oil · 800 g`) instead of the bare snapshot label. The record pen's axes now stack (`below = below760 || logBesideSheet`) whenever the log sits beside the Sheet, not only below 760px.
- `app/src/ui/VersionRow.jsx` — the Record opener, its focus-return ref/effect, and the `openBatch`/`onStartRecording` props are gone; the acts group holds only Next version and Show changes.
- `app/src/ui/RecipePage.jsx` — `BatchRow` renders in every mode (the `mode !== 'developing'` wrapper is removed), passing `versionName={versionIdentity(sortedVersions(versionsForRecipe(versions, version.recipeId)), version)}` and `onStartRecording={handleStartRecording}`; `VersionRow` no longer receives `openBatch`/`onStartRecording`.
- `app/src/ui/useBelowDesktop.js` — `LOG_BESIDE_SHEET_QUERY` (`'(min-width: 1100px)'`) and `useLogBesideSheet()`, node-guarded like the existing `useBelowDesktop`; a new `useBelowDesktop.test.js` pins both hooks' exports.
- `app/src/styles/notebook.css`/`tokens.css` — the reading log's own App rules, scoped `.notebook-log .batch-row…`/`.notebook-log .tasting-reading…`, with four new tokens (`--app-notebook-log-value-size` 20px, `--app-notebook-log-tasted-size` 13px, `--app-notebook-log-cell-gap-row` 16px, `--app-notebook-log-head-gap` 18px) read off 1600-batch.html's own inline styles. The record pen's own frame (`.notebook-log .batch-margin--pen`, the stop track, field-row label widths) fills the column, merged into the file's existing `(max-width: 1099.98px)` step so notebook.test.js's three-named-@media-steps contract stays intact — control hands back to app.css's own rules there, where the log stacks full width below the Sheet.
- `.planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md` — the App-context battery treatment (`route-recipe-batch.md`'s 2026-09-23 amendment) still awaits a canvas drawing; this plan ships the pen's placement in Sheet-grammar CSS, not its redraw.

## Task Commits

1. **Task 1 (tracer): The batch in view reads in App context** — `5ddc9a1` (test), `225275c` (feat)
2. **Task 2: Where the record pen opens (checkpoint:decision)** — pre-answered in the plan's own `decisions_recorded`; no separate commit (a decision record, not code)
3. **Task 3: The record pen opens where the decision placed it (option A)** — `726dac0` (test+feat combined; see Deviations)

**Plan metadata:** committed alongside this SUMMARY.

_Tracer feedback gate (Task 1, `checkpoints.md`): `workflow.human_verify_mode` is `end-of-phase` and Task 1's `<verify>` carried only `<automated>` blocks — per the executor's own protocol this re-ran the tracer's `<verify>` (the scoped test grep and `npm --prefix app test && npm --prefix app run build`) and, on success, continued straight to Task 2 with no checkpoint. Logged `⚡ Tracer verified end-to-end — expanding`._

_Task 2 decision (checkpoint:decision, pre-answered per the plan's own `decisions_recorded` item 4 and this plan's `<project_notes>`): "Record pen placement: A (Mark, 2026-09-25: open the record pen in the column using sketch 008's approved narrow arrangement)." Option A selected — no new drawing invented, the stacked arrangement sketch 008 already drew for narrow widths is reused whenever the log is a column._

## Files Created/Modified

- `app/src/ui/BatchRow.jsx` — head controls (Batches/Correct/Record), the no-batch state, the hand, Problems split out, always-render Next time, versionName provenance, the pen's arrangement union
- `app/src/ui/BatchRow.test.jsx` — updated head/openers/provenance/hand/Problems/no-batch tests
- `app/src/ui/VersionRow.jsx` — Record opener and its ref/effect removed
- `app/src/ui/VersionRow.test.jsx` — Record-opener tests retired
- `app/src/ui/RecipePage.jsx` — BatchRow renders unconditionally with versionName/onStartRecording; VersionRow's Record props dropped
- `app/src/ui/useBelowDesktop.js` — `LOG_BESIDE_SHEET_QUERY`/`useLogBesideSheet`
- `app/src/ui/useBelowDesktop.test.js` (new) — contract pins for both hooks' exports
- `app/src/styles/notebook.css` — the reading log's App restyle; the record pen's column-filling frame, merged into the existing 1099.98px step
- `app/src/styles/tokens.css` — four new `--app-notebook-log-*` tokens
- `.planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md` (new) — the App-context battery drawing, still pending

## Decisions Made

See `key-decisions` in the frontmatter above.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] A first pass at Task 3's CSS reset added a fourth, out-of-order @media block**
- **Found during:** Task 3, running the full test suite after the first implementation pass
- **Issue:** The record pen's own `(max-width: 1099.98px)` reset rules were written as a new, standalone `@media` block placed before notebook.css's existing `1499.98px` step — `notebook.test.js`'s GUARD-05 cardinality test (which pins exactly three named `@media` steps, in file order: 1499.98px, 1099.98px, 759.98px) failed immediately.
- **Fix:** Removed the standalone block and merged its rules into the file's existing `(max-width: 1099.98px)` block, in the correct file position.
- **Files modified:** `app/src/styles/notebook.css`
- **Verification:** Full test suite passes; `notebook.test.js`'s cardinality assertion now reads exactly three steps in the original order.
- **Committed in:** `726dac0` (Task 3's own commit — caught and fixed before that commit, not a follow-up)

---

**Total deviations:** 1 auto-fixed (1 bug, caught and corrected within the same task before its own commit).
**Impact on plan:** No scope creep — a self-corrected implementation mistake against a pre-existing style contract, not a plan gap.

### Documented (not a Rule 1-4 fix)

**Task 3's RED/GREEN order.** `useBelowDesktop.test.js`'s two assertions (pinning `LOG_BESIDE_SHEET_QUERY`'s value and `useLogBesideSheet`'s export shape) were written after `useLogBesideSheet` was already implemented, not before — the hook and the `below = below760 || logBesideSheet` union it feeds into `BatchRow.jsx` were built together with the CSS/DOM investigation the option required (which pen frame rules exist, at which breakpoints, and how the narrow arrangement's stacking already worked via the `.axes-grid--stacked` class). Task 3's own commit therefore combines test and feat rather than the plan's stated "RED first; commit. Then GREEN" order. This follows the precedent recorded in Phase 03's Plan 05 SUMMARY: `workflow.tdd_mode` is `false` (MVP mode), so the strict per-task TDD gate does not apply, and a small new exported surface with no dedicated pre-existing test file to extend does not force a synthetic revert-then-reapply cycle. Task 1's own RED/GREEN split (test commit `5ddc9a1` before feat commit `225275c`) was genuine, since that work extended an existing, already-red-testable component (`BatchRow.jsx`/`BatchRow.test.jsx`).

## Issues Encountered

None beyond the deviation above. The worktree's `app/node_modules` was absent (gitignored, not checked out with the worktree) — symlinked to the main checkout's `app/node_modules` to run tests/build, matching the prior plans' own documented local-execution step (not a tracked change); it must be removed before this worktree is handed back.

## User Setup Required

None — no external service configuration required.

## Known Stubs

None. Every deliverable this plan claims is backed by a passing unit test, a build-level check, or an explicit grep. The one genuinely deferred item (D5, Task 3's real-browser human-check of the record pen's placement and overflow) is recorded in `coverage` with `human_judgment: true` and logged to `.planning/WINDOWS.md` (id 12), not silently claimed.

## Next Phase Readiness

- `npm --prefix app test` (1256 tests, 49 files) and `npm --prefix app run build` both pass on HEAD.
- Task 1's `<verify>` (both automated blocks) and Task 3's automated `<verify>` bullet both pass; Task 3's `<human-check>` — a real-browser side-by-side at 1600/1366 confirming the record pen's placement and zero horizontal overflow, plus a successful save — needs a real layout engine this worktree had no browser-automation tool for, deferred to end-of-phase UAT per `workflow.human_verify_mode: end-of-phase` and logged to `.planning/WINDOWS.md` (id 12, kind `unrun-verify`).
- The App-context battery drawing (`.planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md`) is a pending todo, not this plan's scope — a future canvas session and gap-closure plan build it.
- Plan 08's full side-by-side conformance pass covers this plan's log (every state, every width) and the record pen's placement, alongside every other 03.5 plan's own deferred human-check items (ids 10, 11, 12 in `.planning/WINDOWS.md`).

---
*Phase: 03.5-separate-the-recipe-from-the-sheet*
*Completed: 2026-09-25*

## Self-Check: PASSED

- FOUND: app/src/ui/BatchRow.jsx
- FOUND: app/src/ui/VersionRow.jsx
- FOUND: app/src/ui/RecipePage.jsx
- FOUND: app/src/ui/useBelowDesktop.js
- FOUND: app/src/ui/useBelowDesktop.test.js
- FOUND: app/src/styles/notebook.css
- FOUND: app/src/styles/tokens.css
- FOUND: .planning/todos/pending/2026-09-25-draw-the-record-pen-in-app-context.md
- FOUND: 5ddc9a1, 225275c, 726dac0 (all three task commits present in git log)
- Re-ran the plan-level `<verification>`'s automated bullet: `npm --prefix app test` (1256/49 passing), `npm --prefix app run build` (exits 0) — both pass
- Re-ran every task's own acceptance-criteria greps: Task 1 (`grep -c "Print sheet" app/src/ui/BatchRow.jsx` == 0; `grep -cE "onStartRecording|recordButtonRef" app/src/ui/VersionRow.jsx` == 0), Task 3 (`ls .planning/todos/pending/ | grep -c "draw-the-record-pen-in-app-context"` == 1) — all pass
