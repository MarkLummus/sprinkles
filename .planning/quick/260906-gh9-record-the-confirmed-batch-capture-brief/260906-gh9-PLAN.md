---
phase: quick-260906-gh9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - PRODUCT.md
autonomous: true
requirements: [QUICK-260906-gh9]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The STATE.md Impeccable bullet records that the batch-capture brief exists and is confirmed, and no longer claims the Phase 2 prerequisite is outstanding.
    - PRODUCT.md's "Explicitly undecided" list no longer carries the outcome-dimensions question.
    - PRODUCT.md's "Constraints that bind design" list states that outcome dimensions are four fixed core axes plus recipe-declared axes.
    - The D12 bullet about rating labels and whether an overall rating control exists still stands, untouched, in the undecided list.
    - Both edits land as one docs commit that stages only PRODUCT.md and .planning/STATE.md.
  artifacts:
    - .planning/STATE.md
    - PRODUCT.md
  key_links:
    - The new PRODUCT.md constraint cites `.impeccable/surfaces/route-recipe-batch.md` as the source of the decision.
    - The revised STATE.md bullet cites the same brief path and commit e23d798.
---

<objective>
Record two facts that are already true but not yet written down: the Phase 2 batch-capture brief is confirmed, and the outcome-dimensions question is settled.

Purpose: STATE.md still lists a Phase 2 prerequisite that was met on 2026-09-06, and PRODUCT.md still lists as undecided a question Mark answered in the confirmed brief. Both records are consumed by later planning; leaving them stale means Phase 2 planning either re-opens a settled decision or blocks on a satisfied prerequisite.

Output: two edited prose records, one atomic docs commit.

No tracer task: this is a two-file prose edit with no layers to wire end-to-end. No TDD: both files are documentation, which is an explicit `tdd="true"` exception.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@PRODUCT.md
@.impeccable/surfaces/route-recipe-batch.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the confirmed batch-capture brief in the STATE.md Impeccable bullet</name>
  <files>.planning/STATE.md</files>
  <read_first>
    `.planning/STATE.md` — the single bullet under `### Blockers/Concerns` that begins "Impeccable initialized 2026-09-05:". Read the whole bullet before editing; it is one long line and everything before its trailing sentence must survive byte-for-byte.

    `.impeccable/surfaces/route-recipe-batch.md` — front matter and the `**Mode:** Operate. **Status:**` line, which is the source for "shaped 2026-09-06; confirmed by Mark 2026-09-06".
  </read_first>
  <action>
    Edit the "Impeccable initialized 2026-09-05:" bullet under `### Blockers/Concerns` in `.planning/STATE.md`. Replace only that bullet's trailing sentence — the one asserting that the Phase 2 batch-record brief is still outstanding — with a sentence recording that the brief now exists, is confirmed, and satisfies the prerequisite.

    Write the replacement sentence as: The batch-capture brief for Phase 2 is confirmed: `.impeccable/surfaces/route-recipe-batch.md`, shaped and confirmed by Mark 2026-09-06 and committed in e23d798, so the Phase 2 prerequisite is met.

    Everything else in the bullet stays verbatim — the `PRODUCT.md` mention, the recipe-surface and bench-sheet clause, the "The Formulation Cookbook" direction, seed d1a5d80a, the "Phase 1, 3, and 4 prerequisites met" sentence, the `DESIGN.md` / `.impeccable/design.json` sentence and commit 17bd91f, and the North Star clause. Style match: backticked file paths, bare commit hashes, ISO dates, prose sentences inside one bullet — the surrounding text already does all four.

    Touch nothing else in the file. Specifically do not edit the `## Quick Tasks Completed` table, the `Last activity:` line under `## Current Position`, the front matter, or any other Blockers/Concerns bullet — the orchestrator updates those after this plan executes. The edit is a within-line sentence swap, so the file's newline count must not change.
  </action>
  <verify>
    <automated>grep -qF 'The batch-capture brief for Phase 2 is confirmed' .planning/STATE.md && grep -qF '`.impeccable/surfaces/route-recipe-batch.md`' .planning/STATE.md && grep -qF 'e23d798' .planning/STATE.md && ! grep -qF 'Phase 2 still needs the batch-record brief' .planning/STATE.md && grep -qF 'seed d1a5d80a' .planning/STATE.md && grep -qF 'Phase 1, 3, and 4 prerequisites met' .planning/STATE.md && grep -qF '17bd91f' .planning/STATE.md && test "$(wc -l < .planning/STATE.md)" -eq 130 && S="$(git diff --numstat -- .planning/STATE.md)" && test "$(printf '%s\n' "$S" | awk '{print $1"/"$2}')" = "1/1" && echo PASS</automated>
  </verify>
  <done>The Impeccable bullet cites the confirmed brief path, its confirmation date, and commit e23d798; the outstanding-prerequisite sentence is gone; the rest of the bullet and the rest of the file are byte-identical, proven by a one-line-added / one-line-removed diff against a file whose newline count is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Move the outcome-dimensions decision in PRODUCT.md from undecided to decided, then commit both edits</name>
  <files>PRODUCT.md</files>
  <read_first>
    `PRODUCT.md` lines 48-65 — the `**Constraints that bind design:**` list (currently ending with the TRUST-01 bullet) and the `**Explicitly undecided (do not settle in design work):**` list directly beneath it. Note the house style: plain `- ` bullets, one sentence, no bold lead-ins, attribution in trailing parentheses.

    `.impeccable/surfaces/route-recipe-batch.md` section 3 and its closing "Decided here" line — the source of the four core axes and of the fact that D12's rating question stays open.
  </read_first>
  <action>
    Two scoped edits inside the `## Capabilities and Constraints` section of `PRODUCT.md`, one removal and one addition, in this order.

    First, delete the bullet in the `**Explicitly undecided (do not settle in design work):**` list that begins "Which outcome dimensions" — the whole line, including its newline, so no blank line is left behind. Leave the other five undecided bullets exactly as they are. In particular the D12 bullet about labels for stars/ratings, Optimize, Scale, and Template, and whether an overall rating control exists, stays in the undecided list untouched: the brief settles the dimensions, not the rating question.

    Second, append one new bullet to the end of the `**Constraints that bind design:**` list, immediately after the bullet ending "(TRUST-01)." and before the blank line preceding the undecided heading. Write it as: Outcome dimensions are four fixed core axes — hardness, scoopability, smoothness, sweetness — plus the axes each recipe declares for itself (Mark, 2026-09-06, in `.impeccable/surfaces/route-recipe-batch.md`).

    That matches the neighbouring bullets: plain bullet, single sentence, source in trailing parentheses, backticked path. Do not add bold lead-ins, a new heading, or a sub-list. Do not reflow, re-order, or re-word any other bullet in either list, and do not touch any other section of the file. One line out and one line in leaves the file's newline count unchanged.

    Then commit both files from this plan as one atomic docs commit. Stage exactly `PRODUCT.md` and `.planning/STATE.md` by explicit path — never `git add -A`, `git add .`, or `git commit -a`. The working tree carries unrelated untracked and modified paths (`.planning/config.json`, `.gsd/`, `.impeccable/questions/`, `.planning/state.json`, `.planning/ui-reviews/`, `sprinkles-archive-context/`) that must stay out of this commit, along with this plan file and any SUMMARY — the orchestrator commits those afterwards. Message: `docs(product): record the confirmed batch-capture brief and settle outcome dimensions`, with the repository's configured co-author trailer.
  </action>
  <verify>
    <automated>grep -qF 'Outcome dimensions are four fixed core axes' PRODUCT.md && grep -qF 'hardness, scoopability, smoothness, sweetness' PRODUCT.md && ! grep -qF 'Which outcome dimensions are fixed versus recipe-declared' PRODUCT.md && grep -qF 'whether an overall rating control exists (D12)' PRODUCT.md && grep -qF 'Storage and synchronization architecture (D16)' PRODUCT.md && test "$(wc -l < PRODUCT.md)" -eq 94 && test "$(awk '/^\*\*Constraints that bind design:\*\*/{f=1;next} /^\*\*Explicitly undecided/{f=0} f&&/^- /{c++} END{print c}' PRODUCT.md)" -eq 8 && test "$(awk '/^\*\*Explicitly undecided/{f=1;next} /^## /{f=0} f&&/^- /{c++} END{print c}' PRODUCT.md)" -eq 5 && C="$(git show --name-only --format= HEAD)" && test "$(printf '%s\n' "$C" | sed '/^$/d' | LC_ALL=C sort | tr '\n' ',')" = ".planning/STATE.md,PRODUCT.md," && echo PASS</automated>
  </verify>
  <done>The constraints list holds 8 bullets, the last of which names the four core axes plus recipe-declared axes and cites the brief; the undecided list holds 5 bullets and no longer carries the dimensions question, while the D12 rating bullet and the D16 storage bullet survive; HEAD is a single commit touching exactly PRODUCT.md and .planning/STATE.md and nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Staged content crosses into a permanent commit; over-staging leaks unrelated in-progress files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gh9-01 | Information Disclosure | git commit staging | medium | mitigate | Task 2 stages `PRODUCT.md` and `.planning/STATE.md` by explicit path and forbids `git add -A` / `git add .` / `git commit -a`; verified by asserting HEAD touches exactly those two files. |
| T-gh9-02 | Tampering | PRODUCT.md decision register | medium | mitigate | Edits are scoped sentence/bullet replacements with fixed-string greps asserting that the D12 and D16 bullets and the unchanged STATE.md clauses survive, plus newline-count and diff-size invariants. |
| T-gh9-03 | Tampering | dependency supply chain | low | accept | No package-manager install, no dependency change, no executable code in this plan — the phase adds prose only. |
</threat_model>

<verification>
Run from the repository root after both tasks:

```
git show --stat HEAD
git diff HEAD~1 HEAD -- PRODUCT.md .planning/STATE.md
git status --short
```

Expect: one commit touching exactly two files, one insertion and one deletion in each, and a `git status --short` that still lists the pre-existing unrelated untracked/modified paths as untracked/modified (they must not have been swept into the commit).
</verification>

<success_criteria>
- `.planning/STATE.md` names `.impeccable/surfaces/route-recipe-batch.md`, its 2026-09-06 confirmation, and commit e23d798, and no longer asserts an outstanding Phase 2 brief.
- `PRODUCT.md` lists the four fixed core axes plus recipe-declared axes as a binding constraint, and no longer lists that question as undecided.
- The D12 rating bullet remains in the undecided list.
- Exactly one commit, touching exactly two files.
- No unrelated working-tree path was staged.
</success_criteria>

<output>
Create `.planning/quick/260906-gh9-record-the-confirmed-batch-capture-brief/260906-gh9-SUMMARY.md` when done.
</output>
