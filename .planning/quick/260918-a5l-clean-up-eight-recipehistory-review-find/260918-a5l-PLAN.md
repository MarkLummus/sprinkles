---
phase: quick-260918-a5l
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
  - REVIEW-04
  - REVIEW-05
  - REVIEW-06
  - REVIEW-07
  - REVIEW-08
files_modified:
  - app/src/ui/VersionRow.jsx
  - app/src/ui/VersionRow.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/domain/batch.js
  - app/src/domain/batch.test.js
  - app/src/styles/app.css
  - app/src/styles/tokens.css
  - app/src/ui/RecipeHistory.jsx
  - app/src/ui/RecipeHistory.test.jsx

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Nothing under app/src names batchHistoryWords, and nothing in VersionRow names penReason (REVIEW-01, REVIEW-02)."
    - "The class RecipeHistory emits on the version in view has a CSS rule, and that rule's weight cannot reach a descendant version node (REVIEW-03)."
    - "Neither flex basis in the recipe-history block carries a length literal; both read a custom property defined in tokens.css (REVIEW-04)."
    - "A cyclic parent pair renders — both members appear as roots — and rendering it terminates (REVIEW-05)."
    - "VersionNode's parent-to-children map is named childrenByParent, so React's children slot is unshadowed (REVIEW-06)."
    - "A version belonging to another recipe never renders in the history, proven by a test (REVIEW-07)."
    - "makeBatch declares churn once and behaves exactly as before (REVIEW-08)."
    - "The whole change — the pre-existing uncommitted History work plus these eight fixes — is committed on main as atomic commits."
  artifacts:
    - app/src/ui/RecipeHistory.jsx
    - app/src/ui/RecipeHistory.test.jsx
    - app/src/styles/tokens.css
    - app/src/styles/app.css
    - app/src/domain/batch.js
  key_links:
    - "RecipePage.jsx:855 derives penReason and must keep feeding BatchRow — only the VersionRow call site loses it (REVIEW-01)."
    - "versionForest's returned key stays named children, so the existing forest tests that read forest.children keep passing while the prop is renamed (REVIEW-06)."
    - "Removing the render-time cycle guard is only safe because versionForest stops emitting a cyclic parent edge — a promoted root must not also be somebody's child, or VersionNode recurses forever (REVIEW-05)."
---

<objective>
Close eight code-review findings on the uncommitted recipe-level History disclosure, then commit the whole change.

Purpose: the History disclosure replaced VersionStrip in the working tree and left orphans behind it — a dead prop, a dead domain function, a class with no rule, two literal lengths against the project's token discipline, an unreachable guard hiding a real defect, a shadowed React prop name, a lost test, and a duplicate key. None of this is committed yet, so it can be cleaned before it enters history rather than after.

Output: nine edited source files and the whole working tree committed on `main` as atomic commits.

Not a tracer phase: the scope is a fixed, enumerated list of eight findings against code that already exists and already works end-to-end. There is no architecture to prove with a thin slice, so the tasks are grouped by file ownership instead.
</objective>

<execution_constraints>
**This plan operates on UNCOMMITTED working-tree changes on `main`. Execution is SEQUENTIAL on the main working tree.**

- Do NOT create a worktree. A worktree forked from `origin/HEAD` would not contain this work.
- Do NOT run `git stash`, `git checkout <branch>`, `git switch`, `git reset`, or `git restore`. The only uncommitted copy of the History work is in this tree.
- Do NOT create a branch. Do NOT push.
- Tasks 1, 2 and 3 do NOT commit. Only Task 4 commits.
- **Mutable-scope authority (#3786):** the authorized file set is the live `git status` working tree at execution time, not the list in this plan's frontmatter. Every line number in this plan is advisory — re-read each file before editing; earlier tasks shift later line numbers.

**Surgical discipline (CLAUDE.md § 3):** every changed line must trace to one of the eight findings. No adjacent improvements, no refactors, no comment rewrites beyond what a finding requires. If you notice unrelated dead code, name it in the SUMMARY — do not delete it.

**Simplicity discipline (CLAUDE.md § 2):** these are cleanups. No new abstractions, no options, no defensive handling beyond what each finding names.

**Comment hygiene:** several verification gates below count occurrences of an identifier or a selector in a source file. Do not leave a retired identifier, a retired length literal, or a gated selector inside a code comment in `app/src` — a comment that names what was removed re-fails its own gate.
<!-- planner-discipline-allow: batchHistoryWords, penReason, visited, children, VersionStrip -->
</execution_constraints>

<context>
@/Users/mark/Documents/projects/sprinkles/CLAUDE.md
@/Users/mark/Documents/projects/sprinkles/.claude/CLAUDE.md
@/Users/mark/Documents/projects/sprinkles/.planning/STATE.md
@/Users/mark/Documents/projects/sprinkles/app/src/ui/RecipeHistory.jsx
@/Users/mark/Documents/projects/sprinkles/app/src/ui/RecipeHistory.test.jsx

Read as needed, in the task that needs them:
- `app/src/ui/VersionRow.jsx`, `app/src/ui/VersionRow.test.jsx`, `app/src/ui/RecipePage.jsx`, `app/src/ui/BatchRow.jsx`
- `app/src/domain/batch.js`, `app/src/domain/batch.test.js`, `app/src/domain/lineage.js`
- `app/src/styles/app.css` (the `.recipe-history` block and the `.history-register` block above it), `app/src/styles/tokens.css`

**Measured baseline, this working tree, before any edit:**

| Fact | Value |
|------|-------|
| `npm --prefix app test` | 35 files, **1010 passed** |
| `npm --prefix app run build` | passes (118 modules) |
| `grep -c "penReason=" app/src/ui/RecipePage.jsx` | 2 |
| `grep -c "penReason" app/src/ui/BatchRow.jsx` | 1 |
| `grep -rn "batchHistoryWords" app/src` | 7 lines across 2 files |
| `grep -cE "flex: 1 1 [0-9]" app/src/styles/app.css` | 2 |
| `grep -cE "flex: 1 1 var\(--" app/src/styles/app.css` | 0 |
| `grep -cE "\.recipe-history__version\.is-current" app/src/styles/app.css` | 0 |
| `grep -ci "visited" app/src/ui/RecipeHistory.jsx` | 6 |
| `grep -c "childrenByParent" app/src/ui/RecipeHistory.jsx` | 0 |
| `grep -c "roots, children }" app/src/ui/RecipeHistory.jsx` | 1 |
| `grep -c "  it(" app/src/ui/RecipeHistory.test.jsx` | 7 |

**Test-count arithmetic, stated so drift is visible:** 1010 − 6 (the six removed domain tests) + 2 (the two added tests) = **1006** at the end.

- After Task 1: **1004**
- After Task 2: **1004** (no test changes)
- After Task 3: **1006**
- After Task 4: **1006**

A different number at any gate means something unintended moved. Stop and report it rather than adjusting the expectation.

**There is no automated token-discipline gate.** `app/src/styles/cross-cutting.test.js`, `binder.test.js` and `columns.test.js` read the stylesheet but none of them scans for length literals — the "every visual value reads a custom property" rule in `.claude/CLAUDE.md` is a written convention. REVIEW-04's gate below is therefore a targeted grep, not a whole-file scan. One pre-existing literal outside this plan's scope (`app.css` ~line 2492, a `minmax(min(100%, 280px), 1fr)` in the axes grid) is deliberately left alone: it is not one of the eight findings.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the two orphans the History disclosure left behind (REVIEW-01, REVIEW-02)</name>
  <files>app/src/ui/VersionRow.jsx, app/src/ui/VersionRow.test.jsx, app/src/ui/RecipePage.jsx, app/src/domain/batch.js, app/src/domain/batch.test.js</files>
  <action>
Close REVIEW-01 and REVIEW-02. Both are dead code created by the same deletion — VersionStrip's removal from this working tree took the last consumer of each with it. Re-read every file before editing; the line numbers named here are advisory.

REVIEW-01 — the orphaned `penReason` prop on VersionRow. `VersionRow.jsx` (~line 25) declares it in the prop list with a null default, and the file's body never reads it: its only consumer was the prop VersionRow forwarded to the now-deleted VersionStrip. Delete the parameter from VersionRow's prop list. Delete the line that passes it at the **VersionRow** element in `RecipePage.jsx` (~line 1801). Delete the prop from the three test render calls in `VersionRow.test.jsx` (~lines 39, 162, 341) — at ~162 and ~341 the prop sits in a props object passed to the file's own render helper alongside `openPen`, which stays; remove only the one key, and if removing it makes an assertion in that `it` block meaningless, leave the assertion alone and say so in the SUMMARY rather than rewriting the test.

**Do not touch the BatchRow call site in `RecipePage.jsx` (~line 1849) or the declaration in `BatchRow.jsx` (~line 364).** That copy is live. `RecipePage.jsx` (~line 855) derives the value from `derivePenState` and must keep doing so for BatchRow — leave the derivation and the comment above it exactly as they are.

REVIEW-02 — the dead `batchHistoryWords`. `app/src/domain/batch.js` (~lines 301-320) exports it; VersionStrip was its sole caller. Remove the exported function together with the JSDoc block directly above it. Remove its name from the import list at the top of `app/src/domain/batch.test.js` (~line 20) and remove its whole `describe` block (~lines 472-506, six `it` blocks). `latestChurnDate`, which it calls, has other callers and its own tests — leave it and its tests untouched.

Afterwards nothing under `app/src` may name either identifier, including in prose comments. `app/src/domain/lineage.js` (~line 51) has a doc comment that names the deleted VersionStrip file; that is a separate staleness issue, not one of the eight findings — leave it and note it in the SUMMARY.

Do not commit in this task.
  </action>
  <verify>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app test</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app run build</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -rn "batchHistoryWords" app/src; echo "grep-exit=$?"</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -rn "penReason" app/src/ui/VersionRow.jsx app/src/ui/VersionRow.test.jsx; echo "grep-exit=$?"</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -c "penReason=" app/src/ui/RecipePage.jsx</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -c "penReason" app/src/ui/BatchRow.jsx</automated>
  </verify>
  <done>
`npm --prefix app test` reports **1004 passed** (1010 − the six removed domain tests) and `npm --prefix app run build` succeeds.
Both greps print no matching lines and report `grep-exit=1` — exit 1 is "no match" (the pass); exit 0 means an occurrence survives and exit 2 means the gate itself is broken (bad path), neither of which passes.
`penReason=` in `RecipePage.jsx` counts **1** — the surviving BatchRow call site. `penReason` in `BatchRow.jsx` counts **1** — its declaration, untouched.
Nothing is committed.
  </done>
  <reversibility rating="reversible">Pure deletion of code with no remaining callers; restorable from `git show HEAD:` for the tracked files.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Give the version in view its rule, and move two lengths into tokens (REVIEW-03, REVIEW-04)</name>
  <files>app/src/styles/app.css, app/src/styles/tokens.css</files>
  <action>
Close REVIEW-03 and REVIEW-04, the two stylesheet findings. Re-read the `.recipe-history` block in `app/src/styles/app.css` (~lines 840-975) and the `.history-register` block above it (~lines 771-836) before editing.

REVIEW-03 — `RecipeHistory.jsx` (~line 101) sets a distinguishing class on the version node in view, and `app.css` has no rule for it. Two existing rules already mark a current entry and are the precedent to follow: `.recipe-history__batch.is-current` (~line 937) in this same block, and the older `.history-register__item.is-current` (~line 793). Read both, then give the version node the matching treatment.

One correctness constraint on where the treatment lands. Both precedent rules sit on an element whose box contains that entry and nothing else. The version node's list item is not like that: it contains the version's own sheet **and** `.recipe-history__branches`, which holds its descendant version nodes. A weight declared on the bare list item would inherit down the whole subtree and bold descendants that are not in view; an outline on it would enclose the subtree. So target the current version's own box — its sheet or its name element — as a descendant of the is-current list item, not the list item itself. Choose the declarations from the two precedents; every value must read a custom property, as the surrounding rules already do.

Add the rule at the block's own convention: near the other `.recipe-history__version*` rules, with a short comment in the voice of the neighbouring comments explaining why the treatment is scoped to the version's own box rather than its list item.

REVIEW-04 — the flex basis on `.recipe-history__version-name` (~line 882) and on `.recipe-history__batch-name` (~line 942) is a raw length. These are the only two non-token length literals in a declaration in this block, and `.claude/CLAUDE.md` requires every visual value to read a custom property defined in `app/src/styles/tokens.css`. Define one custom property per rule in `tokens.css`, keeping each rule's current value exactly — this is a relocation, not a retune — and have both rules read them.

Name the tokens for what they are: the width at which the name stops shrinking and the head's flex row wraps its right-hand metadata onto a new line. Follow the naming conventions already in `tokens.css` (it uses a short kind-then-subject form: `--col-batch-cell-min`, `--vmeta-min`, `--field-w-date`, `--track-stop`). Place them with a `/* … */` comment above, in the same voice as the `--col-batch-cell-min` and `--vmeta-min` entries around them, naming what the threshold is for. Do not restate the retired numbers anywhere in `app.css` — they now live in `tokens.css`, which is where a literal belongs.

Touch nothing else in either stylesheet. In particular, leave the axes-grid literal further down `app.css` alone: it is not one of the eight findings.

Do not commit in this task.
  </action>
  <verify>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app test</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app run build</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -cE "flex: 1 1 [0-9]" app/src/styles/app.css; true</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -cE "flex: 1 1 var\(--" app/src/styles/app.css</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -cE "\.recipe-history__version\.is-current[^{,]*\.recipe-history__version-" app/src/styles/app.css</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -cE "^\.recipe-history__version\.is-current[[:space:]]*\{" app/src/styles/app.css; true</automated>
  </verify>
  <done>
`npm --prefix app test` still reports **1004 passed** (this task changes no test) and `npm --prefix app run build` succeeds.
The literal-basis grep counts **0**; the token-basis grep counts **2**.
The scoped is-current grep counts **1** — the rule exists and reaches the version's own box through a descendant selector.
The bare-list-item grep counts **0** — no declaration block is applied to the list item that also carries the subtree.
Both new custom properties are defined in `app/src/styles/tokens.css`, each with a comment naming what the threshold is for, and each carries the value its rule had before.
Nothing is committed.
  </done>
  <reversibility rating="reversible">Stylesheet-only; the visual treatment chosen for the version in view is one declaration block away from being restyled.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix the outline component — the real cycle defect, the shadowed prop, the lost test, the duplicate key (REVIEW-05, REVIEW-06, REVIEW-07, REVIEW-08)</name>
  <files>app/src/ui/RecipeHistory.jsx, app/src/ui/RecipeHistory.test.jsx</files>
  <behavior>
Write these two tests first and watch them fail before changing `RecipeHistory.jsx`.

- **Cyclic pair (REVIEW-05, a `versionForest` unit test):** given exactly two versions of one recipe whose `parentVersionId` values point at each other, `versionForest` returns **both** in `roots`, and neither is listed as a child of the other — so a caller that walks `children` from a root terminates. Today `roots` comes back empty and both versions vanish from the outline.
- **Recipe scoping (REVIEW-07, a `RecipeHistory` render test):** given a versions array containing one version of `recipe-1` and one carrying a different `recipeId`, the rendered markup does not contain the other recipe's version name, and the outline renders exactly one version node. This restores the coverage deleted with `VersionStrip.test.jsx:89`. Use the existing `makeVersion` and `renderHistory` helpers in the file.
  </behavior>
  <action>
Close the four findings in `app/src/ui/RecipeHistory.jsx` and its test file. Re-read both files before editing — Tasks 1 and 2 did not touch them, but state your own read of the current text rather than trusting the line numbers here.

Write the two tests in the `<behavior>` block first, run the suite, and confirm the cyclic-pair test fails for the stated reason before touching the component.

REVIEW-05 — the unreachable guard, and the real defect underneath it. `VersionNode` threads a `Set` of already-rendered ids (declared ~74-88, passed down ~166, seeded ~202) that can never fire: a version that is its own ancestor is never in `roots`, so it never renders at all and the guard is never consulted. Delete the parameter, the copy made on each render, the early return, and both pass-down sites.

That is only safe once `versionForest` (~lines 20-32) stops emitting the edge the guard appeared to protect. Today the function keeps a version's `parentVersionId` whenever that id is present in the set — so a cyclic pair lands entirely inside `children`, `roots` comes back empty, and both versions disappear from the outline. Change the rule to the one the function already applies to an orphan: a version's parent counts only if the chain of parents above it terminates; if walking that chain revisits an id instead of running out, treat the version as parentless and put it in the roots bucket, exactly as a version whose parent id is absent from the set is already treated. Because a promoted version's parent edge is dropped rather than kept, no version can be both a root and somebody's child, which is what makes the guard's removal safe.

Keep `versionForest` pure — no mutation of the array or the records passed in, no store or DOM access. Keep the explanatory comment above it honest about what it now does: its last sentence today speaks only of an absent parent, and after this change the same promotion also covers a chain that closes on itself. Amend that sentence; do not rewrite the comment.

Leave the shape `versionForest` returns alone — the returned key keeps its current name, so the two existing forest tests that read it keep passing.

REVIEW-06 — `VersionNode` takes the parent-to-children `Map` as a prop literally named `children`, shadowing React's own children slot (~80, ~95, ~164, ~186, ~200). Rename it to `childrenByParent` at every one of those sites, including the destructuring in `RecipeHistory` at ~186 that feeds it: rename the local binding there while leaving the key `versionForest` returns unchanged.

REVIEW-08 — in `RecipeHistory.test.jsx`, the `makeBatch` helper (~lines 20-39) declares the same `churn` key twice in one object literal, with the spread of `overrides` between them. The second declaration wins today. Remove the dead first one. The helper must behave identically afterwards: the surviving declaration is the one after the spread, so an `overrides.churn` still merges over the defaults and a caller passing no churn still gets both defaults.

Do not commit in this task.
  </action>
  <verify>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app test</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app run build</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -ci "visited" app/src/ui/RecipeHistory.jsx; true</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -cE "^[[:space:]]+children[,=]|children=\{children\}" app/src/ui/RecipeHistory.jsx; true</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -c "roots, children }" app/src/ui/RecipeHistory.jsx; true</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -c "childrenByParent" app/src/ui/RecipeHistory.jsx</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -c "  it(" app/src/ui/RecipeHistory.test.jsx</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; grep -c "churn: {" app/src/ui/RecipeHistory.test.jsx</automated>
  </verify>
  <done>
`npm --prefix app test` reports **1006 passed** — 1004 plus the two new tests — and `npm --prefix app run build` succeeds.
The suite completes; it does not hang or overflow the stack, so the cyclic pair terminates.
The case-insensitive `visited` grep counts **0**. The bare-prop grep counts **0** and the `roots, children }` grep counts **0**; `childrenByParent` counts **4 or more**.
`  it(` in the test file counts **9** (7 + 2). The `churn: {` grep counts **3** (4 − 1).
The two existing `versionForest` tests still pass unchanged, proving the returned shape did not move.
Nothing is committed.
  </done>
  <reversibility rating="reversible">A behaviour change confined to one pure function and one component; both are uncommitted and covered by the tests written first.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Commit the whole change on main as atomic commits</name>
  <files>the live `git status` working tree — code under `app/`, plus `DESIGN.md` and `.impeccable/surfaces/route-recipe-version.md`</files>
  <action>
Commit everything: the pre-existing uncommitted History work AND the eight fixes from Tasks 1-3.

Before staging anything, run `git status --short` and take the live list as authoritative — the file set at execution time governs, not the list this plan was written against. Do not create a branch, do not push, do not stash, do not reset.

Confirm the tree is green first: `npm --prefix app test` reports 1006 passed and `npm --prefix app run build` succeeds. Do not commit a red tree.

Then make three commits, in this order, staging by explicit path with `git add` (never `git commit -a`, and remember `RecipeHistory.jsx`/`RecipeHistory.test.jsx` are untracked and need adding):

1. **The disclosure itself.** The new `RecipeHistory` component and its test, the deleted `VersionStrip` pair, and every file that rewires to it or styles it: `VersionRow.jsx`, `VersionRow.test.jsx`, `RecipeList.jsx`, `RecipePage.jsx`, `app/src/styles/app.css`, `app/src/styles/tokens.css`, `app/src/styles/cross-cutting.test.js`. Subject in the `feat(...)` form this repo uses.
2. **The domain retirement.** `app/src/domain/batch.js` and `app/src/domain/batch.test.js` alone — the function whose last caller the first commit removed. This commit must come *after* the first: the deleted VersionStrip still imports that function, so landing this first would leave a tree that cannot build.
3. **The documents.** `DESIGN.md` and `.impeccable/surfaces/route-recipe-version.md`, in the `docs(...)` form.

Read `git log -1 --format=%B` on a recent commit first and match the house style: a subject line in the project's voice, then a body of dash bullets naming each file and what changed in it and why — see `8ce66d9` for the shape. Write the bodies from what you actually changed, and name the eight findings the fixes close in the first two commits' bodies.

End every commit message with these two trailer lines, exactly:

    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_011cHYYRAddzeKHZdteKfA1o

Do not fold anything under `.planning/` into these three commits — planning artifacts are committed separately by the quick-task workflow.

Because `git stash` is forbidden, the intermediate trees are not checked out and re-tested; the ordering above is what keeps each commit self-consistent. State that in the SUMMARY rather than claiming per-commit verification you did not run.
  </action>
  <verify>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app test</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; npm --prefix app run build</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; git status --porcelain -- app DESIGN.md .impeccable; echo "git-exit=$?"</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; git log --oneline -3</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; git log -3 --format=%B > /tmp/a5l-msgs.txt &amp;&amp; grep -c "Co-Authored-By: Claude Opus 5" /tmp/a5l-msgs.txt</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; git rev-parse --abbrev-ref HEAD</automated>
    <automated>cd /Users/mark/Documents/projects/sprinkles &amp;&amp; git status -sb</automated>
  </verify>
  <done>
`npm --prefix app test` reports **1006 passed** and `npm --prefix app run build` succeeds, both on the committed tree.
`git status --porcelain` over `app`, `DESIGN.md` and `.impeccable` prints **nothing** and reports `git-exit=0` — nothing from the change is left uncommitted and nothing was left untracked.
`git log --oneline -3` shows the three new commits, newest first: the documents, the domain retirement, the disclosure.
The trailer grep counts **3** — every commit carries the attribution lines.
`git rev-parse --abbrev-ref HEAD` reports **main**; no branch was created.
`git status -sb` shows `main` **ahead of** its upstream by the new commits and behind by none — they are local, nothing was pushed.
  </done>
  <reversibility rating="reversible">Three local commits on `main`, unpushed; `git reset --soft` restores the working tree exactly. (No reset is to be run as part of this task.)</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported store file → domain/UI | `transfer.js` validates a record's shape but not the acyclicity of `parentVersionId`; a crafted or corrupted export can hand the outline a version graph with a cycle |
| stored records → rendered markup | version names, reasons and tasting notes render as text only (no `dangerouslySetInnerHTML` anywhere under `app/src`) — unchanged by this task |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-a5l-01 | Denial of Service | `versionForest` / `VersionNode` in `app/src/ui/RecipeHistory.jsx` | medium | mitigate | Task 3 removes a render-time cycle guard. Removing it is only safe because `versionForest` stops emitting the cyclic parent edge — a promoted root is never also a child, so `VersionNode`'s recursion terminates on any input. The cyclic-pair test in Task 3's `<behavior>` block is the standing proof; the Task 3 `<done>` requires the suite to complete rather than hang. |
| T-a5l-02 | Information Disclosure | `versionsForRecipe` scoping in `app/src/ui/RecipeHistory.jsx` | low | mitigate | REVIEW-07 restores the regression test that a version carrying a different `recipeId` never renders in the outline, so cross-recipe leakage into the history stays provable rather than incidental. |
| T-a5l-03 | Tampering | `app/src/styles/tokens.css`, `app/src/styles/app.css` | low | accept | Stylesheet-only relocation of two length values into custom properties; no data path, no boundary crossed, local-only app. |

No package-manager installs occur in this task, so no package-legitimacy gate applies and no `T-a5l-SC` row is opened.
</threat_model>

<verification>
Run on the final tree, after Task 4:

1. `npm --prefix app test` → **1006 passed**, 35 files. Any other number is drift: report it, do not adjust the expectation.
2. `npm --prefix app run build` → succeeds.
3. `git status --short` → nothing from this change left uncommitted; `HEAD` is `main`; three new commits ahead of `origin/main`; nothing pushed.
4. All eight findings closed, each provable by its own gate in the task that owns it.

**Deferred to end-of-phase UAT (non-blocking, `human_verify_mode: end-of-phase`):** the visual treatment REVIEW-03 gives the version in view is Mark's call. The automated gates prove the rule exists and that its weight cannot reach a descendant version node; whether the chosen declarations read correctly against the batch entry's own treatment wants a look in a browser on a multi-version, forked store. Record it as a UAT item in the SUMMARY.

**Observations to record in the SUMMARY, not to fix here** (CLAUDE.md § 3 — name it, do not delete it): `app/src/domain/lineage.js` (~line 51) has a doc comment naming the deleted `VersionStrip.jsx`; `.history-register__item.is-current` in `app.css` (~line 793) may now be unmatched, since `BatchRow.jsx` renders that item class without the state class. Neither is one of the eight findings.
</verification>

<success_criteria>
- REVIEW-01: `penReason` is gone from `VersionRow.jsx`, its test file, and the VersionRow call site; the BatchRow call site, `BatchRow.jsx`'s declaration and `RecipePage.jsx`'s derivation are untouched.
- REVIEW-02: `grep -rn "batchHistoryWords" app/src` returns nothing; the function, its doc comment, its import and its six-test `describe` block are gone.
- REVIEW-03: the version-in-view class has a rule, scoped to the version's own box so its weight cannot reach a descendant version node.
- REVIEW-04: neither flex basis in the recipe-history block carries a length literal; both read new custom properties defined in `tokens.css` at their previous values.
- REVIEW-05: the render-time cycle guard is gone; `versionForest` promotes a version whose parent chain closes on itself to a root and drops its parent edge; a test proves a cyclic pair still renders and terminates; the function stays pure and its comment stays honest.
- REVIEW-06: the parent-to-children map is `childrenByParent` at every site; `versionForest`'s returned shape is unchanged.
- REVIEW-07: a test proves a version of a different recipe does not render.
- REVIEW-08: `makeBatch` declares `churn` once and behaves identically.
- The whole change — pre-existing History work plus these fixes — is committed on `main` as three atomic commits with the attribution trailers; no branch, no push, no stash.
- Test count lands at 1006; build passes.
- Every changed line traces to one of the eight findings.
</success_criteria>

<output>
Create `.planning/quick/260918-a5l-clean-up-eight-recipehistory-review-find/260918-a5l-SUMMARY.md` when done.

Record in it: the final test count against the 1006 expectation; the token names chosen for REVIEW-04 and the selector chosen for REVIEW-03; the exact rule adopted for REVIEW-05's promotion; the three commit SHAs and what each carries; the deferred UAT item; and the two out-of-scope observations.
</output>
