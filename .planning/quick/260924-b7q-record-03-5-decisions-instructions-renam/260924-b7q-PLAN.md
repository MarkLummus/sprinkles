---
phase: quick-260924-b7q
plan: 01
quick_id: 260924-b7q
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/ui/Method.jsx
  - app/src/ui/Method.test.jsx
  - app/src/ui/RecipePage.jsx
  - app/src/ui/RecipePage.test.jsx
  - DESIGN.md
  - .impeccable/design.json
  - .impeccable/surfaces/route-recipe.md
  - .impeccable/surfaces/route-recipe-version.md
  - .impeccable/surfaces/route-recipe-batch.md
  - .impeccable/surfaces/route-print-recipe-sheet.md
  - .impeccable/surfaces/route.md
  - product-requirements/05-domain-and-language.md
  - .claude/skills/sketch-findings-sprinkles/references/page-shell-front-matter.md
  - .planning/canvas-generators/gen.py
  - .planning/todos/pending/2026-09-24-decide-whether-the-batch-and-tasting-log-is-entered-on-the-phone.md
  - .planning/todos/pending/2026-09-24-write-a-product-brief-for-phone-based-jobs.md
  - .planning/.continue-here.md
  - .planning/HANDOFF.json
autonomous: true
requirements: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On the recipe page the Sheet's steps region shows the heading Instructions, and the region's accessible name is Instructions (Mark, 2026-09-24). The file Method.jsx, the component Method, the class method-region, the method-step classes and version.method are unchanged."
    - "npm --prefix app test passes with 1155 tests (1154 before, plus one new source-text pin), and npm --prefix app run build passes."
    - "DESIGN.md, .impeccable/design.json, the five living briefs, 05-domain-and-language.md and the page-shell sketch finding say Instructions wherever they name the region or its label. Historical records, the evidence from Mark's paper sheets, CSS class names, and generic uses of the word method are unchanged."
    - "route-recipe.md § '03.5 revision' records that Carried forward notes are dropped (Mark, 2026-09-24), lists the code, seed, store and transfer removal as 03.5 scope, and leaves the destination of the three seeded notes' words to the 03.5 discussion. The Inherited notes paragraph in route-recipe-version.md § 3 records that the marker stays on Before you start notes."
    - "gen.py no longer draws the Carried forward block or its fold on any board. Every other byte of every board, and of canvas.json, matches the outputs published from the same generator."
    - "The phone-logging todo says it is deferred to a product brief for phone-based jobs. A new pending todo for that brief exists and links the phone-logging todo as its first input."
    - ".continue-here.md and HANDOFF.json carry the three decisions. Their open items are: the rename label, whether a fold remembers being opened, the Recipe Book yield and timing fields, the Done/Cancel wording, whether code identifiers follow the Instructions rename, and where the seeded carried-forward notes' words go."
  artifacts:
    - "app/src/ui/Method.jsx: the h2 with className region-name reads Instructions (line 590)"
    - "app/src/ui/RecipePage.jsx: the section with className method-region carries aria-label Instructions (line 1884)"
    - "app/src/ui/RecipePage.test.jsx: new source-text describe block pinning the steps region's accessible name"
    - ".impeccable/surfaces/route-recipe.md: two dated bullets closing § 2 of the 03.5 revision (Instructions; Carried forward notes are dropped)"
    - ".planning/canvas-generators/gen.py: SHEET loses the Carried forward block; phone_folds closes the balance fold without it; rung titles no longer fold notes"
    - ".planning/todos/pending/2026-09-24-write-a-product-brief-for-phone-based-jobs.md"
    - ".planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-quick.py (app|docs|handoff) and verify-gen.py: the task gates, already written and committed by the planner"
  key_links:
    - "Method.test.jsx's 'Before you start' order test finds the heading by its markup (indexOf, line 1200). The test and the heading change together, or the order assertions fail."
    - "RecipePage cannot render in the node harness, so its accessible name is pinned on source text. This follows the tabindex block at RecipePage.test.jsx ~778: comments stripped, the block declares its own reads."
    - "Every board is built from gen.py's module-level SHEET (line 14), including R35C_LongHistory through longhist.py's `from gen import *`. Removing the block there removes it from every board."
    - "phone_folds used to close the balance fold (opened at line 445) just before the Carried forward block. With the block gone, only closing div tags lie between that point and the Sheet's single </article>, so the close moves to just before </article>. The board string comes out identical apart from the removed block."
    - "The route-recipe.md 03.5 revision governs over the brief's older sections. The dated Instructions bullet covers older wording (§ 3, § 6, the Direction contract's block-name list) without rewriting history."
---

<objective>
Record three decisions Mark made for Phase 03.5 on 2026-09-24 and apply the Instructions rename. His words: "Drop Carried forward notes; let's defer the phone logging question; I want to do a product brief specifically for phone-based jobs; rename Instructions everywhere."

1. The label "Method" becomes "Instructions" wherever a reader sees it: in the app (heading and accessible name) and in the living docs.
2. Carried forward notes are dropped. The decision goes into the briefs, and the canvas generator stops drawing them. Removing `carriedForward` from the code, seed and store is Phase 03.5 scope; this task records that and does not do it.
3. Phone logging is deferred to a product brief for phone-based jobs, captured as a new todo.
4. The handoff files drop the settled items from the open list and gain one new open item: whether code identifiers follow the rename.

Purpose: the 03.5 discussion opens on a settled vocabulary and a shorter open list, and the next canvas regeneration draws the Sheet without Carried forward.
Output: the app label change with one new test, dated doc edits, the gen.py change, two todos, and the updated handoff files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/.continue-here.md
@CLAUDE.md
@.claude/CLAUDE.md

Facts the planner established (do not re-derive):
- In app/src, only two strings show the label: Method.jsx:590 (the region heading) and RecipePage.jsx:1884 (the section's aria-label). One test asserts it: Method.test.jsx:1200. No CSS selector, print route or index.html uses it. Every other "Method" under app/src is an identifier, an import, a class name, a comment or a test title that names the component.
- Baseline suite: 41 files, 1154 tests, green (2026-09-24).
- npm is not on the default PATH on this machine. Prefix commands with `PATH="$(dirname "$(readlink /opt/homebrew/bin/node)"):$PATH"`; readlink resolves to the nvm default's bin, currently ~/.nvm/versions/node/v24.21.0/bin.
- The folds list ("Details, Balance + Things to check, Carried forward, Tasting") is not written in route-recipe.md. It lives only on the canvas, in .continue-here.md and in HANDOFF.json. The brief therefore gets a dated bullet that records the drop; it does not get a list to edit.
- The previous session's scratchpad still holds the canvas source copies gen.py reads (its SP constant) and the outputs it published at 07:31 from a gen.py byte-identical to the committed one. verify-gen.py runs a copy of the edited gen.py and longhist.py into a temp directory and compares each board and canvas.json with those published outputs. It passes only if the two intended differences are the only ones. The planner prototyped the exact gen.py edit in Task 3, and the script passed on it and failed on the unedited file.
- Do NOT run gen.py or longhist.py directly: their OUT points into the previous session's scratchpad, and a direct run overwrites the published copies there. Never publish to the canvas; regeneration and publish are handoff task 6.
- Out of scope, left for the 05-domain row to govern: older "Method" wording in product-requirements/01-product-brief.md:35, product-requirements/03-decision-register.md:76 (D18) and .planning/ROADMAP.md:23/464/496. Report these in the summary; do not edit them.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The app names the steps region Instructions (heading and accessible name)</name>
  <files>app/src/ui/Method.test.jsx, app/src/ui/RecipePage.test.jsx, app/src/ui/Method.jsx, app/src/ui/RecipePage.jsx</files>
  <read_first>app/src/ui/Method.jsx lines 585-600; app/src/ui/RecipePage.jsx lines 1880-1890; app/src/ui/Method.test.jsx lines 1186-1206; app/src/ui/RecipePage.test.jsx lines 1-30 and 775-800 (the source-text pattern to copy)</read_first>
  <behavior>
    - Method rendered in reading mode with a Before you start note puts the Instructions heading first, then the Before you start block, then the step list (the existing order test, now looking up the new heading).
    - RecipePage.jsx, with comments stripped, has exactly one opening tag for the section whose className is method-region, and that tag carries aria-label="Instructions".
  </behavior>
  <action>Per Mark's decision of 2026-09-24, the Sheet's steps region reads Instructions on screen and in its accessible name. Change only strings a reader sees. Leave these as they are: the file name Method.jsx, the exported component Method and its import in RecipePage.jsx, the class method-region, every method-step class, version.method, every comment, and the describe titles that name the component. Whether identifiers follow the rename is an open question for Mark.

RED:
(a) In app/src/ui/Method.test.jsx, find the describe block whose title says "Before you start" heads the Method (around line 1191). Rename its it-title "renders between the Method heading and the step list" to "renders between the Instructions heading and the step list". On line 1200, headingIndex looks up the heading's markup. Change the argument so it searches for the word Instructions followed immediately by the h2 closing tag: the same shape as before, with the new word. Leave the describe title as it is.
(b) Append one describe block to the end of app/src/ui/RecipePage.test.jsx, modelled on the tabindex block near line 778. Title it "the steps region is named Instructions (Mark, 2026-09-24)". The block declares its own read of RecipePage.jsx: fileURLToPath(new URL('./RecipePage.jsx', import.meta.url)) and readFileSync, then the same two replace calls that strip block and line comments. readFileSync and fileURLToPath are already imported at the top of the file. Add one it: match every opening tag of the form `<section className="method-region"` followed by any non-> characters up to `>` (a /g regex, `?? []` as the tabindex block does), expect exactly one match, and expect that tag toContain aria-label="Instructions".
Run the suite. Exactly these two tests fail, and all the others pass.

GREEN:
(c) app/src/ui/Method.jsx line 590: change the text of the h2 with className region-name to Instructions.
(d) app/src/ui/RecipePage.jsx line 1884: change the aria-label value on the section with className method-region to Instructions.
Run the suite and the build. Nothing else under app/ changes.</action>
  <verify>
    <automated>PATH="$(dirname "$(readlink /opt/homebrew/bin/node)"):$PATH" npm --prefix app test && PATH="$(dirname "$(readlink /opt/homebrew/bin/node)"):$PATH" npm --prefix app run build && python3 .planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-quick.py app</automated>
  </verify>
  <done>The suite reports 1155 passed (41 files), the build succeeds, and verify-quick.py app prints OK: no label occurrence of the old word under app/src, the new heading and aria-label present, and the identifiers and import untouched. Commit: "feat(260924-b7q): name the steps region Instructions on screen and in its accessible name".</done>
</task>

<task type="auto">
  <name>Task 2: Living docs say Instructions and record the Carried forward drop</name>
  <files>DESIGN.md, .impeccable/design.json, .impeccable/surfaces/route-recipe.md, .impeccable/surfaces/route-recipe-version.md, .impeccable/surfaces/route-recipe-batch.md, .impeccable/surfaces/route-print-recipe-sheet.md, .impeccable/surfaces/route.md, product-requirements/05-domain-and-language.md, .claude/skills/sketch-findings-sprinkles/references/page-shell-front-matter.md</files>
  <read_first>Only the lines named below in each file (use grep -n to confirm each anchor before editing); route-recipe.md lines 18-56 (the 03.5 revision); route-recipe-version.md line 63; 05-domain-and-language.md lines 76-82 and 126-153</read_first>
  <action>This task has nine files but it is one concern: small text replacements plus four dated insertions. Each edit below gives the old words and the new words. Use targeted replacements; never rewrite a whole file. Rule for anything not listed: leave lowercase generic uses of "method" alone, and so for CSS grid-area names ('method side'), ds-method-* classes, dated history outside the live sections, Mark's binder evidence, and everything under .impeccable/critique/, .impeccable/audit/, .planning/phases/, .planning/quick/ (other tasks), .planning/sketches/ and .claude/skills/sketch-findings-sprinkles/sources/.

DESIGN.md, six edits:
(1) Line 213, Context Boundary Rule: "ingredients, Method, and Notes belong" becomes "ingredients, Instructions, and Notes belong".
(2) Line 218, Recipe Sheet row: "as-made grams and Method changes in pen blue" becomes "as-made grams and Instructions changes in pen blue".
(3) Line 305, Body type role: the sentence "Method lead-ins are bold within the same size." becomes "Lead-ins in the Instructions are bold within the same size."
(4) Line 401, section role: "(Headnote, Ingredients, Method, Balance, Batch, Tasting)" becomes "(Headnote, Ingredients, Instructions, Balance, Batch, Tasting)".
(5) Line 439: the heading "### Method step" becomes "### Instructions step".
(6) Line 440: "heads the method at the prose measure" becomes "heads the Instructions at the prose measure".
Keep lines 297, 300, 312, 325, 343 and 347 (generic lowercase) and line 353 (grid areas).

.impeccable/design.json, three edits; the file must still parse as JSON:
Line 326: "name": "Method step" becomes "name": "Instructions step".
Lines 392 and 404: "ingredients, Method, and Notes" becomes "ingredients, Instructions, and Notes".
Keep the ds-method-* classes. Keep the authored-notes sample at line 378: it mirrors code that still renders Carried forward until 03.5 removes it.

.impeccable/surfaces/route-recipe.md, § "03.5 revision" only:
(1) Line 27: "ingredients by portion, Method, Notes." becomes "ingredients by portion, Instructions, Notes." Leave "Notes" as it is (see Task 3's open item).
(2) Line 30: "its Method changes" becomes "its Instructions changes".
(3) After the bullet "A version shows one batch in view, possibly none (`not yet churned`)." (line 34), and before "**3. Routes", insert two bullets with this exact text:
"- **Instructions** (Mark, 2026-09-24). The Sheet's steps region is named Instructions: its heading, its accessible name, and its printed page. Older wording in this brief that names the region Method means Instructions. Whether code identifiers (`Method.jsx`, `version.method`) follow is open for the 03.5 discussion."
"- **Carried forward notes are dropped** (Mark, 2026-09-24). The Sheet no longer carries a Carried forward block, and the folds drawn below desktop on the canvas lose their Carried forward fold. Before you start notes stay authored and inheritable; the inherited-note marker and its staleness reading (`route-recipe-version.md` § 3, "Inherited notes") keep their home on them. Proof-content counts elsewhere in the briefs that include three carried-forward notes describe the seed before this decision. **Phase consequence, 03.5 scope:** the code, seed and store lose `carriedForward`: the Carried forward block in `Authored.jsx`, its wiring and dirty check in `RecipePage.jsx`, the three notes in the `olive-oil.js` seed, the `transfer.js` validation, and their tests. `.impeccable/design.json`'s authored-notes sample follows the code. The three seeded notes' words need a destination decided in the 03.5 discussion: drop them, or move them to Before you start or the Sheet description."
Leave every older section as it is, including the Direction contract's "Formulation note. Method. Margin.". The 03.5 revision governs it.

.impeccable/surfaces/route-recipe-version.md:
(1) Line 24: "as-made grams and Method changes sit on the Sheet" becomes "as-made grams and Instructions changes sit on the Sheet".
(2) Line 63, the Inherited notes paragraph. After its last sentence ("…they live on the batch."), append: " (Mark, 2026-09-24: Carried forward notes are dropped. The marker and this staleness marking keep their home on Before you start notes, which stay authored and inheritable; `route-recipe.md` § "03.5 revision".)"

.impeccable/surfaces/route-recipe-batch.md, Assets and processes amendment only:
(1) Line 18: "its Method changes" becomes "its Instructions changes".
(2) Line 21: "Reordered and new Method steps in a batch" becomes "Reordered and new steps in a batch".
(3) Directly after line 21's bullet, insert: "- **Instructions** (Mark, 2026-09-24). The Sheet's steps region is named Instructions on screen, in its accessible name and in print; wording below that names it Method means Instructions. The binder evidence in § 2 keeps the words Mark's sheets used."
Keep line 44 (binder evidence) and lines 58, 83, 107, 123 and 158.

.impeccable/surfaces/route.md: on line 26, "as-made grams and Method changes in pen blue" becomes "as-made grams and Instructions changes in pen blue".

.impeccable/surfaces/route-print-recipe-sheet.md (Phase 4 builds from this brief, so its page names change in place):
(1) Line 48: "2. **Method page.**" becomes "2. **Instructions page.**". In the same line, "heads the method and is closed" becomes "heads the Instructions and is closed".
(2) Line 98: "the method page's" becomes "the Instructions page's".
(3) Vocabulary amendment, line 16: append to the end of the paragraph: " **Instructions** (Mark, 2026-09-24): the Sheet's steps region and its printed page are named Instructions; wording below that names either one method means Instructions."

product-requirements/05-domain-and-language.md:
(1) Line 80, Preferred vocabulary: "| Method; what you did |" becomes "| Instructions; what you did |".
(2) In "Interface labels settled on the recipe page", after the last row (the Headnote row), add the row: "| Method | Instructions | Mark, 2026-09-24: the heading and accessible name of the Sheet's steps, on screen and in print. |"
(3) Line 152, the "Kept as they are:" list: remove "Method, " and "Carried forward, " so the list ends "…Ingredients, Notes, Before you start, Uses."
(4) Directly after that line, add a blank line and then the paragraph "Carried forward is retired with the notes it named (Mark, 2026-09-24)."
Keep line 50 ("intended method", generic).

.claude/skills/sketch-findings-sprinkles/references/page-shell-front-matter.md:
(1) Line 14 becomes: "- **"Before you start" lives at the head of the Instructions** (Method in the sketch; renamed by Mark 2026-09-24), before step 1. Things to check sits under Balance; the carried-forward Notes that sat with it were dropped (Mark, 2026-09-24)."
(2) Line 61: "- Method: `ol` with" becomes "- Instructions: `ol` with".
Keep lines 22 and 57 (grid areas). Do not touch cross-cutting-type-spacing-feedback.md: its "## Verification Method" is generic.</action>
  <verify>
    <automated>python3 .planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-quick.py docs</automated>
  </verify>
  <done>verify-quick.py docs prints OK. It checks four things: every listed old phrase is gone and its replacement is present; design.json parses; the guards survive (method prose, ds-method-step, "Formulation note. Method. Margin.", the binder evidence line, "## Verification Method"); no historical directory appears in git status. Commit: "docs(260924-b7q): Instructions in the living docs; record Carried forward dropped".</done>
</task>

<task type="auto">
  <name>Task 3: The generator stops drawing Carried forward; todos and handoff carry the decisions</name>
  <files>.planning/canvas-generators/gen.py, .planning/todos/pending/2026-09-24-decide-whether-the-batch-and-tasting-log-is-entered-on-the-phone.md, .planning/todos/pending/2026-09-24-write-a-product-brief-for-phone-based-jobs.md, .planning/.continue-here.md, .planning/HANDOFF.json</files>
  <read_first>.planning/canvas-generators/gen.py lines 1-20, 348-358, 436-457 and 488-493; the phone-logging todo (whole file, 40 lines); .planning/.continue-here.md (whole); .planning/HANDOFF.json (whole)</read_first>
  <action>A. gen.py, five edits (per Mark 2026-09-24: the boards no longer draw Carried forward):
(1) Insert two lines between the TABS assignment (line 15) and the STYLESHEET assignment (line 16). First, the comment `# Carried forward notes dropped (Mark, 2026-09-24): the block leaves every board; Before you start keeps the inherited marker`. Second, a statement that rebinds SHEET to re.sub(pattern, '', SHEET, count=1, flags=re.S). The raw-string pattern is the literal opening `<div class="authored"><p class="authored__legend"><span>Carried forward</span><span>authored</span></p><ul class="authored__notes">`, then `.*?`, then `</ul></div>`. On the as-built board this block is the last child of the margin region.
(2) Line 351: in the comment, "(Before you start, Carried forward)" becomes "(Before you start)".
(3) phone_folds docstring, lines 437-438: remove "Carried forward, " so it reads "…Balance (with Things to check), and the log's Tasting; Ingredients, Instructions, Before you start and the churn cells stay open."
(4) phone_folds, lines 447-449: replace the comment "# close the balance fold before Carried forward" and its html.replace statement (the one whose search string starts with the Carried forward authored block and whose replacement adds the "Show 3 notes" disclosure). The new lines are the comment `# close the balance fold after Things to check, the margin's last block now that Carried forward is dropped` and the statement `html = html.replace('</div></article>', '</div></div></article>', 1)`. Why this is equivalent: the fold used to close just before the Carried forward block, which came straight after the derived-advisories close. With the block gone, only closing div tags lie between that point and the Sheet's single </article> on each rung board, so the output string is identical apart from the removed block.
(5) Lines 490-492: in the three rung titles, "details, Balance, notes and Tasting folded" becomes "details, Balance and Tasting folded".
Change nothing else, including the line 66 comment. Do not run gen.py or longhist.py directly; run verify-gen.py (below). If it exits 2 (the old scratchpad is gone), run `python3 -m py_compile .planning/canvas-generators/gen.py` instead and say so in the summary.

B. The phone-logging todo, .planning/todos/pending/2026-09-24-decide-whether-the-batch-and-tasting-log-is-entered-on-the-phone.md:
(1) Between the frontmatter and "## Problem", insert: "## Deferred — 2026-09-24", a blank line, and then "Deferred out of 03.5 (Mark, 2026-09-24) to a product brief for phone-based jobs: `.planning/todos/pending/2026-09-24-write-a-product-brief-for-phone-based-jobs.md`. This todo is that brief's first input; 03.5 designs no phone logging."
(2) In Solution, "Decide in the 03.5 discussion, not on the canvas:" becomes "Decide in the product brief for phone-based jobs, not in 03.5 and not on the canvas:".
(3) In the last paragraph, "(Details, Balance, notes, Tasting closed by default)" becomes "(Details, Balance, Tasting closed by default; Carried forward dropped 2026-09-24)".
(4) Task 2 shifted lines, so refresh the three frontmatter file references with current numbers. Find them with grep -n for "The phone reads and transcribes" and "**Responsiveness:**" in route-recipe.md, and "Desktop only in Phase 2" in route-recipe-batch.md.

C. New todo, .planning/todos/pending/2026-09-24-write-a-product-brief-for-phone-based-jobs.md, in the same format as B:
- Frontmatter, in this order: created (the current UTC time from `date -u +%Y-%m-%dT%H:%M:%S.000Z`), title "Write a product brief for phone-based jobs", area design, severity minor, and a files list. The list holds the phone-logging todo's path, then the same two brief references B(4) found ("The phone reads and transcribes" in route-recipe.md, "Desktop only in Phase 2" in route-recipe-batch.md), with their current line numbers.
- "## Problem": quote Mark (2026-09-24): "I want to do a product brief specifically for phone-based jobs." The briefs give the phone no job of its own. The recipe brief says the phone reads and transcribes and does not formulate in milestone 1. The batch brief records batches only at the desk. The 03.5 canvas's 393 board, checked on Mark's iPhone on 2026-09-24, raised whether the batch and tasting log is entered on the phone at the machine. That question is deferred out of 03.5 to this brief.
- "## Solution": write the brief before any phone-specific design or build. First input: the phone-logging todo (link its file name) and the two options it lays out. Open for Mark when it is picked up: which jobs the brief covers, and where the brief lives and what writes it (a page in product-requirements/ or an `/impeccable shape` surface brief). Until then, 03.5 designs no phone-specific job, and the 393 rung follows the briefs as they stand.

D. .planning/.continue-here.md:
(1) Set the frontmatter last_updated to the current UTC time.
(2) In the anti-pattern row "Regenerating the source drops an earlier edit", change the list "(Instructions, style 6 table, the chip, the hand, the folds)" to "(Instructions, style 6 table, the chip, the hand, the folds, no Carried forward)".
(3) Append this bullet to the end of completed_work: "- Quick task 260924-b7q (2026-09-24): Instructions rename in the app and the living docs; Carried forward drop and phone-logging deferral recorded; gen.py stops drawing Carried forward (not yet regenerated or published)."
(4) At the start of the remaining_work Task 6 bullet, after "Task 6:", insert: "Regenerate first: gen.py no longer draws the Carried forward block or its fold (quick task 260924-b7q). After regenerating, grep the outputs for no "Carried forward" and no "fold-notes" as well as the other approved changes. If the page's sticky note lists Carried forward among the folds, drop it there before publishing."
(5) decisions_made. The folds bullet "Below desktop, folds closed by default: version Details, Balance + Things to check, Carried forward, Tasting." becomes "Below desktop, folds closed by default: version Details, Balance + Things to check, Tasting (Carried forward dropped 2026-09-24)." Keep the rest of that bullet. Then append three bullets:
"- Carried forward notes dropped (Mark, 2026-09-24). The inherited-note marker and its staleness reading keep their home on Before you start notes, which stay authored and inheritable. Recorded in `route-recipe.md` § "03.5 revision" and `route-recipe-version.md` § 3. Removing `carriedForward` from the code, seed, store and transfer validation is 03.5 scope; gen.py already stops drawing it."
"- Phone logging deferred out of 03.5 (Mark, 2026-09-24) to a product brief for phone-based jobs: `.planning/todos/pending/2026-09-24-write-a-product-brief-for-phone-based-jobs.md`, with the phone-logging todo as its first input."
"- Method renamed Instructions everywhere user-visible (Mark, 2026-09-24): the app's heading and the region's accessible name, DESIGN.md, `.impeccable/design.json`, the living briefs, `05-domain-and-language.md` and the page-shell sketch finding. Code identifiers are unchanged pending Mark."
(6) blockers. The bullet becomes: "- None technical. Open for Mark in the 03.5 discussion: the rename label; whether a fold remembers being opened; Recipe Book form yield/timing fields; Done/Cancel wording; whether code identifiers (`Method.jsx`, `version.method`) follow the Instructions rename; where the three seeded carried-forward notes' words go (drop them, or move them to Before you start or the Sheet description), and whether "Notes" still names anything on the Sheet once Carried forward is gone."
(7) Required Reading: remove item 5 (the phone-logging todo, now deferred out of 03.5). Items 1-4 stay as written.

E. .planning/HANDOFF.json. Make targeted text edits in the existing compact style, not a reformat, and confirm the file still parses:
(1) Set timestamp to the current UTC time.
(2) remaining_tasks id 6 name becomes "Regenerate from gen.py (Carried forward dropped 2026-09-24), snapshot the chosen C boards into .planning/sketches/ via /gsd-quick, stylesheet inlined, MANIFEST.md updated".
(3) human_actions_pending[0].action becomes "Decide the open 03.5 items in discussion: rename label, fold memory, Recipe Book yield/timing fields, Done/Cancel wording, whether code identifiers (Method.jsx, version.method) follow the Instructions rename, where the three seeded carried-forward notes' words go and whether \"Notes\" still names anything on the Sheet".
(4) In decisions, the entry whose decision starts "Folds closed by default below desktop" changes: its decision becomes "Folds closed by default below desktop: Details, Balance + Things to check, Tasting", and its rationale gains the suffix " (Carried forward dropped 2026-09-24)".
(5) Append three decision objects, one per line like the others, each with phase "03.5":
decision "Carried forward notes dropped", rationale "Mark, 2026-09-24; the inherited-note marker keeps its home on Before you start notes; removing carriedForward from code, seed, store and transfer validation is 03.5 scope".
decision "Phone logging deferred out of 03.5 to a product brief for phone-based jobs", rationale "Mark, 2026-09-24; todo 2026-09-24-write-a-product-brief-for-phone-based-jobs.md takes the phone-logging todo as its first input".
decision "Method renamed Instructions everywhere user-visible", rationale "Mark, 2026-09-24; app heading and accessible name, DESIGN.md, design.json, living briefs, domain language, sketch findings; code identifiers pending Mark".
Leave uncommitted_files and next_action as they are.</action>
  <verify>
    <automated>python3 .planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-gen.py && python3 .planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-quick.py handoff</automated>
  </verify>
  <done>verify-gen.py prints "OK 16 boards and canvas.json match the published outputs minus Carried forward" (or exits 2 with py_compile passing, noted in the summary). verify-quick.py handoff prints OK. Both todos follow the todo format, and both handoff files carry the three decisions and the revised open list. Commit: "docs(260924-b7q): gen.py drops Carried forward; defer phone logging to a phone-jobs brief; update handoff".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | A static label rendered as JSX text (no markup injection path; the project bans dangerouslySetInnerHTML); markdown and JSON edits; a local generator copy run into a temp directory from local files. No network, no store, no publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q260924-01 | Tampering | gen.py / verify-gen.py | low | mitigate | verify-gen.py runs a copy of gen.py and longhist.py with OUT rewritten to a mkdtemp directory, removes it afterwards, and never writes into the previous session's scratchpad or publishes to the canvas. The plan forbids running gen.py directly. |
| T-q260924-02 | Repudiation | decision records | low | mitigate | Every recorded decision carries "Mark, 2026-09-24" in the briefs, the domain file, both todos and both handoff files, and verify-quick.py checks for the dated markers. |
| T-q260924-03 | Information disclosure | Instructions label and accessible name | low | accept | A static string replaces a static string. No data flow, route or persistence changes. |
</threat_model>

<verification>
Run from the tree root (worktree or main):
- `PATH="$(dirname "$(readlink /opt/homebrew/bin/node)"):$PATH" npm --prefix app test`: 1155 passed.
- `PATH="$(dirname "$(readlink /opt/homebrew/bin/node)"):$PATH" npm --prefix app run build` succeeds.
- `python3 .planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-quick.py app`, then `docs`, then `handoff`: each prints OK.
- `python3 .planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/verify-gen.py` prints OK (or exits 2, noted).
- `git status --porcelain` lists only the files in files_modified, plus whatever the orchestrator itself owns (STATE.md, SUMMARY.md).
</verification>

<success_criteria>
- The recipe page shows Instructions as the steps heading and exposes Instructions as the region's name. No identifier changed.
- Every living doc that names the region says Instructions, and history is untouched.
- The Carried forward drop is recorded in both briefs with its 03.5 consequence and its open question, and no generated board draws it.
- Phone logging is deferred to a new product-brief todo.
- The handoff open list reads: rename label, fold memory, Recipe Book yield/timing, Done/Cancel wording, code identifiers, and the seeded carried-forward notes' destination (with the Notes question).
</success_criteria>

<output>
Create `.planning/quick/260924-b7q-record-03-5-decisions-instructions-renam/260924-b7q-SUMMARY.md` when done. Include:
- The verify-gen.py result.
- The statement that regenerating and publishing the canvas boards happen in handoff task 6.
- The out-of-scope older wording left for the 05-domain row to govern: 01-product-brief.md:35, 03-decision-register.md:76, ROADMAP.md:23/464/496.
- The open question added for Mark: whether code identifiers follow the rename, and whether "Notes" still names anything on the Sheet.
</output>
