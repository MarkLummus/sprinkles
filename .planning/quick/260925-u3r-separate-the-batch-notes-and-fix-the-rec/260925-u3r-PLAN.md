---
phase: quick-260925-u3r
plan: 01
quick_id: 260925-u3r
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/ui/BatchRow.jsx
  - app/src/ui/BatchRow.test.jsx
  - app/src/styles/notebook.css
  - app/src/styles/notebook.test.js
  - app/src/styles/tokens.css
  - .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-probe.mjs
  - .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json
  - .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-after.json
autonomous: true
requirements: [BATCH2-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "In the batch log, the maker's two batch notes ('Soft, not greasy' and 'oil bottle opened 24 Jul') each sit on their own line. As sketch 011 draws them, the first note sits 18px below the measured cells grid and the second 4px below the first. Headless Chrome measures this at 393, 760, 1024, 1100 and 1366, within 0.5px."
    - "Each stored note renders as its own <p class=\"batch-row__note app-hand\"> inside a single <div class=\"batch-row__notes\">, At the machine first, then Ingredient notes. The count is exact: two notes give two paragraphs, one note gives one, and no notes gives no notes block. A note renders as text, never as markup."
    - "No other geometry changes at any measured width. Compared with the before readings, the following match within 0.5px: .recipe-page's grid-template-columns; the Sheet, ingredient, method and side regions (top, left, width, height); the log's top, left and width; the ingredient table's header rects; the tallest name cell; the portion sub-line's line count; the churn cells grid's rect; and the page's scrollWidth. Only what follows the notes inside the log moves down."
    - "Mark rejected both fixes proposed for the 1025–1365 band on 2026-09-25, so the band is deliberately untouched. No media rung, grid track, matchMedia query or ingredient-table column changes in this task. The band's breakpoints will be derived later from measured content constraints, after the table redesign is drawn."
    - "npm --prefix app test passes. Exactly one Vite dev server was started for the measurement and stopped afterwards. The vite preview --host on 4173 was never touched, and app/dist was never rebuilt."
  artifacts:
    - path: app/src/ui/BatchRow.jsx
      provides: "the notes block: one div.batch-row__notes, one p.batch-row__note.app-hand per stored note"
      contains: "batch-row__notes"
    - path: app/src/styles/notebook.css
      provides: "the notes block's flex column (4px gap, 18px above), outside every @media block"
      contains: ".notebook-log .batch-row__notes"
    - path: app/src/styles/tokens.css
      provides: "--app-notebook-log-group-gap: 18px, the board section's own gap between log groups"
      contains: "--app-notebook-log-group-gap"
    - path: .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-probe.mjs
      provides: "the re-runnable real-browser probe: the notes readings, the regression geometry and the before/after checks"
      contains: "gapCellsToFirst"
    - path: .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json
      provides: "the measured before state at five widths"
    - path: .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-after.json
      provides: "the measured after state at five widths"
  key_links:
    - from: app/src/ui/BatchRow.jsx
      to: app/src/styles/notebook.css
      via: "class names batch-row__notes / batch-row__note, styled under .notebook-log"
      pattern: "batch-row__note"
    - from: app/src/styles/notebook.css
      to: app/src/styles/tokens.css
      via: "var(--app-notebook-log-group-gap) and var(--app-notebook-gap-hairline)"
      pattern: "--app-notebook-log-group-gap"
---

<objective>
Separate the batch notes on the recipe route (/notebook/olive-oil-ice-cream/olive-oil-ice-cream-v1), measured in the real app before and after.

**The defect, at every width.** BatchRow.jsx renders the maker's two notes in `aside.notebook-log` as sibling inline `span.app-hand` elements. They share one line and read as "Soft, not greasyoil bottle opened 24 Jul", and the first sits 1px into the measured cells above it.

**The fix.** Sketch 011 is the authority. Every batch board (1600, 1366, 1024 and 393) draws the notes as a flex column with a 4px gap, inside the log section's own 18px gap. After this task each note is its own paragraph, spaced as drawn, and nothing else on the page moves.

**Out of scope, per Mark (2026-09-25).** The grid collapse between 1025 and 1365 (at 1100 the Sheet's 2fr track measures 235px, the name column 47px, and the headers overlap) is not fixed here. Mark rejected both proposed cuts: the band must not get an arbitrary checkpoint. Its breakpoints will be derived later from measured content constraints, after the table redesign is drawn. The constraints are the Sheet's one-column minimum and maximum, its two-column minimum and maximum, the log's minimum and the 224px nav, together with Mark's precedence rules.

This task therefore changes:
- no media rung in notebook.css or app.css
- no LOG_BESIDE_SHEET_QUERY
- no grid track
- no ingredient-table column
- nothing in the shell

Purpose: what happened at the machine reads as two observations, not one run-on line (BATCH2-02).

Output:
- The notes block (JSX, CSS and one token), with tests.
- A re-runnable probe, with committed before and after readings, proving the notes are separated and that no other geometry changed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./.claude/CLAUDE.md
@.planning/STATE.md
@DESIGN.md
@.planning/sketches/011-recipe-route-c/README.md

<interfaces>
Planning-time readings. Headless Chrome was run against the 4173 preview build; its CSS and UI code are identical to HEAD 9880b0f, and only seed data under app/src/data changed since.

Notes as they render today at 393, 1024 and 1366:
- Both spans share one top: 707.2 at 1366.
- The second note starts where the first ends: left 1034, then 1152.
- The first note's top is 1px above the cells' bottom.

The simulated fix used a div (flex column, 4px gap, 18px top margin) with a zero-margin p per note. It measured:
- first-note top minus cells bottom: 18.0
- second-note top minus first-note bottom: 4.0
- each note: the log column's full width, 27.5px tall (22px Caveat at 1.25)

Sketch 011, every batch board (search 1366-batch.html for "Soft, not greasy"):
- The log's section is a flex column with an 18px gap.
- The notes group is a div, a flex column with a 4px gap.
- Each note is a span in Caveat, 22px, line-height 1.25, color #1f3d7a.
- The board draws no caption above the notes.

The code today:
- app/src/ui/BatchRow.jsx, lines 1040-1043, reading branch, directly after the `.batch-row__cells` div: two conditional spans, `openBatch.churn.atTheMachine` then `openBatch.churn.ingredientNotes`, each `<span className="app-hand">`. BatchRow is mounted only inside `aside.notebook-log` (RecipePage.jsx:2064).
- app/src/styles/app.css:
  - `.app-hand` (lines 413-418) sets the face, size max(--size-hand, --size-hand-min), leading and --sheet-pen-blue, with no margin.
  - `.batch-row__cells` has `margin: var(--gap-s) 0 0` (top only).
  - The bare `.batch-margin` has no layout rule; it is a block.
- app/src/styles/notebook.css:
  - The log's reading-state rules are at lines 472-572, all scoped `.notebook-log ...`.
  - notebook.test.js requires every selector to start with `.notebook`, forbids bare px values, and pins the three media steps exactly. Those steps must not change.
- app/src/styles/tokens.css:
  - `--app-notebook-gap-hairline: 4px` (line 440) is the generic hairline gap, already reused.
  - `--app-notebook-log-head-gap: 18px` (line 453) is the head's control-group gap, a different role.
  - tokens.test.js fails on a declared-but-unread token and on an unresolved var().
- The test fixture: app/src/data/batch-2026-08-02.js has atTheMachine 'Soft, not greasy' and ingredientNotes 'oil bottle opened 24 Jul'. BatchRow.test.jsx imports it as augustSecondBatch and renders through renderBatchRow(props) (line 52) with mode 'reading'.
- Probe precedent: the orchestrator's /private/tmp/claude-501/-Users-mark-Documents-projects-sprinkles/ef22d0f1-0260-420e-b417-596f1df98440/scratchpad/shots4.mjs (the playwright-core import path and the Chrome executablePath).
- app/dist/index.html's mtime at planning time: 1790351935. The running 4173 preview serves app/dist, so this must not change.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Batch notes end to end (one notes block, each note its own paragraph in the hand, 18px under the cells and 4px apart), with the probe and the before readings taken first</name>
  <files>.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-probe.mjs, .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json, app/src/ui/BatchRow.jsx, app/src/ui/BatchRow.test.jsx, app/src/styles/notebook.css, app/src/styles/notebook.test.js, app/src/styles/tokens.css</files>
  <read_first>
    - .planning/sketches/011-recipe-route-c/1366-batch.html: the `<aside aria-label="Batch">` block (search "Soft, not greasy"). The section's gap is 18px and the notes div is a flex column with a 4px gap; the 1600, 1024 and 393 batch boards draw the same markup.
    - app/src/ui/BatchRow.jsx, lines 982-1065 (the reading branch)
    - app/src/ui/BatchRow.test.jsx, lines 1-70 (fixtures and renderBatchRow) and 1103-1144
    - app/src/styles/notebook.css, lines 1-20 and 472-572; app/src/styles/notebook.test.js, lines 1-50
    - app/src/styles/tokens.css, lines 415-455
    - app/src/styles/app.css, lines 406-418 (.app-hand)
    - /private/tmp/claude-501/-Users-mark-Documents-projects-sprinkles/ef22d0f1-0260-420e-b417-596f1df98440/scratchpad/shots4.mjs (the probe precedent)
  </read_first>
  <behavior>
    - The reading view of augustSecondBatch contains exactly this markup: div class="batch-row__notes", then p class="batch-row__note app-hand" with the text Soft, not greasy, then p class="batch-row__note app-hand" with the text oil bottle opened 24 Jul, then the closing div.
    - The markup contains exactly one class="batch-row__notes" and exactly two class="batch-row__note app-hand".
    - The notes block comes after the Airiness cell and before class="tasting-reading".
    - A batch whose ingredientNotes is null renders one batch-row__note paragraph (Soft, not greasy). A batch whose notes are both null renders no batch-row__notes at all.
    - A note whose text looks like HTML renders escaped, never as markup: a note of `<b>x</b>` appears as &lt;b&gt;x&lt;/b&gt;.
    - The old sibling-span shape is gone: no span whose class is exactly app-hand carries either note's text.
    - notebook.css has a top-level rule (not inside any @media) .notebook-log .batch-row__notes declaring display: flex, flex-direction: column, gap: var(--app-notebook-gap-hairline) and margin-top: var(--app-notebook-log-group-gap).
    - notebook.css has a top-level rule .notebook-log .batch-row__note declaring margin: 0.
  </behavior>
  <action>
Scope (Mark, 2026-09-25): the batch notes only. Do not touch:
- any @media block in notebook.css or app.css
- useBelowDesktop.js (LOG_BESIDE_SHEET_QUERY)
- any grid-template rule
- IngredientTable.jsx
- the shell

The 1025–1365 band is deferred to a constraints study.

**A. The dev server and the before readings.** Do all of this before editing any app file.

1. Run `ps aux | grep "[v]ite"`. It must list only the `vite preview --host` process, which listens on 4173. Leave that process alone for the whole plan.
   - If another dev server is already running (Mark may have restarted one), use it and note its port. Do not start a second one, and do not stop it at the end.
   - Never run `npm --prefix app run build`. It rewrites app/dist, which the 4173 preview serves to Mark's devices.
   - Record `stat -f %m app/dist/index.html`. The planning-time value is 1790351935.
2. Start exactly one dev server in the background with `PATH="/Users/mark/.nvm/versions/node/v24.21.0/bin:$PATH" npm --prefix app run dev -- --port 5173 --strictPort`. Wait until http://localhost:5173/ answers.
3. Write the probe at `.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-probe.mjs`, as an ES module, to this spec:
   - **Browser.** Import chromium from /Users/mark/.npm/_npx/e41f203b7505f1fb/node_modules/playwright-core/index.mjs. Launch with executablePath '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'; the bundled Chromium is missing.
   - **Arguments, positional:** baseUrl, outJson, shotDir, then optionally beforeJson.
   - **Route:** baseUrl + /notebook/olive-oil-ice-cream/olive-oil-ice-cream-v1.
   - **Widths:** 393, 760, 1024, 1100 and 1366.
     - Use a fresh browser context for each width, so IndexedDB is empty and the app seeds itself.
     - Viewport height 1100; wait for networkidle, then 800 ms.
   - **Readings per width.** Positions are in page coordinates (rect plus scroll offset), rounded to 0.1.
     - innerWidth and documentElement.scrollWidth.
     - gtc: the computed grid-template-columns of .recipe-page.
     - regions:
       - .notebook-body__sheet, .ingredient-table-region, .method-region and .side-region, each as {top, left, width, height}.
       - .notebook-log as {top, left, width}. Its height grows with the notes, so leave it out.
     - headers: each `.ingredient-table thead th` as {trimmed text, left, right}.
     - maxNameCellH: the tallest `td.ingredient-table__col-name`.
     - portionLines: the number of distinct rounded top values among the getClientRects() of a Range spanning the contents of the first `.ingredient-table__portion-note` whose text contains '12 g of 76.0 g'.
     - notes:
       - cells: the rect {top, left, width, height} of the first `.notebook-log .batch-margin .batch-row__cells`, which is the churn cells.
       - cellsBottom: that rect's bottom.
       - items: every element matching `.notebook-log .batch-margin > .app-hand, .notebook-log .batch-row__notes > .app-hand`, in document order, each as {tag, className, text, top, bottom, left, width, height}.
       - gapCellsToFirst: items[0].top minus cellsBottom.
       - gapBetween: items[1].top minus items[0].bottom.
       - sameLine: the two items' tops are equal.
   - **Screenshots into shotDir:**
     - W-log.png of `.notebook-log section.batch-row`.
     - W-page.png of the viewport at scroll 0.
   - **Output JSON:** {route, takenAt, readings: {width: reading}}.
   - **Checks.** They run only when beforeJson is given. Print every failure with its width and field, exit 1 if any fail, and otherwise print "checks passed".
     - Notes, at every width:
       - items has two entries, both P.
       - sameLine is false.
       - gapCellsToFirst is within 0.5 of 18.
       - gapBetween is within 0.5 of 4.
     - Regression, at every width, against the before reading:
       - scrollWidth and gtc are equal.
       - Every listed region field is within 0.5.
       - Header texts are equal, and header left and right are within 0.5.
       - maxNameCellH is within 0.5.
       - portionLines is equal.
       - The cells rect is within 0.5.
4. Take the before readings:
   - Run the probe with outJson `.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json`, and shotDir set to a `260925-u3r/before` folder in your own scratchpad, not the repo.
   - Confirm the defect reproduces: at every width the notes' sameLine is true.
   - If it does not reproduce, stop and report. The defect would then not be the one measured.

**B. RED.**
- In app/src/ui/BatchRow.test.jsx, replace the test at lines 1140-1144 ("renders the at-the-machine and ingredient notes in the hand...") with tests for the behavior list.
  - Build the one-note batch by spreading augustSecondBatch and its churn with ingredientNotes null.
  - Build the no-note batch the same way, with atTheMachine null as well.
- In app/src/styles/notebook.test.js, add a describe "the log's batch notes (260925-u3r, sketch 011)". Through readAllRules, it asserts the two rules from the behavior list, using rules whose media is undefined.
- Run the two files; the new tests must fail.

**C. GREEN.**
- **BatchRow.jsx, lines 1040-1043.**
  - Render the notes block only when atTheMachine or ingredientNotes is present.
  - The block is one div with className batch-row__notes. It holds a p with className "batch-row__note app-hand" for atTheMachine when present, then one for ingredientNotes when present.
  - The text goes in as React children, never as markup.
  - Extend the existing comment: each note is its own paragraph in one column, 4px apart and 18px under the cells, as every sketch 011 batch board draws it (260925-u3r).
- **tokens.css.**
  - After line 453, add `--app-notebook-log-group-gap: 18px;` with a trailing comment. It is the log section's own gap between its groups on every sketch 011 batch board, read here as the space between the measured cells and the maker's notes.
  - Append to line 440's comment that it is reused as the gap between the maker's batch notes in the log (260925-u3r).
  - Use a new token for the 18px because `--app-notebook-log-head-gap` names a different role.
- **notebook.css.**
  - After the `.notebook-log .batch-row__plan` rule (around line 552), outside every @media block, add the two rules from the behavior list, under a comment citing the boards' notes div and 260925-u3r.
  - The `margin: 0` rule resets the UA paragraph margin, so the flex gap alone spaces the notes.
  - Touch no other rule and no media block.
- Run the focused tests, then the full suite; both pass.

**D. Live check.**
- Re-run the probe against the running dev server with beforeJson set to the before file.
- Write its outJson to your scratchpad, not the repo, and its screenshots to a `260925-u3r/notes` folder there.
- It must print "checks passed".
- Read the 1366 and 393 log screenshots to confirm each note sits on its own line.
- Leave the dev server running for Task 2.

**E. Commit** the probe, the before readings and the five app files. Use an English message, for example "fix(260925-u3r): give each batch note its own line, 18px under the cells and 4px apart".
  </action>
  <verify>
    <automated>PATH="/Users/mark/.nvm/versions/node/v24.21.0/bin:$PATH" npm --prefix app test -- src/ui/BatchRow.test.jsx src/styles/notebook.test.js src/styles/tokens.test.js && test -s .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json && test "$(grep -c 'batch-row__notes' app/src/ui/BatchRow.jsx)" -ge 1 && PATH="/Users/mark/.nvm/versions/node/v24.21.0/bin:$PATH" node .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-probe.mjs http://localhost:5173 "$TMPDIR/u3r-notes.json" "$TMPDIR/u3r-notes" .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json</automated>
  </verify>
  <done>
- The before readings are committed and reproduce the defect: the notes share one line at every width.
- Each batch note is now its own paragraph in one notes block, measured in Chrome at all five widths: 18px under the cells, 4px apart, never on a shared line.
- No other measured geometry changed.
- The focused tests and the full suite pass.
- Exactly one dev server is running (on 5173, or the one that was already running), and 4173 is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the after readings, prove that nothing else moved, and stop the dev server</name>
  <files>.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-after.json</files>
  <read_first>
    - .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-before.json
    - .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-probe.mjs
  </read_first>
  <action>
1. **After readings.**
   - Run the probe with outJson `.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-after.json`, beforeJson set to the before file, and shotDir set to a `260925-u3r/after` folder in your scratchpad.
   - It must print "checks passed", which covers both the notes check and the regression check at 393, 760, 1024, 1100 and 1366.
   - Read the before and after log screenshots at each width. By eye, confirm that:
     - each note is on its own line;
     - there is visible space between the cells and the first note;
     - everything above the notes looks unchanged.
   - Read the before and after page screenshots at 1100 and 1366 and confirm they are identical above the notes.
2. **Full suite.** Run `npm --prefix app test`; it passes.
3. **Stop the dev server you started.**
   - Kill the process listening on 5173 (`lsof -tiTCP:5173 -sTCP:LISTEN`) and its npm parent. Skip this if you reused a server Mark had already started.
   - Confirm nothing listens on 5173 (when you started it).
   - Confirm the preview still listens on 4173.
   - Confirm `stat -f %m app/dist/index.html` still reads the value Task 1 recorded.
4. **Commit** the after readings. Use an English message, for example "test(260925-u3r): after readings, batch notes separated and no other geometry moved".
5. **SUMMARY.**
   - For each of the five widths, give before and after for:
     - the two notes' rects and gaps
     - gtc
     - the region widths
     - the header rects
     - maxNameCellH
     - portionLines
   - List the screenshot folders by absolute path.
   - Record, noticed but not fixed:
     - **The 1025–1365 band, deferred to a constraints study (Mark, 2026-09-25).** At 1100 the Sheet beside the log is 480px wide. Its 2fr/1fr spread measures 234.656px 117.344px, the name column is 47px, the headers overlap, and the '12 g of 76.0 g · 9.5% in all' sub-line wraps one word per line; at 1200 the name column is 113px. Mark rejected both proposed cuts: log below with the Sheet in one column below 1366, and log below with the Sheet keeping two columns from 1100. The band's breakpoints will be derived from measured content constraints once the table redesign is drawn: the Sheet's one-column minimum and maximum, its two-column minimum and maximum, the log's minimum and the 224px nav, with Mark's precedence rules.
     - **The log's other spacing doesn't follow the boards' 18px rhythm.** The section gap in `.notebook-log .batch-row` is 20px, the tasting sits 20px plus 12px below, and Next time and provenance each sit 32px down.
     - **columns.test.js's width model is stale.** It has ignored the shell's nav and the log column since 03.5, so its figures overstate the real table width: about 825px at 1366, against 412px real.
     - **The stacked log at 1024 is narrower than drawn.** It is 498.9px wide against the board's 720px, already accepted in 03.5-08's conformance record.
  </action>
  <verify>
    <automated>PATH="/Users/mark/.nvm/versions/node/v24.21.0/bin:$PATH" npm --prefix app test && test -s .planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-readings-after.json && ! lsof -iTCP:5173 -sTCP:LISTEN && lsof -iTCP:4173 -sTCP:LISTEN | grep -q node && test "$(stat -f %m app/dist/index.html)" = "1790351935" && test -z "$(git diff --name-only 9880b0f -- app/src/styles/app.css app/src/ui/useBelowDesktop.js app/src/ui/useBelowDesktop.test.js app/src/ui/IngredientTable.jsx app/src/ui/Shell.jsx app/src/styles/shell.css app/src/styles/home.css DESIGN.md .planning/sketches .impeccable)" && NBDIFF="$(git diff 9880b0f -- app/src/styles/notebook.css)" && test -n "$NBDIFF" && test -z "$(printf '%s\n' "$NBDIFF" | grep '^-[^-]')"</automated>
    <human-check>End-of-phase UAT, deferred (human_verify_mode end-of-phase). When Mark next rebuilds for device UAT (build, then preview --host), he checks on the iPad at 1366 landscape and 1024 portrait that the two notes read on their own lines and nothing else moved.</human-check>
  </verify>
  <done>
- The after readings are committed, and the probe's notes and regression checks pass at all five widths.
- The full suite passes.
- notebook.css only gained lines, and app.css, useBelowDesktop.js, IngredientTable.jsx, the shell, Home, DESIGN.md, the sketches and .impeccable are unchanged since 9880b0f.
- The dev server Task 1 started is stopped, and the 4173 preview and app/dist are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maker-typed batch notes, stored and importable, reaching the rendered log | Free text supplied by a maker (or by an imported store file) is rendered on the recipe route |
| The measurement tooling reaching the running local servers | The executor starts a dev server beside Mark's device-UAT preview on 4173, which shares the workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-u3r-01 | Tampering | BatchRow.jsx notes block | low | mitigate | Notes render as React text children inside p elements, with no dangerouslySetInnerHTML (project convention). Task 1's test asserts that an HTML-like note renders escaped. |
| T-u3r-02 | Denial of service | the 4173 preview and app/dist | low | mitigate | Exactly one dev server on 5173 with --strictPort, stopped at the end. The preview process is never signalled and no build runs. Task 2's verify confirms 4173 still listens, 5173 is free, and app/dist/index.html's mtime is unchanged. |
| T-u3r-SC | Tampering | package installs | low | accept | No package is installed. The probe uses the playwright-core already in the npx cache and the system Chrome. |
</threat_model>

<verification>
- `PATH="/Users/mark/.nvm/versions/node/v24.21.0/bin:$PATH" npm --prefix app test` passes.
- The probe's after run prints "checks passed" at 393, 760, 1024, 1100 and 1366, which means:
  - the notes are 18px under the cells and 4px apart, each on its own line;
  - the grid tracks, the Sheet regions, the log's position and width, the header rects, the name cells, the portion sub-line, the cells grid and scrollWidth all match the before readings.
- The before and after reading files are committed in the quick directory, and the SUMMARY lists the screenshots by absolute path.
- notebook.css only gained lines. No media rung, matchMedia query, grid track or ingredient-table column changed.
- The dev server is stopped, the 4173 preview still runs, and app/dist is untouched.
</verification>

<success_criteria>
- Mark reads "Soft, not greasy" and "oil bottle opened 24 Jul" as two notes, spaced as sketch 011 draws them, at every width.
- Every other measured geometry is unchanged, 1100 included, whose band defect is deliberately left for the constraints study.
- The SUMMARY carries the deferred band and the other items noticed but not fixed.
</success_criteria>

<output>
Create `.planning/quick/260925-u3r-separate-the-batch-notes-and-fix-the-rec/260925-u3r-SUMMARY.md` when done.
</output>
