---
context: sketch
sketch: 007-full-battery
status: in_progress
last_updated: 2026-09-12T16:50:31.861Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| /impeccable live wrong-file scaffold match | The live-mode picker's text-similarity wrap matched the wrong file twice — once a different sketch (005 instead of 007), once real production code (`app/src/ui/BatchRow.jsx`) instead of the static sketch HTML being edited | advisory | Verify the scaffold's target `file` field before writing on every live-mode event; if it doesn't match the sketch you're pointed at, reply `error` and don't write. Both misfires were caught and reverted cleanly this session, but this is worth watching for again if live mode is retried on this file. |
| CSS asserted equal ≠ CSS rendered equal | Two elements with identical coded margin values were not actually equal on screen (grid vs. flex-column margin inclusion differs; `border-width` fractional values silently round on `<input>`/`<textarea>` in Chrome) | advisory | Always measure with `getBoundingClientRect()` / `getComputedStyle()` in a real browser before calling a spacing question resolved — reasoning from the CSS source alone missed several real bugs this session (double margins, a stray UA `<p>` margin, a 1.5px border silently becoming 1px). |

## Required Reading (in order)

1. `.planning/sketches/007-full-battery/README.md` — full design history: why the axes are goldilocks-scaled, why defects are a standalone section, why the note field's position moved, why "Next time" stays a shared field not a section.
2. `.impeccable/critique/2026-09-12T12-51-03Z__planning-sketches-007-full-battery-index-html.md` — the persisted dual-agent critique (25/36), with the 5 open priority issues.
3. `PRODUCT.md` / `DESIGN.md` at repo root — product truth and the "Formulation Cookbook" visual system (Two-Ink Rule, No-Verdict Rule, Bookcloth Rule) that constrain any further changes.

<current_state>
Sketch 007 (`.planning/sketches/007-full-battery/index.html`) is mid-iteration. This session did a long pass of measured spacing/layout fixes, then ran `/impeccable critique` (dual-agent, scored 25/36, 2 P1s + 2 P2s + 1 P3 open), then Mark did a substantial accessibility/typography/responsive hardening pass himself (radiogroups, aria-live announcements, field validation with inline errors, undo, textarea auto-resize, a `@media (max-width:600px)` block, forced-colors support, a new local type scale). Immediately after that, Mark asked for a new exploration: a "tasting hidden until added" interaction, added as a toolbar toggle alongside the existing "always visible" behavior (default unchanged — Option A, 3-column axes, always-visible tasting).
</current_state>

<completed_work>

Completed Tasks:
- Task 1–11: Series of measured spacing/consistency fixes (caption alignment, prose-field vs ink-field usage, underline weight vs. section rule, double-margin bugs across both note-position variants, stray UA `<p>` margin, axis name wrapping in 3-column mode). All verified with real DOM measurements, not just visual inspection.
- Task 12: `/impeccable layout` — ran mechanical scan + manual assessment, both clean, no changes made.
- Task 13: Answered whether "Next time" should be its own `<section>` — Mark chose to leave it as a shared field.
- Task 14: `/impeccable critique sketch007` — dual sub-agent run (design review + detector/browser evidence), scored 25/36 (na: heuristic 10), persisted snapshot, trend 22→25.
- Task 15: Added the "Tasting: always visible" / "Tasting: hidden until added" toolbar toggle with a full add/remove/undo state machine; verified in-browser (collapse, add, remove-while-empty, undo-restores-and-reopens, mode-switch-forces-visible).
</completed_work>

<remaining_work>

- Critique P1: pre-filled numeric/text fields (churn section always, tasting section when in "always visible" mode) render pen-blue on load — indistinguishable from real maker-entered data, contradicting the sketch's own "blank stays blank" rule.
- Critique P1: no visual sub-grouping between the 4 fixed-core axes (Hardness/Scoopability/Smoothness/Sweetness) and the 2 recipe-declared axes (Body/Oil) — PRODUCT.md treats these as a real distinction the layout doesn't express.
- Critique P2: `all-caps-body` detector finding on "Any problems?" — patched visually via a child `text-transform:none` span, but the parent `<p class="lbl">` still declares uppercase over the full sentence. Real fix: move "Select all that apply." out to a sibling element instead of a nested override.
- Critique P2: responsive clipping below 760px — Mark's own hardening pass added `@media (max-width:600px)` and fluid `auto-fit` grid columns after the critique ran. Worth a fresh check (resize to ~400–500px, confirm no more clipped/off-screen controls) before treating this as resolved.
- Critique P3: "Remove tasting" in "always visible" mode has no confirmation and isn't as visibly consequential as the "hidden until added" mode's collapse.
- New from this session: decide whether "hidden until added" should become the primary/default interaction, stay a permanent comparison toggle, or get removed once a decision is made.
</remaining_work>

<decisions_made>

- Variant A (note before Texture & flavor) + 3-column axes = Mark's current preference; set as the toolbar's default init state (`setCols(3); setVariant('A'); setTastingMode('visible');`).
- "Next time" stays a shared field outside both sections, not promoted to its own `<section>` — it's explicitly cross-cutting per its own placeholder text, and promoting it would spend the Bookcloth Rule's region-name green on one textarea while reintroducing a double-rule problem already fixed once before.
- "Density / body" → "Body", "Oil character" → "Oil" — both wrapped to two lines in 3-column mode; anchors already carried the meaning.
- prose-field underline uses a flat 2px, not 1.5px — fractional border-width doesn't reliably render on `<input>`/`<textarea>` in Chrome (measured it silently rounding back to 1px).
</decisions_made>

<blockers>
None. All open items are design-judgment decisions pending Mark's input, not technical blockers.
</blockers>

## Infrastructure State

- No background processes running. Every `python3 -m http.server` instance and the one `impeccable live-server` session started for verification this session were stopped and confirmed dead before moving on.
- `/impeccable live` was configured for this project (`.impeccable/live/config.json` targets `.planning/sketches/**/*.html`) but is not currently running. If resumed, re-verify the scaffold's target file on every event — see Critical Anti-Patterns above.

<context>
The working pattern that stuck for the rest of the session, after two /impeccable live misfires: describe the change in chat, I locate and measure the real DOM state (never just read CSS and assume), edit `index.html` directly, then re-serve via a throwaway `python3 -m http.server` + a fresh Chrome tab to verify before reporting back. That loop is reliable and should continue.
</context>

<next_action>
Start with: ask Mark whether to (a) tackle the critique's remaining P1/P2 issues next, or (b) keep exploring the "hidden until added" tasting interaction he just requested (e.g. deciding its default, or extending the same collapse pattern to the churn section's pre-filled-values problem).
</next_action>
