---
sketch: 008
name: control-sheet
question: "Drawn from the amended DESIGN.md, does one sheet of every control in every state give one place to tweak the control language before it is applied app-wide — and does fill hold for inline text toggles (A) or only boxed controls (B)?"
winner: "B with the leading ink square, checkbox-sized: a text toggle is a checkbox in prose (Mark, 2026-09-14)"
tags: [controls, states, design-system, tokens]
---

# Sketch 008: The control sheet

## Design Question
DESIGN.md was amended on 2026-09-14 (Phase 03.3.1.1, D-21): a picked or pressed control fills pen blue with the paper's colour for its text, with no change of weight and no underline, and under forced colours an ink outline stands in; measured fields are sized by their width tokens. The rule is app-wide, but it was decided on the record pen alone, and the recipe page's other pressed controls (Show changes, Table as written, the step's Skipped checkbox) follow in a quick-batch. This sheet draws every control in every state on one page so the control language can be tweaked once, before that batch runs — and asks the one question the rule leaves open: does a filled inline text toggle read as a button inside prose?

Built unattended on 2026-09-14 while phase planning ran, from DESIGN.md, `.impeccable/surfaces/route-recipe-batch.md`, sketch 007's live CSS (including its uncommitted D-22 revision), sketch 003's page shell, the sketch-findings skill, `app/src/styles/tokens.css` and `app.css`, and 03.3.1.1-CONTEXT.md D-04 to D-13.

## How to View
Served at http://127.0.0.1:8077/008-control-sheet/index.html (the static server on `.planning/sketches/`), or `open .planning/sketches/008-control-sheet/index.html`.

The first cell of every row is live — hover it, tab into it, click it. Every other cell is a drawing with its state forced by a class (`is-hover`, `is-focus`, `on`, `forced`), so the states sit side by side and can be compared without a mouse. "Annotate on hover" in the toolbar prints the hovered element's computed box, border, fill, ink, type, case, underline and outline — measure, then edit. The width buttons constrain the paper to 393, 768 and 1280.

## What is on the sheet
The sheet states the current rule first, in its header, and then draws it: three kinds of control. Joined boxes choose one from a set (the segmented control, the five-cell axis) and the picked box fills pen blue. A square and a word record an independent yes or no (the checkbox, a defect, Show changes) and the small square fills pen blue. A lone hairline box or an underlined word is an action; nothing fills. Hover thickens a border to 2px in place; focus is one 2px ring offset 2px around the whole control and means focus only; invalid is a sentence beneath the field, never a ring; in high contrast selection stays a fill in the system's Highlight colour.

Then one section per control family, each with a one-sentence rule and its states drawn left to right (rest, live · hover · focus · picked or on · picked + focus · high contrast, simulated · high contrast + focus; fields add holding a value, invalid and invalid + focus), with the family's history behind a "history" disclosure. Then "Side by side": the four families complete, one state per row, so each can be judged against the others in the same state (scrolls sideways when narrow; the state names stay put). Then captions and type roles, the below-760px sizes, the open questions as a list, and "Earlier versions" — the retired alternatives behind a disclosure, drawn for the record and never the rule.

## Variants
There are no live variants any more. Variant A of the text toggle (the word filled), the boxed defect chips, the separated stops, the no-hover segment option and the ringed invalid field all sit under Earlier versions.

## What to Look For
- **The text toggle row, A against B.** Does the filled word in A read as a pressed toggle, or as a button that has wandered into the acts row? Does B's 2.5px underline read as pressed, or as a heavier hover? Is the leading square honest (a text toggle is a checkbox) or a third control shape the page does not need?
- **Focus on a filled box** (the "picked + focus" cells): an ink ring 2px off a pen-blue box — does it read as focus, or as a second state?
- **Hover on segment options and chips**: D-06 gives them none; the binder gives every button 1.5px. Which should stand, now both are visible beside each other?
- **The forced-colours cells**: the outline sits on the border (no offset), as 007 draws it, while the focus ring is offset 2px. Should the fallback be offset too?
- **The checkbox fills pen blue** (DESIGN.md § Components) — the app fills it ink today. Which colour is the checkbox's?
- **The invalid cell**: 007 draws the invalid ring at the focus ring's exact weight and offset, so invalid and focus are indistinguishable. Keep, or give invalid the error sentence alone?
- **Captions at 0.04em** (brief, DESIGN.md) — 007's live CSS reads 0.025em on `.lbl` and axis names and 0.035em on region names. Which tracking is the caption's?
- **The field row's two-line caption reservation** (`min-height: 2.4em`, 007 line 39, D-03): the captions sit high above their fields when none wraps.
- Below 760px: 44px text controls (007) against the app's deliberate none — does an inline text control want height?
- **Does the 1.5px weight exist on your screen?** Measured in headless Chromium (Playwright, DPR 1 and emulated DPR 2): every 1.5px border and outline computes and lays out as **1px** — Chromium floors fractional border and outline widths to whole CSS pixels (1.5 → 1, 1.75 → 1, 2.5 → 2; the 1.5px underline is not floored and does render). So in that browser the hover thickening on buttons, stops and checkboxes never appears, the compensated hover box shrinks by 1px instead of holding, and the focus ring is 1px, no heavier than a pressed element's outline. Hover the "hover" cell with Annotate on: if the panel prints `border 1px` on your display too, the hover and focus language needs a whole-pixel weight or a different means. 007 and the app share the same tokens, so whatever this shows holds for them.

## Verified in a browser (2026-09-14, gsd-browser, headless Chromium)
Measured on the served page at 1920×1080 (frame 1280) and on an emulated iPad Mini (768, DPR 2): no console errors; 54 cells, 21 live; no horizontal overflow at either width; nothing in a static cell is tabbable or changes on click; the live stop, segment, chip and text-toggle cells toggle and clear, the axis head switching "(Not recorded)" to "(3)" in pen blue with Clear appearing. Stops 34×32 on a 186px track with matching anchors; the head 24px marked or not; caption-to-field gap 6px; date field 128px; figure field 48px with the unit 4px off it; checkbox 13×13 filling pen blue; chips 32px tall, the declared toggle 20px after them, its helper a lowercase 13px sibling; segment options −1px overlapped, the picked one filled pen blue with a pen-blue border on top; region names 6px above their helper, tracking 0.04em on captions and region names; the touch block at 40×44 stops on 216px, 44px buttons, text controls, options, chips and fields, fields at 16px. Variant B: pressed underline 2.5px, unchanged on hover, no fill, no forced-colours outline; the leading-square alternative shows only in B. Forced cells strip every fill and carry the 2px outline. Not measurable here: the 1.5px weights (see above).

## Choices made unattended (for Mark to confirm)
1. **Mood intake skipped.** The brief fixed the direction, so the workflow ran as `--quick`; no questions were asked.
2. **One sketch, not several.** The brief is one design question, so decomposition produced one row (this sheet) rather than splitting controls across sketches.
3. **One DOM, variant by attribute.** Both variants share the sheet; `data-variant` switches only the inline text toggle's pressed CSS (007's trick), so the rest of the sheet cannot drift between A and B.
4. **The cell set per row:** rest (live), hover, focus, picked, picked + focus, forced; invalid only on the numeric field; pressed + hover only on the text toggle row, where hover and pressed compete for the underline. **No disabled column** — the brief did not ask for one; DESIGN.md's dashed disabled border is drawn nowhere on the sheet.
5. **Hover on segment options and chips: both drawn, neither chosen.** D-06 (no rule) and the binder's 1.5px thickening sit side by side, each labelled.
6. **Forced colours simulated in ink**, not with the `Highlight` keyword, so no fifth colour appears on the paper; the real `@media (forced-colors: active)` rule using Highlight is in the file too. The simulated outline has no offset, as 007 line 141 draws it.
7. **B's pressed treatment is the tick-weight underline** (2.5px), chosen because the page already owns that weight and it survives forced colours; the leading-square alternative is drawn as a labelled extra cell rather than a third variant.
8. **A's filled text toggle takes 2px inline padding with a negative margin**, so the word does not move when it fills — DESIGN.md gives text controls padding 0, so this is an addition to confirm.
9. **Sketch-local tokens on `.app`, not in `themes/default.css`** — the shared theme was left untouched, as 007 did with its `--type-*` roles.
10. **Anchor words in the text face, italic** (007 line 81), not the app's grotesk italic.
11. **The prose field's baseline is the graduation weight** (DESIGN.md "a hairline baseline"; 003 line 41), not 007's literal 2px; its focus is the one ring, not 003's thickened baseline.
12. **Text controls take 44px in the touch block** (007 line 177) though the app deliberately gives them none (`app.css` 1878).
13. **Fields read 1rem in the touch block** (007's ≤600 rule, line 137) though the app drops them to 0.8125rem (`app.css` 1921).
14. **The invalid ring reads its own token** (`--rule-invalid`, pointing at the focus weight as 007 does) so it can be moved without touching focus.
15. **Caption tracking 0.04em** (brief, DESIGN.md), not 007's 0.025em/0.035em; **region-name gap 6px** beneath (DESIGN.md), not 007's 12px.
16. **The checkbox fills pen blue** (DESIGN.md § Components), not ink as the app draws it.
17. **"Table as written" was drawn, then removed (Mark, 2026-09-14):** Phase 03.3 settled step order as the table's only order, with no toggle; the sheet's text-toggle row shows Show changes alone.
18. **Winner left `null`, no ★** — this is Mark's call; the MANIFEST row says pending.
19. **No `--wrap-up`; 007 and 003 untouched; only `008-control-sheet/**` and `MANIFEST.md` staged.**
20. **No arrow-key roving focus in the live cells** — click, hover and tab work; the radio-group keyboard mechanism is 007's and was not redrawn here.

## Where the sources disagree (listed, not resolved)
1. **DESIGN.md against itself.** § binder (line 132), § Shapes (221) and § Components (229, 237, 240, 242) say a picked control fills pen blue; § Elevation & Depth (212: "a pressed toggle all read bold plus a 1px ink outline offset 2px") and the Do's list (292: "carry picked, open, current, and marked states by weight and outline") still carry the retired rule. The D-21 amendment did not reach them.
2. **Checkbox fill colour.** DESIGN.md § Components: "filled pen blue when checked". `app.css` 150: `input[type='checkbox']:checked { background: var(--ink) }`. The brief (route-recipe-batch.md line 115): "an ink square filled when checked" — colour unnamed. DESIGN.md § Front matter "Axis mark" still describes nine stops "filled ink when checked".
3. **Field widths.** DESIGN.md § Fields: "the date-field width token (128px) … the numeric-field width token (48px)". `tokens.css` still holds `--field-w-date: 12ch` and `--field-w-figure: 6ch`. The sketch theme has no width token; 007 uses the literals 128px and 48px (lines 41, 43).
4. **Caption tracking.** Brief and DESIGN.md: 0.04em. 007's live CSS: 0.025em on `.lbl` and axis names (lines 153–154), 0.035em on region names (152). D-10 cites "0.04em as the sketch's `.lbl` (line 36)", but line 153 overrides line 36.
5. **Region-name gap.** DESIGN.md: 6px beneath. 007 line 31: `margin: 0 0 var(--gap-s)` (12px).
6. **Hover on segment options and chips.** DESIGN.md § binder: the border thickens on hover for buttons (chips and options are buttons). D-06: no hover rule for either.
7. **Text-control size.** DESIGN.md `text-control` typography = small print (0.75rem); `app.css` 72 reads `--size-small-print`. 007 line 163 sets `--type-control` (0.8125rem), and DESIGN.md's Control role (0.8125rem) is "actionable choices".
8. **Segment option and chip size.** 007: `--type-control` (0.8125rem). `app.css` 1698 and 1742: `--size-table-body` (0.9375rem).
9. **Prose-field baseline.** 007 line 33: `border-bottom: 2px solid` (a literal). 003 line 41 and DESIGN.md: hairline. 003 line 42 also thickens the baseline on focus with `outline: none`, against DESIGN.md's one focus ring.
10. **Invalid state.** 007 line 132 draws `[aria-invalid="true"]` at the focus ring's weight and offset — indistinguishable from focus. `app.css` 1787–1797 draws no ring ("the field's aria-invalid is the fact, not a red line"). DESIGN.md names no invalid state.
11. **Anchor-word face.** 007 line 81: text face, italic. `app.css` 1659: grotesk, italic.
12. **Text-control touch height.** 007 line 177: 44px. `app.css` 1878: deliberately none.
13. **Field size at ≤600px.** 007 line 137: grows to 1rem. `app.css` 1921: shrinks to `--type-control`.
14. **Status-line measure.** 007 line 169: 70ch. `tokens.css` reconciles the note role to `--measure-prose` (65ch).
15. **"Table as written."** Resolved: not a control — Phase 03.3 made step order the table's only order. Removed from the sheet, from 03.3.1.1-CONTEXT.md's deferred list and from the page-shell findings (2026-09-14).
16. **Forced-colours outline offset.** 007 line 141: `outline: 2px solid Highlight` with no offset, while the focus ring is offset 2px. Nothing states which is intended.
17. **Filled text-control padding.** DESIGN.md § Components gives `text-control` padding 0; a fill with no padding hugs the glyphs. The sheet adds 2px (choice 8); no source decides it.

## Phone-width honesty (2026-09-14, after Mark's review)
Mark saw the pressed text toggle too short at 393px. The sheet's 393 preset only narrowed the frame; the below-760px sizes lived in the second block and nowhere else, so every main row kept desktop sizes at phone width — and the sheet's text controls had no minimum height at all (16.9px) where 007 gives them 24px at desktop (line 128). Fixed: `.text-control` carries `--text-control-min: 24px`; the whole sheet takes the `.touch` sizes under the 393 preset and at a real viewport of 760px or less (`syncTouch`, driven by the preset and `matchMedia`), so every row is honest at phone width. Measured: Full → 24px text toggles; 393 preset and a real 393px viewport → 44px on every row, 44px buttons.

This exposed variant A's phone-width problem: a pressed text toggle that fills its box fills a 44px-tall pen-blue block in the acts row. The touch block now draws both: "A fills the 44px box" and "A alternative: fill on the word, 44px hit box" (`textToggleWord` — the fill sits on an inner span, the control keeps its transparent 44px hit box; forced colours strip the word's fill too). Measured: box fill 44px pen blue; word fill 15px pen blue inside a 44px transparent button.

## Winner: B with the leading ink square (Mark, 2026-09-14)
Mark's reading of the sheet: the inline text toggle and the plain text control looked too much alike, so a pressed state carried only by underline weight or by fill did not separate them. The B alternative — a leading ink square, hollow at rest and filled when pressed — is now variant B's treatment on the sheet (the separate "B alternative" cell is gone), and the square is the checkbox's size (`--size-lead-mark: var(--size-checkbox)`, 13px): a text toggle is a checkbox in prose. The square fills pen blue like the checkbox, so decision 6 (checkbox colour) moves both together; the underline stays hairline on a pressed toggle and hover thickens it on an unpressed one only; under forced colours the square's fill is stripped and an outline stands in. Variant A stays on the sheet for the record; B is the default. D-21's "app-wide" wording narrows to every boxed control fills.

## A word in a box (Mark, 2026-09-14)
Mark's question after the winner: button, chip, segment option and stop are all "a word in a box" — too many overlaps of shape and meaning. The sheet gained a section (`#boxes`, before "Captions and type roles") that draws the control groups side by side in one grid: a column per group, a row per state (rest, live; hover; focus-visible; picked; picked + focus; forced colours, simulated). Nothing in it is new: every cell is the control as the rows above draw it, so the overlap can be judged shape against shape in the same state. The stops sit inside an `.axis` wrapper because their rules are scoped to it.

First cut drew the shapes isolated (one button, one chip, a bare stop track). The Impeccable critique below said that exaggerated the similarity, so the grid now compares complete groups.

## Impeccable critique, applied (2026-09-14)
The critique's headline: remove the outer box from the defect choices and use the leading-square vocabulary already chosen for Show changes, a blue square when selected, the whole label clickable and generously sized, still a compact wrapping row. Three priorities, all drawn:

1. **P2, make multiple selection visible.** The defect row is now square-and-word items in the checkbox's vocabulary (`.defects` / `.defect`): a 13px ink square before the word, no outer box, the whole word the target (a `<button aria-pressed>`, 32px tall at desktop and 44px below 760px), the square thickens on hover as the checkbox does and fills pen blue when marked; forced colours strip the fill and a 2px outline stands in. Bitter keeps the wider gap and "· declared" as a sibling helper, the two wrapped as one unit (`.declared-pair`) so a wrapping row never separates them. The boxed chips are kept as the row's last cell, "chips, before · boxed, for the record". Measured at 1188: five items on one line, 574px wide, 32px tall; Bitter 32px after Greasy film, the helper 6px after Bitter on the same centre. At a real 393: 44px items, the row wraps to 144px tall inside the frame.
2. **P2, compare complete groups.** The grid's columns are now the button, the full three-option segmented group, a realistic defect row (wrapping inside a 200px column, as it would in a narrow pen) and the axis with its name, inline state, Clear and anchors. Measured at 1188: the grid fits the frame's 1068px with no sideways scroll; at 393 it scrolls inside its own wrapper and the page stays at 393.
3. **P3, unify hover.** The border thickens on every box on hover — button, segment option, chip, stop — and on the defect's square; D-06's "no hover on options and chips" is drawn beside the segmented row as "hover · none, as D-06 had it, for comparison" and is otherwise retired on the sheet. (The measured fact stands: 1.5px renders as 1px at DPR 1 in Mark's Chrome, so decision-table row 2's 2px hover is still open.)

What the critique said already works and the sheet keeps: joined segments, ordered numbered stops, one focus ring, pen blue for retained selections. No new colour or corner shape per control. These are the critique's recommendations drawn for Mark's judgment, not decisions; D-06 and D-08 are affected if he takes them.

## Joined stops (Mark, 2026-09-14)
Mark, after the critique: try joined stops for the scale. Both the segmented control and the axis mean "choose one from this set", so they share one visual structure and the content carries the distinction — words at their own widths for a category; five equal numbered cells, the axis name above and soft / right / hard below, for a rating. Joining cuts border noise with three axes across. The system is then: joined single-choice controls, checkbox multi-choice controls (the square-and-word defects), standalone action buttons.

Drawn: the five stops share four hairlines (`margin-right: −1px`, as the segmented options do); the picked cell fills pen blue and sits on top (`z-index: 1`); hover thickens a cell's border and sits it on top as an option does; Clear stays a separate control so an unanswered rating is never read as the midpoint. The tracks keep their widths so 007's geometry does not move: 186px at desktop = five 38px cells sharing four hairlines (was 5 × 34px + 4 × 4px); 216px below 760px = five 44×44 cells (was 40×44 + 4px gaps). The separated stops are kept as the axis row's last cell, "stops, before · separated, for the record" (`.stops5.apart`, the old 34px / 40px sizes).

Measured in Mark's Chrome at 1188: track 186×32, cells 38px each, every seam −1px (one shared hairline), the anchors row 186px under it, the picked cell 38×32 pen blue at z 1, the hover cell's margin −1.5px at z 1, Clear present when picked; the separated record cell 186px with 34px stops. At a real 393 (gsd-browser): track 216×44, cells 44×44, seams −1px; the record cell 216px with 40×44 stops; the page stays at 393.

## One focus rule for every independent on/off (Mark, 2026-09-14)
Mark saw the defect's pressed + focus differ from the checkbox's checked + focus: the checkbox's ring sat on the 13px input, the defect's around square and word. Merged, for checkbox, defect and Show changes alike: selected fills the small square pen blue; focused draws the offset ring around the whole clickable control, square and word; selected + focused shows both; nothing encloses the control at rest, so keyboard focus and selection never read as each other and the full target is visible. The checkbox's ring now sits on the label (`.check:focus-within`, the input's own ring suppressed), and the label takes the control height (32px, 44px below 760px) as a defect does, so the three rings match.

Measured in Mark's Chrome at 1188 — rest / focus / selected + focus: checkbox no ring / 1.5px ring offset 2px on a 32px label / ring + blue square; defect the same on a 32px item; Show changes the same on its 24px text control. Live: tabbing into the checkbox draws the ring on the label, none on the input. At a real 393: checkbox and defect both 44px tall.

## Second critique, applied (2026-09-14, evening)
Mark's summary: 008 now has a coherent control language — joined single-choice groups, checkbox-style independent choices, standalone actions — and needs refinement, not a different identity. Four P2s, all drawn:

1. **Hover visibly works, nothing moves (harden).** 1.5px borders and rings drew as 1px at DPR 1, and the half-pixel padding compensation left the hovered button a pixel smaller. The sheet now uses a whole 2px for hover (`--rule-hover`) and for the focus ring (`--focus-outline-width` overridden locally), with a whole-pixel compensation. Joined controls no longer change their margins on hover; the hovered box thickens in place and stacks above its neighbours (z-index 2, the picked box at 1), so the seam thickens and the group holds. Measured in Mark's Chrome at 1188: button rest and hover both 101.58 × 34.25 with the text at the same offset; segmented group 312.56px at rest and on hover, seams −1px in both; axis track 186px in both, the hovered cell 38 × 32 with a 2px border; the focus ring 2px offset 2px. Underlines keep 1.5px on hover (they render).
2. **Focus, invalid and high contrast separated (harden).** The invalid ring is gone: the value stays, the sentence beneath states the fault, and the ring means focus only; the numeric row gained "invalid + focus". Measured: invalid outline none with the value "cold" and the sentence present; invalid + focus a 2px ring with the sentence. High contrast changed approach so selection can never share an outline with focus: inside `@media (forced-colors: active)` a picked box and an on square keep a fill in the system's Highlight colour through `forced-color-adjust: none`, the same shape as in normal colours. The simulated cells draw that fill in ink on paper, and every family gained "high contrast + focus" (fill plus the ring). Measured: simulated fills ink on the option, the cell, the defect's and checkbox's squares; the on + focus cells show the ring at 2px solid beside the fill; a defect's control carries no outline in high contrast. Real high-contrast rendering remains untested (007 line 141's outline stand-in is superseded here).
3. **The current rule unmistakable (clarify / distill).** The header states the rule in two short paragraphs; each family has a one-sentence rule with its history behind a disclosure; the A/B toolbar is gone; the touch block's captions are current ("Show changes, on · 44 min", "checkbox · 44 min"); the open questions are their own section (twelve items, the drawn value named first); "Earlier versions" holds the retired alternatives — variant A's two fills, the boxed chips, the separated stops, the no-hover option, the ringed invalid field — behind a closed disclosure. Measured: no caption or helper outside Earlier versions mentions a variant, a binder, D-06 or a missing touch rule.
4. **Reading rhythm (layout / typeset).** The family sections had no top margin or rule at all: the rule was written for `.sheet > section` and the rows render inside `#rows`, so it never matched them. Now `.sheet section`: 48px above, a hairline, 20px beneath, at both levels (measured on four consecutive families). Captions are 12px at a darker grey (#55554f, bold #2e2e2a). In the comparison the grid box takes its content width so the sticky state names have a containing block that does not scroll away; measured at a real 393 (gsd-browser): 1017px of grid inside 273px, scrolled 300px, the state name still at the wrapper's left edge on the paper's colour, the cue "scrolls sideways → the state names stay put" shown; at 1188 the grid fits and the cue is hidden.

Tokens and templates kept their names; the app-side mapping (`--rule-hover` → a new app token; the focus ring width) is for the phase plan.

## A joined group is a radio; Clear on both (Mark, 2026-09-14)
Mark asked whether the axis needs Clear when a second click on the picked cell already unpicks it. Answer: keep Clear and drop the toggle-off. A filled radio is expected to stay filled; a maker who clicks the pick again to confirm it would silently erase the rating, and a radio group gives keyboard and screen-reader users no key for "none". The segmented control had the same toggle-off and no Clear, so both joined groups now follow one rule: picking is final, Clear appears in the caption line once something is picked, and Clear is the only way back to not recorded. The segmented control gained a caption line (`.segc .head`, sharing the axis head's rule) carrying "Exit consistency" and its Clear.

Measured live in Mark's Chrome at 1188: picking option 2 shows Clear; clicking option 2 again changes nothing; picking option 3 moves the pick; Clear empties the group, hides itself and moves focus to the first option. The axis behaves the same (pick 3, click 3 again: still (3); Clear: (Not recorded), focus to stop 1). The caption line is 24px tall in both. At a real 393 the segmented control's picked cells show Clear and the page stays at 393.

## Clear must not move the page (Mark, 2026-09-14)
Mark saw the page shift down when Clear appeared on the axis and the segmented control. Measured: at full width nothing moved (the head reserved 24px and Clear is 24px); at the 393 preset and at a real 393 the head grew from 24px to 44px on pick because Clear takes the 44px touch height there, and everything below dropped 20px. Fixed: the caption line centres its contents instead of baseline-aligning them, and under the touch sizes it reserves 44px marked or not, so it is the same height with or without Clear. Measured after the fix, picking then clearing on both live groups: at 1188 the head stays 24px and the label, group, stops and the next family do not move; at the 393 preset and at a real 393 the head stays 44px and nothing below moves.

## Three answers, and Bitter caught up with 007 (Mark, 2026-09-14)
Confirmed: the segmented control's Clear matches the axis — in the caption line, once something is picked.

Bitter: the sheet still drew "· declared" beside Bitter, a suffix 007 retired on 2026-09-14 when the defects moved into two captioned groups, "Every recipe" and "This recipe only" (007 README, ninth round). The sheet now draws the defects that way: two `role="group"` blocks side by side, each with its `.lbl` caption, Bitter alone under "This recipe only", so a second declared flaw lands there with no new design. The "· declared" text survives only on the boxed chips under Earlier versions, as the record of what they were.

Open questions settled, all as drawn:
1. **Fill colour.** Pen blue for every on state. The rule set is: blue fill = this choice is on; outer ring = keyboard focus; shape and grouping = how the choice behaves; label and place = what it affects. "Show changes" already says it controls the view; "Skipped" and the defect words describe observations. A blue-versus-ink split would ask users to learn why identical controls fill differently; reserve it for a demonstrated confusion.
2. **Caption tracking.** 0.04em on labels and region names alike; the alternatives differ by a tenth of a pixel per letter, too little to carry hierarchy. Size, weight, colour and the spacing between sections distinguish region names from field labels. Sentence-case helpers and control text keep normal tracking. 007 moves from 0.025em / 0.035em to 0.04em.
3. **Region-name gap.** 6px beneath the region name; the larger gap above it separates it from the previous section. A helper after the heading keeps the 6px and takes its own gap before the controls. 007 moves from 12px to 6px.

## The last nine, settled (Mark, 2026-09-14)
All in favour of the sheet as drawn, with one change: Show changes moves to the 32px control height (44px below 760px), the same target as the checkbox and a defect, so the three whole-control focus rings match. The rule of thumb Mark gave: small control text, comfortably sized interaction targets, larger text for entering values and observations.

Settled: measured-field widths through named tokens (128px date; numeric widened, below); text controls 0.8125rem; segment options and defects 0.8125rem, comfort from target height and padding, not larger type; prose-field baseline a hairline with no thickening on focus, the ring is the signal; anchor words in the text face, italic, short and consistent across axes; fields grow to 1rem at 600px and below; status line at 65ch; joined cells keep the 186px and 216px tracks (38px and 44px cells). The sheet's "Open questions" section is now "Settled values", twelve lines, the implementer's list.

Mark asked that the field widths be verified. Measured in Mark's Chrome at 1188 (15px field text) and at a real 393 (16px): the date field at 128px holds "2026-08-02" with the picker icon (scrollWidth = clientWidth); the numeric field at 48px holds "−6" but clips "−12.5" and "120.5" (scroll 50 and 53 against a 46px client); at 56px both fit at both sizes (54 / 54); "−120.5" would need about 64px and no field here expects it. `--field-w-figure` is now 56px; the numeric row's history records the change. Measured after: Show changes 32px at 1188 and 44px at 393.

## Axis names at the caption's weight (Mark, 2026-09-14)
"Churn date" and "Hardness" had different weights, the axis name at 600. Both label an input, so the axis name now takes the caption's 500; both stay 0.75rem, uppercase, 0.04em. 600 is reserved for region names ("Tasting", "Batch"). This quiets the three-column axes and clears the hierarchy: region name 600, every input caption 500. 007 line 153's axis-name weight moves with it.

## High contrast, checked in Chrome's emulation (Mark, 2026-09-14)
Mark ran DevTools' `forced-colors: active` emulation on the sheet. The Highlight fill on picked cells and on squares is fine. Picked + focus on the segmented control lost its ring: `forced-color-adjust: none` keeps the fill, but it also keeps the ring's authored ink colour, which disappears against the forced canvas. Fixed inside the forced-colours rule: a picked option's or cell's focus ring takes `CanvasText`. Unpicked controls, the checkbox label, defects and Show changes keep their rings because the ring sits on an element the browser still adjusts. To be re-checked in the same emulation.

## Mark's recording, frame by frame (2026-09-14, night)
Mark recorded the live segmented control under Chrome's forced-colours emulation: tab in, pick Smooth ribbon, tab away, tab back, Clear. The frames (extracted with AVFoundation, 0.4s apart) show two things.

1. **The picked cell's ring was drawn in the authored ink**, a faint dark box on the black canvas — the fault of the previous round, not the CanvasText fix. The tab Claude drives had the fix loaded (`?r=18`) and the forced-colours rule present; the recording most likely came from a tab holding the earlier file (the sketch server sends no cache headers). A hard reload (Cmd+Shift+R) with the emulation on is the re-check.
2. **After a mouse Clear, focus jumped to Smooth ribbon without a ring, so the next Tab looked like it skipped it.** Chrome does not show `:focus-visible` on an element focused by script right after a pointer click. Now Clear moves focus only when it was activated by keyboard (`event.detail === 0`); a mouse Clear leaves the browser's sequential-focus starting point where Clear was, so the next Tab reaches the first cell with its ring. Verified with real clicks and keys in Mark's Chrome: mouse Clear → focus on the document → Tab → Smooth ribbon, `:focus-visible` true, 2px ring; Shift+Tab from a picked Smooth ribbon → Clear; Enter → the group empties, Clear hides, focus on Smooth ribbon with the ring.
