# Sketch Manifest

## Design Direction
The Formulation Cookbook as recorded in DESIGN.md: cool text paper, print ink for everything the system prints, one pen blue for everything recorded, one bookcloth green that names regions and never carries state; a text face for prose and a grotesk with tabular numerals for anything counted; every control drawn in ink at hairline weight with no fill and no radius. Sketches here test structure inside that world, not the world itself.

## Reference Points
The book spread on `route:/recipe` as built through GSD phase 03.1; the right-edge slideouts in `design-explorations/` (recipe settings, scale, optimize), which covered the page with a scrim and are the anti-reference for "push, never cover"; the 2026-09-09 critique in `.impeccable/critique/`.

## Sketches

| # | Name | Design Question | Winner | Tags |
|---|------|----------------|--------|------|
| 001 | pushed-tray | Can a pushed right-edge tray hold versions, batches and ceremonies at 1440 without breaking the table or hiding the formulation note? | C: tray in column two, no third column; the tray itself superseded by 003 | layout, shell, tray |
| 002 | tray-at-top | With the tray at the top of column two, where does the batch record live: margin, tray, or both? | none: A–C kept the tray, D crammed the record into column two; superseded by 003 | layout, shell, tray, batches, tablet |
| 003 | front-matter-rows | Does the front matter read as two stacked rows in column one, version then batch, with lists and the ceremony under their row? | B: rows across the whole page, table in step order, no tray | layout, shell, front-matter |
| 004 | tiered-tastings | With a batch capped at one primary tasting plus up to 3 light "taste again" entries, how does the tiering read without color or icons? | superseded 2026-09-11: Mark dropped extra tasting records entirely (they were for shelf-life/stability testing, not needed) | batches, tastings, front-matter |
| 005 | folded-tasting-pen | With the primary tasting folded into the same recording pen as the churn fields (one Save, no separate ceremony), does the pen stay legible or does it recreate the earlier "260 controls" overload? | pending | batches, tastings, pen, recording |
| 006 | light-tasting-entry | How compact can "Taste again" be, given the axis marks — not the fields 004 already trimmed — are the real source of length? | superseded 2026-09-11: no more light tastings, so no light-tasting entry point | batches, tastings, pen, recording |
| 007 | full-battery | With the expanded criteria (goldilocks axes, new churn/tasting fields, defects checklist) from the 2026-09-10 research pass, where does the defects checklist live, and how long does the full uncollapsed record run? | pending | batches, tastings, pen, recording, dimensions |
| 008 | control-sheet | Drawn from the amended DESIGN.md, does one sheet of every control in every state give one place to tweak the control language before it is applied app-wide — and does fill hold for inline text toggles (A) or only boxed controls (B)? | Settled 2026-09-14: joined single-choice groups (segmented, five-cell axis) with Clear in the caption line; square-and-word independent on/offs (checkbox, defects, Show changes); a lone box or underlined word is an action; 2px hover and focus ring, nothing moves; Highlight fill in high contrast; twelve values settled on the sheet | controls, states, design-system, tokens |
| 009 | wide-touch | What should a rating stop be at a viewport that is wide and touch at the same time (1366, pointer coarse)? | Settled 2026-09-15: axes C, the stop grows in height only (38 × 44, no geometry moves); Melt row M4, equal caption-to-control gap with Clear's 44px target from an overflowing hit area; A, B, D and M1–M3 kept as the record of why | touch, tablet, battery, axes, controls |
| 011 | recipe-route-c | Where do recipe context, the Sheet and the batch log sit, from desktop to phone? | Settled 2026-09-24: layout C, recipe and version as App front matter across the top with the History rail beneath; the log beside the Sheet at 1600 and 1366, below it at 1024 and 393; the pen edits one step at a time; acceptance target for Phase 03.5 | layout, shell, recipe-route, ladder, pen, history |
