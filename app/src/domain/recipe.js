// Stub — RED phase (03.5-04 Task 1). Real implementation follows in GREEN.
export const RECIPE_NAME_REQUIRED = 'Enter a recipe name.';
export const RECIPE_SAVE_ERROR = 'Couldn’t save the name. Try again.';

export function recipeNameMessage() {
  return null;
}

export function renamedRecipe(recipe) {
  return recipe;
}
